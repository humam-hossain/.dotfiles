---
phase: "34"
plan: "02"
title: "Author Phase 34 Test Harness & Execute End-to-End Verification"
type: "execute"
wave: 2
depends_on: ["34-01"]
files_modified:
  - scripts/phase34-verification-assert.sh
autonomous: true
requirements: [INTG-01, INTG-02, INTG-03]

must_haves:
  truths:
    - "scripts/phase34-verification-assert.sh exists at mode 0755 implementing fail-closed 5-section architecture with CLI flag filtering (--section <1-5>) (D-13, D-15)"
    - "Section 1 verifies 11/11 overlay symlinks, bootstrap target directory pre-creation, dynamic colors.json contract, and zero unauthorized hex codes (D-03, D-05, D-11, INTG-01, INTG-02)"
    - "Section 2 executes dual theming drill (native switchwall.sh --noswitch and color seed switchwall.sh --color '#3f51b5') verifying monotonic mtime advance, quickshell process health, and byte-identical zero git churn (D-01, D-02, D-04, D-12, INTG-01)"
    - "Section 3 enforces strict repository verification via arch/dots-hyprland.sh verify --strict with FAIL=0 and FINDINGS=0 (D-09, D-10, INTG-02)"
    - "Section 4 executes isolated bootstrap destub, stow leaf linking, and color priming drill in /tmp/p34-assert-s4-XXXXXX without mutating host files (D-06, D-07, D-08, INTG-03)"
    - "Section 5 executes full milestone v0.6 regression sweep (phase31, phase32, phase33 assert scripts) requiring each to pass with FAIL=0 (D-14)"
    - "Full test harness execution exits 0 with FAIL=0 and FINDINGS=0, maintaining 100% byte-identical git porcelain cleanliness before and after runs (D-12, D-16)"
  artifacts:
    - scripts/phase34-verification-assert.sh
  key_links:
    - "scripts/phase34-verification-assert.sh is the authoritative end-of-milestone test suite validating INTG-01, INTG-02, and INTG-03 across Sections 1 to 5"
    - "arch/dots-hyprland.sh verify --strict is the core repository symlink and capture integrity gate"
    - "bootstrap.sh is the root deployment orchestrator"
  prohibitions:
    - "Must not touch or mutate the operator's real desktop files during bootstrap scratch drills (Section 4 must run in /tmp/)"
    - "Must not leave temporary files or scratch directories on disk after execution (clean up via EXIT trap)"
    - "Must not allow switchwall.sh theme drills to introduce git working tree churn (assert byte-identical porcelain state)"
    - "Must not run test suites or dotfiles operations as root (EUID == 0)"
---

<objective>
Author the comprehensive 5-section automated verification harness (`scripts/phase34-verification-assert.sh`), execute the full suite covering dynamic Material You theming, repository strict verification, isolated bootstrap deployment, and the complete milestone v0.6 regression sweep, proving zero git drift and total desktop shell integrity.

Purpose: Deliver the culminating verification engine for milestone v0.6, validating requirements INTG-01, INTG-02, and INTG-03 with 100% pass rate.
Output: Executable test harness `scripts/phase34-verification-assert.sh` passing all 5 sections with `FAIL=0 FINDINGS=0` and zero working tree churn.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-verification-zero-drift-bootstrap-integration/34-CONTEXT.md
@.planning/phases/34-verification-zero-drift-bootstrap-integration/34-RESEARCH.md
@.planning/phases/34-verification-zero-drift-bootstrap-integration/34-PATTERNS.md
@.planning/phases/34-verification-zero-drift-bootstrap-integration/34-VALIDATION.md
@scripts/phase29-theme-data-contracts-assert.sh
@scripts/phase31-overlay-pill-assert.sh
@scripts/phase32-component-formatting-assert.sh
@scripts/phase33-layout-assert.sh
@bootstrap.sh
@arch/dots-hyprland.sh
</context>

<assumptions>
- Plan 34-01 has committed baseline modifications in `capture/` and `stow/`, hardened `bootstrap.sh`, and aligned `scripts/phase31-overlay-pill-assert.sh`.
- Upstream dots-hyprland wallpaper switcher (`~/.config/quickshell/ii/scripts/colors/switchwall.sh`) is executable and can run in non-interactive dark mode (`prefer-dark`).
- The live Quickshell process (`qs -c ii`) is running in the operator's active desktop session and will hot-reload colors dynamically without dying.
- Scratch directories created under `/tmp/` are fully isolated and automatically cleaned up on `EXIT`, `SIGINT`, or `SIGTERM`.
</assumptions>

<specless_probe_resolutions>
- INTG-01 (adjacency probe): In Section 2, consecutive executions of `switchwall.sh` include `sleep 1` delays, ensuring second-resolution filesystem mtimes on `colors.json` advance monotonically (`B_MTIME < A_MTIME < C_MTIME`) and separate distinctly rather than colliding.
- INTG-01 (empty probe): Section 1 validates `colors.json` via `[[ -s "$COLORS_JSON" ]] && jq empty "$COLORS_JSON"`, failing closed on empty or null files; Section 2 validates that both native and seed fallback theme generation produce non-empty valid JSON.
- INTG-01 (ordering probe): Section 2 compares porcelain snapshots `$DRILL_BEFORE` and `$DRILL_AFTER` via `cmp -s`, ensuring output order is stable, deterministic, and 100% byte-identical.
- INTG-02 (unclassified probe): Section 1 asserts all 11 live overlay symlinks resolve 1:1 to `restow/quickshell/`; Section 3 verifies zero uncommitted changes in tracked packaging trees (`stow/`, `restow/`, `capture/`) and asserts `arch/dots-hyprland.sh verify --strict` exits 0 with `FAIL=0 FINDINGS=0` with `.bak` installer artifacts treated as non-blocking `[INFO]`.
- INTG-03 (unclassified probe): Section 4 runs an isolated scratch drill in `/tmp/p34-assert-s4-XXXXXX`, validating that `run_destub` discovers stubs and archives them with SHA-256 manifests into `~/.dotfiles-backup.<epoch>/`, `run_stow_step` pre-creates directories and creates leaf symlinks without whole-directory folding, and `generate_initial_theme` asserts primed `colors.json`.
</specless_probe_resolutions>

<tasks>

<task id="34-02-01" type="tracer">
  <name>Author scripts/phase34-verification-assert.sh test harness across Sections 1 to 5 (INTG-01, INTG-02, INTG-03, D-01..D-16)</name>
  <read_first>
    scripts/phase29-theme-data-contracts-assert.sh
    scripts/phase33-layout-assert.sh
    bootstrap.sh
    .planning/phases/34-verification-zero-drift-bootstrap-integration/34-CONTEXT.md
    .planning/phases/34-verification-zero-drift-bootstrap-integration/34-PATTERNS.md
    .planning/phases/34-verification-zero-drift-bootstrap-integration/34-RESEARCH.md
  </read_first>
  <files>
    scripts/phase34-verification-assert.sh
  </files>
  <action>
    Author `scripts/phase34-verification-assert.sh` with permissions 0755 adhering strictly to repository test harness conventions and D-01 through D-16:
    1. Base scaffolding: Use `#!/usr/bin/env bash`, `set -euo pipefail`. Resolve `REPO_ROOT` and `ASSERT_SELF`. Export default `XDG_CONFIG_HOME` and `XDG_STATE_HOME`. Guard with non-root check `[[ "${EUID:-$(id -u)}" -ne 0 ]] || { echo "Error: Do not run as root" >&2; exit 1; }`. Initialize `FAIL=0`, `FINDINGS=0`, and logging helpers `pass()`, `fail()`, `finding()`, `info()`.
    2. Trap cleanup: Register arrays `TMP_FILES=()` and `SCRATCH_ROOTS=()`. Implement `cleanup()` removing temporary files with `rm -f` and recursively deleting scratch roots with `rm -rf`, registering `trap cleanup EXIT`.
    3. CLI parsing: Support `--section <1-5>`, `-s <1-5>`, `-h`, `--help` with fail-closed exit 1 on invalid arguments (D-15). Default to running all sections (`RUN_SECTION=0`).
    4. Two-phase porcelain snapshotting: Implement `porcelain_snapshot_raw` and `porcelain_snapshot` filtering ephemeral noise (`.commandcode/`, `scripts/__pycache__/`), writing initial state to `$PORCELAIN_BEFORE` (D-12).
    5. Section 1 (Contracts, 11/11 Symlinks & Pre-Creation Checks):
       - Assert all 11 live overlay files in `~/.config/quickshell/` resolve 1:1 to `$REPO_ROOT/restow/quickshell/` via `readlink -f` (Updates.qml, Privacy.qml, record.sh, system-update.sh, BarContent.qml, SysTray.qml, UpdatesButton.qml, BarGroup.qml, Resources.qml, Resource.qml, ClockWidget.qml) (D-11).
       - Assert `bootstrap.sh` pre-creates sensitive Quickshell target directories: `.config/quickshell/ii/modules/ii/bar`, `.config/quickshell/ii/services`, `.config/quickshell/ii/scripts/videos` (D-05).
       - Assert dynamic `colors.json` token contract defines core tokens: `background`, `surface_container_low`, `on_surface_variant`, `secondary_container`, `primary`, `error` (D-03). Fail closed if file is missing, empty, or invalid JSON.
       - Assert zero unauthorized hardcoded hex codes in `restow/quickshell/` (`grep -rnE '#[0-9a-fA-F]{3,8}' | grep -v '#FFA000'`), allowing only `#FFA000` Amber warning color (D-03).
    6. Section 2 (Material You Dynamic Theming Drill & Zero Git Churn):
       - Preserve live shell config (`~/.config/illogical-impulse/config.json`) to prevent switchwall `--color` JSON reformats from inducing capture drift.
       - Snapshot git porcelain state before drill into `$DRILL_BEFORE`.
       - Record initial mtime of `colors.json` and `sleep 1` to separate timestamps (resolving adjacency probe).
       - Drill A: Execute `switchwall.sh --noswitch` against active wallpaper. Assert exit 0, monotonic mtime advancement (`A_MTIME > B_MTIME`), and quickshell process alive (`pgrep -x qs || pgrep -x quickshell`) (D-01, D-04).
       - `sleep 1`.
       - Drill B: Execute synthetic color seed fallback `switchwall.sh --color "#3f51b5"`. Assert exit 0, monotonic mtime advancement (`C_MTIME > A_MTIME`), valid JSON syntax (`jq empty`), and quickshell process alive (D-01, D-04).
       - Restoration: Restore saved shell config and re-execute `switchwall.sh --noswitch` to return to operator wallpaper palette. Assert quickshell process alive (D-04).
       - Zero git churn: Capture `$DRILL_AFTER`, compare with `$DRILL_BEFORE` (`cmp -s`), and assert 100% byte-identical clean diff (D-12).
    7. Section 3 (Repository Strict Verification Gate):
       - Assert tracked packaging trees (`stow/`, `restow/`, `capture/`) have zero uncommitted modifications in git status.
       - Execute `$REPO_ROOT/arch/dots-hyprland.sh verify --strict`. Assert exit 0 and 0 findings (`FINDINGS=0`) (INTG-02).
       - Retain existing `.bak` installer backup files in `~/.config/quickshell/` as non-blocking `[INFO]` entries (D-10).
    8. Section 4 (Isolated Bootstrap Destub & Stow Scratch Drill):
       - Create isolated temporary sandbox under `/tmp/p34-assert-s4-XXXXXX` registered in `SCRATCH_ROOTS` (D-07).
       - Populate `$MOCK_REPO` mirroring `restow/quickshell` and `$MOCK_HOME` with upstream regular file stubs.
       - Source `bootstrap.sh` in the subshell.
       - Test `run_destub`: assert discovered stubs are unlinked and archived into `.dotfiles-backup.<epoch>/` with SHA-256 manifest (D-06).
       - Test `run_stow_step`: assert target directories are pre-created (`mkdir -p`) and leaf symlinks created without folding whole directories into symlinks (D-05).
       - Test `generate_initial_theme`: assert primed `colors.json` verification gate (D-08).
    9. Section 5 (Full Milestone v0.6 Regression Sweep):
       - Execute `scripts/phase31-overlay-pill-assert.sh`, `scripts/phase32-component-formatting-assert.sh`, and `scripts/phase33-layout-assert.sh` in sequence, asserting each exits 0 with `FAIL=0` (D-14).
    10. Closing porcelain check & summary: Capture `$PORCELAIN_AFTER`, compare with `$PORCELAIN_BEFORE` (`cmp -s`), print `=== done: FAIL=$FAIL FINDINGS=$FINDINGS ===`, and exit 1 if FAIL > 0, else exit 0 (D-16).
  </action>
  <acceptance_criteria>
    test -x scripts/phase34-verification-assert.sh exits 0.
    bash scripts/phase34-verification-assert.sh --help exits 0 and displays --section <1-5> usage.
    bash scripts/phase34-verification-assert.sh --section 1 exits 0 with FAIL=0.
    bash scripts/phase34-verification-assert.sh --section 3 exits 0 with FAIL=0 FINDINGS=0.
    bash scripts/phase34-verification-assert.sh --section 4 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>test -x scripts/phase34-verification-assert.sh && bash scripts/phase34-verification-assert.sh --help | grep -q -- '--section <1-5>' && bash scripts/phase34-verification-assert.sh --section 1</automated>
    <fails_when>non-zero exit code, file not executable, or Section 1 fails</fails_when>
  </verify>
  <done>scripts/phase34-verification-assert.sh exists at mode 0755 with all 5 sections implemented, verified syntactically, and Section 1 passing with FAIL=0.</done>
</task>

<task id="34-02-02" type="execute">
  <name>Execute full 5-section verification suite and confirm zero working tree drift (INTG-01, INTG-02, INTG-03, D-12..D-16)</name>
  <read_first>
    scripts/phase34-verification-assert.sh
    arch/dots-hyprland.sh
    .planning/phases/34-verification-zero-drift-bootstrap-integration/34-VALIDATION.md
    .planning/phases/34-verification-zero-drift-bootstrap-integration/34-RESEARCH.md
  </read_first>
  <files>
    scripts/phase34-verification-assert.sh
  </files>
  <action>
    Execute the complete Phase 34 verification suite end-to-end and confirm zero working tree drift:
    1. Run `bash scripts/phase34-verification-assert.sh` without arguments to execute all 5 sections in sequence:
       - Section 1: Data Contracts, 11/11 Symlinks & Pre-Creation Checks
       - Section 2: Material You Dynamic Theming Drill & Zero Git Churn
       - Section 3: Repository Strict Verification Gate
       - Section 4: Isolated Bootstrap Destub & Stow Scratch Drill
       - Section 5: Full Milestone v0.6 Regression Sweep (Phase 31, 32, 33)
    2. Confirm the harness reports `=== done: FAIL=0 FINDINGS=0 ===` and exits 0.
    3. Run an independent verification with `./arch/dots-hyprland.sh verify --strict` to verify exit code 0 and `FINDINGS=0`.
    4. Assert that `git status --porcelain` shows zero unexpected modifications (strictly clean working tree for tracked files).
  </action>
  <acceptance_criteria>
    bash scripts/phase34-verification-assert.sh exits 0 with FAIL=0 and FINDINGS=0.
    ./arch/dots-hyprland.sh verify --strict exits 0 with FINDINGS=0.
    git status --porcelain capture stow restow returns empty output.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase34-verification-assert.sh && ./arch/dots-hyprland.sh verify --strict | grep -q 'FINDINGS=0'</automated>
    <fails_when>non-zero exit code, any FAIL reported in harness, or strict verify findings > 0</fails_when>
  </verify>
  <done>The complete Phase 34 verification suite passed across all 5 sections with FAIL=0 FINDINGS=0, and the git working tree remains 100% clean with zero drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Assertion Script → Active Desktop Session | Theming drill triggers wallpaper and color reloads in live desktop session without disrupting active user apps |
| Test Assert Harness → Git Working Tree | Assertion script performing theming drills and inspections must leave git working tree 100% byte-identical |
| Scratch Sandbox → Host Environment | Section 4 bootstrap drill runs within an isolated disposable `/tmp` sandbox, never touching live user files |
| User Privileges → System Files | All dotfiles operations and test runs execute strictly under unprivileged user account (EUID != 0) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-05 | Tampering / Working Tree Churn | switchwall.sh theming drill | high | mitigate | Preserve live shell config (`SAVED_SHELL_CONFIG`), snapshot git status before and after drill (`DRILL_BEFORE`/`DRILL_AFTER`), and restore wallpaper state immediately after seed testing (D-12). |
| T-34-06 | Denial of Service / Desktop Crashes | quickshell process during theming | high | mitigate | Assert `pgrep -x qs || pgrep -x quickshell` remains alive after both native and color seed drills without crashing (D-04). |
| T-34-07 | Tampering / Destructive Live Overwrite | bootstrap destub/stow drill | high | mitigate | Restrict Section 4 drill entirely to `/tmp/p34-assert-s4-XXXXXX` using mock HOME and mock REPO directories, cleaning up via EXIT trap (D-07). |
| T-34-08 | Elevation of Privilege / Root Execution | scripts/phase34-verification-assert.sh | medium | mitigate | Non-root execution guard `[[ "${EUID:-$(id -u)}" -ne 0 ]]` prevents accidental root pollution. |
</threat_model>

<verification>
Automated verification commands:
1. `test -x scripts/phase34-verification-assert.sh && bash scripts/phase34-verification-assert.sh --help | grep -q -- '--section <1-5>' && bash scripts/phase34-verification-assert.sh --section 1`
2. `bash scripts/phase34-verification-assert.sh && ./arch/dots-hyprland.sh verify --strict | grep -q 'FINDINGS=0'`
</verification>

<success_criteria>
1. `scripts/phase34-verification-assert.sh` exists at mode 0755 and supports `--section <1-5>`.
2. All 5 sections execute and pass with `FAIL=0` and `FINDINGS=0`.
3. Strict repository verification (`arch/dots-hyprland.sh verify --strict`) exits 0 with 0 findings.
4. Git working tree remains completely clean before and after test execution.
</success_criteria>

## Artifacts this phase produces
- Test assertion harness: `scripts/phase34-verification-assert.sh`
- CLI Flags: `--section <1-5>`, `-s <1-5>`, `-h`, `--help`

<output>
Create `.planning/phases/34-verification-zero-drift-bootstrap-integration/34-02-SUMMARY.md` when done
</output>
