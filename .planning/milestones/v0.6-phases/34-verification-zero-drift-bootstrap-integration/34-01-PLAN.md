---
phase: "34"
plan: "01"
title: "Baseline Commit, Bootstrap Hardening & Regression Alignment"
type: "tracer"
wave: 1
depends_on: []
files_modified:
  - capture/ii/.config/illogical-impulse/config.json
  - stow/hypr/.config/hypr/custom/general.lua
  - bootstrap.sh
  - scripts/phase31-overlay-pill-assert.sh
autonomous: true
requirements: [INTG-01, INTG-02, INTG-03]

must_haves:
  truths:
    - "capture/ii/.config/illogical-impulse/config.json is committed setting showPerformanceProfileToggle: false and matches live config byte-identically (D-09, INTG-02)"
    - "stow/hypr/.config/hypr/custom/general.lua is committed setting refined layout geometry and fluid easeOutQuint animations with zero working tree drift (D-09, INTG-02)"
    - "bootstrap.sh run_stow_step pre-creates sensitive Quickshell overlay directories (.config/quickshell/ii/modules/ii/bar, services, scripts/videos) preventing GNU Stow whole-directory folding (D-05, INTG-03)"
    - "bootstrap.sh generate_initial_theme asserts that colors.json is generated and non-empty before step completion, guaranteeing primed color state (D-08, INTG-03)"
    - "bootstrap.sh probe_session_environment dynamically resolves the newest Hyprland socket signature in /run/user/$(id -u)/hypr/ for resilient probing"
    - "scripts/phase31-overlay-pill-assert.sh Section 3 recognizes borderless container background toggling in BarGroup.qml aligned with Phase 33 layout reorganization (D-14, INTG-01)"
    - "Full regression baseline (phase31, phase32, phase33) passes with FAIL=0 across all assertions (D-14)"
  artifacts:
    - capture/ii/.config/illogical-impulse/config.json
    - stow/hypr/.config/hypr/custom/general.lua
    - bootstrap.sh
    - scripts/phase31-overlay-pill-assert.sh
  key_links:
    - "bootstrap.sh is the root installer orchestrator managing symlink deployment and theme initialization"
    - "scripts/phase31-overlay-pill-assert.sh gates Phase 31 pill foundation invariants"
    - "arch/dots-hyprland.sh verify --strict is the strict repository integrity gate"
  prohibitions:
    - "Must not leave unstaged modifications in capture/, stow/, or restow/ packaging trees"
    - "Must not allow GNU Stow to create folded whole-directory symlinks for Quickshell components"
    - "Must not run test suites or dotfiles operations as root (EUID == 0)"
---

<objective>
Establish zero working tree drift across tracked repository packaging trees (stow/, capture/, restow/), harden `bootstrap.sh` with sensitive directory pre-creation and verified color priming, and align `scripts/phase31-overlay-pill-assert.sh` with the Phase 33 layout reorganization so the complete milestone v0.6 regression baseline passes with `FAIL=0`.

Purpose: Prepare a completely pristine repository state and hardened bootstrap foundation gating Phase 34's automated end-to-end verification suite.
Output: Clean git status across `capture/` and `stow/`, hardened `bootstrap.sh`, aligned `scripts/phase31-overlay-pill-assert.sh`, and passing regression baseline (Phases 31–33).
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-verification-zero-drift-bootstrap-integration/34-CONTEXT.md
@.planning/phases/34-verification-zero-drift-bootstrap-integration/34-RESEARCH.md
@.planning/phases/34-verification-zero-drift-bootstrap-integration/34-PATTERNS.md
@.planning/phases/34-verification-zero-drift-bootstrap-integration/34-VALIDATION.md
@bootstrap.sh
@scripts/phase31-overlay-pill-assert.sh
@scripts/phase32-component-formatting-assert.sh
@scripts/phase33-layout-assert.sh
</context>

<assumptions>
- `capture/ii/.config/illogical-impulse/config.json` and `stow/hypr/.config/hypr/custom/general.lua` contain audited personal refinements that are ready to commit per D-09.
- Live `~/.config/illogical-impulse/config.json` must be kept byte-identical to `capture/ii/.../config.json` to avoid capture content drift findings in `arch/dots-hyprland.sh verify --strict`.
- Upstream dots-hyprland submodule `vendor/dots-hyprland` remains 100% clean and pristine.
- Non-root execution (`[[ "${EUID:-$(id -u)}" -ne 0 ]]`) protects system files from unintended modification.
</assumptions>

<specless_probe_resolutions>
- INTG-01 (empty probe): In `bootstrap.sh` `generate_initial_theme` (Step 6), missing, 0-byte, or null `colors.json` output triggers fail-closed return 1 (`[[ ! -s "$generated_colors" ]]`), preventing unprimed theme progression.
- INTG-02 (unclassified probe): Committed baseline files eliminate working tree churn; `~/.config/illogical-impulse/config.json` byte-parity satisfies `cmp -s` in `verify --strict` with zero findings.
- INTG-03 (unclassified probe): Explicit directory pre-creation in `bootstrap.sh` `run_stow_step` prevents GNU Stow directory folding on fresh installations, guaranteeing leaf symlinks.
</specless_probe_resolutions>

<tasks>

<task id="34-01-01" type="tracer">
  <name>Commit baseline working tree refinements in capture and stow trees (INTG-02, D-09)</name>
  <read_first>
    capture/ii/.config/illogical-impulse/config.json
    stow/hypr/.config/hypr/custom/general.lua
    .planning/phases/34-verification-zero-drift-bootstrap-integration/34-CONTEXT.md
    .planning/phases/34-verification-zero-drift-bootstrap-integration/34-PATTERNS.md
    .planning/phases/34-verification-zero-drift-bootstrap-integration/34-RESEARCH.md
  </read_first>
  <files>
    capture/ii/.config/illogical-impulse/config.json
    stow/hypr/.config/hypr/custom/general.lua
  </files>
  <action>
    Establish zero working tree drift across tracked repository packaging trees adhering to D-09 and INTG-02:
    1. Ensure live `~/.config/illogical-impulse/config.json` is byte-identical to `capture/ii/.config/illogical-impulse/config.json` by copying the repository capture file to `~/.config/illogical-impulse/config.json` (`cp capture/ii/.config/illogical-impulse/config.json ~/.config/illogical-impulse/config.json`).
    2. Validate `capture/ii/.config/illogical-impulse/config.json` syntax via `jq empty` and assert that `showPerformanceProfileToggle` is explicitly set to `false`.
    3. Validate `stow/hypr/.config/hypr/custom/general.lua` syntax and assert it contains personal layout geometry (`gaps_in = 0`, `gaps_out = 0`, `border_size = 2`, `rounding = 5`) and fluid `easeOutQuint` animation curves (speed 3.5 to 5.0).
    4. Stage both modified files: `git add capture/ii/.config/illogical-impulse/config.json stow/hypr/.config/hypr/custom/general.lua`.
    5. Commit the staged changes with commit message: `fix(v0.6): establish baseline capture config and hypr custom general refinements (D-09)`.
    6. Verify `git status --porcelain capture/ stow/` returns 0 lines and `./arch/dots-hyprland.sh verify --strict` confirms zero findings (`FINDINGS=0`).
  </action>
  <acceptance_criteria>
    git status --porcelain capture stow returns empty output (0 lines).
    jq empty capture/ii/.config/illogical-impulse/config.json exits 0.
    grep -q '"showPerformanceProfileToggle": false' capture/ii/.config/illogical-impulse/config.json exits 0.
    grep -q 'easeOutQuint' stow/hypr/.config/hypr/custom/general.lua exits 0.
    ./arch/dots-hyprland.sh verify --strict exits 0 with FINDINGS=0.
  </acceptance_criteria>
  <verify>
    <automated>git status --porcelain capture stow | wc -l | grep -q '^0$' && ./arch/dots-hyprland.sh verify --strict | grep -q 'FINDINGS=0'</automated>
    <fails_when>non-zero exit code, uncommitted changes in capture/stow, or strict verify findings > 0</fails_when>
  </verify>
  <done>capture and stow trees are committed and 100% clean, live config is byte-identical, and arch/dots-hyprland.sh verify --strict passes with 0 findings.</done>
</task>

<task id="34-01-02" type="execute">
  <name>Harden bootstrap.sh with sensitive directory pre-creation, verified color priming, and dynamic socket probing (INTG-03, D-05, D-08)</name>
  <read_first>
    bootstrap.sh
    .planning/phases/34-verification-zero-drift-bootstrap-integration/34-CONTEXT.md
    .planning/phases/34-verification-zero-drift-bootstrap-integration/34-PATTERNS.md
    .planning/phases/34-verification-zero-drift-bootstrap-integration/34-RESEARCH.md
  </read_first>
  <files>
    bootstrap.sh
  </files>
  <action>
    Harden `bootstrap.sh` to prevent directory folding on fresh machines and ensure colors are primed before session start:
    1. In `bootstrap.sh` function `run_stow_step` (around line 518), expand the `mkdir -p` pre-creation list to include sensitive Quickshell overlay target directories:
       - `"$target/.config/quickshell/ii/modules/ii/bar"`
       - `"$target/.config/quickshell/ii/services"`
       - `"$target/.config/quickshell/ii/scripts/videos"`
       This guarantees GNU Stow creates leaf symlinks to individual files without folding whole directories (D-05).
    2. In `bootstrap.sh` function `generate_initial_theme` (around line 665), append an assertion checking that `~/.local/state/quickshell/user/generated/colors.json` exists and is non-empty (`[[ ! -s "$generated_colors" ]]` emits an error and returns 1) before proceeding, ensuring primed Material You palette state before relogin (D-08).
    3. In `bootstrap.sh` function `probe_session_environment` (around line 701), make Hyprland socket detection resilient: if `command -v hyprctl` succeeds but `hyprctl -j status` fails, resolve the newest socket directory in `/run/user/$(id -u)/hypr/` via `ls -td "/run/user/$(id -u)/hypr/"* 2>/dev/null | head -1 | xargs -r basename` and export `HYPRLAND_INSTANCE_SIGNATURE`, preventing subshell probe failures across compositor relogins.
    4. Validate shell syntax with `bash -n bootstrap.sh`.
  </action>
  <acceptance_criteria>
    bash -n bootstrap.sh exits 0.
    grep -q '\.config/quickshell/ii/modules/ii/bar' bootstrap.sh exits 0.
    grep -q '\.config/quickshell/ii/services' bootstrap.sh exits 0.
    grep -q '\.config/quickshell/ii/scripts/videos' bootstrap.sh exits 0.
    grep -q 'colors.json' bootstrap.sh exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n bootstrap.sh && grep -q '\.config/quickshell/ii/modules/ii/bar' bootstrap.sh && grep -q 'colors.json' bootstrap.sh</automated>
    <fails_when>non-zero exit code or missing Quickshell directory pre-creation / color priming check in bootstrap.sh</fails_when>
  </verify>
  <done>bootstrap.sh contains Quickshell target directory pre-creation, verified color priming assertion, and resilient Hyprland socket probing.</done>
</task>

<task id="34-01-03" type="execute">
  <name>Align scripts/phase31-overlay-pill-assert.sh with Phase 33 layout & run regression baseline (INTG-01, D-14)</name>
  <read_first>
    scripts/phase31-overlay-pill-assert.sh
    scripts/phase32-component-formatting-assert.sh
    scripts/phase33-layout-assert.sh
    .planning/phases/34-verification-zero-drift-bootstrap-integration/34-PATTERNS.md
    .planning/phases/34-verification-zero-drift-bootstrap-integration/34-RESEARCH.md
  </read_first>
  <files>
    scripts/phase31-overlay-pill-assert.sh
  </files>
  <action>
    Align `scripts/phase31-overlay-pill-assert.sh` Section 3 with the Phase 33 layout reorganization and verify the complete milestone v0.6 regression suite:
    1. In `scripts/phase31-overlay-pill-assert.sh` Section 3 (lines 211–217), update the borderless container check that currently asserts `visible: Config.options?.bar.borderless` in `BarContent.qml`.
    2. In Phase 33, `VerticalBarSeparator` was intentionally eliminated from `BarContent.qml` (D-02, D-05), and borderless container background toggling resides in `BarGroup.qml` (`color: Config.options?.bar.borderless ? "transparent" : ...`).
    3. Update the assertion in `scripts/phase31-overlay-pill-assert.sh` to check for `color: Config.options?.bar.borderless ? "transparent"` in `BarGroup.qml` OR `visible: Config.options?.bar.borderless` in `BarContent.qml` (D-14), passing with a descriptive log message naming Phase 33 alignment.
    4. Verify shell syntax with `bash -n scripts/phase31-overlay-pill-assert.sh`.
    5. Execute `bash scripts/phase31-overlay-pill-assert.sh` and verify it exits 0 with `FAIL=0 FINDINGS=0`.
    6. Execute the full regression baseline in sequence: `scripts/phase31-overlay-pill-assert.sh`, `scripts/phase32-component-formatting-assert.sh`, and `scripts/phase33-layout-assert.sh`, confirming that each passes cleanly with 0 failures (D-14).
  </action>
  <acceptance_criteria>
    bash -n scripts/phase31-overlay-pill-assert.sh exits 0.
    bash scripts/phase31-overlay-pill-assert.sh exits 0 with FAIL=0.
    bash scripts/phase32-component-formatting-assert.sh exits 0 with FAIL=0.
    bash scripts/phase33-layout-assert.sh exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase31-overlay-pill-assert.sh && bash scripts/phase32-component-formatting-assert.sh && bash scripts/phase33-layout-assert.sh</automated>
    <fails_when>non-zero exit code or any assertion failure across Phase 31, 32, or 33 harnesses</fails_when>
  </verify>
  <done>scripts/phase31-overlay-pill-assert.sh is aligned with Phase 33 layout reorganization, and all three milestone v0.6 regression harnesses pass with 0 failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository Mirror → Live System Files | Files in `capture/` and `stow/` mirror live user configs and must remain byte-identical without diverging |
| Bootstrap State Machine → File System | Installer creating directories and linking files during deployment |
| Unprivileged Execution → System Integrity | Dotfiles scripts and assertions run strictly under unprivileged user account (EUID != 0) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-01 | Tampering / Config Divergence | capture/ii/.../config.json | high | mitigate | Restore live `~/.config/illogical-impulse/config.json` byte-identically from capture mirror before committing, eliminating `cmp -s` drift in `verify --strict` (D-09). |
| T-34-02 | Tampering / Directory Folding | bootstrap.sh run_stow_step | high | mitigate | Pre-create sensitive Quickshell target directories (`modules/ii/bar`, `services`, `scripts/videos`) with `mkdir -p` before invoking Stow (D-05). |
| T-34-03 | Elevation of Privilege / Root Execution | Environment | medium | mitigate | Enforce non-root execution checks (`[[ "${EUID:-$(id -u)}" -ne 0 ]]`) across all modified scripts. |
| T-34-04 | Denial of Service / Stale Socket Pipe Failure | bootstrap.sh probe_session_environment | medium | mitigate | Dynamically query `/run/user/$(id -u)/hypr/` for the newest instance signature when `hyprctl status` fails, preventing subshell pipe crashes. |
</threat_model>

<verification>
Automated verification commands:
1. `git status --porcelain capture stow | wc -l | grep -q '^0$' && ./arch/dots-hyprland.sh verify --strict | grep -q 'FINDINGS=0'`
2. `bash -n bootstrap.sh && grep -q '\.config/quickshell/ii/modules/ii/bar' bootstrap.sh && grep -q 'colors.json' bootstrap.sh`
3. `bash scripts/phase31-overlay-pill-assert.sh && bash scripts/phase32-component-formatting-assert.sh && bash scripts/phase33-layout-assert.sh`
</verification>

<success_criteria>
1. `capture/` and `stow/` trees are committed and 100% clean in git status.
2. `bootstrap.sh` pre-creates Quickshell directories, asserts primed colors, and resiliently resolves Hyprland sockets.
3. `scripts/phase31-overlay-pill-assert.sh` passes with `FAIL=0` and all three prior milestone assert suites pass without error.
</success_criteria>

## Artifacts this phase produces
- Config commit: `capture/ii/.config/illogical-impulse/config.json` (baseline `showPerformanceProfileToggle: false`)
- Config commit: `stow/hypr/.config/hypr/custom/general.lua` (layout geometry and animations baseline)
- Controller modification: `bootstrap.sh` (pre-creation in `run_stow_step`, `generate_initial_theme` color priming gate, `probe_session_environment` dynamic socket detection)
- Test modification: `scripts/phase31-overlay-pill-assert.sh` (aligned Section 3 borderless check)

<output>
Create `.planning/phases/34-verification-zero-drift-bootstrap-integration/34-01-SUMMARY.md` when done
</output>
