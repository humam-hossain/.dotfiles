---
phase: "31"
plan: "01"
type: "tracer"
wave: 1
depends_on: []
files_modified:
  - scripts/phase31-overlay-pill-assert.sh
  - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
  - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml
  - restow/README.md
autonomous: true
requirements: [PILL-01]

must_haves:
  truths:
    - "scripts/phase31-overlay-pill-assert.sh exists at mode 0755 implementing fail-closed conventions (set -euo pipefail, cleanup trap, --section parsing, and porcelain snapshot check) (D-08)"
    - "restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml and BarGroup.qml are initialized as personal overlays from upstream vendor baselines without modifying vendor/dots-hyprland (D-05, PILL-01)"
    - "restow/README.md Section 3 package recovery table includes quickshell mapped to tag rsync-replace and recovery command matching gen-collision-map.sh output (D-06, PILL-01)"
    - "Target files in ~/.config/quickshell/ii/modules/ii/bar/ are deployed as discrete leaf symlinks pointing into restow/quickshell without ancestor directory folding (D-05, PILL-01)"
    - "Section 1 of scripts/phase31-overlay-pill-assert.sh passes with FAIL=0 while vendor/dots-hyprland submodule remains completely clean (D-08, PILL-01)"
  artifacts:
    - scripts/phase31-overlay-pill-assert.sh
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml
    - restow/README.md
  key_links:
    - "scripts/phase31-overlay-pill-assert.sh is the authoritative test harness gating PILL-01, PILL-02, PILL-03, and PILL-04 across Sections 1 to 4"
    - "restow/quickshell/ package is symlinked to ~/.config/quickshell/ii/modules/ii/bar/ using GNU Stow with --no-folding"
    - "collision-map.tsv row 83 mathematically derives tree=restow for $XDG_CONFIG_HOME/quickshell under rsync-replace outcome"
  prohibitions:
    - "Must not edit or modify files in vendor/dots-hyprland/"
    - "Must not use banned stow --adopt when deploying overlay symlinks"
    - "Must not omit --no-folding when invoking GNU Stow"
    - "Must not commit untracked runtime artifacts or generated caches to git"
---

<objective>
Scaffold the Phase 31 assertion harness (`scripts/phase31-overlay-pill-assert.sh`) with fail-closed structure covering Sections 1 through 4 (D-08), establish the personal Quickshell overlay structure in `restow/quickshell/.config/quickshell/ii/modules/ii/bar/` with baseline files copied from upstream `dots-hyprland` preserving `vendor/` cleanliness (D-05, PILL-01), update `restow/README.md` Section 3 table via `./scripts/gen-collision-map.sh --restow-table` (D-06), execute the safe replacement procedure deploying leaf symlinks to `~/.config/quickshell/ii/modules/ii/bar/` using GNU Stow `--no-folding` (D-05), and verify Section 1 passes with `FAIL=0` (D-08, PILL-01).

Purpose: Establish the tracer slice for Phase 31, proving the GNU Stow overlay architecture, symlink resolution, documentation synchronization, and test scaffolding prior to pill geometry modifications.
Output: Executable test harness `scripts/phase31-overlay-pill-assert.sh`, overlay package `restow/quickshell/`, updated `restow/README.md`, and green Section 1 assertions.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-CONTEXT.md
@.planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-RESEARCH.md
@.planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-PATTERNS.md
@.planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-VALIDATION.md
@collision-map.tsv
@restow/README.md
@scripts/gen-collision-map.sh
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/bar/BarContent.qml
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/bar/BarGroup.qml
</context>

<assumptions>
- Upstream dots-hyprland installer tracks $XDG_CONFIG_HOME/quickshell under collision-map.tsv row 83 as install_dir__sync, requiring the restow/ tree and rsync-replace recovery tag.
- Existing live files in ~/.config/quickshell/ii/modules/ii/bar/ are regular files installed by dots-hyprland; GNU Stow will refuse to stow over regular files unless removed first, and stow --adopt is strictly banned per repository policy (stow/README.md).
- Running stow with --verbose=5 --no-folding -t ~ quickshell creates discrete leaf symlinks without folding ancestor directories (~/.config, quickshell, ii, modules, bar).
- Submodule vendor/dots-hyprland must remain 100% clean with zero git diff or untracked file churn.
- Repeated executions of GNU Stow on the same restow/quickshell package are idempotent and preserve existing symlinks cleanly.
</assumptions>

<tasks>

<task id="31-01-01" type="tracer">
  <name>Scaffold Phase 31 overlay and pill geometry assert harness (scripts/phase31-overlay-pill-assert.sh)</name>
  <read_first>
    scripts/phase30-tech-debt-assert.sh
    scripts/phase28-terminal-fuzzel-assert.sh
    .planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-CONTEXT.md
    .planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-PATTERNS.md
  </read_first>
  <files>
    scripts/phase31-overlay-pill-assert.sh
  </files>
  <action>
    Scaffold `scripts/phase31-overlay-pill-assert.sh` with permissions 0755 adhering strictly to repository assert harness conventions per D-08:
    1. Base scaffolding: Use `#!/usr/bin/env bash`, `set -euo pipefail`, resolve `REPO_ROOT` and `ASSERT_SELF`, export default `XDG_CONFIG_HOME` and `XDG_STATE_HOME`, initialize `FAIL=0` and `FINDINGS=0`, and define `pass()`, `fail()`, `finding()`, and `info()` helpers.
    2. Cleanup trap: Initialize `TMP_FILES=()` and `SCRATCH_ROOTS=()`, define `cleanup()` removing temporary files with `rm -f` and scratch directories with `rm -rf`, and register `trap cleanup EXIT`.
    3. CLI argument parsing: Support `--section <1-4>` and `-h|--help`, validating integer range 1 to 4 with fail-closed exit 1 on invalid arguments.
    4. Two-phase working tree snapshots: Implement `porcelain_snapshot_raw` and `porcelain_snapshot` filtering ephemeral noise (`.commandcode/`, `scripts/__pycache__/`), capturing git status before test execution into `$PORCELAIN_BEFORE`.
    5. Scaffold test section stubs:
       - Section 1: Symlink & Packaging Integrity (PILL-01, D-05, D-06)
       - Section 2: QML Property & Unclamped Sizing Integrity (PILL-02, PILL-03, D-01, D-04)
       - Section 3: Dynamic Animation & Visual Defaults (PILL-02, PILL-03, PILL-04, D-02, D-03)
       - Section 4: Repository Hygiene & Verification Engine (PILL-01, D-06, D-08, INTG-02)
    6. Closing porcelain snapshot check: Capture `$PORCELAIN_AFTER`, compare against `$PORCELAIN_BEFORE` using `cmp -s` (failing if git status changed across run), print `=== done: FAIL=$FAIL FINDINGS=$FINDINGS ===`, and exit 1 if FAIL > 0, else exit 0.
  </action>
  <acceptance_criteria>
    test -x scripts/phase31-overlay-pill-assert.sh exits 0.
    bash scripts/phase31-overlay-pill-assert.sh --help exits 0 and outputs Usage: .* --section <1-4>.
    bash scripts/phase31-overlay-pill-assert.sh --section 99 exits 1 with error message on stderr.
  </acceptance_criteria>
  <verify>
    <automated>test -x scripts/phase31-overlay-pill-assert.sh && bash scripts/phase31-overlay-pill-assert.sh --help | grep -q -- '--section <1-4>'</automated>
    <fails_when>non-zero exit code, file not executable, or missing '--section <1-4>' in help output</fails_when>
  </verify>
  <done>scripts/phase31-overlay-pill-assert.sh exists at mode 0755, parses --section <1-4>, enforces porcelain snapshot integrity, and scaffolds Sections 1 through 4.</done>
</task>

<task id="31-01-02" type="execute">
  <name>Establish restow/quickshell package, regenerate restow table, deploy symlinks, and verify Section 1</name>
  <read_first>
    collision-map.tsv
    restow/README.md
    scripts/gen-collision-map.sh
    vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/bar/BarContent.qml
    vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/bar/BarGroup.qml
    .planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-PATTERNS.md
    .planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-CONTEXT.md
  </read_first>
  <files>
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml
    restow/README.md
    scripts/phase31-overlay-pill-assert.sh
  </files>
  <action>
    Establish personal Quickshell overlay in `restow/quickshell/`, regenerate documentation, and deploy leaf symlinks per D-05, D-06, and PILL-01:
    1. Create directory structure `restow/quickshell/.config/quickshell/ii/modules/ii/bar/`.
    2. Seed the overlay by copying upstream baseline files from `vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/bar/BarContent.qml` and `BarGroup.qml` into `restow/quickshell/.config/quickshell/ii/modules/ii/bar/`, verifying `vendor/dots-hyprland` remains 100% clean and unmodified.
    3. Regenerate Section 3 of `restow/README.md` by running `./scripts/gen-collision-map.sh --restow-table`, verifying that package `quickshell` is listed with tag `rsync-replace` and recovery command `cd restow && stow --verbose=5 --no-folding -t ~ quickshell` per D-06.
    4. Execute the safe replacement procedure per `stow/README.md:74-100` and reversibility gate:
       - Check if `$HOME/.config/quickshell/ii/modules/ii/bar/BarContent.qml` and `BarGroup.qml` exist as regular files. If regular files, create timestamped `.bak` backups before unlinking (`cp -p "$file" "$file.bak"`), then remove the regular files. Do NOT use banned `stow --adopt`.
       - Deploy leaf symlinks by executing: `cd "$REPO_ROOT/restow" && stow --verbose=5 --no-folding -t ~ quickshell` per D-05.
    5. Implement Section 1 in `scripts/phase31-overlay-pill-assert.sh` asserting:
       - Target files `$HOME/.config/quickshell/ii/modules/ii/bar/BarContent.qml` and `BarGroup.qml` are symlinks resolving to `$REPO_ROOT/restow/quickshell/.config/quickshell/ii/modules/ii/bar/<file>`.
       - Ancestor directories (`~/.config`, `quickshell`, `ii`, `modules`, `bar`) are real directories, not folded symlinks.
       - Sibling modules (`Bar.qml`, `ActiveWindow.qml`, `ClockWidget.qml`, `Workspaces.qml`) remain intact regular files.
       - Submodule `vendor/dots-hyprland` working tree is 100% clean.
    6. Run `bash scripts/phase31-overlay-pill-assert.sh --section 1` to verify Section 1 passes with FAIL=0.
  </action>
  <acceptance_criteria>
    test -f restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml exits 0.
    test -f restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml exits 0.
    grep -q '| `quickshell` | `rsync-replace` |' restow/README.md exits 0.
    test -L "$HOME/.config/quickshell/ii/modules/ii/bar/BarContent.qml" exits 0.
    test -L "$HOME/.config/quickshell/ii/modules/ii/bar/BarGroup.qml" exits 0.
    git -C vendor/dots-hyprland status --porcelain outputs nothing.
    bash scripts/phase31-overlay-pill-assert.sh --section 1 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase31-overlay-pill-assert.sh --section 1</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>restow/quickshell/ established with baseline BarContent.qml and BarGroup.qml, restow/README.md Section 3 regenerated, live leaf symlinks point to restow/quickshell without directory folding, vendor/dots-hyprland remains clean, and Section 1 passes with FAIL=0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stow Overlay Linking → User Configuration Tree | GNU Stow linking leaf files into active user environment without folding ancestor directories or adopting foreign files |
| Test Assert Harness → Git Working Tree | Assertion scripts executing git status snapshots without creating uncommitted file churn |
| Repository Packaging → Upstream Submodule | Personal modifications confined to restow/ without modifying vendor/dots-hyprland |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering / Test Harness Churn | scripts/phase31-overlay-pill-assert.sh | medium | mitigate | Enforce two-phase porcelain snapshots comparing `$PORCELAIN_BEFORE` and `$PORCELAIN_AFTER` with cleanup traps on exit (D-08). |
| T-31-02 | Tampering / Unintended Adoption | GNU Stow and live files | high | mitigate | Strictly ban `stow --adopt`; back up and remove existing regular files before running `stow --verbose=5 --no-folding -t ~ quickshell` (D-05, CAP-07). |
| T-31-03 | Denial of Service / Directory Folding | GNU Stow directory hierarchy | high | mitigate | Enforce `--no-folding` universally and assert that `~/.config/quickshell` and all ancestor paths remain real directories (PILL-01). |
| T-31-04 | Repudiation / Submodule Pollution | vendor/dots-hyprland | high | mitigate | Maintain personal overlays strictly under `restow/quickshell/` and assert `git -C vendor/dots-hyprland status --porcelain` is empty in Section 1. |
</threat_model>

<verification>
Automated verification commands:
1. `test -x scripts/phase31-overlay-pill-assert.sh && bash scripts/phase31-overlay-pill-assert.sh --help | grep -q -- '--section <1-4>'`
2. `bash scripts/phase31-overlay-pill-assert.sh --section 1`
</verification>

<success_criteria>
1. `scripts/phase31-overlay-pill-assert.sh` exists at mode 0755, handles `--section <1-4>` and `--help`, and enforces porcelain snapshot checks.
2. `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml` and `BarGroup.qml` exist and mirror upstream baseline files.
3. `restow/README.md` Section 3 table is updated with package `quickshell` assigned tag `rsync-replace`.
4. Live target files in `~/.config/quickshell/ii/modules/ii/bar/` resolve to `restow/quickshell/` as symlinks without folded ancestor directories.
5. `vendor/dots-hyprland` submodule remains completely clean with zero uncommitted changes.
6. `bash scripts/phase31-overlay-pill-assert.sh --section 1` exits 0 with FAIL=0.
</success_criteria>

## Artifacts this phase produces
- Test assertion harness: `scripts/phase31-overlay-pill-assert.sh`
- Personal Quickshell bar content overlay: `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml`
- Personal Quickshell bar group container overlay: `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml`
- Restow package documentation and recovery table: `restow/README.md`
- Plan summary: `.planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-01-SUMMARY.md`

<output>
Create `.planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-01-SUMMARY.md` when done
</output>
