---
phase: 05-ux-and-performance-polish
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - .config/nvim/README.md
autonomous: true
requirements:
  - UX-01
  - UX-02
tags:
  - neovim
  - documentation
  - rollout

must_haves:
  truths:
    - "Maintainer can read .config/nvim/README.md and find a single Rollout/Update section that walks through updating a machine end-to-end"
    - "Rollout section documents the full update checklist: git pull, arch/nvim.sh, :Lazy sync, :MasonUpdate, validation harness run"
    - "Rollout section includes a phase-by-phase change summary so the maintainer knows what behavior changed between old and new config"
    - "Rollout section includes post-deploy verification steps (nvim-validate.sh, :checkhealth) with expected outputs"
    - "Rollout section includes rollback instructions covering git revert and lazy-lock.json restore"
    - "Rollout section is addressable by anchor (### Rollout and Update Workflow) so it can be linked externally"
    - "Post-migration startup time is measured via nvim --startuptime and recorded in 05-03-SUMMARY.md under a Startup Time Measurement heading (D-02)"
  artifacts:
    - path: ".config/nvim/README.md"
      provides: "single README with an appended Rollout and Update Workflow section per D-19, D-20"
      contains: "Rollout and Update Workflow"
  key_links:
    - from: ".config/nvim/README.md"
      to: "arch/nvim.sh install script"
      via: "Rollout checklist step invoking bash arch/nvim.sh"
      pattern: "arch/nvim\\.sh"
    - from: ".config/nvim/README.md"
      to: "scripts/nvim-validate.sh harness"
      via: "Rollout verification step invoking ./scripts/nvim-validate.sh all"
      pattern: "nvim-validate\\.sh"
    - from: ".config/nvim/README.md"
      to: "lazy-lock.json rollback path"
      via: "Rollback subsection documenting git checkout of lazy-lock.json"
      pattern: "lazy-lock\\.json"
---

<objective>
Extend `.config/nvim/README.md` with a new top-level section titled `## Rollout and Update Workflow` that documents how to deploy this refactored config to a machine end-to-end. The section MUST cover four sub-areas per D-20: (1) a machine update checklist, (2) a phase-by-phase change summary covering Phases 1 through 5, (3) post-deploy verification steps using the existing validation harness and `:checkhealth`, and (4) rollback instructions covering git revert and `lazy-lock.json` restore.

Purpose: This closes the UX-01 "coherent UI" story from the user-facing perspective: after Plans 01 and 02 replace five plugins with snacks.nvim and polish the statusline/theme, a maintainer needs a single place to read "how do I apply all of this to my other machine safely and back out if it breaks". Per D-19 this is ONE section in the existing README — no new file. Per CLAUDE.md the dotfiles repo already has `arch/nvim.sh` as the canonical install script, so the checklist composes existing tools rather than inventing new automation.

Output: `.config/nvim/README.md` with a new `## Rollout and Update Workflow` section inserted at the top of the file (before the existing `## Phase 4: Tooling and Ecosystem Modernization` heading) so it is the first thing a reader sees. All existing content preserved verbatim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-and-performance-polish/05-CONTEXT.md
@.planning/phases/05-ux-and-performance-polish/05-RESEARCH.md
@.planning/phases/05-ux-and-performance-polish/05-01-PLAN.md
@.planning/phases/05-ux-and-performance-polish/05-02-PLAN.md
@.config/nvim/README.md
@arch/nvim.sh
@scripts/nvim-validate.sh

<interfaces>
<!-- Existing surfaces the documentation must reference correctly. -->

Install script (arch/nvim.sh) — installs pynvim, luarocks, tree-sitter-cli, neovim via pacman, then copies `.config/nvim/*` into `~/.config/nvim/`. Run from repo root: `bash arch/nvim.sh`.

Validation harness (scripts/nvim-validate.sh) subcommands (from Phase 3):
- `./scripts/nvim-validate.sh startup` — headless `nvim +qa` smoke
- `./scripts/nvim-validate.sh sync` — `:Lazy! sync` with 120s timeout
- `./scripts/nvim-validate.sh health` — writes `.planning/tmp/nvim-validate/health.json`
- `./scripts/nvim-validate.sh smoke` — pcall-requires high-risk plugins
- `./scripts/nvim-validate.sh all` — runs startup + sync + smoke + health in order

Lazy lock file (`lazy-lock.json`) lives at the repo root alongside the `.config/` tree. `:Lazy restore` reads it to pin every plugin back to the recorded commit.

Phase summaries already recorded (informational — do NOT reference SUMMARYs from the README text):
- Phase 1: buffer-first lifecycle, autosave policy, cross-platform external-open via `vim.ui.open()`.
- Phase 2: central keymap registry at `lua/core/keymaps/registry.lua` with domain prefixes (f/c/g/e/b/w/t/s).
- Phase 3: validation harness + health snapshot + missing-tool graceful degradation.
- Phase 4: Neovim 0.11+ native LSP (`vim.lsp.config()` / `vim.lsp.enable()`), Mason-first, save-format policy with filetype exclusions.
- Phase 5: snacks.nvim consolidation (replaces noice, nvim-notify, alpha, fzf-lua, indent-blankline); tmux-aware `laststatus` guard; catppuccin snacks integration.

Existing README structure (from reading .config/nvim/README.md):
- Line 1: `# Neovim config`
- Line 3: `## Phase 4: Tooling and Ecosystem Modernization` (first phase section)
- Line 42: `## Phase 2: Central Command and Keymap Architecture`
- Line 96: `## Phase 3: Validation Harness`
- Line 159: `## Phase 1: Reliability and Portability Baseline`
- Line 198: `## Resources`

The new `## Rollout and Update Workflow` section MUST be inserted between the top-level `# Neovim config` heading (line 1) and the existing `## Phase 4: ...` heading (line 3). This places rollout guidance above all phase-specific sections so a new reader sees "how to deploy" first.
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert Rollout and Update Workflow section at the top of .config/nvim/README.md</name>
  <files>.config/nvim/README.md</files>
  <read_first>
    - .config/nvim/README.md
    - arch/nvim.sh
    - scripts/nvim-validate.sh
    - .planning/phases/05-ux-and-performance-polish/05-CONTEXT.md
    - .planning/phases/05-ux-and-performance-polish/05-01-PLAN.md
    - .planning/phases/05-ux-and-performance-polish/05-02-PLAN.md
  </read_first>
  <action>
Edit `.config/nvim/README.md` by inserting the new section immediately after the `# Neovim config` heading (the file's line 1) and before the existing `## Phase 4: Tooling and Ecosystem Modernization` heading (the file's line 3). Preserve every other line in the file verbatim — do NOT edit, reorder, or delete any existing section.

Insert the following exact Markdown block (between the `# Neovim config` title and the first phase section):

```markdown

## Rollout and Update Workflow

This section is the single entry point for applying this config to a new or existing machine, verifying it, and rolling back if something breaks. It assumes the dotfiles repo is cloned at `~/github_repo/.dotfiles` (adjust paths if yours differs).

### Machine Update Checklist

Run these steps in order from the dotfiles repo root. Each step is idempotent.

1. **Pull the latest config**

   ```bash
   cd ~/github_repo/.dotfiles
   git pull --ff-only
   ```

   If the pull is not a fast-forward, resolve manually before continuing — do not `--rebase` against local edits to `.config/nvim/` without reading the phase summaries under `.planning/phases/` first.

2. **Run the install script** (Arch)

   ```bash
   bash arch/nvim.sh
   ```

   This installs `python-pynvim`, `luarocks`, `tree-sitter-cli`, and `neovim` via `pacman -Sy --needed`, then copies `.config/nvim/*` into `~/.config/nvim/`. On Debian/Ubuntu use the `ubuntu/` equivalent if present; on Windows copy `.config/nvim/` into `%LOCALAPPDATA%\nvim\` manually.

3. **Sync plugins against the locked set**

   ```bash
   ./scripts/nvim-validate.sh sync
   ```

   This runs `:Lazy! sync` headlessly with a 120-second timeout. It installs newly added plugins (like `folke/snacks.nvim` introduced in Phase 5) and uninstalls anything removed from the spec tree (`noice.nvim`, `nvim-notify`, `alpha.nvim`, `indent-blankline`, `fzf-lua`). Expected last line: `PASS: sync OK`.

   If you prefer the interactive path, open Neovim and run `:Lazy sync` then quit.

4. **Update Mason-managed tools**

   Open Neovim and run:

   ```
   :MasonUpdate
   ```

   This refreshes the Mason registry so LSP servers, formatters, and linters pull in any new versions. Tools installed system-wide (outside Mason) are untouched — the config's system-binary fallback (Phase 4) handles those.

5. **Run the full validation harness**

   ```bash
   ./scripts/nvim-validate.sh all
   ```

   This runs `startup`, `sync`, `smoke`, and `health` in order and fails fast. Expected final line: `==> all PASS`. See **Post-Deploy Verification** below for what to do on failure.

6. **Launch Neovim and confirm the UI**

   ```bash
   nvim
   ```

   You should see the snacks.nvim dashboard, not the old alpha banner. Press `<leader>ff` to confirm `snacks.picker` opens for file search. Press `<leader>gg` to confirm `snacks.lazygit` opens the lazygit float. These three checks cover the Phase 5 UX-01 coherence surface.

### Phase-by-Phase Change Summary

This summary captures what each phase changed so a maintainer updating a machine from an older config knows what behavior is now different.

| Phase | What Changed | User-Visible Effect |
|-------|--------------|---------------------|
| **Phase 1** | Buffer-first lifecycle; autosave runs only on `FocusLost` for normal buffers; external open uses `vim.ui.open()` | `<C-q>` closes current buffer (not the whole session); special buffers never auto-save; `<C-S-o>` works on Linux, macOS, and Windows |
| **Phase 2** | All custom keymaps moved into `lua/core/keymaps/registry.lua`; leader prefixes organized by domain (`f` search, `c` code, `g` git, `e` explorer, `b` buffers, `w` windows, `t` toggles, `s` save) | Plugin specs no longer define their own `keys = {}`; `:WhichKey` shows grouped, labeled commands |
| **Phase 3** | Added `scripts/nvim-validate.sh` with `startup`/`sync`/`health`/`smoke`/`all` subcommands; added `core.health.snapshot` producing `health.json`; missing external tools degrade silently at runtime and surface only via `health` | Startup no longer nags about missing formatters; validation harness is the single source of truth for "what's missing" |
| **Phase 4** | LSP migrated to Neovim 0.11+ `vim.lsp.config()` + `vim.lsp.enable()`; Mason-first provisioning with system-binary fallback; save-format policy with filetype exclusions (`gitcommit`, `markdown`, `text`, `gitrebase`, `diff`, `NeogitCommitMessage`, `neo-tree`, `qf`); `<leader>cf` manual format; `<leader>sn` save-without-format | Faster LSP startup; no unwanted formatting in commit messages or markdown; cleaner plugin specs |
| **Phase 5** | `noice.nvim`, `nvim-notify`, `alpha.nvim`, `fzf-lua`, and `indent-blankline` replaced by `folke/snacks.nvim` submodules (notifier, dashboard, picker, indent, scroll, words, lazygit, quickfile); lualine `globalstatus = true` with `laststatus` guarded on `$TMUX`; catppuccin integrations pruned (`telescope`/`nvimtree` removed) and `snacks` integration added | Bottom-right toast notifications; minimal snacks dashboard on empty launch; same fuzzy-find keymaps (`<leader>ff`, `<leader>fg`, `<leader>cd`, `<leader>cr`, etc.) now backed by snacks.picker; `<leader>gg` opens lazygit float; statusline visible outside tmux |

### Post-Deploy Verification

Run these checks in order after step 5 of the update checklist. Each check has an expected outcome; anything else is a regression.

1. **Harness: full validation suite**

   ```bash
   ./scripts/nvim-validate.sh all
   ```

   Expected final line: `==> all PASS`. The suite runs `startup`, `sync`, `smoke`, and `health`. Output artifacts land in `.planning/tmp/nvim-validate/` — inspect `health.json` if `health` fails.

2. **In-editor: :checkhealth**

   Open Neovim and run:

   ```
   :checkhealth
   ```

   Scroll through each section. Expected: no errors (`ERROR:` lines) from `snacks`, `lazy`, `lspconfig`, `mason`, `blink.cmp`, `gitsigns`, `neo-tree`, `lualine`, `treesitter`. Warnings (`WARNING:`) about optional tooling (e.g., a missing language LSP you do not use) are acceptable.

3. **Manual keymap smoke**

   | Keymap | Expected Behavior |
   |--------|-------------------|
   | `<leader>ff` | snacks.picker files float opens |
   | `<leader>fg` | snacks.picker grep float opens and finds matches including in hidden files |
   | `<leader>gg` | snacks.lazygit float opens the lazygit TUI |
   | `<leader>cd` | snacks.picker jumps to LSP definition (on a symbol) |
   | `<leader>cr` | snacks.picker lists LSP references |
   | `:echo "test"` + Enter | Bottom-right notification toast appears (snacks.notif) |

4. **Statusline placement check**

   - Inside tmux: Neovim should have no statusline inside its own window; the tmux status bar at the bottom should reflect the current mode/branch/filename (vim-tpipeline forwards lualine's render).
   - Outside tmux (direct terminal or Windows or VS Code embedded): Neovim should show its own statusline at the bottom (guard flips `laststatus=3`).

5. **Dashboard check**

   Launching `nvim` with no file argument should show the snacks.nvim default dashboard sections (Keymaps, Recent Files, Projects, startup footer). If you still see the old alpha ASCII banner, `:Lazy sync` did not clean — rerun step 3 of the checklist.

### Rollback Instructions

If post-deploy verification fails and you cannot fix forward quickly, use the most targeted rollback that matches the failure mode.

#### A. Single-file rollback (config-level regression)

If a specific file broke (e.g., `lualine.lua` or `registry.lua`), revert just that file:

```bash
cd ~/github_repo/.dotfiles
git log --oneline .config/nvim/lua/plugins/lualine.lua
git checkout <commit-before-breakage> -- .config/nvim/lua/plugins/lualine.lua
bash arch/nvim.sh
./scripts/nvim-validate.sh all
```

Use this when one plan's changes misbehave but other plans are fine.

#### B. Phase-level rollback (one plan broke, others are OK)

Revert the commits of the offending plan only. Each plan commit message starts with `feat({phase}-{plan}):` or `docs({phase}-{plan}):`.

```bash
cd ~/github_repo/.dotfiles
git log --oneline --grep='05-01'   # find the plan's commits
git revert <commit-sha>...<commit-sha>   # creates new revert commits (does not rewrite history)
bash arch/nvim.sh
./scripts/nvim-validate.sh all
```

Prefer `git revert` over `git reset --hard` so the history remains linear and other machines can pull the revert cleanly.

#### C. Plugin-set rollback (lazy-lock.json regression)

If `:Lazy sync` pulled a new plugin version that breaks startup, restore the previous lockfile and pin plugins back:

```bash
cd ~/github_repo/.dotfiles
git log --oneline lazy-lock.json
git checkout <previous-commit> -- lazy-lock.json
```

Then inside Neovim:

```
:Lazy restore
```

`:Lazy restore` reads `lazy-lock.json` and resets every plugin to the commit recorded there. Re-run `./scripts/nvim-validate.sh all` to confirm the rollback is healthy.

#### D. Full phase rollback (last resort)

If Phase 5 is the problem and you want the pre-Phase-5 state back:

```bash
cd ~/github_repo/.dotfiles
git log --oneline --grep='05-'                # list all Phase 5 commits
git revert <oldest-05-commit>..<newest-05-commit>
bash arch/nvim.sh
./scripts/nvim-validate.sh all
```

Expect `snacks.nvim` to be uninstalled on the next `:Lazy sync` and the old `noice.nvim`, `nvim-notify`, `alpha.nvim`, `fzf-lua`, `indent-blankline` plugin specs to be restored by the revert.

#### Rollback Sanity Checks

After any rollback path above, the same post-deploy verification suite applies:

```bash
./scripts/nvim-validate.sh all
nvim   # confirm dashboard, <leader>ff, <leader>gg interactively
```

If the harness is still red after rollback, the breakage is upstream of the reverted commit range — widen the revert window or open a fresh branch and bisect with `git bisect` against `./scripts/nvim-validate.sh startup`.

```

(Note: the triple-backtick fence just above is the closing fence of the last code block inside the section. After the section ends, leave one blank line, then the existing `## Phase 4: Tooling and Ecosystem Modernization` heading continues unchanged.)

Implementation constraints:
- Use the `Edit` tool (or equivalent text editor) to splice the block between the file's line 1 and line 3 — do NOT rewrite the whole file to avoid introducing unrelated diffs.
- Ensure exactly one blank line separates `# Neovim config` from `## Rollout and Update Workflow`, and exactly one blank line separates the closing of the Rollback Sanity Checks subsection from `## Phase 4: Tooling and Ecosystem Modernization`.
- Every existing byte below line 2 of the original README MUST remain unchanged — `git diff` should show only insertions, no deletions or line moves outside the inserted range.
- Do NOT create `.planning/phases/05-ux-and-performance-polish/ROLLOUT.md` or any sibling file — per D-19 this is one section in the existing README.
  </action>
  <verify>
    <automated>bash -c 'set -e; grep -q "^## Rollout and Update Workflow" .config/nvim/README.md; grep -q "### Machine Update Checklist" .config/nvim/README.md; grep -q "### Phase-by-Phase Change Summary" .config/nvim/README.md; grep -q "### Post-Deploy Verification" .config/nvim/README.md; grep -q "### Rollback Instructions" .config/nvim/README.md; grep -q "arch/nvim.sh" .config/nvim/README.md; grep -q "nvim-validate.sh all" .config/nvim/README.md; grep -q ":MasonUpdate" .config/nvim/README.md; grep -q ":checkhealth" .config/nvim/README.md; grep -q "lazy-lock.json" .config/nvim/README.md; grep -q ":Lazy restore" .config/nvim/README.md; grep -q "git revert" .config/nvim/README.md; grep -q "^## Phase 4: Tooling and Ecosystem Modernization" .config/nvim/README.md; grep -q "^## Phase 2: Central Command and Keymap Architecture" .config/nvim/README.md; grep -q "^## Phase 3: Validation Harness" .config/nvim/README.md; grep -q "^## Phase 1: Reliability and Portability Baseline" .config/nvim/README.md; grep -q "^## Resources" .config/nvim/README.md; ! test -f .planning/phases/05-ux-and-performance-polish/ROLLOUT.md'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Rollout and Update Workflow" .config/nvim/README.md` outputs `1`
    - `grep -c "^### Machine Update Checklist" .config/nvim/README.md` outputs `1`
    - `grep -c "^### Phase-by-Phase Change Summary" .config/nvim/README.md` outputs `1`
    - `grep -c "^### Post-Deploy Verification" .config/nvim/README.md` outputs `1`
    - `grep -c "^### Rollback Instructions" .config/nvim/README.md` outputs `1`
    - README contains all of: `arch/nvim.sh`, `:Lazy sync`, `:MasonUpdate`, `./scripts/nvim-validate.sh all`, `:checkhealth`, `lazy-lock.json`, `:Lazy restore`, `git revert`
    - README mentions all five phases explicitly inside the "Phase-by-Phase Change Summary" table: `Phase 1`, `Phase 2`, `Phase 3`, `Phase 4`, `Phase 5`
    - README mentions the five plugins removed in Phase 5: `noice.nvim`, `nvim-notify`, `alpha.nvim`, `fzf-lua`, `indent-blankline`
    - README mentions the snacks replacement: `snacks.nvim`
    - README mentions rollback sub-headings for each of the four rollback modes: `Single-file rollback`, `Phase-level rollback`, `Plugin-set rollback`, `Full phase rollback`
    - Section ordering: `## Rollout and Update Workflow` appears before `## Phase 4: Tooling and Ecosystem Modernization` — verify with `awk '/^## / {print NR": "$0}' .config/nvim/README.md` showing Rollout before Phase 4
    - Existing sections preserved: `## Phase 4: Tooling and Ecosystem Modernization`, `## Phase 2: Central Command and Keymap Architecture`, `## Phase 3: Validation Harness`, `## Phase 1: Reliability and Portability Baseline`, `## Resources` all still present
    - No new file created: `test ! -f .planning/phases/05-ux-and-performance-polish/ROLLOUT.md` succeeds
    - File has not lost content: `wc -l .config/nvim/README.md` exceeds the pre-edit line count (original 204 lines) by at least 150 lines (the new section is substantial)
    - `git diff --stat .config/nvim/README.md` shows only insertions (no deletions outside the insertion range) — verify by inspecting `git diff .config/nvim/README.md` shows `-` lines only where a blank line moved, not in existing prose
  </acceptance_criteria>
  <done>`.config/nvim/README.md` contains a new `## Rollout and Update Workflow` section at the top (below the H1, above Phase 4) with four subsections covering checklist, change summary, verification, rollback; every existing phase section and the Resources section preserved verbatim; no sibling ROLLOUT.md file created.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run the validation harness to confirm documentation edit did not break anything and assert the new section is addressable</name>
  <files>.config/nvim/README.md</files>
  <read_first>
    - .config/nvim/README.md
    - scripts/nvim-validate.sh
  </read_first>
  <action>
This task has no code changes — it runs the harness and asserts the rollout section is self-consistent with the repo it describes.

1. Run the full validation harness:

   ```bash
   ./scripts/nvim-validate.sh all
   ```

   Expected last line: `==> all PASS`. A README edit should not affect the harness, but running it here serves two purposes: (a) regression guard against the Plan 01 + Plan 02 state the README now describes, and (b) confirms the commands the README instructs the user to run actually succeed on this machine.

2. Cross-check that every file path the README tells the user to run actually exists:

   ```bash
   test -f arch/nvim.sh
   test -x scripts/nvim-validate.sh
   test -f lazy-lock.json
   test -f .config/nvim/lua/plugins/snacks.lua   # from Plan 01
   test -f .config/nvim/lua/plugins/lualine.lua  # from Plan 02
   test -f .config/nvim/lua/plugins/colortheme.lua  # from Plan 02
   ```

   All six tests must succeed. This enforces that the README is not referencing ghost paths.

3. Cross-check that the plugins the README says were removed are actually gone:

   ```bash
   ! test -f .config/nvim/lua/plugins/notify.lua
   ! test -f .config/nvim/lua/plugins/alpha.lua
   ! test -f .config/nvim/lua/plugins/indent-blankline.lua
   ! test -f .config/nvim/lua/plugins/fzflua.lua
   ```

   All four tests must succeed. This enforces that the "Phase 5 removed" list in the README is truthful.

4. Verify the rollout section anchor is reachable via standard Markdown anchor resolution (GitHub, editors). The anchor derived from `## Rollout and Update Workflow` is `#rollout-and-update-workflow`. Confirm by searching:

   ```bash
   grep -q "^## Rollout and Update Workflow$" .config/nvim/README.md
   ```

5. **Measure and record startup time (D-02):**

   Run `nvim --startuptime` headlessly and capture the final total line:

   ```bash
   nvim --startuptime /tmp/nvim-startup.log +qa 2>/dev/null || true
   STARTUP_MS=$(tail -1 /tmp/nvim-startup.log | awk '{print $1}')
   echo "Startup time: ${STARTUP_MS}ms"
   ```

   Record the result. The D-02 locked decision targets under 100ms; the actual value is hardware-dependent. This step DOES NOT fail if the number exceeds 100ms — it documents the actual measurement so the maintainer can evaluate the gain empirically. The measurement MUST appear in `05-03-SUMMARY.md` under a `## Startup Time Measurement` heading with the format: `Post-migration startup: {STARTUP_MS}ms (D-02 target: <100ms)`.

If any step fails, the documentation is out of sync with the repo state Plans 01 and 02 produced — investigate whether Plan 01/02 is incomplete rather than patching the README to match a broken state.
  </action>
  <verify>
    <automated>bash -c 'set -e; ./scripts/nvim-validate.sh all; test -f arch/nvim.sh; test -x scripts/nvim-validate.sh; test -f lazy-lock.json; test -f .config/nvim/lua/plugins/snacks.lua; test -f .config/nvim/lua/plugins/lualine.lua; test -f .config/nvim/lua/plugins/colortheme.lua; ! test -f .config/nvim/lua/plugins/notify.lua; ! test -f .config/nvim/lua/plugins/alpha.lua; ! test -f .config/nvim/lua/plugins/indent-blankline.lua; ! test -f .config/nvim/lua/plugins/fzflua.lua; grep -q "^## Rollout and Update Workflow$" .config/nvim/README.md; nvim --startuptime /tmp/nvim-startup.log +qa 2>/dev/null || true; test -s /tmp/nvim-startup.log'</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/nvim-validate.sh all` exits 0 with `==> all PASS` in the final line
    - `arch/nvim.sh` exists as a regular file
    - `scripts/nvim-validate.sh` exists and is executable (`-x`)
    - `lazy-lock.json` exists at repo root
    - Plan-01 output artifact `.config/nvim/lua/plugins/snacks.lua` exists
    - Plan-02 output artifacts `.config/nvim/lua/plugins/lualine.lua` and `.config/nvim/lua/plugins/colortheme.lua` exist
    - Plan-01 deletion targets (`notify.lua`, `alpha.lua`, `indent-blankline.lua`, `fzflua.lua`) are all absent
    - `.config/nvim/README.md` contains the exact line `## Rollout and Update Workflow` (no trailing whitespace, no surrounding noise)
    - `/tmp/nvim-startup.log` is non-empty after the startuptime run
    - `05-03-SUMMARY.md` contains a `## Startup Time Measurement` heading with `Post-migration startup:` and `D-02 target: <100ms`
    - The four steps run without requiring manual intervention (fully autonomous)
  </acceptance_criteria>
  <done>Harness green; every path the README instructs a reader to run exists and every path the README says is removed is actually gone; rollout section anchor present; startup time measured and recorded in SUMMARY per D-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human reader ↔ documentation | Maintainer follows README instructions verbatim on a real machine; incorrect steps produce wasted rollbacks, not security issues |
| documentation ↔ repo state | README references paths and commands that must exist in the repo; drift produces broken rollout runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | README rollout instructions | mitigate | Task 2 runs the harness end-to-end and file-exists assertions for every path mentioned in the README (`arch/nvim.sh`, `scripts/nvim-validate.sh`, `lazy-lock.json`, Plan-01/02 artifact set); any drift between prose and repo fails the verify step before commit |
| T-05-10 | Information Disclosure | README content | accept | Rollout instructions reference only project-local paths and public commands (`git pull`, `:Lazy sync`, `:MasonUpdate`); no credentials, secrets, or machine-identifying info disclosed. Low risk for a personal dotfiles repo published publicly |
| T-05-11 | Repudiation | Rollback paths | accept | All rollback modes use `git revert` (which creates audit-trail commits) or `git checkout <sha> -- <file>` followed by a new commit; no history rewriting is documented or recommended. Auditability preserved by design |
</threat_model>

<verification>
After both tasks complete:

1. `grep '^## ' .config/nvim/README.md` outputs exactly this ordered list (section titles): `## Rollout and Update Workflow`, `## Phase 4: Tooling and Ecosystem Modernization`, `## Phase 2: Central Command and Keymap Architecture`, `## Phase 3: Validation Harness`, `## Phase 1: Reliability and Portability Baseline`, `## Resources`.
2. `./scripts/nvim-validate.sh all` exits 0 with `==> all PASS`.
3. Opening `.config/nvim/README.md` in a Markdown renderer (GitHub, nvim with `render-markdown.nvim`) shows the Rollout section as the first H2, with four H3 subsections, three fenced code blocks for shell commands, and one table for the phase-by-phase change summary.
4. `git diff --stat .config/nvim/README.md` shows additions in the 150-220 line range and zero deletions of pre-existing content (insertions only).
</verification>

<success_criteria>
- `## Rollout and Update Workflow` section added to `.config/nvim/README.md` per D-19 (extend, not create sibling file)
- Section covers all four D-20 areas: machine update checklist, phase-by-phase change summary, post-deploy verification, rollback instructions
- Every path and command referenced in the rollout section exists/runs on the current repo (asserted by Task 2)
- Existing README sections (Phase 1-4, Resources) preserved verbatim
- Harness `./scripts/nvim-validate.sh all` green after the edit
- No new file created under `.planning/` or elsewhere to hold rollout content
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-and-performance-polish/05-03-SUMMARY.md`.
</output>
