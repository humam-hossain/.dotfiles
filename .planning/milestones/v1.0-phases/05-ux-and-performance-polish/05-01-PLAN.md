---
phase: 05-ux-and-performance-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .config/nvim/lua/plugins/snacks.lua
  - .config/nvim/lua/plugins/notify.lua
  - .config/nvim/lua/plugins/alpha.lua
  - .config/nvim/lua/plugins/indent-blankline.lua
  - .config/nvim/lua/plugins/fzflua.lua
  - .config/nvim/lua/core/keymaps/registry.lua
  - scripts/nvim-validate.sh
autonomous: true
requirements:
  - UX-01
tags:
  - neovim
  - plugins
  - snacks
  - migration

must_haves:
  truths:
    - "User sees bottom-right toast notifications instead of noice/nvim-notify floats"
    - "User sees the snacks dashboard (default sections) on empty nvim launch instead of alpha ASCII banner"
    - "User fuzzy finds with snacks.picker (keymaps <leader>ff, <leader>fg, <leader>fc, <leader>fh, <leader>fk, <leader>fb, <leader>fw, <leader>fW, <leader>fd, <leader>fr, <leader>fo, <leader><leader>, <leader>/ all work and call snacks)"
    - "User navigates LSP via snacks.picker (<leader>cr, <leader>ci, <leader>cd, grt, gO, gW)"
    - "User opens lazygit float via <leader>gg"
    - "User sees indent guides from snacks.indent (replacing indent-blankline)"
    - "User sees word highlights for symbol under cursor (snacks.words)"
    - "User scrolls with smooth animation via snacks.scroll on <C-d>/<C-u>"
    - "Validation harness probes snacks module successfully; no probes for noice/notify/fzf-lua/alpha"
  artifacts:
    - path: ".config/nvim/lua/plugins/snacks.lua"
      provides: "single snacks.nvim spec replacing notify, alpha, indent-blankline, fzf-lua"
      contains: 'folke/snacks.nvim'
    - path: ".config/nvim/lua/plugins/notify.lua"
      provides: "DELETED (noice + nvim-notify removed)"
    - path: ".config/nvim/lua/plugins/alpha.lua"
      provides: "DELETED (alpha-nvim removed)"
    - path: ".config/nvim/lua/plugins/indent-blankline.lua"
      provides: "DELETED (ibl removed)"
    - path: ".config/nvim/lua/plugins/fzflua.lua"
      provides: "DELETED (fzf-lua removed)"
    - path: ".config/nvim/lua/core/keymaps/registry.lua"
      provides: "picker + lazygit keymap entries wired to Snacks.picker.* and Snacks.lazygit()"
      contains: "Snacks.picker"
    - path: "scripts/nvim-validate.sh"
      provides: "PLUGIN_LIST and smoke list reference 'snacks'; no references to notify/noice/fzf-lua/alpha"
      contains: "'snacks'"
  key_links:
    - from: ".config/nvim/lua/plugins/snacks.lua"
      to: "snacks.nvim repository"
      via: "lazy.nvim plugin spec"
      pattern: "folke/snacks.nvim"
    - from: ".config/nvim/lua/core/keymaps/registry.lua"
      to: "snacks.picker sources"
      via: "action function bodies using Snacks.picker.*"
      pattern: "Snacks\\.picker\\."
    - from: "scripts/nvim-validate.sh"
      to: "snacks runtime module"
      via: "PLUGIN_LIST entries and smoke Lua script"
      pattern: "'snacks'"
---

<objective>
Replace noice.nvim, nvim-notify, alpha.nvim, indent-blankline, and fzf-lua with a single snacks.nvim spec. Rewire every fzf-lua registry entry (13 search + 6 LSP buffer-local) to Snacks.picker.* equivalents and add a snacks.lazygit keymap. Update the validation harness PLUGIN_LIST and smoke list in lockstep so health/smoke checks pass after migration.

Purpose: This is the UX-01 coherence pass — one package replaces five, eliminates stale dependency chains, and unlocks the startup profiling pass in Plan 03. Keymap fidelity is mandatory (D-06, D-22): existing lhs/desc/id values in registry.lua stay identical; only the action function body changes.

Output: New snacks.lua spec; four deleted plugin files; rewritten registry.lua search and lsp-buffer entries; updated nvim-validate.sh harness. Startup smoke and health both green after `:Lazy sync` installs snacks.nvim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ux-and-performance-polish/05-CONTEXT.md
@.planning/phases/05-ux-and-performance-polish/05-RESEARCH.md
@.planning/phases/05-ux-and-performance-polish/05-VALIDATION.md
@.planning/phases/02-central-command-and-keymap-architecture/02-CONTEXT.md
@.planning/phases/04-tooling-and-ecosystem-modernization/04-03-SUMMARY.md
@.config/nvim/lua/core/keymaps/registry.lua
@.config/nvim/lua/plugins/fzflua.lua
@.config/nvim/lua/plugins/notify.lua
@.config/nvim/lua/plugins/alpha.lua
@.config/nvim/lua/plugins/indent-blankline.lua
@scripts/nvim-validate.sh

<interfaces>
<!-- Key contracts. Executor should use these directly — no codebase exploration needed. -->

Central keymap registry shape (existing, from registry.lua):
```lua
-- M.lazy[i]:
{
  id = "search.files",
  lhs = "<leader>ff",
  mode = "n",
  desc = "Find Files in project directory",
  domain = "f",
  scope = "lazy",
  plugin = "ibhagwan/fzf-lua",           -- MUST change to "folke/snacks.nvim"
  action = function() require("fzf-lua").files() end,  -- MUST change to Snacks.picker.files()
}

-- M.buffer[i] (LSP):
{
  id = "lsp.references",
  lhs = "<leader>cr",
  ...
  action = function(opts) require("fzf-lua").lsp_references(opts) end,  -- MUST change
}
```

lazy.lua compiler reads `map.plugin` field at entry [25] for pcall(require, map.plugin) — when plugin is "folke/snacks.nvim", the require path differs from "fzf-lua". The lazy.lua compiler falls through to the function literal path when `mod[map.action]` is nil; since Snacks is a global set up by snacks.nvim's init, `Snacks.picker.files()` runs directly via the function body.

fzf-lua → snacks.picker mapping (copy verbatim):
| fzf-lua call                                           | snacks.picker call                                       |
|--------------------------------------------------------|----------------------------------------------------------|
| require("fzf-lua").files()                             | Snacks.picker.files()                                    |
| require("fzf-lua").live_grep({ rg_opts = ... })        | Snacks.picker.grep({ hidden = true, ignored = false })   |
| require("fzf-lua").files({ cwd = vim.fn.stdpath("config") }) | Snacks.picker.files({ cwd = vim.fn.stdpath("config") }) |
| require("fzf-lua").helptags()                          | Snacks.picker.help()                                     |
| require("fzf-lua").keymaps()                           | Snacks.picker.keymaps()                                  |
| require("fzf-lua").builtin()                           | Snacks.picker()                                          |
| require("fzf-lua").grep_cword()                        | Snacks.picker.grep_word()                                |
| require("fzf-lua").grep_cWORD()                        | Snacks.picker.grep_word()                                |
| require("fzf-lua").diagnostics_document()              | Snacks.picker.diagnostics()                              |
| require("fzf-lua").resume()                            | Snacks.picker.resume()                                   |
| require("fzf-lua").oldfiles()                          | Snacks.picker.recent()                                   |
| require("fzf-lua").buffers()                           | Snacks.picker.buffers()                                  |
| require("fzf-lua").lgrep_curbuf()                      | Snacks.picker.lines()                                    |
| require("fzf-lua").lsp_references(opts)                | Snacks.picker.lsp_references()                           |
| require("fzf-lua").lsp_definitions(opts)               | Snacks.picker.lsp_definitions()                          |
| require("fzf-lua").lsp_implementations(opts)           | Snacks.picker.lsp_implementations()                      |
| require("fzf-lua").lsp_typedefs(opts)                  | Snacks.picker.lsp_type_definitions()                     |
| require("fzf-lua").lsp_document_symbols(opts)          | Snacks.picker.lsp_symbols()                              |
| require("fzf-lua").lsp_live_workspace_symbols(opts)    | Snacks.picker.lsp_workspace_symbols()                    |

Validation harness PLUGIN_LIST (scripts/nvim-validate.sh line 21):
Current: {'notify','noice','lualine','neo-tree','lspconfig','conform','nvim-treesitter.configs','blink.cmp','fzf-lua','gitsigns','ufo','bufferline','which-key','alpha','render-markdown'}
Target:  {'snacks','lualine','neo-tree','lspconfig','conform','nvim-treesitter.configs','blink.cmp','gitsigns','ufo','bufferline','which-key','render-markdown'}

Smoke Lua script plugin list (scripts/nvim-validate.sh lines 239-243):
Current: 'notify','noice','lualine','neo-tree','lspconfig','conform','nvim-treesitter.configs','blink.cmp','fzf-lua','gitsigns','ufo','bufferline','which-key','alpha','render-markdown'
Target:  'snacks','lualine','neo-tree','lspconfig','conform','nvim-treesitter.configs','blink.cmp','gitsigns','ufo','bufferline','which-key','render-markdown'
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create snacks.lua spec and delete the 4 replaced plugin files</name>
  <files>.config/nvim/lua/plugins/snacks.lua, .config/nvim/lua/plugins/notify.lua, .config/nvim/lua/plugins/alpha.lua, .config/nvim/lua/plugins/indent-blankline.lua, .config/nvim/lua/plugins/fzflua.lua</files>
  <read_first>
    - .config/nvim/lua/plugins/notify.lua
    - .config/nvim/lua/plugins/alpha.lua
    - .config/nvim/lua/plugins/indent-blankline.lua
    - .config/nvim/lua/plugins/fzflua.lua
    - .planning/phases/05-ux-and-performance-polish/05-CONTEXT.md
    - .planning/phases/05-ux-and-performance-polish/05-RESEARCH.md
  </read_first>
  <action>
Create `.config/nvim/lua/plugins/snacks.lua` with the following exact content (implements D-04, D-05, D-07, D-08, D-09, D-10, D-11, D-12):

```lua
return {
  "folke/snacks.nvim",
  priority = 1000,
  lazy = false,
  ---@type snacks.Config
  opts = {
    notifier = {
      enabled = true,
      timeout = 3000,
      top_down = false, -- D-04: bottom-right toast accumulation
    },
    dashboard = {
      enabled = true, -- D-05: default/minimal dashboard, no ASCII art port
    },
    picker = {
      enabled = true, -- D-06: replaces fzf-lua; wired via registry
    },
    indent = {
      enabled = true, -- D-08: replaces indent-blankline
    },
    scroll = {
      enabled = true, -- D-10: smooth scroll for <C-d>/<C-u>
    },
    words = {
      enabled = true, -- D-09: LSP word highlights
    },
    lazygit = {
      enabled = true, -- D-07: wired in registry as <leader>gg
    },
    quickfile = {
      enabled = true, -- fast file render before full lazy load (UX-02)
    },
    image = {
      enabled = false, -- D-11: not needed
    },
    terminal = {
      enabled = false, -- D-12: kitty+tmux already provides terminal workflow
    },
    zen = {
      enabled = false, -- D-12: no established use case
    },
  },
}
```

Then delete the four replaced plugin spec files:
- `.config/nvim/lua/plugins/notify.lua` (contained noice.nvim + nvim-notify, replaced by snacks.notifier per D-04)
- `.config/nvim/lua/plugins/alpha.lua` (contained alpha-nvim, replaced by snacks.dashboard per D-05)
- `.config/nvim/lua/plugins/indent-blankline.lua` (contained ibl, replaced by snacks.indent per D-08)
- `.config/nvim/lua/plugins/fzflua.lua` (contained fzf-lua, replaced by snacks.picker per D-06)

Use `git rm` to delete these so they are staged for removal, not just removed from the working tree.

Do NOT delete `lualine.lua` or `colortheme.lua` — those are handled by Plan 02.
Do NOT add any `keys = {}` table inside snacks.lua — per D-22 all keymaps go through the central registry.
  </action>
  <verify>
    <automated>test -f .config/nvim/lua/plugins/snacks.lua && ! test -f .config/nvim/lua/plugins/notify.lua && ! test -f .config/nvim/lua/plugins/alpha.lua && ! test -f .config/nvim/lua/plugins/indent-blankline.lua && ! test -f .config/nvim/lua/plugins/fzflua.lua</automated>
  </verify>
  <acceptance_criteria>
    - `.config/nvim/lua/plugins/snacks.lua` exists
    - `.config/nvim/lua/plugins/snacks.lua` contains the string `"folke/snacks.nvim"`
    - `.config/nvim/lua/plugins/snacks.lua` contains the string `priority = 1000`
    - `.config/nvim/lua/plugins/snacks.lua` contains the string `lazy = false`
    - `.config/nvim/lua/plugins/snacks.lua` contains the string `top_down = false`
    - `.config/nvim/lua/plugins/snacks.lua` contains all of: `notifier`, `dashboard`, `picker`, `indent`, `scroll`, `words`, `lazygit`, `quickfile`, `image`, `terminal`, `zen`
    - `.config/nvim/lua/plugins/snacks.lua` does NOT contain the string `keys =`
    - `.config/nvim/lua/plugins/notify.lua` does NOT exist
    - `.config/nvim/lua/plugins/alpha.lua` does NOT exist
    - `.config/nvim/lua/plugins/indent-blankline.lua` does NOT exist
    - `.config/nvim/lua/plugins/fzflua.lua` does NOT exist
    - `git status --short .config/nvim/lua/plugins/` shows `D  notify.lua`, `D  alpha.lua`, `D  indent-blankline.lua`, `D  fzflua.lua`, and `A  snacks.lua` (or equivalent staged states)
  </acceptance_criteria>
  <done>snacks.lua spec committed with all nine enabled modules + three disabled modules per D-04..D-12; four replaced plugin files deleted; no plugin-local keys table in snacks.lua.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewire registry.lua fzf-lua entries to Snacks.picker and add lazygit entry</name>
  <files>.config/nvim/lua/core/keymaps/registry.lua</files>
  <read_first>
    - .config/nvim/lua/core/keymaps/registry.lua
    - .config/nvim/lua/core/keymaps/lazy.lua
    - .planning/phases/05-ux-and-performance-polish/05-RESEARCH.md
  </read_first>
  <action>
Open `.config/nvim/lua/core/keymaps/registry.lua` and update every entry that references `"ibhagwan/fzf-lua"` or `require("fzf-lua")`. The `id`, `lhs`, `mode`, `desc`, `domain`, `scope` fields MUST NOT change — only `plugin` and `action` change.

**Search domain entries in `M.lazy` (change `plugin = "ibhagwan/fzf-lua"` → `plugin = "folke/snacks.nvim"` and rewrite the `action` body as shown):**

| id | new action body |
|----|-----------------|
| `search.files` | `function() Snacks.picker.files() end` |
| `search.grep` | `function() Snacks.picker.grep({ hidden = true, ignored = false }) end` |
| `search.config` | `function() Snacks.picker.files({ cwd = vim.fn.stdpath("config") }) end` |
| `search.help` | `function() Snacks.picker.help() end` |
| `search.keymaps` | `function() Snacks.picker.keymaps() end` |
| `search.builtin` | `function() Snacks.picker() end` |
| `search.word` | `function() Snacks.picker.grep_word() end` |
| `search.WORD` | `function() Snacks.picker.grep_word() end` |
| `search.diagnostics` | `function() Snacks.picker.diagnostics() end` |
| `search.resume` | `function() Snacks.picker.resume() end` |
| `search.oldfiles` | `function() Snacks.picker.recent() end` |
| `search.buffers` | `function() Snacks.picker.buffers() end` |
| `search.grep_curbuf` | `function() Snacks.picker.lines() end` |

**Buffer-local LSP entries in `M.buffer` (no `plugin` field to change; only the `action` body changes):**

| id | new action body |
|----|-----------------|
| `lsp.references` | `function() Snacks.picker.lsp_references() end` |
| `lsp.implementations` | `function() Snacks.picker.lsp_implementations() end` |
| `lsp.definition` | `function() Snacks.picker.lsp_definitions() end` |
| `lsp.typedefs` | `function() Snacks.picker.lsp_type_definitions() end` |
| `lsp.doc_symbols` | `function() Snacks.picker.lsp_symbols() end` |
| `lsp.workspace_symbols` | `function() Snacks.picker.lsp_workspace_symbols() end` |

**Add new lazygit entry to `M.lazy`** (implements D-07). Insert near the existing Git domain block (after `git.toggle_blame`):

```lua
{
  id = "git.lazygit",
  lhs = "<leader>gg",
  mode = "n",
  desc = "Open LazyGit",
  domain = "g",
  scope = "lazy",
  plugin = "folke/snacks.nvim",
  action = function() Snacks.lazygit() end,
},
```

After the rewrite, `grep -n 'fzf-lua' .config/nvim/lua/core/keymaps/registry.lua` MUST return zero matches. `grep -n 'ibhagwan/fzf-lua' .config/nvim/lua/core/keymaps/registry.lua` MUST return zero matches. All 13 search entries MUST have `plugin = "folke/snacks.nvim"`.

Notes on the lazy.lua compiler (`.config/nvim/lua/core/keymaps/lazy.lua`): the compiler at lines 20-31 does `pcall(require, map.plugin)` where `map.plugin` is the plugin identifier. Since `folke/snacks.nvim` cannot be `require`d by that path (the Lua module is `snacks`, not `folke/snacks.nvim`), the compiler will fall through to the `type(map.action) == "function"` branch and call `map.action()` directly. This is correct because `Snacks.picker.*` works through the global `Snacks` table set up by snacks.nvim's `lazy = false` init, not via `require`. Do NOT modify lazy.lua — the fallback path already handles this case.
  </action>
  <verify>
    <automated>bash -c 'set -e; ! grep -q "fzf-lua" .config/nvim/lua/core/keymaps/registry.lua; ! grep -q "ibhagwan" .config/nvim/lua/core/keymaps/registry.lua; grep -q "Snacks.picker.files" .config/nvim/lua/core/keymaps/registry.lua; grep -q "Snacks.picker.grep" .config/nvim/lua/core/keymaps/registry.lua; grep -q "Snacks.picker.lsp_references" .config/nvim/lua/core/keymaps/registry.lua; grep -q "Snacks.lazygit" .config/nvim/lua/core/keymaps/registry.lua; grep -q "git.lazygit" .config/nvim/lua/core/keymaps/registry.lua'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fzf-lua" .config/nvim/lua/core/keymaps/registry.lua` outputs `0`
    - `grep -c "ibhagwan" .config/nvim/lua/core/keymaps/registry.lua` outputs `0`
    - `grep -c "folke/snacks.nvim" .config/nvim/lua/core/keymaps/registry.lua` outputs `14` or more (13 search entries + 1 lazygit entry)
    - `grep -c "Snacks.picker" .config/nvim/lua/core/keymaps/registry.lua` outputs `19` or more (13 search + 6 lsp)
    - registry.lua contains `id = "git.lazygit"` and `lhs = "<leader>gg"`
    - registry.lua contains `Snacks.picker.files()`, `Snacks.picker.grep`, `Snacks.picker.files({ cwd = vim.fn.stdpath("config") })`, `Snacks.picker.help()`, `Snacks.picker.keymaps()`, `Snacks.picker()`, `Snacks.picker.grep_word()`, `Snacks.picker.diagnostics()`, `Snacks.picker.resume()`, `Snacks.picker.recent()`, `Snacks.picker.buffers()`, `Snacks.picker.lines()`
    - registry.lua contains `Snacks.picker.lsp_references()`, `Snacks.picker.lsp_implementations()`, `Snacks.picker.lsp_definitions()`, `Snacks.picker.lsp_type_definitions()`, `Snacks.picker.lsp_symbols()`, `Snacks.picker.lsp_workspace_symbols()`
    - All original `id`/`lhs` pairs preserved: `grep -c 'lhs = "<leader>ff"' .config/nvim/lua/core/keymaps/registry.lua` outputs `1`; same for `<leader>fg`, `<leader>fc`, `<leader>fh`, `<leader>fk`, `<leader>fb`, `<leader>fw`, `<leader>fW`, `<leader>fd`, `<leader>fr`, `<leader>fo`, `<leader><leader>`, `<leader>/`, `<leader>cr`, `<leader>ci`, `<leader>cd`, `grt`, `gO`, `gW`
    - `luac -p .config/nvim/lua/core/keymaps/registry.lua` exits 0 (syntax valid; use `nvim --headless -c "lua dofile('.config/nvim/lua/core/keymaps/registry.lua')" +qa` if luac unavailable)
  </acceptance_criteria>
  <done>All 19 fzf-lua references in registry.lua replaced with Snacks.picker equivalents; lazygit entry added; keymap surface identical (lhs/id/desc unchanged); file parses cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update validation harness PLUGIN_LIST + smoke list and run Lazy sync to install snacks</name>
  <files>scripts/nvim-validate.sh</files>
  <read_first>
    - scripts/nvim-validate.sh
    - .planning/phases/05-ux-and-performance-polish/05-VALIDATION.md
  </read_first>
  <action>
Open `scripts/nvim-validate.sh` and make two changes:

**Change 1 (line 21):** Replace the current `PLUGIN_LIST` string:

Current:
```bash
PLUGIN_LIST="{'notify','noice','lualine','neo-tree','lspconfig','conform','nvim-treesitter.configs','blink.cmp','fzf-lua','gitsigns','ufo','bufferline','which-key','alpha','render-markdown'}"
```

Target:
```bash
PLUGIN_LIST="{'snacks','lualine','neo-tree','lspconfig','conform','nvim-treesitter.configs','blink.cmp','gitsigns','ufo','bufferline','which-key','render-markdown'}"
```

(Removed: `'notify'`, `'noice'`, `'fzf-lua'`, `'alpha'`. Added: `'snacks'` at the front.)

**Change 2 (lines 239-243):** Replace the plugin list inside the smoke Lua heredoc:

Current:
```lua
local plugins = {
  'notify','noice','lualine','neo-tree','lspconfig','conform',
  'nvim-treesitter.configs','blink.cmp','fzf-lua','gitsigns',
  'ufo','bufferline','which-key','alpha','render-markdown',
}
```

Target:
```lua
local plugins = {
  'snacks','lualine','neo-tree','lspconfig','conform',
  'nvim-treesitter.configs','blink.cmp','gitsigns',
  'ufo','bufferline','which-key','render-markdown',
}
```

Both lists MUST be kept in sync — they probe the same set.

**After both file edits, run `:Lazy sync` headlessly so snacks.nvim is installed and the old plugins are cleaned from the lazy cache:**

```bash
bash scripts/nvim-validate.sh sync
```

This will invoke lazy.nvim's sync operation (install new + clean removed) with a 120s timeout. Expect log `PASS: sync OK`. If it fails, print the tail of `.planning/tmp/nvim-validate/sync.log` and stop — a failed sync typically means `snacks.lua` has a syntax error or an invalid opts key.

**Then run the smoke test:**

```bash
bash scripts/nvim-validate.sh smoke
```

Expected `PASS: smoke OK`. If the smoke still references `'notify'` or any removed plugin the task is not done.

**Then run health:**

```bash
bash scripts/nvim-validate.sh health
```

Expected `PASS: health OK` with `snacks` in the plugin list and all listed plugins loaded=true.

Do NOT touch `lualine.lua` (that is Plan 02's work) — the noice pcall block inside lualine.lua already degrades gracefully per Phase 3 D-08, so its continued presence will not break snacks migration.
  </action>
  <verify>
    <automated>bash -c 'set -e; ! grep -q "notify" scripts/nvim-validate.sh; ! grep -q "noice" scripts/nvim-validate.sh; ! grep -q "fzf-lua" scripts/nvim-validate.sh; ! grep -q "'"'"'alpha'"'"'" scripts/nvim-validate.sh; grep -q "'"'"'snacks'"'"'" scripts/nvim-validate.sh; bash scripts/nvim-validate.sh sync && bash scripts/nvim-validate.sh smoke && bash scripts/nvim-validate.sh health'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'notify'" scripts/nvim-validate.sh` outputs `0`
    - `grep -c "'noice'" scripts/nvim-validate.sh` outputs `0`
    - `grep -c "'fzf-lua'" scripts/nvim-validate.sh` outputs `0`
    - `grep -c "'alpha'" scripts/nvim-validate.sh` outputs `0`
    - `grep -c "'snacks'" scripts/nvim-validate.sh` outputs `2` (once in PLUGIN_LIST, once in smoke list)
    - `bash scripts/nvim-validate.sh sync` exits 0 and last line contains `PASS: sync OK`
    - `bash scripts/nvim-validate.sh smoke` exits 0 and last line contains `PASS: smoke OK`
    - `bash scripts/nvim-validate.sh health` exits 0 and last line contains `PASS: health OK`
    - `.planning/tmp/nvim-validate/health.json` contains `"name":"snacks"` and `"loaded":true` for snacks
    - `.planning/tmp/nvim-validate/health.json` does NOT contain the string `"name":"notify"` or `"name":"noice"` or `"name":"fzf-lua"` or `"name":"alpha"`
  </acceptance_criteria>
  <done>Harness probe lists reference only plugins that still exist; `sync`, `smoke`, and `health` subcommands all pass green; snacks.nvim installed into `~/.local/share/nvim/lazy/snacks.nvim`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem ↔ Neovim | Lua plugin files are loaded at startup; a malformed file can crash nvim or brick UX |
| lazy.nvim ↔ remote GitHub | `:Lazy sync` fetches folke/snacks.nvim from github.com over HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `.config/nvim/lua/plugins/snacks.lua` | mitigate | Plugin spec uses verbatim opts from folke/snacks.nvim README; `luac -p` or headless dofile check in Task 1 acceptance criteria confirms syntax; Task 3 `sync` then `smoke` exits non-zero if snacks fails to load |
| T-05-02 | Denial of Service | Registry keymaps after rewrite | mitigate | Acceptance criteria in Task 2 enforce that all 19 lhs values remain wired; `nvim --headless -c "lua dofile(...)"` parse check detects syntax breakage before runtime |
| T-05-03 | Information Disclosure | `Snacks.picker.grep` hidden-file default | accept | Grep action explicitly sets `hidden = true, ignored = false` to preserve existing fzf-lua behavior; hidden files already accessible on the local filesystem — no new disclosure surface |
| T-05-04 | Spoofing | Remote fetch of folke/snacks.nvim | accept | lazy.nvim pins to the commit in lazy-lock.json after sync; maintainer controls lock file via git; low risk for a solo-developer personal dotfiles repo |
| T-05-05 | Elevation of Privilege | snacks.lazygit shells out to lazygit | accept | lazygit binary already installed and trusted (per CLAUDE.md arch/tools.sh); snacks.lazygit opens an interactive TUI — no automation path for unintended command execution |
</threat_model>

<verification>
After all three tasks complete:

1. `bash scripts/nvim-validate.sh all` prints `==> all PASS`.
2. Interactive smoke (manual, defers to next wave Plan 03): open `nvim`, confirm dashboard appears, press `<leader>ff` → snacks.picker files opens; press `<leader>gg` → lazygit float opens; type `:echo` and press Enter → bottom-right notification fires (if applicable).
3. `lazy-lock.json` contains a `snacks.nvim` entry and does NOT contain `noice.nvim`, `nvim-notify`, `alpha-nvim`, `indent-blankline.nvim`, or `fzf-lua` after sync settles.
</verification>

<success_criteria>
- snacks.lua spec loads all nine enabled submodules per D-04..D-10
- Four replaced plugin files deleted and removed from lazy-lock.json
- All 19 fzf-lua registry entries rewired to Snacks.picker.* with identical lhs/desc
- `<leader>gg` wired to Snacks.lazygit via registry (D-07)
- Validation harness probes updated in lockstep (D-13, D-23)
- `scripts/nvim-validate.sh all` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-ux-and-performance-polish/05-01-SUMMARY.md`.
</output>
