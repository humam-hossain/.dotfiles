---
phase: 01-reliability-and-portability-baseline
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .config/nvim/README.md
  - .planning/phases/01-reliability-and-portability-baseline/01-VALIDATION.md
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
  - CORE-01
  - CORE-02
  - CORE-03
must_haves:
  truths:
    - "Maintainer can read one Phase 1 smoke matrix covering Arch Linux, Debian/Ubuntu, and Windows."
    - "Maintainer can verify buffer, window, tab, and autosave behavior against explicit documented expectations."
    - "Phase validation artifacts contain runnable automated commands plus clearly scoped manual OS checks."
  artifacts:
    - path: ".config/nvim/README.md"
      provides: "User-facing documentation for Phase 1 portability and lifecycle behavior"
    - path: ".planning/phases/01-reliability-and-portability-baseline/01-VALIDATION.md"
      provides: "Updated Nyquist-compliant verification contract for Phase 1"
  key_links:
    - from: ".config/nvim/README.md"
      to: ".planning/phases/01-reliability-and-portability-baseline/01-VALIDATION.md"
      via: "matching smoke scenarios and command names"
      pattern: "FocusLost|<C-q>|<C-S-o>"
    - from: ".planning/phases/01-reliability-and-portability-baseline/01-VALIDATION.md"
      to: "Phase 1 implementation files"
      via: "grep and headless verification commands"
      pattern: "core/open.lua|core/keymaps.lua|plugins/neotree.lua|plugins/bufferline.lua"
---

<objective>
Document the Phase 1 portability and lifecycle baseline so the behavior is testable across supported operating systems.

Purpose: Turn the implemented policy into a repeatable smoke contract instead of tribal knowledge, covering both automated headless checks and manual OS-specific verification.
Output: Updated Neovim README guidance and a Nyquist-compliant Phase 1 validation document.
</objective>

<execution_context>
@/home/pera/.codex/get-shit-done/workflows/execute-plan.md
@/home/pera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reliability-and-portability-baseline/01-CONTEXT.md
@.planning/phases/01-reliability-and-portability-baseline/01-RESEARCH.md
@.planning/phases/01-reliability-and-portability-baseline/01-VALIDATION.md
@.config/nvim/README.md
@.config/nvim/lua/core/open.lua
@.config/nvim/lua/core/keymaps.lua
@.config/nvim/lua/plugins/neotree.lua
@.config/nvim/lua/plugins/bufferline.lua
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the Neovim README with the Phase 1 behavior contract</name>
  <files>.config/nvim/README.md</files>
  <read_first>
.planning/phases/01-reliability-and-portability-baseline/01-CONTEXT.md
.config/nvim/README.md
.config/nvim/lua/core/keymaps.lua
.config/nvim/lua/core/open.lua
.config/nvim/lua/plugins/neotree.lua
.config/nvim/lua/plugins/bufferline.lua
  </read_first>
  <action>Add a compact Phase 1 section to `.config/nvim/README.md` that documents the exact lifecycle model from D-01 through D-12: `<C-q>` closes the current buffer with confirmation, `<leader>xs` closes only the current split, tabs are explicit workspaces and not affected by normal buffer-close commands, autosave runs only on guarded `FocusLost` for normal file buffers, and both `<C-S-o>` and neo-tree `<c-o>` use the same OS-aware external-open helper. Include one short smoke checklist for Arch Linux, Debian/Ubuntu, and Windows using those exact commands and keymaps.</action>
  <verify>
    <automated>rg -n "Phase 1|<C-q>|<leader>xs|<C-S-o>|neo-tree|FocusLost|Windows|Arch|Debian|Ubuntu" .config/nvim/README.md</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "Phase 1" .config/nvim/README.md` matches at least one heading or label.
- `rg -n "<C-q>|<leader>xs|<C-S-o>|<c-o>|FocusLost" .config/nvim/README.md` matches all documented behaviors.
- `rg -n "Windows|Arch Linux|Debian|Ubuntu" .config/nvim/README.md` matches the supported-platform smoke matrix text.
  </acceptance_criteria>
  <done>The README explains the new lifecycle/open behavior in concrete terms and gives the maintainer one place to read the supported Phase 1 smoke steps.</done>
</task>

<task type="auto">
  <name>Task 2: Make the Phase 1 validation contract Nyquist-compliant and implementation-aware</name>
  <files>.planning/phases/01-reliability-and-portability-baseline/01-VALIDATION.md</files>
  <read_first>
.planning/phases/01-reliability-and-portability-baseline/01-RESEARCH.md
.planning/phases/01-reliability-and-portability-baseline/01-VALIDATION.md
.config/nvim/lua/core/open.lua
.config/nvim/lua/core/keymaps.lua
.config/nvim/lua/plugins/neotree.lua
.config/nvim/lua/plugins/bufferline.lua
  </read_first>
  <action>Rewrite `.planning/phases/01-reliability-and-portability-baseline/01-VALIDATION.md` so it reflects the shipped Phase 1 implementation instead of the draft assumptions. Set frontmatter `nyquist_compliant: true` and `wave_0_complete: true`. Replace the stale per-task verification map with exact commands that check for the new helper and lifecycle rules, including at minimum: `nvim --headless "+qa"`, a grep proving no `xdg-open` remains in `.config/nvim/lua`, a grep proving `confirm bdelete` is present in `core/keymaps.lua` and `plugins/bufferline.lua`, and a grep proving only `FocusLost` remains for autosave. Keep Windows verification manual, but make the manual table explicit about `<C-S-o>`, neo-tree `<c-o>`, `<C-q>` on modified and unmodified buffers, split close via `<leader>xs`, and tab isolation. Keep command chains as standalone lines in Markdown; do not use placeholder text like "manual-only" without the exact scenario names.</action>
  <verify>
    <automated>rg -n "nyquist_compliant: true|wave_0_complete: true|nvim --headless|xdg-open|confirm bdelete|FocusLost|<C-q>|<leader>xs|<C-S-o>" .planning/phases/01-reliability-and-portability-baseline/01-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "nyquist_compliant: true|wave_0_complete: true" .planning/phases/01-reliability-and-portability-baseline/01-VALIDATION.md` matches both frontmatter fields.
- `rg -n "nvim --headless" .planning/phases/01-reliability-and-portability-baseline/01-VALIDATION.md` matches at least one automated command.
- `rg -n "core/open.lua|core/keymaps.lua|plugins/neotree.lua|plugins/bufferline.lua" .planning/phases/01-reliability-and-portability-baseline/01-VALIDATION.md` matches the implementation-aware checks.
- `rg -n "<C-q>|<leader>xs|<C-S-o>|<c-o>|Windows|Arch Linux|Debian|Ubuntu" .planning/phases/01-reliability-and-portability-baseline/01-VALIDATION.md` matches the manual smoke matrix.
  </acceptance_criteria>
  <done>The validation file is updated to the final Phase 1 behavior, contains exact automated and manual checks, and is marked Nyquist-compliant.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented verification commands -> maintainer execution | Validation docs must describe the real commands and behaviors, otherwise false confidence is created |
| manual smoke matrix -> supported OS environments | OS-specific verification can drift unless the scenarios match the implemented keymaps and helper names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | R | `.planning/phases/01-reliability-and-portability-baseline/01-VALIDATION.md` | mitigate | Record exact commands and scenario names so verification evidence is reproducible instead of subjective. |
| T-01-08 | D | `.config/nvim/README.md` | mitigate | Document the explicit split between buffer close, split close, and tab behavior so users do not trigger destructive commands by assumption. |
</threat_model>

<verification>
- Run `node /home/pera/.codex/get-shit-done/bin/gsd-tools.cjs verify plan-structure .planning/phases/01-reliability-and-portability-baseline/01-03-PLAN.md` after writing the plan.
- After execution, run the commands listed in `01-VALIDATION.md` and confirm the README behavior descriptions match the implementation files.
</verification>

<success_criteria>
- Phase 1 has a readable behavior contract in `.config/nvim/README.md`.
- Phase 1 validation artifacts contain exact automated commands and explicit manual OS smoke scenarios.
- The documentation matches the implementation files produced by Plans 01-01 and 01-02.
</success_criteria>

<output>
After completion, create `.planning/phases/01-reliability-and-portability-baseline/01-03-SUMMARY.md`
</output>
