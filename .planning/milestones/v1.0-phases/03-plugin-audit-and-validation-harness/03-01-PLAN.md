---
phase: 03-plugin-audit-and-validation-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md
  - .planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md
autonomous: true
requirements:
  - PLUG-01
tags: [plugins, audit, neovim, inventory]

must_haves:
  truths:
    - "Maintainer can see an explicit keep, remove, or replace decision for every effective plugin currently specified in .config/nvim/lua/plugins/*.lua"
    - "Known drift items (duplicate vim-fugitive, catppuccin lockfile mismatch, noice even= typo, hackerman.nvim dormancy) are each recorded in the ledger with a decision"
    - "Every ledger row has plugin repo, source file, feature domain, dependencies, lazy trigger, external tool dependency, risks, decision, rationale, and follow-up action"
    - "Audit rules document captures the aggressive-posture criteria (novelty, redundancy, weak justification, stale, drift-prone = removal candidates by default)"
  artifacts:
    - path: ".planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md"
      provides: "Full plugin inventory with per-plugin keep/remove/replace decision and rationale (D-10, D-11, D-12)"
      contains: "| Plugin | Decision |"
    - path: ".planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md"
      provides: "Aggressive audit criteria so future plugin additions are judged by the same standard (D-01, D-02, D-03)"
      contains: "## Removal Criteria"
  key_links:
    - from: "03-PLUGIN-AUDIT.md"
      to: ".config/nvim/lua/plugins/*.lua"
      via: "Source File column cites the exact spec file owning each plugin"
      pattern: "lua/plugins/.*\\.lua"
    - from: "03-PLUGIN-AUDIT.md"
      to: ".config/nvim/lazy-lock.json"
      via: "Lockfile Status column records whether entry is present, missing, duplicated, or misspelled"
      pattern: "catppucin|lockfile"
---

<objective>
Produce the Phase 3 plugin inventory ledger and aggressive audit rules document. Every effective plugin specified under `.config/nvim/lua/plugins/*.lua` receives an explicit keep, remove, or replace decision with rationale recorded, duplicate declarations are normalized, and lockfile drift items are cross-referenced. This plan is documentation-only — no Neovim config code changes — and it is the source of truth that Plans 02 and 03 will execute against.

Purpose: Phase 3 success criterion 1 (explicit keep/remove/replace decision for every plugin) depends entirely on this ledger existing. Without it, Plan 03's lockfile refresh and missing-tool hardening cannot select targets defensibly.

Output: `.planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md` and `.planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-plugin-audit-and-validation-harness/03-CONTEXT.md
@.planning/phases/03-plugin-audit-and-validation-harness/03-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STACK.md
@.config/nvim/lua/plugins/alpha.lua
@.config/nvim/lua/plugins/blink-cmp.lua
@.config/nvim/lua/plugins/bufferline.lua
@.config/nvim/lua/plugins/colortheme.lua
@.config/nvim/lua/plugins/conform.lua
@.config/nvim/lua/plugins/fzflua.lua
@.config/nvim/lua/plugins/git.lua
@.config/nvim/lua/plugins/indent-blankline.lua
@.config/nvim/lua/plugins/lsp.lua
@.config/nvim/lua/plugins/lualine.lua
@.config/nvim/lua/plugins/misc.lua
@.config/nvim/lua/plugins/neotree.lua
@.config/nvim/lua/plugins/notify.lua
@.config/nvim/lua/plugins/project.lua
@.config/nvim/lua/plugins/treesitter.lua
@.config/nvim/lua/plugins/ufo.lua
@.config/nvim/lua/plugins/vim-indent-object.lua
@.config/nvim/lazy-lock.json

<interfaces>
<!-- Known effective plugin set the audit must cover. Each must appear as at least one row. -->
<!-- Source: enumeration of lua/plugins/*.lua and lazy-lock.json as of 2026-04-15. -->

Plugins declared in lua/plugins/*.lua (must each have a decision):
- alpha.lua:           goolord/alpha-nvim, nvim-tree/nvim-web-devicons (dep)
- blink-cmp.lua:       saghen/blink.cmp, rafamadriz/friendly-snippets, moyiz/blink-emoji.nvim
- bufferline.lua:      akinsho/bufferline.nvim, moll/vim-bbye, nvim-tree/nvim-web-devicons (dep)
- colortheme.lua:      catppuccin/nvim, bjarneo/hackerman.nvim, bjarneo/aether.nvim (dep)
- conform.lua:         stevearc/conform.nvim
- fzflua.lua:          ibhagwan/fzf-lua, nvim-tree/nvim-web-devicons (dep)
- git.lua:             tpope/vim-fugitive (ALSO DECLARED IN misc.lua — duplicate), lewis6991/gitsigns.nvim
- indent-blankline.lua:lukas-reineke/indent-blankline.nvim
- lsp.lua:             neovim/nvim-lspconfig, mason-org/mason.nvim, mason-org/mason-lspconfig.nvim, WhoIsSethDaniel/mason-tool-installer.nvim, j-hui/fidget.nvim, saghen/blink.cmp (dep ref)
- lualine.lua:         vimpostor/vim-tpipeline, nvim-lualine/lualine.nvim, lewis6991/gitsigns.nvim (dep ref), nvim-tree/nvim-web-devicons (dep)
- misc.lua:            christoomey/vim-tmux-navigator, tpope/vim-sleuth, tpope/vim-fugitive (DUPLICATE), tpope/vim-rhubarb, folke/which-key.nvim, windwp/nvim-autopairs, folke/todo-comments.nvim, norcalli/nvim-colorizer.lua, MeanderingProgrammer/render-markdown.nvim, mluders/comfy-line-numbers.nvim, hat0uma/csvview.nvim
- neotree.lua:         nvim-neo-tree/neo-tree.nvim, nvim-lua/plenary.nvim (dep), nvim-tree/nvim-web-devicons (dep), MunifTanjim/nui.nvim (dep), 3rd/image.nvim (optional), s1n7ax/nvim-window-picker (dep)
- notify.lua:          folke/noice.nvim (HAS even= TYPO), MunifTanjim/nui.nvim (dep), rcarriga/nvim-notify (dep)
- project.lua:         ahmedkhalf/project.nvim
- treesitter.lua:      nvim-treesitter/nvim-treesitter
- ufo.lua:             kevinhwang91/nvim-ufo, kevinhwang91/promise-async (dep)
- vim-indent-object.lua: michaeljsmith/vim-indent-object

Lockfile-only entries (no matching spec today, likely dead):
- LuaSnip (no direct spec found; may be transitive via friendly-snippets)
- telescope.nvim, telescope-fzf-native.nvim, telescope-ui-select.nvim (fzf-lua is used instead — Phase 1/README says "fzf-lua is used instead of Telescope")
- none-ls.nvim (no spec; likely orphaned)
- hererocks (lazy.nvim internal; usually fine to leave)
- catppucin (MISSPELLED lock entry for catppuccin)
- lazydev.nvim (no spec found)
</interfaces>

<!-- Only the two output docs in this plan are authoritative. No code changes. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write aggressive audit rules document</name>
  <files>.planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md</files>
  <read_first>
    - .planning/phases/03-plugin-audit-and-validation-harness/03-CONTEXT.md  (decisions D-01 through D-12)
    - .planning/phases/03-plugin-audit-and-validation-harness/03-RESEARCH.md (aggressive audit signals)
    - .planning/codebase/CONCERNS.md                                          (existing drift evidence)
  </read_first>
  <action>
    Create `.planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md` with these exact top-level sections (in this order):

    1. `# Phase 3 Plugin Audit Rules` — one-paragraph preamble stating: "Phase 3 uses an aggressive audit posture. Plugins do not remain by inertia. Every plugin must earn its keep based on strong day-to-day value, reliability, and fit with the cleaned-up cross-platform config (per D-01, D-02, D-03)."
    2. `## Decision Values` — define exactly three decisions with one-sentence semantics each:
       - `keep`: "Plugin earns its place now; no Phase 3 action beyond recording; may still receive Phase 4 config modernization."
       - `remove`: "Plugin is deleted from spec files and lockfile in Plan 03-03; no replacement planned inside v1 scope."
       - `replace`: "Plugin is deleted in Plan 03-03 and a replacement is deferred to Phase 4 with the replacement target named in the rationale."
    3. `## Removal Criteria (Default Disposition)` — bulleted list. A plugin is a removal candidate by default if ANY apply (per D-03):
       - Novelty-only value (fun/cosmetic with no daily workflow benefit)
       - Redundant with another plugin already kept (e.g. duplicate declaration, feature overlap)
       - Weak or missing justification for cross-platform shared config
       - Stale upstream (no commits in 12+ months) or archived repo
       - Drift-prone: large option surface, fragile cross-plugin coupling, or unreliable lazy-load
       - Optional/heavy dependencies that degrade on Linux/Windows parity (e.g. image.nvim, 3rd-party binaries)
       - Lockfile-only orphan (pinned in `lazy-lock.json` with no matching spec in `lua/plugins/*.lua`)
    4. `## Keep Criteria` — bulleted list. A plugin earns keep only if ALL apply:
       - Provides daily-workflow value (editing, navigation, LSP, git, completion, file tree, search, or visible UI users depend on)
       - Works reliably on Arch Linux, Debian/Ubuntu, and Windows without platform-specific forks
       - Actively maintained upstream OR stable enough that "no commits" means "complete", not "abandoned"
       - No functionally superior alternative already present in the keep set
    5. `## Replace Criteria` — bulleted list. A plugin is `replace` (not `remove`) when:
       - Its domain is still required (LSP, completion, tree, etc.) but the current choice fails keep criteria
       - The replacement target is named explicitly (even if the swap itself is deferred to Phase 4)
    6. `## Duplicate Resolution Policy` — state: "When the same plugin is declared in multiple spec files, the inventory records one row per declaration site with decision `remove` on all but one, and the surviving declaration is chosen by owner-file fit (a domain file like `git.lua` beats a sprawl bucket like `misc.lua`)."
    7. `## Lockfile-Only Entry Policy` — state: "Entries present in `lazy-lock.json` with no corresponding `lua/plugins/*.lua` spec are treated as `remove` in the inventory (orphan pins) unless they are lazy.nvim internals (e.g. `hererocks`)."
    8. `## Handoff To Phase 4` — state: "This document plus `03-PLUGIN-AUDIT.md` is the sole input Plan 03-03 uses to decide which specs to delete and which lockfile entries to prune. Any plugin marked `replace` must not have its replacement installed in Phase 3; that work is deferred to Phase 4."

    Use plain Markdown, no YAML frontmatter, no emoji. This document is static text only; do NOT reference plugin-specific decisions here — those live in 03-PLUGIN-AUDIT.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md && grep -Fq "Phase 3 Plugin Audit Rules" .planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md && grep -Fq "Removal Criteria" .planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md && grep -Fq "Keep Criteria" .planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md && grep -Fq "Replace Criteria" .planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md && grep -Fq "Duplicate Resolution Policy" .planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md && grep -Fq "Lockfile-Only Entry Policy" .planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md && grep -Fq "Handoff To Phase 4" .planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md` exists
    - Contains literal heading `# Phase 3 Plugin Audit Rules`
    - Contains literal heading `## Decision Values` with all three values (`keep`, `remove`, `replace`) defined
    - Contains literal heading `## Removal Criteria (Default Disposition)` with 7 bullet criteria
    - Contains literal heading `## Keep Criteria`
    - Contains literal heading `## Replace Criteria`
    - Contains literal heading `## Duplicate Resolution Policy`
    - Contains literal heading `## Lockfile-Only Entry Policy`
    - Contains literal heading `## Handoff To Phase 4`
    - Contains the strings `D-01`, `D-02`, `D-03` linking back to locked decisions
    - No mention of specific plugin names (those belong in 03-PLUGIN-AUDIT.md)
  </acceptance_criteria>
  <done>Audit rules document is committed, follows the aggressive posture from D-01..D-03, and is the decision framework that Task 2 applies to every plugin.</done>
</task>

<task type="auto">
  <name>Task 2: Write full plugin inventory ledger with keep/remove/replace decisions</name>
  <files>.planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md</files>
  <read_first>
    - .planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md  (criteria this ledger applies)
    - .planning/phases/03-plugin-audit-and-validation-harness/03-CONTEXT.md
    - .planning/phases/03-plugin-audit-and-validation-harness/03-RESEARCH.md     (concrete drift findings section)
    - .planning/codebase/CONCERNS.md
    - .config/nvim/lua/plugins/alpha.lua
    - .config/nvim/lua/plugins/blink-cmp.lua
    - .config/nvim/lua/plugins/bufferline.lua
    - .config/nvim/lua/plugins/colortheme.lua
    - .config/nvim/lua/plugins/conform.lua
    - .config/nvim/lua/plugins/fzflua.lua
    - .config/nvim/lua/plugins/git.lua
    - .config/nvim/lua/plugins/indent-blankline.lua
    - .config/nvim/lua/plugins/lsp.lua
    - .config/nvim/lua/plugins/lualine.lua
    - .config/nvim/lua/plugins/misc.lua
    - .config/nvim/lua/plugins/neotree.lua
    - .config/nvim/lua/plugins/notify.lua
    - .config/nvim/lua/plugins/project.lua
    - .config/nvim/lua/plugins/treesitter.lua
    - .config/nvim/lua/plugins/ufo.lua
    - .config/nvim/lua/plugins/vim-indent-object.lua
    - .config/nvim/lazy-lock.json
  </read_first>
  <action>
    Create `.planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md` with these exact top-level sections:

    1. `# Phase 3 Plugin Inventory and Audit Ledger`
    2. `## Scope and Method` — two-paragraph preamble: (a) state that every plugin declared in `.config/nvim/lua/plugins/*.lua` and every entry in `.config/nvim/lazy-lock.json` is covered; (b) reference `03-AUDIT-RULES.md` as the decision framework (per D-01, D-02, D-03, D-10, D-11, D-12).
    3. `## Inventory By Domain` — one subsection per domain heading below. Under each subsection include one Markdown table with these EXACT columns in this order:

       `| Plugin | Source File | Lazy Trigger | Dependencies / Coupling | External Tool Deps | Known Risks / Drift | Decision | Rationale | Phase 4 Follow-up |`

       Domain subsections required (create all of them):
       - `### Startup / Dashboard` (alpha-nvim)
       - `### Completion` (blink.cmp, friendly-snippets, blink-emoji.nvim, LuaSnip lockfile orphan)
       - `### Buffers / Tabs` (bufferline.nvim, vim-bbye)
       - `### Theme` (catppuccin, hackerman.nvim, aether.nvim)
       - `### Formatting` (conform.nvim, none-ls.nvim lockfile orphan)
       - `### Search / Fuzzy` (fzf-lua, telescope.nvim + telescope-fzf-native + telescope-ui-select lockfile orphans, telescope-ui-select orphan)
       - `### Git` (vim-fugitive in git.lua, vim-fugitive DUPLICATE in misc.lua, gitsigns.nvim, vim-rhubarb)
       - `### Indentation UI` (indent-blankline.nvim, vim-indent-object, vim-sleuth)
       - `### LSP` (nvim-lspconfig, mason.nvim, mason-lspconfig.nvim, mason-tool-installer.nvim, fidget.nvim, lazydev.nvim lockfile orphan)
       - `### Statusline / Messaging` (vim-tpipeline, lualine.nvim, noice.nvim [note `even=` typo], nui.nvim, nvim-notify)
       - `### File Tree` (neo-tree.nvim, plenary.nvim, nvim-web-devicons, nui.nvim shared dep, image.nvim optional, nvim-window-picker)
       - `### Project / Navigation` (project.nvim, vim-tmux-navigator)
       - `### Tree-sitter` (nvim-treesitter)
       - `### Folding` (nvim-ufo, promise-async)
       - `### Which-Key / Discovery` (which-key.nvim)
       - `### Editing Helpers` (nvim-autopairs, todo-comments.nvim, nvim-colorizer.lua, render-markdown.nvim, comfy-line-numbers.nvim, csvview.nvim)
       - `### Lazy.nvim Internals` (hererocks, lazy.nvim itself if pinned — leave with decision `keep` and rationale "plugin manager")

    4. `## Duplicate Resolution` — explicit subsection listing:
       - `tpope/vim-fugitive`: declared in BOTH `.config/nvim/lua/plugins/git.lua` AND `.config/nvim/lua/plugins/misc.lua`. Keep the declaration in `git.lua` (domain fit). Mark the declaration in `misc.lua` as `remove`. Plan 03-03 will delete the misc.lua entry only.

    5. `## Lockfile Drift and Orphans` — explicit subsection listing each lazy-lock.json entry with no matching spec in `lua/plugins/*.lua`:
       - `catppucin`: MISSPELLED lockfile key; active spec is `catppuccin`. Plan 03-03 removes the misspelled entry and regenerates a correct `catppuccin` pin.
       - `telescope.nvim`, `telescope-fzf-native.nvim`, `telescope-ui-select.nvim`: superseded by fzf-lua; decision `remove` (orphan pins).
       - `none-ls.nvim`: no matching spec; decision `remove` (orphan pin).
       - `LuaSnip`: no direct spec but referenced transitively by friendly-snippets/blink-cmp; decision `keep` if transitive, `remove` if lazy.nvim no longer resolves it after spec-clean — Plan 03-03 verifies empirically via `Lazy! sync`.
       - `lazydev.nvim`: no matching spec; decision `remove` (orphan pin).
       - `hererocks`: lazy.nvim internal; decision `keep`.

    6. `## Decision Summary` — final counts table:
       `| Decision | Count |`
       `|----------|-------|`
       with totals for `keep`, `remove`, `replace` across the entire ledger.

    Rules the executor MUST follow when filling cells:
    - `Decision` cell value is EXACTLY one of `keep`, `remove`, `replace` (lowercase, no emoji)
    - `Rationale` cell MUST explicitly invoke at least one rule from `03-AUDIT-RULES.md` (e.g. "novelty-only value", "redundant with fzf-lua", "lockfile orphan", "duplicate declaration — git.lua wins")
    - `Lazy Trigger` cell records the actual field from the spec: `event = "..."`, `keys = ...`, `cmd = {...}`, `lazy = false`, or `eager`; for `noice.nvim` record the literal value `even = "VeryLazy" (TYPO)` so Plan 03-03 knows to fix it
    - `Known Risks / Drift` must cite concrete evidence where it exists: the `even=` typo in `notify.lua`, the `catppucin` lockfile mismatch, the duplicate fugitive declaration, image.nvim cross-platform weakness, large option tables in neotree/lsp/neotree
    - `Phase 4 Follow-up` cell is `-` for pure keeps or removes, and names the replacement target for any `replace` decision
    - Rows for pure transitive dependencies (`plenary.nvim`, `nvim-web-devicons`, `nui.nvim`, `promise-async`, `aether.nvim`) are still included with decision `keep` and rationale "required dependency of <kept plugin>"

    Apply the aggressive audit posture consistently. Do not issue `keep` for anything failing any Keep Criterion from `03-AUDIT-RULES.md`; use `remove` or `replace` instead. The user explicitly forbids implicit keeps (D-12).
  </action>
  <verify>
    <automated>test -f .planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md && grep -Fq "# Phase 3 Plugin Inventory and Audit Ledger" .planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md && grep -Fq "## Duplicate Resolution" .planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md && grep -Fq "## Lockfile Drift and Orphans" .planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md && grep -Fq "## Decision Summary" .planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md && grep -Fq "vim-fugitive" .planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md && grep -Fq "catppucin" .planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md && grep -Fq "even = \"VeryLazy\"" .planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md && grep -Ec "^\| " .planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md | awk '{ if ($1 < 40) { print "FAIL: too few table rows (" $1 "); need coverage of every plugin"; exit 1 } }' && awk 'BEGIN{k=0;r=0;rp=0} /\| keep \|/{k++} /\| remove \|/{r++} /\| replace \|/{rp++} END{ if (k+r+rp < 30) { print "FAIL: fewer than 30 rows with decision values"; exit 1 }; print "decisions: keep="k" remove="r" replace="rp }' .planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md` exists
    - Contains heading `# Phase 3 Plugin Inventory and Audit Ledger`
    - Contains heading `## Scope and Method`
    - Contains heading `## Inventory By Domain` with all 17 domain subsections listed in the action
    - Contains heading `## Duplicate Resolution` that explicitly lists `tpope/vim-fugitive` with `git.lua` as the winner and `misc.lua` as the removal target
    - Contains heading `## Lockfile Drift and Orphans` that explicitly names `catppucin`, `telescope.nvim`, `none-ls.nvim`, `lazydev.nvim`, `hererocks`, and `LuaSnip`
    - Contains heading `## Decision Summary` with a counts table
    - Contains the literal string `even = "VeryLazy"` (so Plan 03-03 can grep for the fix target)
    - Contains the literal string `catppucin` (so Plan 03-03 can grep for the lockfile fix target)
    - Every row in every domain table has the `Decision` column populated with exactly one of `keep`, `remove`, or `replace` (no empty cells, no `TBD`, no placeholder text)
    - Total decision rows across tables is >= 30 (covers full plugin surface including transitive deps and lockfile orphans)
    - Rationale cells reference rules from `03-AUDIT-RULES.md` (grep shows strings like `removal candidate`, `redundant`, `orphan`, or `required dependency`)
    - No row uses the string `TBD`, `???`, or `pending` as the decision
  </acceptance_criteria>
  <done>Every effective plugin has an explicit, rationale-backed keep/remove/replace decision. The ledger is complete enough that Plan 03-03 can mechanically delete specs and prune lockfile entries by reading this file alone (no re-interpretation needed).</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/03-plugin-audit-and-validation-harness/03-AUDIT-RULES.md`
- `test -f .planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md`
- Every plugin file under `.config/nvim/lua/plugins/` appears by name at least once in `03-PLUGIN-AUDIT.md`
- Every lockfile key in `.config/nvim/lazy-lock.json` appears by name at least once in `03-PLUGIN-AUDIT.md`
- `grep -c '| keep\b\|| remove\b\|| replace\b' 03-PLUGIN-AUDIT.md` is at least 30
- Duplicate `vim-fugitive` and misspelled `catppucin` are both explicitly called out with a `Phase 4 Follow-up` (for Plan 03-03 handoff)
</verification>

<success_criteria>
- Phase 3 success criterion 1 ("Maintainer has an explicit keep/remove/replace decision for every current plugin") is fully satisfied by this plan's deliverables
- PLUG-01 requirement is covered end-to-end
- No plugin decision is implicit; no row uses placeholder text (D-12 honored)
- Audit rules document is static and reusable for Phase 4 additions
</success_criteria>

<output>
After completion, create `.planning/phases/03-plugin-audit-and-validation-harness/03-01-SUMMARY.md` including:
- List of every plugin keep-decision-count vs remove-decision-count vs replace-decision-count
- Named handoffs for Plan 03-03 (duplicate fugitive removal, catppucin lockfile fix, noice even= typo)
- Any plugin where the audit rules made the decision genuinely borderline so Phase 4 reviewers know where judgment was exercised
</output>
