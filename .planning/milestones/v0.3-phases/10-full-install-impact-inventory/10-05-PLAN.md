---
phase: 10-full-install-impact-inventory
plan: 05
type: execute
wave: 5
depends_on:
  - "10-01"
  - "10-02"
  - "10-03"
  - "10-04"
# Final single-writer after 02→03→04 chain completes all axes.
files_modified:
  - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md
  - .planning/phases/10-full-install-impact-inventory/10-VALIDATION.md
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03
  - INV-04
user_setup: []

must_haves:
  truths:
    - "Dated host snapshot section lists live ~/.config presence relevant to inventory axes (D-03)"
    - "Every inventory effect row has a concrete Source cite (D-07) — no empty Source cells in final tables"
    - "UNKNOWN section retains incomplete-proof rows (hyprlock/ dir legacy gap; exp-files deltas) without dropping them (D-04)"
    - "No Waybar/rofi/swaync clash rows (D-15); no disposition recommendations (D-12)"
    - "SAFE_DEFAULTS residual still present and accurate (INV-04)"
    - "Full suite ./scripts/phase10-inventory-assert.sh --full exits 0"
    - statement: "Spot-check Host present? columns against live machine at finalize time"
      verification: backstop
  artifacts:
    - path: ".planning/phases/10-full-install-impact-inventory/10-INVENTORY.md"
      provides: "Final committed neutral full-install impact inventory"
    - path: ".planning/phases/10-full-install-impact-inventory/10-VALIDATION.md"
      provides: "Wave/task status updates toward phase gate"
  key_links:
    - from: "Host snapshot"
      to: "live ~/.config"
      via: "read-only find/test -e"
      pattern: "Host present"
    - from: "Sources section"
      to: "wrapper + vendor setup paths"
      via: "canonical cite list"
      pattern: "3.files-legacy.sh"
  prohibitions:
    - statement: "Do not perform live full install or drop SAFE_DEFAULTS"
      status: active
      verification: "finalize is docs-only"
    - statement: "Do not add recommended staged full profile (Phase 11 DISP-02)"
      status: active
      verification: "D-12 neutral"
    - statement: "Do not center dual-run chrome"
      status: active
      verification: "D-15"
    - statement: "Do not move inventory to docs/"
      status: active
      verification: "D-01 path"
---

<objective>
Finalize `10-INVENTORY.md`: dated host snapshot, UNKNOWN notes, Source completeness cross-check, D-12/D-15 lint, full assert suite green — ready for Phase 11 dispositions.

Purpose: Close Phase 10 success criteria with a single neutral committed inventory.

Output: Final `10-INVENTORY.md` + VALIDATION.md status updates.
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-full-install-impact-inventory/10-CONTEXT.md
@.planning/phases/10-full-install-impact-inventory/10-RESEARCH.md
@.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md
@.planning/phases/10-full-install-impact-inventory/10-VALIDATION.md
@.planning/ROADMAP.md
@scripts/phase10-inventory-assert.sh
</context>

## Artifacts this phase produces

| Artifact | Path | Notes |
|----------|------|-------|
| Final inventory | `10-INVENTORY.md` | All INV-01..04 + D-03 snapshot |
| Validation status | `10-VALIDATION.md` | Task map / sign-off progress |

## Assumptions (specless)

- Optional setups side effects (`2.setups.sh`) may appear as short LOW-priority note — do not block
- Repo `.config` dual column not required (D-06)

<tasks>

<task type="auto" id="10-05-01">
  <name>Task 1: Host snapshot, UNKNOWN, cross-check, full assert</name>
  <files>.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md, .planning/phases/10-full-install-impact-inventory/10-VALIDATION.md</files>
  <read_first>
    - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md (all axes)
    - .planning/phases/10-full-install-impact-inventory/10-CONTEXT.md (D-03, D-04, D-07, D-12, D-15)
    - .planning/phases/10-full-install-impact-inventory/10-RESEARCH.md (Open Questions RESOLVED, host scan recipe, pitfalls)
    - .planning/phases/10-full-install-impact-inventory/10-PATTERNS.md (read-only host scan anti-patterns; phase07 analog)
    - .planning/ROADMAP.md (Phase 10 success criteria)
    - scripts/phase10-inventory-assert.sh
    - scripts/phase07-live-smoke.sh (PASS/FAIL labels only — do not copy LIVE chrome asserts)
    - Live host: re-run read-only presence for all inventoried paths
  </read_first>
  <action>
    Finalize inventory:

    0. **`--full` host printer contract (PATTERNS):** When running or extending assert `--full`, follow `10-PATTERNS.md` anti-patterns and phase07 style: read-only `test -e` / `printf` PRESENT/ABSENT only; no chrome checklist (waybar/rofi/swaync); no rsync/cp/mv/rm into XDG; no setup without `--dry-run`.

    1. **Host snapshot (D-03):** Replace stub with dated section (today’s date). Summarize live `~/.config` collisions relevant to axes (hypr tree highlights, firstrun marker, misc PRESENT set, optional pacman meta presence). Do not dual-column repo `.config` (D-06).

    2. **Refresh Host present? columns** across Axis A/B/C tables from fresh read-only scan (A4).

    3. **UNKNOWN / research notes (D-04):** Ensure at least:
       - Legacy does not install `hyprlock/` directory while upstream conf may source it
       - `--exp-files` behavioral deltas are notes only (not primary structure)
       - Any row still unproven stays UNKNOWN with recheck source — never drop

    4. **Source completeness (D-07):** Walk every effect table row; empty or “TBD” Source cells are bugs — fill with file:line or host observation.

    5. **Lint pass:**
       - No waybar/rofi/swaync
       - No disposition recommendations
       - Residual INV-04 still accurate
       - Three axes remain independent (no “drop all three” recommendation)

    6. **Sources section:** Complete primary cite list (wrapper, options, legacy, helpers, install-deps, host scan date).

    7. **VALIDATION.md:** Mark task rows done where green; note full suite command; leave formal nyquist_compliant true only if `--full` passes.

    8. Run `./scripts/phase10-inventory-assert.sh --full` and fix inventory until exit 0.

    Do not mutate XDG. Do not live-install. Do not implement Phase 11/12 work.
  </action>
  <verify>
    <automated>./scripts/phase10-inventory-assert.sh --full</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/phase10-inventory-assert.sh --full` exits 0
    - Host snapshot section contains an ISO-like or explicit calendar date
    - UNKNOWN section non-empty with hyprlock/ dir gap
    - `rg -n 'Host present' .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md` matches
    - `rg -ni 'waybar|rofi|swaync' 10-INVENTORY.md` has no matches
    - Residual still contains SAFE_DEFAULTS triple
    - ROADMAP success criteria 1–4 are satisfiable by reading the inventory alone
  </acceptance_criteria>
  <done>Phase 10 inventory finalized, assert --full green, ready for Phase 11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Final markdown → Phase 11 | Must remain neutral facts |
| Full assert --full → host | Read-only presence printer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | finalize temptation to “just install” | high | mitigate | Docs-only; assert read-only |
| T-10-02 | Tampering | disposition language creep | medium | mitigate | D-12 final lint |
| T-10-03 | Information Disclosure | oversized conf paste | low | mitigate | Snapshot summaries not full conf dumps |
</threat_model>

<verification>
Full assert green; inventory alone satisfies ROADMAP Phase 10 success criteria 1–4.
</verification>

<success_criteria>
Committed neutral `10-INVENTORY.md` with residual, three axes, host snapshot, UNKNOWNs, full sources — no mutation, no dispositions, no chrome.
</success_criteria>

<output>
Create `.planning/phases/10-full-install-impact-inventory/10-05-SUMMARY.md` when done
</output>
