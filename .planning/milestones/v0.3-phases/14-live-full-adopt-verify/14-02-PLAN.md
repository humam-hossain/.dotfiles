---
phase: 14-live-full-adopt-verify
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - scripts/phase14-verify.sh
  - .planning/phases/14-live-full-adopt-verify/14-LIVE-VERIFY.md
  - .planning/phases/14-live-full-adopt-verify/14-ADOPT-TRANSCRIPT.txt
autonomous: false
requirements: [ADOPT-01, ADOPT-02, ADOPT-03, ADOPT-04]

user_setup:
  - service: local-machine-console
    why: "The mutating full install is operator-executed from a bare TTY with Hyprland stopped (D-01, D-06). No agent invocation may run it."
    dashboard_config:
      - task: "Work the go/no-go checklist and state the decision aloud so it lands in the transcript"
        location: "docs/phase14-adopt-runbook.md section 3, inside the script(1) session"
      - task: "Stop Hyprland, switch to a bare TTY, and run the adopt sequence"
        location: "Physical console on this machine, repo root"
      - task: "Apply the Phase 13 overlay before relogin, re-stow kitty, reboot, log in via start-hyprland"
        location: "docs/phase14-adopt-runbook.md sections 8-11"

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "The Hyprland session running after the adopt came from the ii Lua entry, proven three independent ways: `~/.config/hypr/hyprland.conf.old` exists, `~/.config/hypr/hyprland.lua` exists, and `hyprctl` no longer reports the pre-adopt config provider (ADOPT-02, D-33)."
    - "`~/.config/hypr/hyprland.conf` no longer exists, so no `.conf` can win over the Lua entry regardless of which the compositor would prefer (ADOPT-02, RESEARCH Open Question 1)."
    - "Enforced: DP-1 is present in `hyprctl -j monitors all` and all eleven Phase 13 workspace rules are live in the running compositor, proving the overlay loaded rather than merely being copied. A missing monitor or a missing rule is a hard failure (ADOPT-03)."
    - "Recorded only: DP-1's live scale is compared against `dp1_scale_pre` from the fixture and any difference is emitted as a `[FINDING]` with a written disposition in `14-LIVE-VERIFY.md`. A wrong scale never fails the phase — it is a Phase 15 item (D-14)."
    - "`qs -c ii` is running and neither `waybar` nor `swaync` is running, matching the accept-remove dual-run policy (ADOPT-03, Phase 11 D-11)."
    - "Rollback tier-1 inputs are proven to exist rather than assumed: `hyprland.conf.old` is present, the repo pre-adopt archive is present, and `~/ii-original-dots-backup/.config/hypr/hyprland.conf` is newer than the install start and matches the recorded pre-adopt sha256 (ADOPT-04, D-26, D-36)."
    - "`./arch/dots-hyprland.sh uninstall --dry-run` and `./arch/dots-hyprland.sh protect --dry-run` both exit 0, so rollback tiers 2 and 3 are reachable (ADOPT-04, D-26)."
    - "Phase 11 D-24 held: live `hyprlock.conf` and `hypridle.conf` are byte-identical to the recorded pre-adopt sha256, and unpromoted `.new` sidecars exist beside them (D-37)."
    - "The repo working tree is clean apart from this phase's transcript and verify artifacts (D-35)."
    - "Every named known loss is confirmed rather than assumed, and the screen-share portal is probed explicitly; a broken portal is a recorded finding, not a phase failure (D-38)."
    - "`14-LIVE-VERIFY.md` holds the verify script's output plus written findings, and the raw `script(1)` transcript is committed (D-11, D-31)."
  artifacts:
    - scripts/phase14-verify.sh
    - .planning/phases/14-live-full-adopt-verify/14-LIVE-VERIFY.md
    - .planning/phases/14-live-full-adopt-verify/14-ADOPT-TRANSCRIPT.txt
  key_links:
    - "`scripts/phase14-verify.sh` reads `14-PRE-ADOPT-BASELINE.txt` from plan 14-01; without that fixture D-36 and D-37 are circular, because after the install the live pre-adopt conf exists only inside the backup the check is meant to validate."
    - "The `hyprctl` probes are the only link between file existence and the running session; `test -f hyprland.lua` alone proves the install, not the load (D-33)."
    - "`14-LIVE-VERIFY.md` is the artifact `/gsd-verify-work` reads for ADOPT-03's manual half; the script output alone does not satisfy D-29."
  prohibitions:
    - requirement_id: ADOPT-03
      category: safety
      status: resolved
      verification: judgment
      statement: "The post-adopt verify script must not mutate live session or config state to make an assertion pass. It probes only: no `pkill`, no `hyprctl keyword`, no `hyprctl reload`, no writes under `~/.config`."
    - requirement_id: ADOPT-02
      category: safety
      status: resolved
      verification: judgment
      statement: "Verification must not report a pass for a condition it could not observe. An unobservable condition — a disconnected monitor, an unanswerable portal, a post-adopt token nobody has seen yet — records an informational line or a finding, never a pass and never a silently-skipped check."
---

<objective>
Prove the live adopt actually landed, on the machine, after the operator's window — and record
what was found, including what was lost.

Purpose: plan 14-01 prepared everything; this plan closes ADOPT-02, ADOPT-03, and ADOPT-04's
input half against real post-adopt state. It cannot start until the operator has run the adopt
window, which may be days after 14-01 completes (D-04). The success bar is D-32: a usable ii
desktop, the Phase 13 overlay applied, Waybar/rofi/swaync gone, and findings logged — not
everything perfect.

Output: `scripts/phase14-verify.sh`, a committed `script(1)` transcript, and
`14-LIVE-VERIFY.md` carrying script output plus written findings.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-live-full-adopt-verify/14-CONTEXT.md
@.planning/phases/14-live-full-adopt-verify/14-RESEARCH.md
@.planning/phases/14-live-full-adopt-verify/14-PATTERNS.md
@.planning/phases/14-live-full-adopt-verify/14-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Gate — go or hold on the one-way adopt window (ADOPT-01, D-01, D-18..D-22)</name>
  <decision>Open the adopt window and run the live full install, or hold</decision>
  <context>
    This is the one-way door of the whole milestone. The run changes live `~/.config`, system
    packages, and the Hyprland session entry. Undoing it is the three-tier rollback in D-23, not a
    re-run. Everything before this point — Phases 10 through 13 and plan 14-01 — was reversible
    preparation.

    Plan 14-01 has produced: `docs/phase14-adopt-runbook.md` (pushed to `origin/main`, and copyable
    to `~/phase14-rollback.txt` for a bare TTY), `scripts/phase14-preflight.sh`,
    `14-PRE-ADOPT-BASELINE.txt`, the pre-adopt `.config` archive, and the `PROTECT_EXPLICIT` edit.

    One gate covers the entire window: install, overlay apply, reboot, first login, verification
    (D-21). There is no second mini-gate later. The preflight exit code is one input to this
    decision, not the decision itself (D-18).

    Read the preflight's output at all three levels, not just its exit code. `[FAIL]` moves the
    exit code; `[FINDING]` does not, and one finding is load-bearing here. The backup directory
    `~/ii-original-dots-backup` exists and holds a `hyprland.conf` older than the live one. Upstream
    skips its backup entirely when that directory is present and `ask` has been flipped false, so
    going without rotating costs rollback tier 1 its third source. Plan 14-01 deliberately reports
    this rather than failing on it: clearing it means a `$HOME` mutation that belongs inside this
    window and nowhere else, so it is a hard no-go on the runbook checklist instead of an exit code
    an agent could clear early. Rotating it is step 5 of the runbook.
  </context>
  <options>
    <option id="go">
      <name>Go — open the window now</name>
      <pros>The hyprlang `.conf` format is slated for removal in Hyprland 0.57 and this machine runs 0.56.2, so the runway is short. All prep artifacts are current, and `./scripts/phase14-preflight.sh` exits 0 with every mechanical go condition green — the one outstanding item is the backup-directory `[FINDING]`, which runbook step 5 clears inside this window. Rollback inputs are in place and proven.</pros>
      <cons>One-way. If the session does not come up, the recovery path is the three tiers, and the desktop may be unusable in between. Needs an uninterrupted block of time and a second device able to reach GitHub.</cons>
    </option>
    <option id="hold">
      <name>Hold — close the window without running</name>
      <pros>Nothing has mutated. `14-01`'s artifacts stay valid; only the volatile live observations in the baseline fixture age. Re-run the preflight when the window reopens.</pros>
      <cons>Phase 14 stays open and ADOPT-02/03 stay unproven. The baseline fixture must be regenerated if live `~/.config/hypr` changes in the meantime.</cons>
    </option>
  </options>
  <resume-signal>Reply "go" to open the window, or "hold" to stop here.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Operator runs the adopt window from a bare TTY, recorded with script(1)</name>
  <reversibility rating="one-way">The run changes live `~/.config`, system packages, and the Hyprland session entry (D-01). Undoing it means the three-tier rollback in D-23, not a re-run. The `checkpoint:decision` immediately above is the gate on this door.</reversibility>
  <precondition>`docs/phase14-adopt-runbook.md` is present on `origin/main` (plan 14-01 pushed it), and the operator has physical console access to this machine plus a second device that can reach GitHub.</precondition>
  <action>Run the adopt window from a bare TTY, following `docs/phase14-adopt-runbook.md` end to end</action>
  <instructions>
    I have prepared everything that can be prepared. The mutating install is yours to run — no
    agent invocation may run it (D-01), and nothing I have executed has changed your live system
    beyond the repo-side archive and the wrapper's protection list.

    Work `docs/phase14-adopt-runbook.md` from section 1 to section 12, in order, without
    reordering. The short form of what you are about to do:

    1. Copy the runbook to a plain local file so section 14 is readable if the desktop dies:
       `cp docs/phase14-adopt-runbook.md ~/phase14-rollback.txt`.
    2. Start the recording: `script(1)`, output-only, writing to
       `.planning/phases/14-live-full-adopt-verify/14-ADOPT-TRANSCRIPT.txt`. Do not use `--log-in`
       or `--log-io` — the install runs `sudo pacman` and those modes record your password.
    3. Run `./scripts/phase14-preflight.sh`. Read every line, not just the exit code — `[FINDING]`
       lines do not move it and one of them is a hard no-go. Work the go/no-go checklist in runbook
       section 3. Say the decision out loud so it lands in the transcript (D-19).
    4. Rotate the backup: `./scripts/phase14-preflight.sh --rotate-backup`. Mandatory whenever the
       preflight reported the `ii-original-dots-backup` line as a `[FINDING]`, which it does today
       because the directory exists with a stale `hyprland.conf` inside it. Then re-run
       `./scripts/phase14-preflight.sh` and confirm that line has flipped to `[PASS]`. Skip only if
       the preflight already reported the directory absent. This is the step that makes upstream's
       backup run on either `ask` branch, which is what keeps rollback tier 1's third source alive.
    5. Stop Hyprland and switch to a bare TTY. Not from inside a live session (D-06).
    6. Run the wrapper's `install` subcommand with the `--full` meta flag and no other flags.
       Answer `y` at the greeting. Never type `yesforall`. Never pass `-f`/`--force`,
       `--skip-backup`, `-F`/`--firstrun`, or `--skip-hyprland-entry` (D-22). If it dies partway,
       capture the output and re-run the identical command — do not hand-patch and do not roll back
       on the first failure (D-07).
    7. Apply the Phase 13 overlay before relogin, using the fence in
       `.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md` §"Apply command (D-18)".
       Named-file copy only, never a delete-sync.
    8. `stow -R kitty` from the repo root (D-17). This restores your personal kitty config, which
       upstream's sync replaced. `starship.toml` is deliberately not protected — its repo copy is
       overwritten and that loss is accepted.
    9. Reboot, unconditionally.
    10. Log in at a TTY and run `start-hyprland`.
    11. Optionally connect HDMI-A-2 first if you want the dual-head half of ADOPT-03 checked; it is
        currently not attached and the verify script degrades gracefully without it.
    12. Exit the `script(1)` session.

    Before you tell me you are done, read the transcript once for host paths, tokens, or anything
    you would not put in a public repo. It is about to be committed.
  </instructions>
  <verification>
    I will check, without changing anything: `~/.config/hypr/hyprland.conf.old` exists,
    `~/.config/hypr/hyprland.lua` exists, `~/.config/hypr/hyprland.conf` is gone,
    `~/.config/hypr/custom/general.lua` matches the repo copy, and
    `.planning/phases/14-live-full-adopt-verify/14-ADOPT-TRANSCRIPT.txt` is non-empty.
  </verification>
  <resume-signal>Reply "done" when you are logged into the new session, or paste the failure output if the install did not complete.</resume-signal>
</task>

<task type="tracer">
  <name>Task 3: End-to-end ADOPT-02 proof — verify script probes the running session and returns an exit code</name>
  <files>scripts/phase14-verify.sh</files>
  <read_first>
    - `scripts/phase12-full-smoke.sh` lines 1-16 and 159-163 — the preamble, `pass`/`fail`, and exit contract to copy.
    - `scripts/phase13-d19-assert.sh` lines 1-22 — the explicit non-mutation header line and the named-constant block shape.
    - `scripts/phase14-preflight.sh` — plan 14-01's script, so the two share idioms rather than diverging.
    - `.planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt` — the recorded facts this script compares against.
    - `.planning/phases/14-live-full-adopt-verify/14-RESEARCH.md` §"Code Examples" → "ADOPT-02 three-way proof (D-33)" — verified probe bodies with the pre-adopt baselines captured verbatim. Use them; do not re-derive.
  </read_first>
  <action>
    Wire ONE proof path end to end: recorded fixture -> live compositor probe -> pass/fail -> exit
    code. Production quality, not a stub — this is the script plan 14-02 finishes with.

    Create `scripts/phase14-verify.sh`, executable, `#!/usr/bin/env bash`, `set -euo pipefail`,
    with the repo's standard header: one-line purpose, an explicit constraint line stating the
    script is read-only against the live session and never kills a process, reloads the compositor,
    or writes under `~/.config`, a `Usage (from REPO_ROOT):` block, and the exit-code contract.
    Then `REPO_ROOT` resolution and `cd "$REPO_ROOT"`, the `FAIL` accumulator with `pass()` and
    `fail()`, and the third level `FINDINGS` with `finding()` that never touches `FAIL`.

    Declare named constants before the checks:
    `XDG="${XDG_CONFIG_HOME:-$HOME/.config}"`,
    `BASELINE=".planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt"`,
    `BACKUP_DIR="$HOME/ii-original-dots-backup"`, `WRAP="./arch/dots-hyprland.sh"`.
    Add a small reader that pulls a value out of `$BASELINE` by key with an anchored grep and
    hard-fails when the key is absent, so a missing fixture is a loud failure rather than an empty
    comparison that silently passes.

    Implement the ADOPT-02 block, five checks, every label prefixed `ADOPT-02`:
    1. `test -f "$XDG/hypr/hyprland.conf.old"` — the rename happened.
    2. `test -f "$XDG/hypr/hyprland.lua"` — the Lua entry installed; its absence means
       `--skip-hyprland-entry` leaked past the ban.
    3. `test ! -f "$XDG/hypr/hyprland.conf"` — nothing remains that could win over the Lua entry.
       This assertion is correct regardless of which format Hyprland 0.56.2 prefers, and it catches
       a failed rename directly.
    4. Config-provider probe. Read `configProvider_pre` from the fixture, then compare it against
       `hyprctl -j status | jq -r '.configProvider'`. Fail when the live value still equals the
       recorded pre-adopt value. Assert the absence of the known-bad value, never the presence of a
       guessed-good one — the pre-adopt token is an observed fact, the post-adopt token is not, and
       hard-coding a guess would produce a spurious failure.
    5. Lua REPL probe, independent of any token. Run `hyprctl eval 'return 1+1'` capturing stderr,
       and fail when the output contains the refusal text meaning the Lua config manager is not in
       use. Pass otherwise, echoing what came back.

    Close with the standard done line echoing both `FAIL` and `FINDINGS`, `exit 1` when `FAIL` is
    above zero, else `exit 0`. `FINDINGS` must never influence the exit code.

    Write `Waybar/rofi/swaync` when naming the bar stack anywhere in this file (D-39).
  </action>
  <verify>
    <automated>bash -n scripts/phase14-verify.sh && test -x scripts/phase14-verify.sh && grep -c 'pkill\|hyprctl reload\|hyprctl keyword' scripts/phase14-verify.sh | grep -qx 0 && ./scripts/phase14-verify.sh</automated>
    <fails_when>non-zero exit — `bash -n` prints a syntax error, the script is not executable, the script contains any process-kill or compositor-mutation call, or the run prints an `[FAIL] ADOPT-02` line and exits 1 because `hyprland.conf.old` or `hyprland.lua` is missing, `hyprland.conf` still exists, `hyprctl -j status` still reports the recorded pre-adopt config provider, or `hyprctl eval` came back refused</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/phase14-verify.sh` exists, is mode `755`, and its header carries an explicit read-only constraint line.
    - The file defines `pass`, `fail`, and `finding`, and `finding` does not increment `FAIL`.
    - The script hard-fails with a named message when `14-PRE-ADOPT-BASELINE.txt` is absent or a required key is missing.
    - `grep -c 'configProvider_pre' scripts/phase14-verify.sh` is at least 1 — the comparison reads the fixture rather than hard-coding a token.
    - The run prints five `[PASS] ADOPT-02` lines and exits 0.
    - `grep -cE 'pkill|hyprctl reload|hyprctl keyword|rm -rf|rsync .*--delete' scripts/phase14-verify.sh` is 0.
  </acceptance_criteria>
  <done>ADOPT-02 is proven three independent ways against the running session, by a script that changes nothing, and the whole chain from recorded fixture to exit code works.</done>
</task>

<task type="auto">
  <name>Task 4: Expand the verify script to ADOPT-03, the ADOPT-04 rollback inputs, and the D-35 through D-38 blocks</name>
  <files>scripts/phase14-verify.sh</files>
  <read_first>
    - `scripts/phase14-verify.sh` — the file created in Task 3, so this extends it.
    - `.planning/phases/14-live-full-adopt-verify/14-RESEARCH.md` §"Code Examples" → "ADOPT-03", "D-37", and "D-38" — verified probe bodies with pre-adopt baselines captured verbatim. Use them; do not re-derive.
    - `.planning/phases/14-live-full-adopt-verify/14-RESEARCH.md` §"Common Pitfalls" 3, 5, 6, 7 — HDMI-A-2 absent, `hyprpaper` stopped-but-installed, the launcher never had an autostart, and the deleted-but-tracked launch script.
    - `.config/hypr/custom/general.lua` — the eleven workspace rules and two monitor declarations the running compositor must now reflect.
    - `arch/dots-hyprland.sh` lines 58-95 — the `uninstall` and `protect` flag surface for the tier-2 and tier-3 reachability probes.
  </read_first>
  <action>
    Extend `scripts/phase14-verify.sh` with five more blocks. Every check stays read-only. Every
    label carries its block prefix so the output is greppable.

    ADOPT-03 block. Assert DP-1 is present in `hyprctl -j monitors all` unconditionally. Compare its
    live scale against `dp1_scale_pre` from the fixture and route any difference to `finding()`, not
    `fail()` — a wrong DP-1 scale is recorded and deferred, never a phase blocker (D-14). Handle
    HDMI-A-2 conditionally: when it is absent from `monitors all`, print an informational line
    stating the dual-head assertion was skipped and why; when it is present, compare its scale and
    transform against the overlay's declared values and route a mismatch to `finding()`. Assert the
    workspace rules unconditionally — `hyprctl workspacerules` reports rules for monitors that are
    not attached, so all eleven are checkable single-headed. Check each of workspaces 1 through 10
    plus the special social workspace, and `fail()` on any missing rule. Assert the ii shell is
    running via `pgrep -f`; that check's label text must contain the literal `qs -c ii`, pinned the
    same way every other greppable token in this task is pinned — the verify greps the label, not
    the probe. Assert the bar daemon and the notification daemon are each not running
    via `pgrep -x`, failing if either is, because the dual-run policy is accept-remove. Probe the
    launcher process too, but route a hit to `finding()` rather than `fail()`: it never had an
    autostart, so its absence is vacuous and its presence would be the surprise.

    ADOPT-04 input block (D-26 — prove the inputs, never rehearse the restore). Assert
    `$XDG/hypr/hyprland.conf.old` exists and is non-empty (tier-1 source 1). Assert repo
    `.config/hypr/hyprland.conf` exists and is non-empty (tier-1 source 2). Assert `$BACKUP_DIR`
    exists and is non-empty (tier-1 source 3). Run `"$WRAP" uninstall --dry-run` and
    `"$WRAP" protect --dry-run`, each captured into its own `mktemp` under a `trap ... EXIT`, and
    fail on a non-zero exit from either — that is tier 2 and tier 3 reachability.

    D-36 block. Read `hyprland_conf_sha256` and `baseline_captured` from the fixture. Assert
    `$BACKUP_DIR/.config/hypr/hyprland.conf` exists, that its sha256 equals the recorded pre-adopt
    value, and that its mtime is newer than the recorded `backup_dir_hyprland_conf_mtime` from the
    fixture. This is the check that catches a silently skipped backup and turns rollback tier 1 from
    assumed-good into checked-good. All three conditions are hard failures.

    D-37 block. Read `hyprlock_conf_sha256`, `hyprlock_conf_bytes`, `hypridle_conf_sha256`, and
    `hypridle_conf_bytes` from the fixture. Assert live `$XDG/hypr/hyprlock.conf` and
    `$XDG/hypr/hypridle.conf` match both their recorded byte size and their recorded sha256 — a
    mismatch means the firstrun path fired and Phase 11 D-24 did not hold. Assert
    `$XDG/hypr/hyprlock.conf.new` and `$XDG/hypr/hypridle.conf.new` exist, which is the not-firstrun
    branch's signature, and that neither has been promoted over its live counterpart.

    D-38 block, every check routing to `finding()` and never to `fail()`. Probe
    `systemctl --user is-active graphical-session.target`; an inactive target means the personal
    session unit's autostart died with the renamed conf, which is expected, and it is why screen
    share may be broken. Probe the ScreenCast portal with
    `busctl --user get-property org.freedesktop.portal.Desktop /org/freedesktop/portal/desktop org.freedesktop.portal.ScreenCast AvailableSourceTypes`
    and compare against `screencast_source_types_pre` from the fixture. Also assert, informationally,
    that `stow/systemd/.config/systemd/user/hyprland-session.service` still exists in the repo — the
    unit file survives, only its autostart is lost, and the operator should not read this as a
    deletion. Print a named known-loss line for each of: `wl-clip-persist`, the four
    workspace-pinned autostarts, `hyprpaper` as stopped-but-installed, and
    `hyprland/scripts/launch_first_available.sh` as deleted from live but tracked identically in the
    repo.

    D-35 block. Assert `git status --porcelain` is empty apart from paths under
    `.planning/phases/14-live-full-adopt-verify/`. Filter with an anchored match on that prefix
    rather than a blanket allowance. A dirty tree outside the phase directory is a hard `fail` whose
    message says to review the diff and commit it, and to record the fact as a finding.
  </action>
  <verify>
    <automated>bash -n scripts/phase14-verify.sh && ./scripts/phase14-verify.sh > /tmp/p14-verify.txt 2>&1; test "$(grep -c '^\[FAIL\]' /tmp/p14-verify.txt)" -eq 0 && grep -q 'ADOPT-03' /tmp/p14-verify.txt && grep -q 'ADOPT-04' /tmp/p14-verify.txt && grep -q 'D-35' /tmp/p14-verify.txt && grep -q 'D-36' /tmp/p14-verify.txt && grep -q 'D-37' /tmp/p14-verify.txt && grep -q 'D-38' /tmp/p14-verify.txt && grep -q 'workspacerules\|Workspace rule' /tmp/p14-verify.txt && grep -q 'uninstall --dry-run' /tmp/p14-verify.txt && grep -q 'protect --dry-run' /tmp/p14-verify.txt && grep -qE '\[PASS\].*qs -c ii' /tmp/p14-verify.txt</automated>
    <fails_when>non-zero exit — `bash -n` prints a syntax error, the captured output holds one or more lines beginning `[FAIL]`, or any of the nine required block-label tokens is absent from the output (a block silently did not run)</fails_when>
  </verify>
  <acceptance_criteria>
    - `./scripts/phase14-verify.sh` exits 0 and its output contains zero lines beginning `[FAIL]`.
    - The output contains labels prefixed `ADOPT-02`, `ADOPT-03`, `ADOPT-04`, `D-35`, `D-36`, `D-37`, and `D-38`.
    - Eleven workspace-rule assertions run: `grep -c 'Workspace rule\|workspace rule' /tmp/p14-verify.txt` is at least 11, or an equivalent per-rule label count of 11.
    - The ii shell is asserted running and its `[PASS]` label carries the literal `qs -c ii`; the bar daemon and the notification daemon are each asserted not running.
    - A DP-1 scale that differs from `dp1_scale_pre` produces a `[FINDING]` line and no `[FAIL]` line — DP-1's presence is enforced, its scale is recorded (D-14).
    - When HDMI-A-2 is absent the output carries an informational skip line and no `[FAIL]` for it.
    - `grep -cE 'pkill|hyprctl reload|hyprctl keyword|rm -rf|rsync .*--delete' scripts/phase14-verify.sh` is 0.
    - `./arch/dots-hyprland.sh uninstall --dry-run` and `./arch/dots-hyprland.sh protect --dry-run` each exit 0 when run standalone.
  </acceptance_criteria>
  <done>All four requirements' automatable halves return an exit code from one read-only script, and every condition it cannot observe is recorded as a finding or an informational skip rather than a pass.</done>
</task>

<task type="auto">
  <name>Task 5: Write `14-LIVE-VERIFY.md` with script output, findings, and the known-loss list; commit the transcript</name>
  <files>.planning/phases/14-live-full-adopt-verify/14-LIVE-VERIFY.md, .planning/phases/14-live-full-adopt-verify/14-ADOPT-TRANSCRIPT.txt</files>
  <read_first>
    - `.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md` — the phase-record shape to mirror: title, one-paragraph scope naming the decision IDs, then `## ` sections each tagged with its decision ID.
    - `.planning/phases/14-live-full-adopt-verify/14-ADOPT-TRANSCRIPT.txt` — the operator's `script(1)` capture, for the load-bearing excerpts.
    - `.planning/phases/14-live-full-adopt-verify/14-CONTEXT.md` — D-31, D-32, D-38, D-39, and the D-14 deferral rule.
    - `scripts/phase14-verify.sh` — so the recorded output matches the script's actual labels.
  </read_first>
  <action>
    Create `.planning/phases/14-live-full-adopt-verify/14-LIVE-VERIFY.md`. Title, then a one-paragraph
    scope statement naming D-29 through D-38 and stating the success bar plainly: a usable ii desktop,
    the Phase 13 overlay applied, Waybar/rofi/swaync gone, and findings logged — not everything
    perfect (D-32).

    Sections, each heading carrying its decision ID in parentheses, mirroring the `13-SOT-APPLY.md`
    convention:

    - `## Verify run` — the full captured output of `./scripts/phase14-verify.sh`, verbatim, inside
      a fenced block, with the run date and whether HDMI-A-2 was attached.
    - `## ADOPT-02 proof (D-33)` — the three conditions and what each returned, including the
      observed post-adopt config-provider token and the `hyprctl eval` response. Record the token as
      an observation, not as something the script asserted equal to a guess.
    - `## ADOPT-03 result` — monitors, the eleven workspace rules, `qs -c ii`, and the dual-run
      policy result. State which mode the verification ran in, single-head or dual-head.
    - `## Human checklist (D-29, D-30)` — the operator's answers to the three manual-only items:
      whether the layout renders correctly on screen, whether the launcher keybind opens the ii
      launcher, and whether the DP-1 scale looks right. Neither the script nor the checklist alone
      satisfies D-29.
    - `## Rollback inputs (ADOPT-04, D-26, D-36)` — what was proven present, with the backup
      directory's post-install mtime and sha256 comparison result.
    - `## Known losses (D-38)` — the two lists as written in the runbook, marked confirmed or not
      confirmed, so the operator reads them as expected rather than as breakage. Include the note
      that the personal session unit file survives under `stow/systemd/` and only its autostart is
      gone, and that `hyprpaper` is stopped-but-installed.
    - `## Findings (D-14, D-38)` — every `[FINDING]` line the script emitted, plus any the operator
      adds. For each: what was observed, whether it blocks (it does not, by decision), and where it
      goes next. A wrong DP-1 scale and a broken screen share both belong here as Phase 15 items,
      not as phase failures.
    - `## Transcript excerpts (D-11, D-19)` — quote the load-bearing lines from
      `14-ADOPT-TRANSCRIPT.txt`: the spoken go decision, the backup confirmation, the
      `hyprland.conf` rename echo, and any failure and re-run.

    Write `Waybar/rofi/swaync` when naming the bar stack; where the browser must be named, write
    only `google-chrome-stable` (D-39).

    Then commit both `14-LIVE-VERIFY.md` and the operator's `14-ADOPT-TRANSCRIPT.txt`. Before
    staging the transcript, read it for host paths, tokens, or anything else that should not enter a
    public repo, and note in the commit body that the review happened. Commit the raw file — quoting
    the load-bearing excerpts in `14-LIVE-VERIFY.md` is what makes it navigable, not a filtered copy.
  </action>
  <verify>
    <automated>test -s .planning/phases/14-live-full-adopt-verify/14-LIVE-VERIFY.md && test -s .planning/phases/14-live-full-adopt-verify/14-ADOPT-TRANSCRIPT.txt && grep -q '## ADOPT-02 proof (D-33)' .planning/phases/14-live-full-adopt-verify/14-LIVE-VERIFY.md && grep -q '## Known losses (D-38)' .planning/phases/14-live-full-adopt-verify/14-LIVE-VERIFY.md && grep -q '## Findings (D-14, D-38)' .planning/phases/14-live-full-adopt-verify/14-LIVE-VERIFY.md && grep -q '## Human checklist (D-29, D-30)' .planning/phases/14-live-full-adopt-verify/14-LIVE-VERIFY.md && grep -q 'Waybar/rofi/swaync' .planning/phases/14-live-full-adopt-verify/14-LIVE-VERIFY.md && grep -q '\[PASS\]' .planning/phases/14-live-full-adopt-verify/14-LIVE-VERIFY.md && test -z "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' .planning/phases/14-live-full-adopt-verify/14-LIVE-VERIFY.md | grep -vx 'google-chrome-stable' || true)" && ./scripts/phase14-verify.sh</automated>
    <fails_when>non-zero exit — either file is empty or absent, any of the four required section headings is missing, the record holds no captured `[PASS]` line (the script output was not pasted in), a chrome-bearing token other than the exact string `google-chrome-stable` appears, or a re-run of the verify script now prints a `[FAIL]` line and exits 1</fails_when>
    <human-check>
      Open the new session and confirm, then record each answer in `14-LIVE-VERIFY.md` §"Human checklist (D-29, D-30)":
      1. DP-1 renders at the expected size and the desktop is usable — the ii bar is up and windows land where you expect. No non-visual proxy exists for "the layout looks right".
      2. The launcher keybind opens the ii launcher. The launcher package never had an autostart, so a process check is vacuous; only pressing the key proves it.
      3. Workspaces land on the monitor the Phase 13 overlay pins them to. If HDMI-A-2 is attached, confirm the dual-head layout; if not, say so.
      4. Screen share: try sharing a window from a browser or a conferencing app. If it fails, that is a recorded finding and a Phase 15 item, not a phase failure.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `14-LIVE-VERIFY.md` exists, is non-empty, and contains all seven required `## ` section headings.
    - The `## Verify run` section contains the verbatim script output including at least one `[PASS]` line and states whether HDMI-A-2 was attached.
    - Every `[FINDING]` line the script emitted appears in `## Findings (D-14, D-38)` with a written disposition.
    - `## Human checklist (D-29, D-30)` carries a written answer for each of the four human checks, not a placeholder.
    - `14-ADOPT-TRANSCRIPT.txt` is committed and non-empty, and the commit body records that it was reviewed before staging.
    - `git status --porcelain` is empty after the commit.
  </acceptance_criteria>
  <done>The phase has a written record: what the script proved, what the operator confirmed by eye, what was lost on purpose, and what is deferred — with the raw transcript of a run no agent performed sitting beside it.</done>
</task>

</tasks>

<!-- planner-discipline-allow: [A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]* -->
<!-- planner-discipline-allow: pkill|hyprctl reload|hyprctl keyword|rm -rf|rsync .*--delete -->

<artifacts_produced>
## Artifacts this phase produces

New files created by this plan:

- `scripts/phase14-verify.sh`
- `.planning/phases/14-live-full-adopt-verify/14-LIVE-VERIFY.md`
- `.planning/phases/14-live-full-adopt-verify/14-ADOPT-TRANSCRIPT.txt` (produced by the operator's `script(1)` session, committed by this plan)

New shell symbols defined in `scripts/phase14-verify.sh`:

- functions: `pass`, `fail`, `finding`, `baseline_value` (the anchored fixture-key reader)
- variables/constants: `REPO_ROOT`, `FAIL`, `FINDINGS`, `XDG`, `BASELINE`, `BACKUP_DIR`, `WRAP`

New markdown section headings other artifacts and later phases grep for, in `14-LIVE-VERIFY.md`:

- `## Verify run`, `## ADOPT-02 proof (D-33)`, `## ADOPT-03 result`,
  `## Human checklist (D-29, D-30)`, `## Rollback inputs (ADOPT-04, D-26, D-36)`,
  `## Known losses (D-38)`, `## Findings (D-14, D-38)`, `## Transcript excerpts (D-11, D-19)`

Live-system state this plan observes but never creates or modifies: `~/.config/hypr/hyprland.lua`,
`~/.config/hypr/hyprland.conf.old`, `~/.config/hypr/hyprlock.conf.new`,
`~/.config/hypr/hypridle.conf.new`, `~/.config/hypr/custom/` (applied copy),
`~/ii-original-dots-backup/`. All are produced by the operator's install run, not by this plan.
</artifacts_produced>

<flagged_assumptions>
## Flagged assumptions — spec-less edge probe (unresolved rows)

Phase 14 has no SPEC, so the edge probe ran against the phase requirement IDs. All four rows came
back `unclassified` and therefore stay `unresolved` — none was auto-resolved with a backstop marker.
These are surfaced, not dropped. Two of the four belong to this plan; ADOPT-01 and ADOPT-04 are
carried in `14-01-PLAN.md`.

| Requirement | Probe status | Assumption this plan proceeds on |
|---|---|---|
| ADOPT-02 | unresolved (unclassified) | That "loads via the ii Lua entry" is fully covered by the three D-33 conditions plus the two `hyprctl` probes, and that asserting the absence of the observed pre-adopt config-provider token is sound where asserting the presence of an unobserved post-adopt token is not. The probe derived no boundary or encoding condition, so no edge case around a partially-installed entry, a stale running session, or a mixed `.conf`/`.lua` state is separately handled beyond the `test ! -f hyprland.conf` assertion. |
| ADOPT-03 | unresolved (unclassified) | That "operator-verified" is satisfied by the scripted block plus the four human checks, and that HDMI-A-2 being physically absent is an observability gap rather than a failure. The probe derived no adjacency or cardinality condition, so a monitor arriving mid-verification, a workspace rule that exists but resolves to a different output, and a `qs -c ii` process that is running but not rendering are all unhandled. The single-head informational skip is the visible edge of that gap. |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live system | The mutating install crosses here. It is the only mutation in this plan and no agent invocation performs it. |
| live session → verify script | `hyprctl`, `busctl`, `pgrep`, and `systemctl --user` read a running compositor and session bus; the script must not write back. |
| live session output → public git repo | The `script(1)` transcript captures a `sudo pacman` run and every prompt of a third-party installer. |
| upstream `./setup` → live `~/.config` | Third-party code writes the operator's home during the window; the verify blocks are what detect a wrong branch after the fact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | agent-runnable artifacts in this plan | critical | mitigate | The mutating install appears only inside the `checkpoint:human-action` instructions as operator prose, described rather than typed as an executable line, and gated `blocking-human` so no auto-mode can advance past it. Task 3 and Task 4 acceptance require zero process-kill and zero compositor-mutation calls in `scripts/phase14-verify.sh`. |
| T-14-05 | Information disclosure | `14-ADOPT-TRANSCRIPT.txt` | high | mitigate | The checkpoint instructions require `script(1)` output-only and explicitly ban `--log-in` and `--log-io`, whose man page warns they record terminal input including passwords. Task 5 requires reading the transcript for host paths and tokens before staging, recorded in the commit body. |
| T-14-04 | Tampering | upstream firstrun branch replacing `hyprlock.conf` / `hypridle.conf` | high | mitigate | Task 4's D-37 block is the detector: byte size and sha256 compared against the recorded pre-adopt fixture, plus the presence of unpromoted `.new` sidecars. A hard failure, not a finding. |
| T-14-08 | Denial of service | silently skipped upstream backup (`ask=false` via greeting `n`, `--force`, or `yesforall`) | high | mitigate | Task 4's D-36 block asserts the backup copy of `hyprland.conf` exists, matches the recorded pre-adopt sha256, and is newer than the recorded pre-install mtime. All three are hard failures. The checkpoint instructions carry the behavioural half. |
| T-14-14 | Tampering | verify script mutating its own subject to green a check | high | mitigate | Task 3 and Task 4 acceptance both require `grep -cE 'pkill\|hyprctl reload\|hyprctl keyword\|rm -rf\|rsync .*--delete'` on the script to be 0, and the header carries an explicit read-only constraint line. |
| T-14-15 | Repudiation | the go decision and what actually happened during a run no agent performed | medium | mitigate | D-11 and D-19: the decision is spoken into the `script(1)` transcript, the transcript is committed, and Task 5 quotes the load-bearing excerpts into `14-LIVE-VERIFY.md`. |
| T-14-16 | Spoofing | a verify pass that reflects a stale or unobservable condition rather than a real one | high | mitigate | The config-provider check asserts the absence of the observed pre-adopt token rather than the presence of a guessed post-adopt one, and is paired with an independent `hyprctl eval` probe. Unobservable conditions (HDMI-A-2 absent, portal unanswerable) emit an informational or `[FINDING]` line, never a `[PASS]`. |
| T-14-09 | Elevation of privilege | `sudo pacman` inside the adopt window | medium | accept | The install requires root for package operations. Scope is the operator's own machine, the command set is upstream's, pinned at `vendor/dots-hyprland` SHA `1a9ffb78f0c272a45f82342587dc3bec72762233`, and no elevated command is issued by this plan. |
| T-14-10 | Spoofing | upstream package set | medium | transfer | Package selection is upstream's, pinned by submodule SHA. No package is chosen by this phase, so no legitimacy audit applies (`14-RESEARCH.md` §Package Legitimacy Audit). |
| T-14-17 | Denial of service | rollback tiers 2 and 3 unreachable when needed | medium | mitigate | Task 4 runs `uninstall --dry-run` and `protect --dry-run` and fails on a non-zero exit, so reachability is proven while the desktop still works rather than discovered when it does not. |

No `npm`, `pip`, `cargo`, or `pacman` install appears in any deliverable of this plan, so no
package-legitimacy checkpoint is required.
</threat_model>

<verification>
Run from the repo root, after the operator's adopt window:

1. `bash -n scripts/phase14-verify.sh` — syntax gate.
2. `./scripts/phase14-verify.sh` — exits 0, prints no `[FAIL]` line. Findings are allowed.
3. `./arch/dots-hyprland.sh uninstall --dry-run` and `./arch/dots-hyprland.sh protect --dry-run` — each exits 0.
4. `test -s .planning/phases/14-live-full-adopt-verify/14-LIVE-VERIFY.md` and `test -s .planning/phases/14-live-full-adopt-verify/14-ADOPT-TRANSCRIPT.txt`.
5. `test -z "$(git status --porcelain)"`.
6. The four `<human-check>` items in Task 5, answered in writing in `14-LIVE-VERIFY.md`.

Nothing in this list mutates the live machine. No step invokes the wrapper without `--dry-run`.
</verification>

<success_criteria>
- The operator has run the adopt window and a usable ii desktop is up (D-32).
- `./scripts/phase14-verify.sh` exits 0 with zero `[FAIL]` lines and its ADOPT-02, ADOPT-03, ADOPT-04, D-35, D-36, D-37, and D-38 blocks all ran.
- ADOPT-02 is proven three ways; ADOPT-03's scripted half is green and its four human checks are answered in writing.
- Rollback tier-1 inputs are proven present and tiers 2 and 3 are proven reachable.
- `14-LIVE-VERIFY.md` holds the script output, the human answers, the known-loss list, and every finding with a disposition.
- `14-ADOPT-TRANSCRIPT.txt` is reviewed and committed.
- The working tree is clean.
</success_criteria>

<output>
Create `.planning/phases/14-live-full-adopt-verify/14-02-SUMMARY.md` when done
</output>
