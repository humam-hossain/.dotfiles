---
phase: 11-disposition-decisions
plan: 03
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md
  - .planning/phases/11-disposition-decisions/11-VALIDATION.md
autonomous: true
requirements:
  - DISP-01
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "§4 Axis B of 11-DISPOSITIONS.md dispositioned all HIGH misc PRESENT paths (fish, fontconfig, kitty, starship.toml) with accept-upstream on live and D-03 columns + inventory cites"
    - "Notable MED/MED–HIGH PRESENT collisions (mpv, dolphinrc, kdeglobals) have accept-upstream rows or equivalent with pre-flight archive note (D-27/D-28)"
    - "LOW–MED greenfield ABSENT misc catalog covered by individual rows or a single accept-upstream-under-full-profile blurb citing inventory misc catalog (D-27 discretion)"
    - "D-28: live home accept-upstream for replaced misc; personal copies remain repo archive after pre-flight; no post-install reapply of personal fish/kitty/starship over live"
    - "§5 Axis C covers full install/install-deps pipeline, sudo pacman -Syu, and implicitize_old_dependencies as HIGH accept-upstream under full profile (D-29)"
    - "Axis C documents illogical-impulse-* metas remain managed accept-upstream (D-31) and plasma-browser-integration accept-upstream if setup wants including possible KDE pull (D-30)"
    - "D-29: asdeps demotion residual accepted; wrapper protect/re-mark remains as implemented — no invented new protect lists"
    - "Every B/C disposition cell uses only the five-value enum; Flag stage uses drop-core / allow-sysupdate / full-profile vocabulary consistent with §2"
    - "§1–§3 from prior plans remain intact (this plan only expands §4 and §5)"
  artifacts:
    - path: ".planning/phases/11-disposition-decisions/11-DISPOSITIONS.md"
      provides: "Complete Axis B + Axis C disposition coverage for remaining DISP-01 HIGH paths outside hypr"
      contains: "fish, fontconfig, kitty, starship.toml, pacman -Syu, install-deps pipeline, asdeps/implicitize, illogical-impulse, plasmaintg"
  key_links:
    - from: "11-DISPOSITIONS.md §4 Axis B rows"
      to: "10-INVENTORY.md Axis drop --core"
      via: "Inventory source column"
      pattern: "10-INVENTORY.md"
    - from: "11-DISPOSITIONS.md §5 Axis C rows"
      to: "10-INVENTORY.md Axis allow sysupdate / package effects"
      via: "Inventory source column"
      pattern: "pacman -Syu"
    - from: "Axis B PRESENT collision rationales"
      to: "§1 pre-flight archive gate (D-07/D-28)"
      via: "Rationale cross-ref live accept + repo archive"
      pattern: "accept-upstream"
  prohibitions:
    - statement: "no live full install"
      status: active
      verification: "Docs-only; no pacman/yay/setup execution"
    - statement: "no XDG mutation"
      status: active
      verification: "No rsync/cp/mv/rm into ~/.config; pre-flight remains documented only"
    - statement: "no wrapper default full"
      status: active
      verification: "arch/dots-hyprland.sh not modified; §2 SAFE_DEFAULTS residual intact"
    - statement: "no invented surfaces"
      status: active
      verification: "All Paths from 10-INVENTORY.md misc/packages tables or explicit catalog blurb cite"
    - statement: "no hypr/custom creation this phase"
      status: active
      verification: "Plan does not touch hypr/custom or §3"
    - statement: "no delete chrome from repo"
      status: active
      verification: "Plan does not expand §6 chrome beyond leaving prior stubs"
---

<objective>
Expand `11-DISPOSITIONS.md` **§4 Axis B (misc / drop `--core`)** and **§5 Axis C (packages / sysupdate)** so every remaining DISP-01 HIGH inventory path outside hypr has an explicit enum + rationale, with live **accept-upstream** under the first full-adopt profile per D-27..D-31.

Purpose: Close misc and package decision gaps Phase 12/14 consume when dropping `--core` and allowing sysupdate — without re-opening hypr Axis A or chrome/lock sections.

Output: Complete §4 + §5 in `11-DISPOSITIONS.md`; VALIDATION DISP-01 B/C gates advanced.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-disposition-decisions/11-CONTEXT.md
@.planning/phases/11-disposition-decisions/11-RESEARCH.md
@.planning/phases/11-disposition-decisions/11-PATTERNS.md
@.planning/phases/11-disposition-decisions/11-PLAN-OUTLINE.md
@.planning/phases/11-disposition-decisions/11-01-PLAN.md
@.planning/phases/11-disposition-decisions/11-02-PLAN.md
@.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md
@scripts/phase11-dispositions-assert.sh
</context>

## Phase Goal

Every high-risk change has an explicit human decision before tooling or live adopt. This plan completes **DISP-01 for Axes B and C** (misc HIGH + package/sysupdate HIGH).

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Disposition assert harness | `scripts/phase11-dispositions-assert.sh` | 11-01 |
| Disposition set | `11-DISPOSITIONS.md` | 11-01 scaffold; 11-02 §3; **11-03 §4–§5**; 11-04 chrome/lock/UNKNOWN |
| Validation contract | `11-VALIDATION.md` | seeded; **11-03 updates DISP-01 B/C progress** |

## Section ownership (hard)

- **This plan edits only §4 Axis B and §5 Axis C.**
- **Do not rewrite** §1 pre-flight, §2 flag profile, or §3 Axis A.
- **Do not expand** §6 chrome, §7 lock residual narrative, or §8 UNKNOWN/extras (11-04).
- Wave-2 file note from outline: if 11-02 and 11-03 run in parallel, each must touch only its owned sections; if conflict risk, run after 11-02 or use section-scoped edits only.

## Decisions locked in this plan

- D-03 / D-04: uniform columns + inventory cites
- D-27: drop `--core` → all misc catalog rows live `accept-upstream`
- D-28: PRESENT collisions (fish, kitty, starship, fontconfig, mpv, dolphinrc, kdeglobals, …): live accept-upstream; personal copies **repo archive only** after pre-flight; **no post-install reapply** over live
- D-29: full deps path accept-upstream — allow Syu, metas, asdeps residual; document risk; no new protect lists (wrapper protect remains)
- D-30: plasma-browser-integration / plasmaintg → accept-upstream if setup wants (incl. possible KDE pull)
- D-31: coarse already-installed `illogical-impulse-*` metas → accept-upstream remain managed; no uninstall campaign
- D-05 linkage: Flag stage for these rows is `full-profile` / `drop-core` / `allow-sysupdate` as appropriate (first adopt drops all three)

## Assumptions

- 11-01 left §4/§5 keyword stubs; executor replaces stubs with complete tables/blurbs.
- LOW greenfield ABSENT misc may be one blurb (Claude discretion) naming the inventory catalog — still must not invent paths outside that catalog.
- PARTIAL asdeps name intersection: document accept residual under D-29 without inventing per-package protect entries.

<tasks>

<task type="auto" id="11-03-01">
  <name>Task 1: Complete §4 Axis B — misc under drop --core</name>
  <files>.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md</files>
  <read_first>
    - .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md (§4 stub from 11-01; do not edit §1–§3)
    - .planning/phases/11-disposition-decisions/11-CONTEXT.md (D-27, D-28)
    - .planning/phases/11-disposition-decisions/11-RESEARCH.md (HIGH paths 7–10; MED mpv; PRESENT collisions; greenfield blurb discretion)
    - .planning/phases/11-disposition-decisions/11-PATTERNS.md (D-03 columns; Axis B section pattern)
    - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md (Axis drop --core: fish, fontconfig, misc catalog, named INV-03 collision set)
  </read_first>
  <action>
    Expand **only §4 Axis B** in `11-DISPOSITIONS.md` into complete dispositions for drop-`--core` misc effects.

    Use D-03 table columns: Path | Inventory risk | Disposition | Rationale | Flag stage | Inventory source.

    **Required HIGH rows (individual table rows):**
    1. `~/.config/fish/` — HIGH — `accept-upstream` — live rsync sync exclude conf.d under full profile; personal fish archived in repo via pre-flight; no post-install reapply (D-27/D-28) — Flag stage `full-profile` / `drop-core` — cite inventory fish row.
    2. `~/.config/fontconfig/` — HIGH — same live accept-upstream + repo archive pattern (D-28) — cite inventory.
    3. `~/.config/kitty/` — HIGH — dir sync `--delete` accept-upstream on live; repo archive only (D-28) — cite inventory.
    4. `~/.config/starship.toml` — HIGH — install_file accept-upstream on live; repo archive only (D-28) — cite inventory.

    **Required notable MED / MED–HIGH PRESENT collisions (individual rows):**
    5. `~/.config/mpv/` — MED–HIGH — `accept-upstream` + pre-flight archive (live-only drift called out in §1) — D-27/D-28.
    6. `~/.config/dolphinrc` — MED — same.
    7. `~/.config/kdeglobals` — MED — same.

    **Greenfield ABSENT catalog (LOW–MED):** Either compact rows or a **single blurb** stating all remaining inventory misc greenfield basenames (chrome-flags, code-flags, darklyrc, foot, fuzzel, kde-material-you-colors, konsolerc, Kvantum, matugen, thorium-flags, wlogout, xdg-desktop-portal, zshrc.d, konsole share, …) → live `accept-upstream` under drop-`--core` (D-27). Blurb must cite `10-INVENTORY.md` full misc catalog section — do not invent basenames absent from inventory.

    **Policy sentence (required in §4 prose or rationales):** After full adopt, live misc surfaces are dots-hyprland-managed; personal copies remain in **repo only as archive** after pre-flight; **no post-install reapply** of personal fish/kitty/starship (or other misc) over live (D-28).

    Enum only five tokens. Do not use `migrate-to-hypr-custom` for misc paths. Do not touch §6 chrome (waybar/rofi/swaync are not Axis B inventory rows).
  </action>
  <verify>
    <automated>rg -n 'fish|fontconfig|kitty|starship\.toml|accept-upstream|drop-core|mpv|dolphinrc|kdeglobals' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -F 'fish' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches Axis B content
    - `rg -F 'fontconfig' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -F 'kitty' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -F 'starship.toml' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -F 'mpv' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -n 'no post-install reapply|repo only as archive|archive' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches D-28 policy language
    - `rg -F 'SAFE_DEFAULTS' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` still matches (§2 intact)
    - `rg -F 'hyprland.conf' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` still matches (§3 intact if 11-02 done; or 11-01 sample if not — at least file still has prior content)
  </acceptance_criteria>
  <done>§4 Axis B complete for HIGH misc + notable PRESENT MED + greenfield coverage under D-27/D-28.</done>
</task>

<task type="auto" id="11-03-02">
  <name>Task 2: Complete §5 Axis C — packages / sysupdate + assert/VALIDATION</name>
  <files>.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md, .planning/phases/11-disposition-decisions/11-VALIDATION.md</files>
  <read_first>
    - .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md (§5 stub; §4 after Task 1)
    - .planning/phases/11-disposition-decisions/11-CONTEXT.md (D-29, D-30, D-31)
    - .planning/phases/11-disposition-decisions/11-RESEARCH.md (HIGH paths 11–13; MED–HIGH illogical-impulse-hyprland; plasmaintg; PARTIAL asdeps)
    - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md (Axis allow sysupdate: pipeline, Syu, implicitize_old_dependencies, meta table, plasmaintg, protect mitigation)
    - scripts/phase11-dispositions-assert.sh
    - .planning/phases/11-disposition-decisions/11-VALIDATION.md
  </read_first>
  <action>
    Expand **only §5 Axis C** in `11-DISPOSITIONS.md`.

    D-03 table (or grouped tables) with inventory cites for:

    **HIGH required:**
    1. Full `install` / `install-deps` pipeline — HIGH overall — `accept-upstream` full deps under first full-adopt profile (D-29) — Flag stage `full-profile` / `allow-sysupdate` — cite inventory pipeline row.
    2. `sudo pacman -Syu` — HIGH — allow when skip-sysupdate dropped (D-05/D-29); document system-wide upgrade risk in rationale — cite inventory Syu row. Path string must include `pacman -Syu` or `Syu` for gates.
    3. `implicitize_old_dependencies` / asdeps demotion — HIGH for dual-run — `accept-upstream` residual; state wrapper PROTECT_EXPLICIT re-mark / protect remains as implemented; **do not invent new protect package lists** (D-29) — cite inventory; note PARTIAL name intersection without fabricating package names beyond inventory.

    **MED–HIGH / policy required:**
    4. `illogical-impulse-hyprland` meta (and coarse `illogical-impulse-*` set) — `accept-upstream` remain managed; no uninstall campaign (D-31). May be one meta row + blurb for the coarse set citing inventory meta table.
    5. `plasma-browser-integration` / optional plasmaintg — `accept-upstream` if setup wants it, including possible KDE pull footprint (D-30) — cite inventory plasmaintg row.
    6. Optional: `remove_deprecated_dependencies` MED always-on deps step — `accept-upstream` under full deps with brief risk note (inventory cite) — discretion if space allows.

    Prose must state: full-profile package path accepts Syu + asdeps residual + metas; default residual install still injects `--skip-sysupdate` (point at §2 / D-10 — do not re-edit wrapper).

    Run `./scripts/phase11-dispositions-assert.sh`; fix only §4/§5 if failures are from missing B/C keywords (fish, starship, Syu, etc.).

    Update `11-VALIDATION.md`: advance 11-DISP-01 for Axis B/C HIGH path `rg` coverage (fish, fontconfig, kitty, starship, pacman -Syu / asdeps); leave DISP-03/04 and full cross-check to 11-04.
  </action>
  <verify>
    <automated>./scripts/phase11-dispositions-assert.sh && rg -n 'pacman -Syu|Syu|asdeps|implicitize|illogical-impulse|plasma-browser-integration|install-deps' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -F 'pacman -Syu' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches OR `rg -F 'Syu' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches in Axis C context
    - `rg -n 'asdeps|implicitize' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -F 'illogical-impulse' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -n 'plasma-browser-integration|plasmaintg' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -F 'fish' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -F 'starship.toml' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `./scripts/phase11-dispositions-assert.sh` exits 0
    - `rg -F 'SAFE_DEFAULTS' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - Plan files_modified does not include `arch/dots-hyprland.sh`
  </acceptance_criteria>
  <done>§5 Axis C complete for DISP-01 package/sysupdate HIGHs; B+C assert green; D-29..D-31 recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor → package manager / live XDG | Documentation only; no pacman/yay/rsync execution |
| §4–§5 dispositions → Phase 12/14 | accept-upstream on live implies pre-flight archive dependency (D-07/D-28) |
| Protect/asdeps notes → wrapper | Must not invent new protect lists that diverge from arch/ implementation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | running Syu or misc rsync while documenting | high | mitigate | Docs-only tasks; verification is rg/assert only |
| T-11-09 | Tampering | accept-upstream misc without pre-flight archive warning | high | mitigate | D-28 policy sentence required in §4; cross-ref §1 pre-flight |
| T-11-10 | Tampering | inventing expanded PROTECT_EXPLICIT lists in dispositions | medium | mitigate | D-29 forbids new protect lists; document residual + existing wrapper protect only |
| T-11-11 | Tampering | treating greenfield blurb as license to add non-inventory paths | medium | mitigate | D-04; blurb must cite inventory misc catalog only |
| T-11-04 | Information Disclosure | dumping host package inventories with sensitive machine detail | low | mitigate | Coarse meta names from inventory only; no full `pacman -Q` paste required |
| T-11-03 | Tampering | editing SAFE_DEFAULTS while describing full package profile | high | mitigate | files_modified excludes wrapper; §2 left intact |
| T-11-SC | Tampering | supply chain | low | accept | No new packages installed this plan |
</threat_model>

<verification>
From REPO_ROOT:
`rg -F` fish, fontconfig, kitty, starship.toml, pacman -Syu (or Syu), asdeps/implicitize, illogical-impulse in `11-DISPOSITIONS.md`;
D-28 archive / no post-install reapply language present;
`./scripts/phase11-dispositions-assert.sh` exits 0;
`rg -F SAFE_DEFAULTS` still hits.
</verification>

<success_criteria>
§4 and §5 complete for DISP-01 misc + package HIGH paths; live accept-upstream + repo archive policy (D-28); Syu/asdeps/metas/plasmaintg decided (D-29..D-31); no host/wrapper mutation; §1–§3 preserved.
</success_criteria>

<output>
Create `.planning/phases/11-disposition-decisions/11-03-SUMMARY.md` when done
</output>
