---
phase: 12-wrapper-full-profile
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - arch/dots-hyprland.sh
autonomous: true
requirements:
  - FULL-01
  - FULL-02
  - FULL-04
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "FULL-01 core: printf 'yes\\n' | ./arch/dots-hyprland.sh install --full --dry-run exits 0 and would-exec omits all three SAFE_DEFAULTS residuals (--core, --skip-hyprland, --skip-sysupdate) per D-03 / DISP-02"
    - "FULL-04: same full dry-run prints a would-exec line; wrapper meta --full is stripped (absent from would-exec argv)"
    - "FULL-02: printf 'yes\\n' | ./arch/dots-hyprland.sh install --dry-run (no --full) still shows --core, --skip-hyprland, and --skip-sysupdate (D-05)"
    - "FULL-02b: printf 'yes\\n' | ./arch/dots-hyprland.sh install-files --dry-run still injects the triple residual"
    - "install-files --full --dry-run also drops all three residuals (D-02 scope)"
    - "D-02: ./arch/dots-hyprland.sh install-deps --full --dry-run exits non-zero with [FAIL]"
    - "bash -n arch/dots-hyprland.sh exits 0; SAFE_DEFAULTS array literal unchanged"
  artifacts:
    - path: "arch/dots-hyprland.sh"
      provides: "Wrapper-owned --full meta-flag strip + conditional SAFE_DEFAULTS injection"
      contains: "full=0 local; --full) case arm; needs_safe_defaults && full==0 injection branch"
  key_links:
    - from: "run_install_family meta strip loop"
      to: "user_flags[] / cmd[]"
      via: "--full stripped like --dry-run; never appended to cmd"
      pattern: "--full"
    - from: "needs_safe_defaults + full flag"
      to: "cmd+= SAFE_DEFAULTS"
      via: "inject only when full==0"
      pattern: "SAFE_DEFAULTS"
    - from: "install --full --dry-run"
      to: "would-exec argv proof"
      via: "printf yes feed → dry-run print"
      pattern: "would exec"
  prohibitions:
    - statement: "no live full install"
      status: active
      verification: "All verifies use --dry-run only; no non-dry-run install --full"
    - statement: "never make full accidental default"
      status: active
      verification: "FULL-02 negative control greps triple residual on bare install --dry-run"
    - statement: "no INV/DISP wrapper gate"
      status: active
      verification: "No filesystem checks for 10-INVENTORY.md or 11-DISPOSITIONS.md in run_install_family"
    - statement: "no PROTECT expand"
      status: active
      verification: "PROTECT_EXPLICIT array contents unchanged this plan"
    - statement: "no chrome teardown"
      status: active
      verification: "No process kill / dual-run stop side effects added"
---

<objective>
Tracer slice for Phase 12: wire wrapper-owned `--full` end-to-end through `run_install_family` so `install --full --dry-run` prints would-exec **without** SAFE_DEFAULTS injection, while default `install --dry-run` still injects the triple residual.

Purpose: Prove the full-profile argv contract (FULL-01/02/04 core, D-01..D-03, D-05) on one thin non-mutating path before gate messaging, help rewrite, and smoke matrix expand.

Output: `arch/dots-hyprland.sh` with `--full` strip, D-02 scope refuse, conditional SAFE_DEFAULTS skip, and dry-run argv evidence.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-wrapper-full-profile/12-CONTEXT.md
@.planning/phases/12-wrapper-full-profile/12-RESEARCH.md
@.planning/phases/12-wrapper-full-profile/12-PATTERNS.md
@.planning/phases/12-wrapper-full-profile/12-VALIDATION.md
@arch/dots-hyprland.sh
</context>

## Phase Goal

**Operator can dry-run and invoke an explicit full-install path without making full the accidental default.**

This plan delivers the **thin E2E `--full` dry-run argv proof**: meta-flag parse, no SAFE_DEFAULTS injection on full, residual injection preserved on default.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Wrapper full-profile CLI | `arch/dots-hyprland.sh` (`--full` strip + injection branch) | **12-01** (core); expanded 12-02..04 |
| Full gate messaging | `backup_gate` / full branch in same file | **12-02** |
| Help / usage primary full path | `usage()` in same file | **12-03** |
| FULL-01..05 smoke proof | plan-task verifies (+ optional harness) | **12-04** |

## Decisions locked in this plan

- D-01: `--full` is a wrapper-owned meta-flag (not a new subcommand)
- D-02: `--full` only on `install` / `install-files`; refuse elsewhere
- D-03: when full, inject nothing from SAFE_DEFAULTS (drop all three residuals)
- D-05: default without `--full` still injects SAFE_DEFAULTS triple

## Assumptions (specless probe)

- **EDGE_ABSENT:** FULL-01/02/04 lifted into must_haves.truths as concrete dry-run greps from RESEARCH Validation Architecture
- Gate messaging polish, usage rewrite, and matrix harness are later plans — tracer only needs working argv branch + default residual control

<tasks>

<task type="tracer" id="12-01-01">
  <name>Task 1: End-to-end install --full --dry-run argv (strip + no SAFE_DEFAULTS)</name>
  <reversibility rating="costly">D-01/D-03: --full and drop-all-three argv become the Phase 14/15 full-adopt contract; undoing renames help, tests, and docs.</reversibility>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (SAFE_DEFAULTS ~L12; needs_safe_defaults ~L127-131; run_install_family meta strip ~L1361-1407; dry-run ~L1411-1423)
    - .planning/phases/12-wrapper-full-profile/12-CONTEXT.md (D-01, D-02, D-03, D-05)
    - .planning/phases/12-wrapper-full-profile/12-PATTERNS.md (Pattern 6 meta-flag strip; Pattern 9 conditional injection)
    - .planning/phases/12-wrapper-full-profile/12-RESEARCH.md (Code Examples: meta-flag parse + conditional SAFE_DEFAULTS skip; Validation Architecture FULL-01/04)
    - .planning/phases/12-wrapper-full-profile/12-VALIDATION.md (12-FULL-01 / 12-FULL-04 / 12-D02 rows)
  </read_first>
  <action>
    Edit only `run_install_family` in `arch/dots-hyprland.sh` (per D-01, D-02, D-03). Do not change `SAFE_DEFAULTS=(--core --skip-hyprland --skip-sysupdate)` contents (D-05 residual constant stays literal).

    1. **Meta strip (D-01):** In the existing arg scan that already handles `--dry-run` and `--allow-skip-backup`, add `local full=0` and a case arm for `--full` that sets `full=1` and does **not** append that token to `user_flags`. Preserve non-meta flag order (WRAP-04). Never forward `--full` into `cmd`.

    2. **Scope refuse (D-02):** After the strip loop (before or right after preflight is fine), if `full==1` and `needs_safe_defaults "$subcmd"` is false, print a clear `[FAIL]` that `--full` is only valid with install or install-files, exit 1. Prefer refuse over silent ignore (RESEARCH A1).

    3. **Conditional injection (D-03):** Replace the unconditional `if needs_safe_defaults; then cmd+=SAFE_DEFAULTS` block so injection runs only when `needs_safe_defaults "$subcmd" && ((full == 0))`. When `needs_safe_defaults && full==1`, do **not** prepend any SAFE_DEFAULTS member; optionally echo a single `[CONFIG] full profile: no SAFE_DEFAULTS injection` style line (discretion; avoid putting bare residual tokens on the would-exec line as prose if it confuses greps).

    4. **Keep** preflight, bare skip-backup refuse, backup_gate call site, dry-run post-setup protect/hooks case arms, and array exec `"${cmd[@]}"` unchanged this task (gate messaging is 12-02; protect proof is 12-04). Do not add INV/DISP file checks (D-10). Do not add chrome teardown or live-adopt side effects (D-17).

    5. **No eval:** keep array exec only (T-12 command-injection control).
  </action>
  <verify>
    <automated>bash -n arch/dots-hyprland.sh &amp;&amp; printf 'yes\n' | ./arch/dots-hyprland.sh install --full --dry-run &gt;/tmp/p12-01-full.txt 2&gt;&amp;1 &amp;&amp; grep -q 'would exec' /tmp/p12-01-full.txt &amp;&amp; ! grep -q -- '--skip-hyprland' /tmp/p12-01-full.txt &amp;&amp; ! grep -q -- '--skip-sysupdate' /tmp/p12-01-full.txt &amp;&amp; ! grep -E -- '(^|[[:space:]])--core([[:space:]]|$)' /tmp/p12-01-full.txt &amp;&amp; ! grep -q -- '--full' /tmp/p12-01-full.txt &amp;&amp; ./arch/dots-hyprland.sh install-deps --full --dry-run &gt;/tmp/p12-01-deps.txt 2&gt;&amp;1; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n arch/dots-hyprland.sh` exits 0
    - `printf 'yes\n' | ./arch/dots-hyprland.sh install --full --dry-run` exits 0
    - Output contains `would exec` (or equivalent dry-run would-exec marker already used by the wrapper)
    - Output does not contain `--skip-hyprland` as an injected residual
    - Output does not contain `--skip-sysupdate` as an injected residual
    - Output has no standalone `--core` token on argv/log lines matching `(^|[[:space:]])--core([[:space:]]|$)`
    - Output does not contain the meta token `--full` on the would-exec path (stripped)
    - `./arch/dots-hyprland.sh install-deps --full --dry-run` exits non-zero
    - `grep -n 'SAFE_DEFAULTS=' arch/dots-hyprland.sh` still shows the triple residual array assignment
    - `run_install_family` strip loop has a `--full` case; no new allowlist subcommand named install-full
  </acceptance_criteria>
  <done>Single happy path: install --full --dry-run proves drop-all-three argv; install-deps --full refused; meta stripped.</done>
</task>

<task type="auto" id="12-01-02">
  <name>Task 2: FULL-02 residual control + install-files --full parity</name>
  <reversibility rating="one-way">D-05: if default install stops injecting SAFE_DEFAULTS, full becomes accidental default — milestone anti-goal; undoing after operators adopt bare install as full is a migration.</reversibility>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (run_install_family injection branch after Task 1; needs_safe_defaults)
    - .planning/phases/12-wrapper-full-profile/12-CONTEXT.md (D-05 FULL-02)
    - .planning/phases/12-wrapper-full-profile/12-RESEARCH.md (Pitfall 1 accidental full default; Validation FULL-02 / FULL-02b)
    - .planning/phases/12-wrapper-full-profile/12-VALIDATION.md (12-FULL-02 / 12-FULL-02b)
  </read_first>
  <action>
    Prove and lock the negative control (per D-05 / FULL-02) without weakening Task 1.

    1. Confirm default path: when `full==0` and `needs_safe_defaults`, wrapper still prepends all three SAFE_DEFAULTS members and still logs the existing safe-defaults `[CONFIG]` line. Do **not** empty or rewrite the `SAFE_DEFAULTS` array.

    2. Confirm `install-files --full --dry-run` uses the same full=1 injection skip as `install` (D-02 scope includes install-files). If Task 1 already covers this via shared `needs_safe_defaults`, only verify — no duplicate logic branches per subcommand name beyond what needs_safe_defaults already provides.

    3. Do not change backup_gate messaging yet (12-02). Do not rewrite usage() yet (12-03).

    4. If any Task 1 regression inverted the full flag or gated `needs_safe_defaults` itself off, fix so default dry-run still shows the triple residual.
  </action>
  <verify>
    <automated>printf 'yes\n' | ./arch/dots-hyprland.sh install --dry-run &gt;/tmp/p12-01-safe.txt 2&gt;&amp;1 &amp;&amp; grep -q -- '--core' /tmp/p12-01-safe.txt &amp;&amp; grep -q -- '--skip-hyprland' /tmp/p12-01-safe.txt &amp;&amp; grep -q -- '--skip-sysupdate' /tmp/p12-01-safe.txt &amp;&amp; printf 'yes\n' | ./arch/dots-hyprland.sh install-files --dry-run &gt;/tmp/p12-01-safe-files.txt 2&gt;&amp;1 &amp;&amp; grep -q -- '--skip-hyprland' /tmp/p12-01-safe-files.txt &amp;&amp; printf 'yes\n' | ./arch/dots-hyprland.sh install-files --full --dry-run &gt;/tmp/p12-01-full-files.txt 2&gt;&amp;1 &amp;&amp; ! grep -q -- '--skip-hyprland' /tmp/p12-01-full-files.txt &amp;&amp; ! grep -q -- '--skip-sysupdate' /tmp/p12-01-full-files.txt &amp;&amp; ! grep -E -- '(^|[[:space:]])--core([[:space:]]|$)' /tmp/p12-01-full-files.txt</automated>
  </verify>
  <acceptance_criteria>
    - Default `install --dry-run` output contains `--core`, `--skip-hyprland`, and `--skip-sysupdate`
    - Default `install-files --dry-run` output contains `--skip-hyprland` (and the other residuals)
    - `install-files --full --dry-run` output omits all three residuals under the same greps as Task 1
    - `SAFE_DEFAULTS` array assignment line still lists all three residual flags
  </acceptance_criteria>
  <done>Default path still safe-defaults; install-files --full matches install --full argv contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell → wrapper | Untrusted argv crosses into `run_install_family` |
| Wrapper → `./setup` | Only stripped, allowlisted subcommands + built `cmd` array |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Elevation of privilege | Default install path / SAFE_DEFAULTS | critical | mitigate | Keep full=0 injection (D-05); FULL-02 greps on bare install --dry-run |
| T-12-02 | Tampering | `--full` meta forward to setup | high | mitigate | Strip `--full` in case arm; assert absent from would-exec |
| T-12-03 | Tampering | Bare `--skip-backup` | medium | accept | Shared refuse already present; FULL-03 expanded in 12-02 |
| T-12-04 | Tampering | Dry-run mutates host | high | mitigate | Tracer verifies only --dry-run; exit before `"${cmd[@]}"` |
| T-12-05 | Injection | cmd construction | high | mitigate | Array exec only; never eval concatenated strings |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase |
</threat_model>

<verification>
bash -n; install --full --dry-run residual absence; install --dry-run residual presence; install-deps --full refuse; install-files both paths.
</verification>

<success_criteria>
Operator can `printf 'yes\n' | ./arch/dots-hyprland.sh install --full --dry-run` and see would-exec without SAFE_DEFAULTS residuals, while bare `install --dry-run` still injects the triple.
</success_criteria>

<output>
Create `.planning/phases/12-wrapper-full-profile/12-01-SUMMARY.md` when done
</output>
