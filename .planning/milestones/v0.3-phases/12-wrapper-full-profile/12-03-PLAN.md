---
phase: 12-wrapper-full-profile
plan: 03
type: execute
wave: 3
depends_on:
  - 12-02
files_modified:
  - arch/dots-hyprland.sh
autonomous: true
requirements:
  - FULL-01
user_setup: []

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "FULL-01 docs: ./arch/dots-hyprland.sh help output contains --full as the documented opt-in full path"
    - "D-04: help no longer says full hypr install requires calling vendor ./setup outside this wrapper (negative grep on that note)"
    - "D-04: help documents wrapper --full on install / install-files as the primary full path (not a new install-full subcommand)"
    - "D-13: help points at docs/dots-hyprland-workflow.md (playbook) so operators know the safe dual-run sequence"
    - "D-13: help mentions INV/DISP phase artifact paths (.planning/phases/10-…/10-INVENTORY.md and …/11-DISPOSITIONS.md or equivalent discoverable pointers)"
    - "Meta flags section lists --full alongside --dry-run and --allow-skip-backup as wrapper-owned (stripped)"
    - "bash -n arch/dots-hyprland.sh exits 0; run_install_family behavior from 12-01/12-02 unchanged by this plan"
  artifacts:
    - path: "arch/dots-hyprland.sh"
      provides: "usage()/help primary full path + playbook/INV/DISP pointers"
      contains: "--full in meta flags and examples; dots-hyprland-workflow; no vendor-outside full note"
  key_links:
    - from: "usage() heredoc"
      to: "operator discovery (FULL-01 docs surface)"
      via: "help|-h|--help → usage()"
      pattern: "--full"
    - from: "usage() pointers"
      to: "docs/dots-hyprland-workflow.md + phase INV/DISP paths"
      via: "D-13 discoverability only (no process gate)"
      pattern: "dots-hyprland-workflow"
  prohibitions:
    - statement: "no live full install"
      status: active
      verification: "Docs/help only; no non-dry-run install tasks"
    - statement: "no INV/DISP hard gate in wrapper"
      status: active
      verification: "Help points only; run_install_family still has no 10-INVENTORY/11-DISPOSITIONS existence checks (D-10)"
    - statement: "no new install-full subcommand"
      status: active
      verification: "ALLOWLIST and usage still use install|install-files + --full meta flag (D-01)"
    - statement: "no playbook full rewrite"
      status: active
      verification: "docs/dots-hyprland-workflow.md not rewritten this phase (Phase 15); help pointer only"
---

<objective>
Wave 3 for Phase 12: rewrite `usage()` / help so wrapper-owned `--full` is the **documented primary full path** (FULL-01 discoverability, D-04) and operators get pointers to the playbook + INV/DISP artifacts (D-13) without any process gate in the wrapper (D-10/D-11).

Purpose: Remove the legacy “call vendor `./setup` outside this wrapper for full hypr” note that would contradict the Phase 12 capability.

Output: Updated `usage()` heredoc in `arch/dots-hyprland.sh` only — no argv/injection changes.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-wrapper-full-profile/12-CONTEXT.md
@.planning/phases/12-wrapper-full-profile/12-RESEARCH.md
@.planning/phases/12-wrapper-full-profile/12-PATTERNS.md
@.planning/phases/12-wrapper-full-profile/12-VALIDATION.md
@.planning/phases/12-wrapper-full-profile/12-01-PLAN.md
@.planning/phases/12-wrapper-full-profile/12-02-PLAN.md
@arch/dots-hyprland.sh
@docs/dots-hyprland-workflow.md
</context>

## Phase Goal

**Operator can dry-run and invoke an explicit full-install path without making full the accidental default.**

This plan delivers the **FULL-01 documentation / discoverability surface** on top of 12-01 argv + 12-02 gate work.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Wrapper full-profile CLI | `arch/dots-hyprland.sh` (`--full` strip + injection) | 12-01 |
| Full gate messaging | `backup_gate` / full branch | 12-02 |
| Help / usage primary full path | `usage()` in same file | **12-03** |
| FULL-01..05 smoke proof | plan-task verifies (+ optional harness) | 12-04 |

## Decisions locked in this plan

- D-04: Rewrite `usage()` — remove vendor-outside full note; document wrapper `--full` as primary full path
- D-13: Help points at playbook (`docs/dots-hyprland-workflow.md`) and INV/DISP phase artifact paths (discoverability only)
- D-01: Still a meta-flag, not `install-full` subcommand
- D-10/D-11: No hard INV/DISP process gate in wrapper (pointers ≠ enforcement)

## Assumptions (specless probe)

- Exact help wording/section layout is Claude discretion as long as D-04 and D-13 hold
- Phase 15 owns full playbook safe-vs-full polish; Phase 12 only needs discoverable pointers

<tasks>

<task type="auto" id="12-03-01">
  <name>Task 1: usage() primary full path + remove vendor-outside note (D-04)</name>
  <reversibility rating="reversible">D-04: help text only; runtime argv contract unchanged.</reversibility>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (usage() heredoc ~L23-116 — especially meta flags, examples, final Note about Full hypr install)
    - .planning/phases/12-wrapper-full-profile/12-CONTEXT.md (D-01, D-04)
    - .planning/phases/12-wrapper-full-profile/12-RESEARCH.md (Pattern 5 usage rewrite; Pitfall 8 help still points outside wrapper)
    - .planning/phases/12-wrapper-full-profile/12-VALIDATION.md (12-D04 row; help greps)
  </read_first>
  <action>
    Edit only the `usage()` heredoc in `arch/dots-hyprland.sh` (and any adjacent comment if needed). Do **not** change `run_install_family`, `SAFE_DEFAULTS`, gate, or protect logic.

    1. **Remove D-04 anti-pattern:** Delete or replace the closing note that currently says full hypr install requires calling `vendor/dots-hyprland/./setup` outside this wrapper (exact current line in usage EOF: `Full hypr install requires calling vendor/dots-hyprland/./setup outside this wrapper.`).

    2. **Document `--full` as primary full path:** In wrapper-owned meta flags section, add `--full` with clear meaning: opt-in full profile on `install` / `install-files` only — does **not** inject SAFE_DEFAULTS (`--core --skip-hyprland --skip-sysupdate`). State that default `install` / `install-files` **without** `--full` still inject SAFE_DEFAULTS (FULL-02 residual).

    3. **Examples:** Add at least one example using `--full` with `--dry-run`, e.g. `printf 'yes\n' | ./arch/dots-hyprland.sh install --full --dry-run`. Keep existing safe dry-run examples.

    4. **Safe defaults section:** Keep documenting that residual triple is still the default injection for install/install-files; optionally cross-link “for full profile use `--full`” so operators are not sent outside the wrapper.

    5. **No new subcommand** in ALLOWLIST or usage allowlist list (D-01).
  </action>
  <verify>
    <automated>bash -n arch/dots-hyprland.sh &amp;&amp; ./arch/dots-hyprland.sh help &gt;/tmp/p12-03-help.txt 2&gt;&amp;1 &amp;&amp; grep -q -- '--full' /tmp/p12-03-help.txt &amp;&amp; ! grep -qi 'Full hypr install requires calling vendor' /tmp/p12-03-help.txt &amp;&amp; ! grep -qi 'outside this wrapper' /tmp/p12-03-help.txt</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n arch/dots-hyprland.sh` exits 0
    - `./arch/dots-hyprland.sh help` exits 0 and stdout/stderr combined contains `--full`
    - Help does **not** match `Full hypr install requires calling vendor` (case-insensitive)
    - Help does **not** tell operators that full install must be done outside this wrapper
    - Meta-flags section lists `--full` as wrapper-owned / stripped
    - Examples include an `install --full --dry-run` (or equivalent) line
    - No `install-full` subcommand appears in allowlisted subcommands list
  </acceptance_criteria>
  <done>Help documents --full as primary full path; vendor-outside full note gone.</done>
</task>

<task type="auto" id="12-03-02">
  <name>Task 2: Playbook + INV/DISP discoverability pointers (D-13)</name>
  <reversibility rating="reversible">D-13: help pointers only; no process gate.</reversibility>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (usage() after Task 1)
    - .planning/phases/12-wrapper-full-profile/12-CONTEXT.md (D-10, D-11, D-13)
    - docs/dots-hyprland-workflow.md (pointer target — do not rewrite playbook body)
    - .planning/phases/12-wrapper-full-profile/12-VALIDATION.md (12-D13 row)
  </read_first>
  <action>
    Extend `usage()` with discoverability pointers only (D-13). Still **no** runtime checks for artifact presence (D-10).

    1. **Playbook:** Point operators at `docs/dots-hyprland-workflow.md` for the safe dual-run / workflow sequence.

    2. **INV/DISP:** Point at phase artifact paths so operators know the intended inventory→disposition→full sequence before live full adopt:
       - `.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md`
       - `.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md`
       Wording may note these are operator/process artifacts for first full adopt (Phase 14 enforces ADOPT-01 process — not this wrapper).

    3. **Do not** implement file-existence refuses or soft-warns based on those paths in `run_install_family` (D-10/D-11).

    4. Do not rewrite `docs/dots-hyprland-workflow.md` (Phase 15).
  </action>
  <verify>
    <automated>./arch/dots-hyprland.sh help &gt;/tmp/p12-03-help2.txt 2&gt;&amp;1 &amp;&amp; grep -q 'dots-hyprland-workflow' /tmp/p12-03-help2.txt &amp;&amp; grep -q '10-INVENTORY' /tmp/p12-03-help2.txt &amp;&amp; grep -q '11-DISPOSITIONS' /tmp/p12-03-help2.txt &amp;&amp; ! grep -n '10-INVENTORY\|11-DISPOSITIONS' arch/dots-hyprland.sh | grep -v usage | grep -qE 'test -f|\[ -f|\[\[ -f' ; test $? -ne 0 || true; bash -n arch/dots-hyprland.sh</automated>
  </verify>
  <acceptance_criteria>
    - `./arch/dots-hyprland.sh help` output contains `dots-hyprland-workflow`
    - Help output contains `10-INVENTORY` (path or filename token)
    - Help output contains `11-DISPOSITIONS` (path or filename token)
    - `run_install_family` (and non-usage functions) still do **not** `test -f` / hard-refuse on INV/DISP paths
    - `docs/dots-hyprland-workflow.md` content unchanged by this plan (no edit required; if touched, revert)
  </acceptance_criteria>
  <done>Help points at playbook + INV/DISP; wrapper still has no process gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → help text | Documentation influences whether full is discovered vs raw vendor setup |
| Help pointers → planning artifacts | Paths are advisory; must not become runtime coupling (D-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-09 | Spoofing / misuse | usage() full path | medium | mitigate | Document wrapper `--full` as primary; remove vendor-outside note (D-04) |
| T-12-10 | Information disclosure | INV/DISP paths in help | low | accept | Paths are repo-local planning artifacts; intentional discoverability (D-13) |
| T-12-11 | Elevation of privilege | Accidental process gate | high | mitigate | Pointers only; no filesystem gate in run_install_family (D-10) |
| T-12-01 | Elevation of privilege | Accidental full default | critical | mitigate | Help still states default install injects SAFE_DEFAULTS without `--full` |
| T-12-SC | Tampering | package installs | low | accept | Docs-only plan |
</threat_model>

<verification>
bash -n; help greps for --full, dots-hyprland-workflow, 10-INVENTORY, 11-DISPOSITIONS; negative grep vendor-outside full note.
</verification>

<success_criteria>
Operator running `./arch/dots-hyprland.sh help` sees `--full` as the primary full path, playbook + INV/DISP pointers, and no instruction to leave the wrapper for full hypr install.
</success_criteria>

<output>
Create `.planning/phases/12-wrapper-full-profile/12-03-SUMMARY.md` when done
</output>
