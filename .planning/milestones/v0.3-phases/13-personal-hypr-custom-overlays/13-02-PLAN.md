---
phase: 13-personal-hypr-custom-overlays
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - .config/hypr/custom/execs.lua
  - .planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md
autonomous: true
requirements:
  - OVL-01
  - OVL-02
  - OVL-03
user_setup: []

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "OVL-01: general.lua still contains DP-1, HDMI-A-2, hl.workspace_rule, special:social, and literal scale = \"auto\""
    - "OVL-02: real repo files exist before Phase 14 (general.lua non-empty; env.lua and execs.lua exist — do not use test -s on empty slots) per D-16"
    - "OVL-03: 13-SOT-APPLY.md exists and names cp -a plus fail-if-general.lua-missing; authoring SoT is .config/hypr/custom/ (D-01, D-05, D-18)"
    - "D-08/D-09: custom/*.lua contain none of the dropped cursor-env, cursor-exec, or venv-overlay tokens named in CONTEXT D-08 and D-09"
    - "D-17: no live $HOME/.config mutation this phase (files_modified has no live home paths)"
    - "D-02: apply is documented apply-after-install; this plan does not run it; Phase 14 runs apply"
    - "D-19: in-repo checks in 13-SOT-APPLY.md In-repo verify fence all pass"
    - "D-20: OVL-01/OVL-02/OVL-03 stay Pending in REQUIREMENTS.md until those files exist AND D-19 checks pass — this plan runs D-19; do not treat CONTEXT.md as completion"
    - "D-22: Phase 12 --full / SAFE_DEFAULTS unchanged; no wrapper apply subcommand this phase"
    - statement: "OVL-01 unclassified probe: overlay content is expressible as hl.monitor + hl.workspace_rule in custom/general.lua (manual review of unclassified edge)"
      verification: backstop
    - statement: "OVL-02 concurrency: interrupted work leaves no live apply and no half-written live $HOME/.config (D-17)"
      verification: backstop
    - statement: "OVL-03 unclassified probe: SoT note states repo vs live vs fork without committing overlays into vendor"
      verification: backstop
  artifacts:
    - path: ".config/hypr/custom/execs.lua"
      provides: "empty hyprland.lua require slot (D-06, D-08)"
    - path: ".planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md"
      provides: "D-01..D-05 fence plus D-18 apply command and D-19 verify fence"
      contains: "cp -a"
  key_links:
    - from: "vendor hyprland.lua require custom.execs after hyprland.execs"
      to: ".config/hypr/custom/execs.lua"
      via: "empty file exists so Phase 14 cp -a can overwrite ii seeds (D-06, D-18)"
      pattern: "custom.execs"
    - from: "D-18 apply steps"
      to: "13-SOT-APPLY.md apply command"
      via: "mkdir -p live custom/; cp -a three named files; fail if repo general.lua missing; warn-and-continue if env.lua or execs.lua missing"
      pattern: "cp -a"
    - from: "D-19 in-repo checks"
      to: "13-SOT-APPLY.md In-repo verify (D-19) bash fence"
      via: "executor runs that fence; PATTERNS Shared Patterns Verification is the analog"
      pattern: "test -s .config/hypr/custom/general.lua"
  prohibitions:
    - statement: "no live ~/.config mutation this phase"
      status: unverified
      flagged: true
    - statement: "never commit machine overlays into vendor/dots-hyprland or the personal fork"
      status: unverified
      flagged: true
    - statement: "do not write custom/keybinds.lua or custom/rules.lua"
      status: unverified
      flagged: true
    - statement: "do not overlay cursor or the upstream-already-set venv path"
      status: unverified
      flagged: true
    - statement: "do not rsync --delete custom/"
      status: unverified
      flagged: true
    - statement: "do not fold apply into the wrapper this phase"
      status: unverified
      flagged: true
    - statement: "do not create .config/hypr/monitors.lua or workspaces.lua"
      status: unverified
      flagged: true
---

<objective>
Complete the Phase 13 overlay set (D-06, D-16): commit empty `.config/hypr/custom/execs.lua` (D-08), write the D-18 apply command into `13-SOT-APPLY.md` (`cp -a` of `general.lua` / `env.lua` / `execs.lua`; fail if repo `general.lua` missing; warn-and-continue if slot files missing; never `rsync --delete`; never copy keybinds/rules/variables), and run the full D-19 in-repo verify. D-02: document apply; do not run it. D-03: apply is one-way repo → live. D-20: OVL IDs stay Pending until files exist and D-19 passes. D-22: do not change wrapper `--full`.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-personal-hypr-custom-overlays/13-CONTEXT.md
@.planning/phases/13-personal-hypr-custom-overlays/13-PATTERNS.md
@.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md
@.config/hypr/custom/general.lua
@.config/hypr/custom/env.lua
@vendor/dots-hyprland/dots/.config/hypr/custom/execs.lua
@vendor/dots-hyprland/dots/.config/hypr/hyprland.lua
</context>

## Phase Goal

**Personal must-keeps selected for migrate exist as ii-compatible `hypr/custom` overlays before live full hypr files rely on them.**

This plan finishes the three named overlay files and the apply rule so Phase 14 can copy repo → live. It does not adopt, does not fold apply into `arch/dots-hyprland.sh` (D-22), and does not write live `$HOME/.config` (D-17).

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Monitor + pin overlay | `.config/hypr/custom/general.lua` (`hl.monitor` DP-1 / HDMI-A-2; `hl.workspace_rule` pins) | 13-01 |
| Empty env require slot | `.config/hypr/custom/env.lua` | 13-01 |
| Empty execs require slot | `.config/hypr/custom/execs.lua` | **13-02** |
| SoT + apply note | `.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md` | 13-01 stub; **13-02** D-18 + D-19 |

## Decisions locked in this plan

- D-01: Authoring SoT remains parent-repo `.config/hypr/custom/`
- D-02: Cold-machine apply is documented apply-after-install; Phase 13 writes the rule; Phase 14 runs apply
- D-03: Apply is one-way repo → live; no sync daemon
- D-04: Never commit machine overlays into vendor/dots-hyprland or the personal fork
- D-05: SoT + apply rule lives in `13-SOT-APPLY.md`
- D-06: `execs.lua` is an empty require slot; still no keybinds.lua or rules.lua
- D-08: No personal cursor overlay in `execs.lua`
- D-16: Real repo files exist before Phase 14
- D-17: No live `~/.config` mutation this phase
- D-18: Document (do not run): `mkdir -p` live custom/; `cp -a` the three named files; never `rsync --delete`; never copy keybinds/rules/variables; overwrite ii seeds; **fail if repo general.lua missing**; if env.lua or execs.lua missing: warn and continue
- D-19: Full in-repo verify block
- D-20: OVL-01/OVL-02/OVL-03 stay Pending until files exist AND D-19 checks pass
- D-22: Phase 12 `--full` / SAFE_DEFAULTS unchanged; no wrapper apply subcommand this phase

## Assumptions (specless probe)

- **OVL-01 unclassified (unresolved):** backstop — content remains `hl.monitor` + `hl.workspace_rule` in `custom/general.lua`
- **OVL-02 concurrency (unresolved):** interrupted work leaves no live apply and no half-written live `$HOME/.config` (D-17)
- **OVL-03 unclassified (unresolved):** SoT note states repo vs live vs fork without committing overlays into vendor
- **Assumption-delta detector** fired on the word "custom" in `hypr/custom` — **false positive** (see `<assumption_delta_decision>`)

<assumption_delta_decision>
detector fired on word "custom" in "hypr/custom" — **false positive**. decision: `no-change`. rationale: custom is the vendor overlay slot path, not a new identity model.
</assumption_delta_decision>

<tasks>

<task type="auto" id="13-02-01">
  <name>Commit empty custom/execs.lua require slot</name>
  <reversibility rating="reversible">D-06/D-08 empty execs.lua slot is a tracked blank file; undo is delete. D-17 still forbids live copies.</reversibility>
  <files>.config/hypr/custom/execs.lua</files>
  <read_first>
    - .config/hypr/custom/execs.lua (create; currently absent)
    - .planning/phases/13-personal-hypr-custom-overlays/13-CONTEXT.md (D-06, D-08, D-17, D-18)
    - .planning/phases/13-personal-hypr-custom-overlays/13-PATTERNS.md (empty execs.lua analog; test -f only)
    - vendor/dots-hyprland/dots/.config/hypr/custom/execs.lua (blank / newline seed; no Lua statements)
    - vendor/dots-hyprland/dots/.config/hypr/hyprland.lua (optional require("custom.execs") after hyprland.execs)
  </read_first>
  <action>
    Create `.config/hypr/custom/execs.lua` in the parent-repo worktree so `hyprland.lua` can `require("custom.execs")` and Phase 14 `cp -a` can overwrite ii seeds (D-06, D-18). Discretion: 0-byte or a single newline so git tracks it. **No Lua statements** — no `hl.on`, no `hl.exec_cmd` (D-08, D-21).

    Do not copy RESEARCH Patterns 4-5. Do not add a personal cursor overlay (D-08). Upstream `hyprland/execs.lua` already owns default cursor. Do not copy autostart from personal conf.

    Write only the worktree file. Do not write live `${XDG_CONFIG_HOME:-$HOME/.config}/hypr/custom/execs.lua` (D-17). Do not modify vendor (D-04). Do not use `test -s` as the pass condition for this file (D-19).
  </action>
  <verify>
    <automated>test -f .config/hypr/custom/execs.lua &amp;&amp; test -z "$(grep -vE '^[[:space:]]*$|^[[:space:]]*--' .config/hypr/custom/execs.lua || true)" &amp;&amp; test "$(realpath -m .config/hypr/custom)" != "$(realpath -m "${XDG_CONFIG_HOME:-$HOME/.config}/hypr/custom")" &amp;&amp; test -z "$(git -C vendor/dots-hyprland status --short -- dots/.config/hypr/custom)"</automated>
  </verify>
  <acceptance_criteria>
    - `.config/hypr/custom/execs.lua` exists (`test -f` only; do not `test -s`)
    - File has no Lua statements (only blank lines and/or `--` comments allowed)
    - No vendor `dots/.config/hypr/custom` dirty files (D-04)
    - Live home custom path is not the worktree path (D-17)
  </acceptance_criteria>
  <done>execs.lua is a git-trackable empty require slot with no Lua statements.</done>
</task>

<task type="auto" id="13-02-02">
  <name>Complete D-18 apply command and D-19 verify fence in 13-SOT-APPLY.md</name>
  <reversibility rating="costly">D-02/D-18: Phase 14 copies this command; a wrong apply (rsync delete, missing fail-on-general.lua) is a coordinated adopt bug.</reversibility>
  <files>.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md</files>
  <read_first>
    - .planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md (stub from 13-01)
    - .planning/phases/13-personal-hypr-custom-overlays/13-CONTEXT.md (D-01, D-02, D-03, D-04, D-05, D-18, D-19)
    - .planning/phases/13-personal-hypr-custom-overlays/13-PATTERNS.md (D-18 apply sketch; Shared Patterns Verification bash fence)
    - vendor/dots-hyprland/dots/.config/hypr/custom/execs.lua (empty-slot analog for warn-and-continue)
  </read_first>
  <action>
    Expand `13-SOT-APPLY.md` so it still includes D-01..D-05 from the stub, and add the runnable-but-not-run D-18 command plus a D-19 verify fence. Do not execute the apply against live home (D-02, D-17). Do not edit `arch/dots-hyprland.sh` (D-22).

    Apply command (document; Phase 14 runs) must specify:
    1. `mkdir -p "${XDG_CONFIG_HOME:-$HOME/.config}/hypr/custom"` if missing
    2. **Fail the apply if repo `.config/hypr/custom/general.lua` is missing** (layout required)
    3. If repo `env.lua` or `execs.lua` is missing: **warn and continue** (empty slots). Do not skip the whole apply solely because a slot file is absent
    4. **`cp -a` the three named files** (`general.lua`, `env.lua`, `execs.lua`) from repo `.config/hypr/custom/` onto live `~/.config/hypr/custom/` — overwrite ii seeds if present
    5. Never `rsync --delete`. Never copy `keybinds.lua` / `rules.lua` / `variables.lua`
    6. Mention apply-then-reload / re-login before expecting dual-head (`create_custom_config.lua` seeds after require; reload is commented out upstream)
    7. Persist live tweaks by copy-back into repo `.config/hypr/custom/` (D-03). No sync daemon

    Also paste, under a heading **In-repo verify (D-19)** and a bash fence that **starts with** `test -s .config/hypr/custom/general.lua`, the Shared Patterns Verification block from `13-PATTERNS.md` (existence, content greps including literal `scale = "auto"`, `cp -a` and fail-if-general.lua-missing greps on this note, negative greps for dropped D-08/D-09 overlay tokens and D-17 leakage strings listed in CONTEXT D-19, no root monitors.lua/workspaces.lua, no custom/keybinds.lua or custom/rules.lua, vendor custom status empty). Copy that PATTERNS fence verbatim so PLAN.md does not need to restate dropped-token literals.

    Do not treat CONTEXT.md, RESEARCH.md, or this PLAN.md as OVL completion (D-20).
  </action>
  <verify>
    <automated>test -f .planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md &amp;&amp; grep -q 'cp -a' .planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md &amp;&amp; grep -q 'general.lua' .planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md &amp;&amp; grep -Eiq 'fail' .planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md &amp;&amp; grep -Eiq 'warn' .planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md &amp;&amp; grep -q 'rsync --delete' .planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md &amp;&amp; grep -q 'test -s .config/hypr/custom/general.lua' .planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md &amp;&amp; grep -Eiq 'authoring SoT|Authoring SoT' .planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md &amp;&amp; test -z "$(git diff --name-only -- arch/dots-hyprland.sh)" &amp;&amp; test -z "$(git -C vendor/dots-hyprland status --short -- dots/.config/hypr/custom)"</automated>
  </verify>
  <acceptance_criteria>
    - `13-SOT-APPLY.md` names `cp -a` and fail-if-`general.lua`-missing (D-18, D-19, OVL-03)
    - Notes warn-and-continue for missing env.lua/execs.lua
    - Mentions never `rsync --delete` and never copying keybinds/rules/variables
    - Contains an In-repo verify (D-19) bash fence starting with `test -s .config/hypr/custom/general.lua`
    - Still names authoring SoT as `.config/hypr/custom/` (D-01)
    - `arch/dots-hyprland.sh` unmodified (D-22)
    - Vendor custom tree clean (D-04)
  </acceptance_criteria>
  <done>Apply command and D-19 fence are written; apply was not run; wrapper untouched.</done>
</task>

<task type="auto" id="13-02-03">
  <name>Run full D-19 in-repo verify against disk</name>
  <reversibility rating="costly">D-17 would be one-way if the documented apply ran against live ~/.config; this task only runs in-repo greps and must not cp to live. Costly flag only — no checkpoint because the live door is not taken.</reversibility>
  <files>.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md</files>
  <read_first>
    - .planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md (In-repo verify (D-19) fence from task 13-02-02)
    - .planning/phases/13-personal-hypr-custom-overlays/13-CONTEXT.md (D-17, D-19, D-20)
    - .planning/phases/13-personal-hypr-custom-overlays/13-PATTERNS.md (Shared Patterns Verification analog)
    - .config/hypr/custom/general.lua
    - vendor/dots-hyprland/dots/.config/hypr/custom/execs.lua (confirm we did not dirty vendor)
  </read_first>
  <action>
    Run the **In-repo verify (D-19)** bash fence from `13-SOT-APPLY.md` against the worktree. Do **not** run the apply command (D-02, D-17). Do not write `${XDG_CONFIG_HOME:-$HOME/.config}/hypr/custom/` (D-17). Do not edit `REQUIREMENTS.md` checkboxes here unless a later GSD verify-phase owns that; D-20 says OVL IDs stay Pending until files exist **and** these checks pass — passing this task is the disk proof, not a CONTEXT-only claim.

    If a D-19 check fails, fix the repo overlay files or the SoT note (still no live writes). Do not add `custom/keybinds.lua` or `custom/rules.lua`. Do not create `.config/hypr/monitors.lua` or `workspaces.lua`. Do not change `arch/dots-hyprland.sh` (D-22).

    Extract the bash fence whose body contains `test -s .config/hypr/custom/general.lua` (the D-19 fence, not the apply example) and execute it with `bash -e` from the repo root.
  </action>
  <verify>
    <automated>python3 -c "from pathlib import Path; t=Path('.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md').read_text(); parts=t.split('```bash'); block=next(p.split('```')[0] for p in parts[1:] if 'test -s .config/hypr/custom/general.lua' in p); Path('/tmp/p13-d19.sh').write_text(block)" &amp;&amp; bash -e /tmp/p13-d19.sh &amp;&amp; test -s .config/hypr/custom/general.lua &amp;&amp; test -f .config/hypr/custom/env.lua &amp;&amp; test -f .config/hypr/custom/execs.lua &amp;&amp; grep -q 'scale = "auto"' .config/hypr/custom/general.lua &amp;&amp; grep -q 'cp -a' .planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md &amp;&amp; test "$(realpath -m .config/hypr/custom)" != "$(realpath -m "${XDG_CONFIG_HOME:-$HOME/.config}/hypr/custom")" &amp;&amp; test -z "$(git diff --name-only -- arch/dots-hyprland.sh)" &amp;&amp; test ! -e .config/hypr/monitors.lua &amp;&amp; test ! -e .config/hypr/workspaces.lua &amp;&amp; test ! -e .config/hypr/custom/keybinds.lua &amp;&amp; test ! -e .config/hypr/custom/rules.lua</automated>
  </verify>
  <acceptance_criteria>
    - D-19 fence in `13-SOT-APPLY.md` executes successfully from repo root
    - `general.lua` exists and is non-empty; `env.lua` and `execs.lua` exist (do not `test -s` those two as the pass condition)
    - `general.lua` still has DP-1, HDMI-A-2, hl.workspace_rule, special:social, literal `scale = "auto"` (OVL-01)
    - `13-SOT-APPLY.md` names `cp -a` and fail-if-general.lua-missing (OVL-03)
    - No live home custom path identity with the worktree (D-17)
    - No root monitors.lua/workspaces.lua; no custom/keybinds.lua or custom/rules.lua
    - Wrapper file unmodified (D-22)
    - Dropped D-08/D-09 overlay tokens and D-17 leakage strings are absent from `custom/*.lua` via the PATTERNS/D-19 fence
  </acceptance_criteria>
  <done>D-19 checks pass on disk; apply was not run; OVL proof is files + verify, not prose-only (D-20).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worktree `.config/hypr/custom/` → live `~/.config/hypr/custom/` | Documented apply would copy Lua into the session tree; this phase must not cross it (D-17) |
| Parent repo → `vendor/dots-hyprland` / personal fork | Machine overlays must not enter product SoT (D-04) |
| Operator paste of apply command | A destructive apply (`rsync --delete`, missing general.lua) could wipe or skip layout |

ASVS L1; block on high.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-live-mutation | Tampering | live `$HOME/.config` | high | mitigate | D-17: do not run apply; verify worktree realpath ≠ live custom; files_modified has no live home paths |
| T-vendor-pollution | Tampering | vendor/dots-hyprland custom/ | high | mitigate | D-04: never write vendor/fork; vendor status --short empty |
| T-rsync-delete | Denial of service | apply command in 13-SOT-APPLY.md | high | mitigate | D-18: document `cp -a` named files only; never `rsync --delete`; never copy keybinds/rules/variables |
| T-apply-missing-general.lua | Denial of service | cold-machine apply | high | mitigate | D-18: fail if repo `general.lua` missing; warn-and-continue only for env.lua/execs.lua |
| T-D17-leakage | Tampering | `custom/*.lua` autostart/apps | high | mitigate | D-19 negative greps (chrome, vesktop/discord, waybar, swaync, `qs -c ii`, google-chrome) plus dropped D-08/D-09 tokens |
| T-13-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase |
</threat_model>

<verification>
Empty execs.lua exists; 13-SOT-APPLY.md names cp -a and fail-if-general.lua-missing; D-19 fence runs clean; no live apply; wrapper unchanged; vendor custom clean.
</verification>

<success_criteria>
Repo has three named custom files (general.lua with dual-head + pins; empty env.lua and execs.lua) and a SoT note Phase 14 can follow, with D-19 passing, without live `$HOME/.config` mutation.
</success_criteria>

<output>
Create `.planning/phases/13-personal-hypr-custom-overlays/13-02-SUMMARY.md` when done
</output>
