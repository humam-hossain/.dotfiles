---
phase: 16-retire-the-safe-profile-full-only-wrapper-and-playbook
plan: 06
type: execute
wave: 4
depends_on: ["16-04", "16-05"]
files_modified:
  - .planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md
  - scripts/phase13-d19-assert.sh
autonomous: true
requirements: [W-3, D-38, D-41, D-43]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A written sweep record exists at `16-DOC-SWEEP.md` recording what changed where, in the same shape as the Phase 15 record (D-41)."
    - "`scripts/phase13-d19-assert.sh` passes again: its wrapper drift baseline is re-pinned to the commit that holds the final Phase 16 wrapper, keyed on the presence of the sweep record (D-38 part 1)."
    - "The new baseline tier is tested before the existing ones, because the marker file the second tier keys on still exists and an `elif` placed after it would never fire (research Pattern 3)."
    - "The playbook's duplicated apply fence is now checked against the Phase 13 source-of-truth fence, using the extractor already in that script rather than a second extraction idiom (W-3, D-38 part 2)."
    - "The sweep record and the re-pin land in the same wave, ahead of the phase gate, so no gate ever runs in the window where the marker exists but the tier does not (D-43)."
  artifacts:
    - path: ".planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md"
      provides: "The phase sweep record, and the marker file the drift baseline keys on"
      contains: "## Corrections applied"
    - path: "scripts/phase13-d19-assert.sh"
      provides: "Overlay asserts plus a three-tier wrapper drift baseline and the apply-fence drift check"
      contains: "16-DOC-SWEEP.md"
  key_links:
    - from: "scripts/phase13-d19-assert.sh"
      to: ".planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md"
      via: "a filesystem presence test selects the Phase 16 drift baseline"
      pattern: "16-DOC-SWEEP\\.md"
    - from: "scripts/phase13-d19-assert.sh"
      to: "docs/dots-hyprland-workflow.md"
      via: "the apply fence is extracted from the playbook and compared against the Phase 13 source of truth"
      pattern: "dots-hyprland-workflow\\.md"
    - from: "scripts/phase13-d19-assert.sh"
      to: "arch/dots-hyprland.sh"
      via: "a git range comparison against the pinned baseline commit"
      pattern: "arch/dots-hyprland\\.sh"
  prohibitions:
    - statement: "The drift check is not loosened, disabled or made advisory. It is re-pinned to a new baseline; the pass/fail verdict block below the tier selection stays exactly as it is (D-38)."
      status: active
      verification: backstop
    - statement: "No second Markdown-fence extraction idiom is introduced. The existing extractor in this script is parameterised and reused, and the fence is never hand-transcribed into the assert."
      status: active
      verification: backstop
    - statement: "No new assert script is created for the fence-drift check; it is added to `scripts/phase13-d19-assert.sh` (D-38 part 2, Claude's Discretion resolution)."
      status: active
      verification: backstop
    - statement: "Neither fence body is edited to make the comparison pass. The two copies already agree apart from one comment line, and the filter for that line is documented in the script rather than applied silently."
      status: active
      verification: backstop
    - statement: "The sweep record quotes stale strings verbatim as findings; it is therefore never added to any forbidden-string grep surface, and the documentation ban in `scripts/phase16-retire-assert.sh` stays scoped to the playbook alone (D-36)."
      status: active
      verification: backstop
    - statement: "`arch/dots-hyprland.sh` is not modified by this plan. Any wrapper edit after the pin invalidates the baseline, because the drift check compares the pinned commit against the working tree rather than against HEAD."
      status: active
      verification: backstop
    - statement: "WR-02 and the D-38 `graphical-session.target` bootstrap stay open and unowned; the sweep record lists them as deferred without owners (CONTEXT.md 'Not in this phase')."
      status: active
      verification: backstop
---

<objective>
Write the phase's sweep record and repair the one assert this phase deliberately broke.

`scripts/phase13-d19-assert.sh` hard-fails on any wrapper diff from a pinned commit, and it has been red since plan `16-01`'s first commit. That is the check working as designed — the wrapper changed on purpose — but the phase gate runs this script, so as written the gate contradicts itself. The repair is a third baseline tier in the file's own existing idiom, keyed on the presence of the sweep record.

That coupling is why these two files land together: the tier test is a filesystem check, so between the wrapper commit and the sweep record's arrival the assert would fail on every run. Both land in this wave, ahead of the gate (D-43).

The same script also gains the fence-drift check the phase's third audit leftover asks for. The playbook duplicates the Phase 13 apply fence and nothing has ever compared the copy; this script is already where that fence is extracted and executed, so the check goes here rather than into a new script.

Purpose: leave the phase with a written record of what changed where, and with every assert green on a committed tree.
Output: `16-DOC-SWEEP.md`, and a three-tier drift baseline plus a fence-drift assert in `scripts/phase13-d19-assert.sh`.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-PATTERNS.md
@.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md
</context>

<tasks>

<!-- planner-discipline-allow: SAFE_DEFAULTS -->
<!-- planner-discipline-allow: dual-run -->
<!-- planner-discipline-allow: safe profile -->
<!-- planner-discipline-allow: --skip-hyprland -->
<!-- planner-discipline-allow: Phase 14 only -->

<task type="auto">
  <name>Task 1: Write the phase sweep record</name>
  <read_first>
    - `.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md` — read in full. It is the format this record follows: a title, a scope paragraph, the standing justification for why forbidden-string asserts never point at the sweep file itself, then `## Corrections applied` with one `### <file path>` subsection each, `## Reviewed, no findings`, `## Flagged, not edited`, `## Deferred fixes`, and `## Phase gate`.
    - The five summaries written so far in this phase: `16-01-SUMMARY.md` through `16-05-SUMMARY.md`. They are the source for the corrections tables — this record consolidates what those plans actually did, not what they were planned to do.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md` — D-41 (write the record), D-38 and D-43 (the record is also a marker file), and the deferred-ideas block, which is the source for the deferred section.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-PATTERNS.md` — the `16-DOC-SWEEP.md` section, which quotes the preamble and the four-column correction table shape.
  </read_first>
  <files>.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md</files>
  <action>
Write the record. It has two jobs: it is the phase's account of what changed where, and it is the marker file the drift baseline keys on. Both jobs are satisfied by the file existing at exactly this path with real content.

**Preamble.** Title it as the Phase 16 retirement sweep. One scope paragraph naming what this phase touched: the wrapper, the assert scripts, the two operator documents, and the planning artifacts. Then reuse the Phase 15 record's second paragraph almost verbatim, because it is the standing justification and it applies unchanged: this is a findings report, not operator instruction; stale strings are quoted verbatim below on purpose; which is why this phase's forbidden-string assertions are scoped to the operator-facing playbook and never to this file.

Add one sentence the Phase 15 record does not have: this file is also a marker. `scripts/phase13-d19-assert.sh` selects its wrapper drift baseline by testing for this file's presence, so moving or renaming it silently changes which commit the drift check compares against.

**`## Corrections applied`.** One `### <file path>` subsection per file this phase changed, in wave order. For each, a four-column table — Line, Stale text, Correction, Severity — with the stale text quoted verbatim in backticks and the correction citing the plan that made it. Cover at minimum:
- `arch/dots-hyprland.sh` — the argv injection, the gate, the two machinery clusters, the usage heredoc. For a deletion-heavy file, use the symbol name in the Line column rather than a line number, since the numbers moved.
- `scripts/phase12-full-smoke.sh`, `scripts/phase14-verify.sh`, and the two deleted scripts.
- `docs/dots-hyprland-workflow.md` and `docs/phase14-adopt-runbook.md`.
- `scripts/phase16-retire-assert.sh` as a creation rather than a correction.

Severity is HIGH for anything a reader could have acted on to their harm, MEDIUM for a false statement with no action attached, LOW for cosmetic staleness.

**`## Reviewed, no findings`.** Name the files that were read and left alone with the reason: `scripts/phase10-inventory-assert.sh`, which deliberately keeps requiring retired language in a frozen Phase 10 record; `README.md`, whose single wrapper mention carries no profile or backup claim; and the archived session-chrome trees, which stay in the repo untouched.

**`## Flagged, not edited`.** The frozen artifacts under `.planning/phases/**` and `.planning/milestones/**` that now contradict current state and are deliberately left as records, plus the six dated codebase snapshots that go false in this phase and are refreshed by a mapper re-run afterwards rather than hand-edited. Use the Phase 15 record's table shape so one superseded-annotation vocabulary is used across the phase.

**`## Deferred fixes`.** Two rows, both unowned: the three roles of the repo copy of the pre-adopt compositor config, and the compositor-target autostart bootstrap. State for each what is wrong, why it is not fixed here, and that the owner is still nobody. Do not invent an owner.

**`## Planning-artifact corrections`** and **`## Phase gate`.** Add both headings now with a one-line note that they are filled after wave 5 and at the gate respectively. Plan `16-10` fills them. The record must exist in this wave for the marker to work; its later sections can grow.

Write `Waybar`, `rofi` and `swaync` literally throughout.
  </action>
  <verify>
    <automated>D=.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md; test -s "$D" && for h in '## Corrections applied' '## Reviewed, no findings' '## Flagged, not edited' '## Deferred fixes' '## Planning-artifact corrections' '## Phase gate'; do grep -qF "$h" "$D" || { echo "MISSING HEADING: $h"; exit 1; }; done</automated>
    <fails_when>a `MISSING HEADING:` line is printed and the loop exits 1, or the file is empty — the record does not follow the Phase 15 shape, or the two sections plan `16-10` fills were not stubbed</fails_when>
    <automated>D=.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md; test "$(grep -c '^### ' "$D")" -ge 6 && test "$(grep -cE '^\| .* \| .* \| .* \| (HIGH|MEDIUM|LOW) \|' "$D")" -ge 8 && grep -q 'phase13-d19-assert' "$D" && grep -qE 'WR-02' "$D" && grep -q 'graphical-session.target' "$D"</automated>
    <fails_when>non-zero exit — fewer than six per-file subsections (the phase touched more files than that), fewer than eight severity-tagged correction rows, no statement that this file is the marker the drift baseline keys on, or a deferred row missing for one of the two items that stay unowned</fails_when>
    <automated>D=.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md; test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' "$D" | grep -vx 'google-chrome-stable' | wc -l)" -eq 0 && ./scripts/phase16-retire-assert.sh >/tmp/p16-06-r.txt 2>&1; grep -q '^=== done: FAIL=0 ===$' /tmp/p16-06-r.txt</automated>
    <fails_when>the collective-noun count is above 0, or the phase-16 assert summary is not exactly `=== done: FAIL=0 ===` — the second check confirms the documentation ban was not accidentally widened to cover this file, which quotes the banned strings by design</fails_when>
    <automated>./scripts/phase13-d19-assert.sh >/tmp/p16-06-d19.txt 2>&1 || true; test "$(grep -c '^\[FAIL\]' /tmp/p16-06-d19.txt)" -eq 1 && grep -q '^\[FAIL\] arch/dots-hyprland.sh changed since' /tmp/p16-06-d19.txt</automated>
    <fails_when>non-zero exit — the overlay assert prints a `[FAIL]` count other than 1, or its single failure is not the `arch/dots-hyprland.sh changed since` drift line. Either means a second thing regressed while the marker file was written, and task 2 must not re-pin on top of it.</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md` exists, is non-empty, and contains all six required `## ` headings.
    - The record has at least six `### <file path>` subsections and at least eight four-column correction rows tagged HIGH, MEDIUM or LOW.
    - The record states that `scripts/phase13-d19-assert.sh` keys its drift baseline on this file's presence.
    - The deferred section has a row for the three-roles item and a row for the compositor-target autostart item, both with no owner named.
    - The record names `Waybar`, `rofi` and `swaync` literally; non-allowlisted `chrome` tokens: 0.
    - `./scripts/phase16-retire-assert.sh` still ends in exactly `=== done: FAIL=0 ===`.
    - `./scripts/phase13-d19-assert.sh` prints exactly one `[FAIL]` line, and it is the wrapper drift failure.
  </acceptance_criteria>
  <done>The phase has a written record of what changed where, in the same shape and vocabulary as the previous phase's record, with two sections stubbed for wave 5 and the gate. The file is now present at the path the drift baseline will key on, which is the precondition for task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Re-pin the wrapper drift baseline and add the apply-fence drift assert</name>
  <precondition>`.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md` exists on disk (created by task 1) — the new baseline tier is selected by a filesystem test on that path, so without it the tier never fires.</precondition>
  <read_first>
    - `scripts/phase13-d19-assert.sh` — read in full. Two regions matter: the python3 fence extractor near the top, and the two-tier baseline block with the git verdict below it. Note the file uses POSIX `[ ... ]` tests throughout rather than the bracket-doubled form used in the wrapper-behavior smoke; match the local style.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-PATTERNS.md` — the two `scripts/phase13-d19-assert.sh` sections: the third-tier pattern with its ordering warning, and the fence-drift section with the parameterised extractor and the comparison shape.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md` — Pattern 3, Pitfall 2 (three constraints that fall out of the drift check comparing a commit against the working tree), and §"Code Examples" for the verified fence relationship and the exact filter line.
    - `.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md` — the apply-command section and its fence, which is one side of the comparison.
    - `docs/dots-hyprland-workflow.md` — the overlay section's named-files subsection and its fence, which is the other side. Read it as left by plan `16-04`; that plan was forbidden from editing it.
  </read_first>
  <files>scripts/phase13-d19-assert.sh</files>
  <action>
Two additions to one script. Neither replaces an existing mechanism; both extend one.

**A. The third baseline tier.**

Resolve the baseline commit at execution time rather than copying a SHA out of a planning document. Run `git log -1 --format=%h -- arch/dots-hyprland.sh` to get the last commit that touched the wrapper — that is plan `16-02`'s usage-heredoc commit — and confirm with `git diff --name-only <that SHA> -- arch/dots-hyprland.sh` that the output is empty. If it is not empty, the wrapper has uncommitted changes and the pin must not be written until they are committed. The drift check compares the pinned commit against the **working tree**, not against HEAD, so a baseline that is not byte-identical to the working tree is a baseline that fails on day one.

Declare a path constant for the sweep record next to the existing marker constant, following the file's convention of constants at the top. Then extend the existing `if`/`else` baseline selection into a three-branch chain with the **new tier first**. The Phase 14 marker file still exists, so a branch placed after it would never be reached — this is the single most likely way to get the edit wrong.

Each branch carries a same-line comment naming the commit subject, exactly as the two existing pins do. Write the new one as the commit subject of the wrapper commit you resolved, so a later reader can find it without running git.

Leave the verdict block below the chain untouched: the `git cat-file -e` existence probe, the informational branch when the baseline is not in the repository, and the pass/fail pair. Do not weaken any of them and do not turn the failure into an informational line.

**B. The apply-fence drift assert.**

The playbook's overlay section duplicates the Phase 13 apply fence and nothing has ever compared the copy. Research verified the two are byte-identical after removing exactly one line from the source-of-truth copy — a comment restricting the fence to the adopt phase.

Parameterise the python3 extractor already in this file over two arguments, the file and the heading to search for, so it can run twice. Do not add an `awk` or `sed` extraction as a second idiom: the script already pays the python3 cost and its extractor is proven, and two extraction idioms for one job guarantee the two drift. Do not hand-transcribe either fence into the assert.

Extract the source-of-truth fence under its apply-command heading and pipe it through a filter dropping the single adopt-phase-only comment line. **Document that filter in a comment naming why it exists**, or a later reader will read it as the assert cheating. Extract the playbook fence under its named-files subheading. Compare the two strings; on a match, pass; on a mismatch, fail and print a `diff` of the two with a trailing `|| true`, since `diff` exits non-zero on difference and the script runs under `set -e`.

Guard both extractions: if either comes back empty, that is a failure with its own message, not a silently-passing comparison of two empty strings. The existing extractor already has this guard for its single use; mirror it.

Keep the existing execution half — the source-of-truth fence is written to a tempfile and run — exactly as it is. The playbook copy is compared, not executed; it stays operator-runnable in the document, and executing it twice would double the side effects of a procedure that touches a live configuration tree.

**C. Do not touch `arch/dots-hyprland.sh` in this task or in any later plan.** The pin you write is only valid while the working tree matches it.
  </action>
  <verify>
    <automated>bash -n scripts/phase13-d19-assert.sh && ./scripts/phase13-d19-assert.sh >/tmp/p16-06b-d19.txt 2>&1; tail -2 /tmp/p16-06b-d19.txt; test "$(grep -c '^\[FAIL\]' /tmp/p16-06b-d19.txt)" -eq 0 && grep -q 'arch/dots-hyprland.sh unmodified since' /tmp/p16-06b-d19.txt</automated>
    <fails_when>`bash -n` exits non-zero, any `[FAIL]` line is printed, or the run never reaches the `arch/dots-hyprland.sh unmodified since` pass line — the last case means the baseline commit exists but the tier chain selected the wrong branch, or the informational not-in-repo branch fired because a bad SHA was written</fails_when>
    <automated>grep -q 'W-3' scripts/phase13-d19-assert.sh && grep -q 'dots-hyprland-workflow.md' scripts/phase13-d19-assert.sh && grep -q '16-DOC-SWEEP.md' scripts/phase13-d19-assert.sh && test "$(grep -c 'python3 - ' scripts/phase13-d19-assert.sh)" -eq 1 && ! grep -qE '^\s*(awk|sed) .*```bash' scripts/phase13-d19-assert.sh</automated>
    <fails_when>non-zero exit — the fence-drift assert is not labelled, does not name the playbook, the sweep-record marker constant is missing, the python3 extractor was duplicated instead of parameterised (a count other than 1), or a second fence-extraction idiom built on `awk` or `sed` was introduced</fails_when>
    <automated>grep -qF 'Phase 14 only' scripts/phase13-d19-assert.sh && grep -B2 -A2 'Phase 14 only' scripts/phase13-d19-assert.sh | grep -qiE '#.*(filter|only difference|load-bearing|expected)'</automated>
    <fails_when>non-zero exit — the load-bearing filter that drops the one differing comment line is either absent (so the assert fails against two files that agree) or present without a comment explaining why, which reads to a later maintainer as the assert being rigged to pass</fails_when>
    <automated>T="$(mktemp /tmp/p16-06-fence-XXXXXX.md)"; cp docs/dots-hyprland-workflow.md "$T"; sed -i '0,/^# Fail if repo general.lua is missing/s//# Fail if repo general.lua is absent/' docs/dots-hyprland-workflow.md; ./scripts/phase13-d19-assert.sh >/tmp/p16-06-neg.txt 2>&1; RC=$?; cp "$T" docs/dots-hyprland-workflow.md; rm -f "$T"; echo "negative-control rc=$RC"; test "$RC" -ne 0 && grep -q 'W-3' /tmp/p16-06-neg.txt</automated>
    <fails_when>the echoed `negative-control rc=` value is 0, or no `W-3` line appears in the captured output — the assert passed a playbook fence that was deliberately perturbed by one word, meaning the drift check is not actually comparing the two fences. The command restores the playbook from a copy before it asserts, so a failure here leaves the file unmodified.</fails_when>
    <automated>./scripts/phase16-retire-assert.sh >/tmp/p16-06-r2.txt 2>&1; ./scripts/phase12-full-smoke.sh >/tmp/p16-06-s.txt 2>&1; ./scripts/phase11-dispositions-assert.sh >/tmp/p16-06-dd.txt 2>&1; ./scripts/phase10-inventory-assert.sh >/tmp/p16-06-i.txt 2>&1; for f in /tmp/p16-06-r2.txt /tmp/p16-06-s.txt /tmp/p16-06-dd.txt /tmp/p16-06-i.txt; do grep -q '^=== done: FAIL=0 ===$' "$f" || { echo "NOT GREEN: $f"; exit 1; }; done</automated>
    <fails_when>a `NOT GREEN: <path>` line is printed and the loop exits 1 — one of the four tree-clean-agnostic suites regressed while this script was edited; all four must be green before the phase reaches wave 5</fails_when>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase13-d19-assert.sh` exits 0 and a run prints zero `[FAIL]` lines.
    - The run includes a pass line reading `arch/dots-hyprland.sh unmodified since <baseline>`, where the baseline is the commit that holds the final Phase 16 wrapper.
    - The baseline selection is a three-branch chain whose **first** branch tests for `16-DOC-SWEEP.md`; each branch carries a same-line comment naming its commit subject.
    - The `git cat-file -e` probe and the pass/fail verdict pair below the chain are unchanged.
    - The script contains exactly one `python3 - ` invocation, now parameterised over file and heading; no `awk` or `sed` fence-extraction idiom was added.
    - The script contains a `W-3`-labelled assert naming `docs/dots-hyprland-workflow.md`, guarding both extractions against emptiness and printing a `diff` with `|| true` on mismatch.
    - The one-line filter that reconciles the two fences is present and carries an explanatory comment adjacent to it.
    - Perturbing one word inside the playbook fence makes the script exit non-zero with a `W-3` line; the negative control restores the file afterwards.
    - `./scripts/phase16-retire-assert.sh`, `./scripts/phase12-full-smoke.sh`, `./scripts/phase11-dispositions-assert.sh` and `./scripts/phase10-inventory-assert.sh` all end in exactly `=== done: FAIL=0 ===`.
    - `git status --porcelain -- arch/dots-hyprland.sh` prints nothing.
  </acceptance_criteria>
  <done>All five tree-clean-agnostic suites are green. The wrapper drift check is live again against a baseline that matches the working tree, selected by the sweep record's presence in the file's own existing tiering idiom. The playbook's duplicated apply fence is now compared against the Phase 13 source of truth on every run, using one extractor rather than two, with the reconciling filter documented rather than silent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assert script → phase gate | Four of these scripts are the gate. A drift check re-pinned to the wrong commit, or loosened to pass, stops being evidence while still printing a pass line. |
| source-of-truth fence → its duplicate in the playbook | An operator runs the playbook copy against a live configuration tree. Nothing compared the copy until now; a silent fork is a procedure that diverges from the record it claims to follow. |
| marker file path → baseline selection | The tier is chosen by a filesystem test. Moving or renaming the sweep record silently changes which commit the wrapper is compared against. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-35 | Repudiation | wrapper drift check | high | mitigate | The baseline is resolved at execution time from `git log -1 -- arch/dots-hyprland.sh` and confirmed empty-diff before being written; the verdict block is asserted unchanged so "re-pin" cannot become "disable" |
| T-16-36 | Tampering | tier ordering | high | mitigate | The new branch must be first; the acceptance criteria require it, and the pass line naming the selected baseline is asserted, so a chain that falls through to the Phase 14 pin fails visibly rather than silently |
| T-16-37 | Tampering | the fence-drift assert | high | mitigate | A negative control perturbs one word inside the playbook fence, requires a non-zero exit and a `W-3` line, and restores the file — a vacuously-passing comparison fails the task |
| T-16-38 | Repudiation | the reconciling filter | medium | mitigate | The filter must exist and must carry an adjacent explanatory comment; without both, a maintainer cannot distinguish a documented reconciliation from a rigged assert |
| T-16-39 | Denial of Service | duplicated extraction idiom | medium | mitigate | Exactly one `python3 - ` invocation is permitted and no `awk`/`sed` fence extractor may be added, so the two extractions cannot drift apart from each other |
| T-16-40 | Tampering | wrapper edited after the pin | high | mitigate | `git status --porcelain -- arch/dots-hyprland.sh` must be empty at the end of this task, and the prohibition block forbids any later plan from touching the wrapper |
| T-16-41 | Information Disclosure | the sweep record | low | accept | The record quotes repository prose and paths. It names no credential, and it is deliberately excluded from every forbidden-string grep because quoting stale strings is its job. |
| T-16-SC | Tampering | package-manager installs | low | accept | This plan adds no `npm` / `pip` / `cargo` / `pacman` install step. `16-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable, with no `[ASSUMED]`, `[SUS]` or `[SLOP]` entries. |
</threat_model>

## Artifacts this phase produces

**Created by this plan:**

| Artifact | Kind | Note |
|----------|------|------|
| `.planning/phases/16-.../16-DOC-SWEEP.md` | new file | the phase sweep record, and the marker the drift baseline keys on (D-41, D-38, D-43) |
| `## Corrections applied`, `## Reviewed, no findings`, `## Flagged, not edited`, `## Deferred fixes` | new headings in the sweep record | filled in this plan |
| `## Planning-artifact corrections`, `## Phase gate` | new headings in the sweep record | stubbed here, filled by plan `16-10` |
| third baseline tier in `scripts/phase13-d19-assert.sh` | new branch + path constant | first in the chain, keyed on the sweep record (D-38 part 1) |
| `W-3` apply-fence drift assert | new assert block in `scripts/phase13-d19-assert.sh` | reuses the existing python3 extractor, parameterised (D-38 part 2) |

**Deleted by this plan:** nothing. Both changes are additive; the existing verdict block, the existing two tiers and the existing fence execution are all preserved.

**Explicitly NOT touched:** `arch/dots-hyprland.sh` (any edit invalidates the pin), both fence bodies, `scripts/phase10-inventory-assert.sh`, and the documentation ban's scope in `scripts/phase16-retire-assert.sh`.

## Flagged Assumptions

| # | Item | Status | Why it is flagged |
|---|------|--------|-------------------|
| A4 | The re-pin cites the last commit touching the wrapper, resolved at execution time, rather than a SHA copied from a planning document | resolved by the planner | Research assumed a single wave-1 wrapper commit. This plan set splits the wrapper across two plans, so a hard-coded SHA from either would be wrong. Resolving at execution time and confirming an empty diff is self-correcting and survives a re-run. |
| A6 | The sweep record's filename and its path under the Phase 16 directory are what the tier keys on | resolved against CONTEXT.md | D-41 and D-38 both name the file; the directory is verified. The record itself now states the coupling, so a later rename cannot happen silently. |
| A18 | The playbook fence is compared but not executed, while the source-of-truth fence continues to be executed | resolved by the planner | The fence touches a live configuration tree. Executing both copies would double the side effects of one procedure. Comparison is what W-3 actually asks for. |
| A19 | `16-DOC-SWEEP.md` is written in wave 4 but records wave-5 corrections in a section filled later | divergence, deliberate | D-43 requires the file to exist by this wave because the tier keys on it, while the planning-artifact corrections do not happen until wave 5. Stubbing the two later sections satisfies both without asserting anything untrue at any point. |

<verification>
- All five assert scripts must be green at the end of this plan. This is the first point in the phase where that is true, and it is the precondition for the wave-5 planning-artifact edits.
- The drift check's semantics are the thing to hold on to: `git diff <BASE> -- <path>` compares BASE against the **working tree**, not against HEAD. Three consequences — the wrapper must be committed before the pin, the pin must name a commit whose blob matches the working tree, and no later plan may touch the wrapper.
- The tier ordering constraint is not stylistic. The Phase 14 marker file still exists on disk, so a branch placed after its test never executes.
- Both negative controls in this plan restore the file they perturb from a copy taken before the perturbation, and they assert afterwards, so a failing control cannot leave the repository modified.
- `scripts/phase14-verify.sh` is still not run here. Its clean-tree assertion cannot pass mid-wave; it runs at the gate in plan `16-10`.
</verification>

<success_criteria>
- The phase has a written record of what changed where.
- Every assert script in the repository is green.
- The wrapper drift check is live against a correct baseline, chosen by a tier that actually fires.
- The playbook's duplicated apply fence is checked against its source of truth, and the check demonstrably fails when the copy drifts.
</success_criteria>

<output>
Create `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-06-SUMMARY.md` when done.
</output>
