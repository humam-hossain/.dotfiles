---
phase: 16-retire-the-safe-profile-full-only-wrapper-and-playbook
plan: 10
type: execute
wave: 7
depends_on: ["16-09"]
files_modified:
  - .planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md
autonomous: true
requirements: [ADOPT-02, ADOPT-03, D-40, D-41]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The phase sweep record is complete: its planning-artifact section lists what wave 5 and wave 6 corrected, and its phase-gate section records the gate transcript (D-41)."
    - "All five assert scripts run green on a committed, clean working tree, with the live verification suite at zero failures and its one known finding (D-40)."
    - "The wrapper drift baseline still holds — nothing modified `arch/dots-hyprland.sh` after the pin (D-38)."
    - "The session still loads through the ii Lua entry and the retired session chrome is still stopped, verified after the phase's changes rather than before them (ADOPT-02, ADOPT-03)."
    - { statement: "A real re-login into a fresh session confirms the desktop still comes up and behaves as it did before the phase, with the compositor-target autostart loss unchanged rather than newly worse.", verification: backstop }
  artifacts:
    - path: ".planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md"
      provides: "The completed phase sweep record including the gate transcript"
      contains: "## Phase gate"
  key_links:
    - from: ".planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md"
      to: "scripts/phase13-d19-assert.sh"
      via: "the record documents that it is the marker file selecting the wrapper drift baseline"
      pattern: "phase13-d19-assert"
    - from: "scripts/phase14-verify.sh"
      to: "docs/dots-hyprland-workflow.md"
      via: "the gate transcript's summary line must match the expectation the playbook quotes"
      pattern: "FINDINGS"
  prohibitions:
    - statement: "No file outside the sweep record is modified by this plan. If the gate finds a real defect, it is recorded and handed to a follow-up rather than fixed inline, because fixing it would dirty the tree the gate just certified."
      status: active
      verification: backstop
    - statement: "The known compositor-target finding stays emitted and stays allowed. It is not silenced, resolved or given an owner (D-40, CONTEXT.md 'Not in this phase')."
      status: active
      verification: backstop
    - statement: "Nothing in this plan runs a live install, a live uninstall or any package operation. The only live action is a re-login the operator performs."
      status: active
      verification: backstop
    - statement: "`~/ii-original-dots-backup` and `~/ii-original-dots-backup.20260904T171128Z` are not moved, rotated, renamed or removed."
      status: active
      verification: backstop
    - statement: "`arch/dots-hyprland.sh` is not modified. The drift baseline pinned in plan `16-06` compares against the working tree, so any edit here turns the gate red for a reason unrelated to the gate."
      status: active
      verification: backstop
    - statement: "WR-02 stays open and unowned (CONTEXT.md 'Not in this phase')."
      status: active
      verification: backstop
---

<objective>
Close the phase: finish the sweep record and run the gate on a committed, clean tree.

The gate is five assert scripts plus one human action. Four of the five are tree-clean-agnostic and have been green since earlier waves. The fifth — the live verification suite — asserts a clean working tree outside a hard-coded prefix, which is why it could not run mid-wave and why this plan runs last and alone.

The human action is a re-login. Everything this phase changed is in the git worktree, but the phase removed two safety mechanisms from the install path, and the only honest confirmation that the running session is unaffected is to end it and start a new one.

Purpose: the sweep record is the artifact a future session reads instead of re-deriving the change set from ten plan bodies. It has two sections stubbed since wave 4; this plan fills them.
Output: a complete sweep record with the gate transcript, and a phase whose every assert is green on a clean tree.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-VALIDATION.md
@.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md
</context>

<tasks>

<!-- planner-discipline-allow: SAFE_DEFAULTS -->
<!-- planner-discipline-allow: dual-run -->
<!-- planner-discipline-allow: graphical-session.target -->

<task type="auto">
  <name>Task 1: Fill the sweep record's planning-artifact section</name>
  <read_first>
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md` — as written in plan `16-06`, with its two stubbed sections.
    - `16-07-SUMMARY.md`, `16-08-SUMMARY.md` and `16-09-SUMMARY.md` — the source for the planning-artifact corrections table. Record what those plans did, not what they were planned to do.
    - `.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md` — for the table shape and for its phase-gate section, which is the model for task 2's entry.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md` — D-41, and the deferred-ideas block for the items that stay unowned.
  </read_first>
  <files>.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md</files>
  <action>
Fill the planning-artifact section that plan `16-06` stubbed, using the same four-column table shape as the rest of the record: Line or site, Stale text quoted verbatim in backticks, Correction citing the plan that made it, Severity.

One subsection per file: the requirement set, the milestone audit, the project file, the Phase 11 disposition record, the roadmap, and the state file. For the two files where the phase used the annotate-rather-than-rewrite rule, say so explicitly and record the annotation wording that was used, so a future reader can grep for it.

Record three things the tables alone will not carry:

- **The coverage change.** Twenty-two requirements became nineteen because three rows were deleted with their mappings. Name the three IDs and the reason each was deleted rather than rewritten.
- **The one documented rule conflict and how it was resolved.** The inventory requirement is a rewritten row, which the general rule maps to this phase, but the specific rule keeps it at Phase 10. The specific rule governed, and it is consistent with leaving the Phase 10 assert verifying the frozen record.
- **The frozen-artifact override.** It was exercised at exactly two sites, both inside the Phase 11 disposition record, plus the roadmap milestone prose and coverage line. Name them, and state that everything else under the phase and milestone directories stayed frozen.

Then extend the existing deferred-fixes section with the items this phase leaves open. It should end with exactly the ones that are genuinely unowned: the three roles of the repository copy of the pre-adopt compositor config, and the compositor-target autostart bootstrap. Do not add an owner to either. Add one row noting that the six dated codebase snapshots describe the wrapper as it was and are refreshed by a mapper re-run rather than hand-edited, with the sites listed.

Leave the phase-gate section stubbed; task 2 fills it with a real transcript.

Write `Waybar`, `rofi` and `swaync` literally.
  </action>
  <verify>
    <automated>D=.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md; grep -qF '## Planning-artifact corrections' "$D" && test "$(sed -n '/## Planning-artifact corrections/,/## Phase gate/p' "$D" | grep -c '^### ')" -ge 6</automated>
    <fails_when>non-zero exit — the planning-artifact section is missing, or it has fewer than six per-file subsections while wave 5 and wave 6 touched six planning files</fails_when>
    <automated>D=.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md; for s in 'FULL-03' 'FULL-05' 'ADOPT-04' 'INV-04' '19' 'codebase'; do grep -qF -- "$s" "$D" || { echo "MISSING RECORD: $s"; exit 1; }; done; grep -qE 'WR-02' "$D" && grep -q 'graphical-session.target' "$D"</automated>
    <fails_when>a `MISSING RECORD: <token>` line is printed and the loop exits 1 — the record does not name the three deleted requirement IDs, the resolved mapping conflict, the new coverage number, or the deferred snapshot refresh; or one of the two unowned items is absent from the deferred section</fails_when>
    <automated>D=.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md; ! grep -qiE '(owner|owned by|assigned)[^.|]{0,40}(WR-02|graphical-session)' "$D" && test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' "$D" | grep -vx 'google-chrome-stable' | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit — an owner was assigned to one of the two items that must stay unowned, or a line used the collective noun instead of naming Waybar, rofi and swaync literally</fails_when>
    <automated>git diff --quiet HEAD -- arch/ docs/ scripts/ .config/ stow/ vendor/ .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md .planning/STATE.md .planning/v0.3-MILESTONE-AUDIT.md</automated>
    <fails_when>non-zero exit — this task left an uncommitted change in a file it does not own; every file listed was finalised in an earlier wave and re-touching one would dirty the tree the gate is about to certify</fails_when>
  </verify>
  <acceptance_criteria>
    - The sweep record's planning-artifact section exists and has at least six per-file subsections.
    - The record names `FULL-03`, `FULL-05` and `ADOPT-04` as deleted with a reason each, states the new coverage number, records the inventory-row mapping conflict and its resolution, and records the frozen-artifact override sites.
    - The deferred section lists the three-roles item, the compositor-target autostart item and the codebase-snapshot refresh, and assigns an owner to none of them.
    - The record names the annotation wording used across the requirement set, the project file and the state file.
    - Non-allowlisted `chrome` tokens in the record: 0.
    - `git diff --quiet HEAD` over the code, documentation and previously-finalised planning files exits 0.
  </acceptance_criteria>
  <done>The sweep record is a complete account of what this phase changed, across the wrapper, the scripts, the two operator documents and the six planning artifacts, with the coverage arithmetic, the one resolved rule conflict and the frozen-artifact override all recorded. Only the gate transcript is outstanding.</done>
</task>

<task type="auto">
  <name>Task 2: Run the phase gate on a committed clean tree and record the transcript</name>
  <precondition>The git working tree is clean — every phase artifact, including this plan's own summary-in-progress and the sweep record edit from task 1, is committed. The live verification suite fails by design on any modification outside a hard-coded Phase 14 prefix, and every Phase 16 path is outside it.</precondition>
  <precondition>A Hyprland session is running and `hyprctl` responds, because the live verification suite probes the running compositor.</precondition>
  <read_first>
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-VALIDATION.md` — the full-suite command list and the manual-only verification row, which is the source for the re-login instruction.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md` — D-40, which names the five scripts and the post-change re-login, and states that the known finding stays allowed.
    - `.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md` — its phase-gate section, the model for what a transcript entry records.
    - `docs/dots-hyprland-workflow.md` — the post-login verification block, so the operator instruction matches what the playbook already tells them to run.
  </read_first>
  <files>.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md</files>
  <action>
Commit everything first, then run the gate, then record what it printed.

**Order matters.** Confirm `git status --porcelain` is empty before running the live verification suite. Its final assertion fails on any modification outside a hard-coded Phase 14 prefix, and every path this phase touched is outside that prefix — including untracked files. If the tree is dirty, commit and re-run rather than reasoning about which failure is which.

**Run all five, in this order,** capturing each transcript:
1. the phase-16 retirement assert,
2. the wrapper-behavior smoke,
3. the dispositions assert,
4. the overlay assert with its re-pinned drift baseline and fence-drift check,
5. the live verification suite.

The first four must each end at zero failures. The fifth must end at zero failures with exactly one finding, and that finding must be the known compositor-target loss. That finding stays emitted and stays allowed, exactly as it was in the previous phase; do not silence it and do not resolve it.

**Record the transcript** in the sweep record's phase-gate section, following the shape of the previous phase's record: one numbered entry per script with its summary line quoted verbatim, then the human step. Quote the real output. A transcript that paraphrases is not evidence.

Cross-check one thing while you are there: the playbook's post-login verification block quotes the live suite's summary line as an expectation. Confirm the line you just observed matches the line the playbook quotes. If it does not, record the mismatch as a finding in the sweep record and hand it to a follow-up — **do not edit the playbook here**, because that would dirty the tree the gate just certified.

**The human step.** Everything above runs against a session that started before this phase. The phase removed two mechanisms from the install path and deleted the machinery that used to inject session hooks, and although none of that touches a running session, the honest confirmation is a fresh one. Record the operator's result in the transcript with the date.

**If the gate finds a real defect:** record it in the sweep record, state which plan owns the file, and stop. Do not fix it inline. A fix here re-dirties the tree and invalidates the run that found it.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain)" || { echo "TREE DIRTY:"; git status --porcelain; exit 1; }</automated>
    <fails_when>a `TREE DIRTY:` line is printed followed by a file list and the command exits 1 — the gate cannot be run yet, because the live verification suite's final assertion fails on any modification outside its hard-coded Phase 14 prefix</fails_when>
    <automated>for s in phase16-retire-assert phase12-full-smoke phase11-dispositions-assert; do ./scripts/$s.sh >/tmp/p16-gate-$s.txt 2>&1 || true; grep -q '^=== done: FAIL=0 ===$' /tmp/p16-gate-$s.txt || { echo "NOT GREEN: $s"; tail -5 /tmp/p16-gate-$s.txt; exit 1; }; done; ./scripts/phase13-d19-assert.sh >/tmp/p16-gate-d19.txt 2>&1 || true; test "$(grep -c '^\[FAIL\]' /tmp/p16-gate-d19.txt)" -eq 0</automated>
    <fails_when>a `NOT GREEN: <script>` line is printed with the tail of its output and the loop exits 1, or the overlay assert prints any `[FAIL]` line — one of the four tree-clean-agnostic suites regressed since its own wave</fails_when>
    <automated>./scripts/phase14-verify.sh >/tmp/p16-gate-p14.txt 2>&1 || true; tail -1 /tmp/p16-gate-p14.txt; test "$(grep -c '^\[FAIL\]' /tmp/p16-gate-p14.txt)" -eq 0 && test "$(grep -c '^\[FINDING\]' /tmp/p16-gate-p14.txt)" -eq 1 && grep -q '^\[FINDING\].*D-38' /tmp/p16-gate-p14.txt</automated>
    <fails_when>non-zero exit — the live verification suite printed any `[FAIL]` line on a clean tree, or its finding count is not exactly 1, or the single finding is not the known compositor-target loss; the printed tail names which</fails_when>
    <automated>D=.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md; grep -qF '## Phase gate' "$D" && test "$(sed -n '/## Phase gate/,$p' "$D" | grep -c 'FAIL=0')" -ge 4 && sed -n '/## Phase gate/,$p' "$D" | grep -q 'FINDINGS=1' && sed -n '/## Phase gate/,$p' "$D" | grep -qiE 're-login|relogin|log(ged)? (back )?in'</automated>
    <fails_when>non-zero exit — the phase-gate section is missing, quotes fewer than four zero-failure summary lines, does not quote the live suite's finding count, or does not record the human re-login step</fails_when>
    <automated>L="$(grep -o '=== done: FAIL=[0-9]* FINDINGS=[0-9]* ===' /tmp/p16-gate-p14.txt | tail -1)"; echo "OBSERVED: $L"; grep -qF "$L" docs/dots-hyprland-workflow.md</automated>
    <fails_when>the grep exits non-zero — the summary line the live suite printed is not the line the playbook quotes as its expectation. Record the mismatch in the sweep record and hand it to a follow-up; do not edit the playbook here.</fails_when>
    <human-check>
      Log out of the current Hyprland session completely, or reboot, then log back in and confirm:
      (a) the desktop comes up and the ii shell is running — the bar, the launcher and the notification surface all appear;
      (b) `hyprctl -j status | jq -r .configProvider` prints `lua`;
      (c) `./scripts/phase14-verify.sh` prints `=== done: FAIL=0 FINDINGS=1 ===`;
      (d) nothing that worked before the phase has stopped working, beyond the compositor-target autostart loss that was already recorded as a known, unowned loss before this phase began.
      Record the date and the observed result in the sweep record's phase-gate section. If (d) surfaces something new, record it as a finding and name the plan that owns the file — do not fix it in this plan.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` is empty before the gate runs.
    - The phase-16 retirement assert, the wrapper-behavior smoke and the dispositions assert each end in exactly `=== done: FAIL=0 ===`.
    - The overlay assert prints zero `[FAIL]` lines, including its re-pinned wrapper drift check and its fence-drift check.
    - The live verification suite prints zero `[FAIL]` lines and exactly one `[FINDING]`, and that finding is the known compositor-target loss.
    - The summary line the live suite printed is present verbatim in `docs/dots-hyprland-workflow.md`, or the mismatch is recorded as a finding with an owning plan named.
    - The sweep record's phase-gate section quotes at least four zero-failure summary lines, the live suite's finding count, and the operator's re-login result with its date.
    - No file other than the sweep record is modified by this plan.
  </acceptance_criteria>
  <done>All five assert scripts are green on a committed, clean tree, with the one known finding still emitted and still allowed. The operator has re-logged in and confirmed the session is unchanged apart from the loss that was already known and unowned. The sweep record carries the transcript, and the phase is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate transcript → the claim that the phase is done | The transcript is the evidence. A paraphrased or partial one lets a red suite pass as green. |
| running session → a session that starts after the phase | Everything the phase changed is in the worktree, but the running session predates it. Only a fresh login distinguishes "still works" from "still running". |
| a gate finding → the temptation to fix it inline | Fixing during the gate re-dirties the tree and invalidates the run that found the defect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-63 | Repudiation | the gate transcript | high | mitigate | Summary lines are quoted verbatim and the record is checked for at least four zero-failure lines plus the finding count; a paraphrased transcript fails the task |
| T-16-64 | Repudiation | running the gate on a dirty tree | high | mitigate | An explicit clean-tree assertion runs first and prints the offending file list; the live suite's own final assertion is the backstop |
| T-16-65 | Denial of Service | the session after the phase | high | mitigate | A human re-login is required and its result recorded with a date; the automated suites cannot distinguish a working session from a surviving one |
| T-16-66 | Repudiation | the known finding silenced to make the gate look clean | high | mitigate | The finding count is asserted at exactly 1 and the finding must match the known loss — both zero findings and two findings fail |
| T-16-67 | Tampering | inline fixes during the gate | medium | mitigate | A prohibition plus a scope check: no file other than the sweep record may be modified, and a mismatch is recorded and handed over rather than fixed |
| T-16-68 | Repudiation | the playbook's quoted expectation drifting from reality | medium | mitigate | The observed summary line is grepped for in the playbook; a mismatch is a recorded finding rather than a silent divergence |
| T-16-69 | Information Disclosure | gate transcripts | low | accept | The transcripts contain paths, package names and process facts. No credential is printed by any of the five scripts. |
| T-16-SC | Tampering | package-manager installs | low | accept | This plan adds no `npm` / `pip` / `cargo` / `pacman` install step, and runs no live install or uninstall. `16-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable, with no `[ASSUMED]`, `[SUS]` or `[SLOP]` entries. |
</threat_model>

## Artifacts this phase produces

**Created by this plan:** none — both sections were stubbed by plan `16-06` and are filled here.

**Modified surfaces:**

| Artifact | Kind | Note |
|----------|------|------|
| `## Planning-artifact corrections` in the sweep record | filled | six per-file subsections, the coverage change, the resolved rule conflict, the override sites |
| `## Deferred fixes` in the sweep record | extended | the two unowned items plus the codebase-snapshot refresh |
| `## Phase gate` in the sweep record | filled | five verbatim summary lines and the operator's re-login result |

**Deleted by this plan:** nothing.

**Explicitly NOT touched:** every other file in the repository. This plan runs on a tree that must stay clean apart from the sweep record, and any defect it finds is recorded and handed to a follow-up rather than fixed here.

## Flagged Assumptions

| # | Item | Status | Why it is flagged |
|---|------|--------|-------------------|
| A29 | The re-login confirmation is a `human-check` inside a task's verify rather than a checkpoint task | resolved against configuration | `workflow.human_verify_mode` is `end-of-phase`, which suppresses planner-emitted human-verify checkpoints and harvests them after the phase. The `human-check` form is the one that survives that mode and still attaches the instruction to the task that needs it. |
| A30 | The gate does not re-run the deleted scripts' coverage in any form | resolved by the planner | The two deleted scripts asserted machinery this phase removed. There is nothing left for them to cover, and the gate's five scripts cover the surviving surface. Recorded so the reduced script count is visible as a decision rather than as attrition. |
| A31 | A defect found at the gate is recorded and handed over rather than fixed | resolved by the planner | Fixing inline dirties the tree the gate certifies, so the run that found the defect could not be the run that closes the phase. Handing over keeps the evidence chain intact. |
| A3 | `run_upstream_uninstall_dangerous` and its flag survived the phase unchanged | carried forward, recorded | Considered in plan `16-01` and deliberately kept: it is orthogonal to the retired profile, but it is now the only remaining route to a cascading removal. Restated here so the phase closes with it visible rather than forgotten. |

<verification>
- The gate is the five scripts named in the phase decision plus the human re-login. Four have been green since their own waves; the live verification suite runs here for the first time on a clean tree.
- Clean-tree ordering is the whole reason this plan is last and alone. Commit first, run second, record third.
- The finding count is asserted at exactly 1 in both directions. Zero findings means the known loss was silenced; two means something new appeared.
- The playbook cross-check compares the observed summary line against the quoted expectation. A mismatch is a recorded finding, not an edit — the playbook is owned by plan `16-04` and the tree must stay clean.
- The human step is the only live action in the entire phase. Nothing here installs, uninstalls, or touches a package.
</verification>

<success_criteria>
- All five assert scripts green on a committed, clean tree, with one known finding.
- The operator has re-logged in and confirmed the session works.
- The sweep record is complete and carries the verbatim transcript.
- Nothing outside the sweep record changed.
</success_criteria>

<output>
Create `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-10-SUMMARY.md` when done.
</output>
