---
phase: 16-retire-the-safe-profile-full-only-wrapper-and-playbook
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - scripts/phase14-verify.sh
  - scripts/phase07-live-smoke.sh
  - scripts/phase14-preflight.sh
autonomous: true
requirements: [ADOPT-02, ADOPT-03, IN-11, D-33, D-37]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`./scripts/phase14-verify.sh` runs against the stripped wrapper without invoking a subcommand or a flag that no longer exists (D-37)."
    - "The live-session assertions that still hold are untouched: the compositor is on the Lua entry, and Waybar, rofi and swaync are still stopped (ADOPT-02, ADOPT-03, D-37 keep list)."
    - "The surviving removal path is still exercised — `uninstall --dry-run` exits 0 — under a label that no longer promises a rollback tier that has been deleted (D-37, D-26 ADOPT-04 deletion)."
    - "Nothing in the repo asserts the integrity of a backup no future install will produce, and the two snapshots on disk are left exactly as they are (D-37)."
    - "`scripts/phase07-live-smoke.sh` and `scripts/phase14-preflight.sh` no longer exist, and nothing in the repo invokes them (D-33)."
    - "IN-11 is closed by deletion: no script in the repo prints backup rotation as a mandatory step before an adopt that already happened (D-33)."
  artifacts:
    - path: "scripts/phase14-verify.sh"
      provides: "Live adopt verification suite, aligned to the stripped wrapper"
      contains: "uninstall --dry-run"
  key_links:
    - from: "scripts/phase14-verify.sh"
      to: "arch/dots-hyprland.sh"
      via: "the surviving removal-path probe invokes the wrapper's uninstall dry-run"
      pattern: "uninstall --dry-run"
    - from: "scripts/phase14-verify.sh"
      to: "docs/dots-hyprland-workflow.md"
      via: "the playbook's post-login verification block quotes this script's summary line verbatim"
      pattern: "FINDINGS"
  prohibitions:
    - statement: "The known D-38 finding stays emitted and stays allowed; this plan does not silence it, resolve it, or give it an owner (D-40, CONTEXT.md 'Not in this phase')."
      status: active
      verification: backstop
    - statement: "The Waybar / rofi / swaync accept-remove assertions are not touched — the retirement reinforces them rather than replacing them (D-37 keep list)."
      status: active
      verification: backstop
    - statement: "The clean-working-tree assertion at the end of the script is not loosened, re-scoped to a Phase 16 prefix, or deleted; it is the reason the phase gate runs on a committed tree (research Pitfall 1)."
      status: active
      verification: backstop
    - statement: "`~/ii-original-dots-backup` and `~/ii-original-dots-backup.20260904T171128Z` are not moved, rotated, renamed or removed. Only the checks that read them go."
      status: active
      verification: backstop
    - statement: "No task in this plan mutates the live machine beyond running the existing read-only probes the verification script already performs."
      status: active
      verification: backstop
    - statement: "WR-02 stays open and unowned (CONTEXT.md 'Not in this phase')."
      status: active
      verification: backstop
---

<objective>
Align the two remaining script surfaces with the wrapper that plan `16-01` shipped: repair the live verification suite so it stops probing machinery that no longer exists, and delete the two scripts whose entire subject matter this phase removed.

`scripts/phase14-verify.sh` currently asserts three things that go false in this phase — the presence of a rollback source no future install will produce, the reachability of a deleted subcommand, and the cryptographic integrity of a backup that will not be taken. Two other scripts assert nothing else: `scripts/phase07-live-smoke.sh` exercises the deleted subcommand, the deleted flags, the retired residual argv and the retired session chrome; `scripts/phase14-preflight.sh` gates an adopt that happened on 2026-09-04 and cannot be re-run.

Purpose: closing IN-11 by deletion rather than by fixing its message, which is what D-33 supersedes the earlier plan with. The preflight script's mandatory-rotation instruction would rename away a rollback source if anyone followed it today.
Output: a verification suite that is green against the stripped wrapper, and two fewer scripts.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-PATTERNS.md
</context>

<tasks>

<!-- planner-discipline-allow: PROTECT_EXPLICIT -->
<!-- planner-discipline-allow: ADOPT-04 -->
<!-- planner-discipline-allow: rotate-backup -->
<!-- planner-discipline-allow: --skip-protect -->

<task type="auto">
  <name>Task 1: Strip the deleted-machinery probes from the live verification suite</name>
  <read_first>
    - `scripts/phase14-verify.sh` — read the tier block around lines 340-425 in full (the tempfile declarations and `trap`, the three tier probes, and the backup-integrity block), the finding emitted around line 521, and the clean-working-tree block at the end around lines 535-560. Re-derive every line number with `grep -n` before editing; the deletions shift everything below them.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md` — D-37 in full: what is deleted, what is kept with a relabel, and what is reworded rather than deleted.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md` — Pitfall 1 (the clean-tree assertion and why the gate cannot run mid-wave), Pitfall 6 (why the summary counts must not drift), and the Runtime State Inventory row warning that a variable left declared is safe under `set -u` but one left read-after-deletion is not.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-PATTERNS.md` — the `scripts/phase14-verify.sh (D-37 deletions)` section, which quotes the three probes verbatim so the middle one can be kept byte-identical.
    - `arch/dots-hyprland.sh` — as left by plan `16-01`, so the surviving probe invokes a subcommand that still exists.
  </read_first>
  <files>scripts/phase14-verify.sh</files>
  <action>
Three deletions, one relabel, one reword. Nothing else in the file changes.

**Delete 1 — the third rollback-source probe.** The `if [[ -d "$BACKUP_DIR" ]] && [[ -n "$(ls -A ...)" ]]` block that passes or fails on the presence and non-emptiness of the backup directory. No future install produces that directory, so asserting it is asserting a historical accident.

**Delete 2 — the third-tier reachability probe.** The block that invokes the wrapper's now-deleted package-marking subcommand with a dry-run flag and passes on exit 0. After plan `16-01` that invocation hits the allowlist refusal and exits 1, so the block would fail every run.

**Delete 3 — the backup-integrity block.** The whole section that resolves the backed-up compositor config inside the backup directory, reads a recorded pre-install mtime from the baseline fixture, compares a `sha256sum` against a recorded fixture hash, and compares mtimes. Its three hard failures all rest on a backup that will not be taken again. Delete the section header comment block with it.

**Keep, with a relabel — the second-tier probe.** The `uninstall --dry-run` block stays byte-identical in structure, including its `printf '' |` stdin feed, its `sed -n '1,40p' "$OUT" || true` failure dump and its tempfile. Only its two message strings change: they currently label it as a rollback tier under a requirement ID this phase deletes. Relabel both to describe what the probe actually establishes — that the wrapper's removal path is still reachable and still exits 0 on a dry run — and cite `D-10` instead of the deleted requirement ID.

**Reword — the finding that names a deleted array.** One `finding` call in the known-losses section cites the removed package-marking array as the reason a binary was expected to remain installed. That array will not exist. Reword the finding text to state the same observation without the dead citation: the binary was expected to remain installed because it was installed as part of the personal stack, and the capability that used to protect that stack from an orphan sweep was retired in Phase 16. Keep it a `finding`, not a `fail` — it sits in an `else` branch that does not fire on this machine, and turning it into a `fail` would change the summary line the playbook quotes.

**Variable and trap hygiene.** After the deletions, check every remaining reader of `BACKUP_DIR` with `grep -n 'BACKUP_DIR' scripts/phase14-verify.sh`. It is declared near the top and echoed once in the configuration banner. Keeping a declared-but-lightly-used variable is safe under `set -u`; removing a declaration that still has a reader is not. Decide once, apply consistently, and if the banner echo is the only remaining reader, keep both. Do the same check for the tempfile that fed the deleted third-tier probe: if it now has no writer, remove both its `mktemp` declaration and its entry in the `trap` list **in the same edit**. Also confirm the baseline-fixture accessor used only by the deleted integrity block has no other caller before removing anything related to it.

**Do not touch:** the Waybar / rofi / swaync accept-remove assertions, the compositor Lua-entry assertions, the overlay assertions, the known-loss findings other than the one reworded above, and the clean-working-tree assertion at the end of the file. That last one is what forces the phase gate onto a committed tree and must keep its hard-coded Phase 14 prefix; re-scoping it to a Phase 16 prefix would defeat the guard exactly when it matters.

**Expected summary after this task.** Research verified that all three deleted probes are `pass`/`fail` pairs, not findings, so the findings count is unchanged at 1 — the known D-38 loss. The failure count on a dirty working tree will be 1, from the clean-tree assertion, and 0 on a committed tree. The playbook quotes that summary line and is corrected in plan `16-04`; re-run this script after committing and paste the real tail there rather than carrying a number forward on trust.
  </action>
  <verify>
    <automated>bash -n scripts/phase14-verify.sh && ./scripts/phase14-verify.sh >/tmp/p16-03-v.txt 2>&1; tail -2 /tmp/p16-03-v.txt; test "$(grep '^\[FAIL\]' /tmp/p16-03-v.txt | grep -vc 'working tree is dirty')" -eq 0</automated>
    <fails_when>`bash -n` exits non-zero, or the count of `[FAIL]` lines other than the clean-working-tree one is above 0 — those lines name probes that still reference deleted machinery. A dirty-tree `[FAIL]` is expected mid-wave and is the one line excluded here.</fails_when>
    <automated>test "$(grep -c '^\[FINDING\]' /tmp/p16-03-v.txt)" -eq 1 && grep -q '^\[FINDING\].*D-38' /tmp/p16-03-v.txt</automated>
    <fails_when>the findings count is not exactly 1, or the single finding is not the known D-38 loss — a deleted probe was a finding rather than a pass/fail pair, or the reworded finding changed tier, either of which invalidates the summary line the playbook quotes</fails_when>
    <automated>for s in 'protect --dry-run' 'sha256sum' 'PROTECT_EXPLICIT' 'backup_dir_hyprland_conf_mtime'; do if grep -q -- "$s" scripts/phase14-verify.sh; then echo "SURVIVOR: $s"; grep -n -- "$s" scripts/phase14-verify.sh || true; exit 1; fi; done; grep -q 'uninstall --dry-run' scripts/phase14-verify.sh && grep -q 'PHASE14_PREFIX' scripts/phase14-verify.sh</automated>
    <fails_when>a `SURVIVOR: <token>` line is printed and the loop exits 1 — a deleted probe or its supporting accessor is still present; or the trailing greps exit non-zero because the surviving removal probe or the clean-working-tree guard was deleted along with them</fails_when>
    <automated>grep -c 'waybar\|rofi\|swaync' scripts/phase14-verify.sh; test "$(git diff -U0 HEAD -- scripts/phase14-verify.sh | grep -cE '^[+-].*(waybar|rofi|swaync|configProvider|hyprland\.lua)')" -eq 0</automated>
    <fails_when>the second `test` exits non-zero — this task's diff added or removed a line mentioning Waybar, rofi, swaync, the compositor config-provider probe or the Lua entry, all of which D-37 puts on the keep list</fails_when>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase14-verify.sh` exits 0.
    - A run of the script produces zero `[FAIL]` lines other than the clean-working-tree one, and exactly one `[FINDING]` line, which names D-38.
    - `scripts/phase14-verify.sh` contains none of: `protect --dry-run`, `sha256sum`, `PROTECT_EXPLICIT`, the baseline accessor key for the backed-up config mtime.
    - `scripts/phase14-verify.sh` still contains `uninstall --dry-run` and `PHASE14_PREFIX`.
    - No message in the file cites `ADOPT-04`; the surviving removal probe cites `D-10` instead.
    - `git diff -U0 HEAD -- scripts/phase14-verify.sh` contains no added or removed line matching `waybar`, `rofi`, `swaync`, `configProvider` or `hyprland.lua`.
    - Every `mktemp` handle declared in the file has at least one writer, and every entry in the `trap` list names a declared variable.
  </acceptance_criteria>
  <done>The live verification suite probes only machinery that exists. Its live-session assertions, its known-loss findings and its clean-tree guard are untouched, so the summary line the playbook quotes keeps its shape: zero failures on a committed tree, one finding. The exact tail is re-captured and pasted into the playbook by plan `16-04`.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the live-smoke and preflight scripts, and prove nothing invoked them</name>
  <read_first>
    - `scripts/phase07-live-smoke.sh` — skim before deleting so the summary can record what coverage is being given up: it asserts the deleted subcommand, the deleted package-marking flag, the hook-disable path, the retired residual argv in dry-run output, and the retired session chrome.
    - `scripts/phase14-preflight.sh` — skim likewise. It carries far more than the IN-11 message: a package-marking assertion, the backup-rotation path with its mandatory-remediation wording, and a gate-fed opt-in install check. The adopt it gated ran on 2026-09-04 and nothing re-runs it.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md` — D-33 in full, including the finding that no Makefile, CI workflow or hook invokes anything in `scripts/`, so neither deletion has a runner to update.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md` — §Environment Availability, which records the absence of a Makefile and of any CI workflow directory as a verified fact.
  </read_first>
  <files>scripts/phase07-live-smoke.sh, scripts/phase14-preflight.sh</files>
  <action>
Remove both files with `git rm`, not with a plain `rm`, so the deletion is staged as a rename-free removal and shows up in the commit as a deletion rather than as an untracked absence.

**Before deleting, prove there is no runner.** Grep the whole worktree for each filename stem in two passes, because the two kinds of hit have opposite dispositions.

Pass one is the gate: everything except `.git/`, `.planning/` and `docs/`. This is where a runner would live — a Makefile target, a git hook, a CI job, a shell alias, another script. The expectation from research is zero hits, and a hit here is a hard failure of this task.

Pass two is a report, not a gate: `docs/` only. Both `docs/phase14-adopt-runbook.md` and `docs/dots-hyprland-workflow.md` cite these stems today. Do **not** widen this task to fix them — they are wave-3 files owned by other plans, and editing documentation from a wave-2 script plan breaks the wrapper-before-documentation ordering of D-42. Record the reported paths in the summary and name their owners: the runbook is corrected in plan `16-05` (D-25) and the playbook in plan `16-04` (D-21), whose own link assert is what proves the dangling pointers are gone.

**What deleting the preflight script closes.** IN-11 is the open review item that the script still prints backup rotation as a mandatory step before go. Running it today would rename away a rollback source. D-33 supersedes the earlier plan to fix that message: the fix is deletion. Record in the summary that IN-11 closes here, because two planning artifacts carry it as open and are corrected in later plans — the milestone audit's tech-debt entry (plan `16-07`) and the state file's open-review entry (plan `16-09`).

**Do not delete anything else in `scripts/`.** Five phase scripts survive — the inventory assert, the dispositions assert, the wrapper-behavior smoke, the overlay assert and the live verification suite — alongside the phase-16 retirement assert that plan `16-01` created, for six in the `scripts/phase*.sh` family. Three further scripts sit outside that family and outside this phase entirely: `scripts/clone_repo.sh`, `scripts/nvim-audit-failures.sh` and `scripts/nvim-validate.sh`. They are not counted by the survivor assert and must not be touched — if a count-based check ever appears to demand their removal, the check is wrong, not the tree. The inventory assert in particular is deliberately left untouched even though it keeps requiring retired language in a frozen Phase 10 record: it verifies what that phase captured, not current policy (D-39).
  </action>
  <verify>
    <automated>test ! -e scripts/phase07-live-smoke.sh && test ! -e scripts/phase14-preflight.sh && test -z "$(git ls-files -- scripts/phase07-live-smoke.sh scripts/phase14-preflight.sh)"</automated>
    <fails_when>non-zero exit — either file still exists on disk, or `git ls-files` still prints it because the removal was staged as a working-tree delete without being recorded in the index</fails_when>
    <automated>H="$(grep -rIl --exclude-dir=.git --exclude-dir=.planning --exclude-dir=docs -e 'phase07-live-smoke' -e 'phase14-preflight' . || true)"; D="$(grep -rIl -e 'phase07-live-smoke' -e 'phase14-preflight' docs || true)"; echo "RUNNERS:${H:-none}"; echo "DOC_HANDOVERS:${D:-none}"; test -z "$H"</automated>
    <fails_when>the echoed `RUNNERS:` line names any path and the `test` exits non-zero — a script, hook, Makefile or config outside `docs/` and `.planning/` still invokes a file that no longer exists. The `DOC_HANDOVERS:` line is reporting only and never fails this task: `docs/dots-hyprland-workflow.md` and `docs/phase14-adopt-runbook.md` both cite the deleted stems today, and both are wave-3 files this wave-2 plan is forbidden to edit. Copy that line into the summary and hand it to plans `16-04` and `16-05`, which assert the dangling links are gone.</fails_when>
    <automated>ls scripts/ | sort | tr '\n' ' '; echo; test "$(ls scripts/phase*.sh | wc -l)" -eq 6 && test -x scripts/phase10-inventory-assert.sh && test -x scripts/phase11-dispositions-assert.sh && test -x scripts/phase12-full-smoke.sh && test -x scripts/phase13-d19-assert.sh && test -x scripts/phase14-verify.sh && test -x scripts/phase16-retire-assert.sh && test -e scripts/clone_repo.sh && test -e scripts/nvim-audit-failures.sh && test -e scripts/nvim-validate.sh</automated>
    <fails_when>non-zero exit — the `scripts/phase*.sh` glob matches a count other than six, one of the six named phase-script survivors is missing or has lost its executable bit, or one of the three scripts outside the phase family is gone; any of those means this task deleted more than the two files it owns. The count is deliberately scoped to the phase-script glob rather than to `scripts/*.sh`: `clone_repo.sh`, `nvim-audit-failures.sh` and `nvim-validate.sh` are outside this phase's subject matter, are never touched here, and are asserted present rather than counted</fails_when>
    <automated>./scripts/phase12-full-smoke.sh >/tmp/p16-03-s.txt 2>&1; ./scripts/phase16-retire-assert.sh >/tmp/p16-03-r.txt 2>&1; grep -q '^=== done: FAIL=0 ===$' /tmp/p16-03-s.txt && grep -q '^=== done: FAIL=0 ===$' /tmp/p16-03-r.txt && ./scripts/phase11-dispositions-assert.sh >/tmp/p16-03-d.txt 2>&1; grep -q '^=== done: FAIL=0 ===$' /tmp/p16-03-d.txt</automated>
    <fails_when>any of the three summary lines is not the exact `=== done: FAIL=0 ===` — deleting a script or editing the verification suite disturbed one of the three suites that are already green and must stay green through this wave</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/phase07-live-smoke.sh` and `scripts/phase14-preflight.sh` are absent from the working tree and from `git ls-files`.
    - No file outside `.git/`, `.planning/` and `docs/` contains the string `phase07-live-smoke` or `phase14-preflight` — nothing that could still invoke a deleted script survives. The two `docs/` hits are expected, are printed as a reporting line rather than treated as a failure, and are recorded in the summary as handovers to plans `16-04` and `16-05`, which own those files and assert the dangling links away.
    - The `scripts/phase*.sh` glob matches exactly six files, all executable: the inventory assert, the dispositions assert, the wrapper-behavior smoke, the overlay assert, the live verification suite, and the phase-16 retirement assert. The three scripts outside the phase family — `scripts/clone_repo.sh`, `scripts/nvim-audit-failures.sh`, `scripts/nvim-validate.sh` — are still present and untouched, and are excluded from the count because this phase has no claim on them.
    - `./scripts/phase12-full-smoke.sh`, `./scripts/phase16-retire-assert.sh` and `./scripts/phase11-dispositions-assert.sh` each end in exactly `=== done: FAIL=0 ===`.
    - The summary records IN-11 as closed by deletion, and names the two planning artifacts that still carry it as open plus the plans that correct them.
  </acceptance_criteria>
  <done>The two scripts whose entire subject matter this phase removed are gone from the index and the worktree, with no runner to update because none existed. IN-11 is closed by deletion. The six surviving phase scripts are intact, the three scripts outside the phase family are untouched, and the three tree-clean-agnostic suites are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification suite → operator confidence | This script is the executable source of truth the playbook points at. A probe that fails for a stale reason trains an operator to ignore failures. |
| deleted script → a reader who runs it anyway | The preflight script's mandatory-rotation instruction renames a rollback source. Deleting the file is the only way to make it unrunnable. |
| verification suite → the two on-disk snapshots | The deleted integrity block was the only thing reading them. Removing checks must not become removing data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-14 | Destruction of data | `scripts/phase14-preflight.sh` rotation path | high | mitigate | Deleted outright rather than reworded (D-33); the reference sweep proves nothing can still invoke it, and the acceptance criteria forbid moving or renaming either snapshot directory |
| T-16-15 | Repudiation | live verification suite as evidence | high | mitigate | The three deletions are enumerated and asserted by absence; the keep list is asserted by a diff-level check that fails if any Waybar / rofi / swaync / Lua-entry line is added or removed |
| T-16-16 | Tampering | clean-working-tree assertion | high | mitigate | Acceptance criteria require the guard and its hard-coded Phase 14 prefix to survive; the phase gate depends on it forcing a committed tree (research Pitfall 1) |
| T-16-17 | Denial of Service | `trap` and variable hygiene after deletions | medium | mitigate | Every `mktemp` handle must have a writer and every `trap` entry must name a declared variable; an actual run under `set -u` is what surfaces the difference |
| T-16-18 | Repudiation | the summary line the playbook quotes | medium | mitigate | The findings count is asserted at exactly 1 and the reworded finding must stay a finding, so the playbook's quoted expectation cannot silently drift (research Pitfall 6) |
| T-16-19 | Information Disclosure | verification output | low | accept | The script prints paths, package names and process facts. No credential is read or echoed. |
| T-16-SC | Tampering | package-manager installs | low | accept | This plan adds no `npm` / `pip` / `cargo` / `pacman` install step. `16-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable, with no `[ASSUMED]`, `[SUS]` or `[SLOP]` entries. |
</threat_model>

## Artifacts this phase produces

**Created by this plan:** none.

**Deleted by this plan — intentional removals, not omissions:**

| Artifact | Kind | Note |
|----------|------|------|
| `scripts/phase07-live-smoke.sh` | file deletion | asserted the retired subcommand, flags, residual argv and session chrome (D-33) |
| `scripts/phase14-preflight.sh` | file deletion | gated an adopt that already ran; its mandatory-rotation message is IN-11 (D-33) |
| third rollback-source probe in `scripts/phase14-verify.sh` | assert deletion | no future install produces the directory it checked (D-37) |
| third-tier reachability probe in `scripts/phase14-verify.sh` | assert deletion | invoked a subcommand the allowlist now refuses (D-37) |
| backup-integrity block in `scripts/phase14-verify.sh` | assert-section deletion | hashed and mtime-compared a backup that will not be taken (D-37) |

**Modified surfaces:** the surviving removal probe's two message strings (relabelled off the deleted requirement ID onto `D-10`), and one known-loss finding's text (stripped of its citation to a deleted array).

**Explicitly NOT touched:** the Waybar / rofi / swaync accept-remove assertions, the compositor Lua-entry assertions, the overlay assertions, the D-38 known-loss finding, the clean-working-tree guard with its hard-coded Phase 14 prefix, `scripts/phase10-inventory-assert.sh` (D-39), and both `~/ii-original-dots-backup*` directories on disk.

## Flagged Assumptions

| # | Item | Status | Why it is flagged |
|---|------|--------|-------------------|
| A5 | The backup-directory variable in `scripts/phase14-verify.sh` may become effectively unused after the deletions | resolved by inspection, not by assumption | Research warns that removing a declaration that still has a reader is a `set -u` abort while leaving an unused one is harmless. The action requires a `grep -n` before deciding, and defaults to keeping the declaration when the configuration banner still echoes it. |
| A9 | `docs/` references to the two deleted scripts are handed to later plans rather than fixed here | resolved by the planner | The reference sweep is expected to hit `docs/phase14-adopt-runbook.md` (owned by `16-05`, D-25) and possibly `docs/dots-hyprland-workflow.md` (owned by `16-04`, D-21). Fixing them here would put a documentation edit in a wave-2 script plan and break the wrapper-before-documentation ordering rationale of D-42. |
| A10 | IN-11 closes by deletion in this plan, but two planning artifacts still record it as open until wave 5 | recorded, deliberate | The milestone audit's tech-debt entry is corrected in plan `16-07` and the state file's open-review entry in plan `16-09`. Between wave 2 and wave 5 the repo says IN-11 is open while its cause is already gone; the summary records the window so a mid-phase reader is not misled. |

<verification>
- `scripts/phase12-full-smoke.sh`, `scripts/phase16-retire-assert.sh` and `scripts/phase11-dispositions-assert.sh` are regression floors for this plan and must all end at `FAIL=0`.
- `scripts/phase13-d19-assert.sh` is still **expected-red** until plan `16-06` re-pins its baseline. Do not run it as a gate here.
- `scripts/phase14-verify.sh` is run in this plan, but its clean-working-tree assertion is expected to fail mid-wave. That single failure is excluded by name in the verify command and by nothing else; any other failure is a real one.
- The findings count is pinned at exactly 1 because the playbook quotes it. Research verified that none of the three deleted probes was a finding, so a count other than 1 means the reworded finding changed tier or a new one was introduced.
- The keep list is enforced by a diff-level check rather than by a presence check, because a presence check cannot tell an untouched assertion from a rewritten one that happens to still mention the same word.
</verification>

<success_criteria>
- The live verification suite is green apart from the expected dirty-tree failure, with exactly one finding.
- No script in the repo probes the deleted subcommand, the deleted flags or the retired backup.
- Two scripts are gone from the index and nothing invokes them.
- IN-11 is closed by deletion and the closure is recorded with its downstream owners.
</success_criteria>

<output>
Create `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-03-SUMMARY.md` when done.
</output>
