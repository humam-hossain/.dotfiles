---
phase: 16-retire-the-safe-profile-full-only-wrapper-and-playbook
plan: 08
type: execute
wave: 5
depends_on: ["16-06"]
files_modified:
  - .planning/PROJECT.md
  - .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md
autonomous: true
requirements: [W-1, W-2, D-03, D-29, D-32, D-39]

estimate:
  tokens: 47000
  raw_tokens: 47000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every current-state claim in `.planning/PROJECT.md` describes the wrapper and the session as they are after this phase (D-29)."
    - "Every per-phase delivered line in `.planning/PROJECT.md` keeps its original text and gains a superseded annotation, because those lines are true statements about what those phases shipped (D-29)."
    - "One superseded-annotation wording is used across the requirement set, the project file and the state file (Claude's Discretion)."
    - "The two stale rows in the Phase 11 dispositions record are corrected in place: the migrate rows and the archive path (W-1, W-2, D-32)."
    - "`scripts/phase11-dispositions-assert.sh` is still green after the in-place rewrite, because the rewrite preserves every literal token that assert requires (D-32)."
    - "The override of the frozen-artifact rule is exercised at the two named sites only; everything else under the phase and milestone directories stays frozen (D-03)."
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Project status prose whose current-state claims are true and whose history is annotated rather than rewritten"
      contains: "superseded"
    - path: ".planning/phases/11-disposition-decisions/11-DISPOSITIONS.md"
      provides: "The Phase 11 disposition record with its two stale rows corrected in place"
      contains: "stow/"
  key_links:
    - from: ".planning/phases/11-disposition-decisions/11-DISPOSITIONS.md"
      to: "scripts/phase11-dispositions-assert.sh"
      via: "the assert greps this record for required literal tokens and a required language pattern"
      pattern: "migrate-to-hypr-custom"
    - from: ".planning/PROJECT.md"
      to: "docs/dots-hyprland-workflow.md"
      via: "the product-surface prose points at the canonical playbook"
      pattern: "dots-hyprland-workflow\\.md"
  prohibitions:
    - statement: "Per-phase delivered lines are annotated, never rewritten. Rewriting them would make the file assert that a past phase shipped something it did not (D-29)."
      status: active
      verification: backstop
    - statement: "The frozen-artifact override applies to the two named sites in the Phase 11 record only. No other file under `.planning/phases/**` or `.planning/milestones/**` is edited by this plan (D-03)."
      status: active
      verification: backstop
    - statement: "The Phase 11 rewrite must preserve the three residual flag tokens, the migrate enum token, the compositor config filename, the ii tree reference, and the sentence carrying the residual-default language pattern. Removing any of them fails the assert that gates the file (D-32)."
      status: active
      verification: backstop
    - statement: "`scripts/phase11-dispositions-assert.sh` is not edited to accommodate the rewrite. The rewrite is constrained by the assert, not the other way round (D-32)."
      status: active
      verification: backstop
    - statement: "The v0.2 milestone history and the pre-milestone research notes in `.planning/PROJECT.md` are left as history; they are the same class of record as the phase directories."
      status: active
      verification: backstop
    - statement: "The archived session-chrome trees under `stow/` are not moved, renamed or deleted. This plan corrects a claim about where they are, not their location."
      status: active
      verification: backstop
    - statement: "WR-02 and the D-38 `graphical-session.target` bootstrap stay open and unowned (CONTEXT.md 'Not in this phase')."
      status: active
      verification: backstop
    - statement: "`arch/dots-hyprland.sh` is not modified. The drift baseline pinned in plan `16-06` compares against the working tree."
      status: active
      verification: backstop
---

<objective>
Correct the project's status prose, and close the two audit warnings that live inside a frozen phase record.

`.planning/PROJECT.md` mixes two kinds of statement in one file: claims about what is true now, and lines recording what each phase delivered. This phase falsifies both, but only the first kind should be rewritten. A delivered line saying that Phase 12 shipped an opt-in profile flag is a true statement about Phase 12; rewriting it would make the file assert a falsehood about the past. So current-state claims are rewritten and delivered lines are annotated (D-29).

The two warnings are the reason the frozen-artifact rule is overridden at named sites in this phase (D-03). One is a set of disposition rows that still say a must-keep will migrate into an overlay tree, when it already has. The other is an archive path that names a directory set which does not exist — the trees are somewhere else, and the audit found that the false path compounded the missing restore route it also recorded.

Purpose: the Phase 11 record is gated by an assert that requires specific literal tokens to be present. The rewrite is constrained by that assert, which makes this the one planning-artifact edit in the phase with an executable check on it.
Output: a project file whose present tense is true and whose past tense is intact, and a corrected disposition record that still passes its gate.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md
@.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md
</context>

<tasks>

<!-- planner-discipline-allow: SAFE_DEFAULTS -->
<!-- planner-discipline-allow: dual-run -->
<!-- planner-discipline-allow: --skip-hyprland -->
<!-- planner-discipline-allow: --core -->
<!-- planner-discipline-allow: --skip-sysupdate -->
<!-- planner-discipline-allow: --full -->
<!-- planner-discipline-allow: migrate-to-hypr-custom -->
<!-- planner-discipline-allow: phase14-preflight.sh -->

<task type="auto">
  <name>Task 1: Sweep the project file — rewrite the present, annotate the past</name>
  <read_first>
    - `.planning/PROJECT.md` — read in full. Distinguish three classes as you read: current-state claims, per-phase delivered lines, and v0.2 or pre-milestone history.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md` — D-29's site list and, more importantly, its rule: current-state claims are rewritten, delivered lines are marked superseded. Also the Claude's-Discretion item asking for one consistent annotation wording.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md` — the planning-artifact sweep section for this file, which lists additional sites D-29 does not name, each already classified as rewrite, supersede or leave-as-history.
    - `16-07-SUMMARY.md` — for the superseded-annotation wording chosen in plan `16-07`. Reuse it verbatim; one vocabulary across the three files is the point.
    - `.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md` — its flagged-not-edited table, the model for the annotation form.
  </read_first>
  <files>.planning/PROJECT.md</files>
  <action>
Sweep the file with scoped edits. Never rewrite it whole — a full-file write over a status document risks losing sections outside the edit window.

**Class 1 — current-state claims. Rewrite these.**
- The position line naming the next phase. It already named a phase that shipped, so it was stale before this phase started. Point it at the current position.
- The milestone goal statement describing an opt-in path out of the residual defaults with the backup gate preserved. Neither half survives. Restate the goal as it actually resolved: the wrapper's only install path is full, and the backup mechanism was retired.
- The not-this-milestone line naming a session-chrome cutover. That cutover was accepted in Phase 11 and executed in the Phase 14 adopt; it is no longer a future exclusion.
- The product-surface lines describing the wrapper, the session and the rollback material. Bring each to post-phase truth, and make the rollback line say what plan `16-05` made the runbook say: recovery is a clean reinstall from the pinned submodule.
- The direction statement about moving toward full session ownership. That move is complete.
- Any other line whose verb is present tense and whose object is a mechanism this phase removed.

**Class 2 — per-phase delivered lines and key-decision rows. Annotate, do not rewrite.**
These state what a phase shipped. They were true when written and they are still true about that phase. Append the superseded annotation from plan `16-07`, and nothing else — do not reword the claim, do not soften it, do not add a second sentence explaining it. The annotation carries the correction by pointing at this phase.

This applies to the Phase 10, 11 and 12 delivered lines that name the residual behavior, the four full-install delivered lines, the key-decision rows citing the opt-in flag, the backup gate and the package-marking capability, and the line attributing enforcement to the deleted preflight script.

**Class 3 — v0.2 milestone history and pre-milestone notes. Leave entirely alone.**
Same class as the phase directories. They record what was true when written and this phase does not own them.

**One annotation wording.** Use exactly the string plan `16-07` chose. If that plan's summary does not record one, choose one now, use it here, and record it in this plan's summary so plan `16-09` can reuse it for the state file. The consistency is the requirement; the exact words are discretionary.

**Write `Waybar`, `rofi` and `swaync` literally** in every line you touch. The file carries three collective-noun occurrences in historical prose the previous phase deliberately left; do not add a fourth and do not sweep those three — they sit in Class 3 and Class 2 lines.
  </action>
  <verify>
    <automated>P=.planning/PROJECT.md; test "$(grep -ci 'supersede' "$P")" -ge 8 && ! grep -qiE 'safe full-install path|backup gate preserved|opt-in out of SAFE_DEFAULTS|next is Phase 15' "$P"</automated>
    <fails_when>non-zero exit — fewer than eight superseded annotations were applied (the delivered lines and key-decision rows number more than that), or a current-state claim this phase falsifies survives verbatim</fails_when>
    <automated>P=.planning/PROJECT.md; test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' "$P" | grep -vx 'google-chrome-stable' | wc -l)" -eq 3</automated>
    <fails_when>the count is above 3 because a rewritten line used the collective noun instead of naming Waybar, rofi and swaync literally, or below 3 because one of the three historical occurrences the previous phase deliberately froze was edited away</fails_when>
    <automated>P=.planning/PROJECT.md; grep -qiE 'reinstall|re-install' "$P" && grep -q 'vendor/dots-hyprland' "$P" && ! grep -qiE 'three-tier|tier 1|tier 2|tier 3' "$P" && grep -q 'dots-hyprland-workflow.md' "$P"</automated>
    <fails_when>non-zero exit — the rollback product-surface line does not describe recovery as a reinstall from the pinned submodule, still names a numbered tier, or the pointer to the canonical playbook was lost</fails_when>
    <automated>P=.planning/PROJECT.md; A="$(grep -oiE 'superseded[^.|)]{0,60}' "$P" | sed 's/[[:space:]]*$//' | sort -u | wc -l)"; echo "annotation variants=$A"; test "$A" -le 2</automated>
    <fails_when>the echoed `annotation variants=` value is above 2 — more than one or two spellings of the superseded annotation are in use, which defeats the single-vocabulary requirement and makes the annotation ungreppable</fails_when>
    <automated>git diff --quiet HEAD -- arch/ docs/ scripts/ .config/ stow/ vendor/ && test -z "$(git diff --name-only HEAD -- .planning/phases/ .planning/milestones/ | grep -v '16-disposition\|11-disposition')"</automated>
    <fails_when>non-zero exit — this task touched a code or documentation directory it does not own, or it modified a frozen artifact under the phase or milestone directories, which the override permits only at the two named sites owned by task 2</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains at least eight superseded annotations, using at most two distinct spellings of the annotation.
    - The file contains none of the retired current-state claims: the opt-in full-install goal statement, the backup-gate-preserved clause, the stale next-phase pointer, or a session-chrome cutover listed as a future exclusion.
    - The rollback product-surface line describes recovery as a reinstall from `vendor/dots-hyprland` and names no numbered tier.
    - The file still points at `docs/dots-hyprland-workflow.md`.
    - Every per-phase delivered line and key-decision row that this phase falsifies retains its original claim text with the annotation appended; none is reworded.
    - Non-allowlisted `chrome` tokens in the file: exactly 3, the pre-existing historical occurrences.
    - `git diff --quiet HEAD -- arch/ docs/ scripts/ .config/ stow/ vendor/` exits 0, and no file under `.planning/phases/` or `.planning/milestones/` other than the Phase 11 and Phase 16 directories is modified.
  </acceptance_criteria>
  <done>The project file's present tense describes the project as it is: one install path, no backup, no package protection, ii owning the session. Its past tense is intact, each falsified delivered line carrying the same annotation pointing at this phase. The state file gets the same treatment in plan `16-09` using the same wording.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the two stale rows in the Phase 11 disposition record</name>
  <read_first>
    - `.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` — read the disposition matrix around the migrate rows and the archive-policy statement in full. Locate both with `grep -n 'migrate-to-hypr-custom'` and `grep -n 'Archive policy'` rather than by quoted line number.
    - `scripts/phase11-dispositions-assert.sh` — read the whole assertion body before editing anything. It requires: three literal residual flag tokens; the migrate enum token, checked twice in two different assertions; the compositor config filename; a reference to the ii tree or its Lua entry; a language pattern matching residual-or-default wording within a hundred characters of a residual term, in either order; and a drop-family pattern within two hundred characters of a residual flag token. Every one of these constrains the rewrite.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md` — D-32 in full, including its explicit list of tokens the rewrite must preserve, and D-03's statement that this is one of exactly two sites where the frozen-artifact rule is overridden.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md` — Pitfall 7, which enumerates the assert's requirements and names the two failure messages that appear when the rewrite breaks one; and the sweep section giving the exact content and falseness of both rows.
    - `.planning/phases/14-live-full-adopt-verify/` summaries — for what actually happened to the must-keeps, so the corrected rows state the outcome rather than a new intention.
  </read_first>
  <files>.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md</files>
  <action>
Two corrections in place, both constrained by an executable gate. **Run `./scripts/phase11-dispositions-assert.sh` immediately after each edit, before moving on** — this is the one planning-artifact edit in the phase that a script can catch, and catching it late means unpicking two changes instead of one.

**Correction one — the migrate rows.** Three disposition rows record must-keeps as destined to migrate into the overlay tree, stated as future intent. They migrated: the overlay files were authored in Phase 13 and applied live in the Phase 14 adopt, where they were verified byte-identical to the repository source of truth. Rewrite the rows so they record the outcome rather than the intention, citing the phase that completed it.

**You must keep the migrate enum token literally present.** The assert checks it twice — once in an enumeration loop over the disposition vocabulary and once as a named axis assertion. Rewriting the rows to say "migrated" without the enum token anywhere in the file fails both. Keep the token as the disposition name and change the surrounding prose.

Leave the fourth row in that group, the one recording an accept-upstream disposition for the primary entry point, exactly as it is. It is not part of this warning.

**Correction two — the archive path.** The archive-policy statement names a directory set under the user configuration path as where the session-chrome trees remain in the repository. That path does not exist. The trees are under the stow tree. Correct the path. Keep the rest of the statement — that the trees remain in the repository as archive, that preflight captures the live state, and that success for the Phases 11 through 14 does not delete them from the repository — because all of that is still true.

State the corrected path explicitly enough that a reader can find the trees, and note in the summary that the milestone audit's matching evidence line was corrected at its own site by plan `16-07`.

**What the rewrite must not disturb.** Beyond the enum token: the three literal residual flag tokens must all still appear somewhere in the file; the compositor config filename must still appear; the ii tree or Lua entry reference must still appear; the sentence carrying the residual-or-default language pattern must stay intact and must stay within the pattern's proximity window; and the drop-family wording near a residual flag token must survive. If a rewrite would remove the last occurrence of any of these, restructure the sentence instead of dropping the token.

**Do not edit the assert.** The rewrite is constrained by the gate, not the reverse. If a correction genuinely cannot be made without breaking the assert, stop and record the conflict rather than loosening the check.

**Do not edit any other frozen artifact.** The override is scoped to this file and to the two corrections named here (D-03).
  </action>
  <verify>
    <automated>./scripts/phase11-dispositions-assert.sh >/tmp/p16-08-d.txt 2>&1; tail -3 /tmp/p16-08-d.txt; grep -q '^=== done: FAIL=0 ===$' /tmp/p16-08-d.txt</automated>
    <fails_when>the summary line is not exactly `=== done: FAIL=0 ===` — each `[FAIL]` line above it names the required token or language pattern the rewrite removed; the two most likely are the missing migrate enum token and the missing drop-family residual wording</fails_when>
    <automated>D=.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md; for t in '--core' '--skip-hyprland' '--skip-sysupdate' 'migrate-to-hypr-custom' 'hyprland.conf'; do grep -qF -- "$t" "$D" || { echo "LOST TOKEN: $t"; exit 1; }; done; grep -qE 'hyprland/|hyprland\.lua' "$D"</automated>
    <fails_when>a `LOST TOKEN: <token>` line is printed and the loop exits 1, or the trailing grep finds no ii-tree reference — the rewrite removed the last occurrence of a literal the gating assert requires</fails_when>
    <automated>D=.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md; grep -q 'stow/' "$D" && ! grep -qF '.config/{waybar,rofi,swaync}' "$D" && test -d stow/waybar/.config/waybar && test -d stow/rofi/.config/rofi && test -d stow/swaync/.config/swaync</automated>
    <fails_when>non-zero exit — the archive-policy statement still names the directory set that does not exist, does not name the location that does, or one of the three archived trees is missing from the repository, which would mean this task moved what it was only supposed to describe</fails_when>
    <automated>D=.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md; grep -qiE '(migrated|applied|completed)[^.]{0,120}(Phase 13|Phase 14)' "$D" && ! grep -qiE 'will migrate|to be migrated|planned migration' "$D"</automated>
    <fails_when>non-zero exit — the migrate rows still read as future intent, or they were rewritten as outcome without citing the phase that completed it, leaving the record unsourced</fails_when>
    <automated>test -z "$(git diff --name-only HEAD -- scripts/)" && test -z "$(git diff --name-only HEAD -- .planning/phases/ | grep -v '11-disposition-decisions\|16-retire-the-safe-profile')" && git diff --quiet HEAD -- arch/ docs/ .config/ stow/ vendor/ && ./scripts/phase10-inventory-assert.sh >/tmp/p16-08-i.txt 2>&1; grep -q '^=== done: FAIL=0 ===$' /tmp/p16-08-i.txt</automated>
    <fails_when>non-zero exit — the gating assert was edited to accommodate the rewrite, a frozen artifact outside the two permitted directories was modified, a code or documentation directory was touched, or the Phase 10 inventory assert regressed</fails_when>
  </verify>
  <acceptance_criteria>
    - `./scripts/phase11-dispositions-assert.sh` ends in exactly `=== done: FAIL=0 ===`.
    - `.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` still contains the literals `--core`, `--skip-hyprland`, `--skip-sysupdate`, `migrate-to-hypr-custom`, `hyprland.conf`, and a reference matching `hyprland/` or `hyprland.lua`.
    - The archive-policy statement names `stow/` and no longer names `.config/{waybar,rofi,swaync}`; all three archived trees still exist in the repository.
    - The migrate rows read as completed outcomes citing Phase 13 or Phase 14, and contain no future-intent phrasing.
    - The accept-upstream row for the primary entry point is byte-identical to its previous state.
    - `git diff --name-only HEAD -- scripts/` is empty — the gating assert was not edited.
    - No file under `.planning/phases/` other than the Phase 11 and Phase 16 directories is modified, and no file under `arch/`, `docs/`, `.config/`, `stow/` or `vendor/` is modified.
    - `./scripts/phase10-inventory-assert.sh` still ends in exactly `=== done: FAIL=0 ===`.
  </acceptance_criteria>
  <done>The two audit warnings that lived inside the frozen Phase 11 record are closed at their source: the must-keep rows record what happened rather than what was intended, and the archive path names the directory the trees are actually in. The gating assert is still green and was not touched, which is the evidence that the rewrite preserved every literal it requires.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| status prose → a reader deciding what to do next | The project file is the first thing read at the start of a session. A present-tense claim about a removed mechanism sends work in the wrong direction. |
| frozen phase record → the project's decision audit trail | Editing a frozen record destroys the evidence of what was decided when. The override exists for exactly two false statements and no more. |
| disposition record → its gating assert | The assert requires literal tokens to be present. A rewrite that reads better and drops one turns a green gate red for a reason unrelated to correctness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-49 | Repudiation | per-phase delivered lines | high | mitigate | Annotate-don't-rewrite is enforced by acceptance criterion; the annotation count floor and the variant ceiling together catch both under-application and inconsistent wording |
| T-16-50 | Repudiation | frozen artifacts outside the two named sites | high | mitigate | A `git diff --name-only` scope check over the phase and milestone directories fails the task if any file other than the Phase 11 and Phase 16 directories is modified (D-03) |
| T-16-51 | Denial of Service | the disposition record's gating assert | high | mitigate | The assert is run after each edit, its required literals are checked individually with a named failure, and editing the assert itself is forbidden by a scope check on `scripts/` |
| T-16-52 | Destruction of data | the archived session-chrome trees | high | mitigate | The task corrects a claim about where the trees are; acceptance criteria assert all three directories still exist, so a "correction" that moved them fails |
| T-16-53 | Tampering | scope creep into code or documentation | medium | mitigate | `git diff --quiet HEAD` over the code and documentation directories, plus a re-run of the Phase 10 assert |
| T-16-54 | Repudiation | inconsistent superseded vocabulary across three files | medium | mitigate | The annotation-variant count is capped at two and the chosen wording is recorded in the summary for plan `16-09` to reuse |
| T-16-55 | Information Disclosure | planning artifacts | low | accept | These files record decisions, paths and package names. No credential appears in either. |
| T-16-SC | Tampering | package-manager installs | low | accept | This plan adds no `npm` / `pip` / `cargo` / `pacman` install step. `16-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable, with no `[ASSUMED]`, `[SUS]` or `[SLOP]` entries. |
</threat_model>

## Artifacts this phase produces

**Created by this plan:** none.

**Modified surfaces:**

| Artifact | Kind | Note |
|----------|------|------|
| current-state claims in `.planning/PROJECT.md` | rewritten | position, milestone goal, not-this-milestone line, product surface, direction statement (D-29) |
| delivered lines and key-decision rows in `.planning/PROJECT.md` | annotated | original text preserved, one annotation wording shared with plans `16-07` and `16-09` (D-29) |
| the three migrate rows in the Phase 11 record | rewritten in place | intent becomes outcome, enum token preserved (W-1, D-32) |
| the archive-policy statement in the Phase 11 record | corrected in place | path corrected to the location the trees are in (W-2, D-32) |

**Deleted by this plan:** nothing. Both files are corrected in place; no row, section or record is removed.

**Explicitly NOT touched:** `scripts/phase11-dispositions-assert.sh` and every other script, the accept-upstream disposition row, the v0.2 milestone history and pre-milestone notes in the project file, the three frozen collective-noun occurrences the previous phase left, every frozen artifact outside the two named sites, and the archived trees themselves.

## Flagged Assumptions

| # | Item | Status | Why it is flagged |
|---|------|--------|-------------------|
| A23 | The superseded annotation wording is chosen once and reused across three files by three different plans | resolved by the planner | The Claude's-Discretion item asks for consistency without specifying words. Plan `16-07` chooses, this plan reuses, plan `16-09` reuses. The variant ceiling in the verify is what makes drift detectable rather than merely discouraged. |
| A24 | The migrate rows are rewritten as completed outcomes rather than annotated as superseded | resolved against D-32 | Elsewhere in this phase, frozen records are annotated rather than rewritten. D-32 explicitly says these two are rewritten **in place**, because they are not statements about what Phase 11 shipped — they are forward-looking commitments that later phases discharged. |
| A25 | The archive-path correction is made here and its downstream copy in the milestone audit is corrected by plan `16-07` | recorded, deliberate | The same false path appears in two files. Fixing it in one and citing the other keeps each plan's file ownership clean; the two plans run in the same wave on different files. |

<verification>
- `scripts/phase11-dispositions-assert.sh` is the executable gate for task 2 and must be run after **each** edit, not only at the end. It is also the one assert in this phase that can fail for a reason unrelated to correctness — a rewrite that reads better but drops a required literal.
- `scripts/phase10-inventory-assert.sh` is re-run as a scope check. It gates a different frozen record which this plan does not own.
- The scope fence for both tasks is `git diff --name-only HEAD` over `scripts/`, over the phase and milestone directories, and `git diff --quiet HEAD` over the code and documentation directories.
- The collective-noun count for the project file is pinned at exactly 3 rather than at zero or at a ceiling: above 3 means a rewritten line introduced one, below 3 means a frozen historical line was edited.
- The annotation-variant count is a ceiling of 2 rather than 1 because a Markdown table cell and a prose sentence may legitimately trim the phrase differently; more than two spellings is drift.
</verification>

<success_criteria>
- The project file's present tense is true and its past tense is intact.
- One superseded annotation wording is in use across the requirement set and the project file.
- The two audit warnings are closed at their source.
- The gating assert for the disposition record is green and was not edited.
</success_criteria>

<output>
Create `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-08-SUMMARY.md` when done.
</output>