---
phase: 16-retire-the-safe-profile-full-only-wrapper-and-playbook
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - arch/dots-hyprland.sh
  - scripts/phase12-full-smoke.sh
autonomous: true
requirements: [FULL-01, FULL-02, FULL-04, D-05, D-11, D-13, D-34]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`./arch/dots-hyprland.sh help` documents only the flag surface that still exists, and names no removed flag or removed subcommand (D-11, D-13)."
    - "The help text still names the opt-in flag, now described as an accepted no-op, so an operator pasting an old transcript learns why it does nothing (D-05)."
    - "The help text still points at `docs/dots-hyprland-workflow.md`, which is the pointer `scripts/phase12-full-smoke.sh` asserts and the playbook reciprocates (Phase 12 D-13)."
    - "`./scripts/phase12-full-smoke.sh` exits 0 against the rewritten wrapper and asserts the new contract rather than the retired one (D-34)."
    - "The wrapper's final state for this phase is reached in this plan; nothing after wave 2 modifies `arch/dots-hyprland.sh`, which is what makes the wave-4 re-pin stable (D-38, D-43)."
  artifacts:
    - path: "arch/dots-hyprland.sh"
      provides: "Usage heredoc describing only the surviving surface"
      contains: "dots-hyprland-workflow"
    - path: "scripts/phase12-full-smoke.sh"
      provides: "Wrapper-behavior suite rewritten to the full-only contract"
      contains: "=== done: FAIL="
  key_links:
    - from: "scripts/phase12-full-smoke.sh"
      to: "arch/dots-hyprland.sh"
      via: "help capture and dry-run argv captures greped for the surviving surface"
      pattern: "HELP_OUT"
    - from: "arch/dots-hyprland.sh"
      to: "docs/dots-hyprland-workflow.md"
      via: "usage heredoc playbook pointer, asserted by the smoke suite"
      pattern: "dots-hyprland-workflow"
  prohibitions:
    - statement: "`scripts/phase12-full-smoke.sh` is rewritten, not retired — one live suite per behavior, and its preamble, `pass`/`fail` triple and summary footer stay byte-identical to the repo's shared assert skeleton (D-34)."
      status: active
      verification: backstop
    - statement: "No new refusal is invented to replace a deleted one; the two refusal asserts whose mechanisms this phase removed are deleted rather than re-pointed at something else."
      status: active
      verification: backstop
    - statement: "The documentation forbidden-string grep does not belong in this suite; it lives in `scripts/phase16-retire-assert.sh` and is scoped to `docs/dots-hyprland-workflow.md` only (D-36)."
      status: active
      verification: backstop
    - statement: "No task in this plan mutates the live machine — help, dry-run and syntax checks only."
      status: active
      verification: backstop
    - statement: "WR-02 and the D-38 `graphical-session.target` bootstrap stay open and unowned (CONTEXT.md 'Not in this phase')."
      status: active
      verification: backstop
---

<objective>
Bring the wrapper's self-description and its behavior suite into agreement with the contract plan `16-01` just installed, and leave `arch/dots-hyprland.sh` in its final state for this phase.

Two files, one idea: the wrapper says what it does, and one executable suite proves it. `scripts/phase12-full-smoke.sh` is deliberately red from the moment `16-01` landed — it asserts that a bare install still injects the residual flags, which is exactly the claim this phase falsifies. It is repaired here rather than retired, because it is the repo's contract of record for wrapper behavior (D-34).

Purpose: after this plan nothing touches `arch/dots-hyprland.sh` again. The drift baseline that plan `16-06` pins compares a pinned commit against the working tree, so the wrapper must be final before the pin (D-38, D-43).
Output: a usage heredoc describing only the surviving surface, and a green wrapper-behavior suite.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-PATTERNS.md
</context>

<tasks>

<!-- planner-discipline-allow: SAFE_DEFAULTS -->
<!-- planner-discipline-allow: --allow-skip-backup -->
<!-- planner-discipline-allow: --skip-protect -->
<!-- planner-discipline-allow: --keep-hypr-hooks -->
<!-- planner-discipline-allow: --skip-hyprland -->
<!-- planner-discipline-allow: --skip-sysupdate -->
<!-- planner-discipline-allow: --core -->
<!-- planner-discipline-allow: protect-list -->
<!-- planner-discipline-allow: Backup gate -->

<task type="auto">
  <name>Task 1: Rewrite the usage heredoc to the surviving surface</name>
  <read_first>
    - `arch/dots-hyprland.sh` — read `usage()` in full (it is a `cat <<'EOF'` block roughly 109 lines long and about half of it documents machinery plan `16-01` deleted), plus the final state of `run_install_family`, `run_uninstall` and `main` so the documented surface matches the parsed surface exactly.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md` — Pitfall 9, which enumerates the deletion surface inside the heredoc and names the two help assertions that must keep passing; and Pitfall 3, which is the reason the help text must not describe the install as unattended.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-PATTERNS.md` — the `[LABEL]` prefixed-stdout convention section; every wrapper echo carries one of `[CONFIG]`, `[INSTALL]`, `[FAIL]`, `[UNINSTALL]`, `[DONE]`, and all `[FAIL]` lines go to stderr.
    - `scripts/phase12-full-smoke.sh` — the two help assertions near the top of the file, which constrain what the rewritten heredoc must still contain.
    - `vendor/dots-hyprland/sdata/lib/functions.sh` — the upstream `pause()` function, around lines 64-70, and how `ask` is set. This is the ground truth behind the "still interactive" wording the help text must carry.
  </read_first>
  <files>arch/dots-hyprland.sh</files>
  <action>
Replace `usage()` in one deliberate rewrite rather than by incremental line deletion. Roughly half the current heredoc documents machinery that no longer exists, and picking it apart line by line is how a stale fragment survives.

**What the rewritten heredoc must document, and nothing else:**

- The five allowlisted subcommands as they now stand in the `ALLOWLIST` array — `install`, `install-deps`, `install-setups`, `install-files`, `uninstall` — plus `help` / `-h` / `--help`. One line each, describing what each actually does after this phase. The uninstall line describes it as the wrapper-owned removal: it gates with its own exact-token confirmation, removes the `illogical-impulse-*` packages without a cascading dependency sweep, and removes ii-owned configs and state. Do not attach the retired dual-run qualifier to it (D-13).
- The two wrapper-owned meta flags: `--dry-run`, which prints the argv the wrapper would exec and exits without calling upstream; and the opt-in profile flag, documented as **accepted but ignored** because full is now the only install behavior, and stated as never forwarded to upstream. Keep the literal flag token in the text — `scripts/phase12-full-smoke.sh` asserts its presence and D-05 keeps it alive for old transcripts.
- The four surviving uninstall flags: `--dry-run`, `--packages-only`, `--configs-only`, `--keep-venv`. Plus the dangerous upstream-removal escape hatch and its type-token guard, described exactly as it is today — that path is unchanged by this phase.
- A short "What this wrapper does" paragraph stating the surviving guarantees: it validates the subcommand against an allowlist, preflights the submodule and the upstream executable, builds argv as an array and execs it (never `eval`), changes into the vendored tree, and forwards the upstream backup-suppression flag on the two file-touching install subcommands.
- **One explicit interactivity note.** State plainly that the wrapper no longer prompts on install, but that upstream `./setup` still runs its own greeting and at least one `Enter to proceed` pause unless force-run. Do **not** describe the new default as unattended, silent, or non-interactive. Deleting the wrapper's gate removed the *wrapper's* prompt only; upstream's `pause()` still fires because `ask` is set false only by its own force flag, which the wrapper never injects (research Pitfall 3).
- Between five and eight worked examples, all of them **bare commands with no profile flag**, covering: dry-run then real `install`; `install-files` for a pin-bump re-apply; `install-deps` when only packages changed; `uninstall --dry-run`; and `uninstall --packages-only`.
- The playbook pointer line naming `docs/dots-hyprland-workflow.md`. Keep it — the smoke suite asserts the literal `dots-hyprland-workflow`, and the playbook points back.

**What must not appear anywhere in the heredoc:** the retired residual flag tokens, the removed backup-gate section and its flag, the removed package-marking flag and subcommand, the removed hook-preservation flag, and the phrase describing a default profile. Comments elsewhere in the file follow the same rule — cite decision IDs, not removed identifiers.

Do not touch any other function. This task changes `usage()` and nothing else, so that the only remaining wrapper diff for the whole phase is a self-description change.
  </action>
  <verify>
    <automated>bash -n arch/dots-hyprland.sh && ./arch/dots-hyprland.sh help >/tmp/p16-02-help.txt 2>&1 && grep -q -- '--full' /tmp/p16-02-help.txt && grep -q 'dots-hyprland-workflow' /tmp/p16-02-help.txt && grep -q -- '--dry-run' /tmp/p16-02-help.txt && grep -q -- '--keep-venv' /tmp/p16-02-help.txt</automated>
    <fails_when>non-zero exit — `bash -n` failed, `help` exited non-zero, or the help capture is missing any of the four literals it must still carry: the opt-in flag token, the playbook filename stem, `--dry-run`, `--keep-venv`</fails_when>
    <automated>for s in 'SAFE_DEFAULTS' 'allow-skip-backup' 'skip-protect' 'keep-hypr-hooks' 'skip-hyprland' 'skip-sysupdate' 'Backup gate' 'protect'; do if grep -qi -- "$s" /tmp/p16-02-help.txt; then echo "STALE HELP: $s"; grep -in -- "$s" /tmp/p16-02-help.txt || true; exit 1; fi; done; grep -E -- '(^|[[:space:]])--core([[:space:]]|$)' /tmp/p16-02-help.txt && exit 1 || true</automated>
    <fails_when>a `STALE HELP: <token>` line is printed with its line number and the loop exits 1, or the trailing ERE matches a standalone residual flag — the help text still documents a flag, section or subcommand this phase removed</fails_when>
    <automated>grep -qiE 'upstream[^.]{0,80}(greeting|pause|Enter to proceed)' /tmp/p16-02-help.txt && ! grep -qiE 'unattended|non-interactive|no prompts|silent install' /tmp/p16-02-help.txt</automated>
    <fails_when>non-zero exit — the help text either omits the note that upstream still prompts, or claims the install is unattended / non-interactive / prompt-free, which research Pitfall 3 disproves against `vendor/dots-hyprland/sdata/lib/functions.sh`</fails_when>
    <automated>./scripts/phase16-retire-assert.sh >/tmp/p16-02-r.txt 2>&1; tail -2 /tmp/p16-02-r.txt; grep -q '^=== done: FAIL=0 ===$' /tmp/p16-02-r.txt</automated>
    <fails_when>the assert summary line is anything but the exact `=== done: FAIL=0 ===` — rewriting the heredoc regressed one of the argv or ignored-note asserts, most likely by disturbing a line the retirement contract greps</fails_when>
  </verify>
  <acceptance_criteria>
    - `bash -n arch/dots-hyprland.sh` exits 0 and `./arch/dots-hyprland.sh help` exits 0.
    - The help capture contains the literals `--full`, `--dry-run`, `--keep-venv`, `--packages-only`, `--configs-only`, `dots-hyprland-workflow`.
    - The help capture contains, case-insensitively, none of: `SAFE_DEFAULTS`, `allow-skip-backup`, `skip-protect`, `keep-hypr-hooks`, `skip-hyprland`, `skip-sysupdate`, `Backup gate`, `protect`; and does not match the standalone-residual ERE `(^|[[:space:]])--core([[:space:]]|$)`.
    - The help capture contains a sentence tying `upstream` to a greeting or an `Enter to proceed` pause, and contains none of `unattended`, `non-interactive`, `no prompts`, `silent install`.
    - The help capture lists exactly the five allowlisted subcommands plus `help`, and every `install` / `install-files` example in it is a bare command with no profile flag.
    - `./scripts/phase16-retire-assert.sh` still ends in exactly `=== done: FAIL=0 ===`.
    - `git diff --name-only HEAD~1 HEAD -- arch/` names only `arch/dots-hyprland.sh`.
  </acceptance_criteria>
  <done>`./arch/dots-hyprland.sh help` describes exactly the surface that exists: five subcommands, two meta flags, four uninstall flags plus the guarded escape hatch, a truthful interactivity note, bare examples, and the playbook pointer. `arch/dots-hyprland.sh` is now final for this phase — no later plan modifies it, which is the precondition for the wave-4 drift re-pin.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the wrapper-behavior smoke suite to the full-only contract</name>
  <read_first>
    - `scripts/phase12-full-smoke.sh` — read in full. Its preamble (shebang through the `pass`/`fail` triple and the tempfile/trap block) and its summary footer survive unchanged; only the assertion bodies change.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-PATTERNS.md` — the `scripts/phase12-full-smoke.sh (rewritten body — D-34)` section, which is an eight-row table mapping each current assertion block to its new meaning, and the "Non-zero-exit assert (refusal shape)" note recording which block is deliberately **not** carried over.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md` — D-34's list of assertion line ranges to replace, Open Question 3 (the job split between this suite and the phase-16 assert), and Pitfall 10 (the refusal that flips to acceptance).
    - `arch/dots-hyprland.sh` — as left by task 1, so the asserted strings are the strings the program actually prints.
    - `scripts/phase16-retire-assert.sh` — so the two suites do not duplicate each other's job.
  </read_first>
  <files>scripts/phase12-full-smoke.sh</files>
  <action>
Rewrite the assertion bodies in place. Keep the file's identity: same path, same shebang, same `set -euo pipefail` and `REPO_ROOT`/`cd` preamble, same `FAIL` / `pass()` / `fail()` triple, same `mktemp` + `trap` idiom with its `# shellcheck disable=SC2064` comment, same `echo "=== done: FAIL=${FAIL} ==="` footer followed by the `if [[ "$FAIL" -gt 0 ]]; then exit 1; fi` and `exit 0`. Every assert stays an `if`/`else` with both branches present and every failure branch dumps context with `sed -n '1,40p' "$OUT" || true`. Do not add a findings tier.

**Update the header comment block first.** It currently announces itself as the Phase 12 full-profile smoke. Restate it as the wrapper-behavior suite for the full-only contract: what it covers (syntax, help surface, argv, exit codes), the `Usage (from REPO_ROOT):` line, the exit contract sentence, and a `Constraints:` list saying non-mutating, never runs a live install. Add one line naming the job split (Open Question 3): **this suite covers wrapper behavior; `scripts/phase16-retire-assert.sh` covers the retirement contract and the documentation ban-grep.** A later reader needs to know which file to extend.

**Per-block treatment, following the mapping table in `16-PATTERNS.md`:**

1. **Syntax gate** — unchanged, stays first.
2. **Help lists the opt-in flag** — keep the assert, restate its message so it reads as "help documents the flag as an accepted no-op alias" rather than as an opt-in profile (D-05).
3. **Help has no vendor-outside note** — keep as-is.
4. **Help points at the playbook** — keep as-is.
5. **The opt-in-flag dry-run argv block** — keep all four omission greps verbatim, including the word-boundary ERE for the standalone residual and the two-stage `grep 'would exec' | grep --` for the meta flag. Restate the messages: these omissions are now true of every invocation, not only of the opt-in one. Keep the block invoking with the flag, because that is the path where a regression would first show.
6. **The bare-install residual block** — **invert**. It currently requires all three residual tokens to be present in the bare `install --dry-run` capture. It must now require all three to be absent, with a failure branch dumping the capture. Use the same word-boundary ERE for the standalone one.
7. **The bare `install-files` residual block** — **invert** the same way.
8. **The bare-skip-backup refusal block** — **delete**. The refusal it exercises was removed with the backup gate; there is nothing left to refuse. Do not replace it with a different refusal.
9. **The dual-key allow block** — **delete**. Its second key no longer exists.
10. **The protect-and-hooks dry-run plan block** — **invert to a ban**: the dry-run capture must contain neither the package-marking plan string nor any mention of enabling session hooks. Keep the two-branch shape; on failure dump the capture.
11. **The scope-refusal on `install-deps`** — **invert**: `install-deps` with the opt-in flag now exits 0 and prints the ignored note, per recorded assumption A2. Assert the zero exit and the note.
12. **Add one new block**: the retired subcommand is refused by the allowlist. Invoke it, assert a non-zero exit, and assert the refusal message names it as non-allowlisted. This is the only place in the suite that proves the array edit rather than the function deletion (D-07).

Rename the per-assert message prefixes so they read against the current requirement IDs rather than the deleted ones: use `FULL-01`, `FULL-02` and `FULL-04` where the behavior still maps to a surviving requirement, and a plain `D-05` / `D-07` / `D-09` prefix where it maps to a phase decision instead. Do not leave a message citing a requirement ID this phase deletes.

Drop any `mktemp` handle that no longer has a consumer after the two deletions, and drop it from the `trap` list in the same edit. An unused variable is harmless; a `trap` naming an unset variable under `set -u` is not.
  </action>
  <verify>
    <automated>bash -n scripts/phase12-full-smoke.sh && ./scripts/phase12-full-smoke.sh >/tmp/p16-02-smoke.txt 2>&1; tail -3 /tmp/p16-02-smoke.txt; grep -q '^=== done: FAIL=0 ===$' /tmp/p16-02-smoke.txt</automated>
    <fails_when>`bash -n` exits non-zero, or the final summary line is anything but the exact `=== done: FAIL=0 ===` — each `[FAIL]` line above it names the assertion that still encodes the retired contract</fails_when>
    <automated>test "$(grep -c '^\[PASS\]' /tmp/p16-02-smoke.txt)" -ge 12 && grep -q 'non-allowlisted' /tmp/p16-02-smoke.txt && ! grep -qE 'FULL-03|FULL-05' /tmp/p16-02-smoke.txt</automated>
    <fails_when>non-zero exit — fewer than 12 `[PASS]` lines (blocks were deleted rather than inverted, so the suite is thinner than the contract it guards), no assert exercising the allowlist refusal ran, or a message still cites one of the two requirement IDs this phase deletes</fails_when>
    <automated>grep -q '=== done: FAIL=${FAIL} ===' scripts/phase12-full-smoke.sh && grep -q "pass() { printf '\[PASS\] %s\\\\n' \"\$1\"; }" scripts/phase12-full-smoke.sh && grep -q 'shellcheck disable=SC2064' scripts/phase12-full-smoke.sh && grep -q 'phase16-retire-assert' scripts/phase12-full-smoke.sh</automated>
    <fails_when>non-zero exit — the shared assert skeleton was replaced rather than preserved (the footer, the `pass()` one-liner or the repo-convention shellcheck comment is gone), or the header comment does not name the sibling suite that owns the retirement contract and the documentation ban-grep</fails_when>
    <automated>./scripts/phase16-retire-assert.sh >/tmp/p16-02-r2.txt 2>&1; grep -q '^=== done: FAIL=0 ===$' /tmp/p16-02-r2.txt && bash -n arch/dots-hyprland.sh</automated>
    <fails_when>non-zero exit — the retirement assert or the wrapper syntax check regressed while the smoke suite was being rewritten, which would mean this task edited more than `scripts/phase12-full-smoke.sh`</fails_when>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/phase12-full-smoke.sh` exits 0 and the script's final line is exactly `=== done: FAIL=0 ===`.
    - The run prints at least 12 `[PASS]` lines and zero `[FAIL]` lines.
    - The run includes an assert whose output contains `non-allowlisted`, proving the retired subcommand is refused by the allowlist.
    - No assert message in the script or its output cites `FULL-03` or `FULL-05`.
    - `scripts/phase12-full-smoke.sh` still contains the literal footer `=== done: FAIL=${FAIL} ===`, the byte-identical `pass()` one-liner, and the `shellcheck disable=SC2064` comment.
    - The header comment block contains `Usage (from REPO_ROOT):`, the exit-contract sentence, a `Constraints` list, and the literal `phase16-retire-assert`.
    - The script contains no assert requiring a residual flag to be present, and no assert requiring a non-zero exit for a bare upstream backup-suppression flag or for the opt-in flag on `install-deps`.
    - `./scripts/phase16-retire-assert.sh` still exits 0 and `bash -n arch/dots-hyprland.sh` still exits 0.
  </acceptance_criteria>
  <done>The wrapper-behavior suite is green against the full-only wrapper and asserts the contract that exists rather than the one that was retired. Two suites now cover the wrapper with distinct jobs, stated in both headers. `scripts/phase13-d19-assert.sh` remains expected-red until plan `16-06`; `scripts/phase14-verify.sh` is edited in plan `16-03` and is not run here.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| help text → operator's shell | Every example in the usage heredoc is pasted verbatim into a root-capable shell. A stale example runs a flag the program no longer parses, or worse, one it forwards upstream. |
| assert suite → phase gate | Four scripts are the D-40 gate. A suite that asserts a retired contract blocks the phase; a suite silently loosened to pass stops being evidence. |
| wrapper stdout → assert greps | The suite and the playbook both grep literal output strings. Rewriting the heredoc is the most likely way to break a grep with no compiler to catch it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-09 | Tampering | usage heredoc examples | high | mitigate | Every documented example is a bare command; the stale-token loop fails the task if any removed flag, section heading or subcommand survives in the help capture, comments included |
| T-16-10 | Denial of Service | operator expectation of an unattended install | medium | mitigate | The help text must tie `upstream` to a greeting or pause, and must not contain `unattended` / `non-interactive` / `no prompts` / `silent install`; both directions are asserted (research Pitfall 3) |
| T-16-11 | Repudiation | `scripts/phase12-full-smoke.sh` as evidence | high | mitigate | The suite is inverted, not weakened: a `[PASS]` floor of 12 plus a ban on the two deleted requirement IDs makes "delete the assert instead of fixing it" fail the task |
| T-16-12 | Denial of Service | `trap` list after removing captures | medium | mitigate | Unused `mktemp` handles are dropped from the declaration and the `trap` in the same edit; `bash -n` plus an actual run catches an unset-variable `trap` under `set -u` |
| T-16-13 | Tampering | scope creep into `arch/dots-hyprland.sh` during task 2 | medium | mitigate | Task 2 re-runs both the retirement assert and `bash -n` on the wrapper; the wrapper must be byte-final from task 1 onward for the wave-4 re-pin to hold |
| T-16-SC | Tampering | package-manager installs | low | accept | No `npm` / `pip` / `cargo` / `pacman` install step exists in this plan. `16-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable, with no `[ASSUMED]`, `[SUS]` or `[SLOP]` entries. |
</threat_model>

## Artifacts this phase produces

**Created by this plan:** none. Both files already exist.

**Modified surfaces:**

| Artifact | Kind | Note |
|----------|------|------|
| `usage()` heredoc in `arch/dots-hyprland.sh` | rewritten | final wrapper state for the phase; the wave-4 re-pin baselines against the commit that lands it |
| Header comment of `scripts/phase12-full-smoke.sh` | rewritten | now states the job split against `scripts/phase16-retire-assert.sh` (Open Question 3) |
| Allowlist-refusal assert in `scripts/phase12-full-smoke.sh` | new assert block | the only assert proving the `ALLOWLIST` array edit rather than the function deletion (D-07) |

**Deleted by this plan — intentional removals, not omissions:** the usage sections documenting the residual-flag defaults, the backup gate, the package-marking subcommand and its flag, and the hook-preservation flag; eight of the fifteen usage examples; the smoke suite's bare-backup-suppression refusal block, its dual-key allow block, and the two `mktemp` handles those blocks consumed.

## Flagged Assumptions

| # | Item | Status | Why it is flagged |
|---|------|--------|-------------------|
| A2 | The opt-in flag is now accepted on `install-deps`, so the suite asserts exit 0 where it previously asserted exit 1 | resolved in plan `16-01` | Carried here because this is the file where the flip becomes visible as a changed assertion. `16-RESEARCH.md` Pitfall 10 anticipates it; D-34 lists that assertion among the ones to replace. |
| A7 | `usage()` is rewritten in wave 2 rather than in the same commit as the surgery | divergence, deliberate | `16-RESEARCH.md` puts the whole wrapper edit in wave 1. Splitting keeps every plan at three tasks or fewer under `granularity: fine`, and the ordering constraint that matters (wrapper final before documentation, D-42) is unaffected because the documentation plans are wave 3. |
| A8 | The suite keeps its `phase12` filename despite now guarding a Phase 16 contract | resolved against D-34 | D-34 says rewrite rather than retire — one live suite per behavior. Renaming would orphan every existing reference to it in `.planning/` and in `16-VALIDATION.md`'s command lists. The header comment carries the correction instead. |

<verification>
- `scripts/phase12-full-smoke.sh` becomes green in this plan and is a regression floor for every plan after it.
- `scripts/phase16-retire-assert.sh` must stay at `FAIL=0` throughout; both tasks re-run it.
- `scripts/phase13-d19-assert.sh` is still **expected-red** — it stays red until plan `16-06` re-pins its baseline. Do not run it as a gate here and do not loosen it.
- `scripts/phase14-verify.sh` is not run here: it asserts a clean working tree outside a hard-coded Phase 14 prefix and cannot pass mid-wave (research Pitfall 1).
- The `[PASS]` floor of 12 is deliberate. The rewrite deletes two blocks and adds one, so a suite that comes back materially thinner than its predecessor has lost coverage rather than inverted it.
</verification>

<success_criteria>
- The wrapper describes only what it does, including the truth that upstream still prompts.
- `./scripts/phase12-full-smoke.sh` exits 0 and asserts the full-only contract.
- `arch/dots-hyprland.sh` is final for the phase.
</success_criteria>

<output>
Create `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-02-SUMMARY.md` when done.
</output>