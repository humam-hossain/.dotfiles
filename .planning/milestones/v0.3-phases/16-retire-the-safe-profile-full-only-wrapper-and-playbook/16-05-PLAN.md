---
phase: 16-retire-the-safe-profile-full-only-wrapper-and-playbook
plan: 05
type: execute
wave: 3
depends_on: ["16-03"]
files_modified:
  - docs/phase14-adopt-runbook.md
autonomous: true
requirements: [ADOPT-02, IN-11, D-20, D-24, D-25]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The runbook's preflight section reads as an account of what was run on 2026-09-04, not as a step a reader can run today, because the script it invoked no longer exists (D-25, D-33)."
    - "The runbook's rollback section describes the recovery that is actually available — a clean reinstall from the pinned submodule — rather than a tier list whose machinery this phase deleted (D-20)."
    - "The runbook keeps its structure, its seventeen sections and its role as the adopt-window narrative; correcting falsehoods is not restructuring (D-24, Phase 15 D-02)."
    - "No relative link or invocation in the runbook points at a file that no longer exists."
    - "The runbook still records what the adopt actually ran, including the flags in use at the time — it is a historical record and is deliberately not grep-gated for the retired vocabulary (D-36)."
  artifacts:
    - path: "docs/phase14-adopt-runbook.md"
      provides: "The 2026-09-04 adopt-window narrative, corrected where it went false"
      contains: "## 14."
  key_links:
    - from: "docs/phase14-adopt-runbook.md"
      to: "docs/dots-hyprland-workflow.md"
      via: "the See-also block defers to the playbook as the canonical operator document"
      pattern: "dots-hyprland-workflow\\.md"
    - from: "docs/phase14-adopt-runbook.md"
      to: "vendor/dots-hyprland"
      via: "the replacement recovery narrative names the pinned submodule as the reinstall source"
      pattern: "vendor/dots-hyprland"
  prohibitions:
    - statement: "The runbook is swept, not restructured. Its section count, its numbering and its role as the adopt-window record are preserved (D-24, Phase 15 D-02)."
      status: active
      verification: backstop
    - statement: "The runbook is not grep-gated for the retired vocabulary by any assert. Banning those tokens here would force rewriting a true account of what the adopt ran (D-36)."
      status: active
      verification: backstop
    - statement: "The literal upstream removal-subcommand token is not reintroduced. The existing prohibition wording deliberately names the capability without writing the command, and that evasion is preserved."
      status: active
      verification: backstop
    - statement: "`~/ii-original-dots-backup` and `~/ii-original-dots-backup.20260904T171128Z` are not moved, rotated, renamed or removed by any instruction added or kept in this file."
      status: active
      verification: backstop
    - statement: "The recorded adopt facts — the install timestamp window, the rotated-backup timestamp, the overlay hashes, the observed session state — are not rewritten. They are true statements about 2026-09-04."
      status: active
      verification: backstop
    - statement: "WR-02 and the D-38 `graphical-session.target` bootstrap stay open and unowned; the runbook keeps recording the latter in its known-losses section and gains no owner for either (CONTEXT.md 'Not in this phase')."
      status: active
      verification: backstop
---

<objective>
Correct the two places where the adopt runbook went false in this phase, and nowhere else.

The runbook is the narrative of a window that closed on 2026-09-04. Phase 15 D-02 fixed its role — the playbook is canonical, the runbook is the historical record — and D-24 holds that role here. Two of its sections stop being true when this phase lands: the preflight section invokes a script that no longer exists, and the rollback section promises tiers whose machinery has been deleted.

Everything else in the file stays, including the retired flag names in the account of what the adopt actually ran. That is why D-36 deliberately excludes this file from the documentation ban-grep: banning the tokens here would force rewriting a true record.

Purpose: an operator reaching for recovery must not be handed a procedure that depends on a deleted subcommand or a snapshot no future install produces.
Output: a runbook whose every runnable instruction is runnable and whose recovery section describes what is actually available.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-PATTERNS.md
@.planning/phases/15-playbook-safe-vs-full/15-CONTEXT.md
</context>

<tasks>

<!-- planner-discipline-allow: rotate-backup -->
<!-- planner-discipline-allow: phase14-preflight.sh -->
<!-- planner-discipline-allow: tier 1 -->
<!-- planner-discipline-allow: tier 2 -->
<!-- planner-discipline-allow: tier 3 -->
<!-- planner-discipline-allow: ./setup uninstall -->

<task type="auto">
  <name>Task 1: Turn the preflight section into historical narrative</name>
  <read_first>
    - `docs/phase14-adopt-runbook.md` — read the go/no-go gate section and the backup-rotation section in full, plus every other place the preflight script is invoked. Research counted six invocation sites across those two sections; find them all with `grep -n 'phase14-preflight'` rather than trusting a line list.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md` — D-24 and D-25, plus D-33's rationale for deleting the script and closing the review item by deletion.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md` — the note that the preflight script lost far more than the review item: a package-marking assertion, the backup-rotation path, and a gate-fed install check.
    - `.planning/phases/15-playbook-safe-vs-full/15-CONTEXT.md` — D-02, the split-roles decision that governs how far this sweep may go.
    - `.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md` — the previous sweep of this same file, so the same correction is not applied twice and the same voice is used.
  </read_first>
  <files>docs/phase14-adopt-runbook.md</files>
  <action>
Convert every runnable invocation of the deleted preflight script into narrative, and correct the backup-rotation section that depended on it. Do not renumber, do not merge, do not split sections. The file keeps its seventeen top-level sections.

**The go/no-go gate section.** It currently instructs a reader to run the preflight script and read its verdict. Rewrite the instruction in the past tense as a record: the gate was run on 2026-09-04 before the install, it produced a go verdict, and the script that produced it was removed in Phase 16 because the adopt it gated has happened and nothing re-runs it. Keep whatever recorded output or verdict text the section already carries — that is evidence and it stays. What changes is the framing around it: a reader must not be able to mistake a deleted command for a step.

Convert the command fences themselves rather than deleting them. A fence that shows what was run is part of the record; a fence that invites a reader to run it is a broken instruction. Prefix each with a short line stating it is a record of the 2026-09-04 run, and make sure no fence in this section is presented as a step to perform now.

**The backup-rotation section.** Same treatment, plus one substantive correction. The section describes rotating an existing snapshot aside so upstream would take a fresh one. That whole mechanism is gone: upstream is now told to skip the backup on every install, so no future install produces one to rotate. Rewrite the section as the record of what was done in the window — the rotation happened at the recorded timestamp, producing the timestamped directory that still exists — and add one plain sentence stating that the mechanism is retired and that no reader should rotate anything now.

Do not delete the recorded timestamp, the recorded directory names, or the recorded sequence. Those are the facts the window produced.

**The review item this closes.** The deleted script printed backup rotation as a mandatory step before go. That message is the open review item, and it closes by the deletion in plan `16-03`. Record in this task's summary that the runbook's own rotation prose was the mitigation for it while the script existed, and that both are now retired.

**Sweep for other references.** Run `grep -n 'phase14-preflight\|phase07-live-smoke' docs/phase14-adopt-runbook.md` after the edits and confirm every remaining occurrence is inside narrative rather than inside an instruction, and that no relative Markdown link points at either deleted file. A link to a deleted path is a broken link even inside a historical record.

**What must not change.** The recorded install timestamp window, the recorded overlay hashes, the recorded observed session state, the flags the adopt actually used, and every other statement about what happened. This file is allowed to name retired flags — it is describing 2026-09-04, not prescribing today.
  </action>
  <verify>
    <automated>test "$(grep -c '^## ' docs/phase14-adopt-runbook.md)" -eq 17 && grep -qF '## 14.' docs/phase14-adopt-runbook.md && grep -qF '## 5.' docs/phase14-adopt-runbook.md</automated>
    <fails_when>non-zero exit — the top-level section count is not 17, or the rollback or rotation section heading is gone, meaning the sweep restructured the file instead of correcting it (D-24)</fails_when>
    <automated>base="$(dirname docs/phase14-adopt-runbook.md)"; grep -o '](\.\{0,2\}/\?[^)#][^)]*)' docs/phase14-adopt-runbook.md | sed 's/](\(.*\))/\1/' | grep -v '^http' | while read -r l; do [ -e "$base/$l" ] || [ -e "$l" ] || { echo "BROKEN -> $l"; exit 1; }; done</automated>
    <fails_when>a `BROKEN -> <path>` line is printed and the loop exits non-zero — a relative Markdown link resolves to a path that no longer exists, most likely one of the two scripts deleted in plan `16-03`</fails_when>
    <automated>grep -n 'phase14-preflight' docs/phase14-adopt-runbook.md | head -20; test "$(grep -cE '^\s*(\./)?scripts/phase14-preflight\.sh' docs/phase14-adopt-runbook.md)" -eq 0 && grep -qi 'removed in Phase 16' docs/phase14-adopt-runbook.md && grep -q '2026-09-04' docs/phase14-adopt-runbook.md</automated>
    <fails_when>non-zero exit — a bare invocation of the deleted preflight script still begins a line (that is a step, not a record), or the file lacks both the statement that the script was removed in Phase 16 and the date anchoring the narrative to the adopt window</fails_when>
    <automated>grep -qiE 'rotate' docs/phase14-adopt-runbook.md && grep -qiE 'retired|no longer|does not (apply|run)' docs/phase14-adopt-runbook.md && grep -q '20260904T171128Z' docs/phase14-adopt-runbook.md</automated>
    <fails_when>non-zero exit — the rotation narrative was deleted outright instead of being marked retired, or the recorded timestamped directory name was removed, which would erase a fact the adopt window actually produced</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/phase14-adopt-runbook.md` still has exactly 17 top-level `## ` headings, including the rotation section and the rollback section.
    - No line in the file begins with a bare invocation of the deleted preflight script.
    - The file contains a statement that the script was removed in Phase 16, and still contains the date `2026-09-04` and the recorded rotated-directory timestamp `20260904T171128Z`.
    - The rotation section contains both the historical account and an explicit statement that the mechanism is retired.
    - Every relative Markdown link in the file resolves to an existing path.
    - No instruction in the file tells a reader to move, rename or remove either snapshot directory.
  </acceptance_criteria>
  <done>Every reference to the deleted preflight script reads as an account of what was run on 2026-09-04 rather than as a step, and the backup-rotation section records what happened while stating plainly that the mechanism is retired. The rollback section is still stale and is replaced in task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the rollback tier list with the recovery that actually exists</name>
  <read_first>
    - `docs/phase14-adopt-runbook.md` — read the rollback section in full, including its prohibition paragraph and each tier with its sources and escalation warnings.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md` — D-20 (the replacement text and where it lands), D-24 (sweep, do not restructure), and D-26's treatment of the rollback requirement row, which is deleted rather than rewritten because no rollback guarantee is being made.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md` — the second-order note in Pitfall 8: the existing prohibition paragraph deliberately names the upstream removal capability without writing the literal command, and that evasion must be preserved.
    - `docs/dots-hyprland-workflow.md` — as left by plan `16-04`, so the runbook's recovery text agrees with the playbook's and neither points at the other for tiers that no longer exist.
    - `arch/dots-hyprland.sh` — as finalised in plan `16-02`, so the replacement text describes the removal path that actually survives.
  </read_first>
  <files>docs/phase14-adopt-runbook.md</files>
  <action>
Replace the tier list in the rollback section. Keep the section, its number and its heading; replace its body.

**Why the tiers go.** Two of the three rest on machinery this phase deleted. The first tier restores a pre-adopt configuration from sources that included a snapshot no future install will produce. The third tier invoked the package-marking subcommand, which no longer exists. Only the second — the wrapper's own removal path — survives, and on its own it is not a rollback, it is a removal.

**The replacement text.** Three plain statements, in the runbook's existing voice:

1. Recovery from a bad install is a clean reinstall from the pinned submodule. Name `vendor/dots-hyprland` and the fact that the pin is fixed in the parent repository, so a reinstall is reproducible rather than a fetch of whatever upstream looks like today.
2. The wrapper never calls upstream's own removal subcommand. Preserve the existing prohibition wording, which names that capability without writing the literal command — that evasion is deliberate and reintroducing the literal would trip a future ban-grep. State that the wrapper's own removal path exists, gates with its own exact-token confirmation, removes the ii packages without a cascading dependency sweep, and removes ii-owned configs and state.
3. Nothing is preserved on install. There is no wrapper prompt, no wrapper-made snapshot, and no undo. State that a pre-adopt snapshot from 2026-09-04 still exists on disk, that it is untouched, and that it is no longer presented as a documented recovery tier. Do not instruct anyone to use it and do not instruct anyone to delete it.

Then one sentence pointing at the playbook as the canonical operator document for install and update, per the split roles.

**What to remove with the tiers.** The escalation warnings between tiers, the per-tier source lists, and any sentence framing one tier as a fallback for another. Also remove any instruction that would destroy a remaining recovery source as a side effect — the previous phase corrected one such instruction and the tiers were its context.

**What must not change.** The prohibition paragraph's substance, the section number and heading, the surrounding sections, and every recorded fact about the window. Do not add a route back to the retired session model anywhere in this file; the audit item closes by retiring the destination.

**Cross-check the playbook.** Plan `16-04` removed the playbook's pointers to these tiers and replaced them with the same clean-reinstall statement. Read that paragraph and make the two agree in substance without copying it verbatim — the two documents have different voices and different roles.
  </action>
  <verify>
    <automated>test "$(grep -c '^## ' docs/phase14-adopt-runbook.md)" -eq 17 && grep -qF '## 14.' docs/phase14-adopt-runbook.md && grep -q 'vendor/dots-hyprland' docs/phase14-adopt-runbook.md && grep -q 'dots-hyprland-workflow.md' docs/phase14-adopt-runbook.md</automated>
    <fails_when>non-zero exit — the section count changed, the rollback section heading is gone, or the replacement text does not name the pinned submodule as the reinstall source and the playbook as the canonical document</fails_when>
    <automated>S="$(awk '/^## 14\./,/^## 15\./' docs/phase14-adopt-runbook.md)"; printf '%s' "$S" | grep -ciE 'tier'; printf '%s' "$S" | grep -qiE 'tier [123]|three-tier|tier-1|tier-2|tier-3' && exit 1; printf '%s' "$S" | grep -qF './setup uninstall' && exit 1; printf '%s' "$S" | grep -qiE 'reinstall|re-install'</automated>
    <fails_when>a non-zero exit from any stage — the rollback section still names a numbered tier, or it reintroduced the literal upstream removal command the existing prohibition deliberately avoids writing, or it does not state that recovery is a reinstall</fails_when>
    <automated>grep -q 'ii-original-dots-backup' docs/phase14-adopt-runbook.md && ! grep -qiE '(rm|mv|rmdir)[^\n]{0,60}ii-original-dots-backup' docs/phase14-adopt-runbook.md && test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' docs/phase14-adopt-runbook.md | grep -vx 'google-chrome-stable' | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit — the snapshot is no longer mentioned at all (so a reader cannot tell it exists), or a destructive command is now aimed at it, or a rewritten line used the collective noun instead of naming Waybar, rofi and swaync literally</fails_when>
    <automated>base="$(dirname docs/phase14-adopt-runbook.md)"; grep -o '](\.\{0,2\}/\?[^)#][^)]*)' docs/phase14-adopt-runbook.md | sed 's/](\(.*\))/\1/' | grep -v '^http' | while read -r l; do [ -e "$base/$l" ] || [ -e "$l" ] || { echo "BROKEN -> $l"; exit 1; }; done; ./scripts/phase16-retire-assert.sh >/tmp/p16-05-r.txt 2>&1; grep -q '^=== done: FAIL=0 ===$' /tmp/p16-05-r.txt</automated>
    <fails_when>a `BROKEN -> <path>` line is printed, or the phase-16 assert summary is not exactly `=== done: FAIL=0 ===` — the second check is a scope guard: this plan must not touch the playbook, and a regression there would surface as a documentation-ban failure</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/phase14-adopt-runbook.md` still has exactly 17 top-level `## ` headings and still contains the rollback section heading.
    - The rollback section matches, case-insensitively, none of `tier 1`, `tier 2`, `tier 3`, `three-tier`, `tier-1`, `tier-2`, `tier-3`.
    - The rollback section does not contain the literal upstream removal command, and the existing prohibition wording that names the capability without writing it is preserved.
    - The rollback section states that recovery is a reinstall, names `vendor/dots-hyprland` as the pinned source, and states that nothing is preserved on install.
    - The file still mentions `ii-original-dots-backup` and contains no destructive command aimed at it.
    - The file contains a pointer to `dots-hyprland-workflow.md` as the canonical operator document.
    - Non-allowlisted `chrome` tokens in the file: 0.
    - Every relative Markdown link resolves, and `./scripts/phase16-retire-assert.sh` still ends in exactly `=== done: FAIL=0 ===`.
  </acceptance_criteria>
  <done>The runbook's recovery section describes the recovery that exists: a reproducible reinstall from the pinned submodule, the wrapper's own removal path, and the plain statement that nothing is preserved on install. The pre-adopt snapshot is acknowledged as present and untouched without being presented as a tier or aimed at with a command. The file keeps its structure and its historical accuracy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| historical record → operator mid-recovery | This file is what a human reaches for after the session is already broken. A tier that depends on deleted machinery is discovered at the worst possible moment. |
| narrative fence → operator's shell | A fence that reads as a step is pasted as one. The distinction between record and instruction is the whole safety property of this sweep. |
| runbook prose → the two on-disk snapshots | The rotation narrative is the only place in the repo that tells anyone to move a snapshot. Correcting it must not turn into aiming a command at one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-28 | Destruction of data | rollback tier list | high | mitigate | The tiers are replaced wholesale with the reinstall narrative; the section is section-scoped grep-checked for every tier spelling, so a half-removed tier fails the task (D-20) |
| T-16-29 | Destruction of data | backup-rotation narrative | high | mitigate | Rewritten as record plus an explicit retirement statement; acceptance criteria forbid any destructive command aimed at either snapshot directory |
| T-16-30 | Denial of Service | deleted preflight invocations presented as steps | high | mitigate | No line may begin with a bare invocation of the deleted script, and the file must state the removal; the link resolver catches a dangling relative link to it |
| T-16-31 | Tampering | reintroducing the literal upstream removal command | medium | mitigate | Section-scoped ban on the literal token; the existing evasion wording is preserved by acceptance criterion (research Pitfall 8, second-order note) |
| T-16-32 | Repudiation | the adopt-window record itself | high | mitigate | Section count pinned at 17, recorded date and rotated-directory timestamp asserted present; the sweep may correct falsehoods but may not erase facts (D-24) |
| T-16-33 | Tampering | scope creep into the playbook | medium | mitigate | Task 2 re-runs the phase-16 assert, whose documentation half is scoped to the playbook — a stray edit there surfaces as a ban failure |
| T-16-34 | Information Disclosure | operator docs | low | accept | The runbook names paths, package names and timestamps. No credential is recorded in it. |
| T-16-SC | Tampering | package-manager installs | low | accept | This plan adds no `npm` / `pip` / `cargo` / `pacman` install step. `16-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable, with no `[ASSUMED]`, `[SUS]` or `[SLOP]` entries. |
</threat_model>

## Artifacts this phase produces

**Created by this plan:** none — no new file, no new heading. The section count is pinned at 17 precisely because this is a sweep.

**Modified surfaces:**

| Artifact | Kind | Note |
|----------|------|------|
| the go/no-go gate section | reframed | runnable invocations become records of the 2026-09-04 run (D-25) |
| the backup-rotation section | reframed plus one correction | record preserved; the mechanism is stated as retired (D-25, D-33) |
| the rollback section body | replaced | tier list out, reinstall narrative in (D-20) |

**Deleted by this plan — intentional removals, not omissions:** the three-tier structure with its per-tier source lists and inter-tier escalation warnings; every instruction presenting the deleted preflight script as a step to run; and any instruction whose side effect would destroy a remaining recovery source.

**Explicitly NOT touched:** the section numbering and count, the recorded install timestamp window, the recorded rotated-directory timestamp, the recorded overlay hashes and observed session state, the flags the adopt actually used, the known-losses section, the prohibition wording that names the upstream removal capability without writing it, and both snapshot directories on disk.

## Flagged Assumptions

| # | Item | Status | Why it is flagged |
|---|------|--------|-------------------|
| A15 | Command fences in the gate and rotation sections are reframed as records rather than deleted | resolved by the planner | D-25 says the section becomes historical narrative rather than a runnable step. Deleting the fences would lose the evidence of what was run; keeping them unmarked would leave a broken instruction. Reframing is the reading that satisfies both D-24 and D-25. |
| A16 | The runbook is not added to any assert's grep surface | resolved against D-36 | D-36 excludes it by name. The consequence is that this file's correctness rests on the sweep and on the link resolver rather than on a token gate — recorded so a later reader does not assume it is gated. |
| A17 | The runbook's recovery text agrees with the playbook's in substance but is not a copy of it | resolved by the planner | Phase 15 D-02 splits the roles; duplicating the paragraph verbatim would create a second drift surface of exactly the kind W-3 exists to catch. |

<verification>
- `scripts/phase16-retire-assert.sh` is a scope guard for this plan, not a subject of it: this plan must not touch the playbook, and the assert's documentation half would surface a stray edit there.
- `scripts/phase13-d19-assert.sh` remains **expected-red** until plan `16-06`. Do not run it as a gate.
- `scripts/phase14-verify.sh` is not run here; its clean-tree assertion cannot pass mid-wave.
- The link resolver is carried over verbatim from the Phase 15 verification vocabulary, where it was run against this same file. Reuse it rather than re-deriving.
- The section count of 17 is the structural invariant. It was 17 before this phase and must be 17 after; that single number is what distinguishes a sweep from a restructure.
- The section-scoped tier ban uses an `awk` range over the rollback section rather than a file-wide ban, because other sections of this record may legitimately refer to what the tiers were at the time.
</verification>

<success_criteria>
- Every runnable instruction in the runbook is runnable; nothing invokes a deleted script.
- The recovery section describes a reproducible reinstall rather than a tier list without machinery.
- The adopt-window facts are intact and the file still has seventeen sections.
- No route back to the retired session model is documented anywhere.
</success_criteria>

<output>
Create `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-05-SUMMARY.md` when done.
</output>