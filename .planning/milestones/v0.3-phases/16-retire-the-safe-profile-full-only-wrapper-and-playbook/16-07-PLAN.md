---
phase: 16-retire-the-safe-profile-full-only-wrapper-and-playbook
plan: 07
type: execute
wave: 5
depends_on: ["16-06"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/v0.3-MILESTONE-AUDIT.md
autonomous: true
requirements: [INV-04, FULL-01, FULL-02, FULL-04, ADOPT-03, DOC-03, IN-11, W-1, W-2, D-03, D-26, D-27, D-31]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "No requirement row in `.planning/REQUIREMENTS.md` asserts a behavior the wrapper no longer has (D-26)."
    - "The three requirement rows whose subject matter this phase removed are deleted from the checklist and from the traceability table together, so no orphan mapping survives (D-26, D-27)."
    - "The coverage arithmetic in `.planning/REQUIREMENTS.md` states nineteen requirements, nineteen mapped, zero unmapped (D-27)."
    - "The inventory requirement keeps its Phase 10 mapping even though its text is rewritten, because the specific rule governs over the general one and the Phase 10 assert deliberately still verifies the frozen record (D-26 over D-27, D-39)."
    - "`.planning/v0.3-MILESTONE-AUDIT.md` records the two blockers as closed by what actually closed them: the wrapper flip, and the retirement of the destination rather than a documented route back to it (D-31)."
    - "The audit's tech-debt entry for the deleted preflight script is closed, and no recommendation prescribes a command or a script that no longer exists (D-31, D-33)."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "The v0.3 requirement set, internally consistent with the shipped wrapper"
      contains: "v0.3 requirements: 19"
    - path: ".planning/v0.3-MILESTONE-AUDIT.md"
      provides: "The milestone audit with its two blockers dispositioned as closed"
      contains: "B-1"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "the coverage arithmetic must agree with the roadmap's coverage line, which plan 16-09 updates"
      pattern: "19"
    - from: ".planning/v0.3-MILESTONE-AUDIT.md"
      to: "docs/dots-hyprland-workflow.md"
      via: "the blocker evidence cites playbook locations that this phase rewrote"
      pattern: "dots-hyprland-workflow\\.md"
  prohibitions:
    - statement: "The disposition requirement row is not edited. It reads as a default that holds unless explicitly accepted otherwise, and Phase 11 explicitly accepted otherwise, so it is still true (D-26)."
      status: active
      verification: backstop
    - statement: "Deleted requirement IDs are removed from the checklist and from the traceability table in the same edit. A row deleted from one and left in the other is a worse state than either (D-26, D-27)."
      status: active
      verification: backstop
    - statement: "No new requirement ID is invented. Rewritten rows keep their existing IDs; this phase amends text and mappings, it does not extend the requirement set."
      status: active
      verification: backstop
    - statement: "`scripts/phase10-inventory-assert.sh` is not edited to agree with the rewritten inventory row. It verifies a frozen Phase 10 record, not current policy (D-39)."
      status: active
      verification: backstop
    - statement: "The audit's recorded findings are dispositioned, not deleted. An audit that erases its own blockers stops being an audit."
      status: active
      verification: backstop
    - statement: "No route back to the retired session model is documented in either file; the second blocker closes by retiring the destination."
      status: active
      verification: backstop
    - statement: "WR-02 and the D-38 `graphical-session.target` bootstrap stay open and unowned in both files (CONTEXT.md 'Not in this phase')."
      status: active
      verification: backstop
    - statement: "`arch/dots-hyprland.sh` is not modified. The drift baseline pinned in plan `16-06` compares against the working tree and any wrapper edit invalidates it."
      status: active
      verification: backstop
---

<objective>
Make the milestone's own bookkeeping agree with what shipped.

Six requirement rows currently assert behavior the wrapper no longer has, and three of them describe capabilities that were removed outright rather than changed. The milestone audit that opened this phase records two blockers whose stated remedies were the opposite of what actually happened: the first was supposed to close by adding an opt-in flag to the update contract, and it closes instead because the flag became unnecessary; the second was supposed to close by documenting a route back to the retired session model, and it closes instead because the destination was retired.

Purpose: leaving the requirement set contradicting the code is how a milestone audit produces a false pass. The amendment is what keeps the milestone internally consistent (D-26).
Output: nineteen coherent requirements with a matching traceability table, and an audit whose blockers are dispositioned honestly.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md
@.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md
</context>

<tasks>

<!-- planner-discipline-allow: SAFE_DEFAULTS -->
<!-- planner-discipline-allow: dual-run -->
<!-- planner-discipline-allow: safe dual-run -->
<!-- planner-discipline-allow: --skip-hyprland -->
<!-- planner-discipline-allow: PROTECT_EXPLICIT -->
<!-- planner-discipline-allow: --full -->
<!-- planner-discipline-allow: phase14-preflight.sh -->

<task type="auto">
  <name>Task 1: Amend the requirement set and its traceability table</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` — read in full. It is short. Note its three parts: the v0.3 checklist grouped by area, the out-of-scope table, and the traceability table with the coverage arithmetic beneath it.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md` — D-26's per-row amendment table in full, D-27's mapping and arithmetic rule, and D-39's instruction to leave the Phase 10 assert alone.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md` — Open Question 2, which names the one place D-26 and D-27 conflict and resolves it; and the note confirming every line reference in D-26's table against the live file, including the confirmed arithmetic.
    - `scripts/phase10-inventory-assert.sh` — read the section that requires retired language in the frozen Phase 10 inventory, so it is clear why rewriting the requirement row does not create a conflict with it.
    - The five summaries from this phase's earlier plans, for what the rewritten rows should now assert.
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Amend the file with scoped edits. Do not rewrite it wholesale — that risks losing rows outside the edit window.

**Rewrites, keeping the ID and the checked state:**

- The inventory row that records the residual-flag behavior: keep the half stating that the inventory recorded that behavior — a true statement about what Phase 10 captured — and drop the clause promising that the retired install profile remains available after this milestone. **Keep its traceability mapping at Phase 10.** This is the one place where D-26's specific rule and D-27's general rule disagree, and the specific rule governs: the row still describes what Phase 10 did, and the Phase 10 assert deliberately still verifies that frozen record.
- The first full-install row: it currently promises a documented explicit opt-in path. Rewrite it to state that the wrapper's only install path is full — no residual injection on any subcommand.
- The second full-install row: it currently asserts that the default still injects the residual flags and that full is never accidental. Rewrite it to state that a bare install or install-files invocation is the full behavior.
- The fourth full-install row: it survives, because the dry-run preview survives. Reword only its clause about residual injection; keep the rest and keep it checked.
- The adopt row asserting an operator-verified session state: drop its clause about the retired session-chrome policy; keep the monitors and shell-chrome halves, which the live verification suite still asserts.
- The documentation row: rewrite to the full-only playbook contract and drop the profile-comparison framing.

**Deletions — row and mapping entry together:**

- The full-install row about the backup gate and the bare-flag refusal. Both mechanisms are gone.
- The full-install row about package re-marking after install. The capability is gone.
- The adopt row about rollback guidance. With backups and package protection removed, no rollback guarantee is being made; deleting is more honest than rewriting to a weaker promise.

For each, remove the checklist row **and** its line in the traceability table in the same edit.

**No edit:** the disposition row about the session chrome. It reads as a default that holds unless explicitly accepted otherwise, and Phase 11 explicitly accepted otherwise, so it is still true. Leave it checked and mapped to Phase 11.

**Mapping for the rewritten rows.** Everything rewritten except the inventory row maps to **Phase 16** and is marked complete when this phase verifies. The inventory row stays at Phase 10.

**Future requirements and out-of-scope.** Annotate the cutover future-requirement as superseded — it was delivered by the Phase 11 decision and the Phase 14 adopt — and note that it stays outside the coverage count. In the out-of-scope table: reword the reason clause of the row deferring the module ports, which currently justifies the deferral by the retired session model; the row itself stands. Remove the row forbidding removal of the session chrome by default and the row forbidding a full default wrapper behavior — both are contradicted by what shipped.

**The arithmetic.** Twenty-two rows minus three deletions is nineteen. Update the coverage block to nineteen total, nineteen mapped, zero unmapped. Plan `16-09` updates the roadmap's coverage line to match; note the dependency in the summary.

**The superseded annotation.** Pick one wording and use it consistently across this file, the project file and the state file. The Phase 15 sweep record's flagged-not-edited table is the model. Record the chosen wording in the summary so plan `16-08` can reuse it verbatim.
  </action>
  <verify>
    <automated>R=.planning/REQUIREMENTS.md; grep -q 'v0.3 requirements: 19' "$R" && grep -q 'Mapped to phases: 19' "$R" && grep -qE 'Unmapped: 0' "$R" && test "$(grep -cE '^- \[[ x]\] \*\*[A-Z]+-[0-9]+\*\*' "$R")" -eq 19</automated>
    <fails_when>non-zero exit — the coverage block does not read nineteen total and nineteen mapped with zero unmapped, or the checklist does not contain exactly nineteen requirement rows, meaning a deletion or a rewrite miscounted</fails_when>
    <automated>R=.planning/REQUIREMENTS.md; for id in FULL-03 FULL-05 ADOPT-04; do if grep -q "$id" "$R"; then echo "ORPHAN: $id"; grep -n "$id" "$R"; exit 1; fi; done; test "$(grep -cE '^\| [A-Z]+-[0-9]+ \| Phase [0-9]+ \|' "$R")" -eq 19</automated>
    <fails_when>an `ORPHAN: <id>` line is printed with its location and the loop exits 1 — a deleted ID survives in the checklist or in the traceability table; or the traceability row count is not 19, meaning a checklist deletion left its mapping behind or vice versa</fails_when>
    <automated>R=.planning/REQUIREMENTS.md; grep -qE '^\| INV-04 \| Phase 10 \|' "$R" && grep -qE '^\| DISP-03 \| Phase 11 \|' "$R" && test "$(grep -cE '^\| (FULL-01|FULL-02|FULL-04|ADOPT-03|DOC-03) \| Phase 16 \|' "$R")" -eq 5</automated>
    <fails_when>non-zero exit — the inventory row was re-mapped to Phase 16 against the specific rule, the disposition row was moved or edited when it should be untouched, or fewer than five rewritten rows carry a Phase 16 mapping</fails_when>
    <automated>R=.planning/REQUIREMENTS.md; ! grep -qiE 'safe dual-run install remains available|still.{0,40}injects SAFE_DEFAULTS|explicit opt-in.{0,20}full-install|backup gate|PROTECT_EXPLICIT re-mark|Safe defaults remain default|Removing Waybar/rofi/swaync by default' "$R" && ./scripts/phase10-inventory-assert.sh >/tmp/p16-07-i.txt 2>&1; grep -q '^=== done: FAIL=0 ===$' /tmp/p16-07-i.txt</automated>
    <fails_when>non-zero exit — a retired claim survives verbatim in a requirement row or an out-of-scope row, or the Phase 10 inventory assert has started failing, which would mean it was edited to agree with the rewritten row instead of being left alone (D-39)</fails_when>
    <automated>git diff --quiet HEAD -- arch/ docs/ scripts/ .config/ stow/ vendor/</automated>
    <fails_when>non-zero exit — this planning-artifact task left an uncommitted change under a directory it does not own; the wrapper in particular must stay byte-identical to the commit plan `16-06` pinned</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains exactly 19 checklist rows and exactly 19 traceability rows, and its coverage block reads 19 total, 19 mapped, 0 unmapped.
    - The file contains no occurrence of `FULL-03`, `FULL-05` or `ADOPT-04`.
    - The traceability table maps the inventory row to Phase 10, the session-chrome disposition row to Phase 11, and the five rewritten rows to Phase 16.
    - The session-chrome disposition row's text is byte-identical to its previous state.
    - The file contains none of the retired claims: the promise that the retired install profile remains available, the assertion that the default still injects the residual flags, the promise of a documented explicit opt-in full path, the backup-gate row, the package-re-marking row, and the two contradicted out-of-scope rows.
    - The cutover future requirement carries the superseded annotation and remains outside the coverage count.
    - `./scripts/phase10-inventory-assert.sh` still ends in exactly `=== done: FAIL=0 ===`.
    - `git diff --quiet HEAD -- arch/ docs/ scripts/ .config/ stow/ vendor/` exits 0.
  </acceptance_criteria>
  <done>The requirement set says what the project does. Three capabilities that were removed no longer have rows promising them, five rewritten rows map to this phase, the inventory row keeps its Phase 10 mapping, and the arithmetic reads nineteen. The roadmap's coverage line still reads twenty-two and is corrected in plan `16-09`.</done>
</task>

<task type="auto">
  <name>Task 2: Disposition the milestone audit's blockers and clear its stale evidence</name>
  <read_first>
    - `.planning/v0.3-MILESTONE-AUDIT.md` — read in full. Work by section rather than by line list: the requirement and integration finding blocks near the top, the requirements-coverage section, the cross-phase integration section, the end-to-end flows section, the tech-debt section, and the recommended-closure section.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md` — D-31's rewrite site list, and D-33's statement that the tech-debt entry closes with the script deletion.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md` — the planning-artifact sweep section for this file, which lists the sites D-31 does not name and advises scoping the rewrite by section rather than by line.
    - The six summaries from this phase's earlier plans, which are the evidence for the dispositions.
    - `docs/dots-hyprland-workflow.md` — as rewritten, so the audit's cited locations and quoted evidence are checked against what is there now.
  </read_first>
  <files>.planning/v0.3-MILESTONE-AUDIT.md</files>
  <action>
Disposition the findings; do not erase them. An audit that deletes its own blockers stops being a record. Each finding keeps its identifier, its severity and its original issue statement, and gains a resolution.

**The first blocker.** Its recorded remedy was to add the opt-in flag to the playbook's update contract. That is not what happened. Record the actual resolution: the wrapper flip made the flag unnecessary, the update contract now prescribes one flat apply command, and the residual injection it warned about no longer exists on any subcommand. Cite the plans that did it. Update its stated location, which points at playbook line numbers that moved.

**The second blocker.** Its recorded remedy was to document a route back to the retired session model. That is the opposite of what happened, and deliberately so: the user's instruction was to remove the destination rather than to document a path to it. Record that resolution plainly — the hook-injection machinery it described is deleted, no restore route is documented anywhere, and the archived trees remain in the repository untouched as archive. Its issue statement quotes a wrapper function and a line range that no longer exist; keep the quote as the historical statement of the problem but mark that the machinery it names was removed in Phase 16.

**The two warnings this phase closes in another file.** Both are rewritten in place in the Phase 11 dispositions record by plan `16-08`. Here, mark them resolved and cite that plan. Do not pre-empt its edit.

**The third warning.** The playbook's duplicated apply fence is now checked against the Phase 13 source of truth by the extended overlay assert. Mark it resolved and cite the assert.

**The disposition-row evidence.** It records the archive path as one that does not exist. Correct the path to the archive location that does exist, and note that the corrected claim is being fixed at its source by plan `16-08`.

**The integration-flow section.** Its evidence quotes wrapper output strings and cites a line range for the backup gate and a package-marking function — all of which are gone. Rewrite the quoted evidence from the current wrapper's actual dry-run output rather than from memory, and drop the citations to deleted symbols. The two flow narratives that name the deleted preflight script and a restoration of the retired session model both need rewriting; one flow's completion status changes as a result, so restate it honestly rather than upgrading it silently.

**The tech-debt section.** The entry for the deleted preflight script's mandatory-rotation message closes here: the script is gone, so the debt is retired rather than paid. Record it as closed by deletion and cite the plan. The cosmetic entry that this phase makes moot goes the same way. Any remaining entry stays.

**The recommended-closure section.** Its recommendations prescribe using the opt-in flag for pin-bumps, writing the restore route, and gating a remediation on the deleted script. All three are obsolete. Replace them with what the phase actually did, and leave the two items that remain genuinely open — the three-roles question and the compositor-target autostart — listed as unowned. Do not assign them an owner.

**The unowned-items counts.** The audit states counts of unowned items in more than one place. Recount after the dispositions rather than adjusting one occurrence and leaving the other.

Write `Waybar`, `rofi` and `swaync` literally in every line you touch. The file currently carries several collective-noun occurrences in untouched historical prose; do not add more, and do not sweep the ones you are not editing.
  </action>
  <verify>
    <automated>A=.planning/v0.3-MILESTONE-AUDIT.md; grep -q 'B-1' "$A" && grep -q 'B-2' "$A" && test "$(grep -ciE '(resolved|closed).{0,200}(phase 16|16-0[1-9])' "$A")" -ge 4</automated>
    <fails_when>non-zero exit — either blocker identifier was deleted from the audit instead of dispositioned, or fewer than four findings carry a resolution citing a Phase 16 plan, meaning the dispositions were asserted without evidence</fails_when>
    <automated>A=.planning/v0.3-MILESTONE-AUDIT.md; for s in 'phase14-preflight' 'PROTECT_EXPLICIT' 'no SAFE_DEFAULTS injection' 'list_hypr_ii_hook_target_files (arch/dots-hyprland.sh:651-665)'; do if grep -qF -- "$s" "$A" && ! grep -qiE "removed in Phase 16|retired in Phase 16|closed by deletion" "$A"; then echo "UNMARKED STALE: $s"; exit 1; fi; done; grep -qiE 'closed by deletion' "$A"</automated>
    <fails_when>an `UNMARKED STALE: <token>` line is printed and the loop exits 1, or the trailing grep finds no closed-by-deletion note — the audit still cites a deleted script, array, output string or function without marking that Phase 16 removed it</fails_when>
    <automated>A=.planning/v0.3-MILESTONE-AUDIT.md; grep -q 'stow/' "$A" && ! grep -qF '.config/{waybar,rofi,swaync}' "$A" && ! grep -qiE '(write|document|add)[^.]{0,60}restore[^.]{0,40}dual-run' "$A"</automated>
    <fails_when>non-zero exit — the archive-path evidence still names the directory set that does not exist, or a recommendation still prescribes writing a route back to the retired session model, which is the remedy the user explicitly rejected</fails_when>
    <automated>A=.planning/v0.3-MILESTONE-AUDIT.md; grep -qiE 'WR-02|three roles' "$A" && grep -q 'graphical-session.target' "$A" && ! grep -qiE '(owner|owned by)[^.]{0,40}(WR-02|graphical-session)' "$A"</automated>
    <fails_when>non-zero exit — the two genuinely open items are no longer listed as remaining open, or one of them was assigned an owner that this phase has no authority to assign</fails_when>
    <automated>test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' .planning/v0.3-MILESTONE-AUDIT.md | grep -vx 'google-chrome-stable' | wc -l)" -le 6 && git diff --quiet HEAD -- arch/ docs/ scripts/ .config/ stow/ vendor/ && ./scripts/phase13-d19-assert.sh >/tmp/p16-07-d.txt 2>&1; test "$(grep -c '^\[FAIL\]' /tmp/p16-07-d.txt)" -eq 0</automated>
    <fails_when>non-zero exit — the collective-noun count rose above its baseline of 6 because a rewritten line used it instead of naming Waybar, rofi and swaync literally; or this planning task left an uncommitted change under a directory it does not own; or the overlay assert regressed, which at this point can only mean the wrapper or the playbook fence was touched</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/v0.3-MILESTONE-AUDIT.md` still contains both blocker identifiers and their original issue statements, each now carrying a resolution citing a Phase 16 plan.
    - At least four findings carry a resolution referencing Phase 16 or a `16-0N` plan.
    - Every surviving citation of the deleted preflight script, the deleted package array, the deleted output string or the deleted hook function is accompanied by a note that Phase 16 removed it; the tech-debt entry reads as closed by deletion.
    - The archive-path evidence names the `stow/` location and no longer names the non-existent `.config/` directory set.
    - No recommendation prescribes writing a route back to the retired session model, using the opt-in flag for a pin-bump, or gating anything on the deleted preflight script.
    - The three-roles item and the compositor-target autostart item are both still listed as open, and neither has an owner.
    - Non-allowlisted `chrome` tokens in the file: at most 6, its pre-existing baseline.
    - `git diff --quiet HEAD -- arch/ docs/ scripts/ .config/ stow/ vendor/` exits 0, and `./scripts/phase13-d19-assert.sh` prints zero `[FAIL]` lines.
  </acceptance_criteria>
  <done>The milestone audit records what actually closed its two blockers rather than what was originally proposed, its stale evidence is either refreshed from the current wrapper or marked as removed, its tech-debt entry for the deleted script is closed by deletion, and the two items that remain genuinely open are still listed without owners.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirement set → milestone audit | A milestone audit reads the requirement set as its input. A row promising a removed capability produces a false pass or a false gap, depending on which way the auditor reads it. |
| checklist → traceability table | The two are maintained by hand and their agreement is the only thing making coverage arithmetic meaningful. |
| audit findings → project memory | These findings are the record of what was wrong. Deleting them to make the audit look clean destroys the reason the phase exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-42 | Repudiation | requirement rows asserting removed capabilities | high | mitigate | Three rows deleted from checklist and traceability together, six rewritten; the orphan check fails the task if a deleted ID survives anywhere in the file |
| T-16-43 | Repudiation | coverage arithmetic | high | mitigate | Row counts and the coverage block are asserted independently at 19, so an edit that deletes a row without updating the arithmetic fails rather than passing silently |
| T-16-44 | Repudiation | audit findings erased rather than dispositioned | high | mitigate | Both blocker identifiers must still be present and each must carry a resolution citing a Phase 16 plan; a deleted finding fails the task |
| T-16-45 | Tampering | the Phase 10 assert edited to agree with a rewritten row | medium | mitigate | The assert is re-run as part of this task's verify; it must stay green, which it does only if it was left alone (D-39) |
| T-16-46 | Tampering | stale evidence quoting deleted symbols | medium | mitigate | Each surviving citation must be accompanied by a removal note; the check pairs the citation with the note rather than banning the citation, because the historical statement has value |
| T-16-47 | Tampering | scope creep into code or docs from a planning-artifact plan | medium | mitigate | `git diff --quiet HEAD` over the five code and doc directories, plus a re-run of the overlay assert whose drift pin depends on the wrapper being untouched |
| T-16-48 | Information Disclosure | planning artifacts | low | accept | These files record decisions, paths and requirement text. No credential appears in either. |
| T-16-SC | Tampering | package-manager installs | low | accept | This plan adds no `npm` / `pip` / `cargo` / `pacman` install step. `16-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable, with no `[ASSUMED]`, `[SUS]` or `[SLOP]` entries. |
</threat_model>

## Artifacts this phase produces

**Created by this plan:** none — no new file, no new requirement ID, no new finding identifier.

**Modified surfaces:**

| Artifact | Kind | Note |
|----------|------|------|
| six requirement rows | rewritten | five re-mapped to Phase 16; the inventory row keeps Phase 10 (D-26 over D-27) |
| the coverage block | recomputed | 22 → 19; the roadmap's matching line is updated in plan `16-09` |
| the cutover future requirement | annotated superseded | outside the coverage count |
| two blocker findings and three warnings | dispositioned | resolutions cite the plans that closed them |
| the integration-flow evidence | re-captured | quoted output taken from the current wrapper rather than carried forward |

**Deleted by this plan — intentional removals, not omissions:** three requirement rows and their three traceability entries — the backup-gate row, the package-re-marking row and the rollback-guidance row; two out-of-scope rows contradicted by what shipped; and the obsolete recommendations prescribing the opt-in flag, the restore route, and a remediation gated on a deleted script.

**Explicitly NOT touched:** the session-chrome disposition row, `scripts/phase10-inventory-assert.sh`, the Phase 11 dispositions record (plan `16-08`), the roadmap and state files (plan `16-09`), and every finding's original issue statement.

## Flagged Assumptions

| # | Item | Status | Why it is flagged |
|---|------|--------|-------------------|
| A20 | The inventory requirement keeps its Phase 10 mapping although its text is rewritten | resolved by the planner | D-26 says it stays mapped to Phase 10; D-27 says rewritten rows map to Phase 16. The two conflict for this one ID. The specific rule governs, and it is consistent with D-39 leaving the Phase 10 assert verifying the frozen record. Recorded because it is a documented conflict, not an oversight. |
| A21 | The audit's findings are dispositioned in place rather than moved to a resolved section | resolved by the planner | The file's structure groups findings by severity, not by state. Adding a resolved section would restructure an audit this phase does not own the shape of; adding a resolution to each finding preserves both the record and the shape. |
| A22 | One end-to-end flow's completion status changes as a result of the dispositions | recorded, deliberate | The flows section counts complete flows. Restating a flow honestly may change that count in either direction. The action requires restating rather than silently upgrading, and requires the unowned-item counts to be recomputed rather than adjusted in one place. |

<verification>
- This is a planning-artifact plan. Its scope fence is `git diff --quiet HEAD` over `arch/`, `docs/`, `scripts/`, `.config/`, `stow/` and `vendor/` — none of which it may touch.
- `arch/dots-hyprland.sh` in particular must stay byte-identical to the commit plan `16-06` pinned; the overlay assert is re-run as the check.
- `scripts/phase10-inventory-assert.sh` is re-run specifically as a contradiction check: it requires retired language in a frozen Phase 10 record, and D-39 leaves it alone. If it goes red, the rewritten requirement row was chased into the assert.
- The coverage arithmetic is checked from two directions — the row counts and the stated totals — because an edit that updates one and not the other is the most likely error and is invisible to a single-sided check.
- The collective-noun count for the audit file is a ceiling at its existing baseline rather than zero, because the file carries several occurrences in historical prose this plan does not sweep. The ceiling catches new ones without forcing an out-of-scope rewrite.
</verification>

<success_criteria>
- No requirement row promises a capability the wrapper does not have.
- The checklist, the traceability table and the coverage arithmetic all say nineteen.
- The audit's two blockers are recorded as closed by what actually closed them.
- The two genuinely open items remain open and unowned.
</success_criteria>

<output>
Create `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-07-SUMMARY.md` when done.
</output>