---
phase: 37-bar-layout-integration-dual-monitor-verification-strict-pack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
  - restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml
  - scripts/phase37-voice-pill-assert.sh
autonomous: true
requirements:
  - INTG-01
  - INTG-02
  - INTG-03
  - INTG-04

estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "VoicePill is directly instantiated in BarContent.qml Right zone immediately following mediaLoader with clean 4px gaps (INTG-01, D-01, D-07, D-10)."
    - "VoicePill receives useShortenedForm binding from BarContent.qml and suppresses text expansion on narrow/rotated screens (useShortenedForm == 2) and vertical bar mode (INTG-01, D-02, D-04, D-08)."
    - "VoicePill allows full text label expansion for live timers and state badges on displays with useShortenedForm < 2 (INTG-01, D-03)."
    - "VoicePill contains an inert MouseArea with parent: root absorbing all mouse buttons (Left, Right, Middle) so clicking the pill never triggers barRightSideMouseArea (INTG-01, D-05, D-06)."
    - "VoicePill uses zero hardcoded hex colors and dynamically binds to Material You Appearance.colors.* tokens (INTG-03, D-12)."
    - "All managed QML files are deployed under restow/quickshell/ as leaf symlinks without ancestor directory folding, keeping vendor/dots-hyprland pristine (INTG-02)."
    - "Automated test harness scripts/phase37-voice-pill-assert.sh passes all 5 sections and arch/dots-hyprland.sh verify --strict exits 0 findings with zero git churn (INTG-04, D-11, D-14, D-15)."
  artifacts:
    - path: "restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml"
      provides: "Right zone status bar layout integrating VoicePill immediately after mediaLoader"
      contains: "VoicePill {"
    - path: "restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml"
      provides: "Voice pill component with useShortenedForm suppression and inert MouseArea"
      contains: "property real useShortenedForm"
    - path: "scripts/phase37-voice-pill-assert.sh"
      provides: "5-section automated verification suite for layout, mouse isolation, responsive geometry, theming, and strict packaging"
      contains: "phase37-voice-pill-assert"
  key_links:
    - from: "restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml"
      to: "restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml"
      via: "direct QML component instantiation inside rightSectionRowLayout with useShortenedForm binding"
      pattern: "VoicePill\\s*\\{[\\s\\S]*useShortenedForm:\\s*root\\.useShortenedForm"
    - from: "restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml"
      to: "restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml"
      via: "inert MouseArea absorbing all mouse button events to prevent triggering barRightSideMouseArea"
      pattern: "acceptedButtons:\\s*Qt\\.AllButtons"
    - from: "scripts/phase37-voice-pill-assert.sh"
      to: "arch/dots-hyprland.sh"
      via: "strict repository verification gate invocation"
      pattern: "\\./arch/dots-hyprland\\.sh verify --strict"
---

<objective>
Integrate `VoicePill` into `BarContent.qml` Right zone immediately after `mediaLoader`, implement responsive multi-monitor adaptation and inert mouse event absorption in `VoicePill.qml`, deploy via `restow/quickshell/` GNU Stow leaf symlinks without folding ancestor directories or altering `vendor/dots-hyprland`, and validate with a 5-section automated assertion test harness (`scripts/phase37-voice-pill-assert.sh`) alongside strict repository verification (`arch/dots-hyprland.sh verify --strict` 0 findings).

Purpose: Complete Milestone v0.7 by promoting the voice status telemetry component into an active, globally distributed status bar indicator across all displays with robust responsive safety and pristine package integrity.
Output: Integrated `BarContent.qml`, updated `VoicePill.qml`, executable test harness `scripts/phase37-voice-pill-assert.sh`, and successful strict verification.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-bar-layout-integration-dual-monitor-verification-strict-pack/37-CONTEXT.md
@.planning/phases/37-bar-layout-integration-dual-monitor-verification-strict-pack/37-RESEARCH.md
@.planning/phases/37-bar-layout-integration-dual-monitor-verification-strict-pack/37-PATTERNS.md
@.planning/phases/37-bar-layout-integration-dual-monitor-verification-strict-pack/37-VALIDATION.md
@restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
@restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml
@scripts/phase36-voice-pill-assert.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Test harness scaffold and BarContent.qml layout integration tracer slice</name>
  <files>scripts/phase37-voice-pill-assert.sh, restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml</files>
  <action>
    Create the phase test harness scaffold at `scripts/phase37-voice-pill-assert.sh` (per D-11) and implement the end-to-end tracer slice integrating `VoicePill` into `BarContent.qml` (per D-01, D-07, D-08, D-09, D-10, INTG-01, INTG-02).

    1. In `scripts/phase37-voice-pill-assert.sh`:
       - Model after `scripts/phase36-voice-pill-assert.sh` with strict bash safety (`set -euo pipefail`), non-root guard (`EUID != 0`), CLI argument parsing for `--section <1-5>` (`-s <1-5>`) and `--syntax` (`-c`), temporary file and mock process cleanup traps, and git porcelain snapshot tracking (`PORCELAIN_BEFORE` and `PORCELAIN_AFTER`).
       - Define headless runner function `run_qs_test()` running temporary QML test snippets via `quickshell -p` with timeouts.
       - Implement Section 1 (Layout Integration & Direct Mounting):
         * Assert that `BarContent.qml` declares `VoicePill` directly inside `rightSectionRowLayout` immediately following `mediaLoader` and preceding `updatesLoader` without any intermediate elements (per D-07, D-10, INTG-01).
         * Assert that `VoicePill` is declared directly without being wrapped in a redundant `Loader` or nested `BarGroup` (per D-07).
         * Assert that `VoicePill` explicitly binds `useShortenedForm: root.useShortenedForm` (per D-08).
         * Assert that `VoicePill` explicitly binds `Layout.alignment: Qt.AlignVCenter` (per D-09).
         * Assert that `~/.config/quickshell/ii/modules/ii/bar/BarContent.qml` is a live leaf symlink pointing to `restow/quickshell/...` and parent directory `~/.config/quickshell/ii/modules/ii/bar` is a real directory, not a directory symlink (per INTG-02).
       - Make `scripts/phase37-voice-pill-assert.sh` executable (`chmod +x`).

    2. In `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml`:
       - Directly instantiate `VoicePill` within `rightSectionRowLayout` immediately following `mediaLoader` (after line 204) and before `updatesLoader` (per D-01, D-07, D-10, INTG-01):
         Set `id: voicePill`, `Layout.alignment: Qt.AlignVCenter` (per D-09), and `useShortenedForm: root.useShortenedForm` (per D-08).
       - Maintain clean sibling spacing (`spacing: 4` owned by `rightSectionRowLayout`) without redundant wrappers.
  </action>
  <verify>
    <automated>./scripts/phase37-voice-pill-assert.sh -s 1</automated>
    <fails_when>non-zero exit, or "[FAIL]" in test output</fails_when>
  </verify>
  <done>
    `scripts/phase37-voice-pill-assert.sh -s 1` passes with zero failures, proving `VoicePill` is correctly declared in `BarContent.qml` right after `mediaLoader` with required properties and valid leaf symlink deployment.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Responsive suppression and inert MouseArea event isolation in VoicePill.qml</name>
  <files>restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml, scripts/phase37-voice-pill-assert.sh</files>
  <behavior>
    - VoicePill exposes property real useShortenedForm with default 0.
    - VoicePill isExpanded evaluates to true when effectiveState !== "idle" AND useShortenedForm < 2 AND !vertical.
    - When useShortenedForm == 2 (narrow/rotated screens like HDMI-A-2 at 720px width), isExpanded evaluates to false, clamping pill width to resting 26px size while preserving soundwave icon and reactive color animation.
    - When vertical is true, isExpanded evaluates to false, clamping pill width to Appearance.sizes.baseVerticalBarWidth.
    - VoicePill contains an inert MouseArea with parent: root covering the entire pill surface (anchors.fill: parent).
    - MouseArea accepts all mouse buttons (Qt.AllButtons), has cursorShape: Qt.ArrowCursor, hoverEnabled: false, and consumes onPressed (event.accepted = true).
    - Mouse clicks on VoicePill do not bubble up to barRightSideMouseArea and do not toggle sidebarRightOpen.
  </behavior>
  <action>
    Update `VoicePill.qml` to handle responsive monitor constraints and isolate mouse events, and expand `scripts/phase37-voice-pill-assert.sh` with Section 2 and Section 3 test suites (per D-02, D-03, D-04, D-05, D-06, D-13, INTG-01, INTG-02).

    1. In `restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml`:
       - Add responsive property contract: `property real useShortenedForm: 0` (per D-08).
       - Update dynamic expansion boolean guard: `readonly property bool isExpanded: effectiveState !== "idle" && useShortenedForm < 2 && !vertical` (per D-02, D-03, D-04).
       - Add public verification alias: `readonly property alias inertMouseArea: inertMouseArea`.
       - Declare the event-absorbing `MouseArea` attached directly to the root item:
         Explicitly set `parent: root` to detach the item from `BarGroup`'s internal `GridLayout` (avoiding anchor warnings and layout deformation per Pitfall 2).
         Set `anchors.fill: parent`, `acceptedButtons: Qt.AllButtons`, `cursorShape: Qt.ArrowCursor`, `hoverEnabled: false`, and `onPressed: event => event.accepted = true` (per D-05, D-06).

    2. In `scripts/phase37-voice-pill-assert.sh`:
       - Implement Section 2 (Inert MouseArea & Event Isolation):
         * Perform AST analysis on `VoicePill.qml` verifying `MouseArea` exists with `parent: root`, `anchors.fill: parent`, `acceptedButtons: Qt.AllButtons`, `cursorShape: Qt.ArrowCursor`, `hoverEnabled: false`, and `onPressed` consuming the event (`event.accepted = true`) (per D-05, D-06).
         * Execute headless QML test verifying `inertMouseArea` properties at runtime.
       - Implement Section 3 (Multi-Monitor Responsive Adaptation & Expansion Suppression):
         * Query `hyprctl monitors -j` to evaluate active compositor geometry (`DP-1` active at 3440px allows expansion per D-03, D-13).
         * Execute headless Quickshell test suite evaluating synthetic monitor geometry:
           - Full width (`useShortenedForm: 0`): `isExpanded` is true during active recording, width expands to ~63px-125px (per D-03).
           - Narrow/rotated screen (`useShortenedForm: 2` e.g. 720px logical width): `isExpanded` is false during recording, width remains clamped at resting 26px (per D-02).
           - Vertical bar (`vertical: true`): `isExpanded` is false during recording, width clamps to `Appearance.sizes.baseVerticalBarWidth` (per D-04).
  </action>
  <verify>
    <automated>./scripts/phase37-voice-pill-assert.sh -s 2 && ./scripts/phase37-voice-pill-assert.sh -s 3</automated>
    <fails_when>non-zero exit, or "[FAIL]" in test output</fails_when>
  </verify>
  <done>
    Sections 2 and 3 of `scripts/phase37-voice-pill-assert.sh` pass with zero failures, confirming mouse event swallowing without sidebar bleed and multi-monitor responsive expansion suppression.
  </done>
</task>

<task type="auto">
  <name>Task 3: Matugen dynamic theme probe, GNU Stow leaf symlink deployment, and strict packaging verification</name>
  <files>scripts/phase37-voice-pill-assert.sh</files>
  <action>
    Complete `scripts/phase37-voice-pill-assert.sh` with Section 4 (Theme Reactivity) and Section 5 (Strict Packaging Gate), deploy and verify all symlinks via `restow/quickshell/`, and execute strict repository verification (per D-11, D-12, D-14, D-15, INTG-02, INTG-03, INTG-04).

    1. In `scripts/phase37-voice-pill-assert.sh`:
       - Implement Section 4 (Dynamic Palette Adaptation & Theme Audit):
         * Verify AST audit of `VoicePill.qml` for zero hardcoded hex colors (`grep -E "#[0-9a-fA-F]{3,8}"` returns zero matches) (per D-12, INTG-03).
         * Verify semantic color token bindings: `recording` -> `Appearance.colors.colPrimary`, `transcribing` -> `Appearance.colors.colTertiary`, `typing`/`speaking`/`wrapup` -> `Appearance.colors.colSecondary`, and idle -> `Appearance.colors.colOnLayer1` (per D-12, INTG-03).
         * Implement soft-detection for running `quickshell` process (`pgrep -x quickshell`): if running, trigger live reload and verify process survival; if absent, emit `[INFO] [SOFT] Quickshell process not running; skipping live reload trigger` (per D-14).
       - Implement Section 5 (Git Invariance, GNU Stow Leaf Symlinks, & Strict Repository Verification Gate):
         * Verify `~/.config/quickshell/ii/modules/ii/bar/VoicePill.qml` and `BarContent.qml` are live leaf symlinks pointing to `restow/quickshell/...` and ancestor directory `modules/ii/bar` is a real directory (per INTG-02).
         * Verify submodule `vendor/dots-hyprland` is 100% clean and unmodified (`git diff --exit-code vendor/dots-hyprland` passes) (per INTG-02).
         * Compare `PORCELAIN_BEFORE` and `PORCELAIN_AFTER` ensuring zero working-tree churn during test execution (per D-12, INTG-03).
         * In non-syntax mode, execute `./arch/dots-hyprland.sh verify --strict` and require output containing `=== done: FAIL=0 FINDINGS=0 ===` (per D-15, INTG-04).

    2. Verify and deploy GNU Stow leaf symlinks:
       - Run `cd restow && stow --verbose=5 --no-folding -t ~ quickshell` if necessary to ensure all leaf symlinks in `~/.config/quickshell/ii/` are fresh and properly linked without ancestor folding.

    3. Execute the full multi-section test harness `./scripts/phase37-voice-pill-assert.sh` across all 5 sections.
  </action>
  <verify>
    <automated>./scripts/phase37-voice-pill-assert.sh</automated>
    <fails_when>non-zero exit, or "[FAIL]" in test output, or "FAIL=" count greater than 0</fails_when>
  </verify>
  <done>
    Full 5-section test harness `./scripts/phase37-voice-pill-assert.sh` exits 0 with FAIL=0 FINDINGS=0, and `./arch/dots-hyprland.sh verify --strict` confirms zero repository packaging findings and zero working tree drift.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Desktop User → Status Bar Pill | User mouse interaction (clicks/drags) crosses into the status bar component hierarchy. |
| Filesystem → Symlink Hierarchy | GNU Stow maps repository overlay files into user `$HOME/.config/` via symlinks. |
| Test Harness → Desktop Environment | Automated assertions execute headless shell and compositor queries against active desktop state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Elevation of Privilege / Tampering | `VoicePill.qml` `MouseArea` | medium | mitigate | Embed internal `MouseArea` with `parent: root`, `acceptedButtons: Qt.AllButtons`, and `onPressed: event => event.accepted = true` so clicks never bubble to `barRightSideMouseArea` (D-05, D-06). |
| T-37-02 | Tampering | GNU Stow deployment (`restow/quickshell/`) | high | mitigate | Strictly deploy via leaf symlinks with `--no-folding`; verify in test harness that ancestor directories (`modules/ii/bar`) are real directories, never folded symlinks (INTG-02). |
| T-37-03 | Tampering / Information Disclosure | `scripts/phase37-voice-pill-assert.sh` | medium | mitigate | Enforce non-root execution (`EUID != 0`), isolated temporary QML runners, and before/after git porcelain snapshot comparison ensuring zero working tree drift (D-12, INTG-03). |
| T-37-SC | Tampering | Dependency supply chain | low | accept | No external package managers (npm, pip, cargo) used in this phase; all components are internal QML and bash scripts. |
</threat_model>

<verification>
Automated multi-section test suite:
- `./scripts/phase37-voice-pill-assert.sh -s 1` (Layout Integration & Direct Mounting)
- `./scripts/phase37-voice-pill-assert.sh -s 2` (Inert MouseArea & Event Isolation)
- `./scripts/phase37-voice-pill-assert.sh -s 3` (Multi-Monitor Responsive Adaptation)
- `./scripts/phase37-voice-pill-assert.sh -s 4` (Dynamic Palette Adaptation & Theme Audit)
- `./scripts/phase37-voice-pill-assert.sh -s 5` (Strict Repository Verification Gate)
- `./scripts/phase37-voice-pill-assert.sh` (Full 5-section suite)
- `./arch/dots-hyprland.sh verify --strict` (0 findings)
</verification>

<success_criteria>
1. `VoicePill` renders immediately after `mediaLoader` in `BarContent.qml` with clean 4px gaps.
2. Narrow and rotated screens (`useShortenedForm == 2`) suppress text expansion to resting 26px icon-only mode.
3. Standard and wide displays (`useShortenedForm < 2`) display full text label expansion during active speech lifecycles.
4. Mouse clicks on `VoicePill` are absorbed without triggering `barRightSideMouseArea` or sidebar toggles.
5. Material You theme tokens adapt dynamically with zero hardcoded hex codes.
6. Deployment preserves GNU Stow leaf symlink structure with real ancestor directories and pristine upstream vendor submodule.
7. Automated test harness exits 0 with `FAIL=0 FINDINGS=0` and `./arch/dots-hyprland.sh verify --strict` reports 0 findings.
</success_criteria>

<output>
Create `.planning/phases/37-bar-layout-integration-dual-monitor-verification-strict-pack/37-01-SUMMARY.md` when done
</output>
