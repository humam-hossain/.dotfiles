---
phase: 37-bar-layout-integration-dual-monitor-verification-strict-pack
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - restow/quickshell/.config/quickshell/ii/modules/ii/verticalBar/VerticalBarContent.qml
  - restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml
  - restow/quickshell/.config/quickshell/ii/services/Voice.qml
  - scripts/phase37-voice-pill-assert.sh
autonomous: true
gap_closure: true
gap_ids:
  - G-37-2
  - G-37-6
requirements:
  - INTG-01
  - INTG-02
  - INTG-04

estimate:
  tokens: 35000
  raw_tokens: 18000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "VoicePill is instantiated in VerticalBarContent.qml bottom section with vertical: true, rendering properly when bar is oriented left or right (G-37-2)."
    - "Voice.qml duration tracking initializes and increments continuously from 0:00 (e.g. 0:01, 0:02...) during active STT recording without freezing at 0:00 (G-37-6)."
    - "recoverStartTime in Voice.qml guarantees elapsedSec is clamped (>= 0 and < maxDurationSeconds) and never sets startTime into the future (G-37-6)."
    - "All changes are managed under restow/quickshell/ and deployed as leaf symlinks into ~/.config/quickshell/ without ancestor directory folding."
    - "Automated test harness passes all sections including vertical bar checks and STT timer verification."
  artifacts:
    - path: "restow/quickshell/.config/quickshell/ii/modules/ii/verticalBar/VerticalBarContent.qml"
      provides: "Vertical bar layout integrating VoicePill component in bottom status zone"
      contains: "VoicePill {"
    - path: "restow/quickshell/.config/quickshell/ii/services/Voice.qml"
      provides: "Hardened duration tracking and bounds-checked start time recovery"
      contains: "Math.max(0, uptimeSec"
    - path: "scripts/phase37-voice-pill-assert.sh"
      provides: "Updated test suite asserting vertical bar integration and live STT duration counter"
      contains: "VerticalBarContent"
---

<objective>
Close UAT gaps G-37-2 (VoicePill not appearing when bar is set to left or right) and G-37-6 (STT duration timer stuck at 0:00 while speaking).
Integrate VoicePill into VerticalBarContent.qml, harden Voice.qml duration timer calculation against clock skew and procfs read drift, deploy via GNU Stow leaf symlinks, and verify with updated automated assertions.
</objective>

<tasks>

<task id="1" name="Vertical Bar Integration (G-37-2)">
<description>
Integrate `VoicePill` into `VerticalBarContent.qml` in `restow/quickshell/.config/quickshell/ii/modules/ii/verticalBar/VerticalBarContent.qml`.
Place `VoicePill` within `bottomSectionColumnLayout` with `vertical: true`.
Verify and ensure `VoicePill.qml` correctly formats its dimensions and layout when `vertical` is true (centering icon, honoring `Appearance.sizes.baseVerticalBarWidth`, suppressing text expansion).
Deploy via GNU Stow leaf symlink into `~/.config/quickshell/ii/modules/ii/verticalBar/VerticalBarContent.qml`.
</description>
<files>
- restow/quickshell/.config/quickshell/ii/modules/ii/verticalBar/VerticalBarContent.qml
- restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml
</files>
</task>

<task id="2" name="STT Recording Duration Timer Fix (G-37-6)">
<description>
Harden duration tracking in `restow/quickshell/.config/quickshell/ii/services/Voice.qml`.
1. In `recoverStartTime(pid)`:
   - Ensure `uptimeSec - (startTicks / 100.0)` is clamped so `elapsedSec` cannot be negative: `const elapsedSec = Math.max(0, uptimeSec - (startTicks / 100.0))`.
   - Validate that `elapsedSec < maxDurationSeconds` and `!isNaN(elapsedSec)`. If invalid or negative, fall back to `Date.now()`.
2. When transitioning from `idle`/`starting` to `recording` or `speaking` during an active shell session:
   - Ensure `startTime` is initialized cleanly with a guaranteed non-future timestamp (`Math.min(Date.now(), recoverStartTime(activePid))`).
3. Deploy updated `Voice.qml` via GNU Stow leaf symlinks into `~/.config/quickshell/ii/services/Voice.qml`.
</description>
<files>
- restow/quickshell/.config/quickshell/ii/services/Voice.qml
</files>
</task>

<task id="3" name="Test Harness Expansion & Verification">
<description>
Extend `scripts/phase37-voice-pill-assert.sh` to include:
1. Assertion verifying `VoicePill` is declared in `VerticalBarContent.qml` with `vertical: true` and leaf symlink exists.
2. Assertion verifying STT recording duration counter increments continuously from 0:00 to 0:01+ during live STT simulation without staying stuck at 0:00.
3. Run full test harness and execute `arch/dots-hyprland.sh verify --strict` with zero git churn and zero findings.
</description>
<files>
- scripts/phase37-voice-pill-assert.sh
</files>
</task>

</tasks>
