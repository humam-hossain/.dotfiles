---
phase: 35-voice-telemetry-state-service-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - restow/quickshell/.config/quickshell/ii/services/Voice.qml
  - scripts/phase35-voice-telemetry-assert.sh
  - /home/pera/github_repo/Voice/voice.py
autonomous: true
requirements:
  - TELEM-01
  - TELEM-02
  - TELEM-03
  - TELEM-04
  - TELEM-05
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Voice.qml Singleton service observes $XDG_RUNTIME_DIR/voice-stt/ state files asynchronously with adaptive polling (100ms active / 500ms idle) without blocking the UI thread (D-01, TELEM-01)."
    - "Service detects and parses speech lifecycle states (idle, starting, recording, transcribing, typing, speaking) with STT priority and 1000ms typing linger window (D-04, D-05, D-06, TELEM-02)."
    - "Service verifies process liveness against /proc/<pid>/cmdline, automatically purges stale PID locks using Quickshell.execDetached, and logs discrete warnings without popups (D-10, D-11, TELEM-03)."
    - "Live wall-clock duration tracks elapsedSeconds, elapsedMs, and formattedDuration (M:SS), freezes during transcribing/typing, resets on idle, and anchors to process start time across Quickshell reload (D-07, D-08, D-09, TELEM-04)."
    - "Active TTS voice metadata (--tts-voice, --tts-backend) is extracted from /proc/<tts_pid>/cmdline with fallback to Kokoro defaults (D-02, TELEM-05)."
    - "Concurrency and partial read edge cases handle file truncation fail-soft without exceptions or console spam (D-03, TELEM-01)."
  artifacts:
    - restow/quickshell/.config/quickshell/ii/services/Voice.qml
    - scripts/phase35-voice-telemetry-assert.sh
    - /home/pera/github_repo/Voice/voice.py
  key_links:
    - "Voice.qml FileView monitors -> $XDG_RUNTIME_DIR/voice-stt/recorder.pid and tts.pid written by voice.py"
    - "Voice.qml liveness validator -> /proc/<pid>/cmdline token matching and Quickshell.execDetached purge"
    - "Voice.qml reload recovery -> /proc/<pid>/stat field 22 starttime and /proc/uptime"
---

<objective>
Implement a centralized, non-blocking Quickshell Singleton service (`Voice.qml`) located in `restow/quickshell/.config/quickshell/ii/services/` that observes local speech runtime state files (`$XDG_RUNTIME_DIR/voice-stt/`), tracks full STT/TTS lifecycles, verifies process liveness, purges stale locks, computes drift-free duration, and exposes active TTS voice metadata.

Purpose: Provide the authoritative desktop telemetry foundation and reactive QML property bindings consumed by downstream UI components (`VoicePill.qml` in Phase 36 and `BarContent.qml` in Phase 37).
Output: Tested `Voice.qml` Singleton service, updated `voice.py` state emitters, and an automated 6-section assertion test harness `scripts/phase35-voice-telemetry-assert.sh`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-voice-telemetry-state-service-architecture/35-CONTEXT.md
@.planning/phases/35-voice-telemetry-state-service-architecture/35-RESEARCH.md
@.planning/phases/35-voice-telemetry-state-service-architecture/35-PATTERNS.md
@.planning/phases/35-voice-telemetry-state-service-architecture/35-VALIDATION.md

# Relevant pattern analogs
@vendor/dots-hyprland/dots/.config/quickshell/ii/services/ResourceUsage.qml
@vendor/dots-hyprland/dots/.config/quickshell/ii/services/Battery.qml
@restow/quickshell/.config/quickshell/ii/services/Privacy.qml
@scripts/phase34-verification-assert.sh
@scripts/phase31-overlay-pill-assert.sh
</context>

## Artifacts this phase produces

- restow/quickshell/.config/quickshell/ii/services/Voice.qml
- scripts/phase35-voice-telemetry-assert.sh
- Voice.sttState
- Voice.ttsState
- Voice.overallState
- Voice.sttPid
- Voice.ttsPid
- Voice.ttsVoice
- Voice.ttsBackend
- Voice.elapsedSeconds
- Voice.elapsedMs
- Voice.formattedDuration
- Voice.maxDurationSeconds
- Voice.startTime
- Voice.formatDuration
- Voice.recoverStartTime
- Voice.resetDuration
- Voice.updateDuration
- Voice.poll
- Voice.typingLingerTimer
- Voice.pollTimer

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end voice telemetry tracer — state emission, assert harness, and Quickshell Singleton service</name>
  <files>
    scripts/phase35-voice-telemetry-assert.sh,
    restow/quickshell/.config/quickshell/ii/services/Voice.qml,
    /home/pera/github_repo/Voice/voice.py
  </files>
  <read_first>
    /home/pera/github_repo/Voice/voice.py,
    vendor/dots-hyprland/dots/.config/quickshell/ii/services/ResourceUsage.qml,
    restow/quickshell/.config/quickshell/ii/services/Privacy.qml,
    scripts/phase34-verification-assert.sh,
    scripts/phase31-overlay-pill-assert.sh,
    .planning/phases/35-voice-telemetry-state-service-architecture/35-CONTEXT.md,
    .planning/phases/35-voice-telemetry-state-service-architecture/35-RESEARCH.md
  </read_first>
  <action>
    Wire the complete end-to-end tracer slice across speech engine, test harness, and Quickshell Singleton service:

    1. Speech Daemon Updates (voice.py per D-05):
       - In `/home/pera/github_repo/Voice/voice.py` around line 1180 in the TTS playback execution handler, replace `write_pid(TTS_PID_FILE)` with `write_pid_state(pid, "speaking", TTS_PID_FILE)`.
       - In `/home/pera/github_repo/Voice/voice.py` around lines 1455-1457 in the STT text insertion handler, insert `write_pid_state(pid, "typing")` immediately before `if insert_text(text, args):` so the typing state is published before text insertion starts.

    2. Automated Assertion Test Harness Scaffold (scripts/phase35-voice-telemetry-assert.sh):
       - Create `scripts/phase35-voice-telemetry-assert.sh` following the `scripts/phase34-verification-assert.sh` structure: bash strict mode (`set -euo pipefail`), fail closed if run as root, cleanup trap on EXIT for temporary files and scratch trees, git status porcelain snapshots before/after, and CLI section parsing (`--section|-s 1-6`).
       - Implement Section 1 (TELEM-01, D-01, D-03): assert `restow/quickshell/.config/quickshell/ii/services/Voice.qml` exists, assert it is deployed to `~/.config/quickshell/ii/services/Voice.qml` as a symlink without folding ancestor directories, assert `pragma Singleton` and `pragma ComponentBehavior: Bound` are declared, assert `FileView` instances have `printErrors: false` and `blockLoading: true`, and assert adaptive polling timer interval (100ms when active, 500ms when idle).
       - Implement Section 2 (TELEM-02, D-04, D-05, D-06): run headless Quickshell test (`quickshell -p`) asserting detection of all discrete states (`idle`, `starting`, `recording`, `transcribing`, `typing`, `speaking`), precedence hierarchy (`recording` > `transcribing` > `speaking` > `typing` > `starting` > `idle`), and `typingLingerTimer` (1000ms duration holding typing state).
       - Add stubs for sections 3, 4, 5, and 6. Make the script executable (`chmod +x`).

    3. Foundational Voice.qml Singleton Service (restow/quickshell/.config/quickshell/ii/services/Voice.qml):
       - Author `Voice.qml` declaring `pragma Singleton` and `pragma ComponentBehavior: Bound`.
       - Define `runtimeDir` resolving `Quickshell.env("XDG_RUNTIME_DIR") + "/voice-stt"` with fallback to `"/run/user/1000/voice-stt"`. Define paths for `recorderPidPath` and `ttsPidPath`.
       - Declare reactive properties: `sttState: "idle"`, `ttsState: "idle"`, and synthesized `overallState` binding matching the strict priority order: `recording` > `transcribing` > `speaking` > `typing` > `starting` > `idle` (D-04).
       - Declare process ID properties: `sttPid: 0` and `ttsPid: 0`.
       - Implement `FileView` instances for `recorderFile` (`path: root.recorderPidPath`) and `ttsFile` (`path: root.ttsPidPath`) with `printErrors: false` and `blockLoading: true` to suppress cold-boot console spam (D-03).
       - Implement `Timer` `typingLingerTimer` with `interval: 1000`, `repeat: false`, resetting `sttState = "idle"` and calling `resetDuration()` upon expiration (D-06).
       - Implement `poll()` function: reload `recorderFile` and `ttsFile`. Parse PID and state from space-delimited text. If files are missing or empty, handle fail-soft: if `sttState === "transcribing"`, transition to `sttState = "typing"` and restart `typingLingerTimer`; otherwise default to `idle` without logging errors (D-03).
       - Implement adaptive `Timer` `pollTimer` with dynamic interval `(root.overallState === "idle" && !typingLingerTimer.running) ? 500 : 100`, `repeat: true`, `running: true`, triggering `root.poll()` (D-01).
       - Deploy service using `stow -d restow --no-folding quickshell` and verify symlink at `~/.config/quickshell/ii/services/Voice.qml`.
  </action>
  <reversibility rating="reversible">
    Service addition in restow/ and state publishing flags in voice.py can be cleanly reverted or removed without touching upstream dots-hyprland or persistent databases.
  </reversibility>
  <verify>
    <automated>./scripts/phase35-voice-telemetry-assert.sh --section 1 && ./scripts/phase35-voice-telemetry-assert.sh --section 2</automated>
    <fails_when>non-zero exit code or FAIL > 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `/home/pera/github_repo/Voice/voice.py` contains `write_pid_state(pid, "speaking", TTS_PID_FILE)` and `write_pid_state(pid, "typing")`.
    - `restow/quickshell/.config/quickshell/ii/services/Voice.qml` exists and is symlinked to `~/.config/quickshell/ii/services/Voice.qml` without folding ancestor directories.
    - `scripts/phase35-voice-telemetry-assert.sh` exists, is executable, and runs sections 1 and 2 green (`FAIL=0`).
    - Service correctly transitions through speech lifecycle states and holds typing for 1000ms.
  </acceptance_criteria>
  <done>
    Tracer slice is verified end-to-end: state files written by voice.py are read by Voice.qml via FileView, evaluated into reactive state properties, and verified by sections 1 and 2 of the test harness.
  </done>
</task>

<task type="auto">
  <name>Task 2: Process liveness verification and automated stale PID lock purging</name>
  <files>
    restow/quickshell/.config/quickshell/ii/services/Voice.qml,
    scripts/phase35-voice-telemetry-assert.sh
  </files>
  <read_first>
    restow/quickshell/.config/quickshell/ii/services/Voice.qml,
    vendor/dots-hyprland/dots/.config/quickshell/ii/services/Battery.qml,
    scripts/phase35-voice-telemetry-assert.sh,
    .planning/phases/35-voice-telemetry-state-service-architecture/35-RESEARCH.md
  </read_first>
  <action>
    Implement procfs command line verification and automated stale lock cleanup in `Voice.qml` and test assertions in `scripts/phase35-voice-telemetry-assert.sh`:

    1. Liveness and Stale Lock Purge Implementation (Voice.qml per D-10, D-11):
       - In `Voice.qml`, define `FileView` instances `sttCmdlineFile` (`path: root.sttPid > 0 ? ("/proc/" + root.sttPid + "/cmdline") : ""`) and `ttsCmdlineFile` (`path: root.ttsPid > 0 ? ("/proc/" + root.ttsPid + "/cmdline") : ""`), both configured with `printErrors: false` and `blockLoading: true`.
       - In `poll()` for STT: when `recorder.pid` contains a numeric PID, assign `root.sttPid = pid` and call `sttCmdlineFile.reload()`. Inspect `sttCmdlineFile.text().toLowerCase()`.
       - If the cmdline text is empty or does not contain `"voice"` or `"voicemode"`, identify the PID lock as stale or recycled (T-35-02). Immediately execute `Quickshell.execDetached(["rm", "-f", root.recorderPidPath])` to purge the lock (D-10).
       - Emit discrete console warning `console.warn("[Voice] Purged stale PID lock: " + pid)` without triggering user notifications (D-11). Clear `root.sttPid = 0`. If `root.sttState === "transcribing"`, transition to `sttState = "typing"` and restart `typingLingerTimer`; otherwise transition to `"idle"`.
       - In `poll()` for TTS: when `tts.pid` contains a numeric PID, assign `root.ttsPid = pid` and call `ttsCmdlineFile.reload()`. Inspect `ttsCmdlineFile.text().toLowerCase()`. If empty or does not contain `"voice"` or `"voicemode"`, purge `root.ttsPidPath` using `Quickshell.execDetached(["rm", "-f", root.ttsPidPath])`, emit `console.warn("[Voice] Purged stale PID lock: " + pid)`, clear `root.ttsPid = 0`, and reset `root.ttsState = "idle"`.
       - Ensure all `Quickshell.execDetached` calls use strict argument arrays `["rm", "-f", path]` rather than shell string concatenation to prevent command injection vulnerabilities (T-35-03).

    2. Automated Assertion Test Implementation (scripts/phase35-voice-telemetry-assert.sh Section 3):
       - In `scripts/phase35-voice-telemetry-assert.sh`, implement Section 3 (TELEM-03):
       - Scenario A (Dead PID): write a dummy `recorder.pid` containing a non-existent PID (e.g. 999999). Run headless Quickshell test, assert that `recorder.pid` is removed from disk, `console.warn` log is emitted with `Purged stale PID lock: 999999`, and `overallState` defaults to `idle`.
       - Scenario B (Recycled PID): write a dummy `recorder.pid` containing a live process PID that is not voice (e.g. systemd PID 1 or bash PID). Run headless Quickshell test, assert that missing `"voice"` in cmdline causes `recorder.pid` to be purged immediately and returns state to `idle`.
       - Scenario C (Healthy PID): write a dummy `recorder.pid` containing a PID whose `/proc/<pid>/cmdline` includes `"voice"`. Run headless Quickshell test, assert that `recorder.pid` is preserved and active state is recognized.
  </action>
  <reversibility rating="reversible">
    Procfs inspection and execDetached file removal are localized to Voice.qml and can be modified or disabled without impacting external state.
  </reversibility>
  <verify>
    <automated>./scripts/phase35-voice-telemetry-assert.sh --section 3</automated>
    <fails_when>non-zero exit code or FAIL > 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `Voice.qml` verifies `/proc/<pid>/cmdline` contains `"voice"` or `"voicemode"`.
    - Stale PID files are purged via `Quickshell.execDetached(["rm", "-f", ...])`.
    - Discrete warning `[Voice] Purged stale PID lock: <pid>` is logged to console.
    - Section 3 in `scripts/phase35-voice-telemetry-assert.sh` passes with 0 failures (`FAIL=0`).
  </acceptance_criteria>
  <done>
    Process liveness verification reliably purges dead and recycled PID locks, preventing ghost states when speech processes terminate abnormally.
  </done>
</task>

<task type="auto">
  <name>Task 3: Elapsed duration counter with procfs reload anchor and TTS voice metadata extraction</name>
  <files>
    restow/quickshell/.config/quickshell/ii/services/Voice.qml,
    scripts/phase35-voice-telemetry-assert.sh
  </files>
  <read_first>
    restow/quickshell/.config/quickshell/ii/services/Voice.qml,
    scripts/phase35-voice-telemetry-assert.sh,
    .planning/phases/35-voice-telemetry-state-service-architecture/35-RESEARCH.md,
    .planning/phases/35-voice-telemetry-state-service-architecture/35-CONTEXT.md
  </read_first>
  <action>
    Implement drift-free wall-clock duration calculation with procfs reload recovery, duration freeze during transcribing/typing, and null-delimited TTS metadata extraction in `Voice.qml`, and finalize test harness sections 4, 5, and 6:

    1. Wall-Clock Duration Counter & Procfs Reload Recovery (Voice.qml per D-07, D-08, D-09):
       - In `Voice.qml`, declare duration properties: `property int elapsedSeconds: 0`, `property real elapsedMs: 0`, `property string formattedDuration: "0:00"`, `readonly property int maxDurationSeconds: 300`, and `property real startTime: 0` (D-09).
       - Implement `formatDuration(sec)` helper returning `Math.floor(sec / 60) + ":" + ((sec % 60) < 10 ? "0" : "") + (sec % 60)`.
       - Add `FileView` instances `sttStatFile` (`path: root.sttPid > 0 ? ("/proc/" + root.sttPid + "/stat") : ""`) and `uptimeFile` (`path: "/proc/uptime"`), both configured with `printErrors: false` and `blockLoading: true`.
       - Implement `recoverStartTime(pid)` helper: reload `sttStatFile` and `uptimeFile`. Extract process start time in jiffies from `/proc/<pid>/stat` (field 22, located after the last closing parenthesis `)` of `comm` at index 19) and uptime seconds from `/proc/uptime`. Compute elapsed seconds as `uptimeSec - (startTicks / 100.0)` and return `Date.now() - (elapsedSec * 1000)` (D-07). Default to `Date.now()` if files are empty or unreadable.
       - Implement `updateDuration()` helper: when `overallState` is `"recording"` or `"speaking"`, initialize `startTime = recoverStartTime(activePid)` if `startTime <= 0`. Compute `diff = Math.max(0, Date.now() - startTime)`, setting `elapsedMs = diff`, `elapsedSeconds = Math.floor(diff / 1000)`, and `formattedDuration = formatDuration(elapsedSeconds)`.
       - When `overallState` is `"transcribing"` or `"typing"`, keep duration metrics frozen at their current values (D-08).
       - When `overallState === "idle"` and `typingLingerTimer` is not running, invoke `resetDuration()` setting `startTime = 0`, `elapsedMs = 0`, `elapsedSeconds = 0`, and `formattedDuration = "0:00"` (D-08).
       - Invoke `root.updateDuration()` at the end of every `poll()` cycle.

    2. TTS Voice & Backend Metadata Extraction (Voice.qml per D-02):
       - In `Voice.qml`, declare `property string ttsVoice: "af_heart"` and `property string ttsBackend: "kokoro"`.
       - In `poll()` when processing a healthy `tts.pid`, read `ttsCmdlineFile.text()`. Split the string on null byte delimiter (`\0` or `\u0000`). Iterate over arguments searching for `--tts-voice <val>` and `--tts-backend <val>`. Update `root.ttsVoice` and `root.ttsBackend` accordingly. Fall back to defaults `"af_heart"` and `"kokoro"` if arguments are omitted.

    3. Test Harness Finalization (scripts/phase35-voice-telemetry-assert.sh Sections 4, 5, 6):
       - Implement Section 4 (TELEM-04, D-07, D-08, D-09): run headless Quickshell test asserting duration updates live during active recording/speaking, formatted duration formats as `"M:SS"`, duration freezes during transcribing and typing, resets to `"0:00"` on idle, and anchors to process start time when initializing with an existing PID.
       - Implement Section 5 (TELEM-05, D-02): run headless Quickshell test asserting `--tts-voice` (e.g. `af_bella`, `am_adam`) and `--tts-backend` (`kokoro`) are parsed from null-delimited cmdline buffers, falling back to `af_heart` and `kokoro` when omitted.
       - Implement Section 6: assert git working-tree invariance (`git status --porcelain` is identical before and after harness run) and execute repository verification `arch/dots-hyprland.sh verify --strict` asserting 0 findings.
       - Run full test suite `./scripts/phase35-voice-telemetry-assert.sh` and verify all 6 sections pass with `FAIL=0 FINDINGS=0`.
  </action>
  <reversibility rating="reversible">
    Duration calculations and procfs parsing logic are self-contained in Voice.qml with fallback defaults, requiring no migrations.
  </reversibility>
  <verify>
    <automated>./scripts/phase35-voice-telemetry-assert.sh</automated>
    <fails_when>non-zero exit code or FAIL > 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `Voice.qml` exposes `elapsedSeconds`, `elapsedMs`, `formattedDuration`, `maxDurationSeconds`, `ttsVoice`, and `ttsBackend`.
    - Elapsed duration updates in real-time while recording/speaking, freezes during transcribing/typing, and resets to `"0:00"` on idle.
    - Quickshell reload duration recovery anchors `startTime` using `/proc/<pid>/stat` and `/proc/uptime`.
    - TTS voice metadata correctly parses `--tts-voice` and `--tts-backend` from null-delimited `/proc/<pid>/cmdline`.
    - Complete assertion harness `./scripts/phase35-voice-telemetry-assert.sh` passes all 6 sections with `FAIL=0 FINDINGS=0`.
    - `./arch/dots-hyprland.sh verify --strict` reports 0 findings.
  </acceptance_criteria>
  <done>
    Full telemetry architecture is implemented and validated: live duration counting, reload recovery, transcription freeze, and TTS voice metadata extraction pass all automated checks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External processes → $XDG_RUNTIME_DIR/voice-stt/ | Untrusted or corrupt runtime state files written to local user tmpfs |
| Linux OS Kernel → Procfs (/proc/<pid>/) | Virtual filesystem process state and command line buffers |
| Quickshell Engine → execDetached Subprocess | Shell command execution for stale PID cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01 | Tampering / DoS | Voice.qml (FileView recorder.pid / tts.pid) | medium | mitigate | Sanitize parsed PID with `parseInt(parts[0])`, enforce `pid > 0`, and reject non-numeric strings with fail-soft fallback to `idle` state. |
| T-35-02 | Tampering | Voice.qml (Process Liveness) | medium | mitigate | Verify `/proc/<pid>/cmdline` contains `"voice"` or `"voicemode"` before trusting process liveness; never rely on bare PID existence to prevent PID recycling hijacking. |
| T-35-03 | Elevation of Privilege | Voice.qml (Quickshell.execDetached) | high | mitigate | Strictly pass command arguments as array literals `["rm", "-f", path]`; never invoke `bash -c` or pass concatenated shell strings. |
| T-35-04 | Denial of Service | Voice.qml (Polling Loop) | medium | mitigate | Use native C++ `Quickshell.Io.FileView` with `blockLoading: true` and `printErrors: false` over Linux RAM tmpfs; dynamic QML Timer throttles to 500ms on idle without child process forks. |
</threat_model>

<verification>
Automated multi-section test suite in `scripts/phase35-voice-telemetry-assert.sh`:
- Section 1: Service presence, restow symlink without directory folding, pragma Singleton, FileView configuration, and adaptive timer intervals (TELEM-01).
- Section 2: Speech lifecycle states (`idle`, `starting`, `recording`, `transcribing`, `typing`, `speaking`), precedence hierarchy, and visual linger timer (TELEM-02).
- Section 3: Process liveness validation against `/proc/<pid>/cmdline`, automated purge of stale locks, and console warning logging (TELEM-03).
- Section 4: Live wall-clock duration counting, freeze during transcription, idle reset, and reload recovery anchor (TELEM-04).
- Section 5: Null-delimited TTS voice metadata extraction and fallback (TELEM-05).
- Section 6: Git working-tree invariance (`git status --porcelain`) and strict dotfiles verification (`arch/dots-hyprland.sh verify --strict` 0 findings).
</verification>

<success_criteria>
1. `Voice.qml` Singleton service is implemented in `restow/quickshell/.config/quickshell/ii/services/` and deployed to `~/.config/quickshell/ii/services/Voice.qml` as a leaf symlink.
2. `voice.py` explicitly emits `speaking` on TTS playback start and `typing` on STT text insertion.
3. State precedence strictly adheres to `recording` > `transcribing` > `speaking` > `typing` > `starting` > `idle`.
4. Stale or recycled PID locks are automatically purged from disk via `Quickshell.execDetached(["rm", "-f", ...])`.
5. Elapsed duration tracks live wall-clock time, freezes during transcription and typing, resets to `0:00` on idle, and recovers across Quickshell reloads.
6. TTS voice ID and backend are extracted from `/proc/<tts_pid>/cmdline`.
7. Full assertion test suite `./scripts/phase35-voice-telemetry-assert.sh` passes all 6 sections (`FAIL=0 FINDINGS=0`).
8. `./arch/dots-hyprland.sh verify --strict` reports 0 findings with zero git working-tree churn.
</success_criteria>

<output>
Create `.planning/phases/35-voice-telemetry-state-service-architecture/35-01-SUMMARY.md` when done
</output>
