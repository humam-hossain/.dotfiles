---
phase: 06-thin-setup-wrapper-safe-defaults
plan: 03
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - arch/dots-hyprland.sh
autonomous: true
requirements:
  - WRAP-01
  - WRAP-02
  - WRAP-03
  - WRAP-04
user_setup: []

must_haves:
  truths:
    - "Full WRAP-01..04 dry/help suite from research Validation Architecture is green without live install mutation"
    - "Wrapper help and install -h passthrough both work (wrapper docs defaults; setup shows upstream flags)"
    - "Preflight fail-closed when setup is non-executable, with +x restored after the test"
    - "Static script shape has no package-install arrays, no shell-eval of commands, no auto submodule init, no verify subcommand"
    - "Phase 6 success criteria 1–4 from ROADMAP are demonstrably true via dry/help only"
  artifacts:
    - path: "arch/dots-hyprland.sh"
      provides: "Verified thin wrapper ready for Phase 7 live install"
      contains: "Complete policy + greppable safe structure"
  key_links:
    - from: "smoke asserts"
      to: "arch/dots-hyprland.sh --dry-run / -h paths"
      via: "06-VALIDATION full suite"
    - from: "install -h"
      to: "vendor/dots-hyprland/./setup install -h"
      via: "passthrough after preflight (non-mutating)"
  prohibitions:
    - "Do not run real install|install-deps|install-setups|install-files without --dry-run"
    - "Do not leave setup non-executable after preflight negative test (always restore +x)"
    - "Do not add verify subcommand"
    - "Do not auto-init submodules as a 'fix' during smoke"
    - "Do not expand scope into Phase 7 session hooks"
---

<objective>
Prove WRAP-01..04 end-to-end with non-mutating smoke against the finished wrapper and submodule `./setup -h`, including preflight fail-closed and static anti-pattern gates (per D-14, D-16, D-17 and ROADMAP success criteria).

Purpose: Close Phase 6 with executable evidence that the operator entrypoint is safe to use in Phase 7 for real installs.

Output: Green full dry/help suite; any small wrapper fixes required for suite green; 06-03-SUMMARY recording results.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-CONTEXT.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-RESEARCH.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-VALIDATION.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-01-SUMMARY.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-02-SUMMARY.md
@arch/dots-hyprland.sh
@vendor/dots-hyprland/setup
</context>

## Phase Goal

Provide a `.dotfiles`-native entrypoint that drives upstream setup without destroying personal Hyprland config. This plan smoke-tests help/dry paths only.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Verified wrapper | `arch/dots-hyprland.sh` | 06-01..**06-03** |
| Smoke evidence | `06-03-SUMMARY.md` (+ optional temp logs under /tmp) | **06-03** |

## Tasks

<task type="auto">
  <name>Task 1: Full WRAP-01..04 dry/help suite + setup -h passthrough</name>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (06-02 complete)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-RESEARCH.md (Non-mutating smoke block; Validation Architecture table)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-VALIDATION.md (Per-Task map 06-01..06-03; full suite notes)
    - .planning/ROADMAP.md (Phase 6 success criteria 1–4)
  </read_first>
  <action>
    From REPO_ROOT, run the full non-mutating suite and fix only wrapper bugs that block green (no scope expansion):

    1. Syntax + help: `bash -n arch/dots-hyprland.sh`; bare `./arch/dots-hyprland.sh` exit 0 with install-files mentioned; `-h`/`--help` exit 0.
    2. Allowlist: `uninstall` (and optionally exp-update) exit non-zero with vendor/setup pointer.
    3. WRAP-02 install defaults: `printf 'yes\n' | ./arch/dots-hyprland.sh install --dry-run` must show `--core`, `--skip-hyprland`, `--skip-sysupdate`, and setup/install.
    4. WRAP-02 install-files defaults: same three flags on `install-files --dry-run` with yes pipe.
    5. WRAP-02 no defaults on deps: `install-deps --dry-run` mentions setup; must not show injected `--skip-hyprland`.
    6. WRAP-03 gate messaging + abort: yes path shows ii-original-dots-backup + quickshell + skip-hyprland; `printf 'no\n' | … install --dry-run` exits non-zero.
    7. WRAP-03 skip-backup: bare refuse; with `--allow-skip-backup` forward skip-backup only.
    8. WRAP-04: `install --exp-files --dry-run` (with yes) shows extras after defaults; optional `install-deps -s --dry-run` shows user -s/--skip-sysupdate passthrough without implying full default set.
    9. Subcommand help passthrough (D-03): `./arch/dots-hyprland.sh install -h` reaches upstream help (grep a known install flag such as `--skip-hyprland` from setup help) without backup gate blocking; non-mutating.
    10. Static WRAP-01 thinness: script must not contain package-manager bulk install invocations or PACKAGES=( illogical arrays). Confirm no verify subcommand branch (D-17).
    11. If any assert fails, patch `arch/dots-hyprland.sh` minimally to match locked decisions — do not weaken D-XX.
    12. Never run install paths without `--dry-run` except `install -h` / help.
  </action>
  <verify>
    <automated>bash -n arch/dots-hyprland.sh && ./arch/dots-hyprland.sh >/tmp/w6-03-help.txt; test $? -eq 0; grep -E 'install-deps|install-setups|install-files' /tmp/w6-03-help.txt && ./arch/dots-hyprland.sh uninstall; test $? -ne 0 && printf 'yes\n' | ./arch/dots-hyprland.sh install --dry-run | tee /tmp/w6-03-i.txt | grep -q -- '--core' && grep -q -- '--skip-hyprland' /tmp/w6-03-i.txt && grep -q -- '--skip-sysupdate' /tmp/w6-03-i.txt && printf 'yes\n' | ./arch/dots-hyprland.sh install-files --dry-run | grep -q -- '--skip-hyprland' && out=$(./arch/dots-hyprland.sh install-deps --dry-run); echo "$out" | grep -q setup; ! echo "$out" | grep -q -- '--skip-hyprland' && printf 'yes\n' | ./arch/dots-hyprland.sh install --dry-run 2>&1 | tee /tmp/w6-03-gate.txt | grep -q 'ii-original-dots-backup'; grep -qi 'quickshell' /tmp/w6-03-gate.txt; grep -q 'skip-hyprland' /tmp/w6-03-gate.txt && printf 'no\n' | ./arch/dots-hyprland.sh install --dry-run; test $? -ne 0 && ./arch/dots-hyprland.sh install --skip-backup --dry-run; test $? -ne 0 && printf 'yes\n' | ./arch/dots-hyprland.sh install --skip-backup --allow-skip-backup --dry-run | tee /tmp/w6-03-sb.txt | grep -q -- '--skip-backup'; ! grep -q -- '--allow-skip-backup' /tmp/w6-03-sb.txt && printf 'yes\n' | ./arch/dots-hyprland.sh install --exp-files --dry-run | grep -E -- '--core.*--exp-files|--skip-hyprland.*--exp-files' && ./arch/dots-hyprland.sh install -h 2>&1 | grep -q -- '--skip-hyprland'</automated>
  </verify>
  <acceptance_criteria>
    - Every automated command in the verify block exits successfully as composed (full suite green).
    - No live deps/files install mutated the machine as part of this task.
    - `install -h` shows upstream flag documentation including --skip-hyprland.
    - ROADMAP criteria 1–4 are satisfiable from these dry/help observations (wrapper calls setup; defaults include core+skip-hyprland; backup gate present; extra flags passthrough).
  </acceptance_criteria>
  <done>Full WRAP dry/help suite green including install -h passthrough.</done>
</task>

<task type="auto">
  <name>Task 2: Preflight fail-closed + static anti-pattern greps</name>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - arch/dots-hyprland.sh
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-CONTEXT.md (D-14, D-15, D-16, D-17)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-RESEARCH.md (Pattern 4; Pitfall 5–6, 8; Validation preflight row)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-VALIDATION.md (06-03-T2; Security Domain)
  </read_first>
  <action>
    Close remaining D-14/D-16 structural proofs:

    1. **Preflight negative test (with restore):** In a subshell or with an explicit trap, `chmod -x vendor/dots-hyprland/setup`, run `./arch/dots-hyprland.sh install-deps --dry-run`, capture non-zero exit, then **always** `chmod +x vendor/dots-hyprland/setup` even on failure (use `trap 'chmod +x vendor/dots-hyprland/setup' EXIT` or equivalent). Confirm fail output uses `[FAIL]` and does not auto-run submodule init.
    2. **Static greps on arch/dots-hyprland.sh** (fix any accidental introductions):
       - No `PACKAGES=(` install-array pattern for ii packages
       - No shell-eval of command strings (prefer proving array exec remains; avoid introducing an eval-based runner)
       - No automatic `git submodule update` / `git submodule init` invocation as executable repair (print-only strings in echo messages are required by D-15 and OK)
       - No verify subcommand / `verify)` dispatcher branch (D-17)
       - SAFE_DEFAULTS still lists full `--skip-hyprland` and does not use entry-only skip as the default profile
    3. Re-run `bash -n` and a quick `install-deps --dry-run` after restoring +x to confirm pin still usable.
    4. Optional discretionary: if a one-shot `scripts/phase06-wrapper-smoke.sh` would help future phases, only add it if it stays non-mutating and is not required — prefer keeping asserts in SUMMARY. Default: no new script unless useful and tiny.
    5. Record suite results in the plan SUMMARY; do not start Phase 7 live install.
  </action>
  <verify>
    <automated>bash -n arch/dots-hyprland.sh && ( trap 'chmod +x vendor/dots-hyprland/setup' EXIT; chmod -x vendor/dots-hyprland/setup; ./arch/dots-hyprland.sh install-deps --dry-run; ec=$?; exit $ec ); test $? -ne 0 && test -x vendor/dots-hyprland/setup && ./arch/dots-hyprland.sh install-deps --dry-run | grep -q setup && ! grep -E '^\s*PACKAGES=\(' arch/dots-hyprland.sh && ! grep -vE '^\s*#|echo |printf ' arch/dots-hyprland.sh | grep -E 'git submodule (update|init)' && ! grep -E '^\s*verify\)|verify[[:space:]]+subcommand' arch/dots-hyprland.sh && grep -q -- '--skip-hyprland' arch/dots-hyprland.sh && ! grep -q 'SAFE_DEFAULTS=.*skip-hyprland-entry' arch/dots-hyprland.sh</automated>
  </verify>
  <acceptance_criteria>
    - With setup temporarily non-executable, wrapper install-deps --dry-run exits non-zero.
    - After the test, `test -x vendor/dots-hyprland/setup` is true (restored).
    - Script has no top-level PACKAGES=( array for package install.
    - Non-comment/non-echo lines do not invoke git submodule update/init as a command.
    - No verify subcommand branch.
    - SAFE_DEFAULTS uses full --skip-hyprland protection, not entry-only as the default profile value.
    - install-deps --dry-run works again after restore.
  </acceptance_criteria>
  <done>Preflight fail-closed proven with restore; static anti-patterns clean; Phase 6 wrapper ready for Phase 7.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke harness → machine state | Must not mutate packages or XDG configs |
| Smoke harness → vendor setup mode bits | chmod -x test must restore +x |
| Final wrapper → Phase 7 operators | Verified policy surface only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | allowlist | high | mitigate | Suite asserts uninstall refused |
| T-06-02 | Tampering | skip-backup | high | mitigate | Suite asserts refuse + dual-key allow |
| T-06-03 | Tampering | hypr defaults | high | mitigate | Suite asserts --skip-hyprland on install/install-files only |
| T-06-04 | Tampering | exec style | high | mitigate | Static review: array exec retained; no eval runner |
| T-06-05 | Tampering | preflight / git | medium | mitigate | Negative chmod test; no auto submodule init |
| T-06-06 | Information Disclosure | logs | low | mitigate | Smoke logs are flag/path only |
| T-06-SC | Tampering | package installs | low | accept | Still no registry packages this phase |
</threat_model>

<verification>
Full 06-VALIDATION suite + preflight trap restore + static greps all green; zero live install mutations.
</verification>

<success_criteria>
Phase 6 ROADMAP success criteria 1–4 proven via help/dry only; wrapper is Phase-7-ready.
</success_criteria>

<output>
Create `.planning/phases/06-thin-setup-wrapper-safe-defaults/06-03-SUMMARY.md` when done
</output>
