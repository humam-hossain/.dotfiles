---
phase: 06-thin-setup-wrapper-safe-defaults
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - arch/dots-hyprland.sh
autonomous: true
requirements:
  - WRAP-01
user_setup: []

must_haves:
  truths:
    - "arch/dots-hyprland.sh exists, is executable, and passes bash -n"
    - "Bare invocation and help|-h|--help print wrapper help and exit 0 without calling ./setup"
    - "Only install|install-deps|install-setups|install-files are accepted; uninstall and other upstream subcommands exit non-zero with a pointer to vendor/dots-hyprland/./setup"
    - "Preflight requires vendor/dots-hyprland/.git and executable setup; on failure prints stock git fix commands and never auto-inits the submodule"
    - "Wrapper-owned --dry-run strips before any setup call and prints the would-exec argv then exits 0"
    - "Allowlisted non-dry paths build argv as bash arrays and exec ./setup from II_ROOT (no package arrays, no shell-eval of command strings)"
  artifacts:
    - path: "arch/dots-hyprland.sh"
      provides: "Thin WRAP-01 entrypoint scaffold (allowlist, help, preflight, dry-run exec)"
      contains: "REPO_ROOT, II_ROOT, SETUP, ALLOWLIST, SAFE_DEFAULTS constant, usage, preflight, main dispatcher"
  key_links:
    - from: "arch/dots-hyprland.sh"
      to: "vendor/dots-hyprland/setup"
      via: "cd II_ROOT && array-exec ./setup <subcmd> <user flags…>"
    - from: "preflight()"
      to: "vendor/dots-hyprland/.git + setup +x"
      via: "fail-closed checks; print git submodule update --init --recursive only"
  prohibitions:
    - "Do not reimplement sdata package lists, pacman/yay install arrays, or rsync of ii configs"
    - "Do not auto-run git submodule update / init inside the wrapper"
    - "Do not accept uninstall, exp-update, exp-merge, virtmon, checkdeps, resetfirstrun, or a verify subcommand"
    - "Do not call ./setup with empty args on bare wrapper invocation"
    - "Do not live-run install|install-deps|install-setups|install-files without --dry-run in this plan"
    - "Do not edit arch/quickshell.sh or any vendor/dots-hyprland content"
    - "Do not enable set -x; use labeled echos only"
---

<objective>
Create `arch/dots-hyprland.sh` as the WRAP-01 thin entrypoint: REPO_ROOT-relative paths, four-subcommand allowlist, wrapper help, preflight, wrapper-owned `--dry-run`, and array exec of vendored `./setup` (per D-01, D-02, D-03, D-04, D-14, D-15, D-16, D-17).

Purpose: Establish the `.dotfiles`-native call path into Phase 5’s pin without reimplementing install logic. Safe-default injection and backup gate land in plan 06-02.

Output: Executable `arch/dots-hyprland.sh` that help/allowlist/preflight/dry-run smoke without mutating the machine.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-CONTEXT.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-RESEARCH.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-PATTERNS.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-VALIDATION.md
@.planning/phases/05-fork-submodule-pin/05-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@arch/quickshell.sh
@vendor/dots-hyprland/setup
</context>

## Phase Goal

Provide a `.dotfiles`-native entrypoint that drives upstream setup without destroying personal Hyprland config. This plan only scaffolds the allowlisted dispatcher and non-mutating exec path.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Thin setup wrapper | `arch/dots-hyprland.sh` | **06-01** (scaffold); completed by 06-02 |
| Safe dual-run defaults + backup gate | same file | 06-02 |
| Full WRAP smoke proof | plan SUMMARY + green asserts | 06-03 |

## Tasks

<task type="auto">
  <name>Task 1: Scaffold arch/dots-hyprland.sh (REPO_ROOT, help, allowlist)</name>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-CONTEXT.md (D-01..D-04, D-17)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-RESEARCH.md (Pattern 1 scaffold; Code Examples dispatcher skeleton; Don't Hand-Roll)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-PATTERNS.md (Imports/bootstrap; labeled echos; main dispatcher)
    - arch/quickshell.sh (structure only — REPO_ROOT, set -euo pipefail, main "$@"; do not copy PACKAGES or yay)
    - .planning/codebase/CONVENTIONS.md (structured generation; no set -x on structured scripts)
  </read_first>
  <action>
    Create new executable file `arch/dots-hyprland.sh` (chmod +x). Mirror structured arch style from quickshell.sh with these locked choices:

    1. Header: `#!/usr/bin/env bash`, `set -euo pipefail` (no xtrace). Comment block states pattern = arch/quickshell.sh structured generation; divergence = no package arrays; all install logic delegated to vendor setup.
    2. Constants (per D-01, D-06 constant ready for 06-02):
       - `REPO_ROOT` via `BASH_SOURCE` parent of `arch/`
       - `II_ROOT="$REPO_ROOT/vendor/dots-hyprland"`
       - `SETUP="$II_ROOT/setup"`
       - `SAFE_DEFAULTS=(--core --skip-hyprland --skip-sysupdate)` defined now (injection wired in 06-02)
       - `ALLOWLIST=(install install-deps install-setups install-files)`
    3. `usage()` here-doc (or equivalent) documents: four allowlisted subcommands; safe-default list and which subcommands will get them; backup gate overview; that bare `--skip-backup` needs `--allow-skip-backup`; examples for install / install-deps / install-files with extra flags; pointer that non-allowlisted ops use `vendor/dots-hyprland/./setup` directly. Do not document a verify subcommand (D-17). Do not encourage skipping backup on first adoption.
    4. `is_allowlisted` helper over ALLOWLIST.
    5. `main` start: if no args, or first arg is `help` / `-h` / `--help` → call usage, exit 0 (D-02, D-03). Never invoke setup with empty args.
    6. If first arg not allowlisted → print `[FAIL]` naming the unknown subcommand, point operator at `vendor/dots-hyprland/./setup`, exit non-zero (D-04). Explicitly refuse paths like uninstall / exp-update / exp-merge via this allowlist (do not special-case a longer deny list beyond allowlist).
    7. Do not implement backup_gate or SAFE_DEFAULTS injection yet (plan 06-02). Stub remaining main body with a clear placeholder comment that Task 2 / plan 02 complete the path — or leave after allowlist shift ready for Task 2 in the same plan.
    8. Do not edit vendor tree or arch/quickshell.sh. Do not add package-manager install arrays.
  </action>
  <verify>
    <automated>test -x arch/dots-hyprland.sh && bash -n arch/dots-hyprland.sh && ./arch/dots-hyprland.sh >/tmp/w6-01-help.txt; test $? -eq 0; grep -E 'install-deps|install-setups|install-files' /tmp/w6-01-help.txt && ./arch/dots-hyprland.sh uninstall; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `test -x arch/dots-hyprland.sh` succeeds.
    - `bash -n arch/dots-hyprland.sh` exits 0.
    - `./arch/dots-hyprland.sh` exits 0 and help text mentions install-deps, install-setups, and install-files.
    - `./arch/dots-hyprland.sh -h` and `./arch/dots-hyprland.sh --help` exit 0.
    - `./arch/dots-hyprland.sh uninstall` exits non-zero; stderr/stdout mentions vendor/dots-hyprland or ./setup.
    - File has no PACKAGES array and no yay/pacman install invocation lines.
    - File has no `verify)` case / verify subcommand dispatcher branch.
    - SAFE_DEFAULTS array literal includes --core, --skip-hyprland, and --skip-sysupdate (even if not yet injected).
  </acceptance_criteria>
  <done>Executable wrapper scaffold prints help on bare invoke and refuses non-allowlisted subcommands.</done>
</task>

<task type="auto">
  <name>Task 2: Preflight, meta --dry-run strip, array exec of ./setup</name>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (Task 1 output)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-CONTEXT.md (D-09, D-10, D-14, D-15, D-16)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-RESEARCH.md (Pattern 2 argv arrays; Pattern 4 preflight; control flow steps 1–4 and 7–9; wrapper-owned flags table; Pitfall 1 meta forward; Pitfall 8 stringy argv)
    - vendor/dots-hyprland/setup (router only — confirm call target name is setup)
  </read_first>
  <action>
    Complete the WRAP-01 exec path in `arch/dots-hyprland.sh` (still without safe-default injection or backup gate — those are 06-02):

    1. After allowlisted `subcmd=$1; shift`, scan remaining `"$@"` into a bash array of user flags:
       - Detect and strip wrapper-owned `--dry-run` (set a dry_run flag; do not forward to setup — setup would reject unknown long opts).
       - Detect and strip wrapper-owned `--allow-skip-backup` (store allow_skip_backup for 06-02 policy; do not forward).
       - Preserve all other tokens in order (including `--skip-backup`, `-h`, `--help`, `--exp-files`, etc.) for WRAP-04 passthrough. Do not auto-strip user duplicates of safe defaults.
    2. Implement `preflight()` (D-14, D-15): fail if `$II_ROOT/.git` missing; fail if `$SETUP` not executable; on failure print `[FAIL]` plus stock fix text `git submodule update --init --recursive` (and chmod hint for setup). Never run submodule update/init from the wrapper.
    3. Call preflight for any path that will invoke setup (including future `install -h` passthrough). Bare wrapper help already returned before this.
    4. Build `cmd=(./setup "$subcmd")` then append user flags array only (no SAFE_DEFAULTS append yet). Log with labeled echos: at least `[INSTALL]` (or `[CONFIG]`) showing the full display line and cwd=`$II_ROOT` (D-10 partial — full defaults log completes in 06-02).
    5. If dry_run: print would-exec line (must include `./setup` and the subcommand), exit 0 — do not chdir-exec setup.
    6. Else: `( cd "$II_ROOT" && "${cmd[@]}" )` — array expansion only; never build a string and pass it through a shell-eval path; never source sdata step scripts from the wrapper.
    7. Do not inject SAFE_DEFAULTS yet; do not implement backup_gate yet; do not live-run mutating install in verification (use --dry-run only).
    8. Skip-backup refuse policy is plan 06-02; for now stripping `--allow-skip-backup` is enough so accidental forward cannot break setup getopt.
  </action>
  <verify>
    <automated>bash -n arch/dots-hyprland.sh && ./arch/dots-hyprland.sh install-deps --dry-run | tee /tmp/w6-01-deps.txt | grep -E './setup|setup' | grep -q install-deps && ! grep -q -- '--allow-skip-backup' /tmp/w6-01-deps.txt && ./arch/dots-hyprland.sh exp-merge --dry-run; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `./arch/dots-hyprland.sh install-deps --dry-run` exits 0, output mentions setup and install-deps, and does not invoke a real deps install (no pacman transaction from this command).
    - Dry-run output does not contain the literal wrapper meta flag allow-skip-backup as a forwarded token.
    - `./arch/dots-hyprland.sh exp-merge` (with or without --dry-run) exits non-zero (not allowlisted).
    - `preflight` function exists and checks both `.git` presence under vendor/dots-hyprland and setup executability.
    - Script body never runs `git submodule update` as an automatic repair action (print-only is OK).
    - Exec path uses a bash array for the setup command (no shell-eval of a concatenated command string).
    - `bash -n arch/dots-hyprland.sh` still exits 0.
  </acceptance_criteria>
  <done>Allowlisted subcommands dry-run to a logged ./setup argv via preflight-gated array exec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator argv → wrapper | Untrusted subcommand/flags; must allowlist and strip wrapper meta |
| Wrapper → vendor `./setup` | Privileged install SoT; only array-exec after preflight |
| Wrapper → git submodule state | Read-only preflight; must not mutate pin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | subcommand dispatch | high | mitigate | Allowlist only install\|install-deps\|install-setups\|install-files (D-04); refuse uninstall/exp-* |
| T-06-04 | Tampering | flag/argv handling | high | mitigate | Bash array exec of ./setup; never shell-eval concatenated commands; strip unknown-to-setup meta flags |
| T-06-05 | Tampering | preflight | medium | mitigate | Require .git + setup +x; print stock git fix only; never auto submodule init (D-15) |
| T-06-06 | Information Disclosure | logging | low | mitigate | Log flags/paths/labels only; no secrets; no set -x |
| T-06-SC | Tampering | package installs | low | accept | No npm/pip/cargo this phase (Package Legitimacy N/A) |
</threat_model>

<verification>
From REPO_ROOT: bash -n + bare help exit 0 + uninstall refused + install-deps --dry-run shows ./setup install-deps without machine mutation.
</verification>

<success_criteria>
WRAP-01 scaffold complete: operator has an executable arch/dots-hyprland.sh that allowlists four subcommands, prefights the pin, and dry-runs array-exec of vendor setup.
</success_criteria>

<output>
Create `.planning/phases/06-thin-setup-wrapper-safe-defaults/06-01-SUMMARY.md` when done
</output>
