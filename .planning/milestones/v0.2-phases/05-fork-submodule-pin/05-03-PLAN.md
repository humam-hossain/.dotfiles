---
phase: 05-fork-submodule-pin
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - .gitmodules
  - vendor/dots-hyprland
autonomous: true
requirements:
  - OWN-01
  - OWN-02
  - OWN-03
user_setup: []

must_haves:
  truths:
    - "git remote -v inside vendor/dots-hyprland shows origin→fork SSH and upstream→end-4 HTTPS (OWN-01)"
    - "Parent HEAD records vendor/dots-hyprland as mode-160000 gitlink with a concrete 40-hex pin SHA (OWN-02)"
    - ".gitmodules is in the same parent commit as the gitlink (D-11) — never only one of the two"
    - "Pinned SHA is exact: git rev-parse vendor/dots-hyprland HEAD matches gitlink SHA (precision / no floating pin)"
    - "git submodule update --init --recursive yields complete tree including shapes LICENSE (OWN-03)"
    - "Idempotent re-run of full OWN checklist exits 0 without mutating pin SHA"
    - "Interrupt/re-run safety: recursive update + path-scoped commit can be re-attempted without sibling conversion"
    - "Pin commit is path-scoped: only .gitmodules + vendor/dots-hyprland (no unrelated dirty QML)"
    - "Phase gate D-12 full OWN-01/02/03 checklist passes"
  artifacts:
    - "Parent git commit containing only .gitmodules + vendor/dots-hyprland gitlink"
    - "git ls-tree HEAD vendor/dots-hyprland → 160000 commit <sha>"
    - "vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/shapes/LICENSE"
  key_links:
    - "Parent commit tree entry (160000) → exact submodule commit content"
    - "Submodule remotes (local) → OWN-01 operator checklist"
    - "Recursive init → nested shapes LICENSE"
  prohibitions:
    - "Do not run ./setup or any install (D-16)"
    - "Do not create arch/dots-hyprland.sh or clone bootstrap helpers (D-15/D-16)"
    - "Do not git add -A or stage .config/quickshell QML dirt with the pin"
    - "Do not commit only .gitmodules without the gitlink (or vice versa)"
    - "Do not add branch auto-track to .gitmodules"
    - "Do not change submodule URL to end-4"
    - "Do not rewrite nested shapes away from end-4/rounded-polygon-qmljs"
    - "Do not delete or rewire ~/github_repo/dots-hyprland"
    - "Do not put tokens or private keys in commit message or logs"
    - "Push to origin is optional — not required for OWN success"
---

<objective>
Run the full OWN-01/02/03 verification checklist and create the path-scoped parent pin commit that records `.gitmodules` and the `vendor/dots-hyprland` gitlink together.

Purpose: Make the pin reproducible for future clones and close Phase 5 only when D-12 checklist is green (D-11, D-12).

Output: One parent commit `chore: pin vendor/dots-hyprland submodule` (or equivalent chore: style) with mode-160000 gitlink proof; all OWN checks green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-fork-submodule-pin/05-CONTEXT.md
@.planning/phases/05-fork-submodule-pin/05-RESEARCH.md
@.planning/phases/05-fork-submodule-pin/05-PATTERNS.md
@.planning/phases/05-fork-submodule-pin/05-VALIDATION.md
@.planning/phases/05-fork-submodule-pin/05-02-SUMMARY.md
@.planning/research/PITFALLS.md
</context>

## Phase Goal

Own and pin dots-hyprland inside `.dotfiles` before any install mutates the machine. This plan verifies and commits the pin.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Personal fork (GitHub) | `humam-hossain/dots-hyprland` | 05-01 |
| Parent `.gitmodules` | `.gitmodules` | 05-02 |
| Outer submodule checkout | `vendor/dots-hyprland/` | 05-02 |
| Nested shapes tree | `…/shapes/` + LICENSE | 05-02 |
| Dual remotes (local) | origin + upstream in vendor | 05-02 |
| Parent pin commit | `HEAD` gitlink 160000 + `.gitmodules` | **05-03** |

## Tasks

<task type="auto">
  <name>Task 1: Full OWN-01 / OWN-02 pre-commit / OWN-03 checklist</name>
  <files></files>
  <read_first>
    - .planning/phases/05-fork-submodule-pin/05-CONTEXT.md (D-12 success checklist)
    - .planning/phases/05-fork-submodule-pin/05-RESEARCH.md (OWN verification smoke sequence)
    - .planning/phases/05-fork-submodule-pin/05-VALIDATION.md (per-task map; no setup gates)
    - .planning/phases/05-fork-submodule-pin/05-02-SUMMARY.md
  </read_first>
  <action>
    From REPO_ROOT, run inline verification only (no new clone-bootstrap script — D-15). Use labeled echos `[VERIFY]` / `[DONE]` if printing status (CONVENTIONS style). Prefer no xtrace.

    **OWN-01 remotes (inside vendor):**
    - `git -C vendor/dots-hyprland remote get-url origin` must equal `git@github.com:humam-hossain/dots-hyprland.git`
    - `git -C vendor/dots-hyprland remote get-url upstream` must equal `https://github.com/end-4/dots-hyprland.git`
    - If upstream missing (edge: interrupted 05-02), re-add with the exact HTTPS URL then re-check — do not touch sibling.

    **OWN-02 registration (pre-commit surface):**
    - `.gitmodules` exists; url for `submodule.vendor/dots-hyprland` is fork SSH URL
    - No auto-tracking branch assignment line in `.gitmodules` (`grep -E '^\s*branch\s*='` must find nothing)
    - `git submodule status vendor/dots-hyprland` shows a 40-hex SHA (exact pin candidate)

    **OWN-03 nested:**
    - `git submodule update --init --recursive` (idempotent re-run)
    - `test -f vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/shapes/LICENSE`
    - `! git -C vendor/dots-hyprland submodule status --recursive | grep -E '^-'`
    - Nested URL still references end-4 rounded-polygon shapes host

    **Boundary / precision / concurrency edges (spec-less probes):**
    - Precision: capture `PIN_SHA=$(git -C vendor/dots-hyprland rev-parse HEAD)` and require it matches the SHA in `git submodule status` (strip status prefix characters).
    - Idempotency: re-run recursive update; PIN_SHA unchanged; LICENSE still present.
    - Concurrency/interrupt: if status shows incomplete nested (`-` prefix), only re-run recursive update — never convert sibling.

    **Prohibitions check:** confirm no `arch/dots-hyprland.sh` was added; do not execute setup; sibling path still present/untouched if it was.

    Do not commit in this task — staging/commit is Task 2 (D-11).
  </action>
  <verify>
    <automated>SM=vendor/dots-hyprland; SHAPES="$SM/dots/.config/quickshell/ii/modules/common/widgets/shapes"; git -C "$SM" remote get-url origin | grep -q 'git@github.com:humam-hossain/dots-hyprland.git' && git -C "$SM" remote get-url upstream | grep -q 'https://github.com/end-4/dots-hyprland.git' && test -f .gitmodules && git config -f .gitmodules --get submodule.vendor/dots-hyprland.url | grep -q 'git@github.com:humam-hossain/dots-hyprland.git' && ! grep -E '^\s*branch\s*=' .gitmodules && git submodule update --init --recursive && test -f "$SHAPES/LICENSE" && ! git -C "$SM" submodule status --recursive | grep -E '^-' && PIN=$(git -C "$SM" rev-parse HEAD) && git submodule status "$SM" | grep -q "$PIN"</automated>
  </verify>
  <acceptance_criteria>
    - OWN-01: origin SSH fork URL and upstream HTTPS end-4 URL both correct inside vendor.
    - OWN-02 pre-commit: `.gitmodules` url is fork SSH; no branch auto-track line; submodule status has 40-hex SHA.
    - OWN-03: shapes LICENSE exists; recursive status has no uninitialized `-` lines.
    - PIN_SHA from `rev-parse HEAD` matches `git submodule status` SHA (exact pin contract).
    - Second recursive update leaves PIN_SHA and LICENSE unchanged.
    - No setup executed; no wrapper script; sibling left alone.
  </acceptance_criteria>
  <done>Full pre-commit OWN checklist green; pin SHA captured and consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Path-scoped parent pin commit and post-commit gitlink proof</name>
  <files>.gitmodules, vendor/dots-hyprland</files>
  <read_first>
    - .planning/phases/05-fork-submodule-pin/05-CONTEXT.md (D-11, D-12)
    - .planning/phases/05-fork-submodule-pin/05-RESEARCH.md (Pattern 3 single parent pin commit; dirty-tree pitfall)
    - .planning/phases/05-fork-submodule-pin/05-PATTERNS.md (path-scoped staging; chore: message style)
  </read_first>
  <action>
    From REPO_ROOT:

    1. Stage **only** pin artifacts per D-11 and dirty-tree pitfall:
       `git add .gitmodules vendor/dots-hyprland`
       Never `git add -A`. Parent may have unrelated modified `.config/quickshell/*.qml` — leave them unstaged.
    2. Review index:
       - `git diff --cached --submodule` should show the submodule commit
       - `git diff --cached --stat` must list only `.gitmodules` and `vendor/dots-hyprland` (reject if any `.qml` or other paths appear — unstage extras)
    3. Commit with repo-style message (discretion + RESEARCH):
       `git commit -m "chore: pin vendor/dots-hyprland submodule"`
       Optional body may mention short SHA; gitlink remains authoritative (do not invent a separate SHA tracking file).
    4. Post-commit OWN-02 proof:
       - `git ls-tree HEAD vendor/dots-hyprland` must start with `160000 commit` and a 40-hex SHA
       - That SHA must equal `git -C vendor/dots-hyprland rev-parse HEAD`
       - Confirm the commit tree includes `.gitmodules` (`git show --name-only --pretty=format: HEAD` lists both paths)
    5. Re-run full OWN checklist once post-commit (D-12 phase done only when all three pass). Idempotent: second `git submodule update --init --recursive` still green.
    6. Push is optional — do not require `git push` for success (parent may already be ahead of origin).
    7. D-16: still no setup, no wrapper, no session hooks. D-15: no bootstrap helper script required.
  </action>
  <verify>
    <automated>git ls-tree HEAD vendor/dots-hyprland | grep -qE '^160000 commit [0-9a-f]{40}' && test -f .gitmodules && git rev-parse HEAD:.gitmodules >/dev/null && PIN=$(git -C vendor/dots-hyprland rev-parse HEAD) && git ls-tree HEAD vendor/dots-hyprland | grep -q "$PIN" && git -C vendor/dots-hyprland remote get-url origin | grep -q 'humam-hossain/dots-hyprland' && git -C vendor/dots-hyprland remote get-url upstream | grep -q 'end-4/dots-hyprland' && test -f vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/shapes/LICENSE && ! grep -E '^\s*branch\s*=' .gitmodules</automated>
  </verify>
  <acceptance_criteria>
    - Latest pin commit message is `chore: pin vendor/dots-hyprland submodule` (or documents equivalent chore: pin wording if amend needed once).
    - `git ls-tree HEAD vendor/dots-hyprland` matches `160000 commit <40-hex>`.
    - Gitlink SHA equals `git -C vendor/dots-hyprland rev-parse HEAD`.
    - The pin commit's changed paths are only `.gitmodules` and `vendor/dots-hyprland` (no QML).
    - OWN-01 remotes still correct post-commit.
    - OWN-03 shapes LICENSE still present post-commit.
    - `.gitmodules` still has no branch auto-track line.
    - Phase success criteria from ROADMAP all true:
      1. remotes origin→fork, upstream→end-4 inside vendor
      2. parent has vendor pin in `.gitmodules` + gitlink SHA
      3. recursive update yields complete shapes tree
  </acceptance_criteria>
  <done>Parent pin committed with mode-160000 gitlink; D-12 OWN-01/02/03 checklist fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Index / commit boundary | Only pin paths may cross into history; dirty QML must not |
| Parent gitlink → submodule content | 160000 SHA is the trust pin for future clones |
| Logs / commit messages | Must not carry credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Spoofing | Committed `.gitmodules` url | high | mitigate | Final verify url remains fork SSH only |
| T-05-02 | Tampering | Gitlink SHA precision | high | mitigate | Assert ls-tree SHA == submodule HEAD; path-scoped commit only |
| T-05-03 | Elevation of Privilege | Accidental setup during verify | high | mitigate | D-16; verification is git-only smoke |
| T-05-04 | Information Disclosure | Commit message / SUMMARY | medium | mitigate | No tokens/keys in commit body or logs |
| T-05-05 | Tampering | Nested shapes host | high | mitigate | Post-commit LICENSE + nested URL still end-4 shapes |
| T-05-06 | Tampering | Half pin (.gitmodules without gitlink) | high | mitigate | D-11 same commit; ls-tree + show name-only both paths |
| T-05-SC | Tampering | Package installs | low | accept | No package installs this phase |
</threat_model>

<verification>
Phase gate (all required):
1. OWN-01: `git -C vendor/dots-hyprland remote -v` origin fork SSH + upstream end-4 HTTPS
2. OWN-02: `.gitmodules` url + `git ls-tree HEAD vendor/dots-hyprland` → 160000
3. OWN-03: recursive update + shapes LICENSE + no `-` nested status
4. Pin commit path-scoped; no setup; sibling untouched
</verification>

<success_criteria>
- ROADMAP Phase 5 success criteria 1–3 all true
- D-11 and D-12 satisfied
- Requirements OWN-01, OWN-02, OWN-03 complete for this phase
- Machine not mutated by install (D-16)
</success_criteria>

<output>
Create `.planning/phases/05-fork-submodule-pin/05-03-SUMMARY.md` when done
</output>
