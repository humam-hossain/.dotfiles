---
phase: 05-fork-submodule-pin
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - .gitmodules
  - vendor/dots-hyprland
autonomous: true
requirements:
  - OWN-01
  - OWN-02
  - OWN-03
user_setup: []

must_haves:
  truths:
    - "Parent .gitmodules registers path vendor/dots-hyprland with url git@github.com:humam-hossain/dots-hyprland.git"
    - ".gitmodules has no auto-tracking branch key for the submodule (commit pin only per D-08)"
    - "vendor/dots-hyprland is a git submodule checkout (gitfile/gitlink), not a plain mv'd directory"
    - "Initial pin SHA equals fork default-branch tip at submodule-add time (D-09); no force-reset to upstream (D-10)"
    - "git submodule update --init --recursive materializes nested shapes with LICENSE present"
    - "Inside vendor: origin is SSH fork URL; upstream is HTTPS end-4 URL (D-04, D-07)"
    - "Nested shapes host remains end-4/rounded-polygon-qmljs (not rewritten)"
    - "Re-running recursive update is idempotent and still leaves shapes LICENSE present"
    - "Canonical local path is vendor/dots-hyprland only (D-13); sibling left alone (D-14)"
  artifacts:
    - ".gitmodules"
    - "vendor/dots-hyprland (submodule working tree + gitlink staged or stageable)"
    - "vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/shapes/LICENSE"
  key_links:
    - ".gitmodules url → git@github.com:humam-hossain/dots-hyprland.git"
    - "vendor origin → fork SSH; vendor upstream → https://github.com/end-4/dots-hyprland.git"
    - "Outer pin SHA → nested shapes pin recorded inside outer commit (recursive init)"
  prohibitions:
    - "Do not run ./setup or vendor/dots-hyprland/setup (D-16)"
    - "Do not create arch/dots-hyprland.sh"
    - "Do not git submodule add with end-4 URL as the outer submodule URL"
    - "Do not pass -b or --branch to git submodule add; do not git submodule set-branch"
    - "Do not mv ~/github_repo/dots-hyprland into vendor/"
    - "Do not seed from or rewrite the sibling clone"
    - "Do not commit the parent pin yet if only half the pair is staged — pin commit is 05-03 (D-11)"
    - "Do not embed secrets into vendor content or logs"
---

<objective>
Register `vendor/dots-hyprland` as a submodule of the personal fork, recursively initialize nested shapes, and configure dual remotes inside the vendored tree.

Purpose: Deliver OWN-02 registration surface and OWN-03 nested completeness, and finish OWN-01 remotes on the local canonical path (D-05, D-06, D-07).

Output: Staged-ready `.gitmodules` + submodule tree with shapes LICENSE and origin/upstream remotes; parent pin commit deferred to 05-03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-fork-submodule-pin/05-CONTEXT.md
@.planning/phases/05-fork-submodule-pin/05-RESEARCH.md
@.planning/phases/05-fork-submodule-pin/05-PATTERNS.md
@.planning/phases/05-fork-submodule-pin/05-01-SUMMARY.md
@.planning/research/PITFALLS.md
</context>

## Phase Goal

Own and pin dots-hyprland inside `.dotfiles` before any install mutates the machine. This plan materializes the submodule and nested tree.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Personal fork (GitHub) | `humam-hossain/dots-hyprland` | 05-01 |
| Parent `.gitmodules` | `.gitmodules` | **05-02** |
| Outer submodule checkout | `vendor/dots-hyprland/` | **05-02** |
| Nested shapes tree | `vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/shapes/` | **05-02** |
| Dual remotes (local config) | origin + upstream inside vendor | **05-02** |
| Parent pin commit | gitlink 160000 + `.gitmodules` | 05-03 |

## Tasks

<task type="auto">
  <name>Task 1: Add vendor/dots-hyprland submodule from fork SSH URL</name>
  <files>.gitmodules, vendor/dots-hyprland</files>
  <read_first>
    - .planning/phases/05-fork-submodule-pin/05-CONTEXT.md (D-05, D-08, D-09, D-10, D-13)
    - .planning/phases/05-fork-submodule-pin/05-RESEARCH.md (Pattern 1 submodule add; Pitfalls 3–5; expected .gitmodules shape)
    - .planning/phases/05-fork-submodule-pin/05-01-SUMMARY.md (confirm fork ready)
  </read_first>
  <action>
    From REPO_ROOT (`git rev-parse --show-toplevel` → `/home/pera/github_repo/.dotfiles`):

    1. Preflight: `test ! -e vendor/dots-hyprland`. If a failed partial add left debris, recover with stock git only: `git submodule deinit -f vendor/dots-hyprland` (if registered), `git rm -f vendor/dots-hyprland` (if indexed), `rm -rf .git/modules/vendor/dots-hyprland`, then re-check path free. Never `mv` the sibling into place (D-05, D-02).
    2. Register submodule with the exact fork SSH URL per D-05 — no end-4 URL, no local-path add:
       `git submodule add git@github.com:humam-hossain/dots-hyprland.git vendor/dots-hyprland`
       Do not pass `-b` or `--branch` (D-08 commit-pin only). Do not hand-edit `.gitmodules` after stock emit.
    3. Pin selection per D-09/D-10: accept whatever commit `submodule add` checked out from the fork default tip. Do not `git fetch upstream` + hard reset to force end-4 tip if the fork looks slightly behind.
    4. Assert `.gitmodules` contains submodule path `vendor/dots-hyprland` and url `git@github.com:humam-hossain/dots-hyprland.git`. Assert there is no auto-tracking branch assignment line written by add flags (D-08).
    5. Do not run setup (D-16). Do not create wrapper scripts. Do not commit yet (D-11 same-commit rule completed in 05-03 after full OWN verify).
  </action>
  <verify>
    <automated>test -f .gitmodules && git config -f .gitmodules --get submodule.vendor/dots-hyprland.url | grep -q 'git@github.com:humam-hossain/dots-hyprland.git' && ! grep -E '^\s*branch\s*=' .gitmodules && test -e vendor/dots-hyprland/.git && git submodule status vendor/dots-hyprland | grep -E '^[ +][0-9a-f]{40} vendor/dots-hyprland'</automated>
  </verify>
  <acceptance_criteria>
    - File `.gitmodules` exists at REPO_ROOT.
    - `git config -f .gitmodules --get submodule.vendor/dots-hyprland.url` equals `git@github.com:humam-hossain/dots-hyprland.git`.
    - `grep -E '^\s*branch\s*=' .gitmodules` exits non-zero (no matches).
    - `vendor/dots-hyprland` exists and contains a `.git` gitfile or directory (submodule, not plain copy).
    - `git submodule status vendor/dots-hyprland` shows a 40-hex SHA for the path (not a missing `-` uninitialized outer).
    - Outer submodule URL is the personal fork, not end-4.
    - Sibling `~/github_repo/dots-hyprland` was not moved or deleted.
  </acceptance_criteria>
  <done>Outer submodule registered at vendor/dots-hyprland against fork SSH URL with commit pin (no branch auto-track).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Recursive nested init, dual remotes, shapes gate</name>
  <files>vendor/dots-hyprland</files>
  <read_first>
    - .planning/phases/05-fork-submodule-pin/05-CONTEXT.md (D-04, D-06, D-07, D-13, D-16)
    - .planning/phases/05-fork-submodule-pin/05-RESEARCH.md (Pattern 2 recursive + dual remotes; OWN-03 LICENSE assert; Security nested host)
    - .planning/research/PITFALLS.md (Pitfall 5 nested shapes)
  </read_first>
  <action>
    From REPO_ROOT:

    1. Nested init per D-06: `git submodule update --init --recursive`. Do not use non-recursive-only update as the final state.
    2. OWN-03 presence gate — require file:
       `vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/shapes/LICENSE`
    3. Nested status: `git -C vendor/dots-hyprland submodule status --recursive` must not show a leading `-` on the shapes line (uninitialized). Idempotency edge: run `git submodule update --init --recursive` a second time; LICENSE must still exist (OWN-03 re-run safe).
    4. Nested host boundary: read nested registry inside the vendored tree (`vendor/dots-hyprland/.gitmodules` or equivalent submodule config) and confirm shapes URL still targets `end-4/rounded-polygon-qmljs` (HTTPS upstream shapes host). Do not rewrite nested URL to a personal fork this phase.
    5. Dual remotes inside vendor per D-04/D-07 (local submodule config — not written into parent `.gitmodules`):
       - Ensure origin: `git -C vendor/dots-hyprland remote set-url origin git@github.com:humam-hossain/dots-hyprland.git`
       - Ensure upstream (idempotent): if `git -C vendor/dots-hyprland remote get-url upstream` fails, then `git -C vendor/dots-hyprland remote add upstream https://github.com/end-4/dots-hyprland.git`; if it exists, set-url to that exact HTTPS URL.
    6. Print remotes with `git -C vendor/dots-hyprland remote -v` and confirm both origin (SSH fork) and upstream (HTTPS end-4).
    7. Canonical path only (D-13): do not point tooling at the sibling path. Leave sibling alone (D-14).
    8. D-16: do not execute `vendor/dots-hyprland/setup` or any install. Do not create `arch/dots-hyprland.sh`. Do not add a Phase 5 clone-bootstrap helper script (D-15 — stock git only).
  </action>
  <verify>
    <automated>git submodule update --init --recursive && test -f vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/shapes/LICENSE && ! git -C vendor/dots-hyprland submodule status --recursive | grep -E '^-' && git -C vendor/dots-hyprland remote get-url origin | grep -q 'git@github.com:humam-hossain/dots-hyprland.git' && git -C vendor/dots-hyprland remote get-url upstream | grep -q 'https://github.com/end-4/dots-hyprland.git' && git -C vendor/dots-hyprland config -f .gitmodules --get-regexp url | grep -q 'rounded-polygon-qmljs'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/shapes/LICENSE` is true.
    - `git -C vendor/dots-hyprland submodule status --recursive` has no line starting with `-`.
    - Second recursive update leaves LICENSE present (idempotent).
    - `git -C vendor/dots-hyprland remote get-url origin` is `git@github.com:humam-hossain/dots-hyprland.git`.
    - `git -C vendor/dots-hyprland remote get-url upstream` is `https://github.com/end-4/dots-hyprland.git`.
    - Nested shapes URL still references `rounded-polygon-qmljs` under end-4.
    - `setup` binary/script may exist under vendor but was not executed.
    - No `arch/dots-hyprland.sh` created.
  </acceptance_criteria>
  <done>Recursive shapes present; dual remotes configured on vendor; OWN-01 remotes + OWN-03 content green pending parent pin commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parent repo → submodule URL in `.gitmodules` | Superproject records who is trusted as vendor source |
| Submodule tree → nested shapes URL | Nested dependency must stay end-4 shapes host |
| Submodule working tree → machine session | setup must not cross into install (D-16) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Spoofing | `.gitmodules` url | high | mitigate | Locked URL only: `git@github.com:humam-hossain/dots-hyprland.git`; verify after add |
| T-05-02 | Tampering | Nested shapes host | high | mitigate | Assert nested URL remains end-4/rounded-polygon-qmljs; do not rewrite |
| T-05-03 | Elevation of Privilege | `vendor/dots-hyprland/setup` | high | mitigate | D-16 forbid execution; verify no install side effects |
| T-05-04 | Information Disclosure | Vendor tree / logs | medium | mitigate | Do not copy secrets into vendor; no xtrace on auth |
| T-05-05 | Tampering | Sibling conversion | high | mitigate | D-05 forbids mv/convert; only submodule add of fork URL |
| T-05-06 | Tampering | Auto-track branch key | medium | mitigate | No `-b` on add; negative grep for branch assignment in `.gitmodules` |
| T-05-SC | Tampering | Package installs | low | accept | No npm/pip/cargo this phase |
</threat_model>

<verification>
1. `.gitmodules` url = fork SSH; no branch auto-track line
2. `git submodule status` shows outer SHA for vendor/dots-hyprland
3. shapes LICENSE present after recursive update
4. origin/upstream remotes correct inside vendor
5. Nested shapes URL still end-4 rounded-polygon
6. No setup executed
</verification>

<success_criteria>
- OWN-02 registration present (url + submodule path; pin commit next plan)
- OWN-03 nested shapes initialized
- OWN-01 remotes true inside `vendor/dots-hyprland`
- D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-13, D-14, D-15, D-16 honored
</success_criteria>

<output>
Create `.planning/phases/05-fork-submodule-pin/05-02-SUMMARY.md` when done
</output>
