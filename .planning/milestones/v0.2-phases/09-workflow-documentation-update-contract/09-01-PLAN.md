---
phase: 09-workflow-documentation-update-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/dots-hyprland-workflow.md
autonomous: true
requirements:
  - DOC-01
user_setup: []

must_haves:
  truths:
    - "Canonical playbook docs/dots-hyprland-workflow.md exists and is the Install/Adopt SoT for DOC-01"
    - "Playbook documents clone of .dotfiles with recursive submodule init (clone --recurse-submodules and/or git submodule update --init --recursive)"
    - "Playbook documents verifying vendor/dots-hyprland remotes (origin=fork, upstream=end-4) and parent pin awareness"
    - "Playbook documents install via ./arch/dots-hyprland.sh with dry-run first, safe defaults (--core --skip-hyprland --skip-sysupdate), and interactive backup gate (type yes)"
    - "Playbook documents personal hypr hooks: env ILLOGICAL_IMPULSE_VIRTUAL_ENV and exec-once = qs -c ii"
    - "Playbook documents dual-run expectations: waybar remains; qs -c ii runs; overlap OK; no cutover required"
    - "Playbook does not instruct running arch/quickshell.sh as the current install path"
    - "Playbook does not teach bare --skip-backup as normal first-adoption practice"
  artifacts:
    - path: "docs/dots-hyprland-workflow.md"
      provides: "Operator install/adopt playbook (DOC-01)"
      contains: "Clone/submodule, wrapper install, hypr hooks, dual-run sections with copy-paste bash"
  key_links:
    - from: "docs/dots-hyprland-workflow.md"
      to: "arch/dots-hyprland.sh help"
      via: "Pointer for full flag/subcommand details (DRY)"
    - from: "docs/dots-hyprland-workflow.md"
      to: "vendor/dots-hyprland"
      via: "Canonical pin path only (Phase 5 D-13)"
  prohibitions:
    - "Must NOT document arch/quickshell.sh as a current installer step"
    - "Must NOT document re-symlinking ~/.config/quickshell into the repo product tree"
    - "Must NOT omit --recursive / --recurse-submodules from submodule init guidance"
    - "Must NOT present bare --skip-backup as the recommended first-adoption path"
    - "Must NOT create scripts/phase09-*-smoke.sh"
    - "Must NOT mutate live machine install solely to satisfy doc tasks"
---

<objective>
Write the Install/Adopt section of the operator playbook so a cold machine can go from clone → recursive submodule → wrapper install → hypr hooks → dual-run expectations without tribal knowledge (DOC-01).

Purpose: Close the documentation gap left after Phases 5–8 product work; root README currently has no desktop-shell workflow.

Output: `docs/dots-hyprland-workflow.md` with complete Install/Adopt content (Update section may be a stub heading reserved for 09-02).
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-workflow-documentation-update-contract/09-RESEARCH.md
@.planning/phases/09-workflow-documentation-update-contract/09-PATTERNS.md
@.planning/phases/09-workflow-documentation-update-contract/09-VALIDATION.md
@.planning/phases/05-fork-submodule-pin/05-CONTEXT.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-CONTEXT.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-CONTEXT.md
@.planning/phases/08-retire-local-quickshell-product/08-CONTEXT.md
@arch/dots-hyprland.sh
@arch/README.md
@README.md
@.gitmodules
</context>

## Phase Goal

**Operator can reinstall without tribal knowledge** — this plan covers the install/adopt half (DOC-01). Update/pin-bump is 09-02.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Canonical operator playbook (install/adopt) | `docs/dots-hyprland-workflow.md` | **09-01** |
| Update/pin-bump + non-goals sections | same playbook | 09-02 |
| README + PROJECT cross-links | `README.md`, `.planning/PROJECT.md` | 09-03 |

## Tasks

<task type="auto">
  <name>Task 1: Create playbook skeleton and prerequisites</name>
  <files>docs/dots-hyprland-workflow.md</files>
  <read_first>
    - .planning/phases/09-workflow-documentation-update-contract/09-RESEARCH.md (Primary recommendation, Recommended doc structure, Code Examples)
    - .planning/phases/09-workflow-documentation-update-contract/09-PATTERNS.md (playbook structure pattern)
    - arch/README.md (procedural bash-block style to match)
    - README.md (current discovery surface — do not dump full playbook here yet)
  </read_first>
  <action>
    Create directory `docs/` if missing. Create `docs/dots-hyprland-workflow.md` as the **canonical** operator playbook.

    Required top matter:
    - Title making clear this is the dots-hyprland / illogical-impulse workflow for `.dotfiles`
    - Short purpose: single product path after Phases 5–8; `arch/dots-hyprland.sh` is the only Arch install entry; in-repo Quickshell product and `arch/quickshell.sh` are retired
    - Prerequisites: Arch Linux primary; git + SSH to GitHub; `yay`/AUR as required by upstream setup; Hyprland session with personal conf ownership
    - Canonical path callout: only `vendor/dots-hyprland` (not a sibling clone as SoT)
    - Section outline headings reserved for later tasks: Clone &amp; submodule, Verify remotes/pin, Install via wrapper, Session hooks &amp; dual-run, Update (pin-bump) — stub OK, Non-goals — stub OK, See also

    Style: match `arch/README.md` — clear headings, fenced bash blocks, short warnings. Do not invent wrapper flags; point details to `./arch/dots-hyprland.sh help`.
  </action>
  <verify>
    <automated>test -f docs/dots-hyprland-workflow.md &amp;&amp; rg -q 'vendor/dots-hyprland' docs/dots-hyprland-workflow.md &amp;&amp; rg -q 'arch/dots-hyprland\.sh' docs/dots-hyprland-workflow.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/dots-hyprland-workflow.md` exists
    - File contains the string `vendor/dots-hyprland`
    - File contains the string `arch/dots-hyprland.sh`
    - File states that `arch/quickshell.sh` is retired OR must not list it as a current install command
  </acceptance_criteria>
  <done>Playbook file exists with prerequisites and canonical path callout.</done>
</task>

<task type="auto">
  <name>Task 2: Document clone and recursive submodule init</name>
  <files>docs/dots-hyprland-workflow.md</files>
  <read_first>
    - .planning/phases/05-fork-submodule-pin/05-CONTEXT.md (D-04 remotes, D-06 recursive, D-13 canonical path, D-15 clone commands)
    - .gitmodules
    - .planning/phases/09-workflow-documentation-update-contract/09-RESEARCH.md (Cold clone example)
  </read_first>
  <action>
    Edit **only** `docs/dots-hyprland-workflow.md` (created in Task 1). Fill the **Clone &amp; recursive submodule init** section with copy-pasteable commands covering both:
    1. `git clone --recurse-submodules git@github.com:humam-hossain/.dotfiles.git` (confirm with `git remote -v` at REPO_ROOT if origin differs; do not invent a second canonical remote)
    2. Repair path from REPO_ROOT: `git submodule update --init --recursive`

    Also document in that same file:
    - Why `--recursive` matters (nested shapes / rounded-polygon; OWN-03)
    - A simple presence check: `git submodule status` and/or shapes path under `vendor/dots-hyprland`
    - That Phase 5 does not provide a custom bootstrap script — stock git only
    - Canonical work path remains `vendor/dots-hyprland` only (Phase 5 D-13)
  </action>
  <verify>
    <automated>rg -n 'recurse-submodules|submodule update --init --recursive' docs/dots-hyprland-workflow.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Playbook contains `recurse-submodules` OR equivalent clone-with-submodules guidance
    - Playbook contains exact substring `submodule update --init --recursive`
    - Playbook mentions nested/recursive need (shapes or OWN-03 or "nested")
  </acceptance_criteria>
  <done>Clone + recursive init documented with real commands.</done>
</task>

<task type="auto">
  <name>Task 3: Document remote/pin verify, wrapper install, hooks, dual-run</name>
  <files>docs/dots-hyprland-workflow.md</files>
  <read_first>
    - arch/dots-hyprland.sh (usage/help, SAFE_DEFAULTS, backup gate, enable_hypr_ii_hooks, allowlist)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-CONTEXT.md (D-05–D-13 safe defaults and backup gate)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-CONTEXT.md (D-05 install, D-12/D-13 hooks, D-15 dual-run)
    - .planning/phases/09-workflow-documentation-update-contract/09-RESEARCH.md (Code Examples install + dual-run; Pitfall 4 re-enable)
  </read_first>
  <action>
    Complete these sections with concrete commands and expectations:

    **Verify remotes &amp; pin:**
    - `git submodule status vendor/dots-hyprland`
    - `git -C vendor/dots-hyprland remote -v` — expect origin → personal fork, upstream → end-4
    - Parent records pin via gitlink (no auto-track branch requirement)

    **Install via wrapper:**
    - Preflight is built into wrapper (missing submodule → print fix, no auto-init)
    - Dry-run first: `./arch/dots-hyprland.sh install --dry-run` — argv must show `--core --skip-hyprland --skip-sysupdate`
    - Live: `./arch/dots-hyprland.sh install` — operator types `yes` at backup gate; upstream backup dir `~/ii-original-dots-backup`
    - Explicit: do **not** pass bare `--skip-backup` on first adoption; `--force` is never auto-injected
    - Allowlisted subcommands summary; for full flags run `./arch/dots-hyprland.sh help`
    - Note: successful install enables hypr ii hooks via wrapper (`enable_hypr_ii_hooks`); uninstall comments them — re-install re-enables

    **Session hooks &amp; dual-run:**
    - Document the two hook lines: `env = ILLOGICAL_IMPULSE_VIRTUAL_ENV,~/.local/state/quickshell/.venv` and `exec-once = qs -c ii`
    - Live product path: `~/.config/quickshell` must be a **real directory** (not symlink into repo); expect `ii/shell.qml`
    - Dual-run: keep waybar (and existing swaync/rofi as configured); both bars OK if they overlap; no Waybar cutover this milestone
    - Optional verify commands from RESEARCH dual-run block (`test ! -L`, `test -f ii/shell.qml`, `pgrep` soft checks)
    - Mid-session: `hyprctl reload` + restart `qs` may be needed; full re-login also OK

    Do **not** write the full Update/pin-bump body here (09-02) beyond a stub heading if not already present.
  </action>
  <verify>
    <automated>rg -q 'install --dry-run' docs/dots-hyprland-workflow.md &amp;&amp; rg -q 'skip-hyprland' docs/dots-hyprland-workflow.md &amp;&amp; rg -q 'ILLOGICAL_IMPULSE_VIRTUAL_ENV' docs/dots-hyprland-workflow.md &amp;&amp; rg -q 'qs -c ii' docs/dots-hyprland-workflow.md &amp;&amp; rg -q 'waybar' docs/dots-hyprland-workflow.md &amp;&amp; rg -q 'backup' docs/dots-hyprland-workflow.md</automated>
  </verify>
  <acceptance_criteria>
    - Playbook contains `./arch/dots-hyprland.sh install --dry-run` (or equivalent dry-run install line)
    - Playbook contains `--skip-hyprland` and mentions `--core` and `--skip-sysupdate` (or SAFE_DEFAULTS wording covering all three)
    - Playbook contains `ILLOGICAL_IMPULSE_VIRTUAL_ENV` and `qs -c ii`
    - Playbook contains `waybar` in dual-run context
    - Playbook mentions backup gate / typing yes / `ii-original-dots-backup` (at least backup + confirmation)
    - Playbook does not contain an instructional line that runs `arch/quickshell.sh` as the adopt installer
  </acceptance_criteria>
  <done>DOC-01 install chain fully documented in the playbook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playbook text → operator shell | Docs are untrusted instructions operators will copy; wrong steps destroy personal dots or reintroduce retired paths |
| Playbook → live XDG + hypr | Install narrative drives privileged setup and session hooks |
| Playbook → git submodule pin | Clone/init guidance must preserve recursive pin integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-01 | Tampering | Submodule init docs | high | mitigate | Require `--recursive` / `--recurse-submodules` in playbook; acceptance greps |
| T-9-02 | Tampering | Retired installer narrative | high | mitigate | Forbid instructional `arch/quickshell.sh`; state retired single-path wrapper only |
| T-9-03 | Tampering / info loss | Backup gate docs | high | mitigate | Document interactive yes gate; never recommend bare `--skip-backup` on first adoption |
| T-9-04 | Denial of Service / Tampering | Hypr protection docs | high | mitigate | Document full `--skip-hyprland` safe defaults; dual-run keeps waybar |
| T-9-SC | Tampering | Package installs via docs phase | low | accept | No npm/pip/cargo; docs only |
</threat_model>

<verification>
From REPO_ROOT run the Task 3 automated greps plus: `test -f docs/dots-hyprland-workflow.md` and confirm no current-path `arch/quickshell.sh` install instruction.
</verification>

<success_criteria>
DOC-01 install/adopt narrative is complete in `docs/dots-hyprland-workflow.md` with clone→recursive submodule→wrapper dry-run/install→hooks→dual-run, matching Phases 5–7 contracts and wrapper behavior.
</success_criteria>

<output>
Create `.planning/phases/09-workflow-documentation-update-contract/09-01-SUMMARY.md` when done
</output>
