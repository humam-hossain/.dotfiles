---
phase: 09-workflow-documentation-update-contract
plan: 03
type: execute
wave: 3
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - README.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - docs/dots-hyprland-workflow.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
user_setup: []

must_haves:
  truths:
    - "Root README.md links to docs/dots-hyprland-workflow.md (or clearly points operators there under a Desktop shell / dots-hyprland section)"
    - "PROJECT.md workflow documentation open item is checked off or replaced with a done note linking the playbook"
    - "REQUIREMENTS.md DOC-01/DOC-02 remain traceable; optional status note if updated must not drop the requirement text"
    - "Product-facing paths (README, docs/, arch/) do not re-teach arch/quickshell.sh as the current install step"
    - "Playbook See also links PROJECT/REQUIREMENTS or roadmap concepts without requiring chat history"
    - "Full DOC-01 and DOC-02 content greps from 09-01/09-02 still pass"
  artifacts:
    - path: "README.md"
      provides: "Cold-clone discovery of playbook"
      contains: "Link or path docs/dots-hyprland-workflow.md"
    - path: ".planning/PROJECT.md"
      provides: "Milestone checklist closed for workflow docs"
      contains: "Playbook path or checked workflow doc item"
    - path: "docs/dots-hyprland-workflow.md"
      provides: "See also cross-links"
      contains: "Pointers to PROJECT/REQUIREMENTS or .planning paths"
  key_links:
    - from: "README.md"
      to: "docs/dots-hyprland-workflow.md"
      via: "Markdown link or explicit relative path"
    - from: "PROJECT.md"
      to: "docs/dots-hyprland-workflow.md"
      via: "Checklist completion + path"
  prohibitions:
    - "Must NOT paste the entire playbook into README (pointer only)"
    - "Must NOT resurrect arch/quickshell.sh"
    - "Must NOT rewrite historical scripts/phase0x* to pass greps"
    - "Must NOT delete planning phase history that mentions quickshell.sh in past tense"
---

<objective>
Cross-link the playbook from root README and PROJECT (and light REQUIREMENTS touch), add See also to the playbook, and retire any remaining product-facing quickshell-only install instructions so a new machine finds the dual-run path from docs alone.

Purpose: Discoverability + close PROJECT open doc task + final DOC-01/02 hygiene.

Output: Updated README.md, PROJECT.md, optional REQUIREMENTS.md, playbook See also; clean negative greps on product-facing surfaces.
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@docs/dots-hyprland-workflow.md
@README.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/09-workflow-documentation-update-contract/09-RESEARCH.md
@.planning/phases/09-workflow-documentation-update-contract/09-VALIDATION.md
@.planning/phases/08-retire-local-quickshell-product/08-CONTEXT.md
</context>

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| README discovery section | `README.md` | **09-03** |
| PROJECT checklist close | `.planning/PROJECT.md` | **09-03** |
| Playbook See also | `docs/dots-hyprland-workflow.md` | **09-03** |
| Optional REQUIREMENTS note | `.planning/REQUIREMENTS.md` | **09-03** |

## Tasks

<task type="auto">
  <name>Task 1: Link playbook from root README</name>
  <files>README.md</files>
  <read_first>
    - README.md (existing Neovim/Tmux structure)
    - docs/dots-hyprland-workflow.md (confirm path and title for accurate link text)
    - .planning/phases/09-workflow-documentation-update-contract/09-PATTERNS.md (README pointer pattern)
  </read_first>
  <action>
    Add a top-level section to `README.md`, e.g. `## Desktop shell (dots-hyprland)` (or equivalent clear heading), **before or after** existing Neovim/Tmux sections without deleting them.

    Section must:
    - State in 1–3 sentences that the desktop shell is managed via personal fork + `vendor/dots-hyprland` submodule and `arch/dots-hyprland.sh`
    - Link to `docs/dots-hyprland-workflow.md` for clone/install/update/dual-run
    - Not embed the full playbook

    Optional one-liner: Arch OS bootstrap remains in `arch/README.md` (different doc).
  </action>
  <verify>
    <automated>rg -q 'dots-hyprland-workflow' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains the path or link text `dots-hyprland-workflow`
    - Neovim/Tmux sections still present (file still contains `Neovim` or `T-MUX` / `Tmux`)
  </acceptance_criteria>
  <done>Root README discovers the playbook.</done>
</task>

<task type="auto">
  <name>Task 2: Close PROJECT workflow doc item and cross-link playbook</name>
  <files>.planning/PROJECT.md, docs/dots-hyprland-workflow.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/PROJECT.md (open checkbox "Document `.dotfiles` workflow…"; Key Decisions table)
    - .planning/REQUIREMENTS.md (DOC-01, DOC-02 rows)
    - docs/dots-hyprland-workflow.md
  </read_first>
  <action>
    1. In `.planning/PROJECT.md`, mark the open workflow documentation item complete and reference `docs/dots-hyprland-workflow.md`. Update "Last updated" line if present to reflect Phase 9 docs (date of execution).
    2. In `docs/dots-hyprland-workflow.md`, add **See also** with links/paths to:
       - `.planning/PROJECT.md` (product goals / non-goals)
       - `.planning/REQUIREMENTS.md` (DOC-01/DOC-02 and Out of Scope)
       - `arch/dots-hyprland.sh` (`./arch/dots-hyprland.sh help`)
       - Optional: `.planning/ROADMAP.md` Phase 9 success criteria
    3. Optionally in REQUIREMENTS.md: leave checkboxes for execute/verify-work to flip, or add a one-line note under DOC that playbook path is `docs/dots-hyprland-workflow.md` — do **not** remove DOC-01/DOC-02 requirement statements. Prefer leaving `[ ]` until verification if project convention is verifier flips them; if other complete rows use `[x]` only after ship, follow that convention (Phase 5–8 show `[x]` when complete — executor may set `[x]` only when content greps pass at end of this plan).
  </action>
  <verify>
    <automated>rg -q 'dots-hyprland-workflow' .planning/PROJECT.md &amp;&amp; rg -q 'PROJECT.md|REQUIREMENTS.md' docs/dots-hyprland-workflow.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains `dots-hyprland-workflow`
    - Playbook See also (or equivalent) references PROJECT and REQUIREMENTS
    - DOC-01 and DOC-02 strings still exist in `.planning/REQUIREMENTS.md`
  </acceptance_criteria>
  <done>PROJECT and playbook cross-linked; requirements text preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Retire product-facing quickshell-only install teaching + full suite greps</name>
  <files>README.md, docs/dots-hyprland-workflow.md, arch/</files>
  <read_first>
    - .planning/phases/08-retire-local-quickshell-product/08-CONTEXT.md (D-11 minimal cleanup scope)
    - .planning/phases/09-workflow-documentation-update-contract/09-VALIDATION.md (full suite + negative greps)
    - docs/dots-hyprland-workflow.md
  </read_first>
  <action>
    1. Search product-facing docs for instructional current-path uses of `arch/quickshell.sh`:
       `git grep -n 'arch/quickshell\.sh' -- README.md docs arch || true`
    2. Fix only hits that **re-teach** running the deleted installer as current procedure. Mentions that say "retired/removed/do not use" are OK. Do **not** edit `.planning/**` historical CONTEXT/SUMMARYs or frozen `scripts/phase0x*`.
    3. Run full DOC content suite (inline):
       - Playbook exists
       - recursive submodule guidance present
       - wrapper install dry-run + skip-hyprland + hooks + waybar dual-run
       - pin-bump (fetch upstream + parent pin + re-run setup)
       - exp-merge non-primary language
       - online cache non-primary language
       - README links playbook
    4. If `arch/README.md` is a natural place for a one-line pointer to the playbook, optional add — not required if root README already discovers it.
  </action>
  <verify>
    <automated>test -f docs/dots-hyprland-workflow.md &amp;&amp; rg -q 'submodule update --init --recursive' docs/dots-hyprland-workflow.md &amp;&amp; rg -q 'install --dry-run' docs/dots-hyprland-workflow.md &amp;&amp; rg -q 'exp-merge' docs/dots-hyprland-workflow.md &amp;&amp; rg -q 'dots-hyprland-workflow' README.md &amp;&amp; rg -q 'fetch upstream' docs/dots-hyprland-workflow.md</automated>
  </verify>
  <acceptance_criteria>
    - Full suite automated command above exits 0
    - `git grep -n 'arch/quickshell\.sh' -- README.md docs` returns either no hits or only retired/do-not-use framing (no "run arch/quickshell.sh to install" current step)
    - `test ! -e arch/quickshell.sh` still true (do not recreate)
  </acceptance_criteria>
  <done>Cross-links live; product-facing docs clean; DOC-01/02 suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README discovery → playbook | Wrong/missing link leaves tribal knowledge gap (SC-3 failure) |
| Stale installer mentions → operator | Re-teaching deleted installer undoes RET-02 |
| PROJECT checklist → future agents | Open checkbox after docs ship causes duplicate work |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-06 | Information Disclosure / Tampering | Discovery &amp; stale refs | medium | mitigate | README + PROJECT must reference playbook; neg-grep quickshell.sh instructional hits on product-facing paths |
| T-9-02b | Tampering | Stale installer re-teach | high | mitigate | Task 3 greps README/docs/arch; no recreate of arch/quickshell.sh |
| T-9-SC | Tampering | Package installs | low | accept | Docs/cross-link only |
</threat_model>

<verification>
Full VALIDATION.md suite greps green; README and PROJECT reference playbook; no instructional arch/quickshell.sh on product-facing docs.
</verification>

<success_criteria>
A clean read of README → playbook covers install + update contracts; PROJECT workflow doc item closed; retired installer not re-taught outside planning history.
</success_criteria>

<output>
Create `.planning/phases/09-workflow-documentation-update-contract/09-03-SUMMARY.md` when done
</output>
