---
phase: 09-workflow-documentation-update-contract
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - docs/dots-hyprland-workflow.md
autonomous: true
requirements:
  - DOC-02
user_setup: []

must_haves:
  truths:
    - "Playbook documents primary update as pin-bump: fetch/merge upstream in fork/submodule, bump parent gitlink SHA, re-run setup via wrapper"
    - "Playbook explicitly marks exp-merge (and exp-update if mentioned) as non-primary / experimental — not the default update path"
    - "Playbook explicitly marks online cache install (~/.cache/dots-hyprland or curl-cache style) as non-primary / not the managed .dotfiles path"
    - "Playbook states auto-bump of submodule on every parent pull is out of scope / not desired"
    - "Playbook non-goals include full Waybar cutover and full hyprland.lua takeover for this milestone"
    - "Update section does not replace Install/Adopt content from 09-01"
  artifacts:
    - path: "docs/dots-hyprland-workflow.md"
      provides: "Update contract + non-goals (DOC-02)"
      contains: "Pin-bump steps; Non-goals section with exp-merge and online cache non-primary"
  key_links:
    - from: "Update section"
      to: "./arch/dots-hyprland.sh install|install-files"
      via: "Re-run setup after pin bump with same safe defaults/gate"
    - from: "Non-goals"
      to: ".planning/REQUIREMENTS.md Out of Scope"
      via: "Aligned non-primary list"
  prohibitions:
    - "Must NOT present exp-merge or exp-update as the primary/recommended update path"
    - "Must NOT present online cache / curl-to-cache install as the managed adoption path"
    - "Must NOT document auto-bump submodule on every parent pull as desired behavior"
    - "Must NOT delete or gut DOC-01 install sections while adding update content"
---

<objective>
Complete the Update/pin-bump contract and Non-goals section in the operator playbook (DOC-02).

Purpose: Operators must know how to pull end-4 updates reproducibly via fork + parent pin, and must not treat experimental exp-merge or online cache install as the default.

Output: Updated `docs/dots-hyprland-workflow.md` with full Update + Non-goals sections.
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/09-workflow-documentation-update-contract/09-RESEARCH.md
@.planning/phases/09-workflow-documentation-update-contract/09-01-PLAN.md
@.planning/phases/05-fork-submodule-pin/05-CONTEXT.md
@.planning/research/STACK.md
@docs/dots-hyprland-workflow.md
@arch/dots-hyprland.sh
</context>

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Update + Non-goals sections | `docs/dots-hyprland-workflow.md` | **09-02** |

## Tasks

<task type="auto">
  <name>Task 1: Write primary pin-bump update procedure</name>
  <files>docs/dots-hyprland-workflow.md</files>
  <read_first>
    - docs/dots-hyprland-workflow.md (must preserve 09-01 install content)
    - .planning/phases/09-workflow-documentation-update-contract/09-RESEARCH.md (Pin-bump update example)
    - .planning/phases/05-fork-submodule-pin/05-CONTEXT.md (D-07 dual remotes, D-08 pin-only, D-13 canonical path)
    - .planning/REQUIREMENTS.md (DOC-02 text; Out of Scope auto-bump / exp-merge)
  </read_first>
  <action>
    Replace any Update stub with a full **Update contract (pin-bump)** section that is the **primary** documented update path:

    Required steps (concrete commands):
    1. `cd vendor/dots-hyprland` (canonical path only)
    2. Ensure `upstream` remote points at end-4; `git fetch upstream`
    3. Merge or rebase desired upstream ref onto the fork branch; resolve conflicts; `git push origin HEAD` (origin = personal fork)
    4. From REPO_ROOT: stage submodule gitlink (`git add vendor/dots-hyprland`), commit parent pin bump with a clear message
    5. Apply on machine: re-run `./arch/dots-hyprland.sh install-files` and/or `install` / `install-deps` as needed — same safe defaults + backup gate as first adoption; never bare `--skip-backup` casually
    6. Optional after deps: `./arch/dots-hyprland.sh protect` if documenting asdeps demotion recovery (brief; pointer to help OK)

    Also state:
    - Parent pin is intentional reproducibility — **not** auto-bump on every parent pull
    - Do not develop against a sibling checkout as SoT
  </action>
  <verify>
    <automated>rg -q 'fetch upstream' docs/dots-hyprland-workflow.md &amp;&amp; rg -q 'vendor/dots-hyprland' docs/dots-hyprland-workflow.md &amp;&amp; rg -n 'git add vendor/dots-hyprland|gitlink|bump.*pin|pin.*bump|submodule.*SHA|parent pin' docs/dots-hyprland-workflow.md | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Playbook contains `fetch upstream` (or `git fetch upstream`)
    - Playbook documents committing/staging the parent submodule pin (`git add vendor/dots-hyprland` or equivalent gitlink bump wording)
    - Playbook documents re-running `./arch/dots-hyprland.sh` install or install-files after the bump
    - Playbook still contains DOC-01 markers from 09-01 (`install --dry-run` and `qs -c ii`)
  </acceptance_criteria>
  <done>Primary pin-bump update path documented.</done>
</task>

<task type="auto">
  <name>Task 2: Write Non-goals / non-primary paths (exp-merge, online cache, cutover)</name>
  <files>docs/dots-hyprland-workflow.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (Out of Scope table — exp-merge, auto-bump, cutover, hypr lua)
    - .planning/research/STACK.md (online cache non-managed; exp-merge not primary)
    - arch/dots-hyprland.sh (exp-merge refused — allowlist FAIL message)
    - .planning/phases/09-workflow-documentation-update-contract/09-RESEARCH.md (Non-primary examples)
  </read_first>
  <action>
    Add a **Non-goals / non-primary paths** section that explicitly states:

    1. **`exp-merge` / `exp-update` are NOT the primary update path** — experimental; wrapper **refuses** them (`./arch/dots-hyprland.sh exp-merge` → non-allowlisted). If an operator truly needs upstream experimental tools, they use `vendor/dots-hyprland/./setup` directly and own the risk — still not the documented default.
    2. **Online cache / curl install into `~/.cache/dots-hyprland` (or similar) is NOT the managed `.dotfiles` path** — bypasses parent submodule pin and fork ownership.
    3. **Auto-bump submodule on every parent pull** — out of scope; breaks reproducibility.
    4. **Full Waybar/rofi/swaync cutover** — dual-run intentional this milestone; customs ports deferred.
    5. **Full hyprland.lua / ii hypr tree takeover** — personal hypr conf remains SoT with `--skip-hyprland`.
    6. **Reimplementing package lists in arch/** without `./setup` — forbidden.
    7. Brief: wrapper `verify` subcommand is future POLISH-01, not required here.

    Keep tone firm but short — a table or bullet list is fine. Do not write a tutorial that walks through exp-merge as if recommended.
  </action>
  <verify>
    <automated>rg -q 'exp-merge' docs/dots-hyprland-workflow.md &amp;&amp; rg -qi 'non-primary|not the primary|experimental|not primary' docs/dots-hyprland-workflow.md &amp;&amp; rg -qi 'cache|online' docs/dots-hyprland-workflow.md</automated>
  </verify>
  <acceptance_criteria>
    - Playbook contains the string `exp-merge`
    - Same file contains language marking it non-primary or experimental (e.g. `non-primary`, `not the primary`, `experimental`, or `not primary`)
    - Playbook mentions online cache or `~/.cache/dots-hyprland` or "cache install" as non-managed/non-primary
    - Playbook does not say to use exp-merge as the default/recommended update (no sentence like "to update, run exp-merge" as the main path)
  </acceptance_criteria>
  <done>DOC-02 non-primary contract documented beside pin-bump.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Update docs → fork/submodule git | Wrong primary path loses pin reproducibility |
| Non-goals text → operator habits | Missing warnings make experimental tools into defaults |
| Update docs → wrapper re-run | Re-install without gate/backup endangers dots |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-05 | Tampering | Update contract narrative | high | mitigate | Pin-bump is primary; exp-merge + online cache explicitly non-primary; greps in acceptance |
| T-9-03b | Tampering / info loss | Post-bump re-install docs | high | mitigate | Re-state backup gate / safe defaults on update re-run |
| T-9-SC | Tampering | Package installs | low | accept | Docs only; no registry installs this plan |
</threat_model>

<verification>
DOC-02 greps green; 09-01 install greps still green on the same file.
</verification>

<success_criteria>
Playbook documents pin-bump as primary update and explicitly demotes exp-merge and online cache install; non-goals cover cutover/auto-bump/hypr takeover.
</success_criteria>

<output>
Create `.planning/phases/09-workflow-documentation-update-contract/09-02-SUMMARY.md` when done
</output>
