---
phase: 07-install-session-hooks-dual-run-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.config/quickshell
autonomous: false
requirements:
  - LIVE-01
user_setup: []

must_haves:
  truths:
    - "No qs or quickshell process is running before install-files (D-03)"
    - "Live ~/.config/quickshell is not a symlink (D-01); preferred first-run state is path absent so setup creates a real directory"
    - "In-repo .config/quickshell product tree still exists under REPO_ROOT (D-04); unlink did not delete the symlink target"
    - "If install is interrupted later: fix cause and re-run wrapper only (D-08); no parallel second install against the same live path; pre-condition remains qs stopped and path not a symlink (LIVE-01 concurrency edge)"
    - "Operator confirms pre-install ready before 07-02 live install begins"
  artifacts:
    - path: "~/.config/quickshell"
      provides: "Absent path (or non-symlink real dir on re-run) ready for upstream install_dir__sync"
      contains: "Not a symlink into .dotfiles/.config/quickshell"
    - path: ".config/quickshell (in-repo)"
      provides: "Untouched v0.1 product tree (Phase 8 retirement only)"
      contains: "Still present as a directory under REPO_ROOT"
  key_links:
    - from: "pkill -x qs / quickshell"
      to: "no process holding live QS path"
      via: "D-03 stop before unlink"
    - from: "plain rm on symlink only"
      to: "path absent; repo target preserved"
      via: "D-01 unlink; never follow/delete repo target"
  prohibitions:
    - "Must NOT follow or delete the repo target of the quickshell symlink (D-01)"
    - "Must NOT use recursive force-remove on ~/.config/quickshell while it is a symlink"
    - "Must NOT run arch/quickshell.sh (would re-symlink; undoes LIVE-01)"
    - "Must NOT retire or modify in-repo .config/quickshell this phase (D-04 / Phase 8)"
    - "Must NOT start live wrapper install in this plan (07-02 owns dry-run + install)"
    - "Must NOT run a parallel second install against the same live path"
---

<objective>
Stop running Quickshell and safely break the live `~/.config/quickshell` symlink so the path is absent (or non-symlink) before any install-files rsync — per D-01, D-03, D-04 — protecting the git tree from rsync-through-symlink (T-7-01 / LIVE-01 preconditions).

Purpose: Eliminate Pitfall 2 (rsync into `.dotfiles/.config/quickshell`) before Phase 6 wrapper live install.

Output: qs stopped; live QS path not a symlink (preferably absent); in-repo product tree intact; operator confirmation to proceed to 07-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-CONTEXT.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-RESEARCH.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-PATTERNS.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-VALIDATION.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-CONTEXT.md
@arch/dots-hyprland.sh
@arch/quickshell.sh
</context>

## Phase Goal

**Land a running illogical-impulse shell beside Waybar using personal session ownership.** This plan only clears the live symlink hazard so install-files can create a real directory tree (LIVE-01 preconditions).

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Pre-install host state (qs stopped, QS path not symlink) | `~/.config/quickshell` absent or real dir | **07-01** |
| Real installed QS tree from upstream | `~/.config/quickshell` + `ii/shell.qml` | 07-02 |
| Personal hypr hooks (env + exec-once) | `.config/hypr/hyprland.conf` (repo + live) | 07-03 |
| Dual-run LIVE-01..04 evidence | 07-03 SUMMARY + operator chrome confirm | 07-03 |

## Tasks

<task type="auto">
  <name>Task 1: Stop running qs/quickshell before path mutation (D-03)</name>
  <files>~/.config/quickshell</files>
  <read_first>
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-CONTEXT.md (D-03, D-01, phase boundary)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-RESEARCH.md (Pre-install qs stop Code Example; Pitfall 1; Pattern 1)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-PATTERNS.md (Live QS pre-install break sequence; labeled echo vocabulary)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-VALIDATION.md (07-01 row; LIVE-01 pre asserts)
  </read_first>
  <action>
    From any shell in the Hyprland session (mid-session OK per D-07 for later install; this task only stops qs):

    1. Per D-03, stop running Quickshell so nothing holds the live path mid-rsync later:
       - If `pgrep -x qs` or `pgrep -x quickshell` is true: echo `[CONFIG] stopping running Quickshell`, then `pkill -x qs` and `pkill -x quickshell` (ignore non-zero if already gone), then `sleep 0.5`.
       - Else: echo `[SKIP] no qs/quickshell process` (warn optional; not a failure).
    2. Hard-fail if qs is still running: if `pgrep -x qs` succeeds after stop, echo `[FAIL] qs still running` and exit non-zero.
    3. Do not touch `~/.config/quickshell` yet (Task 2). Do not run `arch/quickshell.sh`. Do not start wrapper install.
    4. Labeled echos only (`[CONFIG]`, `[SKIP]`, `[FAIL]`); no `set -x`.
  </action>
  <verify>
    <automated>pgrep -x qs && exit 1 || true; pgrep -x quickshell && exit 1 || true; echo "[VERIFY] OK no qs/quickshell process"</automated>
  </verify>
  <acceptance_criteria>
    - `pgrep -x qs` finds no process (exit non-zero from pgrep is success for this gate).
    - `pgrep -x quickshell` finds no process.
    - No install or symlink mutation occurred in this task.
  </acceptance_criteria>
  <done>qs/quickshell processes stopped; path ready for safe unlink.</done>
</task>

<task type="auto">
  <name>Task 2: Unlink live QS symlink only; leave path absent; preserve repo (D-01, D-04)</name>
  <files>~/.config/quickshell, .config/quickshell</files>
  <read_first>
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-CONTEXT.md (D-01, D-04)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-RESEARCH.md (Pattern 1 Safe symlink break; Pitfall 1; LIVE-01 assert inverted from quickshell verify)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-PATTERNS.md (anti-pattern arch/quickshell.sh symlink_config — do the opposite; plain rm only)
    - arch/quickshell.sh (read symlink_config only as anti-pattern; do not execute)
  </read_first>
  <action>
    Break the live symlink hazard without touching the repo product (per D-01, D-04, T-7-01):

    1. Record pre-state: `ls -ld "$HOME/.config/quickshell"` (expect symlink into `.dotfiles/.config/quickshell` on first run).
    2. If path is a symlink (`[ -L "$HOME/.config/quickshell" ]`): echo `[CONFIG] removing QS symlink (repo target preserved)` and remove **only the link** with plain `rm "$HOME/.config/quickshell"` — never recursive force-remove on that path while it is a symlink.
    3. Elif path exists and is not a symlink: echo `[WARN]` with `ls -ld` output. D-01 only mandates unlink-of-symlink; a real directory is acceptable on re-run (upstream rsync --delete). Do not delete a real directory in this plan.
    4. Assert post-state:
       - `test ! -L "$HOME/.config/quickshell"` (not a symlink).
       - Preferred first-run: `test ! -e "$HOME/.config/quickshell"` (path absent so setup creates real dir). If a real dir already exists from a prior partial install, document it and continue only if not a symlink.
    5. Assert repo product untouched (D-04): `test -d "$(git rev-parse --show-toplevel)/.config/quickshell"` still true. Do not delete, rename, or empty the in-repo tree.
    6. Explicitly do **not** run `arch/quickshell.sh` (T-7-06 / would re-create symlink). Do not invoke `./setup` or `arch/dots-hyprland.sh` yet.
    7. Concurrency edge (LIVE-01): do not start a parallel second install; leave machine in single-operator pre-install state for 07-02.
  </action>
  <verify>
    <automated>test ! -L "$HOME/.config/quickshell"; test ! -e "$HOME/.config/quickshell" || test -d "$HOME/.config/quickshell"; test -d "$(git rev-parse --show-toplevel)/.config/quickshell"; echo "[VERIFY] OK LIVE-01 pre-install path state"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -L "$HOME/.config/quickshell"` succeeds.
    - Preferred: `test ! -e "$HOME/.config/quickshell"` succeeds on first-run hosts; if path exists it must be a real directory not a symlink.
    - `test -d "$(git rev-parse --show-toplevel)/.config/quickshell"` succeeds (repo product still present).
    - No mass unexpected changes under in-repo `.config/quickshell` from this unlink step.
    - `arch/quickshell.sh` was not executed.
  </acceptance_criteria>
  <done>Live QS path is not a symlink (preferably absent); repo product intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator confirms pre-install ready for wrapper install</name>
  <files>~/.config/quickshell</files>
  <read_first>
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-01-PLAN.md (Tasks 1–2 outputs)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-VALIDATION.md (07-01 row)
  </read_first>
  <what-built>
    qs/quickshell stopped; live `~/.config/quickshell` unlinked if it was a symlink; path preferably absent; in-repo `.config/quickshell` still present. Machine is ready for 07-02 dry-run then live `arch/dots-hyprland.sh install`.
  </what-built>
  <action>
    Present pre-install state to the operator and obtain explicit approval before 07-02 mutates the machine. Re-run the automated gates from Tasks 1–2 so the operator sees current truth: no qs process; live QS path not a symlink (preferably absent); in-repo product directory still present. Do not start wrapper install in this task. Do not re-run arch/quickshell.sh. Record approval (or issues) in 07-01-SUMMARY.
  </action>
  <how-to-verify>
    1. Run: `ls -ld ~/.config/quickshell 2>&1` — expect "No such file" (preferred) or a real directory listing, never `->` symlink into `.dotfiles`.
    2. Run: `pgrep -a qs || echo "no qs"` — expect no qs process.
    3. Run: `test -d "$(git rev-parse --show-toplevel)/.config/quickshell" && echo "repo product OK"`.
    4. Confirm you are ready for a long interactive install (yay/sudo, wrapper `yes` gate, upstream backup prompt) in plan 07-02.
  </how-to-verify>
  <verify>
    <automated>pgrep -x qs && exit 1 || true; test ! -L "$HOME/.config/quickshell"; test -d "$(git rev-parse --show-toplevel)/.config/quickshell"</automated>
  </verify>
  <resume-signal>Type "approved" to proceed to 07-02, or describe issues</resume-signal>
  <done>Operator approved pre-install state; 07-02 may begin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell → live XDG config | Unlink mutates host session state only |
| Live `~/.config/quickshell` → repo product | Symlink edge must not become a delete-into-git path |
| Pre-install → install-files (07-02) | Files install must never see a symlink destination |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-01 | Tampering | `~/.config/quickshell` symlink → rsync | high | mitigate | D-01 plain unlink only; assert `! -L`; leave path absent; never follow/delete repo target |
| T-7-06 | Tampering | `arch/quickshell.sh` re-run | high | mitigate | Explicit forbid re-run; do not execute in this plan |
| T-7-03 | Tampering / data loss | accidental skip-backup | high | mitigate | No install flags this plan; 07-02 enforces D-02 (no skip-backup) |
| T-7-02 | Tampering | hypr conf rename | medium | accept | No install this plan; residual owned by 07-02 `--skip-hyprland` |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | No registry package installs in this plan (N/A) |
</threat_model>

<verification>
From host: no qs process; `test ! -L ~/.config/quickshell`; preferred `test ! -e ~/.config/quickshell`; in-repo `.config/quickshell` directory still exists; operator approved checkpoint.
</verification>

<success_criteria>
LIVE-01 preconditions hold: qs stopped, live path not a symlink (preferably absent), repo product untouched — safe to run wrapper install in 07-02.
</success_criteria>

<output>
Create `.planning/phases/07-install-session-hooks-dual-run-verify/07-01-SUMMARY.md` when done
</output>
