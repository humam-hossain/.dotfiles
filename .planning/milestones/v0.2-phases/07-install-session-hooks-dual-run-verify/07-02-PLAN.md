---
phase: 07-install-session-hooks-dual-run-verify
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - ~/.config/quickshell
  - ~/.local/state/quickshell/.venv
autonomous: false
requirements:
  - LIVE-01
user_setup:
  - service: local-host-install
    why: "Interactive wrapper backup gate + upstream backup prompt + yay/sudo during one-shot install"
    env_vars: []
    dashboard_config:
      - task: "At wrapper gate type exact yes (case-sensitive)"
        location: "Terminal running ./arch/dots-hyprland.sh install"
      - task: "At upstream auto_backup prompt prefer y to create ~/ii-original-dots-backup"
        location: "Same install session after wrapper gate"
      - task: "Approve yay/sudo package prompts as needed for install-deps"
        location: "Same install session"

must_haves:
  truths:
    - "Dry-run first (D-06): printf yes pipe to install --dry-run shows --core --skip-hyprland --skip-sysupdate and dry-run would-exec without calling setup"
    - "Live one-shot install (D-05) uses arch/dots-hyprland.sh install with Phase 6 safe defaults; operator answers backup gate (D-02) without skip-backup flags"
    - "After install: ~/.config/quickshell is a real directory (not symlink) with ii/shell.qml present (LIVE-01)"
    - "readlink -f of live QS path does not resolve under .dotfiles/.config/quickshell"
    - "Optional setups artifact: ~/.local/state/quickshell/.venv directory exists after successful setups (LIVE-02 empty-input edge: venv path present post-install)"
    - "Personal hyprland.conf was not renamed to .old (T-7-02 / --skip-hyprland)"
    - "On mid-install failure: fix cause and re-run wrapper only (D-08); no automated rollback; no parallel second install (LIVE-01 concurrency)"
  artifacts:
    - path: "~/.config/quickshell"
      provides: "Real installed ii tree from vendor dots via setup install_dir__sync"
      contains: "ii/shell.qml; not a symlink"
    - path: "~/.local/state/quickshell/.venv"
      provides: "ii Python venv created by install-setups"
      contains: "Directory present after successful setups"
    - path: "arch/dots-hyprland.sh"
      provides: "Invoke-only Phase 6 wrapper (do not rewrite)"
      contains: "SAFE_DEFAULTS + backup_gate + allowlist"
  key_links:
    - from: "arch/dots-hyprland.sh install"
      to: "vendor/dots-hyprland/./setup install --core --skip-hyprland --skip-sysupdate"
      via: "SAFE_DEFAULTS injection + backup_gate yes-token"
    - from: "setup install-files"
      to: "~/.config/quickshell real dir"
      via: "install_dir__sync rsync --delete (only safe after 07-01 unlink)"
    - from: "setup install-setups"
      to: "~/.local/state/quickshell/.venv"
      via: "uv venv at XDG_STATE_HOME/quickshell/.venv"
  prohibitions:
    - "Must NOT pass --skip-backup or --allow-skip-backup on first adoption (D-02)"
    - "Must NOT bypass wrapper with raw ./setup for first adoption"
    - "Must NOT run arch/quickshell.sh after unlink (T-7-06)"
    - "Must NOT live-run without dry-run-first in this phase sequence (D-06)"
    - "Must NOT install or replace full ii hyprland.lua / hypr tree (D-09; --skip-hyprland holds)"
    - "Must NOT retire in-repo .config/quickshell this phase (D-04 / Phase 8)"
    - "Must NOT invent automated rollback from ~/ii-original-dots-backup (D-08 re-run only)"
    - "Must NOT rewrite arch/dots-hyprland.sh or vendor/dots-hyprland content"
---

<objective>
Run Phase 6 wrapper dry-run then one-shot live `install` (deps + setups + files) with safe defaults and interactive backup gate — per D-02, D-05, D-06, D-07, D-08 — producing LIVE-01 real installed tree at `~/.config/quickshell`.

Purpose: First machine-mutating adoption of vendor dots-hyprland without destroying personal hyprland.conf or re-symlinking into the git tree.

Output: Dry-run argv proof; successful live install; LIVE-01 filesystem asserts green; no hypr conf rename.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-CONTEXT.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-RESEARCH.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-PATTERNS.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-VALIDATION.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-01-SUMMARY.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-CONTEXT.md
@arch/dots-hyprland.sh
@vendor/dots-hyprland/setup
</context>

## Phase Goal

**Land a running illogical-impulse shell beside Waybar using personal session ownership.** This plan performs the wrapper-mediated live install that materializes the real QS tree (LIVE-01).

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Pre-install host state | `~/.config/quickshell` absent / non-symlink | 07-01 |
| Real installed QS tree | `~/.config/quickshell/ii/shell.qml` | **07-02** |
| ii Python venv | `~/.local/state/quickshell/.venv` | **07-02** (setups) |
| Optional upstream backup | `~/ii-original-dots-backup` | **07-02** (if operator accepts) |
| Personal hypr hooks | `.config/hypr/hyprland.conf` | 07-03 |

## Tasks

<task type="auto">
  <name>Task 1: Dry-run install; assert safe-default argv (D-06)</name>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (SAFE_DEFAULTS, backup_gate, dry-run path — invoke only, do not edit)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-CONTEXT.md (D-05, D-06, D-02)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-RESEARCH.md (Pattern 2 Dry-run argv gate; Code Examples dry-run)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-PATTERNS.md (wrapper invoke patterns; dry-run then live sequence)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-VALIDATION.md (D-06 / 07-02 dry-run row)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-01-SUMMARY.md (confirm pre-install green)
  </read_first>
  <action>
    From REPO_ROOT, prove safe defaults before any live mutation (per D-06):

    1. Reconfirm 07-01 preconditions still hold: no qs process; `test ! -L "$HOME/.config/quickshell"`. If symlink reappeared, stop and re-run 07-01 — do not install.
    2. Run dry-run with gate yes-token piped (gate still requires `yes` even on dry-run):
       `out=$(printf 'yes\n' | ./arch/dots-hyprland.sh install --dry-run 2>&1)`
       Tee to `/tmp/p7-dry.txt` for SUMMARY evidence.
    3. Assert output contains each of: `--core`, `--skip-hyprland`, `--skip-sysupdate`, and a dry-run would-exec line (grep `dry-run: would exec` or equivalent wrapper wording). Expected conceptual argv: `./setup install` plus the three SAFE_DEFAULTS flags.
    4. Assert exit code 0. Confirm setup was **not** actually executed (no pacman/yay transaction from this command; dry-run exits before chdir-exec).
    5. Do **not** pass skip-backup flags (D-02). Do not edit the wrapper. Do not call raw `./setup`.
    6. If dry-run fails preflight (missing `.git` or non-executable setup): print stock fix `git submodule update --init --recursive` for the operator; do not auto-init from the plan executor as a silent repair beyond documenting the command.
  </action>
  <verify>
    <automated>test ! -L "$HOME/.config/quickshell" && printf 'yes\n' | ./arch/dots-hyprland.sh install --dry-run 2>&1 | tee /tmp/p7-dry.txt | grep -q -- '--core' && grep -q -- '--skip-hyprland' /tmp/p7-dry.txt && grep -q -- '--skip-sysupdate' /tmp/p7-dry.txt && grep -q 'dry-run: would exec' /tmp/p7-dry.txt && echo "[VERIFY] OK D-06 dry-run safe defaults"</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/p7-dry.txt` contains `--core`, `--skip-hyprland`, and `--skip-sysupdate`.
    - Output includes wrapper dry-run would-exec wording.
    - Dry-run exits 0 without mutating `~/.config/quickshell` into a new tree yet (still absent or pre-existing non-symlink only).
    - No `--skip-backup` appeared in the operator command line for this task.
    - Live path is still not a symlink before Task 2.
  </acceptance_criteria>
  <done>Dry-run proves safe-default argv; ready for interactive live install.</done>
</task>

<task type="auto">
  <name>Task 2: Live one-shot wrapper install with operator gates (D-05, D-02, D-07, D-08)</name>
  <files>~/.config/quickshell, ~/.local/state/quickshell/.venv</files>
  <read_first>
    - arch/dots-hyprland.sh (backup_gate messaging; live exec path)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-CONTEXT.md (D-05, D-02, D-07, D-08)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-RESEARCH.md (Installation operator sequence step 2; Pitfall 2 double prompts; Pitfall 7 long install-deps; Environment Availability python3.12)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-PATTERNS.md (UAT double-prompt wording; Error/recovery D-08)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-CONTEXT.md (wrapper D-05..D-13 locked — do not re-open)
  </read_first>
  <action>
    Perform first-adoption live install mid-session after qs stopped (D-05, D-07) via wrapper only:

    1. From REPO_ROOT run **interactive** (do not pipe yes for live run so operator sees prompts):
       `./arch/dots-hyprland.sh install`
       — one-shot deps + setups + files with injected `--core --skip-hyprland --skip-sysupdate`.
    2. Operator gate wording (Pitfall 2 / D-02):
       - Wrapper: type exact `yes` (case-sensitive) at backup gate.
       - Upstream may then ask to backup clashing paths to `~/ii-original-dots-backup` — **prefer `y`**.
       - Do **not** pass or suggest `--skip-backup` / `--allow-skip-backup` on first adoption.
    3. Expect long runtime (network/AUR/yay/sudo). Approve package prompts as needed. Setup may provision python 3.12 for uv if missing (A4 / Pitfall 7).
    4. On failure mid-install (D-08): diagnose cause (network, yay conflict, missing base-devel, python 3.12); **fix cause and re-run** `./arch/dots-hyprland.sh install` only. No automated rollback script. Optionally re-dry-run if flags uncertain. No parallel second install against the same live path (LIVE-01 concurrency).
    5. Do not run `arch/quickshell.sh`. Do not call non-allowlisted setup subcommands. Do not rewrite wrapper or vendor tree. Do not install full ii hypr tree (D-09; skip-hyprland already injected).
    6. Soft-watch: if `~/.config/hypr/hyprland.conf.old` appears, treat as T-7-02 regression (entry-only skip bug) — stop and investigate; full `--skip-hyprland` should have prevented rename.
  </action>
  <verify>
    <automated>test ! -L "$HOME/.config/quickshell" && test -d "$HOME/.config/quickshell" && test -f "$HOME/.config/quickshell/ii/shell.qml" && test -f "$HOME/.config/hypr/hyprland.conf" && echo "[VERIFY] OK post-install tree presence (full LIVE-01 in Task 3)"</automated>
  </verify>
  <acceptance_criteria>
    - Install command completed with exit 0, or D-08 re-run after fix completed with exit 0.
    - Operator typed `yes` at wrapper gate; did not use skip-backup flags.
    - `~/.config/quickshell` exists as a directory after install.
    - `~/.config/hypr/hyprland.conf` still exists (not solely replaced by a rename-away without replacement).
    - Soft: `hyprland.conf.old` absent preferred; if present, recorded as failure to investigate before 07-03.
  </acceptance_criteria>
  <done>Live one-shot install finished; tree present for LIVE-01 hard asserts.</done>
</task>

<task type="auto">
  <name>Task 3: LIVE-01 hard asserts — real dir, ii tree, not under .dotfiles</name>
  <files>~/.config/quickshell, ~/.local/state/quickshell/.venv</files>
  <read_first>
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-RESEARCH.md (LIVE-01 assert Code Example; Validation Architecture LIVE-01 rows)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-VALIDATION.md (Requirement → Test Map LIVE-01)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-PATTERNS.md (inverted verify from arch/quickshell.sh verify_install)
    - .planning/REQUIREMENTS.md (LIVE-01 text)
  </read_first>
  <action>
    Prove LIVE-01 after files install (goal-backward truths):

    1. Real directory not symlink:
       `test ! -L "$HOME/.config/quickshell"`
       `test -d "$HOME/.config/quickshell"`
       `test -f "$HOME/.config/quickshell/ii/shell.qml"`
    2. Must not resolve into repo product:
       `case "$(readlink -f "$HOME/.config/quickshell")" in */.dotfiles/.config/quickshell|*/.dotfiles/.config/quickshell/*) echo "[FAIL] still under .dotfiles product path"; exit 1 ;; esac`
    3. In-repo product still present and separate (D-04): `test -d "$(git rev-parse --show-toplevel)/.config/quickshell"`.
    4. Venv empty-input edge (post-setups): `test -d "$HOME/.local/state/quickshell/.venv"` — required for later LIVE-02/04 python scripts. If missing, setups failed — fix and re-run wrapper (D-08), do not hand-roll venv.
    5. Soft hypr protection assert: `test -f "$HOME/.config/hypr/hyprland.conf"`; warn if `.old` sibling exists.
    6. Record all assert outputs in 07-02-SUMMARY. Do not add hypr hooks yet (07-03 / D-14 order: hooks after install, before LIVE-04).
  </action>
  <verify>
    <automated>test ! -L "$HOME/.config/quickshell" && test -d "$HOME/.config/quickshell" && test -f "$HOME/.config/quickshell/ii/shell.qml" && case "$(readlink -f "$HOME/.config/quickshell")" in */.dotfiles/.config/quickshell|*/.dotfiles/.config/quickshell/*) echo "[FAIL] under .dotfiles"; exit 1 ;; esac && test -d "$HOME/.local/state/quickshell/.venv" && test -d "$(git rev-parse --show-toplevel)/.config/quickshell" && echo "[VERIFY] OK LIVE-01"</automated>
  </verify>
  <acceptance_criteria>
    - Live QS path is not a symlink and is a directory.
    - `ii/shell.qml` exists under live QS path.
    - `readlink -f` of live path is not under `*/.dotfiles/.config/quickshell`.
    - `~/.local/state/quickshell/.venv` exists as a directory.
    - In-repo `.config/quickshell` still exists (not retired).
    - SUMMARY records LIVE-01 green.
  </acceptance_criteria>
  <done>LIVE-01 fully verified on machine; ready for session hooks in 07-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator yes-token → setup files install | Human must acknowledge backup/overwrite before rsync |
| Wrapper argv → vendor `./setup` | Only allowlisted subcommands + injected safe defaults |
| setup rsync → `~/.config/quickshell` | Destination must not be a symlink into git (07-01 precondition) |
| yay/sudo → host packages | Interactive operator privilege only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-01 | Tampering | rsync through symlink into git | high | mitigate | Require 07-01 unlink; re-assert `! -L` before and after install; refuse if symlink returns |
| T-7-02 | Tampering | accidental hypr conf rename | high | mitigate | Wrapper injects full `--skip-hyprland` (not entry-only); soft-assert no `.old`; never raw setup without defaults |
| T-7-03 | Tampering / data loss | accidental `--skip-backup` | high | mitigate | D-02: do not pass skip-backup flags; wrapper refuses bare skip without allow meta |
| T-7-04 | Elevation of Privilege | unexpected setup subcommand | high | mitigate | Invoke only allowlisted `install` via wrapper; no uninstall/exp-* |
| T-7-05 | Tampering | command injection via flags | medium | accept | Residual mitigated by Phase 6 array exec; do not re-open wrapper |
| T-7-06 | Tampering | re-run `arch/quickshell.sh` | high | mitigate | Explicit forbid; never execute in this plan |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | No planner-introduced registry packages; AUR via upstream setup only (N/A legitimacy table) |
</threat_model>

<verification>
Dry-run `/tmp/p7-dry.txt` shows three safe defaults; post-install LIVE-01 asserts (`! -L`, `-d`, `ii/shell.qml`, not under `.dotfiles`, `.venv` present); hypr conf still present.
</verification>

<success_criteria>
LIVE-01 true: real upstream-synced QS tree at `~/.config/quickshell` with `ii/shell.qml`, not a symlink into `.dotfiles`; safe defaults and backup gate honored; ready for 07-03 hooks.
</success_criteria>

<output>
Create `.planning/phases/07-install-session-hooks-dual-run-verify/07-02-SUMMARY.md` when done
</output>
