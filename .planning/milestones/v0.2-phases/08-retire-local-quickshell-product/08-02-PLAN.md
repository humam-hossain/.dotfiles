---
phase: 08-retire-local-quickshell-product
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - .config/quickshell
autonomous: true
requirements:
  - RET-01
user_setup: []

must_haves:
  truths:
    - "Before delete: live ~/.config/quickshell still real dir with ii/shell.qml and not under repo (08-01 hold rechecked)"
    - "In-repo .config/quickshell product tree fully removed from REPO_ROOT via git rm -r (D-07) — ~933 tracked files staged for deletion"
    - "Uncommitted WIP under the in-repo tree discarded without committing first (D-08); path absent after cleanup"
    - "Tree delete is its own atomic git commit (D-09); no annotated recovery tag (D-10)"
    - "After delete: live home QS tree still present and not a symlink (D-14/D-15) — retirement was repo-only"
    - "No phase08 smoke script created; phase07-live-smoke D-04 expected red if re-run — leave frozen (D-03/D-11/D-12)"
  artifacts:
    - path: ".config/quickshell"
      provides: "ABSENT after RET-01 — no longer shipped product tree"
      contains: "Path must not exist under REPO_ROOT"
    - path: "~/.config/quickshell/ii/shell.qml"
      provides: "Live product preserved (not the retirement target)"
      contains: "Still a real file under home after repo delete"
  key_links:
    - from: "08-01 live health green"
      to: "git rm -r .config/quickshell"
      via: "D-14 delete-after-verify; REPO_ROOT-scoped only"
    - from: "git commit tree delete"
      to: "RET-01 shipped absence"
      via: "D-09 atomic commit separate from installer delete"
  prohibitions:
    - "Must NOT delete or recursive-remove live $HOME/.config/quickshell as the retirement action (D-14 / T-8-01)"
    - "Must NOT re-symlink live QS path to any repo path (D-15 / T-8-03)"
    - "Must NOT run the retired local-product installer under arch/ (T-8-02)"
    - "Must NOT create scripts/phase08-*-smoke.sh or rewrite phase07-live-smoke.sh / phase04 asserts (D-03/D-11)"
    - "Must NOT salvage or commit WIP QML under the tree before delete (D-07/D-08)"
    - "Must NOT create an annotated recovery tag (D-10)"
    - "Must NOT also delete arch installer or do ref cleanup in this commit (D-09 — 08-03 owns those)"
    - "Must NOT uninstall packages (D-05)"
---

<objective>
Remove the entire in-repo v0.1 `.config/quickshell` product tree from `.dotfiles` with REPO-scoped `git rm -r`, discard untracked WIP, and atomic-commit — per RET-01 and D-07, D-08, D-09, D-10, D-14 — while proving the live home install remains intact.

Purpose: Stop shipping the dual product path. Live session continues from installed `~/.config/quickshell`.

Output: `.config/quickshell` absent under REPO_ROOT; one dedicated tree-delete commit; live ii path still healthy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-retire-local-quickshell-product/08-CONTEXT.md
@.planning/phases/08-retire-local-quickshell-product/08-RESEARCH.md
@.planning/phases/08-retire-local-quickshell-product/08-PATTERNS.md
@.planning/phases/08-retire-local-quickshell-product/08-VALIDATION.md
@.planning/phases/08-retire-local-quickshell-product/08-01-SUMMARY.md
</context>

## Phase Goal

**Single product path — remove the v0.1 in-repo Quickshell tree and old installer.** This plan delivers RET-01 (in-repo tree gone) only after 08-01 live health.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Live health green | host `~/.config/quickshell` | 08-01 |
| **In-repo product tree deleted (RET-01)** | `REPO_ROOT/.config/quickshell` **ABSENT** | **08-02** |
| Atomic tree-delete commit | git history | **08-02** |
| Old installer hard-deleted (RET-02) | `arch/quickshell.sh` gone | 08-03 |

## Non-gate notice (D-03 / D-11)

After this plan commits, `./scripts/phase07-live-smoke.sh` D-04 ("in-repo still present") is **expected red**. Do not rewrite that script. Do not create `scripts/phase08-*`. Use only the inline verifies in this plan.

## Tasks

<task type="auto">
  <name>Task 1: Pre-delete re-assert live independence (D-14)</name>
  <files>~/.config/quickshell, ~/.local/state/quickshell/.venv, .config/quickshell</files>
  <read_first>
    - .planning/phases/08-retire-local-quickshell-product/08-01-SUMMARY.md (must be health green)
    - .planning/phases/08-retire-local-quickshell-product/08-CONTEXT.md (D-07, D-08, D-09, D-14, D-15)
    - .planning/phases/08-retire-local-quickshell-product/08-RESEARCH.md (Pattern 1 delete-after-verify; Pitfall 1 symlink; Pitfall 2 home delete)
    - .planning/phases/08-retire-local-quickshell-product/08-PATTERNS.md (order pattern; never touch live home as retirement)
  </read_first>
  <action>
    Before any repo product deletion, re-prove live path independence (D-14; T-8-01/T-8-03):

    1. Require 08-01-SUMMARY reports live health green. If not, stop and return to 08-01 — do not delete.
    2. From REPO_ROOT, re-run hard live asserts:
       - `test ! -L "${HOME}/.config/quickshell"`
       - `test -d "${HOME}/.config/quickshell"`
       - `test -f "${HOME}/.config/quickshell/ii/shell.qml"`
       - `case "$(readlink -f "${HOME}/.config/quickshell")" in */.dotfiles/.config/quickshell*) echo "[FAIL]"; exit 1 ;; esac`
       - `test -d "${HOME}/.local/state/quickshell/.venv"`
    3. Confirm in-repo tree still present (delete target): `test -d "${REPO_ROOT}/.config/quickshell"`.
    4. Print clearly: retirement target is **REPO_ROOT/.config/quickshell only**; live home path is protected.
    5. Do not run home recursive remove. Do not run the retired arch installer. Do not re-symlink. Do not start `git rm` until Task 2.
  </action>
  <verify>
    <automated>set -euo pipefail; REPO_ROOT="$(git rev-parse --show-toplevel)"; test ! -L "${HOME}/.config/quickshell"; test -f "${HOME}/.config/quickshell/ii/shell.qml"; case "$(readlink -f "${HOME}/.config/quickshell")" in */.dotfiles/.config/quickshell*) exit 1 ;; esac; test -d "${REPO_ROOT}/.config/quickshell"; echo "[VERIFY] OK pre-delete live independent + repo tree present"</automated>
  </verify>
  <acceptance_criteria>
    - Live QS path is not a symlink and has `ii/shell.qml`.
    - Live path does not resolve under `*/.dotfiles/.config/quickshell*`.
    - In-repo `.config/quickshell` still exists as the delete target.
    - No home-tree deletion occurred in this task.
  </acceptance_criteria>
  <done>Pre-delete gate green; safe to REPO-scope git rm.</done>
</task>

<task type="auto">
  <name>Task 2: git rm -r in-repo tree; discard WIP; atomic RET-01 commit (D-07, D-08, D-09, D-10)</name>
  <files>.config/quickshell</files>
  <read_first>
    - .planning/phases/08-retire-local-quickshell-product/08-CONTEXT.md (D-07, D-08, D-09, D-10)
    - .planning/phases/08-retire-local-quickshell-product/08-RESEARCH.md (Safe tree removal Code Example; Pitfall 5 untracked leftovers)
    - .planning/phases/08-retire-local-quickshell-product/08-PATTERNS.md (tree delete pattern; REPO-only rm for remnants)
    - .planning/REQUIREMENTS.md (RET-01)
  </read_first>
  <action>
    Delete the in-repo product tree only (RET-01 / D-07 / D-08 / D-09 / D-10):

    1. `cd "$(git rev-parse --show-toplevel)"` — all paths relative to REPO_ROOT.
    2. Stage full tree removal: `git rm -r .config/quickshell` (tracked ~933 files; do not salvage).
    3. Untracked WIP leftovers (D-08): if path still exists after `git rm` (`[[ -e .config/quickshell ]]`), remove remnants with **REPO-scoped** `rm -rf .config/quickshell` only — never target `${HOME}/.config/quickshell` for this step.
    4. Assert: `test ! -e .config/quickshell` under REPO_ROOT.
    5. Atomic commit **only** the tree deletion (D-09). Do not stage installer deletion or comment cleanups in this commit. Suggested message: `chore(08): remove in-repo v0.1 .config/quickshell product tree (RET-01)`.
    6. Do **not** create an annotated recovery tag (D-10) — git history is enough.
    7. Do **not** run the retired local-product installer. Do not re-symlink live→repo. Do not rewrite historical smoke/assert scripts. Do not uninstall packages.
  </action>
  <verify>
    <automated>set -euo pipefail; REPO_ROOT="$(git rev-parse --show-toplevel)"; test ! -e "${REPO_ROOT}/.config/quickshell"; test ! -L "${HOME}/.config/quickshell"; test -f "${HOME}/.config/quickshell/ii/shell.qml"; git -C "$REPO_ROOT" log -1 --oneline | grep -qiE 'quickshell|RET-01|08'; echo "[VERIFY] OK RET-01 tree absent; live intact; commit present"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e "$(git rev-parse --show-toplevel)/.config/quickshell"` succeeds.
    - `git status` shows no remaining staged/untracked content under that path.
    - Latest commit message documents tree removal / RET-01 and does not also remove the arch installer file.
    - Live `${HOME}/.config/quickshell/ii/shell.qml` still exists.
    - Live path is still not a symlink.
    - No annotated tag was created for recovery.
  </acceptance_criteria>
  <done>RET-01 tree delete committed; live home product untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Post-delete live hold + document expected phase07 D-04 red</name>
  <files>~/.config/quickshell, ~/.local/state/quickshell/.venv, .config/quickshell, arch/quickshell.sh, .config/hypr/hyprland.conf</files>
  <read_first>
    - .planning/phases/08-retire-local-quickshell-product/08-VALIDATION.md (08-02 row; non-gates)
    - .planning/phases/08-retire-local-quickshell-product/08-RESEARCH.md (Post-RET asserts; Pitfall 4)
    - .planning/phases/08-retire-local-quickshell-product/08-PATTERNS.md (post-delete live hold)
  </read_first>
  <action>
    Prove RET-01 shipping absence without harming live product (D-14/D-15; T-8-01/T-8-03):

    1. Repo absence: `test ! -e "${REPO_ROOT}/.config/quickshell"`.
    2. Live hold: same LIVE-01 hard path asserts as Task 1 (not symlink; dir; `ii/shell.qml`; not under repo product; venv present).
    3. Soft dual-run (non-blocking): `grep -E 'exec-once = waybar' "${REPO_ROOT}/.config/hypr/hyprland.conf"` still true; optional `pgrep -x waybar`.
    4. Document in 08-02-SUMMARY:
       - RET-01 complete (in-repo tree gone).
       - Live path still independent.
       - `./scripts/phase07-live-smoke.sh` D-04 is now **expected red** — leave frozen; do not thrash (D-03/D-11/D-12).
       - Installer hard-delete remains 08-03 (D-09 separation).
    5. Do not delete the arch installer in this plan. Do not create phase08 smoke files.
  </action>
  <verify>
    <automated>set -euo pipefail; REPO_ROOT="$(git rev-parse --show-toplevel)"; test ! -e "${REPO_ROOT}/.config/quickshell"; test ! -L "${HOME}/.config/quickshell"; test -d "${HOME}/.config/quickshell"; test -f "${HOME}/.config/quickshell/ii/shell.qml"; case "$(readlink -f "${HOME}/.config/quickshell")" in */.dotfiles/.config/quickshell*) exit 1 ;; esac; test -d "${HOME}/.local/state/quickshell/.venv"; test -f "${REPO_ROOT}/arch/quickshell.sh"; test ! -e "${REPO_ROOT}/scripts/phase08-live-smoke.sh"; echo "[VERIFY] OK post-RET-01 live hold"</automated>
  </verify>
  <acceptance_criteria>
    - In-repo `.config/quickshell` is absent.
    - Live `ii/shell.qml` still present under home.
    - Live path is not a symlink and does not resolve under the deleted repo product path.
    - SUMMARY records expected phase07 D-04 red and no phase08 smoke file created.
    - Arch installer file still exists pending 08-03 (D-09 order).
  </acceptance_criteria>
  <done>RET-01 verified; live hold green; ready for 08-03 installer retirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REPO_ROOT `.config/quickshell` vs `$HOME/.config/quickshell` | Same relative name; delete must never cross into home |
| git index → working tree | Large staged deletion; untracked remnants need REPO-only cleanup |
| Post-delete session | qs must keep loading live `ii`, not missing repo path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-01 | Tampering / DoS | Accidental live home tree delete | high | mitigate | Only `git rm -r .config/quickshell` under REPO_ROOT; remnant `rm -rf` only when cwd is REPO and path is relative `.config/quickshell`; post-assert live `ii/shell.qml` exists |
| T-8-02 | Tampering | Running retired installer mid-delete | high | mitigate | Forbid execution; installer still present until 08-03 but must not be run |
| T-8-03 | Tampering | Re-symlink live → (now missing) repo | high | mitigate | Forbid re-symlink; post-delete `test ! -L` + live file present |
| T-8-04 | Tampering | "Fix" phase07 D-04 after tree gone | medium | accept | Document expected red; leave historical scripts; inline verifies only |
| T-8-05 | Tampering / info loss | Unplanned reinstall during delete | high | mitigate | No reinstall in this plan; health was 08-01 only |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | No registry packages (D-05) |
</threat_model>

<verification>
`test ! -e REPO/.config/quickshell`; live `! -L` + `ii/shell.qml` + not under repo; dedicated tree-delete commit; no phase08 smoke; phase07 D-04 expected red if re-run.
</verification>

<success_criteria>
RET-01 true: `.dotfiles` no longer ships the v0.1 `.config/quickshell` product tree. Live session path still real installed ii under home. Tree delete is its own commit. Ready for RET-02 in 08-03.
</success_criteria>

<output>
Create `.planning/phases/08-retire-local-quickshell-product/08-02-SUMMARY.md` when done
</output>
