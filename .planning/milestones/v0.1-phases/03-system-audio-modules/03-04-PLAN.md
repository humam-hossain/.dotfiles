---
phase: 03-system-audio-modules
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .config/quickshell/services/Audio.qml
  - .config/quickshell/modules/ii/screenCorners/ScreenCorners.qml
  - .config/hypr/hyprland.conf
autonomous: true
requirements:
  - BAR-08
must_haves:
  truths:
    - User-driven volume raise can reach 130% linear (maxVolume 1.30) via Audio.incrementVolume (D-22)
    - Changing volume while muted auto-unmutes sink (D-21) for bar scroll and external wpctl paths
    - Changing input volume while mic-muted auto-unmutes source (D-21 symmetry)
    - ScreenCorners volume raise path does not hard-cap at 100% (uses Audio.incrementVolume or maxVolume)
    - Hyprland XF86 raise bind uses wpctl -l 1.3 for keyboard boost parity (D-22)
  artifacts:
    - path: .config/quickshell/services/Audio.qml
      provides: "maxVolume 1.30, auto-unmute, clamp on inc/dec"
      contains: "maxVolume"
    - path: .config/quickshell/modules/ii/screenCorners/ScreenCorners.qml
      provides: "raise path aligned to 130%"
    - path: .config/hypr/hyprland.conf
      provides: "XF86AudioRaiseVolume -l 1.3"
  key_links:
    - from: Audio.incrementVolume / Connections onVolumeChanged
      to: sink.audio.muted / volume
      via: "explicit muted=false on volume change"
      pattern: "muted = false"
    - from: Hyprland XF86 raise
      to: wpctl set-volume -l 1.3
      via: "keyboard path ceiling"
      pattern: "-l 1.3"
  prohibitions:
    - Do not build a new volume OSD (D-24)
    - Do not add a dedicated volume module widget (D-17)
    - Do not raise hardMaxValue above 2.00 safety rail
    - Do not leave Math.min(1, ...) as the only raise clamp on user paths
---

<objective>
Productize Audio service for BAR-08: 130% ceiling (D-22), auto-unmute on any volume change including keyboard (D-21), and align ScreenCorners + Hyprland XF86 raise so no path silently caps at 100%.

Purpose: Today incrementVolume and ScreenCorners use Math.min(1,…); wpctl while muted leaves MUTED; keyboard bind is -l 1 — all fight D-21/D-22.
Output: Audio.qml + ScreenCorners.qml + hyprland.conf raise bind updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-system-audio-modules/03-CONTEXT.md
@.planning/phases/03-system-audio-modules/03-RESEARCH.md
@.planning/phases/03-system-audio-modules/03-PATTERNS.md
@.config/quickshell/services/Audio.qml
@.config/quickshell/modules/ii/screenCorners/ScreenCorners.qml
@.config/hypr/hyprland.conf
</context>

## Artifacts this phase produces (this plan)

| Symbol | Kind | Path |
|--------|------|------|
| `maxVolume` | readonly real 1.30 | Audio.qml |
| auto-unmute on sink volume change | Connections logic | Audio.qml |
| auto-unmute on source volume change | Connections logic | Audio.qml |
| `incrementVolume` / `decrementVolume` unmute | functions | Audio.qml |
| XF86 raise `-l 1.3` | hypr bind | hyprland.conf |

## Decisions locked

- D-21 auto-unmute any volume path (scroll + keyboard via Connections)
- D-22 max 1.30; keep hardMaxValue 2.00
- D-24 no new OSD
- Hyprland `-l 1.3` YES (binds live in this repo — open question closed)

<tasks>

<task type="auto" id="03-04-01" tdd="false">
  <name>Task 1: Audio maxVolume 1.30 + auto-unmute + inc/dec clamp</name>
  <files>.config/quickshell/services/Audio.qml</files>
  <read_first>
    - .config/quickshell/services/Audio.qml (full)
    - .planning/phases/03-system-audio-modules/03-RESEARCH.md (auto-unmute + 130% section; Pitfall 4/5)
    - .planning/phases/03-system-audio-modules/03-CONTEXT.md (D-21, D-22, D-24)
  </read_first>
  <action>
    Edit `.config/quickshell/services/Audio.qml` only:

    1. ADD `readonly property real maxVolume: 1.30` (D-22). Keep `hardMaxValue: 2.00`.

    2. Rewrite `incrementVolume`:
       - If sink.audio.muted, set muted false first (D-21 scroll path).
       - Step logic: keep existing small-step when volume &lt; 0.1 else 0.02.
       - Set volume to Math.min(maxVolume, current + step) — not Math.min(1, …).

    3. Rewrite `decrementVolume`:
       - If muted, unmute first (volume change while muted).
       - Subtract step; clamp floor at 0 if not already.

    4. Auto-unmute for external paths (keyboard wpctl): extend Connections on sink.audio:
       - Outside or after protection block: if sink.audio.muted and volume actually changed from lastVolume, set muted false (D-21). Track lastVolume even when protection.enable is false (protection currently early-returns — auto-unmute must NOT be gated solely on protection.enable).
       - Prefer a dedicated Connections or restructure so auto-unmute always runs.

    5. Symmetric source auto-unmute: Connections on source.audio onVolumeChanged — if muted and volume changed, muted false (D-21 mic).

    6. Do not implement openMixer here (BarContent plan 03-07). Do not build OSD.

    Note: protection.maxAllowed dual-write is plan 03-02; if protection later enabled, maxAllowed 130 prevents fighting boost.
  </action>
  <acceptance_criteria>
    - Audio.qml declares maxVolume with value 1.30 (or 1.3)
    - incrementVolume does not use Math.min(1, as the raise cap; uses maxVolume
    - Source contains muted = false (or muted = false assignment) in volume-change paths (inc/dec and/or Connections)
    - Auto-unmute logic is not exclusively inside `if (!Config.options.audio.protection.enable) return` early-exit path
    - hardMaxValue remains 2.00
    - No new OnScreenDisplay QML file created
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'maxVolume|1\.3|Math\.min\(1|muted = false|incrementVolume|decrementVolume|onVolumeChanged|hardMaxValue' .config/quickshell/services/Audio.qml</automated>
  </verify>
  <done>Audio service supports 130% boost and auto-unmute for UI and external volume changes.</done>
</task>

<task type="auto" id="03-04-02" tdd="false">
  <name>Task 2: Align ScreenCorners and Hyprland XF86 raise to 130%</name>
  <files>.config/quickshell/modules/ii/screenCorners/ScreenCorners.qml, .config/hypr/hyprland.conf</files>
  <read_first>
    - .config/quickshell/modules/ii/screenCorners/ScreenCorners.qml (onScrollUp volume path ~L107-116)
    - .config/hypr/hyprland.conf (XF86AudioRaiseVolume ~L418)
    - .config/quickshell/services/Audio.qml (incrementVolume after task 1)
  </read_first>
  <action>
    1. ScreenCorners.qml right-corner onScrollUp: replace direct `Audio.sink.audio.volume = Math.min(1, …)` with call to `Audio.incrementVolume()` (preferred — inherits unmute + maxVolume). If call is impractical, use Math.min(Audio.maxVolume, …) and unmute explicitly. Same spirit for onScrollDown: prefer Audio.decrementVolume().

    2. Hyprland: change raise bind from `wpctl set-volume -l 1` to `wpctl set-volume -l 1.3` on XF86AudioRaiseVolume (D-22 keyboard parity). Leave lower/mute/mic-mute binds unchanged.

    3. Grep for other user-facing `Math.min(1, Audio.sink` raise paths under `.config/quickshell` and fix any remaining that would block 130% for interactive raise (do not rewrite every volume display). Document any residual in SUMMARY if found outside ScreenCorners/Audio.

    Do not enable cornerOpen brightness paths; no ddcutil.
  </action>
  <acceptance_criteria>
    - ScreenCorners.qml onScrollUp volume path does not contain Math.min(1, Audio.sink (or equivalent 100% hard cap)
    - Either ScreenCorners calls Audio.incrementVolume or uses Audio.maxVolume
    - `rg -n 'XF86AudioRaiseVolume' .config/hypr/hyprland.conf` shows `-l 1.3`
    - Lower/mute binds still present
    - `timeout 4 quickshell 2>&amp;1 | rg 'Configuration Loaded|Error'` shows Configuration Loaded
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'incrementVolume|maxVolume|Math\.min\(1' .config/quickshell/modules/ii/screenCorners/ScreenCorners.qml; rg -n 'XF86AudioRaiseVolume|set-volume' .config/hypr/hyprland.conf; timeout 4 quickshell 2&gt;&amp;1 | rg 'Configuration Loaded|Error' || true</automated>
  </verify>
  <done>All primary user raise paths allow 130%; keyboard bind matches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PipeWire session | Local volume/mute; user session only |
| Hyprland binds | Local keyboard → wpctl |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | volume ceiling | high | mitigate | maxVolume 1.30 + hardMaxValue 2.00; no path leaves unlimited raise |
| T-03-01 | Tampering | protection maxAllowed | medium | mitigate | Dual-write in 03-02; this plan hardcodes UI maxVolume independently |
| T-03-02 | Injection | N/A this plan | low | accept | No shell command construction |
| T-03-SC | Tampering | packages | high | accept | No installs |
</threat_model>

<verification>
- Static gates on maxVolume, muted=false, -l 1.3, no Math.min(1 raise in ScreenCorners
- Smoke Configuration Loaded
</verification>

<success_criteria>
- Scroll and keyboard can boost to ~130%
- Volume change while muted unmutes
- Stock OSD remains (no new OSD)
</success_criteria>

<output>
Create `.planning/phases/03-system-audio-modules/03-04-SUMMARY.md` when done
</output>
