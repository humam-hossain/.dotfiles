---
phase: 03-system-audio-modules
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - .config/quickshell/modules/common/Config.qml
  - /home/pera/.config/illogical-impulse/config.json
autonomous: true
requirements:
  - BAR-05
  - BAR-06
  - BAR-07
  - BAR-08
must_haves:
  truths:
    - Config.qml and live config.json both encode CPU thresholds 40/80, RAM 75/95, disk 80/95 (D-07, D-13)
    - alwaysShowCpu true and alwaysShowSwap false dual-written (D-04, D-05)
    - Poll intervals dual-written: CPU 1000 ms, memory 3000 ms, disk 10000 ms (D-08, D-14)
    - audio.protection.maxAllowed is 130 with enable left false (D-22)
    - python3 scripts/phase03-config-assert.py exits 0
  artifacts:
    - path: .config/quickshell/modules/common/Config.qml
      provides: "bar.resources dual thresholds + resources.* intervals + audio.protection.maxAllowed 130"
      contains: "cpuErrorThreshold"
    - path: /home/pera/.config/illogical-impulse/config.json
      provides: "Live dual-write of Phase 3 keys"
  key_links:
    - from: Config.qml bar.resources / resources / audio.protection
      to: ~/.config/illogical-impulse/config.json
      via: "dual-write same keys so FileView cannot stale-win"
      pattern: "cpuWarningThreshold"
  prohibitions:
    - Do not dual-write only Config.qml without live JSON
    - Do not enable audio.protection.enable
    - Do not change volumeMixer command string
    - Do not add brightness/ddcutil config
---

<objective>
Dual-write Config.qml defaults and live `~/.config/illogical-impulse/config.json` for Phase 3 resource thresholds, always-show flags, multi-rate intervals, and audio maxAllowed 130 so runtime matches CONTEXT decisions D-04..D-08, D-13, D-14, D-22.

Purpose: Persisted JSON currently holds cpuWarning 90, mem 95, interval 3000, maxAllowed 99 — without dual-write the bar never sees Phase 3 thresholds (T-03-01).
Output: Updated Config.qml + live config.json; phase03-config-assert.py green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-system-audio-modules/03-CONTEXT.md
@.planning/phases/03-system-audio-modules/03-RESEARCH.md
@.planning/phases/03-system-audio-modules/03-PATTERNS.md
@.config/quickshell/modules/common/Config.qml
@scripts/phase03-config-assert.py
@/home/pera/.config/illogical-impulse/config.json
</context>

## Artifacts this phase produces (this plan)

| Symbol / artifact | Kind | Path |
|-------------------|------|------|
| `cpuErrorThreshold` | Config property | `Config.options.bar.resources` |
| `memoryErrorThreshold` | Config property | `Config.options.bar.resources` |
| `diskWarningThreshold` | Config property | `Config.options.bar.resources` |
| `diskErrorThreshold` | Config property | `Config.options.bar.resources` |
| `memoryUpdateInterval` | Config property | `Config.options.resources` |
| `diskUpdateInterval` | Config property | `Config.options.resources` |
| `audio.protection.maxAllowed: 130` | Config default + live | Config.qml + config.json |

## Decisions locked

- D-04 alwaysShowSwap false; D-05 alwaysShowCpu true
- D-07 CPU 40/80, RAM 75/95; D-13 disk 80/95
- D-08/D-14 split intervals dual-written (not counter-only)
- D-22 maxAllowed 130; protection enable stays false

<tasks>

<task type="auto" id="03-02-01">
  <name>Task 1: Update Config.qml Phase 3 defaults</name>
  <files>.config/quickshell/modules/common/Config.qml</files>
  <read_first>
    - .config/quickshell/modules/common/Config.qml (audio.protection ~L145-151; bar.resources ~L246-252; resources ~L461-464)
    - .planning/phases/03-system-audio-modules/03-CONTEXT.md (D-04..D-08, D-13, D-14, D-22)
    - .planning/phases/03-system-audio-modules/03-PATTERNS.md (Config.qml section)
    - scripts/phase03-config-assert.py (exact assert keys)
  </read_first>
  <action>
    Edit only `.config/quickshell/modules/common/Config.qml` defaults:

    1. In `property JsonObject audio` → `protection`:
       - Set `maxAllowed: 130` (D-22). Keep `enable: false` and `maxAllowedIncrease: 10`.

    2. In `property JsonObject bar` → `resources`:
       - `alwaysShowSwap: false` (D-04)
       - `alwaysShowCpu: true` (D-05)
       - `cpuWarningThreshold: 40` (D-07)
       - ADD `cpuErrorThreshold: 80` (D-07)
       - `memoryWarningThreshold: 75` (D-07)
       - ADD `memoryErrorThreshold: 95` (D-07)
       - Keep `swapWarningThreshold` as-is for service/history (not bar UI)
       - ADD `diskWarningThreshold: 80` (D-13)
       - ADD `diskErrorThreshold: 95` (D-13)

    3. In top-level `property JsonObject resources` (poll block ~L461, NOT bar.resources):
       - `updateInterval: 1000` (CPU feel D-08)
       - ADD `memoryUpdateInterval: 3000` (D-08)
       - ADD `diskUpdateInterval: 10000` (D-14)
       - Keep `historyLength: 60`

    4. Do not change `apps.volumeMixer`, workspaces shown, tray, weather, or time keys.
  </action>
  <acceptance_criteria>
    - `rg -n 'cpuWarningThreshold:|cpuErrorThreshold:|memoryWarningThreshold:|memoryErrorThreshold:|diskWarningThreshold:|diskErrorThreshold:' .config/quickshell/modules/common/Config.qml` shows 40, 80, 75, 95, 80, 95 respectively
    - `alwaysShowSwap: false` and `alwaysShowCpu: true` in bar.resources
    - `updateInterval: 1000` and `memoryUpdateInterval: 3000` and `diskUpdateInterval: 10000` in resources JsonObject
    - `maxAllowed: 130` under audio.protection; enable remains false
    - volumeMixer string unchanged
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'cpuWarningThreshold:|cpuErrorThreshold:|memoryWarningThreshold:|memoryErrorThreshold:|diskWarningThreshold:|diskErrorThreshold:|alwaysShowSwap:|alwaysShowCpu:|updateInterval:|memoryUpdateInterval:|diskUpdateInterval:|maxAllowed:' .config/quickshell/modules/common/Config.qml</automated>
  </verify>
  <done>Config.qml defaults encode all Phase 3 dual-write keys.</done>
</task>

<task type="auto" id="03-02-02">
  <name>Task 2: Dual-write live config.json and prove assert green</name>
  <files>/home/pera/.config/illogical-impulse/config.json</files>
  <read_first>
    - /home/pera/.config/illogical-impulse/config.json (current bar.resources, resources, audio)
    - .config/quickshell/modules/common/Config.qml (values just set)
    - scripts/phase03-config-assert.py
  </read_first>
  <action>
    Dual-write live host config (Phase 1/2 learning — persisted JSON wins):

    1. Load `~/.config/illogical-impulse/config.json` with Python json.
    2. Merge keys (preserve all other structure):
       - bar.resources.alwaysShowCpu = true
       - bar.resources.alwaysShowSwap = false
       - bar.resources.cpuWarningThreshold = 40
       - bar.resources.cpuErrorThreshold = 80
       - bar.resources.memoryWarningThreshold = 75
       - bar.resources.memoryErrorThreshold = 95
       - bar.resources.diskWarningThreshold = 80
       - bar.resources.diskErrorThreshold = 95
       - resources.updateInterval = 1000
       - resources.memoryUpdateInterval = 3000
       - resources.diskUpdateInterval = 10000
       - audio.protection.maxAllowed = 130
       - leave audio.protection.enable false
    3. Write JSON with 2-space indent + trailing newline; re-load to validate.
    4. Run `python3 scripts/phase03-config-assert.py` — must print `config asserts OK` and exit 0.
    5. Optional smoke: `timeout 4 quickshell 2>&1 | rg 'Configuration Loaded|Error'`.

    Do not change volumeMixer, weather, workspaces.shown (keep UAT value 4), or brightness keys.
  </action>
  <acceptance_criteria>
    - `python3 scripts/phase03-config-assert.py` exits 0 and prints `config asserts OK`
    - Live JSON bar.resources thresholds match 40/80, 75/95, 80/95
    - Live JSON resources intervals are 1000, 3000, 10000
    - Live JSON audio.protection.maxAllowed is 130
    - Live JSON alwaysShowSwap is false
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/phase03-config-assert.py</automated>
  </verify>
  <done>Live config dual-written; Wave 0 assert green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config.qml defaults → live JSON | Trusted dual-write path; JSON overrides QML at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | live thresholds / maxAllowed | high | mitigate | Dual-write known defaults + assert green |
| T-03-03 | DoS | disk interval | medium | mitigate | diskUpdateInterval 10000 dual-written |
| T-03-04 | Tampering | volume max | high | mitigate | maxAllowed 130 dual-written; enable false so protection does not fight boost |
| T-03-02 | Injection | volumeMixer | medium | accept | volumeMixer string not modified (existing trusted path) |
| T-03-05 | Info disclosure | disk paths | low | accept | No tooltip config added |
| T-03-SC | Tampering | package installs | high | accept | No package installs |
</threat_model>

<verification>
- phase03-config-assert.py exit 0
- Config.qml and live JSON key parity for Phase 3 keys
</verification>

<success_criteria>
- All Phase 3 config predicates dual-written and asserted
- Runtime will load new thresholds/intervals/maxAllowed on next quickshell start
</success_criteria>

<output>
Create `.planning/phases/03-system-audio-modules/03-02-SUMMARY.md` when done
</output>
