---
phase: 03-system-audio-modules
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .config/quickshell/modules/ii/bar/Resource.qml
autonomous: true
requirements:
  - BAR-05
  - BAR-06
  - BAR-07
must_haves:
  truths:
    - Resource rings support dual thresholds: warning tier then error tier (D-07, D-13)
    - Warning tier uses Appearance.colors.colPrimary; error tier uses Appearance.colors.colError
    - Resource can display custom labelText (capacity strings) instead of bare integer percent
    - CPU-style percent labels can show a trailing percent sign via labelText or built-in format
    - TextMetrics width accommodates capacity labels like 99.9/99.9 GB without clipping
  artifacts:
    - path: .config/quickshell/modules/ii/bar/Resource.qml
      provides: "errorThreshold, labelText, dual-color ladder, flexible TextMetrics"
      contains: "errorThreshold"
  key_links:
    - from: Resource.qml percentage / thresholds
      to: ClippedFilledCircularProgress.colPrimary
      via: "isError / isWarning color ladder"
      pattern: "errorThreshold"
  prohibitions:
    - Do not invent a new progress component — keep ClippedFilledCircularProgress
    - Do not add click handlers that open popups on Resource
    - Do not hardcode CPU/RAM/Disk values inside Resource.qml
---

<objective>
Extend `Resource.qml` with dual-threshold ring colors and flexible labels so Resources strip can render CPU `%`, RAM used/total, and Disk free/total with warning vs error recolor (D-01, D-02, D-03, D-07, D-10, D-13).

Purpose: Current Resource only has single warningThreshold → colError and fixed 0–100 integer text — cannot meet Phase 3 label or dual-threshold UX.
Output: Productized Resource.qml component ready for 03-06 wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-system-audio-modules/03-CONTEXT.md
@.planning/phases/03-system-audio-modules/03-RESEARCH.md
@.planning/phases/03-system-audio-modules/03-PATTERNS.md
@.config/quickshell/modules/ii/bar/Resource.qml
</context>

## Artifacts this phase produces (this plan)

| Symbol | Kind | Path |
|--------|------|------|
| `errorThreshold` | property int | Resource.qml |
| `labelText` | property string | Resource.qml |
| `isError` / `isWarning` | readonly bool | Resource.qml |
| dual-tier `colPrimary` bind | binding | ClippedFilledCircularProgress |

## Decisions / discretion locked

- D-01 keep ii rings
- Warning color token: `Appearance.colors.colPrimary` (no colWarning exists; RESEARCH A1)
- Error color: `Appearance.colors.colError`
- Empty `labelText` → default percent display with `%` suffix for CPU use (D-02); when set, show labelText as-is (RAM/Disk)

<tasks>

<task type="auto" id="03-03-01" tdd="false">
  <name>Task 1: Dual-threshold color ladder on Resource ring</name>
  <files>.config/quickshell/modules/ii/bar/Resource.qml</files>
  <read_first>
    - .config/quickshell/modules/ii/bar/Resource.qml (full file)
    - .planning/phases/03-system-audio-modules/03-PATTERNS.md (Resource.qml dual-threshold excerpt)
    - .planning/phases/03-system-audio-modules/03-CONTEXT.md (D-07, D-13)
  </read_first>
  <action>
    Extend `Resource.qml` threshold/color logic (per D-07):

    1. Keep `property int warningThreshold: 100`.
    2. ADD `property int errorThreshold: 100` (default 100 means error tier off unless parent sets lower).
    3. Replace single `property bool warning` with:
       - `readonly property real pct: percentage * 100`
       - `readonly property bool isError: pct &gt;= errorThreshold`
       - `readonly property bool isWarning: !isError &amp;&amp; pct &gt;= warningThreshold`
    4. Bind `ClippedFilledCircularProgress.colPrimary` to ladder:
       - isError → Appearance.colors.colError
       - isWarning → Appearance.colors.colPrimary
       - else → Appearance.colors.colOnSecondaryContainer
    5. Bind `accountForLightBleeding` to `!(isError || isWarning)` (or equivalent).
    6. Keep MouseArea acceptedButtons Qt.NoButton (no click productization on Resource itself).

    Do not edit Resources.qml or Config.qml in this plan.
  </action>
  <acceptance_criteria>
    - Resource.qml declares `property int errorThreshold`
    - Source contains isError and isWarning (or equivalent dual-tier names) derived from percentage and both thresholds
    - colPrimary assignment references both colError and colPrimary (warning tier) and colOnSecondaryContainer (normal)
    - No single-branch `warning ? colError : colOnSecondaryContainer` remains as the only color path
    - MouseArea still has acceptedButtons Qt.NoButton
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'errorThreshold|isError|isWarning|colError|colPrimary|colOnSecondaryContainer|warningThreshold' .config/quickshell/modules/ii/bar/Resource.qml</automated>
  </verify>
  <done>Resource rings support two-step warning/error recolor.</done>
</task>

<task type="auto" id="03-03-02" tdd="false">
  <name>Task 2: labelText + TextMetrics for capacity strings</name>
  <files>.config/quickshell/modules/ii/bar/Resource.qml</files>
  <read_first>
    - .config/quickshell/modules/ii/bar/Resource.qml (after task 1)
    - .planning/phases/03-system-audio-modules/03-PATTERNS.md (TextMetrics footgun section)
    - .planning/phases/03-system-audio-modules/03-CONTEXT.md (D-02, D-03, D-10, D-15)
  </read_first>
  <action>
    Extend label rendering in the same Resource.qml:

    1. ADD `property string labelText: ""` — when non-empty, StyledText displays labelText; when empty, display percent with trailing `%` (e.g. Math.round(percentage*100) + "%") for D-02 CPU default.
    2. Fix TextMetrics width footgun: metrics text must not stay fixed at `"100"`. Use either:
       - `text: labelText.length &gt; 0 ? labelText : "100%"` or
       - a wide sample string such as `"99.9/99.9 GB"` when labelText empty is insufficient for capacity parents — prefer binding metrics to the actual displayed string so layout matches content.
    3. Keep font.pixelSize Appearance.font.pixelSize.small and existing layout structure.
    4. Do not change iconName / percentage required props.

    Vertical bar Resource copy is out of scope unless trivial; horizontal ii bar is success surface.
  </action>
  <acceptance_criteria>
    - Resource.qml declares `property string labelText`
    - StyledText text expression uses labelText when set; otherwise percent with `%` character
    - TextMetrics text is not the bare three-digit sample alone without capacity/label awareness (must reference labelText or a capacity-width sample)
    - `timeout 4 quickshell 2>&amp;1 | rg 'Configuration Loaded|Error'` still shows Configuration Loaded (no hard error from Resource API change — Resources.qml still compiles with old call sites until 03-06)
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'labelText|TextMetrics|%|percentageText' .config/quickshell/modules/ii/bar/Resource.qml &amp;&amp; timeout 4 quickshell 2&gt;&amp;1 | rg 'Configuration Loaded|Error' || true</automated>
  </verify>
  <done>Resource supports custom capacity labels and percent-with-suffix default without clipping metrics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parent → Resource props | Thresholds and percentage are local desktop metrics; no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | threshold props | medium | mitigate | Thresholds come from dual-written Config (03-02); Resource only renders |
| T-03-05 | Info disclosure | labels | low | mitigate | labelText is capacity numbers only; no mount path strings in Resource |
| T-03-SC | Tampering | packages | high | accept | No installs |
</threat_model>

<verification>
- Static rg for errorThreshold, labelText, dual colors
- Smoke Configuration Loaded
</verification>

<success_criteria>
- Resource.qml ready for CPU/RAM/Disk parents with dual thresholds and flexible labels
</success_criteria>

<output>
Create `.planning/phases/03-system-audio-modules/03-03-SUMMARY.md` when done
</output>
