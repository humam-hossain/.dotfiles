---
phase: 02-core-bar-modules
plan: 06
type: execute
wave: 6
gap_closure: true
gap_ids: [G-02-4]
depends_on:
  - "02-05"
files_modified:
  - .config/quickshell/modules/ii/bar/StyledPopup.qml
  - .config/quickshell/modules/ii/bar/ClockWidget.qml
autonomous: true
requirements:
  - BAR-02
must_haves:
  truths:
    - Clicking the bar clock opens (or pins) ClockWidgetPopup with date/uptime/todos
    - Hover still shows the popup when unpinned
    - Popup is not Google Calendar and still uses ClockWidgetPopup content
  artifacts:
    - path: .config/quickshell/modules/ii/bar/StyledPopup.qml
      provides: "pin/forceActive path so popup can stay open without hover"
      contains: "forceActive"
    - path: .config/quickshell/modules/ii/bar/ClockWidget.qml
      provides: "onClicked toggles popup pin"
      contains: "onClicked"
  key_links:
    - from: ClockWidget MouseArea
      to: ClockWidgetPopup / StyledPopup
      via: "click toggles forceActive; hover still uses containsMouse"
  prohibitions:
    - Do not open external calendar URLs
    - Do not remove hover behavior entirely
    - Do not break other StyledPopup consumers (Battery, Resources, Weather) — forceActive default false
---

<objective>
Close G-02-4: make clock **click** open ClockWidgetPopup (date/uptime/todos), while keeping hover.

Purpose: UAT found hover works but click is a no-op because StyledPopup is hover-only.
Output: Clickable clock popup path via optional pin on StyledPopup + ClockWidget onClicked.
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-bar-modules/02-UAT.md
@.planning/debug/clock-click-no-op.md
@.config/quickshell/modules/ii/bar/StyledPopup.qml
@.config/quickshell/modules/ii/bar/ClockWidget.qml
@.config/quickshell/modules/ii/bar/ClockWidgetPopup.qml
</context>

<tasks>

<task type="auto" id="02-06-01">
  <name>Task 1: Add forceActive pin to StyledPopup</name>
  <files>.config/quickshell/modules/ii/bar/StyledPopup.qml</files>
  <read_first>
    - .config/quickshell/modules/ii/bar/StyledPopup.qml
    - .planning/debug/clock-click-no-op.md
  </read_first>
  <action>
    In StyledPopup.qml:

    1. Add `property bool forceActive: false` (default false so Battery/Resources/Weather unchanged).
    2. Change activation from:
       `active: hoverTarget && hoverTarget.containsMouse`
       to:
       `active: forceActive || (hoverTarget && hoverTarget.containsMouse)`
    3. Do not change layout, margins, or PanelWindow structure.

    Other consumers must keep working with default forceActive false.
  </action>
  <acceptance_criteria>
    - StyledPopup.qml defines `property bool forceActive`
    - `active:` expression includes both `forceActive` and `containsMouse`
    - Default forceActive is false
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'forceActive|active:' .config/quickshell/modules/ii/bar/StyledPopup.qml</automated>
  </verify>
  <done>StyledPopup can stay open when forceActive is true without hover.</done>
</task>

<task type="auto" id="02-06-02">
  <name>Task 2: Wire ClockWidget click to toggle popup pin</name>
  <files>.config/quickshell/modules/ii/bar/ClockWidget.qml</files>
  <read_first>
    - .config/quickshell/modules/ii/bar/ClockWidget.qml
    - .config/quickshell/modules/ii/bar/ClockWidgetPopup.qml
    - .config/quickshell/modules/ii/bar/StyledPopup.qml (after task 1)
  </read_first>
  <action>
    In ClockWidget.qml MouseArea:

    1. Give ClockWidgetPopup an id (e.g. `clockPopup`).
    2. On `onClicked`: toggle `clockPopup.forceActive = !clockPopup.forceActive`.
    3. Keep `hoverTarget: mouseArea` and existing hoverEnabled logic.
    4. Optional polish: when mouse leaves and forceActive is false, behavior unchanged; when forceActive true, popup stays until second click.

    Do not change clock text format or showDate binding.
  </action>
  <acceptance_criteria>
    - ClockWidget.qml contains onClicked
    - ClockWidgetPopup (or alias) sets/toggles forceActive
    - hoverTarget still bound to mouseArea
    - `timeout 4 quickshell 2>&1 | rg -c 'Configuration Loaded'` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'onClicked|forceActive|hoverTarget|ClockWidgetPopup' .config/quickshell/modules/ii/bar/ClockWidget.qml; timeout 4 quickshell 2>&amp;1 | rg 'Configuration Loaded|Error|ReferenceError' | head -15</automated>
  </verify>
  <done>Click toggles clock popup open; hover still works; smoke green.</done>
</task>

</tasks>

<success_criteria>
- G-02-4 closable: click opens clock popup with date/uptime/todos
- Hover path preserved
- No Google Calendar URL
</success_criteria>
