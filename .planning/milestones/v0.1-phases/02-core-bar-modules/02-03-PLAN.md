---
phase: 02-core-bar-modules
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - .config/quickshell/modules/ii/bar/BarContent.qml
autonomous: true
requirements:
  - BAR-01
  - BAR-02
must_haves:
  truths:
    - Left region visual L→R is LeftSidebarButton → ActiveWindow → Workspaces → Resources (D-15 left, D-17)
    - Center region is ClockWidget then UtilButtons only (D-15 center); weather not enabled (D-16)
    - ClockWidget call site forces showDate false so longDate does not duplicate the format string (D-08)
    - Workspaces component remains the stock ii widget (not rebuilt); only reparented for BAR-01 placement
    - Non-core modules LeftSidebar, ActiveWindow, Resources, UtilButtons remain present (D-17 rearrange-only)
  artifacts:
    - path: .config/quickshell/modules/ii/bar/BarContent.qml
      provides: "Left + center regions rewired to D-15; ClockWidget showDate false"
      contains: "showDate: false"
  key_links:
    - from: BarContent.qml leftSectionRowLayout
      to: Workspaces.qml
      via: "Workspaces child reparented into left RowLayout after ActiveWindow"
      pattern: "Workspaces"
    - from: BarContent.qml ClockWidget
      to: DateTime.time
      via: "showDate false; DateTime.time uses Config time.format from plan 02-02"
      pattern: "showDate: false"
  prohibitions:
    - Do not strip LeftSidebar, ActiveWindow, Resources, or UtilButtons
    - Do not enable bar.weather / WeatherBar for Phase 2
    - Do not open external calendar URLs from the clock
    - Do not reintroduce plugin workspace dispatchers
    - Do not move Media/Battery/SysTray/Indicators in this plan (owned by 02-04)
---

<objective>
Rewire BarContent.qml left and center regions to the user-locked D-15 layout: workspaces on the left after ActiveWindow (BAR-01 placement), clock+util center with showDate forced false (BAR-02 / D-08), weather remaining off (D-16), non-core modules rearranged not stripped (D-17).

Purpose: Stock ii puts Workspaces in center and clock in right-center; CONTEXT locks a different L→R contract.
Output: Updated leftSectionRowLayout + simplified middleSection in BarContent.qml.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-bar-modules/02-CONTEXT.md
@.planning/phases/02-core-bar-modules/02-RESEARCH.md
@.planning/phases/02-core-bar-modules/02-PATTERNS.md
@.config/quickshell/modules/ii/bar/BarContent.qml
@.config/quickshell/modules/ii/bar/ClockWidget.qml
@.config/quickshell/modules/ii/bar/Workspaces.qml
</context>

<tasks>

<task type="auto" id="02-03-01">
  <name>Task 1: Reparent Workspaces and Resources into left region</name>
  <files>.config/quickshell/modules/ii/bar/BarContent.qml</files>
  <read_first>
    - .config/quickshell/modules/ii/bar/BarContent.qml (leftSectionRowLayout ~L80–99; leftCenterGroup Resources/Media ~L111–125; middleCenterGroup Workspaces ~L131–151)
    - .planning/phases/02-core-bar-modules/02-PATTERNS.md (left target order)
    - .planning/phases/02-core-bar-modules/02-CONTEXT.md (D-15 table left row; D-17)
  </read_first>
  <action>
    Edit `BarContent.qml` left + remove Workspaces/Resources from their current center homes (per D-15 left, D-17):

    Target leftSectionRowLayout child order (declaration = visual LTR):
    1. LeftSidebarButton (keep id leftSidebarButton and existing margins/hover colors)
    2. ActiveWindow (keep fillWidth, shortened visibility, margins)
    3. Workspaces (move the existing Workspaces block including id workspacesWidget and its right-click overview MouseArea from middleCenterGroup)
    4. Resources (move from leftCenterGroup; keep alwaysShowAllResources binding)

    Rules:
    - Reparent existing component instances — do not create parallel copies of Workspaces.qml/Resources.qml files.
    - Preserve brightness scroll / left sidebar click behavior on barLeftSideMouseArea.
    - After move, leftCenterGroup must not still declare Resources (will be emptied or removed in task 2 with Media).
    - Do not change Workspaces.qml dispatch logic (D-04 already stock from Phase 1).
    - Do not touch rightSectionRowLayout yet (plan 02-04).

    If middleCenterGroup becomes empty after removing Workspaces, leave a temporary empty group only if required for compile; task 2 will rebuild center cleanly.
  </action>
  <acceptance_criteria>
    - In `leftSectionRowLayout`, source order of type names is LeftSidebarButton, then ActiveWindow, then Workspaces, then Resources (allow properties/whitespace between)
    - `Workspaces {` with `id: workspacesWidget` appears under left section, not under middleCenterGroup
    - No second `Workspaces {` instance remains elsewhere in BarContent.qml (`rg -c 'Workspaces {'` == 1)
    - No second `Resources {` instance (`rg -c 'Resources {'` == 1)
    - LeftSidebarButton and ActiveWindow still present
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'LeftSidebarButton|ActiveWindow|Workspaces|Resources|leftSectionRowLayout|middleCenterGroup|leftCenterGroup' .config/quickshell/modules/ii/bar/BarContent.qml | head -50</automated>
  </verify>
  <done>Left region matches D-15 order; Workspaces and Resources reparented once each.</done>
</task>

<task type="auto" id="02-03-02">
  <name>Task 2: Simplify center to ClockWidget + UtilButtons with showDate false</name>
  <files>.config/quickshell/modules/ii/bar/BarContent.qml</files>
  <read_first>
    - .config/quickshell/modules/ii/bar/BarContent.qml (middleSection ~L102–188 after task 1)
    - .config/quickshell/modules/ii/bar/ClockWidget.qml (showDate property + ClockWidgetPopup)
    - .planning/phases/02-core-bar-modules/02-CONTEXT.md (D-08, D-15 center, D-16)
    - .planning/phases/02-core-bar-modules/02-RESEARCH.md (Pitfall 3 date duplication; center sketch)
  </read_first>
  <action>
    Rebuild middleSection for D-15 center + D-08 + D-16:

    1. Center content must be ClockWidget then UtilButtons only (visual L→R). Prefer a single BarGroup (or equivalent Row/BarGroup) centered via existing middleSection horizontalCenter anchors.
    2. ClockWidget call site MUST set `showDate: false` (D-08) — do not use the verbose/shortened expression that currently enables longDate beside the clock once format already includes date parts.
    3. Keep UtilButtons with its existing visibility gate on bar.verbose and useShortenedForm === 0 (or equivalent stock gate) so D-17 keeps the module present.
    4. Remove from center: Workspaces (already moved), Resources (moved), Media (will live on right in 02-04 — temporarily park Media by leaving it only if still referenced; if Media is still in leftCenterGroup, remove that group entirely and ensure Media is not deleted from the file without a home — if 02-04 owns Media move, either (a) move Media into right section now as a minimal park after fill, or (b) keep a hidden holding Loader. Preferred: leave Media and BatteryIndicator temporarily in middleSection trailing group ONLY if right rewire is not done yet is wrong — better: move Media and BatteryIndicator into rightSectionRowLayout now in declaration positions that plan 02-04 will finalize. To avoid breaking the bar between plans, reparent Media and BatteryIndicator into rightSectionRowLayout as children (order can be rough; 02-04 will set final LTR order and layoutDirection). Do not drop them from the tree (D-17).
    5. Do NOT wrap ClockWidget in a MouseArea whose onPressed toggles GlobalStates.sidebarRightOpen — clock clicks must stay with ClockWidgetPopup (D-06). Right-side mouse area remains the sidebar toggle surface.
    6. Weather: do not set weather enable true. If a Weather Loader exists only on the right, leave it for 02-04; do not add an active WeatherBar in center.
    7. Remove now-empty leftCenterGroup / middleCenterGroup / rightCenterGroup structures and orphan VerticalBarSeparators that only separated removed groups if they no longer apply. Keep file QML-valid.
    8. Preserve workspacesWidget id if still needed for padding references; if padding: workspacesWidget.widgetPadding was on middleCenterGroup only, drop that padding binding or attach to the Workspaces parent on the left if visually needed.

    Smoke after edit: `timeout 4 quickshell 2>&1 | rg 'Configuration Loaded|Error|Cannot assign|ReferenceError'`.
  </action>
  <acceptance_criteria>
    - ClockWidget call site includes literal `showDate: false` (not a verbose boolean expression)
    - middleSection (or sole center group) contains ClockWidget and UtilButtons and does not contain Workspaces, Resources, Media, or BatteryIndicator
    - Media { and BatteryIndicator { each still appear exactly once somewhere in BarContent.qml (reparented toward right if needed)
    - No MouseArea directly parenting ClockWidget that sets sidebarRightOpen onPressed
    - `rg -n 'weather.enable' .config/quickshell/modules/ii/bar/BarContent.qml` still gates Weather via Config (not forced true)
    - `timeout 4 quickshell 2>&1 | rg -c 'Configuration Loaded'` is ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'showDate:|ClockWidget|UtilButtons|Workspaces|Resources|Media|BatteryIndicator|middleSection|sidebarRightOpen' .config/quickshell/modules/ii/bar/BarContent.qml | head -60; timeout 4 quickshell 2>&amp;1 | rg 'Configuration Loaded|Error|ReferenceError' | head -20</automated>
  </verify>
  <done>Center is clock+utils with showDate false; left D-15 order intact; Media/Battery still in tree for 02-04.</done>
</task>

</tasks>

## Artifacts this phase produces

- Rewired left region in `.config/quickshell/modules/ii/bar/BarContent.qml` (D-15 left)
- Simplified center region with `ClockWidget { showDate: false }` + UtilButtons (D-08, D-15 center)
- Media/Battery preserved in tree for right-region finalization

## Assumptions (specless edges)

- Dual-monitor: both screens keep the same panel content (Phase 1 D-13); shown:10 means both monitors list workspaces 1–10 (RESEARCH Pitfall 4 — intentional per D-02).
- Temporarily parking Media/Battery on the right before 02-04 final LTR order is acceptable if smoke stays green.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bar UI → Hyprland dispatch | Workspaces reparent only; dispatch path unchanged from Phase 1 stock |
| Bar UI → GlobalStates | Must not steal clock clicks into sidebar toggle |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06 | Elevation of privilege | Workspaces click dispatch | low | accept | Stock `workspace N` only; no new Process; no plugin dispatcher |
| T-02-07 | Spoofing | Clock click action | low | mitigate | Keep ClockWidgetPopup only; do not wire external calendar URL; remove parent sidebar MouseArea on clock |
| T-02-SC | Tampering | package installs | low | accept | No installs |
</threat_model>

<verification>
- Static order of left children matches D-15
- showDate false present
- quickshell Configuration Loaded
</verification>

<success_criteria>
- BAR-01 workspaces live on left strip in locked order
- BAR-02 clock is center, datetime-only chrome without longDate duplication
- D-17 non-core modules not stripped
</success_criteria>

<output>
Create `.planning/phases/02-core-bar-modules/02-03-SUMMARY.md` when done
</output>
