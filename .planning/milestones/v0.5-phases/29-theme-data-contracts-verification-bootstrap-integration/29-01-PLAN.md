---
phase: "29"
plan: "01"
type: "tracer"
wave: 1
depends_on: []
files_modified:
  - scripts/phase29-theme-data-contracts-assert.sh
  - guard-paths.tsv
  - .gitignore
  - restow/README.md
  - arch/kitty.sh
  - arch/dots-hyprland.sh
  - restow/fuzzel/.config/fuzzel/fuzzel.ini
  - restow/kitty/.config/kitty/kitty.conf
  - restow/kitty/.config/kitty/search.py
  - restow/kitty/.config/kitty/scroll_mark.py
  - scripts/phase28-terminal-fuzzel-assert.sh
autonomous: true
requirements: [INTG-01, INTG-02]

must_haves:
  truths:
    - "scripts/phase29-theme-data-contracts-assert.sh exists at mode 0755 implementing fail-closed conventions (set -euo pipefail, cleanup trap, --section parsing, and porcelain snapshot check) (D-14, D-15)"
    - "guard-paths.tsv documents all 8 dynamic theme outputs with v0.5 architecture rationale while strictly retaining Q7 and Q8 compatibility markers (D-03, INTG-01)"
    - "root .gitignore contains kde-material-you-colors/ ensuring 1:1 parity with guard-paths.tsv entries (D-01, INTG-01)"
    - "fuzzel and kitty packages reside under restow/ (restow/fuzzel, restow/kitty) and are removed from stow/ to honor collision-map.tsv mathematical outcome derivation (D-05, INTG-01, INTG-02)"
    - "restow/README.md Section 3 package recovery table is regenerated via ./scripts/gen-collision-map.sh --restow-table with fuzzel and kitty assigned rsync-replace tags (D-06, INTG-01)"
    - "arch/kitty.sh stows from ../restow while strictly preserving the PAIR_COUNT == 18 invariant across arch/*.sh (D-07, INTG-02)"
    - "scripts/phase28-terminal-fuzzel-assert.sh checks restow/ for kitty.conf, search.py, scroll_mark.py, and fuzzel.ini, preventing false regression failures (D-09, INTG-01, INTG-02)"
    - "arch/dots-hyprland.sh enforces hierarchical prefix matching for guarded paths and recursively validates that live counterparts of guarded directories never symlink into the git repository (D-02, D-04, INTG-01)"
    - "Live symlinks in ~/.config/fuzzel/ and ~/.config/kitty/ point into restow/ with zero broken or dangling pointers (D-08, INTG-02)"
    - "Section 1 and Section 3 of scripts/phase29-theme-data-contracts-assert.sh pass with FAIL=0, and arch/dots-hyprland.sh verify --strict exits 0 with 0 findings (INTG-01, INTG-02)"
  artifacts:
    - scripts/phase29-theme-data-contracts-assert.sh
    - guard-paths.tsv
    - .gitignore
    - restow/README.md
    - arch/kitty.sh
    - arch/dots-hyprland.sh
    - restow/fuzzel/.config/fuzzel/fuzzel.ini
    - restow/kitty/.config/kitty/kitty.conf
    - restow/kitty/.config/kitty/search.py
    - restow/kitty/.config/kitty/scroll_mark.py
    - scripts/phase28-terminal-fuzzel-assert.sh
  key_links:
    - "scripts/phase29-theme-data-contracts-assert.sh is the authoritative test harness gating INTG-01, INTG-02, and INTG-03 across Sections 1 to 5"
    - "guard-paths.tsv and .gitignore form the 1:1 data contract preventing repository churn across dynamic theme generators"
    - "collision-map.tsv derivation (symlink DESTROYED -> restow) is strictly honored by relocating fuzzel and kitty to restow/"
    - "arch/dots-hyprland.sh verify --strict is the repository verification engine validating link identity, guard exclusion, and clean working tree"
  prohibitions:
    - "Must not edit files under vendor/dots-hyprland/"
    - "Must not alter closed-phase assert scripts under scripts/ except phase28-terminal-fuzzel-assert.sh for restow path alignment (D-09)"
    - "Must not use stow --adopt when symlinking restow/fuzzel or restow/kitty (CAP-07)"
    - "Must not alter the PAIR_COUNT == 18 count of '--verbose=5 --no-folding' across arch/*.sh"
    - "Must not commit generated theme outputs (kdeglobals, gtk.css, colors.lua, fuzzel_theme.ini, etc.) to git tracking"
---

<objective>
Scaffold the Phase 29 assertion harness (`scripts/phase29-theme-data-contracts-assert.sh`) with fail-closed structure covering Sections 1 through 5 (D-14, D-15), reconcile `guard-paths.tsv` documentation and `.gitignore` 1:1 parity (D-01, D-03), relocate `fuzzel` and `kitty` packages from `stow/` to `restow/` to satisfy `collision-map.tsv` derivation (D-05), regenerate `restow/README.md` Section 3 table (D-06), update `arch/kitty.sh` to stow from `../restow` preserving `PAIR_COUNT == 18` (D-07), align `scripts/phase28-terminal-fuzzel-assert.sh` to `restow/` (D-09), verify Section 1 passes (INTG-01), update `arch/dots-hyprland.sh` with hierarchical prefix guard matching and recursive directory checks (D-02, D-04), re-point live symlinks to `restow/` (D-08), and verify Section 3 and `arch/dots-hyprland.sh verify --strict` pass with `FAIL=0 FINDINGS=0` (INTG-01, INTG-02).

Purpose: Establish the tracer slice for Phase 29, enforcing data contracts, gitignore parity, three-tree capture taxonomy alignment, and strict verification engine compliance with correct prerequisite sequencing.
Output: Executable `scripts/phase29-theme-data-contracts-assert.sh`, updated contracts and wrappers, relocated `restow/` packages, and verified green Section 1 and Section 3 assertions.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-CONTEXT.md
@.planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-RESEARCH.md
@.planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-PATTERNS.md
@.planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-VALIDATION.md
@guard-paths.tsv
@.gitignore
@collision-map.tsv
@restow/README.md
@arch/kitty.sh
@arch/dots-hyprland.sh
@scripts/phase28-terminal-fuzzel-assert.sh
</context>

<assumptions>
- Upstream dots-hyprland installer subcommands (`install`, `install-files`) invoke `install_dir__sync` on `.config/fuzzel` and `.config/kitty`, destroying symlinks via `rsync -av --delete`.
- Placing `fuzzel` and `kitty` in `restow/` is mathematically required by the outcome derivation in `collision-map.tsv`.
- GNU Stow requires unlinking existing symlinks pointing into `stow/` before stowing from `restow/` to avoid collision conflicts.
- Section 1 of the test suite asserts that `fuzzel` and `kitty` already reside in `restow/` and are listed in `restow/README.md`; therefore, package relocation and table regeneration must occur before Section 1 is executed.
- Header comments in `guard-paths.tsv` can be expanded with v0.5 architecture details as long as `Q7:` and `Q8:` compatibility markers are preserved verbatim.
- Hierarchical prefix matching traverses ancestor directories up to root, correctly matching any sub-file or sub-directory of a guarded path.
</assumptions>

<tasks>

<task id="29-01-01" type="tracer">
  <name>Scaffold Phase 29 theme data contracts assertion harness (scripts/phase29-theme-data-contracts-assert.sh)</name>
  <read_first>
    scripts/phase28-terminal-fuzzel-assert.sh
    scripts/phase23-bootstrap-assert.sh
    .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-PATTERNS.md
    .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-CONTEXT.md
  </read_first>
  <files>
    scripts/phase29-theme-data-contracts-assert.sh
  </files>
  <action>
    Scaffold `scripts/phase29-theme-data-contracts-assert.sh` with permissions 0755 adhering strictly to repository assert harness conventions per D-14, D-15, and INTG-01:
    1. Base scaffolding: Use `#!/usr/bin/env bash`, `set -euo pipefail`, resolve `REPO_ROOT` and `ASSERT_SELF`, export default `XDG_CONFIG_HOME` and `XDG_STATE_HOME`, initialize `FAIL=0` and `FINDINGS=0`, and define `pass()`, `fail()`, `finding()`, and `info()` helpers.
    2. Cleanup trap: Initialize `TMP_FILES=()` and `SCRATCH_ROOTS=()`, define `cleanup()` removing temporary files with `rm -f` and scratch directories with `rm -rf`, and register `trap cleanup EXIT`.
    3. CLI argument parsing: Support `--section <1-5>` and `-h|--help`, validating integer range 1 to 5 with fail-closed exit 1 on invalid arguments.
    4. Two-phase working tree snapshots: Implement `porcelain_snapshot_raw` and `porcelain_snapshot` filtering ephemeral noise (`.commandcode/`, `scripts/__pycache__/`), capturing git status before test execution into `$PORCELAIN_BEFORE`.
    5. Scaffold stubs for Sections 1 through 5.
    6. Closing porcelain snapshot check: Capture `$PORCELAIN_AFTER`, compare against `$PORCELAIN_BEFORE` using `cmp -s` (failing if git status changed across run per D-15), print `=== done: FAIL=$FAIL FINDINGS=$FINDINGS ===`, and exit 1 if FAIL > 0, else exit 0.
  </action>
  <acceptance_criteria>
    test -x scripts/phase29-theme-data-contracts-assert.sh exits 0.
    bash scripts/phase29-theme-data-contracts-assert.sh --help exits 0 and outputs Usage: .* --section <1-5>.
    bash scripts/phase29-theme-data-contracts-assert.sh --section 99 exits 1 with error message on stderr.
  </acceptance_criteria>
  <verify>
    <automated>test -x scripts/phase29-theme-data-contracts-assert.sh && bash scripts/phase29-theme-data-contracts-assert.sh --help | grep -q -- '--section <1-5>'</automated>
    <fails_when>non-zero exit code, file not executable, or missing '--section <1-5>' in help output</fails_when>
  </verify>
  <done>scripts/phase29-theme-data-contracts-assert.sh is created at mode 0755, handles --section and --help, initializes porcelain snapshots, and scaffolds test sections 1 through 5.</done>
</task>

<task id="29-01-02" type="execute">
  <name>Reconcile contracts, relocate fuzzel & kitty to restow/, regenerate restow table, and verify Section 1</name>
  <read_first>
    guard-paths.tsv
    .gitignore
    collision-map.tsv
    restow/README.md
    scripts/gen-collision-map.sh
    arch/kitty.sh
    scripts/phase28-terminal-fuzzel-assert.sh
    scripts/phase22-kde-and-gtk-capture-assert.sh
    .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-PATTERNS.md
    .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-CONTEXT.md
  </read_first>
  <files>
    guard-paths.tsv
    .gitignore
    restow/fuzzel/.config/fuzzel/fuzzel.ini
    restow/kitty/.config/kitty/kitty.conf
    restow/kitty/.config/kitty/search.py
    restow/kitty/.config/kitty/scroll_mark.py
    restow/README.md
    arch/kitty.sh
    scripts/phase28-terminal-fuzzel-assert.sh
    scripts/phase29-theme-data-contracts-assert.sh
  </files>
  <action>
    Reconcile repository contracts, package relocation, and Section 1 assertions per D-01, D-03, D-05, D-06, D-07, D-09, INTG-01, and INTG-02:
    1. Update `guard-paths.tsv` header comments to document the complete v0.5 Material You architecture rationale across all 8 entries (kdeglobals, Kvantum, gtk-3.0/gtk.css, gtk-4.0/gtk.css, fuzzel/fuzzel_theme.ini, hyprland/colors.lua, hyprlock/colors.conf, kde-material-you-colors) per D-03. Retain literal compatibility tokens "Q7:" and "Q8:" and referenced strings verbatim to ensure compatibility with `scripts/phase22-kde-and-gtk-capture-assert.sh:484,490`.
    2. Add `kde-material-you-colors/` under the `# Generated theme output (D-14)` section of root `.gitignore` to establish 100% 1:1 parity with `guard-paths.tsv` per D-01.
    3. Relocate packages from `stow/` to `restow/` via `git mv stow/fuzzel restow/fuzzel` and `git mv stow/kitty restow/kitty` per D-05, satisfying the `collision-map.tsv` derivation where `install_dir__sync` destroys symlinks (`symlink_outcome=DESTROYED` -> `tree=restow`).
    4. Regenerate `restow/README.md` Section 3 table via `./scripts/gen-collision-map.sh --restow-table` per D-06, establishing `rsync-replace` tags and recovery commands for `fuzzel` and `kitty`.
    5. In `arch/kitty.sh` line 10, update stow source directory from `../stow` to `../restow`:
       `cd "$(dirname "${BASH_SOURCE[0]}")/../restow" && stow --verbose=5 --no-folding -t ~ kitty`
       Verify that the repository-wide `PAIR_COUNT == 18` invariant across `arch/*.sh` is strictly preserved per D-07.
    6. Update `scripts/phase28-terminal-fuzzel-assert.sh` lines 108, 122, and 150 to check `restow/` instead of `stow/` for `kitty.conf`, `search.py`, `scroll_mark.py`, and `fuzzel.ini` per D-09. Run `bash scripts/phase28-terminal-fuzzel-assert.sh --section 1` to ensure zero regression.
    7. Implement Section 1 in `scripts/phase29-theme-data-contracts-assert.sh` asserting:
       - `guard-paths.tsv` exists, contains exactly 8 valid tab-separated rows, and preserves "Q7:" and "Q8:" markers (D-03).
       - `.gitignore` contains all 8 guarded theme patterns including `kde-material-you-colors/` (D-01).
       - `fuzzel` and `kitty` exist in `restow/` and are absent from `stow/` (D-05).
       - `collision-map.tsv` derives `tree=restow` for fuzzel and kitty with `DESTROYED` symlink outcome.
       - `restow/README.md` Section 3 table contains fuzzel and kitty with `rsync-replace` tag (D-06).
       - `arch/kitty.sh` stows from `../restow` (D-07).
       - Repository-wide `PAIR_COUNT` across `arch/*.sh` equals 18.
    8. Run `bash scripts/phase29-theme-data-contracts-assert.sh --section 1` to verify all Section 1 assertions pass with FAIL=0.
  </action>
  <acceptance_criteria>
    grep -q 'kde-material-you-colors/' .gitignore exits 0.
    grep -q 'Q7:' guard-paths.tsv exits 0 and grep -q 'Q8:' guard-paths.tsv exits 0.
    test -d restow/fuzzel && test -d restow/kitty exits 0.
    test ! -e stow/fuzzel && test ! -e stow/kitty exits 0.
    grep -q '| `fuzzel` | `rsync-replace` |' restow/README.md exits 0.
    grep -q '| `kitty` | `rsync-replace` |' restow/README.md exits 0.
    grep -q '\.\./restow.*stow.*kitty' arch/kitty.sh exits 0.
    test "$(grep -ho -- '--verbose=5 --no-folding' arch/*.sh | wc -l)" -eq 18.
    bash scripts/phase28-terminal-fuzzel-assert.sh --section 1 exits 0 with FAIL=0.
    bash scripts/phase29-theme-data-contracts-assert.sh --section 1 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase29-theme-data-contracts-assert.sh --section 1</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>guard-paths.tsv documented with v0.5 architecture preserving Q7/Q8 markers, .gitignore updated with kde-material-you-colors/, fuzzel and kitty relocated to restow/, restow/README.md table regenerated via ./scripts/gen-collision-map.sh --restow-table, arch/kitty.sh updated to ../restow preserving PAIR_COUNT == 18, phase28-terminal-fuzzel-assert.sh aligned to restow/, and Section 1 passes with FAIL=0.</done>
</task>

<task id="29-01-03" type="execute">
  <name>Update verifier with hierarchical guard matching, re-stow live symlinks to restow/, and verify Section 3</name>
  <read_first>
    arch/dots-hyprland.sh
    guard-paths.tsv
    restow/README.md
    scripts/phase29-theme-data-contracts-assert.sh
    .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-PATTERNS.md
    .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-CONTEXT.md
  </read_first>
  <files>
    arch/dots-hyprland.sh
    scripts/phase29-theme-data-contracts-assert.sh
  </files>
  <action>
    Update verification engine, re-stow live symlinks to `restow/`, and verify strict engine compliance per D-02, D-04, D-08, INTG-01, and INTG-02:
    1. Update `arch/dots-hyprland.sh`:
       - Under `run_verify()` guard path exclusion checks (~line 1158), add recursive inspection: if a guarded path is a directory (e.g. `Kvantum`, `kde-material-you-colors`), find all symlinks within it and fail if any resolve into the repository root per D-04.
       - In `classify_entry()` (~line 1340), replace exact lookup with a hierarchical prefix walk: while `$check_entry` is non-empty and not `/` or `$HOME`, test if `${guarded_entries["$check_entry"]:-}` is set, recognizing any nested child path inside guarded directories as `guarded theme output` per D-02.
    2. Re-stow live symlinks to `restow/` per D-08:
       - Unstow old symlinks: `cd "$REPO_ROOT/stow" && stow -D --verbose=5 --no-folding -t ~ fuzzel kitty 2>/dev/null || true`
       - Stow from restow: `cd "$REPO_ROOT/restow" && stow --verbose=5 --no-folding -t ~ fuzzel kitty`
       - Verify that `readlink -f ~/.config/fuzzel/fuzzel.ini` resolves to `$REPO_ROOT/restow/fuzzel/.config/fuzzel/fuzzel.ini` and `readlink -f ~/.config/kitty/kitty.conf` resolves to `$REPO_ROOT/restow/kitty/.config/kitty/kitty.conf`.
    3. Implement Section 3 in `scripts/phase29-theme-data-contracts-assert.sh` asserting:
       - Packaging trees (`stow/`, `restow/`, `capture/`) are completely clean in git (`git status --porcelain stow/ restow/ capture/` empty).
       - `arch/dots-hyprland.sh verify --strict` exits 0 with `FAIL=0 FINDINGS=0` (INTG-02).
       - Live symlinks in `~/.config/fuzzel/` and `~/.config/kitty/` point into `restow/` (D-08).
    4. Run `bash scripts/phase29-theme-data-contracts-assert.sh --section 3 && ./arch/dots-hyprland.sh verify --strict`, verifying exit 0 with zero findings.
  </action>
  <acceptance_criteria>
    grep -q 'while.*check_entry.*guarded_entries' arch/dots-hyprland.sh exits 0.
    readlink -f ~/.config/fuzzel/fuzzel.ini matches .*/restow/fuzzel/.config/fuzzel/fuzzel.ini.
    readlink -f ~/.config/kitty/kitty.conf matches .*/restow/kitty/.config/kitty/kitty.conf.
    bash scripts/phase29-theme-data-contracts-assert.sh --section 3 exits 0 with FAIL=0.
    ./arch/dots-hyprland.sh verify --strict exits 0 with FAIL=0 FINDINGS=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase29-theme-data-contracts-assert.sh --section 3 && ./arch/dots-hyprland.sh verify --strict</automated>
    <fails_when>non-zero exit code, "[FAIL]" in assert output, or findings reported by verify --strict</fails_when>
  </verify>
  <done>arch/dots-hyprland.sh updated with hierarchical prefix guard matching and recursive directory checks, live symlinks re-stowed to restow/, and Section 3 plus arch/dots-hyprland.sh verify --strict pass with exit 0 and zero findings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification Engine → Git Working Tree | Verifier or test harness executions mutating git tracked files or leaving uncommitted changes |
| GNU Stow Package Relocation → Live User Directory | Relocating packages between stow/ and restow/ without breaking live symlinks or folding |
| Guarded Paths → Repository Packaging | Dynamic theme generation output leaking into git tracking or being linked into repository trees |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-01 | Tampering / Git Churn | guard-paths.tsv, .gitignore, and verify | high | mitigate | Reconcile guard-paths.tsv with 8 paths, mirror into .gitignore 1:1, enforce hierarchical prefix matching in is_guarded_path(), and recursively assert guarded paths never link into repo packaging (D-01..D-04, INTG-01). |
| T-29-02 | Denial of Service / Broken Links | stow/ to restow/ package migration | high | mitigate | Relocate packages before Section 1 runs, unstow from stow/ before moving in git, then stow from restow/ using --no-folding without --adopt, asserting live symlink targets resolve to restow/ (D-05, D-08, CAP-07, INTG-02). |
| T-29-03 | Elevation of Privilege / Insecure Stow Linking | arch/kitty.sh and GNU Stow | medium | mitigate | Maintain --no-folding and preserve exact PAIR_COUNT == 18 count of stow invocations across arch/*.sh, verifying no accidental directory folding or foreign file adoption (D-07). |
</threat_model>

<verification>
Automated verification commands:
1. `test -x scripts/phase29-theme-data-contracts-assert.sh && bash scripts/phase29-theme-data-contracts-assert.sh --help | grep -q -- '--section <1-5>'`
2. `bash scripts/phase29-theme-data-contracts-assert.sh --section 1`
3. `bash scripts/phase29-theme-data-contracts-assert.sh --section 3 && ./arch/dots-hyprland.sh verify --strict`
</verification>

<success_criteria>
1. `scripts/phase29-theme-data-contracts-assert.sh` exists at 0755 permissions with working `--section <1-5>` support and porcelain checks.
2. `guard-paths.tsv` documents all 8 paths with v0.5 architecture and preserves Q7/Q8 markers.
3. `.gitignore` has 1:1 parity with `guard-paths.tsv`, including `kde-material-you-colors/`.
4. `fuzzel` and `kitty` reside in `restow/`, `restow/README.md` table is regenerated, `arch/kitty.sh` stows from `../restow`, and `PAIR_COUNT == 18` is preserved.
5. `arch/dots-hyprland.sh` implements hierarchical prefix matching and recursive directory guard checks.
6. Live symlinks in `~/.config/fuzzel/` and `~/.config/kitty/` resolve to `restow/`.
7. `scripts/phase28-terminal-fuzzel-assert.sh --section 1` and `scripts/phase29-theme-data-contracts-assert.sh --section 1 --section 3` pass with FAIL=0.
8. `./arch/dots-hyprland.sh verify --strict` exits 0 with FAIL=0 FINDINGS=0.
</success_criteria>

<output>
Create `.planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-01-SUMMARY.md` when done
</output>
