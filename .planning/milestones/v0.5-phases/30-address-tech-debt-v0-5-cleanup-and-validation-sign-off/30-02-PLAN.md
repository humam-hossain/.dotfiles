---
phase: "30"
plan: "02"
type: "execute"
wave: 2
depends_on:
  - "30-01"
files_modified:
  - scripts/phase30-tech-debt-assert.sh
  - .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-VALIDATION.md
  - .planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [DEBT-07, DEBT-08]

must_haves:
  truths:
    - "scripts/phase30-tech-debt-assert.sh exists at mode 0755, supporting --section <1-5>, fail-closed error handling (set -euo pipefail), cleanup trap, and git working tree porcelain snapshot bracketing (D-09, D-14, DEBT-08)"
    - "Section 1 of scripts/phase30-tech-debt-assert.sh asserts background_opacity 0.90 in restow/kitty/, probes native kitty config via python, and verifies phase28 assert script alignment (D-01, D-02, DEBT-05)"
    - "Section 2 of scripts/phase30-tech-debt-assert.sh asserts bootstrap virtualenv fallback export, idempotent template hooks, live wrapper parameter expansion fallback, live applycolor.sh signaling, and runs an isolated scratch drill (D-05, D-06, D-07, DEBT-06)"
    - "Section 3 of scripts/phase30-tech-debt-assert.sh asserts Nyquist validation compliance across all Milestone v0.5 phases (25 through 30), verifies 0 pending tasks in 29-VALIDATION.md, verifies DEBT-05..08 registered in REQUIREMENTS.md with 0 stale Pending markers, and checks ROADMAP.md Phase 30 requirements mapping (D-08, D-10, D-11, D-12, DEBT-07)"
    - "Section 4 of scripts/phase30-tech-debt-assert.sh executes arch/dots-hyprland.sh verify --strict, requiring exit code 0 and FAIL=0 FINDINGS=0 (D-14, DEBT-08)"
    - "Section 5 of scripts/phase30-tech-debt-assert.sh runs phase25, phase26, phase27, phase28, and phase29 assert scripts sequentially with all 5 passing cleanly (D-09, D-14, DEBT-08)"
    - "29-VALIDATION.md is reconciled from status draft to status: validated, nyquist_compliant: true, wave_0_complete: true, and all 6 task rows marked green (D-08, DEBT-07)"
    - "30-VALIDATION.md is authored with Wave 0 assert harness mapping, nyquist_compliant: true, and task verification mappings (D-10, DEBT-08)"
    - "REQUIREMENTS.md registers DEBT-05 through DEBT-08 under ### DEBT, maps them to Phase 30 in Traceability, updates coverage to 19/19 (100%), and contains zero Pending markers (D-11, DEBT-07)"
    - "ROADMAP.md Phase 30 entry documents goals, dependencies, requirements (DEBT-05..08), 2 plans, and success criteria (D-12, DEBT-07)"
    - "scripts/phase30-tech-debt-assert.sh passes all 5 sections, arch/dots-hyprland.sh verify --strict exits 0 with 0 findings, and git working tree remains byte-identical clean (D-14, DEBT-08)"
  artifacts:
    - scripts/phase30-tech-debt-assert.sh
    - .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-VALIDATION.md
    - .planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-VALIDATION.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "scripts/phase30-tech-debt-assert.sh is the authoritative test harness gating Milestone v0.5 closeout"
    - "arch/dots-hyprland.sh verify --strict is the repository verification engine validating link identity, guard exclusion, and clean working tree"
    - "29-VALIDATION.md and 30-VALIDATION.md establish formal Nyquist compliance across Milestone v0.5"
    - "REQUIREMENTS.md and ROADMAP.md form the authoritative project traceability record"
  prohibitions:
    - "Must not edit files under vendor/dots-hyprland/"
    - "Must not alter closed-phase assert scripts under scripts/ (except phase28-terminal-fuzzel-assert.sh per D-02 in Plan 30-01)"
    - "Must not leave temporary files or scratch directories in /tmp after assert harness completes"
    - "Must not allow git status --porcelain drift across test harness execution"
---

<objective>
Scaffold the dedicated Phase 30 assertion harness (`scripts/phase30-tech-debt-assert.sh`) with 5 automated sections and fail-closed reporting (D-09, D-14), reconcile `29-VALIDATION.md` frontmatter and task map to complete validation status (D-08), complete `30-VALIDATION.md` validation contract (D-10), register `DEBT-05` through `DEBT-08` in `REQUIREMENTS.md` and synchronize `ROADMAP.md` (D-11, D-12), and execute the full test suite, strict system verifier, and multi-phase regression sweep across Phases 25 through 29 with zero git working tree churn (D-09, D-14).

Purpose: Establish 100% Nyquist validation compliance across Milestone v0.5, verify multi-phase regression stability, and fulfill the mandatory phase exit gate for milestone closeout.
Output: Executable `scripts/phase30-tech-debt-assert.sh`, reconciled validation contracts, updated requirements traceability, and passing regression suite.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-CONTEXT.md
@.planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-RESEARCH.md
@.planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-PATTERNS.md
@.planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-VALIDATION.md
@.planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-VALIDATION.md
@scripts/phase24-tech-debt-assert.sh
@scripts/phase29-theme-data-contracts-assert.sh
@arch/dots-hyprland.sh
</context>

<assumptions>
- scripts/phase30-tech-debt-assert.sh follows the 5-section architecture demonstrated in scripts/phase24-tech-debt-assert.sh and scripts/phase29-theme-data-contracts-assert.sh.
- Sourcing bootstrap.sh in an isolated subshell against mock filesystem roots allows non-destructive testing of idempotent sanitization expressions.
- Section 5 multi-phase regression sweep executes scripts/phase25-gtk-material-you-assert.sh through scripts/phase29-theme-data-contracts-assert.sh sequentially; all must exit 0 without modifying git working tree state.
- arch/dots-hyprland.sh verify --strict exits 0 with FAIL=0 FINDINGS=0 when all stow and restow symlinks, guarded dynamic theme paths, and repo files conform to data contracts.
- Git porcelain comparison before and after test runs guarantees assert scripts never dirty the repository.
</assumptions>

<tasks>

<task id="30-02-01" type="execute">
  <name>Scaffold scripts/phase30-tech-debt-assert.sh (Wave 0 assert harness with Sections 1–5, fail-closed mechanics, and porcelain snapshotting)</name>
  <read_first>
    scripts/phase24-tech-debt-assert.sh
    scripts/phase29-theme-data-contracts-assert.sh
    .planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-PATTERNS.md
    .planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-CONTEXT.md
  </read_first>
  <files>
    scripts/phase30-tech-debt-assert.sh
  </files>
  <action>
    Author `scripts/phase30-tech-debt-assert.sh` with permissions 0755 adhering strictly to repository test harness conventions per D-09, D-14, and DEBT-08:
    1. Base scaffolding: Use `#!/usr/bin/env bash`, `set -euo pipefail`, resolve `REPO_ROOT` and `ASSERT_SELF`, export default `XDG_CONFIG_HOME` and `XDG_STATE_HOME`, initialize `FAIL=0` and `FINDINGS=0`, and define `pass()`, `fail()`, `finding()`, and `info()` helper functions.
    2. Cleanup trap: Initialize `TMP_FILES=()` and `SCRATCH_ROOTS=()`, define `cleanup()` removing temporary files with `rm -f` and scratch directories with `chmod -R u+rwX` and `rm -rf`, and register `trap cleanup EXIT`.
    3. CLI argument parsing: Support `--section <1-5>` and `-h|--help`, validating integer range 1 to 5 with fail-closed exit 1 on invalid arguments.
    4. Two-phase working tree snapshots: Implement `porcelain_snapshot_raw` and `porcelain_snapshot` filtering ephemeral noise (`.commandcode/`, `scripts/__pycache__/`), capturing git status before test execution into `$PORCELAIN_BEFORE`.
    5. Section 1 (DEBT-05): Check `restow/kitty/.config/kitty/kitty.conf` for `background_opacity 0.90`, probe native Kitty config via `kitty +runpy` verifying loaded opacity satisfies `abs(opts.background_opacity - 0.90) <= 0.01`, verify `scripts/phase28-terminal-fuzzel-assert.sh` checks for 0.90 opacity, and run `scripts/phase28-terminal-fuzzel-assert.sh --section 3` verifying exit code 0.
    6. Section 2 (DEBT-06): Check `bootstrap.sh` for `export ILLOGICAL_IMPULSE_VIRTUAL_ENV` fallback and idempotent alignment hooks for `kde-material-you-colors-wrapper.sh` and `applycolor.sh`, verify live `~/.config/matugen/templates/kde/kde-material-you-colors-wrapper.sh` contains parameter expansion fallback `${ILLOGICAL_IMPULSE_VIRTUAL_ENV:-$XDG_STATE_HOME/quickshell/.venv}`, verify live `~/.config/quickshell/ii/scripts/colors/applycolor.sh` contains `killall -SIGUSR1 kitty 2>/dev/null || true` and no longer contains `pidof kitty` or `pgrep -f kitty`, and execute a scratch sandbox drill in `/tmp` verifying idempotent sed transformations against mock template files.
    7. Section 3 (DEBT-07): Verify `status: validated`, `nyquist_compliant: true`, and `wave_0_complete: true` across all Milestone v0.5 validation contracts (`25-VALIDATION.md`, `26-VALIDATION.md`, `27-VALIDATION.md`, `28-VALIDATION.md`, `29-VALIDATION.md`, `30-VALIDATION.md`), verify `29-VALIDATION.md` has 0 pending tasks, verify `REQUIREMENTS.md` contains `DEBT-05` through `DEBT-08` with 0 stale `Pending` markers, and verify `ROADMAP.md` reflects Phase 30 requirements mapping.
    8. Section 4 (DEBT-08): Execute `./arch/dots-hyprland.sh verify --strict`, asserting exit code 0 and `FAIL=0 FINDINGS=0`.
    9. Section 5 (DEBT-08): Execute full multi-phase regression sweep running `scripts/phase25-gtk-material-you-assert.sh`, `scripts/phase26-qt-kde-material-you-assert.sh`, `scripts/phase27-accent-coordination-assert.sh`, `scripts/phase28-terminal-fuzzel-assert.sh`, and `scripts/phase29-theme-data-contracts-assert.sh` sequentially, asserting each exits 0.
    10. Closing porcelain snapshot check: Capture `$PORCELAIN_AFTER`, compare against `$PORCELAIN_BEFORE` using `cmp -s` (failing if git status changed across run per D-14), print `=== done: FAIL=$FAIL FINDINGS=$FINDINGS ===`, and exit 1 if `FAIL > 0`, else exit 0.
  </action>
  <acceptance_criteria>
    test -x scripts/phase30-tech-debt-assert.sh exits 0.
    bash scripts/phase30-tech-debt-assert.sh --help exits 0 and outputs Usage: .* --section <1-5>.
    bash scripts/phase30-tech-debt-assert.sh --section 99 exits 1 with error message on stderr.
  </acceptance_criteria>
  <verify>
    <automated>test -x scripts/phase30-tech-debt-assert.sh && bash scripts/phase30-tech-debt-assert.sh --help | grep -q -- '--section <1-5>'</automated>
    <fails_when>non-zero exit code, file not executable, or missing '--section <1-5>' in help output</fails_when>
  </verify>
  <done>scripts/phase30-tech-debt-assert.sh is created at mode 0755, handles --section and --help, initializes porcelain snapshots, and scaffolds Sections 1 through 5.</done>
</task>

<task id="30-02-02" type="execute">
  <name>Reconcile Phase 29 validation sign-off and update REQUIREMENTS.md & ROADMAP.md traceability</name>
  <read_first>
    .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-VALIDATION.md
    .planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-VALIDATION.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-PATTERNS.md
  </read_first>
  <files>
    .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-VALIDATION.md
    .planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-VALIDATION.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </files>
  <action>
    Reconcile validation contracts and update project-wide traceability per D-08, D-10, D-11, D-12:
    1. In .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-VALIDATION.md:
       - Update frontmatter: change status from draft to validated, nyquist_compliant to true, wave_0_complete to true, and add validated: "2026-09-18" (D-08).
       - In Per-Task Verification Map table, update all 6 task rows (29-01-01 through 29-02-03) from pending / red to ✅ exists and ✅ green backed by the passing 30/30 assertions in scripts/phase29-theme-data-contracts-assert.sh (D-08).
       - In Validation Sign-Off section, check off all 6 items [x] and set Approval: complete (D-08).
    2. In .planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-VALIDATION.md:
       - Update frontmatter: change status from draft to validated, nyquist_compliant to true, wave_0_complete to true, and add validated: "2026-09-18" (D-10).
       - Update Per-Task Verification Map table rows to map tasks 30-01-01, 30-01-02, 30-02-01, 30-02-02, 30-02-03 to their respective sections in scripts/phase30-tech-debt-assert.sh with ✅ exists and ✅ green (D-10).
       - In Wave 0 Requirements, check off [x] scripts/phase30-tech-debt-assert.sh.
       - In Validation Sign-Off section, check off all items [x] and set Approval: complete (D-10).
    3. In .planning/REQUIREMENTS.md:
       - Under ## v1 Requirements, add ### DEBT (Technical Debt & Validation Cleanup) section registering DEBT-05, DEBT-06, DEBT-07, and DEBT-08 with completed checkboxes [x] and concise descriptions (D-11).
       - In ## Traceability table, add rows for DEBT-05, DEBT-06, DEBT-07, and DEBT-08 mapped to Phase 30 with status Complete (D-11).
       - In Coverage section, update v1 requirements count to 19 total, Mapped to phases: 19, Unmapped: 0 ✓ (D-11).
       - Verify zero Pending markers exist in the file (D-11).
    4. In .planning/ROADMAP.md:
       - Update the Milestone v0.5 progress table row for Phase 30 to reflect 2/2 plans (D-12).
       - In ### Phase 30: Address tech debt: v0.5 cleanup and validation sign-off:
         - Set Goal: Address accumulated technical debt, visual polish preferences, environment fallback robustness, and validation coverage gaps from Milestone v0.5 audit, establishing 100% Nyquist compliance and multi-phase regression coverage (D-12).
         - Set Requirements: DEBT-05, DEBT-06, DEBT-07, DEBT-08 (D-12).
         - Document Success Criteria 1 through 4 (Kitty opacity 0.90, bootstrap virtualenv fallback, Phase 29 sign-off, Phase 30 assert harness and regression sweep) (D-12).
         - List Plans: 30-01-PLAN.md (Visual polish, environment fallback, and script signaling robustness) and 30-02-PLAN.md (Validation sign-off, Phase 30 assert harness, full regression sweep, and milestone closeout readiness) (D-12, D-13).
  </action>
  <acceptance_criteria>
    grep -q 'status: validated' .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-VALIDATION.md exits 0.
    grep -q 'nyquist_compliant: true' .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-VALIDATION.md exits 0.
    grep -q 'status: validated' .planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-VALIDATION.md exits 0.
    grep -q 'nyquist_compliant: true' .planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-VALIDATION.md exits 0.
    grep -q 'DEBT-05' .planning/REQUIREMENTS.md exits 0.
    grep -q 'DEBT-08' .planning/REQUIREMENTS.md exits 0.
    grep -c -E '\|\s*Pending\s*\|' .planning/REQUIREMENTS.md returns 0.
    grep -q 'DEBT-05, DEBT-06, DEBT-07, DEBT-08' .planning/ROADMAP.md exits 0.
    bash scripts/phase30-tech-debt-assert.sh --section 3 exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase30-tech-debt-assert.sh --section 3</automated>
    <fails_when>non-zero exit code, missing DEBT requirements in REQUIREMENTS.md, unvalidated 29-VALIDATION.md or 30-VALIDATION.md, stale Pending markers, or ROADMAP.md desynchronization</fails_when>
  </verify>
  <done>29-VALIDATION.md and 30-VALIDATION.md are marked validated and Nyquist-compliant, REQUIREMENTS.md registers all 19 requirements with 0 pending markers, and ROADMAP.md is synchronized.</done>
</task>

<task id="30-02-03" type="execute">
  <name>Execute full Phase 30 assert harness, strict system verification, and multi-phase regression sweep across Phases 25–29</name>
  <read_first>
    scripts/phase30-tech-debt-assert.sh
    arch/dots-hyprland.sh
    .planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-CONTEXT.md
  </read_first>
  <files>
    scripts/phase30-tech-debt-assert.sh
    arch/dots-hyprland.sh
  </files>
  <action>
    Execute the full verification suite and enforce the mandatory phase exit gate per D-09, D-14, and DEBT-08:
    1. Run all 5 sections of scripts/phase30-tech-debt-assert.sh:
       - Section 1: Kitty opacity 0.90 check and Phase 28 assert alignment (DEBT-05).
       - Section 2: Environment fallback export and hardened process signaling (DEBT-06).
       - Section 3: Nyquist compliance across all v0.5 phases (25–30) and requirements traceability (DEBT-07).
       - Section 4: Strict verifier gate (arch/dots-hyprland.sh verify --strict exits 0 with FAIL=0 FINDINGS=0) (DEBT-08).
       - Section 5: Full v0.5 regression sweep consecutively executing scripts/phase25-gtk-material-you-assert.sh, scripts/phase26-qt-kde-material-you-assert.sh, scripts/phase27-accent-coordination-assert.sh, scripts/phase28-terminal-fuzzel-assert.sh, and scripts/phase29-theme-data-contracts-assert.sh (DEBT-08).
    2. Confirm that the assert harness exits 0, reports 0 FAIL, 0 FINDINGS, and confirms git status --porcelain unchanged across run (D-14).
    3. Run arch/dots-hyprland.sh verify --strict independently and confirm exit code 0 (D-14).
    4. Confirm git status --porcelain is empty, proving the repository working tree is completely clean (D-14).
  </action>
  <acceptance_criteria>
    bash scripts/phase30-tech-debt-assert.sh exits 0 with FAIL=0 and FINDINGS=0.
    ./arch/dots-hyprland.sh verify --strict exits 0 with 0 findings.
    git status --porcelain output is empty.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase30-tech-debt-assert.sh && ./arch/dots-hyprland.sh verify --strict && test -z "$(git status --porcelain)"</automated>
    <fails_when>non-zero exit code from phase30 assert harness or strict verifier, any failed assertions, or dirty git working tree</fails_when>
  </verify>
  <done>All 5 sections of Phase 30 assert harness pass, strict repository verification succeeds with zero findings, multi-phase regression sweep passes across Phases 25 through 29, and git working tree is porcelain clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Multi-Phase Regression Suite → Repository Hygiene | Running multiple legacy assert scripts sequentially without introducing temporary files or modifying git-tracked state |
| Strict Verifier Engine → Link & Guard Taxonomy | Auditing packaging invariants across all stow and restow packages to guarantee zero dangling pointers or unmanaged paths |
| Requirements & Validation Contracts → Milestone Audit Gate | Maintaining 1:1 parity between documented requirements, phase plans, and automated test verdicts before milestone closeout |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-04 | Tampering / Silent Regression | scripts/phase30-tech-debt-assert.sh Section 5 | high | mitigate | Execute Phase 25 through Phase 29 assert scripts sequentially with fail-closed semantics (exit code 1 on any failure) and report output on error (D-09, D-14). |
| T-30-05 | Repudiation / Unverified Work | .planning/phases/29-*/29-VALIDATION.md & 30-VALIDATION.md | high | mitigate | Reconcile 29-VALIDATION.md and author 30-VALIDATION.md with explicit automated assert commands for all tasks, wave 0 mapping, and nyquist_compliant: true (D-08, D-10). |
| T-30-06 | Tampering / Git Working Tree Churn | Assert scripts and scratch test runners | medium | mitigate | Enforce PORCELAIN_BEFORE and PORCELAIN_AFTER comparison in scripts/phase30-tech-debt-assert.sh, failing if any test leaves uncommitted or untracked changes in repository (D-14). |
</threat_model>

<verification>
Automated verification commands:
1. `test -x scripts/phase30-tech-debt-assert.sh && bash scripts/phase30-tech-debt-assert.sh --help | grep -q -- '--section <1-5>'`
2. `bash scripts/phase30-tech-debt-assert.sh --section 1`
3. `bash scripts/phase30-tech-debt-assert.sh --section 2`
4. `bash scripts/phase30-tech-debt-assert.sh --section 3`
5. `bash scripts/phase30-tech-debt-assert.sh --section 4`
6. `bash scripts/phase30-tech-debt-assert.sh --section 5`
7. `bash scripts/phase30-tech-debt-assert.sh && ./arch/dots-hyprland.sh verify --strict && test -z "$(git status --porcelain)"`
</verification>

<success_criteria>
1. `scripts/phase30-tech-debt-assert.sh` exists at mode 0755 and supports `--section <1-5>` CLI options.
2. `29-VALIDATION.md` and `30-VALIDATION.md` are marked `status: validated` and `nyquist_compliant: true`.
3. `REQUIREMENTS.md` registers all 19 requirements (including `DEBT-05` through `DEBT-08`) with 0 `Pending` markers, and `ROADMAP.md` reflects Phase 30 requirements.
4. All 5 sections of `scripts/phase30-tech-debt-assert.sh` pass, `./arch/dots-hyprland.sh verify --strict` exits 0 with `FAIL=0 FINDINGS=0`, and the git working tree remains byte-identical clean.
</success_criteria>
