---
phase: "30"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - restow/kitty/.config/kitty/kitty.conf
  - scripts/phase28-terminal-fuzzel-assert.sh
  - bootstrap.sh
  - /home/pera/.config/matugen/templates/kde/kde-material-you-colors-wrapper.sh
  - /home/pera/.config/quickshell/ii/scripts/colors/applycolor.sh
autonomous: true
requirements: [DEBT-05, DEBT-06]

must_haves:
  truths:
    - "restow/kitty/.config/kitty/kitty.conf specifies background_opacity 0.90 per Phase 28 UAT preference (D-01, DEBT-05)"
    - "scripts/phase28-terminal-fuzzel-assert.sh lines 273, 282, 289 assert background_opacity 0.90 within float tolerance abs(opts.background_opacity - 0.90) > 0.01 with comments citing UAT preference and Phase 30 alignment (D-02, DEBT-05)"
    - "Live running Kitty instances are signaled via killall -SIGUSR1 kitty 2>/dev/null || true without terminating active shell sessions (D-03, DEBT-05)"
    - "Visual theming adjustments are strictly confined to Kitty background opacity without altering Fuzzel launcher alpha, terminal margins, or fonts (D-04, DEBT-05)"
    - "bootstrap.sh generate_initial_theme() exports ILLOGICAL_IMPULSE_VIRTUAL_ENV fallback before running switchwall.sh (D-05, DEBT-06)"
    - "bootstrap.sh contains idempotent sanitization blocks aligning kde-material-you-colors-wrapper.sh virtualenv parameter expansion fallback and applycolor.sh Kitty process signaling (D-06, D-07, DEBT-06)"
    - "Live ~/.config/matugen/templates/kde/kde-material-you-colors-wrapper.sh contains parameter expansion fallback ${ILLOGICAL_IMPULSE_VIRTUAL_ENV:-$XDG_STATE_HOME/quickshell/.venv} on line 46 (D-06, DEBT-06)"
    - "Live ~/.config/quickshell/ii/scripts/colors/applycolor.sh replaces pgrep/pidof signaling with killall -SIGUSR1 kitty 2>/dev/null || true (D-07, DEBT-06)"
  artifacts:
    - restow/kitty/.config/kitty/kitty.conf
    - scripts/phase28-terminal-fuzzel-assert.sh
    - bootstrap.sh
    - /home/pera/.config/matugen/templates/kde/kde-material-you-colors-wrapper.sh
    - /home/pera/.config/quickshell/ii/scripts/colors/applycolor.sh
  key_links:
    - "restow/kitty/.config/kitty/kitty.conf is symlinked to ~/.config/kitty/kitty.conf via GNU Stow from restow/"
    - "scripts/phase28-terminal-fuzzel-assert.sh is invoked by multi-phase regression sweeps in Phase 29 and Phase 30"
    - "bootstrap.sh Step 6 (capture_seed) invokes generate_initial_theme() on fresh machine installations"
    - "applycolor.sh and kde-material-you-colors-wrapper.sh are live hooks executed during dynamic wallpaper reloads"
  prohibitions:
    - "Must not modify files under vendor/dots-hyprland/"
    - "Must not alter Fuzzel launcher configuration or non-Kitty visual settings"
    - "Must not remove or alter non-opacity assertion checks in scripts/phase28-terminal-fuzzel-assert.sh"
    - "Must not use non-idempotent sed substitutions that corrupt files on repeated bootstrap execution"
---

<objective>
Implement Kitty visual polish and script robustness items identified in the Milestone v0.5 Audit: update Kitty background opacity from 0.85 to 0.90 in `restow/kitty/.config/kitty/kitty.conf` per Phase 28 UAT preference (D-01), align `scripts/phase28-terminal-fuzzel-assert.sh` to check for 0.90 opacity with tolerance and traceability comments (D-02), signal active Kitty windows via `killall -SIGUSR1 kitty 2>/dev/null || true` (D-03, D-04), export `ILLOGICAL_IMPULSE_VIRTUAL_ENV` fallback in `bootstrap.sh`'s `generate_initial_theme()` (D-05), add idempotent template alignment hooks to `bootstrap.sh` (D-06, D-07), and directly patch live `kde-material-you-colors-wrapper.sh` and `applycolor.sh` scripts (D-06, D-07).

Purpose: Fulfill user visual preferences from Phase 28 UAT and eliminate failure modes in non-graphical bootstrap runs and dynamic terminal signaling.
Output: Aligned `kitty.conf`, updated `scripts/phase28-terminal-fuzzel-assert.sh`, hardened `bootstrap.sh`, and patched live wrapper and signaling scripts.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-CONTEXT.md
@.planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-RESEARCH.md
@.planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-PATTERNS.md
@.planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-VALIDATION.md
@restow/kitty/.config/kitty/kitty.conf
@scripts/phase28-terminal-fuzzel-assert.sh
@bootstrap.sh
</context>

<assumptions>
- Updating background_opacity from 0.85 to 0.90 in restow/kitty/.config/kitty/kitty.conf honors the user preference recorded in 28-UAT.md.
- Kitty reloads configuration dynamically upon receiving POSIX signal SIGUSR1 without closing active terminal sessions or interrupting running commands.
- scripts/phase28-terminal-fuzzel-assert.sh is invoked during unified regression sweeps; updating its opacity assertion prevents false positive regressions.
- Non-graphical bootstrap runs (raw TTY, SSH) lack Hyprland environment hooks; exporting ILLOGICAL_IMPULSE_VIRTUAL_ENV provides child scripts with the virtualenv path.
- In ~/.config/quickshell/ii/scripts/colors/applycolor.sh, replacing pgrep -f kitty and kill -SIGUSR1 $(pidof kitty) with killall -SIGUSR1 kitty 2>/dev/null || true avoids regex false matches and missing PID errors.
</assumptions>

<tasks>

<task id="30-01-01" type="execute">
  <name>Update Kitty opacity to 0.90 in restow, align phase28 assert, and reload live terminals</name>
  <read_first>
    restow/kitty/.config/kitty/kitty.conf
    scripts/phase28-terminal-fuzzel-assert.sh
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-UAT.md
    .planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-CONTEXT.md
    .planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-PATTERNS.md
  </read_first>
  <files>
    restow/kitty/.config/kitty/kitty.conf
    scripts/phase28-terminal-fuzzel-assert.sh
  </files>
  <action>
    Update Kitty background opacity and harmonize Phase 28 regression assertions per D-01, D-02, D-03, D-04:
    1. In restow/kitty/.config/kitty/kitty.conf, edit line 3 from background_opacity 0.85 to background_opacity 0.90, fulfilling the user preference documented in 28-UAT.md (D-01). Strictly confine visual adjustments to this setting; preserve JetBrains Mono Nerd Font, font size 11.0, margin 21.75, shell zsh, and custom key bindings untouched (D-04).
    2. In scripts/phase28-terminal-fuzzel-assert.sh, update the native Kitty parser probe block around lines 268-292:
       - Update the comment above the python probe to explain that background_opacity was updated from 0.85 to 0.90 per Phase 28 UAT preference (28-UAT.md) and Phase 30 alignment (DEBT-05, D-02).
       - In line 273, change abs(opts.background_opacity - 0.85) > 0.01 to abs(opts.background_opacity - 0.90) > 0.01.
       - In line 274, change background_opacity expected 0.85 to background_opacity expected 0.90.
       - In line 282, change PASS: Kitty config loaded: opacity=0.85 to PASS: Kitty config loaded: opacity=0.90.
       - In line 289, change S3: Kitty configuration validated natively: opacity=0.85 to S3: Kitty configuration validated natively: opacity=0.90, shell=zsh, margin=21.75 (D-01, D-02, D-09).
    3. Signal all active Kitty instances by executing killall -SIGUSR1 kitty 2>/dev/null || true so running terminal windows apply opacity 0.90 immediately without dropping active shell sessions (D-03).
  </action>
  <acceptance_criteria>
    grep -q '^background_opacity 0.90' restow/kitty/.config/kitty/kitty.conf exits 0.
    grep -q 'abs(opts.background_opacity - 0.90) > 0.01' scripts/phase28-terminal-fuzzel-assert.sh exits 0.
    grep -q 'opacity=0.90' scripts/phase28-terminal-fuzzel-assert.sh exits 0.
    bash scripts/phase28-terminal-fuzzel-assert.sh --section 3 exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '^background_opacity 0.90' restow/kitty/.config/kitty/kitty.conf && grep -q 'abs(opts.background_opacity - 0.90) > 0.01' scripts/phase28-terminal-fuzzel-assert.sh && bash scripts/phase28-terminal-fuzzel-assert.sh --section 3</automated>
    <fails_when>non-zero exit code, background_opacity not 0.90 in restow/kitty/.config/kitty/kitty.conf, or phase28 assert section 3 failure</fails_when>
  </verify>
  <done>restow/kitty/.config/kitty/kitty.conf specifies background_opacity 0.90, scripts/phase28-terminal-fuzzel-assert.sh asserts 0.90 with tolerance, Phase 28 Section 3 passes cleanly, and live Kitty instances reloaded.</done>
</task>

<task id="30-01-02" type="execute">
  <name>Harden bootstrap.sh, applycolor.sh, and kde-material-you-colors-wrapper.sh environment fallback and signaling</name>
  <read_first>
    bootstrap.sh
    /home/pera/.config/matugen/templates/kde/kde-material-you-colors-wrapper.sh
    /home/pera/.config/quickshell/ii/scripts/colors/applycolor.sh
    .planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-CONTEXT.md
    .planning/phases/30-address-tech-debt-v0-5-cleanup-and-validation-sign-off/30-PATTERNS.md
  </read_first>
  <files>
    bootstrap.sh
    /home/pera/.config/matugen/templates/kde/kde-material-you-colors-wrapper.sh
    /home/pera/.config/quickshell/ii/scripts/colors/applycolor.sh
  </files>
  <action>
    Harden virtual environment fallback and process signaling across bootstrap and live scripts per D-05, D-06, D-07:
    1. In bootstrap.sh function generate_initial_theme() (around line 597):
       - Define local kde_wrapper="$target/.config/matugen/templates/kde/kde-material-you-colors-wrapper.sh" and local applycolor="$target/.config/quickshell/ii/scripts/colors/applycolor.sh".
       - Add an idempotent alignment block for kde_wrapper: if file exists and ! grep -Fq '${ILLOGICAL_IMPULSE_VIRTUAL_ENV:-', then in DRY_RUN mode log [DRY-RUN] Would add virtualenv fallback to kde-material-you-colors-wrapper.sh; else run sed -i 's|source "$(eval echo \$ILLOGICAL_IMPULSE_VIRTUAL_ENV)/bin/activate"|source "$(eval echo ${ILLOGICAL_IMPULSE_VIRTUAL_ENV:-$XDG_STATE_HOME/quickshell/.venv})/bin/activate"|g' "$kde_wrapper" and log [FIX] Aligned kde-material-you-colors-wrapper.sh virtualenv fallback (D-06).
       - Add an idempotent alignment block for applycolor: if file exists and grep -q 'kill -SIGUSR1 \$(pidof kitty)' "$applycolor", then in DRY_RUN mode log [DRY-RUN] Would align applycolor.sh Kitty process signaling; else run sed -i '/if ! pgrep -f kitty >\/dev\/null; then/,/kill -SIGUSR1 \$(pidof kitty)/c\  killall -SIGUSR1 kitty 2>/dev/null || true' "$applycolor" and log [FIX] Aligned applycolor.sh Kitty process signaling (D-07).
       - Export virtualenv fallback for non-graphical bootstrap execution: export ILLOGICAL_IMPULSE_VIRTUAL_ENV="${ILLOGICAL_IMPULSE_VIRTUAL_ENV:-${XDG_STATE_HOME:-$HOME/.local/state}/quickshell/.venv}" before switchwall.sh execution (D-05).
    2. Directly patch live ~/.config/matugen/templates/kde/kde-material-you-colors-wrapper.sh line 46: replace source "$(eval echo $ILLOGICAL_IMPULSE_VIRTUAL_ENV)/bin/activate" with source "$(eval echo ${ILLOGICAL_IMPULSE_VIRTUAL_ENV:-$XDG_STATE_HOME/quickshell/.venv})/bin/activate" (D-06).
    3. Directly patch live ~/.config/quickshell/ii/scripts/colors/applycolor.sh lines 45-48: replace if ! pgrep -f kitty >/dev/null; then return; fi; kill -SIGUSR1 $(pidof kitty) with killall -SIGUSR1 kitty 2>/dev/null || true (D-07).
  </action>
  <acceptance_criteria>
    grep -q 'export ILLOGICAL_IMPULSE_VIRTUAL_ENV="\${ILLOGICAL_IMPULSE_VIRTUAL_ENV:-' bootstrap.sh exits 0.
    grep -q 'Aligned kde-material-you-colors-wrapper.sh virtualenv fallback' bootstrap.sh exits 0.
    grep -q 'Aligned applycolor.sh Kitty process signaling' bootstrap.sh exits 0.
    grep -Fq '${ILLOGICAL_IMPULSE_VIRTUAL_ENV:-$XDG_STATE_HOME/quickshell/.venv}' "$HOME/.config/matugen/templates/kde/kde-material-you-colors-wrapper.sh" exits 0.
    grep -Fq 'killall -SIGUSR1 kitty 2>/dev/null || true' "$HOME/.config/quickshell/ii/scripts/colors/applycolor.sh" exits 0.
    grep -q 'pidof kitty' "$HOME/.config/quickshell/ii/scripts/colors/applycolor.sh" exits 1.
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'export ILLOGICAL_IMPULSE_VIRTUAL_ENV="\${ILLOGICAL_IMPULSE_VIRTUAL_ENV:-' bootstrap.sh && grep -q 'Aligned kde-material-you-colors-wrapper.sh virtualenv fallback' bootstrap.sh && grep -q 'Aligned applycolor.sh Kitty process signaling' bootstrap.sh && grep -Fq '${ILLOGICAL_IMPULSE_VIRTUAL_ENV:-$XDG_STATE_HOME/quickshell/.venv}' "$HOME/.config/matugen/templates/kde/kde-material-you-colors-wrapper.sh" && grep -Fq 'killall -SIGUSR1 kitty 2>/dev/null || true' "$HOME/.config/quickshell/ii/scripts/colors/applycolor.sh" && ! grep -q 'pidof kitty' "$HOME/.config/quickshell/ii/scripts/colors/applycolor.sh"</automated>
    <fails_when>non-zero exit code, missing virtualenv export or alignment hooks in bootstrap.sh, unpatched live wrapper, or unpatched live applycolor.sh</fails_when>
  </verify>
  <done>bootstrap.sh contains virtualenv export and idempotent sanitization blocks, live kde-material-you-colors-wrapper.sh has parameter expansion fallback, and live applycolor.sh uses atomic killall signaling without pidof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Terminal Process Signaling → System Process Table | Emitting POSIX signals from scripts to desktop terminals without affecting non-target processes or failing when terminals are inactive |
| Virtualenv Resolution → Script Execution Context | Sourcing python virtualenv across graphical, headless TTY, or SSH bootstrap environments without path injection |
| Bootstrap Orchestrator → User Configuration Tree | Stream-editing live template files idempotently without corrupting configuration syntax across repeated runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-01 | Denial of Service / Signaling Mismatch | ~/.config/quickshell/ii/scripts/colors/applycolor.sh | medium | mitigate | Replace pgrep -f / pidof construct with atomic killall -SIGUSR1 kitty 2>/dev/null \|\| true matching binary name strictly and ignoring absent instances fail-soft (D-07). |
| T-30-02 | Denial of Service / Headless Bootstrap Failure | bootstrap.sh & kde-material-you-colors-wrapper.sh | medium | mitigate | Export ILLOGICAL_IMPULSE_VIRTUAL_ENV fallback in generate_initial_theme() and add parameter expansion fallback ${ILLOGICAL_IMPULSE_VIRTUAL_ENV:-$XDG_STATE_HOME/quickshell/.venv} in wrapper script (D-05, D-06). |
| T-30-03 | Tampering / Submodule Modification Risk | vendor/dots-hyprland/ | high | mitigate | Confine modifications strictly to restow/kitty/, bootstrap.sh, and live ~/.config/ user files; leave pinned vendor submodule untouched (D-04, CAP-07). |
</threat_model>

<verification>
Automated verification commands:
1. `grep -q '^background_opacity 0.90' restow/kitty/.config/kitty/kitty.conf && grep -q 'abs(opts.background_opacity - 0.90) > 0.01' scripts/phase28-terminal-fuzzel-assert.sh && bash scripts/phase28-terminal-fuzzel-assert.sh --section 3`
2. `grep -q 'export ILLOGICAL_IMPULSE_VIRTUAL_ENV="\${ILLOGICAL_IMPULSE_VIRTUAL_ENV:-' bootstrap.sh && grep -q 'Aligned kde-material-you-colors-wrapper.sh virtualenv fallback' bootstrap.sh && grep -q 'Aligned applycolor.sh Kitty process signaling' bootstrap.sh`
3. `grep -Fq '${ILLOGICAL_IMPULSE_VIRTUAL_ENV:-$XDG_STATE_HOME/quickshell/.venv}' "$HOME/.config/matugen/templates/kde/kde-material-you-colors-wrapper.sh"`
4. `grep -Fq 'killall -SIGUSR1 kitty 2>/dev/null || true' "$HOME/.config/quickshell/ii/scripts/colors/applycolor.sh" && ! grep -q 'pidof kitty' "$HOME/.config/quickshell/ii/scripts/colors/applycolor.sh"`
</verification>

<success_criteria>
1. `restow/kitty/.config/kitty/kitty.conf` contains `background_opacity 0.90`, and `scripts/phase28-terminal-fuzzel-assert.sh --section 3` passes with exit code 0.
2. `bootstrap.sh` exports `ILLOGICAL_IMPULSE_VIRTUAL_ENV` fallback and contains idempotent alignment blocks for both template scripts.
3. Live `kde-material-you-colors-wrapper.sh` and `applycolor.sh` scripts contain hardened fallback syntax and fail-soft `killall` process signaling.
</success_criteria>
