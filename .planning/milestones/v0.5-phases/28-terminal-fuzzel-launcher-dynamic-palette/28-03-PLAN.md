---
id: 28-03
phase: 28-terminal-fuzzel-launcher-dynamic-palette
name: Live dynamic reload drill, strict repository verification, and validation sign-off
type: execute
wave: 3
depends_on: ["28-01", "28-02"]
files_modified:
  - scripts/phase28-terminal-fuzzel-assert.sh
  - .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-VALIDATION.md
autonomous: true
requirements: [TERM-01, TERM-02, INTG-02]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "~/.config/quickshell/ii/scripts/colors/switchwall.sh exists and is executable (D-05)"
    - "Executing switchwall.sh --noswitch runs Matugen synchronously, refreshes fuzzel_theme.ini, populates kitty-theme.conf and sequences.txt, and dispatches non-blocking reload signals, completing with exit 0 (D-05, D-06, D-12, TERM-01, TERM-02)"
    - "Executing switchwall.sh --noswitch advances file modification timestamps (mtime) on fuzzel_theme.ini, kitty-theme.conf, and sequences.txt (AFTER_MTIME > BEFORE_MTIME), resolving filesystem second-granularity race conditions via 1-second clock tick delay (D-17, D-18)"
    - "Dual-mode Kitty process probe dispatches live kill -SIGUSR1 to running Kitty instances (verifying the process remains alive with kill -0) or falls back gracefully to headless configuration validation in non-GUI / closed environments (D-05, D-18)"
    - "Packaging directories (stow/, restow/, capture/) remain 100% clean in git status --porcelain across wallpaper switching and test runs (INTG-02, D-19)"
    - "arch/dots-hyprland.sh verify --strict passes with FAIL=0 FINDINGS=0, confirming search.py and scroll_mark.py are verified symlinks and fuzzel_theme.ini is a guarded theme output (D-03, D-16, INTG-02)"
    - "Closing git porcelain snapshot check confirms zero repository drift across test runs (PORCELAIN_BEFORE equals PORCELAIN_AFTER) (D-19)"
    - "Full suite scripts/phase28-terminal-fuzzel-assert.sh passes all Sections 1 to 5 with FAIL=0 FINDINGS=0 (D-17)"
    - "28-VALIDATION.md is updated with status: validated and nyquist_compliant: true"
  artifacts:
    - scripts/phase28-terminal-fuzzel-assert.sh
    - .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-VALIDATION.md
  key_links:
    - "switchwall.sh is the master wallpaper switcher coordinating launcher and terminal palette generation"
    - "arch/dots-hyprland.sh verify --strict ensures Phase 28 maintains strict data contracts and zero git drift"
  prohibitions:
    - "Must not edit files under vendor/dots-hyprland/"
    - "Must not touch closed-phase assert scripts under scripts/"
    - "Must not restart terminal processes or kill active user sessions"
    - "Must not leave untracked or modified files in git status after test execution"
---

<objective>
Implement and verify Section 4 (Live reload drill via `switchwall.sh --noswitch` testing mtime advancement on `fuzzel_theme.ini`, `kitty-theme.conf`, and `sequences.txt`, plus dual-mode Kitty SIGUSR1 / headless probe covering TERM-01, TERM-02, D-05, D-18) and Section 5 (Packaging directories cleanliness, `arch/dots-hyprland.sh verify --strict` zero findings claiming `search.py`/`scroll_mark.py`, and closing git porcelain snapshot check covering INTG-01, INTG-02, D-17, D-19) in `scripts/phase28-terminal-fuzzel-assert.sh`. Execute the complete 5-section test suite, verify clean repository state, and complete validation sign-off in `28-VALIDATION.md`.

Purpose: Guarantee that the dynamic wallpaper reload pipeline executes reliably with high-precision mtime updates, seamless Kitty signal reload, secondary terminal escape sequence broadcasts, zero findings in strict dotfiles verification, and zero repository drift.
Output: Validated Section 4 and Section 5 in `scripts/phase28-terminal-fuzzel-assert.sh`, passing `verify --strict` watchdog, and signed-off `28-VALIDATION.md`.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-CONTEXT.md
@.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-RESEARCH.md
@.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-PATTERNS.md
@.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-VALIDATION.md
@scripts/phase28-terminal-fuzzel-assert.sh
@arch/dots-hyprland.sh
</context>

<assumptions>
- switchwall.sh --noswitch generates dynamic palettes for Fuzzel and Kitty without altering current desktop wallpaper image.
- Filesystem timestamp resolution on standard Linux ext4 is 1 second; a 1-second delay guarantees measurable mtime advancement.
- pgrep -f kitty accurately detects running Kitty processes for signal testing without matching editor plugins or scripts.
</assumptions>

<tasks>

<task id="28-03-01" type="execute">
  <title>Task 1: Implement and verify Section 4 (Live dynamic reload drill & process signaling)</title>
  <read_first>
    ~/.config/quickshell/ii/scripts/colors/switchwall.sh
    ~/.config/quickshell/ii/scripts/colors/applycolor.sh
    ~/.config/fuzzel/fuzzel_theme.ini
    ~/.local/state/quickshell/user/generated/terminal/kitty-theme.conf
    ~/.local/state/quickshell/user/generated/terminal/sequences.txt
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-PATTERNS.md
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-RESEARCH.md
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-CONTEXT.md
  </read_first>
  <action>
    Implement Section 4 assertions in scripts/phase28-terminal-fuzzel-assert.sh and verify execution per D-05, D-06, D-12, D-18, TERM-01, and TERM-02:
    1. Assert $XDG_CONFIG_HOME/quickshell/ii/scripts/colors/switchwall.sh exists and is executable.
    2. Record pre-drill modification timestamps (stat -c %Y) for:
       - $XDG_CONFIG_HOME/fuzzel/fuzzel_theme.ini
       - $XDG_STATE_HOME/quickshell/user/generated/terminal/kitty-theme.conf
       - $XDG_STATE_HOME/quickshell/user/generated/terminal/sequences.txt
    3. Execute sleep 1 to guarantee a filesystem clock tick before reload.
    4. Run "$SWITCHWALL" --noswitch and assert exit code 0 (fail-soft reload execution).
    5. Record post-drill modification timestamps and assert strictly monotonic advancement (AFTER > BEFORE) on all three generated target files (D-05, D-06, D-12).
    6. Dual-mode Kitty process probe (D-05, D-18):
       - If pgrep -f kitty finds a running instance: send kill -SIGUSR1 to the first PID, sleep 0.2s, and assert kill -0 confirms the process handled the signal live without terminating.
       - If Kitty is not running: log info notice affirming headless syntax verification was satisfied by Section 3.
    7. Run bash scripts/phase28-terminal-fuzzel-assert.sh --section 4 and confirm FAIL=0.
  </action>
  <acceptance_criteria>
    bash scripts/phase28-terminal-fuzzel-assert.sh --section 4 exits 0 with FAIL=0.
    fuzzel_theme.ini, kitty-theme.conf, and sequences.txt all exhibit advanced mtimes after switchwall execution.
    Running Kitty instance survives live SIGUSR1 dispatch without terminating.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase28-terminal-fuzzel-assert.sh --section 4</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
</task>

<task id="28-03-02" type="execute">
  <title>Task 2: Implement and verify Section 5 (Strict verification engine & zero git drift) and complete validation sign-off</title>
  <read_first>
    arch/dots-hyprland.sh
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-PATTERNS.md
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-VALIDATION.md
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-CONTEXT.md
  </read_first>
  <action>
    Implement Section 5 in scripts/phase28-terminal-fuzzel-assert.sh, execute full verification suite, and sign off 28-VALIDATION.md per D-03, D-16, D-17, D-19, INTG-01, and INTG-02:
    1. Section 5 implementation:
       - Assert packaging directories (stow/, restow/, capture/) are 100% clean in git status (git status --porcelain stow/ restow/ capture/ is empty).
       - Execute $REPO_ROOT/arch/dots-hyprland.sh verify --strict and assert it exits 0 with zero findings per INTG-02.
       - Assert verify engine classifies search.py and scroll_mark.py as verified symlinks ([PASS] verified:) rather than unclaimed stubs (D-03).
       - Assert verify engine classifies fuzzel_theme.ini as guarded theme output ([INFO] guarded theme output:) rather than an untracked finding (D-16).
    2. Closing porcelain snapshot check (D-19):
       - Compare $PORCELAIN_BEFORE against $PORCELAIN_AFTER using cmp -s, proving running the harness and reload drill causes zero repo mutations.
    3. Run individual Section 5: bash scripts/phase28-terminal-fuzzel-assert.sh --section 5.
    4. Run full test suite: bash scripts/phase28-terminal-fuzzel-assert.sh && ./arch/dots-hyprland.sh verify --strict.
       - Confirm all 5 sections report [PASS] with FAIL=0 FINDINGS=0.
    5. Update .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-VALIDATION.md:
       - Update task statuses in per-task verification map to completed.
       - Check Wave 0 requirements.
       - Set status: validated and nyquist_compliant: true in frontmatter and sign-off block.
  </action>
  <acceptance_criteria>
    bash scripts/phase28-terminal-fuzzel-assert.sh --section 5 exits 0 with FAIL=0.
    bash scripts/phase28-terminal-fuzzel-assert.sh && ./arch/dots-hyprland.sh verify --strict exits 0 with FAIL=0 FINDINGS=0.
    arch/dots-hyprland.sh verify --strict classifies search.py and scroll_mark.py as verified links and fuzzel_theme.ini as guarded theme output.
    28-VALIDATION.md frontmatter contains status: validated and nyquist_compliant: true.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase28-terminal-fuzzel-assert.sh && ./arch/dots-hyprland.sh verify --strict</automated>
    <fails_when>non-zero exit code or "[FAIL]" in assert output or non-zero findings in verify output</fails_when>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process Signaling (`kill -SIGUSR1`) → Host Desktop Sessions | Dispatching signals to process IDs matching running terminal emulators |
| Test Harness Execution → Git Repository Status | Exercising full wallpaper switching pipelines without polluting repository trees |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-05 | Denial of Service / Process Termination | Terminal process signaling | high | mitigate | Scope kill -SIGUSR1 strictly to user-owned $(pidof kitty), check process liveness with kill -0 post-signal, and provide graceful headless bypass if kitty is not running (D-05, D-18). |
| T-28-06 | Repudiation / Repository Pollution | Dynamic theme reload drill | medium | mitigate | Enforce git status porcelain snapshot comparison before and after execution to guarantee zero untracked or modified repository files across runs (D-19, INTG-02). |
</threat_model>

<verification>
Per-task automated checks:
1. `bash scripts/phase28-terminal-fuzzel-assert.sh --section 4`
2. `bash scripts/phase28-terminal-fuzzel-assert.sh && ./arch/dots-hyprland.sh verify --strict`
</verification>

<success_criteria>
1. `switchwall.sh --noswitch` executes cleanly with exit 0.
2. Modification timestamps for `fuzzel_theme.ini`, `kitty-theme.conf`, and `sequences.txt` advance monotonically across reload drill.
3. Dual-mode probe verifies live SIGUSR1 handling or headless syntax validity.
4. `arch/dots-hyprland.sh verify --strict` exits 0 with 0 findings, claiming helper kittens as verified symlinks.
5. `scripts/phase28-terminal-fuzzel-assert.sh` passes all 5 sections with FAIL=0.
6. `28-VALIDATION.md` signed off with `nyquist_compliant: true`.
</success_criteria>

<output>
Create `.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-03-SUMMARY.md` when done
</output>

## Multi-Source Coverage Audit

| Requirement | REQUIREMENTS.md | ROADMAP.md | VALIDATION.md | Plan & Task | Status |
|---|---|---|---|---|---|
| **TERM-01** | Line 29 | Phase 28 Criterion 1 | Task 28-03-01 | Plan 28-03 Task 1 | Planned (Wave 3) |
| **TERM-02** | Line 30 | Phase 28 Criterion 2 | Task 28-03-01 | Plan 28-03 Task 1 | Planned (Wave 3) |
| **INTG-02** | Line 35 | Phase 29 Criterion 2 | Task 28-03-02 | Plan 28-03 Task 2 | Planned (Wave 3) |

## Artifacts this phase produces
- Test assert harness: `scripts/phase28-terminal-fuzzel-assert.sh` (complete 5-section implementation)
- Signed validation artifact: `.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-VALIDATION.md`
- Plan summary: `.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-03-SUMMARY.md`
