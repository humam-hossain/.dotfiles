---
id: 28-01
phase: 28-terminal-fuzzel-launcher-dynamic-palette
name: Tracer harness scaffolding, template & config readiness, and Stow package deployments
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase28-terminal-fuzzel-assert.sh
  - stow/kitty/.config/kitty/kitty.conf
  - stow/kitty/.config/kitty/search.py
  - stow/kitty/.config/kitty/scroll_mark.py
  - stow/fuzzel/.config/fuzzel/fuzzel.ini
autonomous: true
requirements: [INTG-01, TERM-01, TERM-02]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "scripts/phase28-terminal-fuzzel-assert.sh exists at 0755 permissions and implements fail-closed dotfiles assert harness conventions (set -euo pipefail, cleanup trap, --section parsing, and porcelain snapshot check) (D-17)"
    - "guard-paths.tsv is verified to guard $XDG_CONFIG_HOME/fuzzel/fuzzel_theme.ini with strict tab-separation as generated_theme (D-16, INTG-01)"
    - "Matugen Fuzzel template vendor/dots-hyprland/dots/.config/matugen/templates/fuzzel/fuzzel_theme.ini declares token bindings for background (ff alpha), text (ff alpha), and border (dd alpha) (D-12, D-13, TERM-01)"
    - "~/.config/matugen/config.toml registers [templates.fuzzel] mapping to ~/.config/fuzzel/fuzzel_theme.ini (D-12)"
    - "stow/fuzzel/.config/fuzzel/fuzzel.ini is tracked in repo and symlinked to ~/.config/fuzzel/fuzzel.ini, declaring include=~/.config/fuzzel/fuzzel_theme.ini, terminal=kitty -1, radius=17, prompt='>>  ', and exit-immediately-if-empty=yes (D-14..D-16, TERM-01)"
    - "stow/kitty/.config/kitty/kitty.conf includes ~/.local/state/quickshell/user/generated/terminal/kitty-theme.conf, sets background_opacity 0.85, font_family JetBrains Mono Nerd Font 11.0, shell zsh, window_margin_width 21.75, cursor_trail 3, and upstream keybindings (D-01, D-02, D-08, D-09, TERM-02)"
    - "stow/kitty/.config/kitty/search.py and stow/kitty/.config/kitty/scroll_mark.py are copied from upstream vendor and stowed into ~/.config/kitty/, converting unclaimed stubs to managed symlinks (D-03, TERM-02)"
    - "Section 1 of scripts/phase28-terminal-fuzzel-assert.sh passes with FAIL=0 (INTG-01, TERM-01, TERM-02)"
  artifacts:
    - scripts/phase28-terminal-fuzzel-assert.sh
    - stow/kitty/.config/kitty/kitty.conf
    - stow/kitty/.config/kitty/search.py
    - stow/kitty/.config/kitty/scroll_mark.py
    - stow/fuzzel/.config/fuzzel/fuzzel.ini
  key_links:
    - "scripts/phase28-terminal-fuzzel-assert.sh is the gating test harness verifying all Phase 28 deliverables across Sections 1 to 5"
    - "guard-paths.tsv registration ensures arch/dots-hyprland.sh verify --strict recognizes ~/.config/fuzzel/fuzzel_theme.ini as a guarded dynamic theme path"
    - "stow/fuzzel and stow/kitty deploy declarative symlinks into ~/.config without folding or adopting"
  prohibitions:
    - "Must not edit files under vendor/dots-hyprland/"
    - "Must not touch closed-phase assert scripts under scripts/"
    - "Must not use stow --adopt when symlinking stow/kitty or stow/fuzzel"
    - "Must not specify shell fish in kitty.conf"
    - "Must not commit generated theme outputs (fuzzel_theme.ini, kitty-theme.conf, sequences.txt) to git tracking"
---

<objective>
Scaffold the Phase 28 assertion harness (`scripts/phase28-terminal-fuzzel-assert.sh`) with fail-closed structure covering Sections 1 through 5 (D-17), create the `stow/fuzzel` package tracking `fuzzel.ini` with upstream geometry, font, prompt, single-instance terminal runner (`terminal=kitty -1`), and dynamic theme inclusion (TERM-01, D-14..D-16), upgrade `stow/kitty/.config/kitty/kitty.conf` to upstream layout with `include ~/.local/state/quickshell/user/generated/terminal/kitty-theme.conf`, `background_opacity 0.85`, `shell zsh`, margin `21.75`, and personal cursor trails (TERM-02, D-01, D-02, D-08, D-09), claim upstream helper kittens `search.py` and `scroll_mark.py` into `stow/kitty/` (D-03), deploy both packages via GNU Stow without `--adopt`, and verify Section 1 passes with `FAIL=0` (INTG-01).

Purpose: Establish the automated verification harness gating all Phase 28 deliverables, deploy declarative stow packages for Fuzzel launcher and Kitty terminal, claim upstream helper kittens as verified symlinks, and confirm template and data contract readiness.
Output: Executable `scripts/phase28-terminal-fuzzel-assert.sh`, new package `stow/fuzzel`, upgraded package `stow/kitty`, and passing Section 1 assertions.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-CONTEXT.md
@.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-RESEARCH.md
@.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-PATTERNS.md
@.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-VALIDATION.md
@guard-paths.tsv
@stow/kitty/.config/kitty/kitty.conf
@vendor/dots-hyprland/dots/.config/kitty/kitty.conf
@vendor/dots-hyprland/dots/.config/fuzzel/fuzzel.ini
@scripts/phase27-accent-coordination-assert.sh
</context>

<assumptions>
- Fuzzel executes ephemerally on demand rather than as a background daemon; each launch reads live configuration and theme files fresh from disk.
- Kitty handles SIGUSR1 signals asynchronously without dropping child shells or active multiplexer sessions.
- In clean setups where ~/.local/state/quickshell/user/generated/terminal/kitty-theme.conf is not yet populated, switchwall.sh --noswitch generates the required palette.
- Secondary terminals (Alacritty, Foot) receive OSC color escape sequences broadcast to /dev/pts/* without modifying static repository configs.
</assumptions>

<tasks>

<task id="28-01-01" type="tracer">
  <title>Task 1: Scaffold Phase 28 terminal & Fuzzel launcher assertion harness (scripts/phase28-terminal-fuzzel-assert.sh)</title>
  <read_first>
    scripts/phase27-accent-coordination-assert.sh
    scripts/phase26-qt-kde-material-you-assert.sh
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-PATTERNS.md
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-CONTEXT.md
  </read_first>
  <action>
    Scaffold scripts/phase28-terminal-fuzzel-assert.sh with permissions 0755 adhering strictly to repository assert harness conventions per D-17, INTG-01, and INTG-02:
    1. Base scaffolding: Use #!/usr/bin/env bash, set -euo pipefail, resolve REPO_ROOT and ASSERT_SELF, export default XDG_CONFIG_HOME and XDG_STATE_HOME, initialize FAIL=0 and FINDINGS=0, and define pass(), fail(), finding(), and info() output helpers.
    2. Cleanup trap: Initialize TMP_FILES=() and SCRATCH_ROOTS=(), define cleanup() removing temporary files with rm -f and scratch directories with rm -rf, and register trap cleanup EXIT.
    3. CLI argument parsing: Support --section <1-5> and -h|--help, validating integer range 1 to 5 with fail-closed exit 1 on invalid arguments.
    4. Two-phase working tree snapshots: Implement porcelain_snapshot_raw and porcelain_snapshot filtering ephemeral noise (.commandcode/, scripts/__pycache__/), capturing git status before test execution into $PORCELAIN_BEFORE.
    5. Scaffold stubs for Sections 1 through 5.
    6. Closing porcelain snapshot check: Capture $PORCELAIN_AFTER, compare against $PORCELAIN_BEFORE using cmp -s (failing if git status changed across run per D-19), print === done: FAIL=$FAIL FINDINGS=$FINDINGS ===, and exit 1 if FAIL > 0, else exit 0.
  </action>
  <acceptance_criteria>
    test -x scripts/phase28-terminal-fuzzel-assert.sh exits 0.
    bash scripts/phase28-terminal-fuzzel-assert.sh --help exits 0 and outputs Usage: .* --section <1-5>.
    bash scripts/phase28-terminal-fuzzel-assert.sh --section 99 exits 1 with error message on stderr.
  </acceptance_criteria>
  <verify>
    <automated>test -x scripts/phase28-terminal-fuzzel-assert.sh && bash scripts/phase28-terminal-fuzzel-assert.sh --help | grep -q -- '--section <1-5>'</automated>
    <fails_when>non-zero exit code or missing --section help text</fails_when>
  </verify>
</task>

<task id="28-01-02" type="execute">
  <title>Task 2: Author stow/fuzzel package, upgrade stow/kitty with upstream layout and claimed kittens, link via GNU Stow, and verify Section 1</title>
  <read_first>
    vendor/dots-hyprland/dots/.config/fuzzel/fuzzel.ini
    vendor/dots-hyprland/dots/.config/kitty/kitty.conf
    stow/kitty/.config/kitty/kitty.conf
    vendor/dots-hyprland/dots/.config/kitty/search.py
    vendor/dots-hyprland/dots/.config/kitty/scroll_mark.py
    guard-paths.tsv
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-PATTERNS.md
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-CONTEXT.md
  </read_first>
  <action>
    Author repository configuration packages and verify Section 1 per D-01, D-02, D-03, D-04, D-07, D-08, D-09, D-12, D-13, D-14, D-15, D-16, TERM-01, TERM-02, and INTG-01:
    1. Create stow/fuzzel/.config/fuzzel/fuzzel.ini declaring: include="~/.config/fuzzel/fuzzel_theme.ini", font=Google Sans Flex:weight=medium, terminal=kitty -1 (D-15), prompt=">>  ", layer=overlay, [border] with radius=17 and width=1, and [dmenu] with exit-immediately-if-empty=yes per D-14, D-15, D-16.
    2. Upgrade stow/kitty/.config/kitty/kitty.conf adopting upstream dots-hyprland layout: include ~/.local/state/quickshell/user/generated/terminal/kitty-theme.conf, background_opacity 0.85, font_family JetBrains Mono Nerd Font 11.0, cursor_shape beam, personal cursor trail parameters (cursor_trail 3, cursor_trail_decay 0.1 0.4, cursor_trail_start_threshold 2), window_margin_width 21.75, confirm_os_window_close 0, explicitly shell zsh (omitting upstream fish per D-02), map ctrl+c copy_or_interrupt, map ctrl+f and kitty_mod+f to search.py kitten, and zoom/scroll bindings per D-01, D-02, D-08, D-09.
    3. Ensure cold-start seed per D-07: verify ~/.local/state/quickshell/user/generated/terminal/kitty-theme.conf exists, or seed from upstream template if missing so Kitty never logs a missing include error on clean setups.
    4. Copy vendor/dots-hyprland/dots/.config/kitty/search.py and vendor/dots-hyprland/dots/.config/kitty/scroll_mark.py into stow/kitty/.config/kitty/ to claim them under repository management per D-03.
    5. Safe Stow deployment: unlink existing regular files ~/.config/fuzzel/fuzzel.ini, ~/.config/kitty/search.py, and ~/.config/kitty/scroll_mark.py. From stow directory, execute stow --verbose=5 --no-folding -t ~ fuzzel and stow --verbose=5 --no-folding -t ~ kitty without using --adopt (CAP-07).
    6. Implement Section 1 in scripts/phase28-terminal-fuzzel-assert.sh asserting:
       - Matugen Fuzzel template exists and contains M3 background (ff alpha), text (ff alpha), and border (dd alpha) tokens (D-12, D-13).
       - ~/.config/matugen/config.toml registers [templates.fuzzel] mapping to output path ~/.config/fuzzel/fuzzel_theme.ini (D-12).
       - stow/kitty/.config/kitty/kitty.conf contains include statement for kitty-theme.conf (D-01) and cold-start seed target exists (D-07).
       - stow/fuzzel/.config/fuzzel/fuzzel.ini contains include statement for fuzzel_theme.ini, terminal=kitty -1 (D-15), radius=17, and exit-immediately-if-empty=yes (D-14, D-15, D-16).
       - guard-paths.tsv line 18 guards $XDG_CONFIG_HOME/fuzzel/fuzzel_theme.ini with tab separation and category generated_theme (D-16, INTG-01).
       - Helper kittens search.py and scroll_mark.py exist in stow/kitty/.config/kitty/ (D-03).
    7. Run bash scripts/phase28-terminal-fuzzel-assert.sh --section 1 and assert FAIL=0.
  </action>
  <acceptance_criteria>
    bash scripts/phase28-terminal-fuzzel-assert.sh --section 1 exits 0 with FAIL=0.
    test -L ~/.config/fuzzel/fuzzel.ini exits 0 and points to ../../github_repo/.dotfiles/stow/fuzzel/.config/fuzzel/fuzzel.ini or relative target.
    test -L ~/.config/kitty/search.py exits 0 and test -L ~/.config/kitty/scroll_mark.py exits 0.
    grep -q '^shell zsh' stow/kitty/.config/kitty/kitty.conf exits 0.
    grep -q '^background_opacity 0.85' stow/kitty/.config/kitty/kitty.conf exits 0.
    grep -q '^terminal=kitty -1' stow/fuzzel/.config/fuzzel/fuzzel.ini exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase28-terminal-fuzzel-assert.sh --section 1</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Assert Harness Execution → Git Working Tree State | Verification script execution mutating git tracked files or leaving temporary artifacts |
| Stow Symlink Management → User Home Directory | Creating symlinks without overwriting or adopting foreign host state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Tampering / Accidental Git Churn | guard-paths.tsv & porcelain checks | high | mitigate | Enforce git status porcelain snapshot comparison before and after harness execution, and assert $XDG_CONFIG_HOME/fuzzel/fuzzel_theme.ini is strictly registered in guard-paths.tsv to prevent repository tracking of dynamic launcher theme state (D-16, D-19, INTG-01). |
| T-28-02 | Elevation of Privilege / Insecure Package Linking | GNU Stow invocation | medium | mitigate | Prohibit stow --adopt strictly; unlink known regular files manually before running stow --no-folding to prevent pulling foreign files into git tracking (CAP-07). |
</threat_model>

<verification>
Per-task automated checks:
1. `test -x scripts/phase28-terminal-fuzzel-assert.sh && bash scripts/phase28-terminal-fuzzel-assert.sh --help | grep -q -- '--section <1-5>'`
2. `bash scripts/phase28-terminal-fuzzel-assert.sh --section 1`
</verification>

<success_criteria>
1. `scripts/phase28-terminal-fuzzel-assert.sh` exists with mode 0755, supporting `--section <1-5>` with clean porcelain snapshots.
2. `stow/fuzzel/.config/fuzzel/fuzzel.ini` exists, declaring upstream geometry, font, single-instance kitty runner, and dynamic theme include.
3. `stow/kitty/.config/kitty/kitty.conf` incorporates upstream layout, theme include, opacity 0.85, shell zsh, and upstream keybindings.
4. Upstream helper kittens `search.py` and `scroll_mark.py` are stowed under `stow/kitty/`.
5. `bash scripts/phase28-terminal-fuzzel-assert.sh --section 1` passes with FAIL=0.
</success_criteria>

<output>
Create `.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-01-SUMMARY.md` when done
</output>

## Multi-Source Coverage Audit

| Requirement | REQUIREMENTS.md | ROADMAP.md | VALIDATION.md | Plan & Task | Status |
|---|---|---|---|---|---|
| **INTG-01** | Line 34 | Phase 29 Criterion 1 | Task 28-01-01, 28-01-02 | Plan 28-01 Tasks 1 & 2 | Planned (Wave 1) |
| **TERM-01** | Line 29 | Phase 28 Criterion 1 | Task 28-01-02 | Plan 28-01 Task 2 | Planned (Wave 1) |
| **TERM-02** | Line 30 | Phase 28 Criterion 2 | Task 28-01-02 | Plan 28-01 Task 2 | Planned (Wave 1) |

## Artifacts this phase produces
- Test assert harness: `scripts/phase28-terminal-fuzzel-assert.sh`
- Fuzzel configuration package: `stow/fuzzel/.config/fuzzel/fuzzel.ini`
- Kitty configuration package: `stow/kitty/.config/kitty/kitty.conf`
- Kitty helper kittens: `stow/kitty/.config/kitty/search.py`, `stow/kitty/.config/kitty/scroll_mark.py`
- Plan summary: `.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-01-SUMMARY.md`
