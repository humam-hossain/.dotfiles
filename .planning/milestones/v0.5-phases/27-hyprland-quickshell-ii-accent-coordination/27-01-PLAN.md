---
phase: 27-hyprland-quickshell-ii-accent-coordination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase27-accent-coordination-assert.sh
autonomous: true
requirements: [SHELL-01, INTG-01]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "scripts/phase27-accent-coordination-assert.sh exists at 0755 permissions and implements fail-closed dotfiles assert harness conventions (set -euo pipefail, cleanup trap, --section parsing, and porcelain snapshot check) (D-29)"
    - "guard-paths.tsv is verified to guard $XDG_CONFIG_HOME/hypr/hyprland/colors.lua with strict tab-separation as generated_theme (INTG-01)"
    - "Upstream Matugen template vendor/dots-hyprland/dots/.config/matugen/templates/hyprland/colors.lua declares token bindings for active_border (outline_variant 77%), inactive_border (surface_container_low 33%), background_color (surface.dark FF%), and pinned border rule (primary accent gradient) (D-01..D-03, D-08, SHELL-01)"
    - "Live ~/.config/hypr/hyprland/colors.lua exists and is non-empty, declaring valid syntax (D-01, D-02)"
    - "stow/hypr/.config/hypr/custom/general.lua preserves upstream overlay purity with zero personal border overrides or col. declarations (D-04)"
    - "Section 1 of scripts/phase27-accent-coordination-assert.sh passes with FAIL=0 (INTG-01, SHELL-01)"
  artifacts:
    - scripts/phase27-accent-coordination-assert.sh
  key_links:
    - "scripts/phase27-accent-coordination-assert.sh is the gating test harness verifying all Phase 27 deliverables across Sections 1 to 5"
    - "guard-paths.tsv registration ensures arch/dots-hyprland.sh verify --strict recognizes ~/.config/hypr/hyprland/colors.lua as a guarded dynamic theme path"
  prohibitions:
    - "Must not edit files under vendor/dots-hyprland/ (permanent repository invariant)"
    - "Must not touch closed-phase assert scripts under scripts/ (D-20 frozen history rule)"
    - "Must not declare active_border, inactive_border, or col. overrides in stow/hypr/.config/hypr/custom/general.lua (D-04 upstream overlay purity)"
---

<objective>
Scaffold the Phase 27 assertion harness (`scripts/phase27-accent-coordination-assert.sh`) with fail-closed structure covering Sections 1 through 5 (D-29), verify that `$XDG_CONFIG_HOME/hypr/hyprland/colors.lua` is strictly guarded in `guard-paths.tsv` to protect generated colors from repository churn (INTG-01), assert that upstream Matugen templates bind the required border and background color tokens (D-01, D-02, D-03, D-08, SHELL-01), and verify upstream overlay purity in `stow/hypr/.config/hypr/custom/general.lua` with zero border overrides (D-04).

Purpose: Establish the automated test harness gating all Phase 27 deliverables, verify template readiness and data contract protection, and enforce upstream overlay purity.
Output: Executable `scripts/phase27-accent-coordination-assert.sh` passing Section 1 with FAIL=0.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-CONTEXT.md
@.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-RESEARCH.md
@.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-PATTERNS.md
@.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-VALIDATION.md
@guard-paths.tsv
@stow/hypr/.config/hypr/custom/general.lua
@scripts/phase26-qt-kde-material-you-assert.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Scaffold Phase 27 accent coordination assertion harness (scripts/phase27-accent-coordination-assert.sh)</name>
  <files>scripts/phase27-accent-coordination-assert.sh</files>
  <read_first>
    - scripts/phase26-qt-kde-material-you-assert.sh (lines 1-75: runner scaffolding, cleanup trap, --section parsing, porcelain snapshot functions)
    - .planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-PATTERNS.md (Pattern 1: phase27 assert harness structure)
    - .planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-CONTEXT.md (D-29: 5-section automated test suite specification)
  </read_first>
  <action>
    Scaffold `scripts/phase27-accent-coordination-assert.sh` with permissions 0755 adhering strictly to dotfiles assert harness patterns per D-29, INTG-01, and INTG-02:
    1. Base scaffolding: Use `#!/usr/bin/env bash`, `set -euo pipefail`, resolve `REPO_ROOT` and `ASSERT_SELF`, initialize `FAIL=0` and `FINDINGS=0`, define `pass()`, `fail()`, `finding()`, and `info()` output helpers.
    2. Cleanup trap: Initialize `TMP_FILES=()` and `SCRATCH_ROOTS=()`, define `cleanup()` removing temporary files and scratch directories with `rm -f / rm -rf`, and register `trap cleanup EXIT`.
    3. CLI argument parsing: Support `--section <1-5>` and `-h|--help`, validating integer range 1 to 5 with fail-closed exit 1 on invalid arguments.
    4. Two-phase working tree snapshots: Implement `porcelain_snapshot_raw` and `porcelain_snapshot` filtering ephemeral noise (`.commandcode/`, `scripts/__pycache__/`), snapshotting git status before test execution into `$PORCELAIN_BEFORE` to ensure zero repository drift across test runs.
    5. Scaffold stubs for Sections 1 through 5.
    6. Closing porcelain snapshot check: Capture `$PORCELAIN_AFTER`, compare against `$PORCELAIN_BEFORE` with `cmp -s` (failing if git status changed across run), print `=== done: FAIL=$FAIL FINDINGS=$FINDINGS ===`, and exit 1 if `FAIL > 0`, else exit 0.
  </action>
  <acceptance_criteria>
    - test -x scripts/phase27-accent-coordination-assert.sh returns 0.
    - bash scripts/phase27-accent-coordination-assert.sh --help returns 0 and outputs Usage: .* --section <1-5>.
    - bash scripts/phase27-accent-coordination-assert.sh --section 99 exits 1 with error message.
  </acceptance_criteria>
  <verify>
    <automated>test -x scripts/phase27-accent-coordination-assert.sh && bash scripts/phase27-accent-coordination-assert.sh --help | grep -q -- '--section <1-5>'</automated>
    <fails_when>non-zero exit code or missing --section help text</fails_when>
  </verify>
  <done>scripts/phase27-accent-coordination-assert.sh scaffolded at mode 0755 with fail-closed dotfiles assert harness structure and CLI argument parser.</done>
</task>

<task type="execute">
  <name>Task 2: Implement and verify Section 1 (Template & config readiness, guard-paths contract, and upstream overlay purity)</name>
  <files>scripts/phase27-accent-coordination-assert.sh</files>
  <read_first>
    - guard-paths.tsv (lines 1-22: guarded paths and tab-separation structure)
    - vendor/dots-hyprland/dots/.config/matugen/templates/hyprland/colors.lua (lines 1-17: upstream token declarations)
    - stow/hypr/.config/hypr/custom/general.lua (lines 1-28: verify absence of border overrides)
    - .planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-CONTEXT.md (D-01..D-04, D-08, D-29)
  </read_first>
  <action>
    Implement Section 1 assertions in `scripts/phase27-accent-coordination-assert.sh` and verify execution per D-01..D-04, D-08, INTG-01, and SHELL-01:
    1. Matugen hyprland template token verification (D-01..D-03, D-08):
       - Assert `$REPO_ROOT/vendor/dots-hyprland/dots/.config/matugen/templates/hyprland/colors.lua` (or fallback `$XDG_CONFIG_HOME/matugen/templates/hyprland/colors.lua`) exists.
       - Assert template binds `active_border` to `outline_variant` with 77% alpha (`rgba({{colors.outline_variant.default.hex_stripped}}77)`) per D-01.
       - Assert template binds `inactive_border` to `surface_container_low` with 33% alpha (`rgba({{colors.surface_container_low.default.hex_stripped}}33)`) per D-02.
       - Assert template binds canvas `background_color` to `surface.dark` with FF alpha (`rgba({{colors.surface.dark.hex_stripped}}FF)`) per D-08.
       - Assert template binds pinned window rule `border_color` to primary accent gradient (`rgba({{colors.primary.default.hex_stripped}}AA) rgba({{colors.primary.default.hex_stripped}}77)`) per D-03.
    2. Live `colors.lua` verification:
       - Assert `$XDG_CONFIG_HOME/hypr/hyprland/colors.lua` exists, is non-empty, and contains valid syntax.
    3. `guard-paths.tsv` contract verification (INTG-01):
       - Assert `$XDG_CONFIG_HOME/hypr/hyprland/colors.lua` is registered in `guard-paths.tsv`.
       - Assert the guard entry is classified as `generated_theme`, generated by `matugen`, and strictly delimited by tab characters (`IFS=$'\t'`) with zero spaces between columns.
    4. Upstream overlay purity assertion (D-04):
       - Assert `stow/hypr/.config/hypr/custom/general.lua` exists in repository.
       - Assert `stow/hypr/.config/hypr/custom/general.lua` contains zero `active_border`, `inactive_border`, `col.active`, `col.inactive`, `border_color`, or `background_color` declarations, ensuring upstream `colors.lua` retains sole dynamic authority over window decorations without personal overlay collision.
    5. Run `bash scripts/phase27-accent-coordination-assert.sh --section 1` and confirm it passes with `FAIL=0`.
  </action>
  <acceptance_criteria>
    - bash scripts/phase27-accent-coordination-assert.sh --section 1 exits 0 with FAIL=0.
    - grep -E -q '(active_border|inactive_border|col\.active|col\.inactive|border_color|background_color)' stow/hypr/.config/hypr/custom/general.lua returns 1 (no personal overrides).
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase27-accent-coordination-assert.sh --section 1</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>Section 1 template readiness, colors.lua existence, guard-paths tab contract, and upstream overlay purity assertions pass with FAIL=0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Assert Harness Execution → Git Working Tree State | Verification script execution mutating git tracked files or leaving temporary artifacts |
| Upstream Template Files → Live Shell Configuration | Matugen reading templates to generate live Lua configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering / Accidental Git Churn | `guard-paths.tsv` & porcelain checks | high | mitigate | Enforce git status porcelain snapshot comparison before and after harness execution, and assert `$XDG_CONFIG_HOME/hypr/hyprland/colors.lua` is strictly registered in `guard-paths.tsv` to prevent repository tracking of dynamic theme state (D-28, INTG-01). |
| T-27-02 | Configuration Injection via Untrusted Templates | Matugen templates | medium | mitigate | Matugen templates are read strictly from tracked repository vendor sources (`vendor/dots-hyprland/dots/.config/matugen/templates/`); no user-provided strings are evaluated as executable Lua code without template sanitization (D-01..D-04). |
</threat_model>

<verification>
Per-task automated checks:
1. `test -x scripts/phase27-accent-coordination-assert.sh && bash scripts/phase27-accent-coordination-assert.sh --help | grep -q -- '--section <1-5>'`
2. `bash scripts/phase27-accent-coordination-assert.sh --section 1`
</verification>

<success_criteria>
1. `scripts/phase27-accent-coordination-assert.sh` exists with mode 0755, supporting `--section <1-5>` with clean porcelain snapshots.
2. `guard-paths.tsv` contains `$XDG_CONFIG_HOME/hypr/hyprland/colors.lua` tab-separated as `generated_theme`.
3. `stow/hypr/.config/hypr/custom/general.lua` preserves upstream overlay purity with zero border overrides.
4. `bash scripts/phase27-accent-coordination-assert.sh --section 1` passes with FAIL=0.
</success_criteria>

<output>
Create `.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-01-SUMMARY.md` when done
</output>

## Multi-Source Coverage Audit

| Requirement | REQUIREMENTS.md | ROADMAP.md | VALIDATION.md | Plan & Task | Status |
|---|---|---|---|---|---|
| **SHELL-01** | Line 23 | Phase 27 Criterion 1 | Task 27-01-02 | Plan 27-01 Task 2 | Planned (Wave 1) |
| **INTG-01** | Line 34 | Phase 29 Criterion 1 | Task 27-01-01, 27-01-02 | Plan 27-01 Tasks 1 & 2 | Planned (Wave 1) |

## Artifacts this phase produces
- Test assert harness: `scripts/phase27-accent-coordination-assert.sh`
- Plan summary: `.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-01-SUMMARY.md`
