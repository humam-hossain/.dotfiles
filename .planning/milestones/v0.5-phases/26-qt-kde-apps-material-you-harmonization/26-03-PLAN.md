---
phase: 26-qt-kde-apps-material-you-harmonization
plan: 03
type: execute
wave: 3
depends_on: ["26-01", "26-02"]
files_modified:
  - scripts/phase26-qt-kde-material-you-assert.sh
  - .planning/phases/26-qt-kde-apps-material-you-harmonization/26-VALIDATION.md
autonomous: true
requirements: [QT-03, INTG-02]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "~/.config/xdg-desktop-portal/hyprland-portals.conf (or portals.conf) maps org.freedesktop.impl.portal.FileChooser = kde ensuring desktop file dialogs invoke xdg-desktop-portal-kde with Material You dark styling (D-12)"
    - "Dolphin (/usr/bin/dolphin) and Gwenview (/usr/bin/gwenview) are installed and executable, with Kate dropped from scope (QT-03, D-10)"
    - "~/.config/gwenviewrc remains unmanaged live runtime state to prevent git churn (D-11)"
    - "Git working tree in stow/ and restow/ remains completely clean across test execution (INTG-02)"
    - "arch/dots-hyprland.sh verify --strict exits 0 with 0 findings, classifying kde-material-you-colors as guarded (INTG-02)"
    - "Full suite scripts/phase26-qt-kde-material-you-assert.sh passes all Sections 1 to 5 with FAIL=0 FINDINGS=0 and porcelain unchanged (D-16, INTG-02)"
    - "26-VALIDATION.md is updated with status: validated and nyquist_compliant: true"
  artifacts:
    - scripts/phase26-qt-kde-material-you-assert.sh
    - .planning/phases/26-qt-kde-apps-material-you-harmonization/26-VALIDATION.md
  key_links:
    - "hyprland-portals.conf ensures file dialogs in sandboxed and native applications instantiate KDE portal pickers matching kdeglobals"
    - "arch/dots-hyprland.sh verify --strict ensures Phase 26 produces zero repository drift or symlink violations"
  prohibitions:
    - "Must not edit files under vendor/dots-hyprland/ (permanent invariant)"
    - "Must not touch closed-phase assert scripts under scripts/ (D-20 frozen history rule)"
    - "Must not track ~/.config/gwenviewrc in repository (D-11 volatile state prevention)"
    - "Must not install or force Kate integration (D-10 scope boundary)"
---

<objective>
Validate desktop portal file picker integration (`hyprland-portals.conf` mapping FileChooser to `kde` per D-12), verify KDE application readiness (Dolphin and Gwenview per D-10, leaving `gwenviewrc` unmanaged per D-11), execute the complete 5-section assert suite (`scripts/phase26-qt-kde-material-you-assert.sh` per D-16), run strict repository verification (`arch/dots-hyprland.sh verify --strict` per INTG-02), and complete validation sign-off in `26-VALIDATION.md`.

Purpose: Ensure desktop file pickers and primary KDE applications render consistently with the dark Material You theme, verify complete system integrity with zero repository drift, and fulfill all phase validation contracts.
Output: Validated Section 5 in `scripts/phase26-qt-kde-material-you-assert.sh`, passing `verify --strict` watchdog, and signed-off `26-VALIDATION.md`.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-qt-kde-apps-material-you-harmonization/26-CONTEXT.md
@.planning/phases/26-qt-kde-apps-material-you-harmonization/26-RESEARCH.md
@.planning/phases/26-qt-kde-apps-material-you-harmonization/26-PATTERNS.md
@.planning/phases/26-qt-kde-apps-material-you-harmonization/26-VALIDATION.md
@scripts/phase26-qt-kde-material-you-assert.sh
@arch/dots-hyprland.sh
</context>

<tasks>

<task type="execute">
  <name>Task 1: Validate desktop portal file picker mapping, KDE applications binary presence, and unmanaged runtime state rules (Section 5 part 1)</name>
  <files>scripts/phase26-qt-kde-material-you-assert.sh</files>
  <read_first>
    - scripts/phase26-qt-kde-material-you-assert.sh (lines 368-426: Section 5 implementation)
    - ~/.config/xdg-desktop-portal/hyprland-portals.conf (FileChooser mapping)
    - .planning/phases/26-qt-kde-apps-material-you-harmonization/26-CONTEXT.md (D-10, D-11, D-12)
  </read_first>
  <action>
    Implement and execute Section 5 portal and KDE application assertions in scripts/phase26-qt-kde-material-you-assert.sh per D-10, D-11, and D-12:
    1. Desktop FileChooser portal configuration (D-12):
       - Check `~/.config/xdg-desktop-portal/hyprland-portals.conf` (or fallback `portals.conf`).
       - Assert that `org.freedesktop.impl.portal.FileChooser = kde` is declared, ensuring all portal file dialogs instantiate KFileDialog and render with Material You dark colors.
    2. KDE application binary presence (D-10):
       - Assert that `/usr/bin/dolphin` is installed and executable.
       - Assert that `/usr/bin/gwenview` is installed and executable.
       - Affirm that Kate is dropped from testing scope as unneeded on this host per D-10.
    3. Unmanaged runtime state rule (D-11):
       - Assert that `~/.config/gwenviewrc` is NOT tracked in `stow/`, `restow/`, or git root to prevent git churn from window state and recent files.
    4. Run `bash scripts/phase26-qt-kde-material-you-assert.sh --section 5` to confirm portal and binary assertions pass.
  </action>
  <acceptance_criteria>
    - grep -q "org\.freedesktop\.impl\.portal\.FileChooser.*=.*kde" "$HOME/.config/xdg-desktop-portal/hyprland-portals.conf" returns 0.
    - test -x /usr/bin/dolphin && test -x /usr/bin/gwenview returns 0.
    - bash scripts/phase26-qt-kde-material-you-assert.sh --section 5 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase26-qt-kde-material-you-assert.sh --section 5</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>Section 5 portal and KDE application integration checks pass with 0 failures.</done>
</task>

<task type="execute">
  <name>Task 2: Execute full 5-section assert suite, enforce zero repository churn, run strict verification watchdog, and complete validation sign-off (Section 5 part 2)</name>
  <files>.planning/phases/26-qt-kde-apps-material-you-harmonization/26-VALIDATION.md</files>
  <read_first>
    - scripts/phase26-qt-kde-material-you-assert.sh (full script: sections 1-5, porcelain check)
    - arch/dots-hyprland.sh (lines 1120-1385: verify --strict implementation)
    - .planning/phases/26-qt-kde-apps-material-you-harmonization/26-VALIDATION.md (validation criteria and task map)
  </read_first>
  <action>
    Execute end-to-end verification, strict repository validation, and update Phase 26 validation sign-off per D-16 and INTG-02:
    1. Full assert suite execution (D-16):
       - Execute `bash scripts/phase26-qt-kde-material-you-assert.sh` without arguments (running all Sections 1 through 5).
       - Confirm output reports `=== done: FAIL=0 FINDINGS=0 ===`.
       - Verify closing porcelain comparison confirms zero git working tree modifications across the run.
    2. Strict repository verifier execution (INTG-02):
       - Run `./arch/dots-hyprland.sh verify --strict`.
       - Confirm `[PASS] guard path excluded: $XDG_CONFIG_HOME/kde-material-you-colors` is reported.
       - Confirm the watchdog exits 0 with zero findings.
    3. Update Phase 26 validation sign-off:
       - In `.planning/phases/26-qt-kde-apps-material-you-harmonization/26-VALIDATION.md`:
         - Update frontmatter: set `status: validated`, `nyquist_compliant: true`, and `wave_0_complete: true`.
         - In the Per-Task Verification Map table, mark all tasks (26-01-01 through 26-03-02) as completed (`✅ green`).
         - In Wave 0 Requirements, check off both items (`scripts/phase26-qt-kde-material-you-assert.sh` and `guard-paths.tsv` update).
         - In Validation Sign-Off, check all 6 checklist items and set `Approval: validated (2026-09-17)`.
  </action>
  <acceptance_criteria>
    - bash scripts/phase26-qt-kde-material-you-assert.sh exits 0 with FAIL=0 FINDINGS=0.
    - ./arch/dots-hyprland.sh verify --strict exits 0 with 0 findings.
    - grep -q "^nyquist_compliant: true" .planning/phases/26-qt-kde-apps-material-you-harmonization/26-VALIDATION.md returns 0.
    - grep -q "^status: validated" .planning/phases/26-qt-kde-apps-material-you-harmonization/26-VALIDATION.md returns 0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase26-qt-kde-material-you-assert.sh && ./arch/dots-hyprland.sh verify --strict</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>Full assert suite and strict repository verification pass with 0 errors, and validation sign-off is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Desktop Applications → XDG Desktop Portal Router (`xdg-desktop-portal`) | Sandboxed flatpaks and unprivileged GUI applications calling session DBus portal interfaces |
| Test Harness Execution → Git Working Tree State | Verification script execution mutating git tracked files or leaving temporary artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-02 | Accidental Secret or Git Churn Exposure | `stow/` and `restow/` trees & porcelain checks | medium | mitigate | Enforce git status porcelain snapshot comparison before and after harness execution, ensuring stow/ and restow/ remain 100% clean with zero untracked artifacts (INTG-02). |
| T-26-03 | Elevation of Privilege | `xdg-desktop-portal-kde` portal integration | low | accept | Portal communicates over unprivileged user session DBus via standard Freedesktop specifications; portal implementation runs entirely within user session permissions (D-12). |
</threat_model>

<verification>
Per-task automated checks:
1. `bash scripts/phase26-qt-kde-material-you-assert.sh --section 5`
2. `bash scripts/phase26-qt-kde-material-you-assert.sh && ./arch/dots-hyprland.sh verify --strict`
</verification>

<success_criteria>
1. `hyprland-portals.conf` maps `FileChooser` portal to `kde`.
2. `dolphin` and `gwenview` are confirmed installed and executable.
3. `gwenviewrc` is confirmed unmanaged.
4. `bash scripts/phase26-qt-kde-material-you-assert.sh` passes all 5 sections with `FAIL=0 FINDINGS=0`.
5. `./arch/dots-hyprland.sh verify --strict` passes with 0 findings.
6. `26-VALIDATION.md` is updated with `status: validated` and `nyquist_compliant: true`.
</success_criteria>

<output>
Create `.planning/phases/26-qt-kde-apps-material-you-harmonization/26-03-SUMMARY.md` when done
</output>

## Multi-Source Coverage Audit

| Requirement | REQUIREMENTS.md | ROADMAP.md | VALIDATION.md | Plan & Task | Status |
|---|---|---|---|---|---|
| **QT-03** | Line 19 | Phase 26 Criterion 3 | Task 26-03-01 | Plan 26-03 Task 1 | Planned (Wave 3) |
| **INTG-02** | Line 35 | Phase 29 Criterion 2 | Task 26-03-02 | Plan 26-03 Task 2 | Planned (Wave 3) |

## Artifacts this phase produces
- Assert harness final pass: `scripts/phase26-qt-kde-material-you-assert.sh`
- Validation sign-off: `.planning/phases/26-qt-kde-apps-material-you-harmonization/26-VALIDATION.md`
- Plan summary: `.planning/phases/26-qt-kde-apps-material-you-harmonization/26-03-SUMMARY.md`
