---
phase: 25-gtk-material-you-theming-catppuccin-de-linking
plan: 04
gap_closure: true
gap_ids: [G-25-5]
subsystem: desktop-theming
tags: [gtk, matugen, gap-closure, uat]
type: execute
wave: 4
depends_on: ["25-01", "25-02", "25-03"]
files_modified:
  - scripts/phase25-gtk-material-you-assert.sh
  - .planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-UAT.md
autonomous: true
requirements: [GTK-01]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "~/.config/matugen/templates/gtk-4.0/gtk.css line 312 defines .boxed-list row:disabled with zero occurrences of :insensitive"
    - "Regenerated ~/.config/gtk-4.0/gtk.css contains .boxed-list row:disabled and zero occurrences of :insensitive"
    - "GTK 4 CssProvider initializes and loads ~/.config/gtk-4.0/gtk.css with zero Gtk-WARNING parser errors"
    - "Section 4 of scripts/phase25-gtk-material-you-assert.sh asserts CSS validity, :disabled presence, and clean GTK 4 stylesheet loading without warnings"
    - "Toolkit boundaries between GTK 3/4 and Qt 6/KDE (dolphin, pavucontrol-qt, kdeglobals in Phase 26) and icon theme accents (Tela-circle-dracula-dark) are explicitly documented and resolved in 25-UAT.md"
    - "arch/dots-hyprland.sh verify --strict passes with FAIL=0 FINDINGS=0 and git status remains completely clean"
  artifacts:
    - scripts/phase25-gtk-material-you-assert.sh
    - .planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-UAT.md
  key_links:
    - "matugen template ~/.config/matugen/templates/gtk-4.0/gtk.css generates ~/.config/gtk-4.0/gtk.css on wallpaper switch"
    - "scripts/phase25-gtk-material-you-assert.sh gates verification of GTK 4 CSS parser compliance"
    - ".planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-UAT.md records UAT gap closure for G-25-5"
  prohibitions:
    - "Must not edit files under vendor/dots-hyprland/ (permanent invariant)"
    - "Must not edit ~/.config/kdeglobals manually; Qt/KDE theming belongs to Phase 26 (QT-01..QT-03)"
    - "Must not run matugen interactively without --source-color-index (prevents stdin hang)"
    - "Must not commit generated theme files ~/.config/gtk-3.0/gtk.css or ~/.config/gtk-4.0/gtk.css to git"
---

<objective>
Close UAT gap G-25-5 by fixing the GTK 4 Matugen template pseudo-class error (`.boxed-list row:insensitive` -> `.boxed-list row:disabled`) in `~/.config/matugen/templates/gtk-4.0/gtk.css`, regenerating `~/.config/gtk-4.0/gtk.css` non-interactively with active wallpaper colors, updating the Phase 25 assertion harness (`scripts/phase25-gtk-material-you-assert.sh`) to assert clean GTK 4 CSS loading without parser warnings, documenting the desktop toolkit and icon boundaries (differentiating GTK 3/4 from Qt 6/KDE `kdeglobals` and `Tela-circle-dracula-dark` folder icons), and signing off gap closure in `25-UAT.md`.

Purpose: Eliminate the GTK 4 theme parser warning on initialization, ensure GTK 4 applications parse dynamic stylesheets cleanly, clarify toolkit boundaries preventing cross-toolkit visual confusion, and achieve 5/5 passing UAT tests.
Output: Fixed Matugen GTK 4 template, cleanly regenerated `~/.config/gtk-4.0/gtk.css`, enhanced assert harness with GTK 4 parser check, and resolved `25-UAT.md`.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-CONTEXT.md
@.planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-UAT.md
@.planning/debug/DEBUG-gtk-visual-theming-pink-accent.md
@scripts/phase25-gtk-material-you-assert.sh
@guard-paths.tsv
@.gitignore
@arch/dots-hyprland.sh
</context>

<tasks>

<task type="execute">
  <name>Task 1: Fix GTK 4 Matugen template pseudo-class (:insensitive -> :disabled) and regenerate runtime GTK 4 stylesheet</name>
  <files>~/.config/matugen/templates/gtk-4.0/gtk.css, ~/.config/gtk-4.0/gtk.css</files>
  <read_first>
    - .planning/debug/DEBUG-gtk-visual-theming-pink-accent.md (Root Cause Analysis and Investigation Log)
    - ~/.config/matugen/templates/gtk-4.0/gtk.css (lines 305-325: .boxed-list row:insensitive rule)
    - ~/.config/gtk-4.0/gtk.css (lines 305-325: current runtime state)
  </read_first>
  <action>
    1. In `~/.config/matugen/templates/gtk-4.0/gtk.css`, replace `.boxed-list row:insensitive` on line 312 with `.boxed-list row:disabled`:
       ```css
       .boxed-list row:disabled {
         background-color: @card_bg_color;
       }
       ```
    2. Confirm that zero occurrences of `:insensitive` remain in `~/.config/matugen/templates/gtk-4.0/gtk.css`.
    3. Regenerate runtime GTK CSS stylesheets non-interactively using the active wallpaper:
       ```bash
       WP_PATH="$(cat "$HOME/.local/state/quickshell/user/generated/wallpaper/path.txt" 2>/dev/null || echo "$HOME/Pictures/55192173787_b8322b1190_o.jpg")"
       matugen --source-color-index 0 --mode dark image "$WP_PATH"
       ```
    4. Confirm that `~/.config/gtk-4.0/gtk.css` now contains `.boxed-list row:disabled` and zero occurrences of `:insensitive`.
    5. Test GTK 4 stylesheet loading in Python to verify clean parsing:
       ```bash
       python3 -c "import gi; gi.require_version('Gtk', '4.0'); from gi.repository import Gtk; Gtk.init(); p = Gtk.CssProvider(); p.load_from_path('$HOME/.config/gtk-4.0/gtk.css')"
       ```
       Ensure stderr emits zero `Gtk-WARNING` parser errors.
  </action>
  <verify>
    <automated>! grep -q ':insensitive' ~/.config/matugen/templates/gtk-4.0/gtk.css && grep -q '\.boxed-list row:disabled' ~/.config/matugen/templates/gtk-4.0/gtk.css && ! grep -q ':insensitive' ~/.config/gtk-4.0/gtk.css && grep -q '\.boxed-list row:disabled' ~/.config/gtk-4.0/gtk.css && ! python3 -c "import gi; gi.require_version('Gtk', '4.0'); from gi.repository import Gtk; Gtk.init(); p = Gtk.CssProvider(); p.load_from_path('/home/pera/.config/gtk-4.0/gtk.css')" 2>&1 | grep -q 'Gtk-WARNING'</automated>
    <fails_when>non-zero exit code or Gtk-WARNING emitted on stderr</fails_when>
  </verify>
  <done>~/.config/matugen/templates/gtk-4.0/gtk.css uses :disabled, ~/.config/gtk-4.0/gtk.css is regenerated with active wallpaper colors, and GTK 4 loads the stylesheet with zero parser warnings.</done>
</task>

<task type="execute">
  <name>Task 2: Extend Phase 25 assertion harness (scripts/phase25-gtk-material-you-assert.sh) with GTK 4 CSS parser compliance assertion</name>
  <files>scripts/phase25-gtk-material-you-assert.sh</files>
  <read_first>
    - scripts/phase25-gtk-material-you-assert.sh (lines 224-270: Section 4 implementation)
  </read_first>
  <action>
    1. In `scripts/phase25-gtk-material-you-assert.sh` under Section 4 (GTK-01 Dynamic Matugen CSS Generation):
       Add explicit checks verifying that:
       - `~/.config/gtk-4.0/gtk.css` does NOT contain `:insensitive` (GTK 3 deprecated selector).
       - `~/.config/gtk-4.0/gtk.css` contains `.boxed-list row:disabled`.
       - GTK 4 loads `~/.config/gtk-4.0/gtk.css` cleanly via `Gtk.CssProvider` with zero parser warnings:
         ```bash
         # 5. Assert GTK 4 CSS parser compliance (no Gtk-WARNING on load)
         if grep -q ':insensitive' "$CSS4"; then
           fail "GTK-01 (S4): ~/.config/gtk-4.0/gtk.css contains invalid :insensitive pseudo-class"
         else
           pass "GTK-01 (S4): ~/.config/gtk-4.0/gtk.css free of invalid :insensitive pseudo-class"
         fi

         if grep -q '\.boxed-list row:disabled' "$CSS4"; then
           pass "GTK-01 (S4): ~/.config/gtk-4.0/gtk.css specifies valid GTK 4 .boxed-list row:disabled selector"
         else
           fail "GTK-01 (S4): ~/.config/gtk-4.0/gtk.css missing .boxed-list row:disabled selector"
         fi

         GTK4_WARN="$(python3 -c "import gi; gi.require_version('Gtk', '4.0'); from gi.repository import Gtk; Gtk.init(); p = Gtk.CssProvider(); p.load_from_path('$CSS4')" 2>&1 || true)"
         if [[ -z "$GTK4_WARN" ]]; then
           pass "GTK-01 (S4): GTK 4 CssProvider loaded ~/.config/gtk-4.0/gtk.css with zero parser warnings"
         else
           fail "GTK-01 (S4): GTK 4 CssProvider emitted parser warnings: $GTK4_WARN"
         fi
         ```
    2. Run `./scripts/phase25-gtk-material-you-assert.sh --section 4` to verify the new assertion passes.
    3. Run `./scripts/phase25-gtk-material-you-assert.sh` across all 5 sections to verify overall green status.
  </action>
  <verify>
    <automated>./scripts/phase25-gtk-material-you-assert.sh</automated>
    <fails_when>non-zero exit code, FAIL > 0, or FINDINGS > 0</fails_when>
  </verify>
  <done>scripts/phase25-gtk-material-you-assert.sh includes automated GTK 4 CSS parser compliance assertion, and all sections pass with FAIL=0 FINDINGS=0.</done>
</task>

<task type="execute">
  <name>Task 3: Document toolkit boundaries, update 25-UAT.md to close gap G-25-5, and verify strict repository cleanliness</name>
  <files>.planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-UAT.md</files>
  <read_first>
    - .planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-UAT.md
    - .planning/debug/DEBUG-gtk-visual-theming-pink-accent.md
  </read_first>
  <action>
    1. Update `.planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-UAT.md`:
       - Change frontmatter `status` from `diagnosed` to `complete`.
       - In Test 5 ("Visual GTK Application Theming and Accent Rendering"):
         - Set `result: pass`.
         - Replace `reported` with verification notes:
           - GTK 4 CSS parser error eliminated (`:disabled` validated).
           - GTK 3 and GTK 4 applications (`nautilus`, `pavucontrol`, GTK file pickers) render dark Adwaita styling with dynamic wallpaper-derived teal/cyan (`#82d3e1`) accents and Google Sans Flex typography with zero parser warnings.
           - Toolkit boundary documented: The desktop default shortcuts launch Qt 6/KDE applications (`dolphin` on `SUPER + E` and `pavucontrol-qt` on volume hotkey) which read `~/.config/kdeglobals`. `kdeglobals` currently retains dots-hyprland's mauve/pink accents (`#cdb9fb` / `#b875dc`) and is harmonized in Phase 26 (`QT-01`..`QT-03`). Testing GTK theming requires invoking actual GTK applications (`nautilus`, `pavucontrol`).
           - Icon theme boundary documented: Folder tab accents in Nautilus render purple/pink (`#bd93f9`) because the active icon theme `Tela-circle-dracula-dark` defines Dracula palette SVGs; this is distinct from GTK widget styling (buttons, switches, selections) which renders wallpaper cyan.
       - Update Summary counts:
         ```yaml
         total: 5
         passed: 5
         issues: 0
         pending: 0
         skipped: 0
         blocked: 0
         ```
       - Update Gap `G-25-5`:
         - Set `status: resolved`.
         - Append `resolution`: "Eliminated GTK 4 parser warning by replacing :insensitive with :disabled in ~/.config/matugen/templates/gtk-4.0/gtk.css and regenerating ~/.config/gtk-4.0/gtk.css. Verified GTK 4 CssProvider loads cleanly with zero warnings. Documented toolkit boundary: Qt/KDE apps (Dolphin, pavucontrol-qt) read ~/.config/kdeglobals (harmonized in Phase 26), and folder icon tabs derive from Tela-circle-dracula-dark icon theme."
    2. Run strict verification engine to confirm zero repository drift:
       ```bash
       ./arch/dots-hyprland.sh verify --strict
       ```
    3. Check git status to ensure working tree remains clean without unexpected drift:
       ```bash
       git status --porcelain
       ```
  </action>
  <verify>
    <automated>./arch/dots-hyprland.sh verify --strict && grep -q 'status: resolved' .planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-UAT.md && grep -q 'passed: 5' .planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-UAT.md</automated>
    <fails_when>non-zero exit code or UAT test not marked resolved/passed</fails_when>
  </verify>
  <done>25-UAT.md updated with 5/5 tests passed and G-25-5 marked resolved with explicit toolkit and icon boundary documentation, strict verification passes with 0 findings, and git status is completely clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External Image Input (Wallpaper) → Matugen Template Engine | Active wallpaper image parsed by Matugen CLI to extract Material color palette |
| Matugen Template File → Live Runtime Stylesheet | `~/.config/matugen/templates/gtk-4.0/gtk.css` transformed into `~/.config/gtk-4.0/gtk.css` |
| Generated Theme Stylesheets → Git Repository Working Tree | Dynamically generated stylesheets residing in user config directories managed by GNU Stow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-04 | Denial of Service / Parser Crash | GTK 4 CSS Parser | low | mitigate | Replace deprecated GTK 3 `:insensitive` pseudo-class with GTK 4 `:disabled` in Matugen template; verify via `Gtk.CssProvider.load_from_path` that GTK 4 initialization generates zero `Gtk-WARNING` parser errors. |
| T-25-05 | Scope Creep / Configuration Drift | Qt/KDE `kdeglobals` Configuration | medium | mitigate | Explicitly restrict Phase 25 scope to GTK 3/4. Do not mutate `~/.config/kdeglobals` in Phase 25; defer Qt 5/6 and KDE application harmonization to Phase 26 (`QT-01`..`QT-03`). |
| T-25-06 | Accidental Secret or Git Churn Exposure | Generated `gtk.css` Stylesheets | medium | mitigate | Ensure `gtk-3.0/gtk.css` and `gtk-4.0/gtk.css` remain guarded by `guard-paths.tsv` and root `.gitignore`; `arch/dots-hyprland.sh verify --strict` asserts zero tracking in git and zero repository drift. |
</threat_model>

<verification>
Per-task automated checks:
1. `! grep -q ':insensitive' ~/.config/matugen/templates/gtk-4.0/gtk.css && grep -q '\.boxed-list row:disabled' ~/.config/matugen/templates/gtk-4.0/gtk.css && ! grep -q ':insensitive' ~/.config/gtk-4.0/gtk.css && grep -q '\.boxed-list row:disabled' ~/.config/gtk-4.0/gtk.css && ! python3 -c "import gi; gi.require_version('Gtk', '4.0'); from gi.repository import Gtk; Gtk.init(); p = Gtk.CssProvider(); p.load_from_path('/home/pera/.config/gtk-4.0/gtk.css')" 2>&1 | grep -q 'Gtk-WARNING'`
2. `./scripts/phase25-gtk-material-you-assert.sh`
3. `./arch/dots-hyprland.sh verify --strict && grep -q 'status: resolved' .planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-UAT.md && grep -q 'passed: 5' .planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-UAT.md`
</verification>

<success_criteria>
1. `~/.config/matugen/templates/gtk-4.0/gtk.css` and `~/.config/gtk-4.0/gtk.css` define `.boxed-list row:disabled` with zero occurrences of `:insensitive`.
2. GTK 4 `CssProvider` loads `~/.config/gtk-4.0/gtk.css` with zero `Gtk-WARNING` parser errors.
3. `scripts/phase25-gtk-material-you-assert.sh` passes all sections including the new GTK 4 parser compliance check with `FAIL=0 FINDINGS=0`.
4. `25-UAT.md` documents toolkit boundaries (GTK 3/4 vs Qt 6/KDE Phase 26 and Dracula icon folder tabs) and records 5/5 passed tests with gap `G-25-5` marked resolved.
5. `arch/dots-hyprland.sh verify --strict` passes with 0 findings and git status remains completely clean.
</success_criteria>

<output>
Create `.planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-04-SUMMARY.md` when done
</output>

## Multi-Source Coverage Audit

| Requirement | REQUIREMENTS.md | ROADMAP.md | UAT Gap | Plan & Task | Status |
|---|---|---|---|---|---|
| **GTK-01** | Line 10 | Phase 25 Criterion 3 | G-25-5 | Plan 25-04 Tasks 1 & 2 | Gap Closure (Wave 4) |
| **INTG-01** | Line 34 | Phase 25 Section 5 | - | Plan 25-04 Task 3 | Guard Enforced |
| **INTG-02** | Line 35 | Phase 25 Section 5 | - | Plan 25-04 Task 3 | Guard Enforced |

## Artifacts this phase produces
- Gap closure plan: `.planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-04-PLAN.md`
- Updated assert harness: `scripts/phase25-gtk-material-you-assert.sh`
- Updated UAT results: `.planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-UAT.md`
- Plan summary: `.planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-04-SUMMARY.md`
