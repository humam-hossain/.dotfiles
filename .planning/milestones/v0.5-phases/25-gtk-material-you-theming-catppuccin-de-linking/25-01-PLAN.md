---
phase: 25-gtk-material-you-theming-catppuccin-de-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase25-gtk-material-you-assert.sh
autonomous: true
requirements: [GTK-02]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "scripts/phase25-gtk-material-you-assert.sh exists at 0755 permissions and implements fail-closed dotfiles assert harness conventions (set -euo pipefail, four prefixes, cleanup trap, --section parsing, and porcelain snapshot check) (D-14)"
    - "Legacy Catppuccin symlinks in ~/.config/gtk-4.0/ (assets, gtk.css, gtk-dark.css) pointing into /usr/share/themes/ are permanently unlinked and removed (D-07)"
    - "No unmanaged gtk-dark.css symlink or file is created in ~/.config/gtk-4.0/ (D-08)"
    - "De-linking is strictly confined to unprivileged user configuration; system packages in /usr/share/themes/ remain untouched (D-09)"
    - "~/.config/gtk-4.0/ remains an unfolded physical directory containing the settings.ini symlink into stow/gtk/ (D-09)"
    - "Section 1 of scripts/phase25-gtk-material-you-assert.sh verifies Catppuccin symlink absence and parent directory unfolding, passing with FAIL=0 (GTK-02)"
  artifacts:
    - scripts/phase25-gtk-material-you-assert.sh
  key_links:
    - "scripts/phase25-gtk-material-you-assert.sh is the gating test harness verifying all Phase 25 deliverables across Sections 1 to 5"
    - "Unlinking ~/.config/gtk-4.0/gtk.css frees the inode so Matugen can create a writable regular file without permission errors"
  prohibitions:
    - "Must not edit files under vendor/dots-hyprland/ (permanent invariant)"
    - "Must not touch closed-phase assert scripts under scripts/ (D-20 frozen history rule)"
    - "Must not run pacman -R or delete files in /usr/share/themes/ (D-09 root isolation)"
    - "Must not create an unmanaged gtk-dark.css symlink in ~/.config/gtk-4.0/ (D-08)"
---

<objective>
Scaffold the Phase 25 assertion harness (`scripts/phase25-gtk-material-you-assert.sh`) with fail-closed structure covering Sections 1 through 5, and safely de-link legacy Catppuccin symlinks (`assets`, `gtk.css`, `gtk-dark.css`) from `~/.config/gtk-4.0/` to resolve the root-ownership write-lock (Q8) and establish `~/.config/gtk-4.0/` as an unfolded, unprivileged user directory ready for Matugen dynamic theming.

Purpose: Eliminate legacy root-pointing symlinks that block dynamic GTK 4 / libadwaita styling and provide the automated test harness gating all Phase 25 deliverables.
Output: Executable `scripts/phase25-gtk-material-you-assert.sh` and clean `~/.config/gtk-4.0/` directory state with passing Section 1.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-CONTEXT.md
@.planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-RESEARCH.md
@.planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-PATTERNS.md
@.planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-VALIDATION.md
@scripts/phase24-tech-debt-assert.sh
@scripts/phase22-kde-and-gtk-capture-assert.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Scaffold Phase 25 assertion harness (scripts/phase25-gtk-material-you-assert.sh) with fail-closed structure and Sections 1 to 5</name>
  <files>scripts/phase25-gtk-material-you-assert.sh</files>
  <read_first>
    - scripts/phase24-tech-debt-assert.sh (lines 1-75: scaffolding, cleanup trap, --section parsing, porcelain snapshots)
    - scripts/phase22-kde-and-gtk-capture-assert.sh (lines 234-332: GTK layout, unfolding, and guard path checks)
    - .planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-PATTERNS.md (Patterns 1.1-1.7: assert harness structure and section implementations)
    - .planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-CONTEXT.md (D-14: Phase assert test harness specification)
  </read_first>
  <action>
    Create scripts/phase25-gtk-material-you-assert.sh with permissions 0755 adhering strictly to dotfiles harness patterns per D-14:
    1. Runner scaffolding: Use #!/usr/bin/env bash, fail-closed set -euo pipefail, resolve REPO_ROOT and ASSERT_SELF, initialize FAIL=0 and FINDINGS=0 counters, define pass, fail, finding, and info helper functions.
    2. Cleanup trap: Initialize TMP_FILES and SCRATCH_ROOTS arrays, define cleanup function removing temporary files and scratch roots, and register trap cleanup EXIT.
    3. CLI argument parsing: Support --section <1-5> and -h|--help flags, validating integer range 1 to 5 with fail-closed exit 1 on invalid arguments.
    4. Working tree snapshots: Implement porcelain_snapshot_raw and porcelain_snapshot functions, snapshotting git status before tests into PORCELAIN_BEFORE to guarantee zero churn.
    5. Implement Section 1 (GTK-02 Catppuccin De-linking in ~/.config/gtk-4.0/):
       - Assert ~/.config/gtk-4.0/assets does NOT exist.
       - Assert ~/.config/gtk-4.0/gtk-dark.css does NOT exist per D-08.
       - Assert ~/.config/gtk-4.0/gtk.css is NOT a symlink into /usr/share/themes/catppuccin-*.
       - Assert ~/.config/gtk-4.0 is an unfolded physical directory (-d and ! -L).
    6. Implement Section 2 (GTK-03 Repository Settings Alignment in stow/gtk/):
       - Assert stow/gtk/.config/gtk-3.0/settings.ini and stow/gtk/.config/gtk-4.0/settings.ini contain 0 occurrences of catppuccin.
       - Assert gtk-theme-name=adw-gtk3-dark in both files per D-01.
       - Assert gtk-application-prefer-dark-theme=1 in both files per D-01.
       - Assert gtk-font-name=Google Sans Flex Medium 11 @opsz=11,wght=500 in both files per D-02.
       - Assert gtk-cursor-theme-name=Bibata-Modern-Classic and gtk-cursor-theme-size=24 in both files per D-03.
       - Assert gtk-icon-theme-name=Tela-circle-dracula-dark in both files per D-04.
       - Assert GTK 3 font rendering and sound flags preserved in gtk-3.0/settings.ini per D-05.
       - Assert bookmarks file preserved per D-06.
       - Assert live symlinks in $HOME/.config/ match repo inodes (-ef).
    7. Implement Section 3 (GTK-04 GNOME Desktop Interface GSettings):
       - Assert gsettings get org.gnome.desktop.interface gtk-theme is 'adw-gtk3-dark' per D-11.
       - Assert color-scheme is 'prefer-dark' per D-11.
       - Assert font-name is 'Google Sans Flex Medium 11 @opsz=11,wght=500' per D-11.
       - Assert cursor-theme is 'Bibata-Modern-Classic' and cursor-size is 24 per D-11.
       - Assert icon-theme is 'Tela-circle-dracula-dark' per D-11.
    8. Implement Section 4 (GTK-01 Dynamic Matugen CSS Generation):
       - Assert ~/.config/gtk-3.0/gtk.css is a non-empty regular file (-f, ! -L, -s) per D-12.
       - Assert ~/.config/gtk-4.0/gtk.css is a non-empty regular file (-f, ! -L, -s) per D-12.
       - Assert ~/.config/gtk-3.0/gtk.css contains @define-color accent_color and @define-color window_bg_color.
       - Assert ~/.config/gtk-4.0/gtk.css contains @media (prefers-color-scheme: dark) and @define-color accent_color.
       - Execute non-interactive matugen dry-run test with active wallpaper query passing --source-color-index 0.
    9. Implement Section 5 (INTG-01 & INTG-02 Verification Engine & Zero Churn):
       - Assert guard-paths.tsv guards both $XDG_CONFIG_HOME/gtk-3.0/gtk.css and $XDG_CONFIG_HOME/gtk-4.0/gtk.css per D-13.
       - Assert root .gitignore excludes gtk.css and gtk-dark.css per D-13.
       - Assert git status --porcelain stow/gtk is clean.
       - Execute ./arch/dots-hyprland.sh verify --strict and assert exit code 0 with zero findings per D-13.
    10. Closing bracket and summary: Compare porcelain snapshots, output summary line === done: FAIL=X FINDINGS=Y ===, and exit 1 if FAIL > 0, exit 0 otherwise.
  </action>
  <acceptance_criteria>
    - test -x scripts/phase25-gtk-material-you-assert.sh returns 0.
    - bash scripts/phase25-gtk-material-you-assert.sh --help returns 0 and outputs Usage: .* --section <1-5>.
    - bash scripts/phase25-gtk-material-you-assert.sh --section 99 exits 1 with error message.
  </acceptance_criteria>
  <verify>
    <automated>test -x scripts/phase25-gtk-material-you-assert.sh && bash scripts/phase25-gtk-material-you-assert.sh --help | grep -q -- '--section <1-5>'</automated>
    <fails_when>non-zero exit code or missing --section help text</fails_when>
  </verify>
  <done>scripts/phase25-gtk-material-you-assert.sh scaffolded with 0755 permissions, fail-closed structure, and full Sections 1-5 implementation.</done>
</task>

<task type="execute">
  <name>Task 2: Safely de-link legacy Catppuccin symlinks in ~/.config/gtk-4.0/ and verify unprivileged directory state</name>
  <files>scripts/phase25-gtk-material-you-assert.sh</files>
  <read_first>
    - .planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-CONTEXT.md (D-07, D-08, D-09, D-10)
    - .planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-RESEARCH.md (Section Runtime State Inventory, Pitfalls 2 & 4, Threat T-25-01)
    - .planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-PATTERNS.md (Pattern 1.3 & Pattern 4: De-linking execution)
  </read_first>
  <action>
    Safely unlink legacy Catppuccin symlinks in ~/.config/gtk-4.0/ to eliminate root-pointing references and prevent permission errors per D-07, D-08, D-09, and Threat T-25-01:
    1. Run rm -f "$HOME/.config/gtk-4.0/assets" to remove the symlink pointing to /usr/share/themes/catppuccin-mocha-teal-standard+default/gtk-4.0/assets per D-07.
    2. Run rm -f "$HOME/.config/gtk-4.0/gtk.css" to remove the symlink pointing to /usr/share/themes/catppuccin-mocha-teal-standard+default/gtk-4.0/gtk.css per D-07.
    3. Run rm -f "$HOME/.config/gtk-4.0/gtk-dark.css" to remove the legacy symlink per D-07.
    4. Enforce D-08: Do not recreate any gtk-dark.css symlink; libadwaita evaluates dark mode internally within gtk.css.
    5. Enforce D-09: Confine unlinking strictly to user configuration (~/.config/gtk-4.0/); leave system packages in /usr/share/themes/ completely untouched.
    6. Enforce D-10: Leave legacy GTK 2.0 files unmanaged.
    7. Verify directory integrity: Confirm that $HOME/.config/gtk-4.0 remains an unfolded regular physical directory (-d "$HOME/.config/gtk-4.0" && ! -L "$HOME/.config/gtk-4.0") containing the GNU Stow symlink settings.ini pointing into stow/gtk/.config/gtk-4.0/settings.ini.
    8. Reversibility procedure: If an operator ever needs to revert prior to Phase 25 completion, the static symlinks can be restored via:
       ln -s /usr/share/themes/catppuccin-mocha-teal-standard+default/gtk-4.0/assets "$HOME/.config/gtk-4.0/assets"
       ln -s /usr/share/themes/catppuccin-mocha-teal-standard+default/gtk-4.0/gtk.css "$HOME/.config/gtk-4.0/gtk.css"
       ln -s /usr/share/themes/catppuccin-mocha-teal-standard+default/gtk-4.0/gtk-dark.css "$HOME/.config/gtk-4.0/gtk-dark.css"
    9. Run bash scripts/phase25-gtk-material-you-assert.sh --section 1 to verify Section 1 passes with FAIL=0.
  </action>
  <acceptance_criteria>
    - test ! -e "$HOME/.config/gtk-4.0/assets" returns 0.
    - test ! -e "$HOME/.config/gtk-4.0/gtk-dark.css" returns 0.
    - test ! -L "$HOME/.config/gtk-4.0/gtk.css" returns 0.
    - test -d "$HOME/.config/gtk-4.0" && test ! -L "$HOME/.config/gtk-4.0" returns 0.
    - bash scripts/phase25-gtk-material-you-assert.sh --section 1 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase25-gtk-material-you-assert.sh --section 1</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>Legacy Catppuccin symlinks in ~/.config/gtk-4.0/ removed, directory verified unfolded, and Section 1 of assert harness passes with 0 failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Configuration (`~/.config/gtk-4.0/`) → System Packages (`/usr/share/themes/`) | User-space configuration symlinking into root-owned system package directories, creating permission write-locks and privilege boundaries |
| Test Harness Execution → Git Working Tree State | Verification script execution mutating git tracked files or leaving temporary artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Elevation of Privilege / Tampering | `~/.config/gtk-4.0/` symlinks | high | mitigate | Safely remove symlinks pointing to `/usr/share/themes/catppuccin-...` so user configuration cannot write through to system files or suffer EACCES denial of service during Matugen execution; keep system packages untouched (D-07, D-09). |
| T-25-03 | Accidental Secret or Git Churn Exposure | `scripts/phase25-gtk-material-you-assert.sh` | medium | mitigate | Enforce git status porcelain snapshot comparison before and after harness execution to ensure zero repository churn or untracked test debris. |
</threat_model>

<verification>
Per-task automated checks:
1. `test -x scripts/phase25-gtk-material-you-assert.sh && bash scripts/phase25-gtk-material-you-assert.sh --help | grep -q -- '--section <1-5>'`
2. `bash scripts/phase25-gtk-material-you-assert.sh --section 1`
</verification>

<success_criteria>
1. `scripts/phase25-gtk-material-you-assert.sh` exists with mode 0755, supporting `--section <1-5>` with clean porcelain snapshots.
2. `~/.config/gtk-4.0/assets`, `~/.config/gtk-4.0/gtk.css`, and `~/.config/gtk-4.0/gtk-dark.css` are removed.
3. `~/.config/gtk-4.0/` remains an unfolded physical directory with its `settings.ini` symlink intact.
4. `bash scripts/phase25-gtk-material-you-assert.sh --section 1` passes with FAIL=0.
</success_criteria>

<output>
Create `.planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-01-SUMMARY.md` when done
</output>

## Multi-Source Coverage Audit

| Requirement | REQUIREMENTS.md | ROADMAP.md | VALIDATION.md | Plan & Task | Status |
|---|---|---|---|---|---|
| **GTK-02** | Line 11 | Phase 25 Criterion 1 | Task 25-01-02 | Plan 25-01 Task 2 | Planned (Wave 1) |

## Artifacts this phase produces
- Test assert harness: `scripts/phase25-gtk-material-you-assert.sh`
- Plan summary: `.planning/phases/25-gtk-material-you-theming-catppuccin-de-linking/25-01-SUMMARY.md`
