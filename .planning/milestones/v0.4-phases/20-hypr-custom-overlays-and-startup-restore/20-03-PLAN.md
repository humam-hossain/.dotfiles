---
phase: 20-hypr-custom-overlays-and-startup-restore
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - stow/hypr/.config/hypr/custom/keybinds.lua
  - scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh
autonomous: true
requirements: [HYPR-02]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "`stow/hypr/.config/hypr/custom/keybinds.lua` precedes all personal keybind overrides with `hl.unbind` calls for the 8 upstream collisions (`SUPER + C, L, K, J, D, P, M, S`) plus `SUPER + Minus` per D-06"
    - "Personal keybinds map window closing (SUPER + C), window float toggle (SUPER + D), fullscreen maximize toggle (SUPER + ALT + D), pseudo-tile toggle (SUPER + P), split layout toggle (SUPER + Z), Vim focus navigation (SUPER + H/L/K/J), audio mute (SUPER + M), shell search (SUPER + Space), session lock (Scroll_Lock), session logout (SUPER + ALT + Scroll_Lock), special workspaces (SUPER + grave for social, SUPER + Minus for btop), relative workspace cycling (CTRL + SUPER + H/L), and window moves per D-07..D-14"
    - "Every keybind description strictly adheres to Category: Label cheatsheet taxonomy per D-15"
    - "Lua syntax for `stow/hypr/.config/hypr/custom/keybinds.lua` is validated and valid"
    - "Section 6 of `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` passes (`FAIL=0`)"
  artifacts:
    - stow/hypr/.config/hypr/custom/keybinds.lua
  key_links:
    - "`hl.unbind` calls remove upstream dispatchers before personal bindings are registered, preventing double-firing on shared key combinations"
    - "The `'Category: Label'` description syntax is parsed by Quickshell's `HyprlandKeybinds.qml` to display grouped categories in the `SUPER + /` cheatsheet"
---

<objective>
Author the repository-side keybinding configuration in `stow/hypr/.config/hypr/custom/keybinds.lua` implementing `hl.unbind` for all replaced upstream binds (D-06) and personal bindings labeled under the `"Category: Label"` cheatsheet taxonomy (HYPR-02, D-07..D-15).

Purpose: Eliminate duplicate keybinding actions on modified keys, re-establish intuitive window management and Vim focus navigation, and integrate personal keybinds cleanly into the Quickshell cheatsheet UI.
Output: Authored `stow/hypr/.config/hypr/custom/keybinds.lua` and Section 6 wired into the assert script.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-CONTEXT.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-RESEARCH.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-PATTERNS.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-02-SUMMARY.md
@vendor/dots-hyprland/dots/.config/hypr/hyprland/keybinds.lua
@/usr/share/hypr/stubs/hl.meta.lua
@docs/archive/hyprland.conf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author keybinds.lua with upstream unbinds and cheatsheet taxonomy</name>
  <files>stow/hypr/.config/hypr/custom/keybinds.lua</files>
  <read_first>
    - vendor/dots-hyprland/dots/.config/hypr/hyprland/keybinds.lua
    - /usr/share/hypr/stubs/hl.meta.lua (lines 850-870, 890-950)
    - docs/archive/hyprland.conf (lines 260-345)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-CONTEXT.md (D-06 through D-15)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-PATTERNS.md (lines 25-94)
  </read_first>
  <action>
    Create `stow/hypr/.config/hypr/custom/keybinds.lua` with the authoring header comment and the complete set of unbinds and personal keybinds (HYPR-02, D-06..D-15):
    1. Add authoring header:
       `-- Authoring SoT: parent-repo .config/hypr/custom/ (see 13-SOT-APPLY.md). Do not commit into vendor/dots-hyprland.`
    2. Upstream unbinds (D-06): Place all unbind calls at the top of the file before any binding:
       - `hl.unbind("SUPER + C")` (upstream code editor)
       - `hl.unbind("SUPER + L")` (upstream lock)
       - `hl.unbind("SUPER + K")` (upstream on-screen keyboard)
       - `hl.unbind("SUPER + J")` (upstream bar toggle)
       - `hl.unbind("SUPER + D")` (upstream maximize)
       - `hl.unbind("SUPER + P")` (upstream window pin)
       - `hl.unbind("SUPER + M")` (upstream media controls)
       - `hl.unbind("SUPER + S")` (upstream special scratchpad)
       - `hl.unbind("SUPER + Minus")` (upstream zoom out, which conflicts with special:btop)
    3. Window management bindings (D-07, D-08, D-10):
       - `hl.bind("SUPER + C", hl.dsp.window.close(), { description = "Window: Close" })`
       - `hl.bind("SUPER + D", hl.dsp.window.float({ action = "toggle" }), { description = "Window: Float/Tile" })`
       - `hl.bind("SUPER + ALT + D", hl.dsp.window.fullscreen({ mode = "maximized", action = "toggle" }), { description = "Window: Toggle maximize" })`
       - `hl.bind("SUPER + P", hl.dsp.window.pseudo(), { description = "Window: Toggle pseudo-tile" })`
       - `hl.bind("SUPER + Z", hl.dsp.layout("togglesplit"), { description = "Window: Toggle split layout" })`
    4. Vim-style window focus navigation bindings (D-09):
       - `hl.bind("SUPER + H", hl.dsp.focus({ direction = "l" }), { description = "Window: Focus left" })`
       - `hl.bind("SUPER + L", hl.dsp.focus({ direction = "r" }), { description = "Window: Focus right" })`
       - `hl.bind("SUPER + K", hl.dsp.focus({ direction = "u" }), { description = "Window: Focus up" })`
       - `hl.bind("SUPER + J", hl.dsp.focus({ direction = "d" }), { description = "Window: Focus down" })`
    5. Audio control binding (D-11):
       - `hl.bind("SUPER + M", hl.dsp.exec_cmd("wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle"), { description = "Audio: Toggle mute" })`
    6. Shell search binding (D-12):
       - `hl.bind("SUPER + Space", hl.dsp.global("quickshell:searchToggleRelease"), { description = "Shell: Toggle search" })`
    7. Session control bindings (D-13):
       - `hl.bind("Scroll_Lock", hl.dsp.exec_cmd("hyprlock"), { description = "Session: Lock screen" })`
       - `hl.bind("SUPER + ALT + Scroll_Lock", hl.dsp.exit(), { description = "Session: Logout" })`
    8. Special workspace and relative cycling bindings (D-14):
       - `hl.bind("SUPER + grave", hl.dsp.workspace.toggle_special("social"), { description = "Workspace: Toggle social" })`
       - `hl.bind("SUPER + SHIFT + grave", hl.dsp.window.move({ workspace = "special:social" }), { description = "Window: Move to social" })`
       - `hl.bind("SUPER + Minus", hl.dsp.workspace.toggle_special("btop"), { description = "Workspace: Toggle btop" })`
       - `hl.bind("SUPER + SHIFT + Minus", hl.dsp.window.move({ workspace = "special:btop" }), { description = "Window: Move to btop" })`
       - `hl.bind("CTRL + SUPER + H", hl.dsp.focus({ workspace = "e-1" }), { description = "Workspace: Previous (relative)" })`
       - `hl.bind("CTRL + SUPER + L", hl.dsp.focus({ workspace = "e+1" }), { description = "Workspace: Next (relative)" })`
       - `hl.bind("CTRL + SHIFT + SUPER + H", hl.dsp.window.move({ workspace = "e-1" }), { description = "Window: Move to previous workspace" })`
       - `hl.bind("CTRL + SHIFT + SUPER + L", hl.dsp.window.move({ workspace = "e+1" }), { description = "Window: Move to next workspace" })`
    9. Retain quick user editing helper:
       - `hl.bind("CTRL + SUPER + ALT + Slash", hl.dsp.exec_cmd("xdg-open ~/.config/hypr/custom/keybinds.lua"), { description = "System: Edit user keybinds" })`
    10. Verify Lua syntax using `python3 -c` with `luac -p` or Lua loader.
  </action>
  <acceptance_criteria>
    - `stow/hypr/.config/hypr/custom/keybinds.lua` exists.
    - Lua syntax check passes (`luac -p stow/hypr/.config/hypr/custom/keybinds.lua` exits 0).
    - Contains all 9 `hl.unbind` calls per D-06.
    - All `hl.bind` calls carry a `description` property formatted strictly as `"Category: Label"` per D-15.
  </acceptance_criteria>
  <verify>
    <automated>luac -p stow/hypr/.config/hypr/custom/keybinds.lua</automated>
  </verify>
  <done>Keybind overrides authored with unbinds, vim navigation, special workspaces, and cheatsheet taxonomy.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Section 6 asserts into assert script</name>
  <files>scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh</files>
  <read_first>
    - stow/hypr/.config/hypr/custom/keybinds.lua (from Task 1)
    - scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-CONTEXT.md (D-06, D-15, D-22)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-PATTERNS.md (lines 367-396)
  </read_first>
  <action>
    Implement Section 6 in `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh`: "HYPR-02: keybind unbinds and cheatsheet taxonomy" (D-06, D-15, D-22):
    1. Verify unbind declarations:
       Assert that `stow/hypr/.config/hypr/custom/keybinds.lua` contains all 9 unbind chords:
       `SUPER + C`, `SUPER + L`, `SUPER + K`, `SUPER + J`, `SUPER + D`, `SUPER + P`, `SUPER + M`, `SUPER + S`, `SUPER + Minus`.
    2. Verify cheatsheet taxonomy format:
       Use Python or awk to scan every `description = "..."` entry in `custom/keybinds.lua`.
       Assert that each description contains a colon separator separating a non-empty category and non-empty label (e.g. `Window: Close`, `Audio: Toggle mute`, `Shell: Toggle search`, `Session: Lock screen`, `Workspace: Toggle social`, `System: Edit user keybinds`).
    3. Verify static non-duplication:
       Assert that no key chord is bound more than once within `custom/keybinds.lua`.
    4. Run Section 6 via `--section 6` and ensure it exits 0 with `FAIL=0`.
  </action>
  <acceptance_criteria>
    - Section 6 is implemented in `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh`.
    - Executing `./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 6` exits 0.
    - Output logs `[PASS]` for all 9 unbind chords, `[PASS]` for cheatsheet taxonomy parsing, and `[PASS]` for non-duplication.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 6</automated>
  </verify>
  <done>Assert Section 6 verifies keybind unbinds and cheatsheet taxonomy formatting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keybind configuration → Compositor input handler | Keybinds directly affect compositor control; conflicting binds can cause unexpected window destruction or lockup |
| Hyprland IPC → Quickshell UI | Invalid description syntax can crash or corrupt the cheatsheet QML parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-06 | Tampering | Keybind table dispatch | high | mitigate | Precede all replaced keybindings with `hl.unbind` calls, preventing double-firing on identical keys |
| T-20-07 | Denial of Service | Lua syntax in keybinds.lua | medium | mitigate | Validate Lua syntax using `luac -p` before committing and applying |
| T-20-08 | Information Disclosure / Spoofing | Cheatsheet category taxonomy | low | mitigate | Assert Section 6 validates all descriptions match `"Category: Label"` format required by `HyprlandKeybinds.qml` |
</threat_model>

<verification>
Automated test command:
`luac -p stow/hypr/.config/hypr/custom/keybinds.lua && ./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 6`
Expected outcome: Lua syntax validation succeeds and assert section 6 reports `[PASS]` with exit 0 (`FAIL=0 FINDINGS=0`).
</verification>

<success_criteria>
1. `stow/hypr/.config/hypr/custom/keybinds.lua` exists and passes `luac -p` validation.
2. All 9 replaced upstream keys are unbound via `hl.unbind`.
3. All keybind descriptions adhere to `"Category: Label"` taxonomy.
4. Assert Section 6 passes with exit 0.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- `stow/hypr/.config/hypr/custom/keybinds.lua` — Complete personal keybind configuration with unbinds, vim focus navigation, and cheatsheet taxonomy (HYPR-02, D-06..D-15).
- `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` — Extended with Section 6.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-03-SUMMARY.md` when done
</output>
