---
phase: 20-hypr-custom-overlays-and-startup-restore
plan: 04
type: execute
wave: 4
depends_on: ["20-03"]
files_modified:
  - scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh
autonomous: true
requirements: [HYPR-01, SAFE-01]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Every GNU Stow invocation runs from the canonical main worktree root (parent of git-common-dir), refusing execution if running inside a linked worktree"
    - "A timestamped backup of ~/.config/hypr/custom is created before removing any live file per D-20"
    - "The live escape route drill is executed on the real system (stow -D, restore from backup, verify plain files restored, re-clean, re-stow) per SAFE-01 and D-21"
    - "All six files in ~/.config/hypr/custom/{env,execs,general,rules,keybinds,variables}.lua are valid symlinks resolving to the identical inodes in stow/hypr/.config/hypr/custom/ per HYPR-01"
    - "~/.config/hypr/custom is a real directory and no parent directory is a symlink (universal --no-folding honored)"
    - "Compositor reloads cleanly via hyprctl reload without errors"
    - "arch/dots-hyprland.sh verify --strict exits 0 with FAIL=0 FINDINGS=0"
    - "The complete assert suite scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh exits 0 with FAIL=0"
    - "Post-verification manual login check is documented for the operator per D-23"
  artifacts:
    - scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh
  key_links:
    - "Every live custom overlay file is identical in inode to the repository source under stow/hypr/.config/hypr/custom/"
    - "Compositor running under hyprland.lua loads the updated overlays without restarts"
---

<objective>
Execute the SAFE-01 bulk migration drill and live escape route rehearsal on the real desktop session, transition `~/.config/hypr/custom/` to full GNU Stow link-identity management (HYPR-01), reload the Hyprland compositor, and gate completion behind `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` and `arch/dots-hyprland.sh verify --strict`.

Purpose: Establish the first bulk GNU Stow link layer over live desktop files with proven reversibility (SAFE-01), ensuring every personal Hyprland override is the exact same inode in git and in the live session (HYPR-01).
Output: Live symlink farm established in `~/.config/hypr/custom/`, Section 7 passing in assert script, full suite green (`FAIL=0`), and documented post-verification login check.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-CONTEXT.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-RESEARCH.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-PATTERNS.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-03-SUMMARY.md
@.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-11-PLAN.md
@arch/dots-hyprland.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute SAFE-01 migration drill and live escape route rehearsal</name>
  <files>scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh</files>
  <read_first>
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-CONTEXT.md (D-20, D-21)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-PATTERNS.md (lines 263-289, 475-490)
    - .planning/phases/18-capture-model-three-trees-and-the-collision-map/18-11-PLAN.md (lines 95-125)
  </read_first>
  <action>
    Execute the SAFE-01 live migration and live escape rehearsal protocol strictly from the main repository worktree (SAFE-01, HYPR-01, D-20, D-21):
    1. Resolve the canonical main worktree root:
       `MAIN_ROOT="$(cd "$(git rev-parse --path-format=absolute --git-common-dir)/.." && pwd)"`.
       Assert that `git rev-parse --path-format=absolute --git-dir` equals `$(git rev-parse --path-format=absolute --git-common-dir)`. Refuse to proceed if running inside a disposable linked worktree, because stowing from a linked worktree creates symlinks resolving into a directory scheduled for wave deletion.
    2. Step 1 (Backup): Create a timestamped backup of the live custom overlay directory:
       `BACKUP_DIR="$HOME/.config/hypr/custom.backup.$(date +%s)"`
       `cp -a "$HOME/.config/hypr/custom" "$BACKUP_DIR"`
       Assert that `BACKUP_DIR` exists and contains regular files (`test -f "$BACKUP_DIR/keybinds.lua"`).
    3. Step 2 (Stub removal): Remove the 3 unmanaged plain stub files from `~/.config/hypr/custom/`:
       `rm "$HOME/.config/hypr/custom/keybinds.lua"`
       `rm "$HOME/.config/hypr/custom/rules.lua"`
       `rm "$HOME/.config/hypr/custom/variables.lua"`
       Assert that these three files are removed (`test ! -e`).
    4. Step 3 (Dry-run stow): Execute Stow dry run from `stow/` using `--no-folding`:
       `cd "$MAIN_ROOT/stow" && stow -n -v --no-folding -t ~ hypr`
       Assert exit code 0 and absence of conflict warnings.
    5. Step 4 (Bulk stow): Execute bulk stow from `stow/` using `--no-folding`:
       `cd "$MAIN_ROOT/stow" && stow -v --no-folding -t ~ hypr`
       Assert all 6 files in `~/.config/hypr/custom/` are symlinks (`test -L`).
    6. Step 5 (Rehearse live escape route):
       Unstow: `cd "$MAIN_ROOT/stow" && stow -D -v --no-folding -t ~ hypr`.
       Assert stow symlinks are removed.
       Restore from backup: `cp -a "$BACKUP_DIR/." "$HOME/.config/hypr/custom/"`.
       Assert that the restored files are regular files (`test ! -L "$HOME/.config/hypr/custom/keybinds.lua"`).
       Re-clean stubs: `rm "$HOME/.config/hypr/custom/keybinds.lua" "$HOME/.config/hypr/custom/rules.lua" "$HOME/.config/hypr/custom/variables.lua"`.
       Final re-stow: `cd "$MAIN_ROOT/stow" && stow -v --no-folding -t ~ hypr`.
       Assert all 6 files in `~/.config/hypr/custom/` are symlinks resolving to `$MAIN_ROOT/stow/hypr/.config/hypr/custom/*` (`test "$MAIN_ROOT/stow/hypr/.config/hypr/custom/keybinds.lua" -ef "$HOME/.config/hypr/custom/keybinds.lua"`).
    7. Step 6 (Compositor reload): Reload Hyprland configuration dynamically:
       `hyprctl reload`
  </action>
  <acceptance_criteria>
    - All 6 files (`env.lua`, `execs.lua`, `general.lua`, `keybinds.lua`, `rules.lua`, `variables.lua`) in `~/.config/hypr/custom/` are symlinks (`test -L`).
    - Each symlink is identical in inode to the repository source under `stow/hypr/.config/hypr/custom/` (`test "$repo" -ef "$live"`).
    - `~/.config/hypr/custom` is a real directory (`test ! -L`).
    - The timestamped backup directory exists under `~/.config/hypr/`.
    - `hyprctl reload` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>test -L ~/.config/hypr/custom/keybinds.lua && test -L ~/.config/hypr/custom/rules.lua && test -L ~/.config/hypr/custom/variables.lua && test ! -L ~/.config/hypr/custom && ./arch/dots-hyprland.sh verify --strict</automated>
  </verify>
  <done>Bulk stow migration and live escape route drill executed cleanly with inode link identity established.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Section 7 asserts, execute full phase gate, and document manual post-step</name>
  <files>scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (lines 1000-1045)
    - scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-CONTEXT.md (D-22, D-23)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-VALIDATION.md (lines 60-68)
  </read_first>
  <action>
    Complete the assert suite and phase verification (HYPR-01, SAFE-01, D-22, D-23):
    1. Implement Section 7 in `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh`: "HYPR-01: live overlay link identity and no parent directory folding":
       - Assert that `~/.config/hypr/custom` is a real directory, NOT a symlink (`test -d "$HOME/.config/hypr/custom"` and `test ! -L "$HOME/.config/hypr/custom"`).
       - Iterate over the 6 overlay files: `env.lua`, `execs.lua`, `general.lua`, `keybinds.lua`, `rules.lua`, `variables.lua`.
       - For each file, assert `test -L "$HOME/.config/hypr/custom/$f"`, assert `test -e "$HOME/.config/hypr/custom/$f"`, and assert `test "$REPO_ROOT/stow/hypr/.config/hypr/custom/$f" -ef "$HOME/.config/hypr/custom/$f"`.
       - Assert that `./arch/dots-hyprland.sh verify --strict` exits 0 with `FAIL=0 FINDINGS=0`.
    2. Run the full assert harness: `./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` without arguments so all 7 sections run. Assert that it exits 0 with `FAIL=0 FINDINGS=0`.
    3. Document the manual post-verification check for the operator (D-23, 20-VALIDATION.md):
       - On the next fresh graphical login, verify that:
         a) Chrome launches on workspace 1.
         b) Kitty with tmux launches on workspace 1.
         c) btop launches silently on special:btop (`SUPER + -` toggles).
         d) Vesktop/Discord launches silently on special:social (`SUPER + \`` toggles).
         e) Polkit KDE authentication agent prompts for password on administrative actions (`pkexec true`).
  </action>
  <acceptance_criteria>
    - Section 7 of `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` is implemented and passing.
    - Executing `./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` runs all 7 sections and exits 0 with `FAIL=0 FINDINGS=0`.
    - Executing `./arch/dots-hyprland.sh verify --strict` exits 0 with `FAIL=0 FINDINGS=0`.
    - Manual post-verification steps are clearly stated in plan summary.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh && ./arch/dots-hyprland.sh verify --strict</automated>
  </verify>
  <done>Phase 20 automated gate is green (FAIL=0) across all 7 sections and verify --strict; manual re-login check documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stow execution → Live filesystem | Bulk stow must never fold parent directory `~/.config/hypr/custom` or stow from an ephemeral worktree |
| Escape route → Session integrity | If stow or compositor reload encounters unexpected errors, the tested one-line undo must reliably restore the session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-08 | Tampering | Directory symlink folding | high | mitigate | All Stow commands strictly pass `--no-folding`; Section 7 assert explicitly tests `test ! -L ~/.config/hypr/custom` |
| T-20-09 | Tampering | Worktree isolation hazard | critical | mitigate | Refuse Stow execution if `git-dir != git-common-dir`; execute strictly from canonical main worktree root |
| T-20-10 | Information Disclosure / Loss | Live configuration files | high | mitigate | Take timestamped `cp -a` backup before removing unmanaged stubs; rehearse live escape route before final re-stow |
</threat_model>

<verification>
Automated test command:
`./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh && ./arch/dots-hyprland.sh verify --strict`
Expected outcome: All 7 assert sections report `[PASS]`, verify --strict reports `[PASS]`, and both commands exit 0 (`FAIL=0 FINDINGS=0`).
</verification>

<success_criteria>
1. All six overlay files in `~/.config/hypr/custom/` are symlinks resolving into `stow/hypr/.config/hypr/custom/` with identical inodes.
2. `~/.config/hypr/custom` is a real directory (not folded).
3. The SAFE-01 escape route drill is executed on the real system and verified.
4. `hyprctl reload` succeeds without errors.
5. `./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` exits 0 with `FAIL=0 FINDINGS=0`.
6. `./arch/dots-hyprland.sh verify --strict` exits 0.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- `~/.config/hypr/custom/env.lua` — Symlink to `stow/hypr/.config/hypr/custom/env.lua` (HYPR-01).
- `~/.config/hypr/custom/execs.lua` — Symlink to `stow/hypr/.config/hypr/custom/execs.lua` (HYPR-01).
- `~/.config/hypr/custom/general.lua` — Symlink to `stow/hypr/.config/hypr/custom/general.lua` (HYPR-01).
- `~/.config/hypr/custom/keybinds.lua` — Symlink to `stow/hypr/.config/hypr/custom/keybinds.lua` (HYPR-01).
- `~/.config/hypr/custom/rules.lua` — Symlink to `stow/hypr/.config/hypr/custom/rules.lua` (HYPR-01).
- `~/.config/hypr/custom/variables.lua` — Symlink to `stow/hypr/.config/hypr/custom/variables.lua` (HYPR-01).
- `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` — Completed with Section 7.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-04-SUMMARY.md` when done
</output>
