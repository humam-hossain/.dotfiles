---
phase: 19-link-aware-verify
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - arch/dots-hyprland.sh
  - scripts/phase19-link-aware-verify-assert.sh
autonomous: false
requirements: [VER-01]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A managed file whose ancestor directory is itself a symlink into the repo produces exactly one `[FAIL]` for that directory, carrying an unfold command, and not one `[FAIL]` per file underneath it (D-04)."
    - "The folded-ancestor test runs BEFORE the per-file link test, so a folded directory is reported as a folded directory rather than as N misplaced links (D-04)."
    - "A declared live path that is a symlink whose target does not exist is `[FAIL]` when the unresolved target lands under the repo, because that is `PITFALLS.md` A-6 — repo unavailable at login (D-06)."
    - "`readlink -f` is never used to decide the D-06 split: it returns the empty string for a dangling link and has already discarded where the link pointed. The raw target is read and resolved manually against the link's own directory (RESEARCH Pitfall 3)."
    - "A `stow/` or `restow/` repo-side file whose content differs from `HEAD` is `[INFO]`, never `[FINDING]` and never `[FAIL]` — the check cannot distinguish an installer write-through from an ordinary uncommitted edit, and reporting a guess as a defect violates the governing principle (D-21)."
    - "A `stow/` or `restow/` repo-side file that exists on disk but was never committed is reported as its own `[INFO]` class, because `git diff --quiet HEAD` returns 0 for it — a false statement of 'matches HEAD' (RESEARCH Pitfall 5, Open Question 3, decided in scope)."
    - "`git` is a declared required binary: its absence is a precondition failure with exit 2, decided before the walk starts (D-23)."
    - "The cp-through destruction class is reported as a content observation and not as a link failure: the link still `[PASS]`es, `verify` still exits 0, and the repo file's divergence from `HEAD` appears as `[INFO]` (D-27)."
  artifacts:
    - "arch/dots-hyprland.sh — the folded-ancestor pre-check, the dangling-into-repo arm, the repo-vs-`HEAD` observation and the untracked-file observation inside the repo-side walk, plus `git` in the required-binary list"
    - "scripts/phase19-link-aware-verify-assert.sh — Section 4, the cp-through boundary case"
  key_links:
    - "folded-ancestor check keyed per managed directory -> a memo of already-reported directories (without it the check emits one failure per file, which is the exact thing D-04 forbids)"
    - "`main_root` from `get_main_repo_root()` -> the repo-prefix criterion for both the folded-ancestor test and the D-06 dangling split (it must stay independent of `$HOME`, or the VER-04 harness silently compares against the wrong root)"
    - "`git ls-files` tracked set, read once -> the untracked-file arm (94 per-path `--error-unmatch` calls is the anti-pattern research names explicitly)"
  prohibitions:
    - statement: "`verify` must never accuse the operator of drift for an ordinary uncommitted edit, or for an unrelated file living in a shared root — a verdict that cries wolf teaches the operator to ignore the one run that matters."
      status: resolved
      verification: test
---

<objective>
Give the repo-side walk the three observations it structurally cannot make today: a folded ancestor directory, a link that dangles into the repo, and a repo-side file whose content no longer matches `HEAD`.

Purpose: criterion 1 requires `verify` to check, in order and failing at the first miss, that a declared path is a link, that it resolves to the expected repo path, that no ancestor directory is a symlink into the repo, and that it does not dangle. The existing Phase 18 loop covers the first two. This plan adds the other two plus the content observation that pins the boundary between the link class and the content class — the boundary the cp-through destruction sits exactly on.

Output: `run_verify()`'s repo-side walk gains a per-directory folded-ancestor pre-check, a dangling-into-repo arm, a repo-vs-`HEAD` `[INFO]`, and an untracked-repo-file `[INFO]`; `git` joins the declared required binaries; and the assert gains the cp-through section that proves the link class and the content class stay distinct.

**Carried red window:** `./scripts/phase13-d19-assert.sh` remains red for the wrapper drift pin until plan `19-05`. Expected. Do not re-pin here.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-link-aware-verify/19-CONTEXT.md
@.planning/phases/19-link-aware-verify/19-RESEARCH.md
@.planning/phases/19-link-aware-verify/19-PATTERNS.md
@.planning/phases/19-link-aware-verify/19-01-SUMMARY.md
@arch/dots-hyprland.sh
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Folded ancestor directories and links that dangle into the repo</name>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - `arch/dots-hyprland.sh` lines 715-845 — `get_main_repo_root()`, and `run_verify()` as left by plan `19-01`: the flag parser, the precondition block, the counters at 758-763, the repo-side walk, the `capture/` block, the summary
    - `.planning/research/PITFALLS.md` entry A-6 — repo unavailable at login, the behaviour split by whether the target's parent exists, and the `-xtype l` detection mandate
    - `.planning/phases/19-link-aware-verify/19-RESEARCH.md` — Architecture Patterns §Pattern 2 (the folded-ancestor walk), §Don't Hand-Roll, Pitfall 3 (`readlink -f` returns empty for a dangling link), and the measured finding that today's tree has zero folded ancestors and zero dangling links into the repo
    - `.planning/phases/19-link-aware-verify/19-PATTERNS.md` — the link-before-content test and recovery-message format excerpt, and the "Couplings to Respect" table
    - `scripts/phase17-unblock-assert.sh` lines 85-95 — the counted flag-pair grep this task must not disturb
  </read_first>
  <action>
Edit the repo-side walk in `run_verify()` only. The `capture/` block and the summary are untouched.

FOLDED-ANCESTOR PRE-CHECK (D-04). For each declared live path, walk its ancestor directory chain from the file's own directory upward, stopping before `$HOME` and before `/`. For each component that is itself a symlink, resolve it with `readlink -f` and test whether the resolved target lands under `$main_root`. A hit is a `[FAIL]` naming the component, its resolved target, and the unfold command. Only a symlink *into the repo* is the pathology criterion 1 names — a component that symlinks somewhere outside the repo is allowed and is resolved through silently (D-10).

The check must be per-directory, not per-file. Memo the directories already tested in an associative array keyed on the directory path, so one folded directory produces exactly one `[FAIL]` rather than one per file underneath it. That "one failure, not N" property is the whole point of D-04 and is also the `scripts/phase14-verify.sh` principle about a wall of derived failures. Run the check BEFORE the existing per-file `-e`/`-L` tests and, on a hit, skip the per-file tests for that file the way the existing arms already `continue`.

Write the recovery text as an unstow followed by a re-stow, both carrying the no-folding flag and `-t ~` with the package name. Do NOT include stow's verbosity flag anywhere in it, and do not add a code comment quoting that verbosity flag immediately followed by the no-folding flag: `scripts/phase17-unblock-assert.sh` lines 89-90 count that adjacent pair across `arch/*.sh` and assert the total is 18, `arch/dots-hyprland.sh` contributes zero today, and this file must keep contributing zero. The recovery form named here adds no match.

DANGLING-INTO-REPO ARM (D-06). Add an arm to the per-file chain, positioned after the `-L` test and before the `readlink -f` equality comparison, that handles a live path which is a symlink whose target does not exist. `readlink -f` is useless here: it returns the empty string for a dangling link, which both discards where the link pointed and, worse, compares equal to the equally-empty resolution of a missing repo-side target — the existing equality test at 793-797 would then report a broken pair as `[PASS]`. Read the raw target with plain `readlink --`, make it absolute by joining it against the link's own directory when it is relative, and take a `case` on the result: a target under `$main_root` is `[FAIL]` naming the link and its raw target, because that is precisely the A-6 condition (repo unavailable at login) that this phase exists to make loud; anything else is `[INFO]`. Keep the same `continue` discipline the surrounding arms use.

Add a short comment beside the new arm recording the measurement that today's tree has zero folded ancestors and zero dangling links into the repo — Phase 17's audit found two folded directories and Phase 18's redistribution resolved both — so neither check has a live positive to validate against, and the composite fixture in plan `19-03` is the only proof either one works.

Preserve the existing quoting and status discipline throughout: `--` before every path argument, every expansion quoted, `|| true` on probes whose non-zero status is expected, `local x` declared on a line separate from `x="$(cmd)"`, and counters incremented with the arithmetic-assignment form the file already uses.
  </action>
  <acceptance_criteria>
    - `run_verify()` contains a folded-ancestor loop that walks components upward and stops at `$HOME`, memoized per directory
    - the folded-ancestor `[FAIL]` message contains both an unstow and a re-stow invocation, each carrying the no-folding flag, and contains no occurrence of stow's verbosity flag
    - `grep -ho -- '--verbose=5 --no-folding' arch/*.sh | wc -l` still reports 18
    - the dangling arm reads the link target with a bare `readlink` (no `-f`) and resolves a relative target against the link's directory before testing the repo prefix
    - the dangling arm emits `[FAIL]` for a repo-bound target and `[INFO]` otherwise
    - `bash -n arch/dots-hyprland.sh` exits 0 and `./arch/dots-hyprland.sh verify` still exits 0 on the real tree with a `FAIL=0` summary
    - `./scripts/phase19-link-aware-verify-assert.sh` and `./scripts/phase17-unblock-assert.sh` and `./scripts/phase18-capture-model-assert.sh` all exit 0
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -u; bash -n arch/dots-hyprland.sh && [ "$(grep -ho -- "--verbose=5 --no-folding" arch/*.sh | wc -l)" -eq 18 ] && V="$(./arch/dots-hyprland.sh verify)" && printf "%s\n" "$V" | grep -q -- "=== done: FAIL=0" && ./scripts/phase19-link-aware-verify-assert.sh >/dev/null && ./scripts/phase17-unblock-assert.sh >/dev/null && ./scripts/phase18-capture-model-assert.sh >/dev/null'</automated>
    <fails_when>non-zero exit; or the stow flag-pair count reads anything other than 18 (the new recovery text introduced the counted adjacent pair); or the real-tree `verify` summary reports a non-zero FAIL count, which on a tree measured to have zero folded ancestors and zero repo-bound dangling links means the new checks are firing on something they should resolve through</fails_when>
    <automated>bash -c 'set -u; D="$(mktemp -d "${TMPDIR:-/tmp}/p19t1-XXXXXX")"; trap "rm -rf $D" EXIT; mkdir -p "$D/repo/arch" "$D/repo/stow/fx/.config/fxpkg" "$D/home/.config"; cp arch/dots-hyprland.sh "$D/repo/arch/"; chmod +x "$D/repo/arch/dots-hyprland.sh"; printf "v1\n" > "$D/repo/stow/fx/.config/fxpkg/conf"; ( cd "$D/repo" && git init -q . && git config user.email t@t && git config user.name t && git add -A && git commit -qm init ) >/dev/null; ln -s "$D/repo/stow/fx/.config/fxpkg" "$D/home/.config/fxpkg"; rc=0; OUT="$( cd "$D/repo" && HOME="$D/home" ./arch/dots-hyprland.sh verify )" || rc=$?; [ "$rc" -eq 1 ] || { echo "folded_rc=$rc"; exit 1; }; [ "$(printf "%s\n" "$OUT" | grep -c "^\[FAIL\]")" -eq 1 ] || { echo "not_one_fail"; printf "%s\n" "$OUT"; exit 1; }; printf "%s\n" "$OUT" | grep -q -- "stow" || { echo "no_recovery_command"; exit 1; }; echo FOLDED_OK'</automated>
    <fails_when>non-zero exit, or any of `folded_rc=`, `not_one_fail`, `no_recovery_command` appears, or `FOLDED_OK` is absent — `not_one_fail` in particular means the folded directory produced more than one failure, which is the per-file fan-out D-04 forbids</fails_when>
  </verify>
  <done>A folded ancestor directory that symlinks into the repo produces exactly one `[FAIL]` carrying an unfold command; a link that dangles into the repo produces a `[FAIL]` naming its raw target; a link that dangles outside the repo produces `[INFO]`; and the real tree, which has neither, still reports `FAIL=0`.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Make `git` a hard dependency of `./arch/dots-hyprland.sh verify` — its absence becomes exit 2</decision>
  <context>
D-23 is marked **one-way** in `19-CONTEXT.md`. Today `get_main_repo_root()` shells out to `git rev-parse` but falls back to `$REPO_ROOT` when it fails, so the dependency is soft: `verify` runs to a real verdict on a machine with no `git`. D-21's repo-vs-`HEAD` comparison has no such fallback — there is no filesystem substitute for "does this file differ from the last commit".

Once `verify` exits 2 without `git`, that is a published contract: any caller that runs it in a git-less context (a container, a rescue shell, an early bootstrap stage before the clone finishes) gets a precondition failure instead of a verdict. Reverting the decision later means dropping D-21 entirely, because D-21 is the only reason the dependency became hard.

The one known future caller is BOOT-04 in Phase 23 — a fresh-machine bootstrap that ends in a `verify` pass. That bootstrap clones the repo before it stows, so `git` is necessarily present by the time it calls `verify`. No caller outside this repo was found: grep locates the dispatch arm plus references in `scripts/phase18-capture-model-assert.sh`, nothing else.
  </context>
  <options>
    <option id="hard-dependency">
      <name>Declare `git` required — absent `git` is exit 2 (implements D-23 as written)</name>
      <pros>D-21's observation is either made honestly or refused outright, never silently degraded; matches the governing principle that `verify` never reports a condition it could not observe; the precondition block already exists and adding one entry to its binary list is a two-line change</pros>
      <cons>one-way: `verify` stops producing a verdict at all on a git-less machine, and reverting means dropping D-21's content observation entirely</cons>
    </option>
    <option id="soft-degrade">
      <name>Keep `git` optional — skip D-21's content observation and emit one `[INFO]` when `git` is absent</name>
      <pros>reversible; `verify` still produces a link verdict without `git`, which is the majority of its value</pros>
      <cons>contradicts D-23 as locked; creates a second, quieter meaning for a green run that a caller cannot distinguish from a full one; the `[INFO]` would be the only thing between a partial verdict and a full one, and nothing consumes it</cons>
    </option>
  </options>
  <recommendation>hard-dependency — this is D-23 as locked in `19-CONTEXT.md`, the one future caller provably has `git`, and `19-RESEARCH.md` §Environment Availability records "none — D-23 makes absence exit 2 by design" as the intended fallback. Escalated only because the decision is rated one-way.</recommendation>
  <resume-signal>Reply `hard-dependency` to proceed as locked, or `soft-degrade` to revise D-23 before Task 3 runs.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: The content observation, the untracked-file gap, and the cp-through boundary</name>
  <files>arch/dots-hyprland.sh, scripts/phase19-link-aware-verify-assert.sh</files>
  <read_first>
    - `arch/dots-hyprland.sh` lines 715-845 as left by Task 1 — the precondition block's required-binary list, and the repo-side walk's `pass "verified: …"` arm at the end of the per-file chain
    - `scripts/phase19-link-aware-verify-assert.sh` as left by plan `19-01` — the fixture builder, the runner, the criteria summary block
    - `.planning/phases/19-link-aware-verify/19-RESEARCH.md` — Pitfall 5 (the untracked-file gap, with its measured `rc=0` probe), §Alternatives Considered (the measured 0.437 s vs 0.006 s per-file-versus-batched `git` cost), §Don't Hand-Roll, §Open Questions item 3, §Code Examples "cp-through class"
    - `.planning/phases/19-link-aware-verify/19-CONTEXT.md` — D-21, D-22, D-23, D-27
    - `scripts/phase14-verify.sh` lines 10-14 — the principle that decides `[INFO]` over `[FINDING]` here
  </read_first>
  <action>
PRODUCTION HALF — `arch/dots-hyprland.sh`.

Add `git` to the required-binary list in the precondition block (D-23, per the checkpoint's resolution). Absent `git` prints `[FAIL] precondition: …` on fd 2 and exits 2, before the walk starts.

Add the content observation to the repo-side walk, immediately after the existing `pass "verified: …"` line, so a path that failed any link test never reaches it (D-46: link-ness before content, unconditionally). Compute it as `git -C <main repo root> diff --quiet HEAD -- <repo-side path>` exactly as D-21 specifies, with the root coming from `get_main_repo_root()` so it stays independent of `$HOME` — the VER-04 harness overrides `HOME` and the repo root must not follow it. A non-zero status is an `[INFO]` naming the repo-side path and stating that its content differs from `HEAD`. It is `[INFO]` and not `[FINDING]`: the check cannot distinguish an installer write-through from an ordinary uncommitted edit, and measured right now `git diff --name-only HEAD -- stow restow` reports one of the operator's own edits. Reporting a guess as a defect is what `scripts/phase14-verify.sh:10-14` forbids, and ROADMAP criterion 5 mandates `[INFO]` independently.

Honour D-21's per-file form literally. Research measured the per-file form at 0.437 s against 0.006 s for a single batched call, with identical results, on a subcommand whose total wall time is under a second. That cost is known and accepted; do not "optimise" it into a batch. Add a one-line comment saying so, so a later reader does not re-litigate it.

Close the untracked-file gap in the same pass (RESEARCH Pitfall 5; Open Question 3 is hereby decided IN SCOPE, as `[INFO]`, per the research recommendation — record that decision in a comment naming it as an extension of D-21 rather than a new one). The walk enumerates repo-side files from the filesystem, so a file that exists on disk but was never committed returns status 0 from `git diff HEAD` — a literal statement that it matches `HEAD`, which is false. Read the tracked set ONCE, before the walk, with a single `git ls-files -z -- stow restow` into an associative array keyed on the repo-relative path, and test membership per file. Do not add a per-path trackedness call: 94 additional `git` invocations on top of D-21's 94 is the anti-pattern research names explicitly. An untracked repo-side file is its own `[INFO]` class, distinct in wording from the differs-from-`HEAD` one, and still `[INFO]` because `verify` cannot distinguish a deliberate addition awaiting commit from a stray file.

Both new emissions are `[INFO]`, so neither moves a counter and neither can move the exit code under any flag (D-13). The `capture/` block keeps its `[FINDING]` for content drift unchanged (D-22) — that is a different observation with higher confidence, comparing two real artifacts rather than a file against a guess, and the differing labels are correct rather than an inconsistency.

ASSERT HALF — `scripts/phase19-link-aware-verify-assert.sh`, Section 4, appended before the closing block.

Section 4 proves the cp-through destruction class (D-27), which is the case that pins the boundary between the link class and the content class. Build a fresh fixture with the Task-1-of-`19-01` builder. Stage a vendor payload with different content. Then `cp -f` that payload over the *live* stowed path — through the link, not replacing it. Run the runner with no flags and assert four things: `rc` equals 0; the live path appears on a `[PASS]` line, because the link survived every `test -L` and `readlink -f` assertion intact; the repo-side path appears on an `[INFO]` line naming it as differing from `HEAD`; and the run's summary reports `FAIL=0 FINDINGS=0`, because an `[INFO]` moves neither counter.

Add a second case in the same section for the untracked arm: write a new repo-side file into the fixture's `stow/fixture` package without committing it, stow the package again so the live link exists, and assert it appears on the untracked `[INFO]` line while `rc` stays 0 and both counters stay 0. This is the case where `git diff --quiet HEAD` returns 0 and would otherwise have said nothing at all.

Add a short comment above Section 4 recording why this section is the one that makes `verify` honest: `PITFALLS.md` §30's claim is that both destroying installer primitives leave the repo file untouched, so a content-only `verify` reports no drift in exactly the case that matters — and the mirror of that claim is that a link-only `verify` reports a clean `[PASS]` for a repo file that was overwritten through an intact link. Section 1 proves the first half; Section 4 proves the second.

Extend the criteria summary block so ROADMAP criterion 4's cp-through half points at Section 4 and criterion 5's `[INFO]` half points at Section 4 as well.
  </action>
  <acceptance_criteria>
    - `git` appears in the required-binary list in `run_verify()`'s precondition block
    - the repo-side walk contains a `git -C` invocation using `diff --quiet HEAD --` positioned after the passing link assertion, and a single `git ls-files -z -- stow restow` read before the walk
    - `grep -c -- 'ls-files' arch/dots-hyprland.sh` reports at least 1 and the walk contains no `--error-unmatch` call
    - both new emissions go through the `info` helper; neither goes through `fail` or `finding`
    - `./arch/dots-hyprland.sh verify` exits 0 on the real tree and reports `FINDINGS=0`
    - Section 4 of the assert asserts `rc` equals 0, a `[PASS]` on the live path, an `[INFO]` on the repo path and a `FAIL=0 FINDINGS=0` summary for the cp-through case
    - Section 4 asserts a distinct untracked-file `[INFO]` for a never-committed repo-side file
    - `./scripts/phase19-link-aware-verify-assert.sh` exits 0 with `FAIL=0` and now emits at least 24 `[PASS]` lines
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -u; bash -n arch/dots-hyprland.sh && bash -n scripts/phase19-link-aware-verify-assert.sh && grep -q -- "diff --quiet HEAD" arch/dots-hyprland.sh && grep -q -- "ls-files" arch/dots-hyprland.sh && ! grep -q -- "error-unmatch" arch/dots-hyprland.sh && B="$(git status --porcelain --ignored)" && OUT="$(./scripts/phase19-link-aware-verify-assert.sh)" && A="$(git status --porcelain --ignored)" && [ "$B" = "$A" ] && printf "%s\n" "$OUT" | grep -q -- "=== done: FAIL=0" && [ "$(printf "%s\n" "$OUT" | grep -c "^\[PASS\]")" -ge 24 ] && V="$(./arch/dots-hyprland.sh verify)" && printf "%s\n" "$V" | grep -q -- "=== done: FAIL=0 FINDINGS=0 ==="'</automated>
    <fails_when>non-zero exit; or the per-path trackedness probe is present in the wrapper (the batched read was not used); or fewer than 24 `[PASS]` lines from the assert (Section 4 added nothing proven); or the real-tree summary reports a non-zero FINDINGS count, which would mean the content observation was wired as a finding rather than as information</fails_when>
    <automated>bash -c 'set -u; D="$(mktemp -d "${TMPDIR:-/tmp}/p19git-XXXXXX")"; trap "rm -rf $D" EXIT; mkdir -p "$D/bin"; for b in env bash find readlink cmp dirname basename mktemp realpath sed grep cat; do p="$(command -v "$b" 2>/dev/null)" && ln -sf "$p" "$D/bin/$b"; done; rc=0; PATH="$D/bin" ./arch/dots-hyprland.sh verify >/dev/null 2>"$D/err" || rc=$?; [ "$rc" -eq 2 ] || { echo "git_missing_rc=$rc"; head -5 "$D/err"; exit 1; }; grep -q -- "precondition" "$D/err" || { echo "no_precondition_reason"; head -5 "$D/err"; exit 1; }; echo GIT_PRECONDITION_OK'</automated>
    <fails_when>non-zero exit, or `git_missing_rc=` appears with any value other than 2, or `no_precondition_reason` appears, or `GIT_PRECONDITION_OK` is absent — a run with no `git` on PATH must refuse with a named precondition failure rather than fall back to a partial verdict</fails_when>
  </verify>
  <done>`verify` names a repo-side file that differs from `HEAD` and one that was never committed, both as `[INFO]` that moves no counter; a file overwritten through an intact link is `[PASS]` on the link and `[INFO]` on the content with exit 0; and `verify` refuses with exit 2 when `git` is absent.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing, and not subject to drift verification):

- `arch/dots-hyprland.sh`: the folded-ancestor memo associative array and its directory-walk loop inside `run_verify()`; the tracked-set associative array populated from a single `git ls-files -z` read; `git` as a member of the required-binary list
- New message classes: `[FAIL] folded ancestor directory: <dir> -> <target> — unfold with: …`, `[FAIL] dangling symlink into repo: <link> -> <raw target>`, `[INFO] dangling symlink (outside repo): <link> -> <raw target>`, `[INFO] repo file differs from HEAD: <path>`, `[INFO] repo file is untracked: <path>`
- `scripts/phase19-link-aware-verify-assert.sh`: Section 4 and its cp-through and untracked-file cases

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live filesystem → `run_verify()` | a `$HOME`-shaped path can reach the repo through a symlink, and one already does |
| `git` subprocess → `run_verify()` | repo-relative paths are passed to `git` as arguments |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-02 | Tampering | folded-ancestor and dangling-arm repo-prefix tests | high | mitigate | compare `readlink -f`-resolved (or manually-resolved) strings against `"$main_root"/*`, never a literal prefix on the unresolved argument — STATE.md Phase 17 records a live instance where `~/.config/systemd/user/hyprland-session.service` resolves into `stow/systemd/` and is accepted by a literal test and refused by the resolved one |
| T-19-03 | Tampering | `git -C … -- <path>` argument construction | medium | mitigate | `--` before every path argument so a path beginning with a dash cannot be read as an option; every expansion quoted; paths sourced from `find -print0`, never from word-splitting |
| T-19-04 | Denial of verification | new arms running under `set -euo pipefail` | high | mitigate | `git diff --quiet` status captured with the `rc=0; cmd || rc=$?` idiom rather than tested inline; `local x` split from `x="$(cmd)"`; no post-increment arithmetic |
| T-19-06 | Information disclosure | `[INFO]` lines naming repo-side paths | low | accept | paths only, never content; output is local and already reachable by the operator via `git status` |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | this phase installs no external packages; `19-RESEARCH.md` §Package Legitimacy Audit records the gate as vacuous |
</threat_model>

<verification>
- `bash -n` clean on both files
- `./arch/dots-hyprland.sh verify` exits 0 on the real tree with `FAIL=0 FINDINGS=0`
- `./arch/dots-hyprland.sh verify` with no `git` on PATH exits 2
- a scratch fixture with a folded ancestor directory produces exactly one `[FAIL]`
- a scratch fixture whose live file was overwritten through an intact link produces `[PASS]` on the link, `[INFO]` on the content, and exit 0
- `./scripts/phase19-link-aware-verify-assert.sh`, `./scripts/phase17-unblock-assert.sh`, `./scripts/phase18-capture-model-assert.sh` all exit 0
- `grep -ho -- '--verbose=5 --no-folding' arch/*.sh | wc -l` still reports 18
</verification>

<success_criteria>
ROADMAP criterion 1 is met for the repo-side half — link-ness, resolved-target equality, no ancestor directory symlinked into the repo, and no dangling link, checked in that order and failing at the first miss, before any content is read. ROADMAP criterion 4's cp-through case is met. ROADMAP criterion 5's first half is met: a stowed file whose repo content differs from `HEAD` is `[INFO]`.
</success_criteria>

<output>
Create `.planning/phases/19-link-aware-verify/19-02-SUMMARY.md` when done
</output>
