---
phase: 22-kde-and-gtk-capture
plan: 04
type: execute
wave: 4
depends_on: [22-01, 22-02, 22-03]
files_modified:
  - restow/chrome-flags/.config/chrome-flags.conf
  - restow/README.md
  - scripts/phase22-kde-and-gtk-capture-assert.sh
autonomous: true
requirements: [KDE-03]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "restow/chrome-flags/.config/chrome-flags.conf exists as a restow package holding Chrome Wayland, Ozone, and rendering flags"
    - "Live ~/.config/chrome-flags.conf is a symlink resolving into restow/chrome-flags/.config/chrome-flags.conf with matching inode"
    - "restow/README.md generated package table lists chrome-flags tagged cp-through with its exact git checkout recovery command"
    - "Section 4 of the assert script verifies restow/chrome-flags/ packaging, collision map tag, and restow/README.md generated table match"
    - "Section 5 of the assert script runs the live cp-through drill: verifies clean working tree, executes install-files, proves restow/dolphinrc and restow/chrome-flags are modified through their links, and restores clean state via git checkout"
    - "Phase 22 assertion harness (all 6 sections) passes with FAIL=0 FINDINGS=0"
    - "arch/dots-hyprland.sh verify --strict passes across the entire repository with FAIL=0 FINDINGS=0"
    - "Regression safety invariant: scripts/phase17-unblock-assert.sh passes with PAIR_COUNT == 18 intact, proving zero script proliferation (no arch/kde.sh or arch/gtk.sh)"
  artifacts:
    - restow/chrome-flags/.config/chrome-flags.conf
    - restow/README.md
    - scripts/phase22-kde-and-gtk-capture-assert.sh
  key_links:
    - "Live ~/.config/chrome-flags.conf symlinks into restow/chrome-flags/ so Chrome reads personal flags on Wayland"
    - "Upstream install_file (cp -f) writes through the symlink during install-files, and git checkout restores the repo copy per the restow recovery contract"
    - "scripts/phase17-unblock-assert.sh verifies PAIR_COUNT == 18 is preserved (D-25)"
---

<objective>
Adopt `chrome-flags.conf` into `restow/chrome-flags/` following the SAFE-01 protocol, mechanically regenerate Section 3 of `restow/README.md` via `scripts/gen-collision-map.sh --restow-table`, implement Sections 4 and 5 in `scripts/phase22-kde-and-gtk-capture-assert.sh` to rehearse the live `install-files` cp-through drill and recovery, and execute the full Phase 22 assertion suite and strict repository verification gate.

Purpose: Manage colliding desktop flags in `restow/` with empirical proof of write-through overwriting and clean `git checkout` recovery, completing Phase 22.
Output: Tracked `restow/chrome-flags/`, regenerated `restow/README.md`, completed `scripts/phase22-kde-and-gtk-capture-assert.sh`, and green strict verification gate.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-kde-and-gtk-capture/22-CONTEXT.md
@.planning/phases/22-kde-and-gtk-capture/22-RESEARCH.md
@.planning/phases/22-kde-and-gtk-capture/22-PATTERNS.md
@.planning/phases/22-kde-and-gtk-capture/22-VALIDATION.md
@collision-map.tsv
@restow/README.md
@scripts/gen-collision-map.sh
@scripts/phase22-kde-and-gtk-capture-assert.sh
@scripts/phase17-unblock-assert.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package restow/chrome-flags via SAFE-01, regenerate restow/README.md table, and implement Section 4 in assert script</name>
  <files>restow/chrome-flags/.config/chrome-flags.conf, restow/README.md, scripts/phase22-kde-and-gtk-capture-assert.sh</files>
  <read_first>
    - collision-map.tsv (line 63: chrome-flags.conf collision row)
    - scripts/gen-collision-map.sh (lines 350-400: --restow-table generator logic)
    - restow/README.md (lines 40-70: restow package table section)
    - .planning/phases/22-kde-and-gtk-capture/22-CONTEXT.md (D-14, D-15, D-17)
    - .planning/phases/22-kde-and-gtk-capture/22-PATTERNS.md (lines 264-285, 324-343, 373-384: chrome-flags packaging, restow table regeneration)
  </read_first>
  <action>
    Package `chrome-flags.conf` into `restow/` and regenerate documentation (per D-14, D-15, D-17):
    1. Adopt `chrome-flags.conf` following the SAFE-01 protocol:
       - Take timestamped backup:
         `EPOCH="$(date +%s)"`
         `cp -a "$HOME/.config/chrome-flags.conf" "$HOME/.config/chrome-flags.conf.bak.$EPOCH"`
       - Create package directory: `mkdir -p "$REPO_ROOT/restow/chrome-flags/.config"`.
       - Copy live file to repo: `cp -a "$HOME/.config/chrome-flags.conf" "$REPO_ROOT/restow/chrome-flags/.config/chrome-flags.conf"`.
       - Dry run stow: `stow -n --no-folding -d "$REPO_ROOT/restow" -t "$HOME" chrome-flags`.
       - Remove live regular file: `rm -f "$HOME/.config/chrome-flags.conf"`.
       - Link with Stow: `stow --verbose=5 --no-folding -d "$REPO_ROOT/restow" -t "$HOME" chrome-flags`.
       - Assert symlink identity: `test -L "$HOME/.config/chrome-flags.conf"` and `test "$(stat -c %i "$HOME/.config/chrome-flags.conf")" -eq "$(stat -c %i "$REPO_ROOT/restow/chrome-flags/.config/chrome-flags.conf")"`.
    2. Regenerate Section 3 of `restow/README.md` using `scripts/gen-collision-map.sh --restow-table` (D-15):
       - Execute `./scripts/gen-collision-map.sh --restow-table` to produce the updated Markdown table.
       - Replace the generated block in `restow/README.md` between `<!-- BEGIN generated: gen-collision-map.sh --restow-table -->` and `<!-- END generated: gen-collision-map.sh --restow-table -->`.
       - Verify that `chrome-flags` appears tagged `cp-through` with recovery command: `git checkout -- restow/chrome-flags/.config/chrome-flags.conf && cd restow && stow --verbose=5 --no-folding -t ~ chrome-flags`.
       - Verify that `dolphinrc`, `hypr`, and `starship` remain properly tagged and `kdeglobals` does not appear in the table.
    3. Add Section 4 to `scripts/phase22-kde-and-gtk-capture-assert.sh`: "KDE-03: Restow chrome-flags packaging and restow/README.md generated table match" (per D-14, D-15, D-17, D-26):
       - Assert `restow/chrome-flags/.config/chrome-flags.conf` exists in repo.
       - Assert live `~/.config/chrome-flags.conf` is a symlink resolving to `restow/chrome-flags/.config/chrome-flags.conf`.
       - Assert inode identity using `stat -c %i`.
       - Assert `collision-map.tsv` maps `chrome-flags.conf` to `restow` tree and `OVERWRITTEN` repo outcome.
       - Assert Section 3 of `restow/README.md` matches `./scripts/gen-collision-map.sh --restow-table` output exactly.
    4. Emit `[PASS]` for `chrome-flags` packaging, live symlink and inode identity, and `restow/README.md` table match.
  </action>
  <acceptance_criteria>
    - `restow/chrome-flags/.config/chrome-flags.conf` exists in repository.
    - Live `~/.config/chrome-flags.conf` is a symlink resolving into `restow/chrome-flags/`.
    - `restow/README.md` generated block includes `chrome-flags` tagged `cp-through` with exact recovery command.
    - `restow/README.md` generated block does NOT contain `kdeglobals`.
    - Executing `./scripts/phase22-kde-and-gtk-capture-assert.sh --section 4` exits 0 with `FAIL=0 FINDINGS=0`.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase22-kde-and-gtk-capture-assert.sh --section 4</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0</fails_when>
  </verify>
  <done>restow/chrome-flags stowed and linked live, restow/README.md generated table updated, and Section 4 assert passes cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Section 5 in assert script (live cp-through drill: clean preflight, install-files, write-through check, git checkout recovery)</name>
  <files>scripts/phase22-kde-and-gtk-capture-assert.sh</files>
  <read_first>
    - vendor/dots-hyprland/sdata/subcmd-install/3.files.sh (lines 46-52, 93-101: cp_file and install_files loop)
    - scripts/phase22-kde-and-gtk-capture-assert.sh (from Task 1)
    - .planning/phases/22-kde-and-gtk-capture/22-CONTEXT.md (D-16)
    - .planning/phases/22-kde-and-gtk-capture/22-PATTERNS.md (lines 454-473: cp-through live drill pattern)
  </read_first>
  <action>
    Implement Section 5 in `scripts/phase22-kde-and-gtk-capture-assert.sh`: "KDE-03: Live cp-through drill: clean-tree preflight, install-files execution, modified status check, and git checkout recovery" (per D-16):
    1. Preflight check:
       - Run `git status --porcelain` on the repository working tree.
       - Assert working tree is completely clean before executing the installer (if dirty, abort with failure).
    2. Invoke upstream installer files subcommand:
       - Run `./arch/dots-hyprland.sh install-files`.
       - Assert command exits 0.
    3. Verify write-through modification:
       - Check `git status --porcelain` to observe files modified through symlinks:
         - Assert `restow/dolphinrc/.config/dolphinrc` shows modified status `M`.
         - Assert `restow/chrome-flags/.config/chrome-flags.conf` shows modified status `M`.
       - This provides empirical proof that upstream's `install_file` (`cp -f`) follows symlinks and overwrites the repository mirror.
    4. Execute recovery step per documented restow contract:
       - Run `git checkout -- restow/dolphinrc restow/chrome-flags`.
       - Run `git status --porcelain` and assert working tree is clean once again.
    5. Emit `[PASS]` for clean working tree preflight, `[PASS]` for installer execution, `[PASS]` for write-through modification of `dolphinrc` and `chrome-flags`, and `[PASS]` for `git checkout` recovery.
  </action>
  <acceptance_criteria>
    - Executing `./scripts/phase22-kde-and-gtk-capture-assert.sh --section 5` exits 0 with `FAIL=0 FINDINGS=0`.
    - Section 5 output prints `[PASS]` for clean preflight, `[PASS]` for install-files execution, `[PASS]` for detected write-through modification on `restow/dolphinrc` and `restow/chrome-flags`, and `[PASS]` for `git checkout` recovery.
    - Git working tree is completely clean after Section 5 runs.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase22-kde-and-gtk-capture-assert.sh --section 5</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0</fails_when>
  </verify>
  <done>Live cp-through drill verified end-to-end: install-files writes through restow symlinks and git checkout restores clean repository state.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 22 Phase Gate — run full 6-section assert suite and strict repository verification</name>
  <files>scripts/phase22-kde-and-gtk-capture-assert.sh</files>
  <read_first>
    - scripts/phase22-kde-and-gtk-capture-assert.sh (all sections 1-6)
    - arch/dots-hyprland.sh (lines 740-1385: run_verify)
    - scripts/phase17-unblock-assert.sh (lines 85-98: PAIR_COUNT == 18 check)
    - .planning/phases/22-kde-and-gtk-capture/22-CONTEXT.md (D-25, D-26)
    - .planning/phases/22-kde-and-gtk-capture/22-VALIDATION.md
  </read_first>
  <action>
    Execute the Phase 22 phase gate and verify regression protection across previous phases (per D-25, D-26):
    1. Run `./scripts/phase22-kde-and-gtk-capture-assert.sh` with no arguments, executing all 6 sections:
       - Section 1: KDE package layout, inode identity, and 0600 mode documentation check.
       - Section 2: Dolphin/KIO write-through scratch fixture and double-toggle test.
       - Section 3: GTK package layout, unfolded parent directory assertion, and scratch link severance test.
       - Section 4: Restow chrome-flags packaging and restow/README.md generated table match.
       - Section 5: Live cp-through drill: clean-tree preflight, install-files execution, modified status check, and git checkout recovery.
       - Section 6: GUARD list data integrity, Q7/Q8 documentation check, kdeglobals unlinking, and verify GUARD pass.
       - Assert exit code is 0 and output ends with `=== done: FAIL=0 FINDINGS=0 ===`.
    2. Run `./arch/dots-hyprland.sh verify --strict`:
       - Assert exit code is 0 and output ends with `=== done: FAIL=0 FINDINGS=0 ===`.
       - Verify that `--strict` reports zero findings and zero failures across all packages.
    3. Run `./scripts/phase17-unblock-assert.sh`:
       - Assert `PAIR_COUNT == 18` invariant holds (proves no `arch/kde.sh` or `arch/gtk.sh` was created, per D-25).
    4. Run `./scripts/phase18-capture-model-assert.sh`:
       - Assert collision map, tree taxonomy, and redistribution destination checks pass.
    5. Run `./scripts/phase21-ii-bar-config-capture-assert.sh`:
       - Assert Quickshell bar capture assertions pass.
  </action>
  <acceptance_criteria>
    - Executing `./scripts/phase22-kde-and-gtk-capture-assert.sh` exits 0 with `FAIL=0 FINDINGS=0`.
    - Executing `./arch/dots-hyprland.sh verify --strict` exits 0 with `FAIL=0 FINDINGS=0`.
    - Executing `./scripts/phase17-unblock-assert.sh` exits 0 with `PAIR_COUNT == 18`.
    - Executing `./scripts/phase18-capture-model-assert.sh` exits 0.
    - Executing `./scripts/phase21-ii-bar-config-capture-assert.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase22-kde-and-gtk-capture-assert.sh && ./arch/dots-hyprland.sh verify --strict && ./scripts/phase17-unblock-assert.sh</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0, or findings in strict mode</fails_when>
  </verify>
  <done>Phase 22 assertion suite and strict verification pass with zero failures and zero findings; all regression harnesses green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream installer (install-files) → Git working tree | Upstream installer cp_file follows symlinks into restow/ packages, modifying tracked repository files |
| Live session → Recovery command | git checkout recovers personal configurations overwritten by installer write-through |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-13 | Tampering | `restow/chrome-flags/` / `restow/dolphinrc/` | high | mitigate | Package colliding desktop files into restow/ tagged cp-through with documented recovery command `git checkout -- restow/<pkg>` in restow/README.md; rehearse live recovery in Section 5 drill (per D-14, D-15, D-16) |
| T-22-14 | Integrity | Git working tree during drill | high | mitigate | Section 5 asserts clean git status before running install-files, preventing uncommitted user edits from being mixed with installer changes (per D-16) |
| T-22-15 | Repudiation | Historical assert regression | medium | mitigate | Enforce PAIR_COUNT == 18 in scripts/phase17-unblock-assert.sh to prevent script proliferation (no arch/kde.sh or arch/gtk.sh created, per D-25) |
| T-22-SC | Tampering | Script dependencies | low | accept | Relies on existing repo scripts, git, stow, and coreutils |
</threat_model>

<verification>
Automated test harness verification across Sections 4 and 5, followed by strict phase gate:
`./scripts/phase22-kde-and-gtk-capture-assert.sh --section 4`
`./scripts/phase22-kde-and-gtk-capture-assert.sh --section 5`
`./scripts/phase22-kde-and-gtk-capture-assert.sh && ./arch/dots-hyprland.sh verify --strict`
</verification>

<success_criteria>
1. `restow/chrome-flags/.config/chrome-flags.conf` is stowed and linked live.
2. `restow/README.md` Section 3 table is regenerated, listing `chrome-flags` tagged `cp-through`.
3. Live `install-files` drill proves write-through modification and `git checkout` recovery.
4. `./scripts/phase22-kde-and-gtk-capture-assert.sh` passes all 6 sections with `FAIL=0 FINDINGS=0`.
5. `./arch/dots-hyprland.sh verify --strict` passes with `FAIL=0 FINDINGS=0`.
6. `./scripts/phase17-unblock-assert.sh` passes with `PAIR_COUNT == 18`.
</success_criteria>

<output>
Create `.planning/phases/22-kde-and-gtk-capture/22-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces
- Restow package: `restow/chrome-flags/.config/chrome-flags.conf`
- Documentation update: `restow/README.md` Section 3 package table
- Assert harness completion: Sections 4 and 5 in `scripts/phase22-kde-and-gtk-capture-assert.sh`
- Verification sign-off: Green `./arch/dots-hyprland.sh verify --strict`
