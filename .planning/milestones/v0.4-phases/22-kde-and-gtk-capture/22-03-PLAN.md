---
phase: 22-kde-and-gtk-capture
plan: 03
type: execute
wave: 3
depends_on: [22-01, 22-02]
files_modified:
  - guard-paths.tsv
  - docs/archive/kdeglobals
  - docs/archive/README.md
  - docs/config-redistribution.md
  - arch/dots-hyprland.sh
  - scripts/phase22-kde-and-gtk-capture-assert.sh
autonomous: true
requirements: [KDE-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "guard-paths.tsv exists at repository root as a tab-separated data file tracking 7 guarded paths with comment header documenting empirical Q7 and Q8 resolutions"
    - "restow/kdeglobals/.config/kdeglobals is retired to docs/archive/kdeglobals, removing the restow package directory"
    - "docs/archive/README.md contains a registry entry for kdeglobals documenting retirement due to kde-material-you-colors wallpaper churn"
    - "docs/config-redistribution.md row 36 points to docs/archive/kdeglobals, keeping scripts/phase18-capture-model-assert.sh green"
    - "Live ~/.config/kdeglobals is converted from a symlink to a standalone regular file, eliminating git working tree churn on wallpaper changes"
    - "arch/dots-hyprland.sh run_verify enforces that no guarded path is tracked in stow/, restow/, or capture/, and no live path symlinks into the repo"
    - "arch/dots-hyprland.sh classify_sweep_entry emits [INFO] guarded theme output for unmanaged regular files matching guard-paths.tsv instead of unclaimed upstream stub"
    - "Section 6 of the assert script verifies guard-paths.tsv data integrity, Q7/Q8 documentation, kdeglobals archive placement, live unlinking, and verify GUARD enforcement"
  artifacts:
    - guard-paths.tsv
    - docs/archive/kdeglobals
    - docs/archive/README.md
    - docs/config-redistribution.md
    - arch/dots-hyprland.sh
    - scripts/phase22-kde-and-gtk-capture-assert.sh
  key_links:
    - "arch/dots-hyprland.sh verify reads guard-paths.tsv to fail closed if any generated theme output is tracked in repo trees or symlinked live"
    - "docs/config-redistribution.md row 36 update preserves historical provenance while satisfying phase18 assert destination check"
---

<objective>
Check in the GUARD data contract (`guard-paths.tsv`) recording empirical Q7 and Q8 resolutions, retire `restow/kdeglobals` to `docs/archive/kdeglobals`, update historical documentation (`docs/archive/README.md` and row 36 of `docs/config-redistribution.md`), convert live `~/.config/kdeglobals` into a standalone regular file, integrate GUARD validation and sweep classification into `arch/dots-hyprland.sh`, and implement Section 6 in `scripts/phase22-kde-and-gtk-capture-assert.sh`.

Purpose: Provably exclude generated theme files from git tracking and eliminate live repository churn caused by `kde-material-you-colors` on wallpaper changes.
Output: Checked-in `guard-paths.tsv`, archived `docs/archive/kdeglobals`, updated verification engine in `arch/dots-hyprland.sh`, and passing Section 6 in `scripts/phase22-kde-and-gtk-capture-assert.sh`.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-kde-and-gtk-capture/22-CONTEXT.md
@.planning/phases/22-kde-and-gtk-capture/22-RESEARCH.md
@.planning/phases/22-kde-and-gtk-capture/22-PATTERNS.md
@.planning/phases/22-kde-and-gtk-capture/22-VALIDATION.md
@collision-map.tsv
@docs/archive/README.md
@docs/config-redistribution.md
@arch/dots-hyprland.sh
@scripts/phase22-kde-and-gtk-capture-assert.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author guard-paths.tsv, retire restow/kdeglobals to docs/archive/, update documentation, and sever live kdeglobals symlink</name>
  <files>guard-paths.tsv, docs/archive/kdeglobals, docs/archive/README.md, docs/config-redistribution.md</files>
  <read_first>
    - collision-map.tsv (lines 1-25: header format and comments)
    - docs/archive/README.md (lines 19-35: archive registry table)
    - docs/config-redistribution.md (lines 30-40: row 36 kdeglobals entry)
    - .planning/phases/22-kde-and-gtk-capture/22-CONTEXT.md (D-18, D-19, D-20, D-21, D-22)
    - .planning/phases/22-kde-and-gtk-capture/22-RESEARCH.md (lines 514-522: Q7/Q8 empirical findings)
    - .planning/phases/22-kde-and-gtk-capture/22-PATTERNS.md (lines 142-176, 288-305, 386-394: guard-paths TSV pattern, archive pattern, redistribution update)
  </read_first>
  <action>
    Create the GUARD data contract and execute kdeglobals retirement (per D-18, D-19, D-20, D-21, D-22):
    1. Create `guard-paths.tsv` at repository root formatted identically to `collision-map.tsv`:
       - Header comments recording empirical Q7 and Q8 resolutions:
         - Q7: `kde-material-you-colors` is actively invoked by `switchwall.sh` in the quickshell virtualenv, rewriting `kdeglobals` on every wallpaper change. Excluded to permanently stop repo churn.
         - Q8: `~/.config/gtk-4.0/gtk.css` is a symlink to root-owned `/usr/share/themes/catppuccin-mocha-teal-standard+default/gtk-4.0/gtk.css` (0644). User writes cannot overwrite root system files. Both GTK3 and GTK4 `gtk.css` files are guarded out.
       - Tab-separated columns: `path\tcategory\tgenerator\treason`.
       - Exactly 7 tracked data rows (per D-19):
         - `$XDG_CONFIG_HOME/kdeglobals\tgenerated_theme\tkde-material-you-colors\tActive wallpaper churn (Q7)`
         - `$XDG_CONFIG_HOME/Kvantum\tvendor_theme\tdots-hyprland\tUpstream directory sync`
         - `$XDG_CONFIG_HOME/gtk-3.0/gtk.css\tgenerated_theme\tmatugen\tMatugen template output`
         - `$XDG_CONFIG_HOME/gtk-4.0/gtk.css\tgenerated_theme\tmatugen\tRoot-owned theme symlink (Q8)`
         - `$XDG_CONFIG_HOME/fuzzel/fuzzel_theme.ini\tgenerated_theme\tmatugen\tMatugen template output`
         - `$XDG_CONFIG_HOME/hypr/hyprland/colors.lua\tgenerated_theme\tmatugen\tMatugen template output`
         - `$XDG_CONFIG_HOME/hypr/hyprlock/colors.conf\tgenerated_theme\tmatugen\tMatugen template output`
    2. Retire `kdeglobals` from `restow/` to `docs/archive/` (D-20):
       - Ensure `docs/archive/` exists.
       - Move `restow/kdeglobals/.config/kdeglobals` to `docs/archive/kdeglobals` via `git mv` (or copy and remove).
       - Remove directory `restow/kdeglobals/`.
    3. Update `docs/archive/README.md`:
       - Add a new row to the Archive Entry Registry table:
         `| \`kdeglobals\` | Phase 22 (D-20) | Actively churned by \`kde-material-you-colors\` on wallpaper changes (Q7) | Standalone unmanaged live file + \`guard-paths.tsv\` |`
    4. Update `docs/config-redistribution.md`:
       - Update row 36 (`.config/kdeglobals`): change destination column to `docs/archive/kdeglobals`, change disposition to `Archive`, and note retirement in Phase 22 per D-20 (D-20).
       - Run `./scripts/phase18-capture-model-assert.sh` to assert Section 6b destination check passes.
    5. Convert live `~/.config/kdeglobals` to a standalone unmanaged regular file (D-20):
       - If `~/.config/kdeglobals` is a symlink:
         `cp -a "$HOME/.config/kdeglobals" "$HOME/.config/kdeglobals.tmp"`
         `rm -f "$HOME/.config/kdeglobals"`
         `mv -f "$HOME/.config/kdeglobals.tmp" "$HOME/.config/kdeglobals"`
       - Assert `test ! -L "$HOME/.config/kdeglobals" && test -f "$HOME/.config/kdeglobals"`.
  </action>
  <acceptance_criteria>
    - `guard-paths.tsv` exists at repo root and contains all 7 guarded paths.
    - `guard-paths.tsv` comment header contains Q7 and Q8 measurement explanations.
    - `docs/archive/kdeglobals` exists.
    - `restow/kdeglobals/` is completely removed from the repository.
    - `docs/archive/README.md` lists `kdeglobals` in its registry table.
    - `docs/config-redistribution.md` row 36 points to `docs/archive/kdeglobals`.
    - `./scripts/phase18-capture-model-assert.sh` exits 0.
    - Live `~/.config/kdeglobals` is a regular file (`test ! -L "$HOME/.config/kdeglobals" && test -f "$HOME/.config/kdeglobals"`).
  </acceptance_criteria>
  <verify>
    <automated>test -f guard-paths.tsv && test -f docs/archive/kdeglobals && test ! -e restow/kdeglobals && test ! -L "$HOME/.config/kdeglobals" && test -f "$HOME/.config/kdeglobals" && ./scripts/phase18-capture-model-assert.sh</automated>
    <fails_when>non-zero exit code or missing guard-paths.tsv or missing archive file or lingering restow/kdeglobals or live kdeglobals is a symlink</fails_when>
  </verify>
  <done>guard-paths.tsv checked in, kdeglobals retired to docs/archive/ with docs updated and Phase 18 assert green, and live kdeglobals unlinked to regular file.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate GUARD checks into arch/dots-hyprland.sh and implement Section 6 in assert script</name>
  <files>arch/dots-hyprland.sh, scripts/phase22-kde-and-gtk-capture-assert.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (lines 1090-1320: run_verify, managed_roots, classify_sweep_entry)
    - scripts/phase22-kde-and-gtk-capture-assert.sh (from Plan 22-02)
    - .planning/phases/22-kde-and-gtk-capture/22-CONTEXT.md (D-23, D-24, D-26)
    - .planning/phases/22-kde-and-gtk-capture/22-PATTERNS.md (lines 312-369: verify GUARD integration, sweep classifier)
  </read_first>
  <action>
    Integrate GUARD checks into `arch/dots-hyprland.sh` and gate them in `scripts/phase22-kde-and-gtk-capture-assert.sh` (per D-23, D-24, D-26):
    1. Update `arch/dots-hyprland.sh` `run_verify()`:
       - Declare associative array `local -A guarded_entries=()` before tree walks.
       - Read `guard-paths.tsv` from `$main_root/guard-paths.tsv` (D-23):
         - Parse line-by-line using `IFS=$'\t' read -r g_path g_cat g_gen g_reason`, skipping comments (`#*`) and blank lines.
         - Expand `$XDG_CONFIG_HOME` to `$HOME/.config` and `$HOME` to `$HOME`.
         - Populate `guarded_entries["$expanded"]=1`.
         - Check repository trees: for each `tree` in `stow`, `restow`, `capture`, assert that no package contains the relative path `${expanded#"$HOME"/}`. If found, call `fail "guard path tracked in $tree: $pkg_dir/$rel_sub"`.
         - Check live filesystem: if `test -L "$expanded"`, resolve real path `live_target="$(readlink -f -- "$expanded" 2>/dev/null || true)"`. If `"$live_target" == "$main_root_real"/*`, call `fail "guard path live counterpart symlinks into repo: $expanded -> $live_target"`.
         - If neither violation occurs, call `pass "guard path excluded: $g_path"` (respecting quiet flag).
    2. Update `arch/dots-hyprland.sh` `classify_sweep_entry()`:
       - Before Arm 7 ("unclaimed upstream stub"), add guarded entry check (D-24):
         `if [[ -n "${guarded_entries[$entry]:-}" ]]; then info "guarded theme output: $entry"; return 0; fi`
       - This prevents regular files generated by matugen (such as `~/.config/gtk-3.0/gtk.css`) from being reported as unclaimed stubs in managed roots.
    3. Add Section 6 to `scripts/phase22-kde-and-gtk-capture-assert.sh`: "KDE-02: GUARD list data integrity, Q7/Q8 documentation check, kdeglobals unlinking, and verify --strict pass" (per D-26):
       - Assert `guard-paths.tsv` exists, is non-empty, and contains all 7 expected path identifiers.
       - Assert Q7 and Q8 empirical resolutions are present in comments in `guard-paths.tsv`.
       - Assert `docs/archive/kdeglobals` exists and `restow/kdeglobals` is absent.
       - Assert live `~/.config/kdeglobals` is a regular file and not a symlink.
       - Scratch fixture test: create a scratch repository containing a mock package with a forbidden GUARD file (e.g. `stow/badpkg/.config/kdeglobals`), run the verification logic targeting that scratch tree, and assert it exits non-zero with `guard path tracked in stow`.
       - Run `./arch/dots-hyprland.sh verify` against the real repository and assert it passes with 0 failures.
  </action>
  <acceptance_criteria>
    - `bash -n arch/dots-hyprland.sh` passes with 0 syntax errors.
    - Executing `./scripts/phase22-kde-and-gtk-capture-assert.sh --section 6` exits 0 with `FAIL=0 FINDINGS=0`.
    - Section 6 output prints `[PASS]` for `guard-paths.tsv` schema, Q7/Q8 comments, `kdeglobals` archive placement, live unlinking, scratch guard violation failure, and clean verify pass.
    - `./arch/dots-hyprland.sh verify` passes with all 7 GUARD paths confirmed excluded.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase22-kde-and-gtk-capture-assert.sh --section 6</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0</fails_when>
  </verify>
  <done>Verification engine augmented with GUARD validation and sweep classifier refinement; Section 6 passing cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theming pipeline (switchwall.sh / matugen) → Git working tree | Dynamic color themes generated at runtime must never contaminate or dirty the repository working tree |
| Live filesystem → Verification engine | run_verify validates live filesystem paths against guard-paths.tsv to prevent rogue symlinks into repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-09 | Tampering | `guard-paths.tsv` / `run_verify` | high | mitigate | Fail closed in run_verify if any of the 7 GUARD paths are tracked in stow/, restow/, or capture/, or if a live counterpart symlinks into the repo (per D-18, D-23) |
| T-22-10 | Information Disclosure | Git working tree | medium | mitigate | Retire kdeglobals from restow/ to docs/archive/ and unlink live symlink to eliminate perpetual git status noise and accidental commit of machine state (per D-20) |
| T-22-11 | Elevation of Privilege | Live GTK4 theme | medium | mitigate | Document that ~/.config/gtk-4.0/gtk.css points to root-owned theme (0644); guard it from repo to prevent unprivileged user scripts from attempting system overwrites (per D-22) |
| T-22-12 | Tampering | Assert harness (`phase22-kde-and-gtk-capture-assert.sh`) | high | mitigate | Confine scratch guard violation tests to `mktemp -d /tmp/p22-assert-s6-XXXXXX` registered with EXIT trap |
| T-22-SC | Tampering | Script dependencies | low | accept | Relies on internal bash logic and coreutils |
</threat_model>

<verification>
Automated test harness verification for Section 6:
`./scripts/phase22-kde-and-gtk-capture-assert.sh --section 6`
`./arch/dots-hyprland.sh verify`
</verification>

<success_criteria>
1. `guard-paths.tsv` exists with 7 paths and Q7/Q8 documentation comments.
2. `kdeglobals` retired to `docs/archive/kdeglobals` and live file converted to regular file.
3. `arch/dots-hyprland.sh verify` enforces GUARD exclusions and classifies guarded sweep entries.
4. Section 6 of `scripts/phase22-kde-and-gtk-capture-assert.sh` passes with 0 failures and 0 findings.
</success_criteria>

<output>
Create `.planning/phases/22-kde-and-gtk-capture/22-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces
- Data file: `guard-paths.tsv` at repository root
- Archive file: `docs/archive/kdeglobals`
- Documentation updates: `docs/archive/README.md` and `docs/config-redistribution.md`
- Verification enhancements: GUARD validation and sweep classifier refinement in `arch/dots-hyprland.sh`
- Assert harness: Section 6 added to `scripts/phase22-kde-and-gtk-capture-assert.sh`
