---
phase: 22-kde-and-gtk-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase22-kde-and-gtk-capture-assert.sh
  - stow/kde/.config/kiorc
  - stow/kde/.config/ktrashrc
  - stow/kde/.config/kservicemenurc
autonomous: true
requirements: [KDE-01]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A dedicated assert harness scripts/phase22-kde-and-gtk-capture-assert.sh exists, is executable, and adheres to the dotfiles assert harness contract (set -euo pipefail, four prefixes [PASS]/[FAIL]/[FINDING]/[INFO], counters FAIL/FINDINGS, trap cleanup, --section parsing)"
    - "stow/kde/.config/{kiorc,ktrashrc,kservicemenurc} exists as a clean GNU Stow package holding non-colliding Dolphin and KIO configurations"
    - "Live ~/.config/{kiorc,ktrashrc,kservicemenurc} are symlinks resolving into stow/kde/.config/ with identical inodes"
    - "File mode 100644 in git is documented as harmless with no bootstrap chmod required because git ignores chmod between 0644 and 0600 and KConfig setPermissions enforces 0600 automatically on write"
    - "KConfig symlink write-through is verified: kwriteconfig6 writes through symlinks in scratch XDG and live kiorc double-toggle modifies repo file through the link without severance"
    - "Section 1 of the assert script verifies KDE package layout, live symlink identity, 0600 mode documentation, and path portability"
    - "Section 2 of the assert script proves KConfig write-through in an isolated scratch XDG fixture and executes the live kiorc double-toggle drill"
  artifacts:
    - scripts/phase22-kde-and-gtk-capture-assert.sh
    - stow/kde/.config/kiorc
    - stow/kde/.config/ktrashrc
    - stow/kde/.config/kservicemenurc
  key_links:
    - "Live ~/.config/{kiorc,ktrashrc,kservicemenurc} resolve into stow/kde/.config/ via stow --verbose=5 --no-folding"
    - "KConfig kwriteconfig6 updates write directly through the symlink to stow/kde/.config/kiorc, preserving inode and link identity"
---

<objective>
Scaffold the Phase 22 assertion harness (`scripts/phase22-kde-and-gtk-capture-assert.sh`) with Section 1 and Section 2, adopt Dolphin/KIO configurations (`kiorc`, `ktrashrc`, `kservicemenurc`) into `stow/kde/` following the SAFE-01 adoption protocol, resolve the file mode 0600 question (Q11), and verify KConfig write-through semantics (Q10) via scratch and live double-toggle drills.

Purpose: Manage non-colliding KDE and Dolphin configurations declaratively in GNU Stow while proving that KConfig writes through symlinks without link destruction.
Output: Scaffolding script `scripts/phase22-kde-and-gtk-capture-assert.sh` with passing Sections 1 and 2, and new package `stow/kde/` linked live.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-kde-and-gtk-capture/22-CONTEXT.md
@.planning/phases/22-kde-and-gtk-capture/22-RESEARCH.md
@.planning/phases/22-kde-and-gtk-capture/22-PATTERNS.md
@.planning/phases/22-kde-and-gtk-capture/22-VALIDATION.md
@scripts/phase21-ii-bar-config-capture-assert.sh
@stow/qbittorrent/.config/qBittorrent/qBittorrent.conf
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end tracer — assert harness scaffolding, live KDE adoption via SAFE-01 protocol, and 0600 file mode verification (Section 1)</name>
  <files>scripts/phase22-kde-and-gtk-capture-assert.sh, stow/kde/.config/kiorc, stow/kde/.config/ktrashrc, stow/kde/.config/kservicemenurc</files>
  <read_first>
    - scripts/phase21-ii-bar-config-capture-assert.sh (lines 1-77: boilerplate, trap cleanup, CLI parsing)
    - stow/qbittorrent/.config/qBittorrent/qBittorrent.conf (lines 1-10: INI format reference)
    - .planning/phases/22-kde-and-gtk-capture/22-CONTEXT.md (D-01, D-02, D-03, D-05, D-06, D-07, D-25, D-26)
    - .planning/phases/22-kde-and-gtk-capture/22-RESEARCH.md (lines 95-96, 248-268, 523-526: KConfig write-through, mode 0600 vs 0644)
    - .planning/phases/22-kde-and-gtk-capture/22-PATTERNS.md (lines 38-77, 178-231, 411-440: assert harness pattern, KDE package pattern, SAFE-01 protocol)
  </read_first>
  <action>
    Scaffold `scripts/phase22-kde-and-gtk-capture-assert.sh` with permissions 0755 adhering to the dotfiles assert harness contract (per D-26):
    1. Shebang `#!/usr/bin/env bash`, purpose header citing KDE-01, KDE-02, KDE-03, `set -euo pipefail`.
    2. Resolve `REPO_ROOT` as `$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)` and `cd "$REPO_ROOT"`.
    3. Initialize counters `FAIL=0` and `FINDINGS=0`, and define standard helpers: `pass()`, `fail()`, `finding()`, `info()`.
    4. Set up dynamic cleanup arrays `TMP_FILES=()` and `SCRATCH_ROOTS=()`. Implement `cleanup()` function that iterates over `TMP_FILES` removing them, and iterates over `SCRATCH_ROOTS` applying `chmod -R u+rwX` before `rm -rf`, registered via `trap cleanup EXIT`.
    5. Implement CLI argument parsing supporting `--section <N>` (where N is 1 to 6) to run a specific section, or running all sections if omitted. Add `-h|--help` usage display.
    6. Implement Section 1: "KDE-01: KDE package layout, inode identity, and 0600 mode documentation check" (per D-01, D-02, D-03, D-05, D-06, D-07):
       - Assert `stow/kde/.config/kiorc`, `stow/kde/.config/ktrashrc`, and `stow/kde/.config/kservicemenurc` exist in repo.
       - Assert live counterparts `~/.config/{kiorc,ktrashrc,kservicemenurc}` are symlinks resolving to their respective files in `stow/kde/.config/`.
       - Assert inode identity for all 3 files using `stat -c %i`.
       - Assert file mode resolution documentation (Q11, D-03): verify that files in `stow/kde/.config/` carry standard permissions, document that git tracks only executable bits (100644), and verify that KConfig's `setPermissions` sets 0600 on write without git dirty status.
       - Assert path portability in `ktrashrc`: verify `[/home/pera/.local/share/Trash]` section header is present as-is (per D-07).
       - Assert scope boundary: verify no unmanaged KDE desktop cache files (`baloofileinformationrc`, `kwalletrc`, `kconf_updaterc`, `darklyrc`, `konsolerc`) are tracked in `stow/kde/` (per D-06).
    7. Add terminal summary block printing `=== done: FAIL=${FAIL} FINDINGS=${FINDINGS} ===` and exiting with code 1 if `FAIL > 0`, else code 0.

    Adopt live KDE configuration files into `stow/kde/` following the SAFE-01 protocol (per D-01, D-02, D-05):
    1. Preflight check: assert no running Dolphin GUI instances exist (`if pgrep -x dolphin >/dev/null 2>&1; then echo "Error: Dolphin is running" >&2; exit 1; fi`).
    2. Take timestamped backup of live files:
       `EPOCH="$(date +%s)"`
       `for f in kiorc ktrashrc kservicemenurc; do cp -a "$HOME/.config/$f" "$HOME/.config/$f.bak.$EPOCH"; done`
    3. Create repo package directory: `mkdir -p "$REPO_ROOT/stow/kde/.config"`.
    4. Copy live content to repo: `for f in kiorc ktrashrc kservicemenurc; do cp -a "$HOME/.config/$f" "$REPO_ROOT/stow/kde/.config/$f"; done`.
    5. Perform dry-run stow: `stow -n --no-folding -d "$REPO_ROOT/stow" -t "$HOME" kde`.
    6. Remove live regular files and link:
       `for f in kiorc ktrashrc kservicemenurc; do rm "$HOME/.config/$f"; done`
       `stow --verbose=5 --no-folding -d "$REPO_ROOT/stow" -t "$HOME" kde`
    7. Assert inode identity: `test "$(stat -c %i "$HOME/.config/kiorc")" -eq "$(stat -c %i "$REPO_ROOT/stow/kde/.config/kiorc")"`.
  </action>
  <acceptance_criteria>
    - `scripts/phase22-kde-and-gtk-capture-assert.sh` exists and is executable (`test -x scripts/phase22-kde-and-gtk-capture-assert.sh`).
    - `bash -n scripts/phase22-kde-and-gtk-capture-assert.sh` passes with 0 syntax errors.
    - `stow/kde/.config/kiorc`, `stow/kde/.config/ktrashrc`, and `stow/kde/.config/kservicemenurc` exist in repository.
    - `test -L "$HOME/.config/kiorc"` and `test -L "$HOME/.config/ktrashrc"` and `test -L "$HOME/.config/kservicemenurc"` evaluate true.
    - Executing `./scripts/phase22-kde-and-gtk-capture-assert.sh --section 1` exits 0 with `FAIL=0 FINDINGS=0`.
    - Section 1 output prints `[PASS]` for repo package presence, live symlink targets, inode identity, 0600 mode resolution, and trash path portability.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase22-kde-and-gtk-capture-assert.sh --section 1</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0</fails_when>
  </verify>
  <done>End-to-end tracer slice complete: assert harness scaffolded, kiorc, ktrashrc, and kservicemenurc stowed with SAFE-01, inodes matching, and Section 1 green.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Section 2 in scripts/phase22-kde-and-gtk-capture-assert.sh (scratch XDG kwriteconfig6 drill and live kiorc double-toggle test)</name>
  <files>scripts/phase22-kde-and-gtk-capture-assert.sh</files>
  <read_first>
    - scripts/phase22-kde-and-gtk-capture-assert.sh (from Task 1)
    - .planning/phases/22-kde-and-gtk-capture/22-CONTEXT.md (D-04)
    - .planning/phases/22-kde-and-gtk-capture/22-RESEARCH.md (lines 95-96, 520-522: Q10 resolution, kwriteconfig6)
    - .planning/phases/22-kde-and-gtk-capture/22-PATTERNS.md (lines 106-116: scratch fixture isolation)
  </read_first>
  <action>
    Add Section 2 to `scripts/phase22-kde-and-gtk-capture-assert.sh`: "KDE-01: Dolphin/KIO write-through scratch fixture and double-toggle test" (per D-04):
    1. Implement Stage 1 (Scratch fixture drill proving KConfig writes through symlinks without link destruction):
       - Create scratch fixture root `S2_ROOT="$(mktemp -d /tmp/p22-assert-s2-XXXXXX)"` and append to `SCRATCH_ROOTS`.
       - Create mock repository file `$S2_ROOT/repo/testkiorc` with initial content `[Confirmations]\nConfirmTrash=false\n`.
       - Create mock live target `$S2_ROOT/home/.config/testkiorc` as a symbolic link pointing to `$S2_ROOT/repo/testkiorc`.
       - Assert `test -L "$S2_ROOT/home/.config/testkiorc"` is true.
       - Execute `kwriteconfig6` targeting the scratch environment:
         `XDG_CONFIG_HOME="$S2_ROOT/home/.config" kwriteconfig6 --file testkiorc --group Confirmations --key ConfirmTrash --type bool true`
       - Assert `test -L "$S2_ROOT/home/.config/testkiorc"` remains TRUE (symlink was not destroyed or replaced by a plain file).
       - Assert repository file `$S2_ROOT/repo/testkiorc` updated to `ConfirmTrash=true`.
       - Assert inode identity of `$S2_ROOT/home/.config/testkiorc` and `$S2_ROOT/repo/testkiorc` remains identical.
    2. Implement Stage 2 (Live double-toggle test on live `kiorc` per D-04):
       - Assert git status on `stow/kde/.config/kiorc` is clean before starting.
       - Read existing value of `ConfirmTrash`: `ORIG_VAL="$(kreadconfig6 --file kiorc --group Confirmations --key ConfirmTrash)"`.
       - Determine toggled boolean value: if `"$ORIG_VAL"` is `"true"`, toggle to `"false"`; else toggle to `"true"`.
       - Execute `kwriteconfig6 --file kiorc --group Confirmations --key ConfirmTrash --type bool "$TOGGLE_VAL"`.
       - Assert `test -L "$HOME/.config/kiorc"` remains TRUE.
       - Assert `git diff --name-only stow/kde/.config/kiorc` contains `stow/kde/.config/kiorc`.
       - Toggle back: `kwriteconfig6 --file kiorc --group Confirmations --key ConfirmTrash --type bool "$ORIG_VAL"`.
       - Revert git modification: `git checkout -- stow/kde/.config/kiorc`.
       - Assert `git status --porcelain stow/kde/.config/kiorc` is empty.
    3. Emit `[PASS]` for scratch symlink preservation, `[PASS]` for scratch repo file update, `[PASS]` for live symlink preservation during write, and `[PASS]` for live double-toggle reversion.
  </action>
  <acceptance_criteria>
    - Executing `./scripts/phase22-kde-and-gtk-capture-assert.sh --section 2` exits 0 with `FAIL=0 FINDINGS=0`.
    - Section 2 output prints `[PASS]` for scratch KConfig write-through symlink preservation, `[PASS]` for scratch repo update, and `[PASS]` for live kiorc double-toggle and clean reversion.
    - Live `~/.config/kiorc` remains a valid symlink to `stow/kde/.config/kiorc` after the test.
    - Git status on `stow/kde/.config/kiorc` is clean after the test.
    - Scratch directories are wiped on exit.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase22-kde-and-gtk-capture-assert.sh --section 2</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0</fails_when>
  </verify>
  <done>Section 2 write-through verification proves that KConfig writes through symlinks without link destruction in both scratch and live environments.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live user configuration → Git working tree (`stow/kde/`) | Live desktop configurations read from `$HOME/.config/` are committed into the repository |
| Test fixture → Host filesystem | Assert test fixtures must remain completely quarantined within `/tmp/` and must never delete or corrupt real user files in `$HOME` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-01 | Tampering | `stow/kde/.config/` | high | mitigate | Adopt live configurations using SAFE-01 protocol (`.bak.<epoch>` backups, `stow -n --no-folding` dry run, atomic commit) to prevent configuration loss (per D-02) |
| T-22-02 | Denial of Service | Live Dolphin process | medium | mitigate | Check `pgrep -x dolphin` and require closing Dolphin GUI before stowing or altering links to prevent in-memory cache overwrites on exit (per D-05) |
| T-22-03 | Information Disclosure | `stow/kde/.config/` | low | mitigate | Audit `kiorc`, `ktrashrc`, and `kservicemenurc` ensuring no credentials, auth tokens, or private secrets exist; verify files contain only UI flags and thresholds (per D-03) |
| T-22-04 | Tampering | Assert harness (`phase22-kde-and-gtk-capture-assert.sh`) | high | mitigate | Confine fixture operations strictly to `mktemp -d /tmp/p22-assert-s2-XXXXXX` and register with EXIT trap using `chmod -R u+rwX` before `rm -rf` |
| T-22-SC | Tampering | Script dependencies | low | accept | Relies exclusively on existing local system binaries (`stow`, `kwriteconfig6`, `kreadconfig6`, `bash`, `git`, `stat`) |
</threat_model>

<verification>
Automated test harness verification across Sections 1 and 2:
`./scripts/phase22-kde-and-gtk-capture-assert.sh --section 1`
`./scripts/phase22-kde-and-gtk-capture-assert.sh --section 2`
</verification>

<success_criteria>
1. `scripts/phase22-kde-and-gtk-capture-assert.sh` exists with trap cleanup and CLI section parsing.
2. `stow/kde/.config/{kiorc,ktrashrc,kservicemenurc}` is stowed to `~/.config/` with identical inodes.
3. KConfig write-through semantics verified through scratch fixture and live double-toggle drill.
4. Sections 1 and 2 of the assert script pass with 0 failures and 0 findings.
</success_criteria>

<output>
Create `.planning/phases/22-kde-and-gtk-capture/22-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces
- New file paths: `scripts/phase22-kde-and-gtk-capture-assert.sh` (Sections 1 and 2)
- Stow package: `stow/kde/.config/kiorc`
- Stow package: `stow/kde/.config/ktrashrc`
- Stow package: `stow/kde/.config/kservicemenurc`
