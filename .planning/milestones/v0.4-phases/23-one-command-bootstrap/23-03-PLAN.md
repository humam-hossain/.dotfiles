---
phase: 23-one-command-bootstrap
plan: 03
type: execute
wave: 3
depends_on: [23-02]
files_modified:
  - bootstrap.sh
  - scripts/phase23-bootstrap-assert.sh
  - arch/pkglist-native.txt
  - arch/pkglist-aur.txt
autonomous: true
requirements: [BOOT-03, BOOT-04, BOOT-05]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Snapshots sorted alphabetically with sort -u; regenerated only on --snapshot (BOOT-05, D-20, D-21)"
    - "arch/pkglist-native.txt and arch/pkglist-aur.txt exist, committed as repository data with metadata headers (Hostname, Timestamp, Kernel, Pacman, Count)"
    - "Standard bootstrap invocations (--dry-run or live) never modify tracked snapshot files, guaranteeing zero working-tree drift (D-21)"
    - "Stage 1 pauses with relogin banner; Stage 2 checks graphical session (BOOT-03, D-08, D-09, D-10)"
    - "Stage 1 saves state marking stage 2 pending, renders eye-catching bordered instruction box with logout/login steps, and exits 0 (D-08, D-09), skipping pause when --no-pause is passed"
    - "Stage 2 probes HYPRLAND_INSTANCE_SIGNATURE and configProvider == 'lua' via hyprctl -j status, warning if run in a non-graphical TTY (D-10)"
    - "Stage 2 executes systemctl --user daemon-reload and systemctl --user --now enable dotfiles-capture.timer, asserting timer is active (D-11)"
    - "Bootstrap exit code is strictly verify --strict exit code (BOOT-04, D-23)"
    - "PAIR_COUNT == 18 invariant in arch/*.sh is strictly preserved (scripts/phase17-unblock-assert.sh)"
    - "Section 4 and Section 5 in scripts/phase23-bootstrap-assert.sh pass cleanly and the full test suite passes with zero failures"
  artifacts:
    - bootstrap.sh
    - scripts/phase23-bootstrap-assert.sh
    - arch/pkglist-native.txt
    - arch/pkglist-aur.txt
  key_links:
    - "./bootstrap.sh --snapshot regenerates arch/pkglist-native.txt and arch/pkglist-aur.txt deterministically without manual sorting"
    - "Stage 1 pauses before verify to allow compositor reload into hyprland.lua, and Stage 2 finishes with strict verify"
    - "Bootstrap exit code is bound 1-to-1 to arch/dots-hyprland.sh verify --strict"
  prohibitions:
    - "Must not modify package snapshots during standard bootstrap (must require --snapshot flag) (D-21)"
    - "Must not create new scripts under arch/ (must preserve PAIR_COUNT == 18)"
    - "Must not exit 0 if arch/dots-hyprland.sh verify --strict returns non-zero (BOOT-04)"
    - "Must not fire systemd user timers in Stage 1 before graphical session initialization (D-11)"
    - "Must not call hostname binary which is absent on Arch core (use uname -n or /etc/hostname) (Pitfall 4)"
---

<objective>
Implement package snapshot generation (`--snapshot`) committing `arch/pkglist-native.txt` and `arch/pkglist-aur.txt` with formatted metadata headers and deterministic sorting (`sort -u`), build the two-stage execution boundary across compositor relogin with the operator instruction banner and runtime session probe, wire the post-relogin systemd capture timer activation, enforce the strict verification exit code gate (`verify --strict`), and complete Sections 4 and 5 in `scripts/phase23-bootstrap-assert.sh`.

Purpose: Guarantee that package baselines are captured as committed data without working-tree drift, the operator is guided smoothly across the session relogin hop, and the bootstrap command exits 0 if and only if strict repository verification passes.
Output: Committed package snapshots `arch/pkglist-*.txt`, complete `./bootstrap.sh` pipeline, and passing Sections 4 and 5 in `scripts/phase23-bootstrap-assert.sh`.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-one-command-bootstrap/23-CONTEXT.md
@.planning/phases/23-one-command-bootstrap/23-RESEARCH.md
@.planning/phases/23-one-command-bootstrap/23-PATTERNS.md
@.planning/phases/23-one-command-bootstrap/23-VALIDATION.md
@arch/dots-hyprland.sh
@bootstrap.sh
@scripts/phase23-bootstrap-assert.sh
@scripts/phase17-unblock-assert.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end tracer — package snapshot data generation (--snapshot), metadata header formatting, LC_ALL=C sort -u, and Section 4 assert</name>
  <files>bootstrap.sh, arch/pkglist-native.txt, arch/pkglist-aur.txt, scripts/phase23-bootstrap-assert.sh</files>
  <read_first>
    - .planning/phases/23-one-command-bootstrap/23-CONTEXT.md (D-20, D-21)
    - .planning/phases/23-one-command-bootstrap/23-RESEARCH.md (lines 278-285, 462-500, 533)
    - .planning/phases/23-one-command-bootstrap/23-PATTERNS.md (lines 504-541, 697-739, 846-883)
    - bootstrap.sh (from Plan 23-02)
  </read_first>
  <action>
    1. Implement `generate_package_snapshots()` in `./bootstrap.sh` (per D-20, D-21, and Pattern 1.7):
       - Determine hostname without relying on the `hostname` binary (which is absent on Arch core per Pitfall 4):
         `local host; host="$(uname -n 2>/dev/null || cat /etc/hostname 2>/dev/null || echo "${HOSTNAME:-arch}")"`.
       - Determine current UTC timestamp: `local ts; ts="$(date -u +"%Y-%m-%dT%H:%M:%SZ")"`.
       - Determine kernel release: `local kernel; kernel="$(uname -r)"`.
       - Determine pacman version string:
         `local pac_ver; pac_ver="$(pacman -V 2>/dev/null | grep -o 'Pacman v[0-9.]* - libalpm v[0-9.]*' || echo "unknown")"`.
       - Generate `arch/pkglist-native.txt` atomically via `$nat_file.tmp.$$` and `mv`:
         Write comments header containing `# arch/pkglist-native.txt — explicitly installed native packages`, `# Hostname: <host>`, `# Timestamp: <ts>`, `# Kernel: <kernel>`, `# Pacman: <pac_ver>`, and `# Count: <count>` (per D-20).
         Follow header with package entries from `pacman -Qqen | LC_ALL=C sort -u`.
       - Generate `arch/pkglist-aur.txt` atomically via `$aur_file.tmp.$$` and `mv`:
         Write comments header containing `# arch/pkglist-aur.txt — explicitly installed foreign/AUR packages`, `# Hostname: <host>`, `# Timestamp: <ts>`, `# Kernel: <kernel>`, `# Pacman: <pac_ver>`, and `# Count: <count>` (per D-20).
         Follow header with package entries from `pacman -Qqem | LC_ALL=C sort -u`.
       - Support `--snapshot` flag in main dispatch: if `SNAPSHOT_MODE=1`, execute `generate_package_snapshots` and exit 0 immediately without executing bootstrap steps (per D-21).

    2. Execute `./bootstrap.sh --snapshot` to generate the initial tracked snapshot data files (`arch/pkglist-native.txt` and `arch/pkglist-aur.txt`).

    3. Implement Section 4 in `scripts/phase23-bootstrap-assert.sh` ("Package snapshot validation and zero git drift" per Pattern 3.1):
       - Check `arch/pkglist-native.txt` and `arch/pkglist-aur.txt` exist.
       - Assert all 5 metadata headers (`# Hostname:`, `# Timestamp:`, `# Kernel:`, `# Pacman:`, `# Count:`) are present in each file.
       - Assert package lists are sorted deterministically via `LC_ALL=C sort -C`.
       - Test zero git drift (D-21): take `git status --porcelain`, run `./bootstrap.sh --dry-run >/dev/null 2>&1`, and assert `git status --porcelain` is identical (standard bootstrap never modifies snapshots).
  </action>
  <acceptance_criteria>
    - `arch/pkglist-native.txt` and `arch/pkglist-aur.txt` exist and contain correct comment headers.
    - Package entries in both snapshot files are strictly sorted alphabetically via `LC_ALL=C sort -C`.
    - `./bootstrap.sh --snapshot` regenerates snapshots idempotently.
    - Normal `./bootstrap.sh --dry-run` invocation causes zero git working-tree drift.
    - Executing `./scripts/phase23-bootstrap-assert.sh --section 4` exits 0 with `FAIL=0 FINDINGS=0`.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase23-bootstrap-assert.sh --section 4</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0</fails_when>
  </verify>
  <done>Package snapshots generated via --snapshot with metadata headers and LC_ALL=C sort -u, and Section 4 assert passes cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Stage 1 relogin pause banner, Stage 2 session runtime probe, and operator guidance</name>
  <files>bootstrap.sh, scripts/phase23-bootstrap-assert.sh</files>
  <read_first>
    - .planning/phases/23-one-command-bootstrap/23-CONTEXT.md (D-08, D-09, D-10)
    - .planning/phases/23-one-command-bootstrap/23-RESEARCH.md (lines 431-460)
    - .planning/phases/23-one-command-bootstrap/23-PATTERNS.md (lines 432-460)
    - bootstrap.sh (from Task 1)
  </read_first>
  <action>
    1. Implement Stage 1 completion banner `show_relogin_banner()` in `./bootstrap.sh` (per D-08, D-09, and Pattern 1.8):
       - Check if output is a terminal (`[[ -t 1 ]]`); if so, apply bold, green, and cyan ANSI formatting; else strip colors.
       - Render formatted bordered instruction box per D-09 detailing: title `BOOTSTRAP: STAGE 1 COMPLETE`, notification that all dotfiles, overlays, and session configs are placed on disk and session entry point has changed to upstream `hyprland.lua`, and relogin instructions listing step 1 to exit current session via `hyprctl dispatch exit`, step 2 to log back in via display manager / SDDM, and step 3 to complete bootstrap by running `./bootstrap.sh`.
       - Record stage 2 in persistent state: `set_stage 2`.
       - If `--no-pause` is passed, skip exiting and proceed directly to Stage 2 (for automated/headless testing).
       - If `--no-pause` is NOT passed, exit 0 to allow the operator to perform the relogin cleanly.

    2. Implement Stage 2 runtime session probe `probe_session_environment()` in `./bootstrap.sh` (per D-10 and Pattern 1.8):
       - On resumption of `./bootstrap.sh`, if `get_stage == 2`:
         Check `HYPRLAND_INSTANCE_SIGNATURE`: if empty, emit warning: `"[WARN] HYPRLAND_INSTANCE_SIGNATURE is not set. Graphical session may not be active."`.
         Check `hyprctl`: if installed, query `hyprctl -j status | jq -r '.configProvider // "unknown"'`.
         If `configProvider != "lua"`, emit warning: `"[WARN] Active configProvider is '$provider' (expected 'lua'). Did you relogin?"`.
         Warn operator if running in a bare TTY without a graphical session before proceeding to Step 7.

    3. Update Section 1 in `scripts/phase23-bootstrap-assert.sh` to assert relogin banner and stage transitions:
       - Verify that `--no-pause` bypasses Stage 1 exit and completes both stages.
       - Verify that without `--no-pause`, Stage 1 terminates with exit code 0 and sets `.stage == 2` in the state file.
  </action>
  <acceptance_criteria>
    - Stage 1 terminates with exit code 0 after Step 6 (`capture_seed`) when `--no-pause` is not provided.
    - Relogin banner formats cleanly with box characters and clear instructions (`hyprctl dispatch exit`).
    - Persistent state file records `.stage = 2`.
    - Resuming `./bootstrap.sh` probes `HYPRLAND_INSTANCE_SIGNATURE` and `configProvider`.
    - `--no-pause` successfully bypasses the interactive pause for headless test runs.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase23-bootstrap-assert.sh --section 1</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0</fails_when>
  </verify>
  <done>Two-stage execution boundary implemented: Stage 1 renders eye-catching relogin banner and exits 0; Stage 2 probes runtime session on resumption; --no-pause verified.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Step 7 verification gate, systemd timer activation, Section 5 assert suite, and phase sign-off</name>
  <files>bootstrap.sh, scripts/phase23-bootstrap-assert.sh</files>
  <read_first>
    - .planning/phases/23-one-command-bootstrap/23-CONTEXT.md (D-11, D-22, D-23)
    - .planning/phases/23-one-command-bootstrap/23-RESEARCH.md (lines 462-503, 534)
    - .planning/phases/23-one-command-bootstrap/23-PATTERNS.md (lines 462-503, 742-775)
    - scripts/phase17-unblock-assert.sh (lines 80-110: PAIR_COUNT == 18)
    - bootstrap.sh (from Task 2)
  </read_first>
  <action>
    1. Implement Step 7 `run_stage2_verification()` in `./bootstrap.sh` (per D-11, D-23, BOOT-04):
       - If `--dry-run`:
         Print `"[DRY-RUN] systemctl --user daemon-reload"`.
         Print `"[DRY-RUN] systemctl --user --now enable dotfiles-capture.timer"`.
         Print `"[DRY-RUN] $REPO_ROOT/arch/dots-hyprland.sh verify --strict"`.
         Return 0.
       - Live execution:
         a. Execute `systemctl --user daemon-reload`.
         b. Execute `systemctl --user --now enable dotfiles-capture.timer`.
         c. Verify `systemctl --user is-active --quiet dotfiles-capture.timer` (per D-11). If not active, log failure and return 1.
         d. Execute repository verification gate: `"$REPO_ROOT/arch/dots-hyprland.sh" verify --strict`.
         e. BOOT-04 gate: Capture the exact return code of `verify --strict`. The exit code of `./bootstrap.sh` MUST strictly equal the exit code of `verify --strict`. If verify exits non-zero, bootstrap exits non-zero.

    2. Implement Section 5 in `scripts/phase23-bootstrap-assert.sh` ("Live host dry-run, PAIR_COUNT == 18, and verify --strict"):
       - Test 1: Run `./bootstrap.sh --dry-run` on live host: assert exit code 0 and structured dry-run output across all 7 steps.
       - Test 2: Invariant audit: `PAIR_COUNT="$(grep -ho -- '--verbose=5 --no-folding' arch/*.sh | wc -l || true)"`. Assert `PAIR_COUNT` is strictly 18 (preserves Phase 17 invariant).
       - Test 3: Run `./arch/dots-hyprland.sh verify --strict` on live host: assert exit 0 with `FAIL=0 FINDINGS=0`.

    3. Run the full verification suite across all sections and sibling phase gates:
       `./scripts/phase23-bootstrap-assert.sh && ./arch/dots-hyprland.sh verify --strict && ./scripts/phase17-unblock-assert.sh`
  </action>
  <acceptance_criteria>
    - `./bootstrap.sh --dry-run` exits 0 on live host and displays complete pipeline preview.
    - `PAIR_COUNT == 18` in `arch/*.sh` is strictly preserved.
    - Bootstrap Step 7 exit code strictly equals `verify --strict` exit code.
    - Executing `./scripts/phase23-bootstrap-assert.sh --section 5` exits 0 with `FAIL=0 FINDINGS=0`.
    - Full phase assert suite `./scripts/phase23-bootstrap-assert.sh && ./arch/dots-hyprland.sh verify --strict && ./scripts/phase17-unblock-assert.sh` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase23-bootstrap-assert.sh && ./arch/dots-hyprland.sh verify --strict && ./scripts/phase17-unblock-assert.sh</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0</fails_when>
  </verify>
  <done>Step 7 systemd activation and verify exit gate implemented; PAIR_COUNT == 18 preserved; all 5 sections of phase assert suite and full regression gate passing cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System pacman database → Repository data (`arch/pkglist-*.txt`) | Pacman package query outputs committed to git repository data |
| Bootstrap process → Systemd user manager | D-Bus commands to enable and start user timer |
| Bootstrap process → Exit status gate | Return code forwarded to caller/operator environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-09 | Tampering | Package Snapshots | medium | mitigate | Regenerate package snapshot files ONLY when `--snapshot` is explicitly specified; ensure standard bootstrap runs never mutate tracked files (per D-21) |
| T-23-10 | Denial of Service | Hostname Resolution | low | mitigate | Avoid calling `hostname` binary which is absent on clean Arch Linux; use `uname -n` and `/etc/hostname` fallbacks (Pitfall 4) |
| T-23-11 | Tampering | Systemd User Activation | medium | mitigate | Activate user capture timer strictly in Stage 2 after relogin when user D-Bus and graphical session are verified (per D-11) |
| T-23-12 | Repudiation | Exit Code Gate | high | mitigate | Forward the exit code of `arch/dots-hyprland.sh verify --strict` directly as the bootstrap process exit code; fail closed if any link is broken or file has drifted (per BOOT-04) |
</threat_model>

<verification>
Automated test harness verification across Sections 4 and 5, followed by full phase regression gate:
`./scripts/phase23-bootstrap-assert.sh --section 4`
`./scripts/phase23-bootstrap-assert.sh --section 5`
`./scripts/phase23-bootstrap-assert.sh && ./arch/dots-hyprland.sh verify --strict && ./scripts/phase17-unblock-assert.sh`
</verification>

<success_criteria>
1. `arch/pkglist-native.txt` and `arch/pkglist-aur.txt` exist with valid headers and alphabetical sorting.
2. Stage 1 pauses with eye-catching relogin banner and Stage 2 checks graphical session.
3. Post-relogin activates `dotfiles-capture.timer` and executes `verify --strict`.
4. Bootstrap exit code strictly equals `verify --strict` exit code.
5. `PAIR_COUNT == 18` in `arch/*.sh` is strictly preserved.
6. Full test suite passes cleanly with zero failures and zero findings.
</success_criteria>

<output>
Create `.planning/phases/23-one-command-bootstrap/23-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces
- Package snapshots: `arch/pkglist-native.txt`, `arch/pkglist-aur.txt`
- Banner & probe functions in `bootstrap.sh`: `show_relogin_banner()`, `probe_session_environment()`
- Stage 2 verification functions in `bootstrap.sh`: `run_stage2_verification()`, `generate_package_snapshots()`
- Test sections: Section 4 and Section 5 in `scripts/phase23-bootstrap-assert.sh`
