---
phase: 17-unblock-stow-and-restore-the-session-target
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - .gitattributes
  - .gitignore
  - scripts/phase17-unblock-assert.sh
autonomous: false
requirements: [FIX-06]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`.gitattributes` exists at the repo root and contains the line `* text=auto eol=lf` as a whole line"
    - "Every `.gitignore` pattern this plan adds is proven to match its intended target by `git check-ignore -v` exiting 0 and naming the pattern's line number — no pattern is accepted on inspection alone"
    - "The generated-theme paths named in PROJECT.md — `kdeglobals`, `Kvantum/`, both `gtk.css` siblings, and the matugen output — are covered by patterns that reach them wherever they sit in the repo, not only at the repo root"
    - "Machine-state patterns follow the tree-qualified shape of the existing qBittorrent block: fully qualified from the repo root under a comment header, never a bare mid-slash path anchored at a tree that does not exist"
    - "A ban or match assertion over a missing or empty file is prevented: every grep section asserts its input exists and is non-empty before asserting anything about its content"
    - statement: "A `.gitignore` pattern has no effect on a file already tracked in the index — `.config/kdeglobals` is tracked today, so its new pattern governs future writes only. Untracking it is a Phase 18 redistribution decision (FIX-03) and is recorded as a handoff row rather than performed here"
      verification: backstop
    - statement: "`* text=auto eol=lf` normalises line endings on checkin for content git classifies as text; blobs already committed with CRLF are not rewritten until they are re-added, and this plan does not re-add them. Path comparison in `git check-ignore` is byte-wise against the pattern, so the proof commands above are the whole encoding claim this plan makes"
      verification: backstop
  artifacts:
    - path: ".gitattributes"
      provides: "repo-wide line-ending normalisation (D-10)"
      contains: "* text=auto eol=lf"
    - path: ".gitignore"
      provides: "machine-state and generated-theme exclusion patterns (D-14), tree-qualified"
      contains: "kdeglobals"
    - path: "scripts/phase17-unblock-assert.sh"
      provides: "criterion 4 sections 4a and 4b — the exact-line check and the per-pattern check-ignore proofs"
      contains: "check-ignore"
  key_links:
    - from: "scripts/phase17-unblock-assert.sh"
      to: ".gitignore"
      via: "one `git check-ignore -v` invocation per newly added pattern, asserting exit 0"
      pattern: "git check-ignore"
    - from: ".gitattributes"
      to: "every tracked text file"
      via: "the `*` glob applying text=auto eol=lf repo-wide"
      pattern: "text=auto"
  prohibitions:
    - statement: "Must NOT read, echo, paste, print, or commit the contents of the tracked `.env` under `stow/system_monitor/` into any transcript, plan, summary, commit message, or terminal scrollback — the triage decides disposition from path, tracked status and the operator's own knowledge, never from the value"
      status: resolved
      verification: judgment
    - statement: "Must NOT `git rm --cached` any file that another phase owns as part of a hygiene commit — repo-root `.config/` is Phase 18 territory, and a quiet untracking inside an unrelated commit removes a decision surface a later phase is relying on"
      status: resolved
      verification: judgment
---

<objective>
Settle the secret-triage decision that gates this wave, then land the git-metadata half of
FIX-06: `.gitattributes` with the normalisation line, and `.gitignore` patterns that are
proven by `git check-ignore` to reach the paths they were written for.

Purpose: two of the `.gitignore` patterns that exist today look like they cover a tracked
`.env` and do not — a pattern containing a non-trailing slash is anchored to the directory
holding the `.gitignore`, which is the repo root, and the real file sits under `stow/`. A
pattern set written the same way would silently cover nothing. This plan makes every new
pattern prove itself.
Output: one new dotfile, an extended `.gitignore`, two new assert sections.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-CONTEXT.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-RESEARCH.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-PATTERNS.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: triage the tracked .env before any FIX-06 work</name>
  <what-built>
    Nothing yet — this gate runs before any FIX-06 work, including before gitleaks is installed.

    Research found a file tracked in git that the phase's "zero findings" criterion depends on,
    and its disposition is a judgement only the operator can make:

    - `stow/system_monitor/.config/system_monitor/ping/.env` is **tracked in git**. So is a
      sibling `.env.example`.
    - Its contents were deliberately never read during research and must not be read now.
    - `.gitignore` lines 1 and 2 name `.config/system_monitor/ping/.env` and
      `.config/system_monitor/ping/data/`. Both contain a non-trailing slash, so both are
      anchored at the repo root and neither reaches the `stow/` copy. Repo-root
      `.config/system_monitor/` does not exist at all, so both patterns are dead.
    - A `.gitignore` line never untracks an already-tracked file, and a `git rm --cached`
      leaves the value in git history, which the secret scanner reads by default.

    Three further strings carrying the literal token `REDACTED` sit in comments under
    `stow/waybar/.config/waybar/config.jsonc` and `stow/zsh/.zshrc`. They appear pre-redacted
    and are triage candidates rather than findings.
  </what-built>
  <how-to-verify>
    Decide which of these three applies. Do not open the file in a shared terminal, and do not
    paste any part of its contents into the reply.

    A) **No live credential.** The file holds no secret worth protecting. Outcome: it stays
       tracked, and if the scanner flags it the finding gets an allowlist entry in
       `.gitleaks.toml` carrying a one-line reason. Criterion 4's "zero findings" is reachable.

    B) **Live credential present.** Outcome: rotate the credential at its source first, then
       `git rm --cached` the path, add a correctly tree-qualified ignore pattern, and add an
       allowlist entry for the historical blob whose reason records the rotation. Criterion 4
       is reachable, but only with that allowlist entry — the value stays in history and a
       history rewrite is out of scope for this phase.

    C) **History must be clean.** Outcome: this is a history rewrite across a repository that
       carries archived milestone evidence. It is out of scope for Phase 17 and must be
       escalated as its own piece of work; this wave stops here.

    Also state whether the three `REDACTED` comment strings may be allowlisted on sight if the
    scanner flags them.
  </how-to-verify>
  <resume-signal>Reply with `A`, `B`, or `C`, plus one line on the REDACTED comment strings. If `B`, confirm the rotation has been done before replying.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Create .gitattributes and extend .gitignore with tree-qualified patterns</name>
  <files>.gitattributes, .gitignore</files>
  <read_first>
    - `.gitignore` — all 19 lines, including the dead root-anchored pair at lines 1-2 and the correctly-shaped qBittorrent block at lines 8-13 that the new patterns must imitate.
    - `.planning/PROJECT.md` — the generated-theme exclusion list this plan turns into patterns.
    - `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-PATTERNS.md` § `.gitignore` — the analog block and the named anti-pattern in the same file.
    - `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-RESEARCH.md` §F-6, §F-8, §F-9, and §Pitfall 5.
  </read_first>
  <action>
    Create `.gitattributes` at the repo root containing the single line `* text=auto eol=lf`
    (D-10). No `.gitattributes` exists today; the content is fixed and not open to
    interpretation.

    Extend `.gitignore` with two new comment-headed blocks, both written in the tree-qualified
    shape of the existing qBittorrent block — fully qualified from the repo root, one path per
    line, grouped under a `#` header. Do not imitate lines 1-2 of the same file: a pattern
    containing a slash anywhere except the end is anchored to the directory holding the
    `.gitignore`, which is why those two reach nothing.

    Block one, generated theme output (the list PROJECT.md already excludes): `kdeglobals`,
    the `Kvantum/` directory, both `gtk.css` siblings, and the matugen output. Where the intent
    is "this basename anywhere in the repo", write a slash-free basename pattern — a slash-free
    pattern matches at any depth, which is exactly the semantics these need, since the same
    generated file can appear under `stow/`, under `restow/`, and at the repo root. Where the
    intent is one specific known path, write it fully qualified.

    Block two, machine state: caches, sockets, lockfiles, `.venv`, and state directories. The
    precise glob list is left to the executor's judgement beyond the named generated-theme
    paths; keep it to categories that are demonstrably machine-written rather than authored,
    and prove each one in Task 2. Per-tree ignore rules are deliberately not written here —
    they need Phase 18's collision map to say which tree a path belongs to (D-14).

    Apply the triage outcome from the checkpoint. Under outcome A, add nothing for the `.env`.
    Under outcome B, add the correctly tree-qualified ignore pattern for
    `stow/system_monitor/.config/system_monitor/ping/.env` and perform the `git rm --cached` for
    that one path only. Under either outcome, do **not** `git rm --cached` `.config/kdeglobals`:
    it is tracked, repo-root `.config/` is Phase 18's territory under FIX-03, and its new ignore
    pattern correctly governs any future write. Record it instead as a Phase 18 handoff row in
    this plan's summary, so plan 17-05 can carry it into the handoff document.

    Leave the two dead patterns at lines 1-2 in place. They are inert, removing them is a
    repo-root `.config/` decision, and this plan does not own that tree.
  </action>
  <acceptance_criteria>
    - `git check-ignore -v <path>` exits 0 and prints a `.gitattributes`-free, `.gitignore`-sourced match line for **every** path each new pattern was written for — one invocation per pattern, all of them proven, none accepted on inspection.
    - `grep -Fxq '* text=auto eol=lf' .gitattributes` succeeds.
    - `.gitattributes` is exactly one non-empty line.
    - `git ls-files .config/kdeglobals` still prints that path — the file was not untracked by this plan.
    - `git status --porcelain` shows no deletion of any path under `.config/` .
    - Under triage outcome B only: `git ls-files stow/system_monitor/.config/system_monitor/ping/.env` prints nothing, and `git check-ignore -v` on that path exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -Fxq '* text=auto eol=lf' .gitattributes &amp;&amp; echo ATTR_OK</automated>
    <fails_when>non-zero exit, or `ATTR_OK` is not printed — the exact required line is absent from `.gitattributes`</fails_when>
    <automated>git ls-files .config/kdeglobals</automated>
    <fails_when>nothing is printed — a file owned by Phase 18 was untracked by this plan</fails_when>
    <automated>git check-ignore -v .gsd/</automated>
    <fails_when>non-zero exit — the control probe proves the `check-ignore` harness itself works; a red here means the proofs in Task 2 cannot be trusted</fails_when>
  </verify>
  <done>`.gitattributes` carries the required line, `.gitignore` carries tree-qualified generated-theme and machine-state blocks, and no file another phase owns was untracked.</done>
</task>

<task type="auto">
  <name>Task 2: Assert criterion 4a and 4b in the harness with per-pattern check-ignore proofs</name>
  <files>scripts/phase17-unblock-assert.sh</files>
  <read_first>
    - `scripts/phase17-unblock-assert.sh` — the harness and the vacuity-guard idiom already used in criteria 1 and 3.
    - `.gitattributes` and `.gitignore` as written in Task 1.
    - `scripts/phase16-retire-assert.sh` lines 251-264 — the vacuity guard before a content assertion.
  </read_first>
  <action>
    Add a criterion 4 section to `scripts/phase17-unblock-assert.sh` covering 4a and 4b only;
    the scan half (4c, 4d) belongs to plan 17-04 and must not be stubbed here.

    Section 4a: assert `.gitattributes` exists and is non-empty first — a whole-line grep over
    a missing file passes vacuously in the wrong direction — then assert the exact line with
    `grep -Fxq '* text=auto eol=lf'`. Use the fixed-string, whole-line form; a substring match
    would accept a line that merely contains the text.

    Section 4b: for every pattern Task 1 added, run `git check-ignore -v` against the path it
    was written for and assert exit 0, emitting one `pass`/`fail` per pattern so a single
    silently-dead pattern cannot hide behind a passing sibling. Precede the loop with a guard
    asserting the path list is non-empty. Include at least one deliberate **negative** control:
    a path that must NOT be ignored — a tracked authored file such as `arch/btop.sh` — asserted
    to make `git check-ignore` exit non-zero, so an over-broad pattern that swallows source is
    caught rather than celebrated. Each failure branch re-prints the failing `git check-ignore -v`
    output suffixed `|| true`.

    Keep the section non-mutating: `git check-ignore` is read-only, and no `git add`,
    `git rm`, or index write belongs in the assert script.
  </action>
  <acceptance_criteria>
    - The assert script emits one `[PASS]` per new `.gitignore` pattern from the `check-ignore` loop, plus one `[PASS]` for the exact `.gitattributes` line, plus one `[PASS]` for the not-ignored negative control.
    - The criterion 4 section in the assert script contains no `git add`, no `git rm`, and no `git commit`.
    - Deleting `.gitattributes` makes section 4a report `[FAIL]` rather than passing vacuously — confirm once by hand during the task and restore the file.
    - `./scripts/phase17-unblock-assert.sh` closes `=== done: FAIL=0 ===`.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase17-unblock-assert.sh</automated>
    <fails_when>non-zero exit, or the closing line is not `=== done: FAIL=0 ===`, or no `[PASS]` line mentioning `check-ignore` appears in stdout</fails_when>
    <automated>grep -cE 'git (add|rm|commit)' scripts/phase17-unblock-assert.sh || true</automated>
    <fails_when>the printed count is anything other than `0` — the assert script gained an index write and is no longer non-mutating</fails_when>
    <automated>./scripts/phase14-verify.sh</automated>
    <fails_when>non-zero exit, or the closing line does not report `FAIL=0` — this wave adds git metadata and that script asserts a clean working tree, so commit before running it</fails_when>
  </verify>
  <done>Every new ignore pattern proves itself against its target path, the normalisation line is asserted whole-line, and an over-broad pattern would be caught by the negative control.</done>
</task>

</tasks>

<threat_model>
**ASVS level: L1**, blocking on `high`. Dispositions below are assigned at OWASP ASVS
Level 1 under the project's configured `security_block_on: high`: no row rated `high` or
`critical` may be dispositioned `accept` — every such row carries a `mitigate` with a named
implementation. `accept` is available only to `medium` and `low` rows, each with a written
rationale.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git index/history | anything not excluded becomes a permanent, distributable artifact; history retention makes the boundary one-way |
| operator terminal → transcript/commit message | triage output is a place a secret value can escape into a durable record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-02 | Information Disclosure | tracked `.env` under `stow/system_monitor/` | high | mitigate | blocking-human triage before any scan work; disposition decided from path and tracked status only; under outcome B, rotate first, then untrack, then ignore with a tree-qualified pattern, then allowlist the historical blob with a written reason (D-13) |
| T-17-07 | Information Disclosure | secret value reaching a transcript, summary, or commit message during triage | medium | mitigate | the checkpoint forbids opening or pasting the file; every later scan invocation runs with redaction on (plan 17-04) |
| T-17-13 | Tampering | `.gitignore` patterns that silently match nothing | medium | mitigate | every pattern is proven with `git check-ignore -v` exit 0, one assertion per pattern, plus a not-ignored negative control so an over-broad pattern is caught |

Residual: generic secret-handling and data-retention controls are canon and are covered by
`/gsd-secure-phase`; this register records the two bespoke decisions — the triage gate and
the per-pattern proof — that no canon tool would produce.
</threat_model>

<verification>
`./scripts/phase17-unblock-assert.sh` after every task commit, closing `=== done: FAIL=0 ===`.
At wave close also `./scripts/phase14-verify.sh`, which asserts a clean working tree, so commit
first.
</verification>

<success_criteria>
- `.gitattributes` exists with `* text=auto eol=lf`, and `.gitignore` carries the generated-theme and machine-state patterns (ROADMAP criterion 4, first half).
- Every added pattern is proven to match its target; no pattern is accepted on inspection.
- The `.env` disposition is an operator decision on record, not an executor judgement.
</success_criteria>

## Artifacts this phase produces

New symbols created by this plan:

- `.gitattributes` — new file at the repo root.
- Two new comment-headed pattern blocks in `.gitignore` (generated theme output; machine state).
- Criterion 4 sections 4a and 4b inside `scripts/phase17-unblock-assert.sh`.
- `.gitleaks.toml` is **not** created here — it is conditional on the scan outcome and belongs to plan 17-04.

## Flagged planner assumptions

None new in this plan. The two backstop truths above record measured limits of `.gitignore`
and `.gitattributes` semantics, not assumptions.

## Prohibition recall breadcrumbs

- "Secret committed to VCS" as a general control is canon — covered by `/gsd-secure-phase`; the bespoke halves (no value in a durable record; no cross-phase untracking) are minted above.

<output>
Create `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-03-SUMMARY.md` when done.
Record the triage outcome letter, the full list of patterns added with the `check-ignore` proof
for each, and the `.config/kdeglobals` handoff row for plan 17-05 to carry forward.
</output>
