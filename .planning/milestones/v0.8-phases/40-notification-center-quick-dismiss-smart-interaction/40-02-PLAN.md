---
phase: 40-notification-center-quick-dismiss-smart-interaction
plan: 02
type: execute
wave: 2
depends_on:
  - "40-01"
files_modified:
  - restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml
  - restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml
  - scripts/phase40-notification-interaction-assert.sh
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - NAV-01
  - OTP-02

estimate:
  tokens: 42000
  raw_tokens: 20000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "NotificationGroup.qml card header in Right Sidebar displays an always-visible 'X' close button for single notifications (!root.multipleNotifications && !root.popup) that invokes root.destroyWithAnimation() (NOTIF-01, D-01)."
    - "NotificationGroup.qml preserves 100% upstream default UI for grouped notifications (expandButton with chevron and count) and suppresses the 'X' button on desktop toast popups (root.popup === true) (NOTIF-02, D-02, D-03)."
    - "NotificationGroup.qml background implicitHeight calculates full height for single notifications ((root.expanded || !root.multipleNotifications) ? row.implicitHeight + padding * 2 : Math.min(80, ...)) eliminating the 80px ceiling that would clip chips."
    - "Clicking notification card body invokes sending application's default D-Bus action via Notifications.attemptInvokeAction(notificationId, 'default') when available, falling back to Qt.openUrlExternally(url) when an embedded URL is detected, and closing the Right Sidebar (GlobalStates.sidebarRightOpen = false) in all paths (NAV-01, D-04, D-05, D-06)."
    - "NotificationItem.qml renders an icon-free Material 3 'Copy [Code]' pill chip directly beneath notification text using Appearance.colors.colSecondaryContainer and colOnSecondaryContainer (OTP-02, D-08)."
    - "Clicking the OTP chip copies strictly the extracted code to Quickshell.clipboardText and displays 'Copied!' for 1500ms without opening links, triggering D-Bus, or closing the sidebar (D-09, T-40-02)."
    - "All QML modifications are deployed as leaf symlinks via restow/quickshell/ without modifying vendor/dots-hyprland (INTG-01, D-10)."
    - "Full 5-section test suite scripts/phase40-notification-interaction-assert.sh and ./arch/dots-hyprland.sh verify --strict pass with FAIL=0 FINDINGS=0 (INTG-02, INTG-03)."
  artifacts:
    - path: "restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml"
      provides: "Notification group container overlay adding single-notification 'X' close button, height auto-expansion, and body click routing"
      contains: "closeButton"
    - path: "restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml"
      provides: "Notification item overlay implementing smart body click routing and icon-free Material 3 OTP quick-action chip"
      contains: "collapsedOtpChip"
    - path: "scripts/phase40-notification-interaction-assert.sh"
      provides: "Full 5-section automated assertion test harness validating packaging, static AST properties, OTP regex matrix, URL extraction, and strict dots verification"
      contains: "phase40-notification-interaction-assert"
  key_links:
    - from: "restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml"
      to: "restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml"
      via: "NotificationUtils.extractUrl call during body click activation"
      pattern: "NotificationUtils\\.extractUrl"
    - from: "restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml"
      to: "restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml"
      via: "NotificationUtils.extractOtpCode property binding and extractUrl call"
      pattern: "NotificationUtils\\.extractOtpCode"
    - from: "restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml"
      to: "Quickshell"
      via: "Quickshell.clipboardText assignment on OTP chip click"
      pattern: "Quickshell\\.clipboardText\\s*="
    - from: "scripts/phase40-notification-interaction-assert.sh"
      to: "arch/dots-hyprland.sh"
      via: "strict repository verification gate invocation"
      pattern: "\\./arch/dots-hyprland\\.sh verify --strict"
---

<objective>
Implement the QML notification overlays in `NotificationGroup.qml` and `NotificationItem.qml`: provide an always-visible 'X' close button on single-notification sidebar cards while strictly preserving upstream multi-group and toast popup behavior (NOTIF-01, NOTIF-02, D-01, D-02, D-03); fix card height clamping (`Math.min(80, ...)`) to prevent chip clipping; implement smart body click routing prioritizing D-Bus "default" action with URL fallback and sidebar closure (NAV-01, D-04, D-05, D-06); render an icon-free Material 3 "Copy [Code]" pill chip with 1.5s visual confirmation (OTP-02, D-08, D-09, T-40-02); deploy overlays as leaf symlinks via GNU Stow, and pass the complete 5-section assertion test suite and `./arch/dots-hyprland.sh verify --strict` with zero git churn.

Purpose: Deliver effortless single-click dismissal, context-aware notification body navigation, and streamlined 2FA clipboard copying without dropdown expansion or visual clutter.
Output: Restow overlays `NotificationGroup.qml` and `NotificationItem.qml`, and complete 5-section test suite `scripts/phase40-notification-interaction-assert.sh`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-CONTEXT.md
@.planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-RESEARCH.md
@.planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-PATTERNS.md
@.planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-VALIDATION.md
@.planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-01-PLAN.md
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/NotificationGroupExpandButton.qml
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/Appearance.qml
@scripts/phase40-notification-interaction-assert.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header close button, height auto-expansion, and body click in NotificationGroup.qml (NOTIF-01, NOTIF-02, NAV-01, D-01..D-06)</name>
  <read_first>
    - vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml
    - vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/NotificationGroupExpandButton.qml
    - .planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-CONTEXT.md
    - .planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-RESEARCH.md
    - .planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-PATTERNS.md
  </read_first>
  <files>restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml, scripts/phase40-notification-interaction-assert.sh</files>
  <action>
    Seed `NotificationGroup.qml` from upstream into `restow/quickshell/`, add the single-notification 'X' close button to `topRow`, update `expandButton.visible` for multi-group and toast parity, remove the 80px height clamp for single notifications, add smart body click activation routing, and deploy via GNU Stow leaf symlinks (per D-01..D-06, NOTIF-01, NOTIF-02, NAV-01).

    1. Author `restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml`:
       - Create parent directory `restow/quickshell/.config/quickshell/ii/modules/common/widgets`.
       - Seed file from `vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml`.
       - In `topRow` (around line 175):
         * Update `NotificationGroupExpandButton` with `id: expandButton` visibility to `visible: root.multipleNotifications || root.popup` (per D-02, D-03).
         * Add `RippleButton` with `id: closeButton` visible strictly when `!root.multipleNotifications && !root.popup` (per D-01, NOTIF-01, NOTIF-02).
         * Anchor `closeButton` to `anchors.right: parent.right` and `anchors.verticalCenter: parent.verticalCenter`. Set dimensions `implicitWidth: topRow.fontSize + 4 * 2` and `implicitHeight: topRow.fontSize + 4 * 2`. Use `Appearance.rounding.full` for `buttonRadius`. Style background with mixed `Appearance.colors.colLayer2` and `Appearance.colors.colLayer2Hover` (0.5), hover background `Appearance.colors.colLayer2Hover`, and ripple `Appearance.colors.colLayer2Active`.
         * Place `MaterialSymbol { anchors.centerIn: parent; text: "close"; iconSize: expandButton.iconSize; color: Appearance.colors.colOnLayer2 }` and `StyledToolTip { text: Translation.tr("Dismiss notification") }` inside `closeButton`.
         * On `closeButton.onClicked`: invoke `root.destroyWithAnimation()`.
         * Update `topTextRow` anchors: `anchors.right: expandButton.visible ? expandButton.left : (closeButton.visible ? closeButton.left : parent.right)`.
         * Update `topRow` implicitHeight: `Math.max(topTextRow.implicitHeight, expandButton.visible ? expandButton.implicitHeight : closeButton.implicitHeight)`.
       - In `background` (around line 120):
         * Update `implicitHeight` formula to prevent 80px clamping on single notification cards: `(root.expanded || !root.multipleNotifications) ? row.implicitHeight + padding * 2 : Math.min(80, row.implicitHeight + padding * 2)` (preventing chip clipping per Pitfall 3).
       - In `dragManager` (around line 81):
         * Update `onClicked` handler: if `mouse.button === Qt.MiddleButton`, invoke `root.destroyWithAnimation()`; if `mouse.button === Qt.LeftButton`, if `root.multipleNotifications` call `root.toggleExpanded()`, else call `root.activateNotification()`.
       - On `root`:
         * Define `activateNotification()` function: if `root.notifications.length === 0`, return early. Inspect first notification `notif`. Check if `notif.actions?.some(a => a.identifier === "default")`. If default action exists, call `Notifications.attemptInvokeAction(notif.notificationId, "default")`. Else extract URL via `NotificationUtils.extractUrl(notif.body)`: if URL found, open via `Qt.openUrlExternally(extractedUrl)` and discard notification via `Notifications.discardNotification(notif.notificationId)`. Else discard notification directly via `Notifications.discardNotification(notif.notificationId)`. In all pathways, close the right sidebar via `GlobalStates.sidebarRightOpen = false` (per D-04, D-05, D-06, NAV-01).

    2. Deploy `NotificationGroup.qml` via GNU Stow:
       - Note reversibility: check if `~/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml` exists as a regular file; if so, preserve it by moving to `NotificationGroup.qml.bak` before stowing.
       - Ensure `~/.config/quickshell/ii/modules/common/widgets` is a real directory (no folding).
       - Run `stow --verbose=5 --no-folding -d "$REPO_ROOT/restow" -t "$HOME" quickshell`.
       - Verify that `~/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml` is now a valid leaf symlink pointing into `restow/quickshell/`.

    3. Expand `scripts/phase40-notification-interaction-assert.sh`:
       - In Section 1: add symlink assertion for `NotificationGroup.qml`.
       - In Section 2: add static AST asserts verifying `closeButton` declaration, `visible: !root.multipleNotifications && !root.popup`, `root.destroyWithAnimation()`, `expandButton.visible` checking `root.multipleNotifications || root.popup`, and `activateNotification` definition with D-Bus check and `GlobalStates.sidebarRightOpen = false`.
  </action>
  <verify>
    <automated>./scripts/phase40-notification-interaction-assert.sh --section 2</automated>
    <fails_when>non-zero exit code or AST verification failure</fails_when>
    <automated>grep -q "id: closeButton" restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml &amp;&amp; grep -q "visible: !root.multipleNotifications &amp;&amp; !root.popup" restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml</automated>
    <fails_when>exit code 1 or missing closeButton definition</fails_when>
    <automated>grep -q "root.destroyWithAnimation()" restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml</automated>
    <fails_when>exit code 1 or missing destroyWithAnimation call</fails_when>
    <automated>test -L "$HOME/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml"</automated>
    <fails_when>exit code 1 or NotificationGroup.qml is not a symlink</fails_when>
  </verify>
  <acceptance_criteria>
    - `NotificationGroup.qml` contains `closeButton` with `visible: !root.multipleNotifications && !root.popup`.
    - Clicking `closeButton` invokes `root.destroyWithAnimation()`.
    - `expandButton` visibility strictly preserves upstream multi-group and toast behavior (`root.multipleNotifications || root.popup`).
    - Card height calculates full natural height for single notifications without 80px truncation.
    - Card body clicks route to `activateNotification()`, checking D-Bus `"default"` action before URL fallback, and setting `GlobalStates.sidebarRightOpen = false`.
    - `NotificationGroup.qml` is deployed as a leaf symlink.
  </acceptance_criteria>
  <done>NotificationGroup.qml overlay is authored, stowed as a leaf symlink, and verified via Section 2 AST checks.</done>
</task>

<task type="auto">
  <name>Task 2: Smart body click routing and Material 3 icon-free OTP "Copy [Code]" pill chip in NotificationItem.qml (NAV-01, OTP-02, D-04..D-06, D-08, D-09, T-40-02)</name>
  <read_first>
    - vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml
    - vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/Appearance.qml
    - .planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-CONTEXT.md
    - .planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-RESEARCH.md
    - .planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-PATTERNS.md
  </read_first>
  <files>restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml, scripts/phase40-notification-interaction-assert.sh</files>
  <action>
    Seed `NotificationItem.qml` from upstream into `restow/quickshell/`, declare the `otpCode` property, add smart body click routing in `dragManager.onClicked`, implement the clean icon-free Material 3 "Copy [Code]" pill chip in collapsed and expanded states, and deploy via GNU Stow leaf symlinks (per D-04..D-06, D-08, D-09, NAV-01, OTP-02, T-40-02).

    1. Author `restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml`:
       - Seed file from `vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml`.
       - Declare property on `root`: `property string otpCode: NotificationUtils.extractOtpCode(notificationObject?.body, notificationObject?.summary)`.
       - Define `activateNotification()` on `root`:
         * Check `if (!notificationObject) return;`.
         * Check `notificationObject.actions?.some(a => a.identifier === "default")`. If found, invoke `Notifications.attemptInvokeAction(notificationObject.notificationId, "default")`.
         * Else extract URL via `NotificationUtils.extractUrl(notificationObject.body)`: if found, open via `Qt.openUrlExternally(extractedUrl)` and discard notification via `Notifications.discardNotification(notificationObject.notificationId)`.
         * Else discard notification directly via `Notifications.discardNotification(notificationObject.notificationId)`.
         * In all pathways, close right sidebar via `GlobalStates.sidebarRightOpen = false`.
       - In `dragManager` (around line 65):
         * Update `onClicked` handler: if `mouse.button === Qt.MiddleButton`, call `root.destroyWithAnimation()`; if `mouse.button === Qt.LeftButton`, call `root.activateNotification()`.
       - In `contentColumn` (around line 140):
         * Directly beneath `summaryRow`, add `RippleButton` with `id: collapsedOtpChip` visible when `!root.expanded && root.otpCode.length > 0`.
         * Set `Layout.alignment: Qt.AlignLeft`, `implicitHeight: 28`, `implicitWidth: collapsedOtpChipText.implicitWidth + 24`.
         * Set `buttonRadius: Appearance.rounding.full`. Style with `colBackground: Appearance.colors.colSecondaryContainer`, hover mixed with `colOnSecondaryContainer` (0.15), and ripple mixed (0.3).
         * Add `property bool copied: false` and `Timer { id: collapsedTimer; interval: 1500; repeat: false; onTriggered: collapsedOtpChip.copied = false }`.
         * On `collapsedOtpChip.onClicked`: set `Quickshell.clipboardText = root.otpCode`, set `collapsedOtpChip.copied = true`, restart `collapsedTimer`. Ensure click is consumed within the chip and does not trigger body activation or sidebar closure (T-40-02).
         * Inside `collapsedOtpChip`, add `StyledText` with `id: collapsedOtpChipText`: centered, font size `Appearance.font.pixelSize.smaller`, font weight `Font.DemiBold`, color `Appearance.colors.colOnSecondaryContainer`. Strictly no decorative icons (D-08). Text binding: `collapsedOtpChip.copied ? Translation.tr("Copied!") : Translation.tr("Copy %1").arg(root.otpCode)`.
       - In `expandedContentColumn` (around line 182):
         * Directly beneath `notificationBodyText`, add `RippleButton` with `id: expandedOtpChip` visible when `root.expanded && root.otpCode.length > 0`, with identical styling, timer, clipboard setter, and text binding.
       - In `notificationBodyText.onLinkActivated`: preserve upstream behavior (`Qt.openUrlExternally(link)` and `GlobalStates.sidebarRightOpen = false`).

    2. Deploy `NotificationItem.qml` via GNU Stow:
       - Note reversibility: check if `~/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml` exists as a regular file; if so, preserve it by moving to `NotificationItem.qml.bak` before stowing.
       - Ensure `~/.config/quickshell/ii/modules/common/widgets` is a real directory (no folding).
       - Run `stow --verbose=5 --no-folding -d "$REPO_ROOT/restow" -t "$HOME" quickshell`.
       - Verify that `~/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml` is now a valid leaf symlink pointing into `restow/quickshell/`.

    3. Expand `scripts/phase40-notification-interaction-assert.sh`:
       - In Section 1: add symlink assertion for `NotificationItem.qml`.
       - In Section 2: add static AST asserts verifying `property string otpCode`, `activateNotification` definition, `collapsedOtpChip` and `expandedOtpChip` definitions, `colSecondaryContainer` / `colOnSecondaryContainer` token bindings, `Quickshell.clipboardText = root.otpCode` assignment, 1500ms `Timer` interval, and absence of decorative icon components inside the chips.
  </action>
  <verify>
    <automated>./scripts/phase40-notification-interaction-assert.sh --section 2</automated>
    <fails_when>non-zero exit code or AST verification failure</fails_when>
    <automated>grep -q "property string otpCode" restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml &amp;&amp; grep -q "Quickshell.clipboardText = root.otpCode" restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml</automated>
    <fails_when>exit code 1 or missing otpCode / clipboard assignment</fails_when>
    <automated>grep -q "colSecondaryContainer" restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml &amp;&amp; grep -q "Translation.tr(\"Copied!\")" restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml</automated>
    <fails_when>exit code 1 or missing M3 styling or Copied label</fails_when>
    <automated>test -L "$HOME/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml"</automated>
    <fails_when>exit code 1 or NotificationItem.qml is not a symlink</fails_when>
  </verify>
  <acceptance_criteria>
    - `NotificationItem.qml` exposes `otpCode` property bound to `NotificationUtils.extractOtpCode`.
    - Clean icon-free Material 3 pill chip appears in collapsed and expanded states when an OTP code is detected.
    - Clicking the chip copies strictly the code to `Quickshell.clipboardText` and flips text to "Copied!" for 1.5 seconds.
    - Clicking the card body activates D-Bus default action or URL fallback and closes the sidebar.
    - `NotificationItem.qml` is deployed as a leaf symlink.
  </acceptance_criteria>
  <done>NotificationItem.qml overlay is authored, stowed as a leaf symlink, and verified via Section 2 AST checks.</done>
</task>

<task type="auto">
  <name>Task 3: End-to-end integration, full assertion harness completion, and strict repository verification (INTG-01, INTG-02, INTG-03, D-10)</name>
  <read_first>
    - scripts/phase40-notification-interaction-assert.sh
    - .planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-VALIDATION.md
    - arch/dots-hyprland.sh
  </read_first>
  <files>scripts/phase40-notification-interaction-assert.sh</files>
  <action>
    Finalize `scripts/phase40-notification-interaction-assert.sh` across all 5 sections, execute the full assertion test suite, run strict repository verification, and assert zero git working-tree churn in `vendor/dots-hyprland` (per INTG-01, INTG-02, INTG-03).

    1. Finalize Section 1 (Symlink & Packaging Integrity):
       - Assert that all 3 restow files exist:
         * `restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml`
         * `restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml`
         * `restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml`
       - Assert that all 3 deployed files in `~/.config/quickshell/ii/...` are valid leaf symlinks resolving into `restow/quickshell/`.
       - Assert that parent directories `modules/common/functions` and `modules/common/widgets` are real directories (no folding).
       - Assert that git submodule `vendor/dots-hyprland` has zero git diff or untracked changes.

    2. Finalize Section 2 (Static AST & QML Property Verification):
       - Complete AST assertions across `NotificationGroup.qml`, `NotificationItem.qml`, and `NotificationUtils.qml`.
       - Assert absence of unanchored regexes, absence of icons in OTP chips, and presence of D-Bus action checks before invocation.

    3. Finalize Section 3 (OTP Code Regex Extraction Test Matrix):
       - Execute full 19-case test matrix against `NotificationUtils.extractOtpCode` verifying 100% detection accuracy.

    4. Finalize Section 4 (URL Extraction & Body Click Routing Execution):
       - Verify Chromium HTML anchor parsing, raw URL parsing, scheme whitelist enforcement, and click routing priority logic.

    5. Finalize Section 5 (Repository Integrity & Strict Verification):
       - Execute `./arch/dots-hyprland.sh verify --strict` and assert output contains `FAIL=0 FINDINGS=0`.
       - Compare porcelain snapshots before and after execution to guarantee zero unexpected git modifications.

    6. Execute the full test suite `./scripts/phase40-notification-interaction-assert.sh` and `./arch/dots-hyprland.sh verify --strict`.
  </action>
  <verify>
    <automated>./scripts/phase40-notification-interaction-assert.sh</automated>
    <fails_when>non-zero exit code, output containing [FAIL], or findings count greater than 0</fails_when>
    <automated>./arch/dots-hyprland.sh verify --strict</automated>
    <fails_when>non-zero exit code or output containing [FAIL] or non-zero findings</fails_when>
  </verify>
  <acceptance_criteria>
    - All 5 sections of `scripts/phase40-notification-interaction-assert.sh` exit 0 with `FAIL=0 FINDINGS=0`.
    - All 3 QML components resolve as leaf symlinks with no directory folding.
    - Submodule `vendor/dots-hyprland` is 100% clean with zero git diff.
    - `./arch/dots-hyprland.sh verify --strict` exits 0 with 0 findings.
  </acceptance_criteria>
  <done>End-to-end integration is verified, full test harness passes across all 5 sections, and repository strict verification confirms zero upstream churn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FreeDesktop Notification Text Ingestion | Untrusted notification body and summary strings originating from arbitrary third-party D-Bus applications. |
| System Clipboard Interface | Writing sensitive credentials (OTP / 2FA codes) into the system clipboard via `Quickshell.clipboardText`. |
| Browser / External URL Dispatch | External URL strings passed to system handler via `Qt.openUrlExternally()`. |
| Filesystem & Stow Deployment | Leaf symlinks deployed by GNU Stow from `restow/quickshell/` to `~/.config/quickshell/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-02 | Information Disclosure / Clipboard Access | `NotificationItem.qml` OTP chip | medium | mitigate | Copy strictly the extracted code string digits to `Quickshell.clipboardText` without surrounding context, body text, or sender metadata; provide immediate 1.5s visual confirmation ("Copied!") to the user. |
| T-40-03 | Elevation of Privilege / Command Injection | Notification body click URL dispatch | high | mitigate | Inspect extracted URL with strict scheme whitelist (`http://` or `https://` only) before calling `Qt.openUrlExternally()`; reject `javascript:`, `file:`, `data:`, or shell payloads. |
| T-40-04 | Tampering | Git submodule `vendor/dots-hyprland` | high | mitigate | Author all modifications exclusively in `restow/quickshell/`; assert zero submodule drift via `arch/dots-hyprland.sh verify --strict` (INTG-01, INTG-03). |
</threat_model>

## Artifacts this phase produces

- `restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml`: Notification group container overlay adding single-notification 'X' close button, height auto-expansion, and body click routing.
- `restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml`: Notification item overlay implementing smart body click routing and icon-free Material 3 OTP quick-action chip.
- `scripts/phase40-notification-interaction-assert.sh`: 5-section test harness validating symlink packaging, AST properties, OTP regex test matrix, URL extraction, and strict dots-hyprland verification.

## Multi-Source Coverage Audit

| Source | Item ID / Reference | Description | Status | Covered By | Notes |
|---|---|---|---|---|---|
| GOAL | ROADMAP §Phase 40 | Notification Center Quick-Dismiss & Smart Interaction. | COVERED | Task 1, Task 2, Task 3 | Full stack delivered across header close button, body click, and OTP copy chip. |
| REQ | NOTIF-01 | Card header in Right Sidebar displays always-visible 'X' close button for immediate 1-click dismissal without dropdown expansion. | COVERED | Task 1 | `closeButton` visible when `!root.multipleNotifications && !root.popup`, calling `root.destroyWithAnimation()`. |
| REQ | NOTIF-02 | Toast notification popups strictly suppress header 'X' close button (`visible: !root.popup`), preserving clean hover/timeout dismissal. | COVERED | Task 1 | `visible: !root.multipleNotifications && !root.popup` suppresses 'X' on toasts. |
| REQ | NAV-01 | Clicking notification card body invokes sending app's default D-Bus action to focus/open application. | COVERED | Task 1, Task 2 | `activateNotification()` calls `Notifications.attemptInvokeAction(..., "default")` before fallback. |
| REQ | OTP-02 | Notification card renders prominent "Copy [Code]" quick-action chip that copies extracted code to clipboard with visual confirmation. | COVERED | Task 2 | `collapsedOtpChip` and `expandedOtpChip` with `colSecondaryContainer`, `Quickshell.clipboardText`, and 1.5s timer. |
| REQ | INTG-01 | All QML modifications deployed via `restow/quickshell/` leaf symlinks without modifying `vendor/dots-hyprland`. | COVERED | Task 1, Task 2, Task 3 | Deployed via GNU Stow leaf symlinks; verified in Section 1. |
| REQ | INTG-02 | Automated assertion test harness validates notification dismissal, link opening, and OTP parsing. | COVERED | Task 3 | Finalized across all 5 sections of `scripts/phase40-notification-interaction-assert.sh`. |
| REQ | INTG-03 | `arch/dots-hyprland.sh verify --strict` passes with 0 findings and zero git working-tree churn. | COVERED | Task 3 | Verified in Section 5 and phase gate. |
| CONTEXT | D-01 | Single notification close button invoking `root.destroyWithAnimation()`. | COVERED | Task 1 | Added to `NotificationGroup.qml` `topRow`. |
| CONTEXT | D-02 | Multi-notification group retains 100% upstream default UI (`expandButton`). | COVERED | Task 1 | `expandButton.visible: root.multipleNotifications || root.popup`. |
| CONTEXT | D-03 | Toast popups retain 100% upstream hover/timeout behavior. | COVERED | Task 1 | `popup: true` suppresses 'X' button. |
| CONTEXT | D-04 | D-Bus action first on body click. | COVERED | Task 1, Task 2 | Checks `actions.some(a => a.identifier === "default")`. |
| CONTEXT | D-06 | Body click dismisses notification and closes right sidebar. | COVERED | Task 1, Task 2 | Calls `Notifications.discardNotification` and sets `GlobalStates.sidebarRightOpen = false`. |
| CONTEXT | D-08 | Clean icon-free action pill chip beneath text. | COVERED | Task 2 | Pure text pill chip without decorative icons. |
| CONTEXT | D-09 | Visual confirmation on copy (1.5s). | COVERED | Task 2 | Flips to "Copied!" for 1500ms via `Timer`. |
| CONTEXT | D-10 | Deploy modified components under `restow/quickshell/` via GNU Stow leaf symlinks. | COVERED | Task 1, Task 2, Task 3 | Stowed with `--no-folding`. |
| RESEARCH | PATTERNS §2 | `NotificationGroup.qml` Header Close Button Pattern. | COVERED | Task 1 | Implemented in `topRow` with height auto-expansion fix. |
| RESEARCH | PATTERNS §3 | `NotificationItem.qml` Body Click & OTP Pill Chip Pattern. | COVERED | Task 2 | Implemented in `contentColumn` and `expandedContentColumn`. |
| RESEARCH | PATTERNS §4 | `scripts/phase40-notification-interaction-assert.sh` 5-Section Test Harness Pattern. | COVERED | Task 3 | Complete 5-section execution. |

<verification>
Execute the full test harness:
`./scripts/phase40-notification-interaction-assert.sh`
Assert strict repository cleanliness:
`./arch/dots-hyprland.sh verify --strict`
</verification>

<success_criteria>
1. Single notification cards in the Right Sidebar display an immediate 'X' close button that animates out and dismisses the card.
2. Grouped notifications retain 100% upstream default UI and desktop toast popups strictly suppress the 'X' close button.
3. Notification card height automatically accommodates content without 80px truncation.
4. Clicking the card body invokes the sending application's D-Bus default action or opens the extracted web link in the browser, and closes the right sidebar.
5. Incoming notifications with OTP codes display an icon-free "Copy [Code]" pill chip that copies to the clipboard with a 1.5s "Copied!" confirmation.
6. All 3 QML components are deployed as leaf symlinks via `restow/quickshell/` leaving `vendor/dots-hyprland` pristine.
7. `./scripts/phase40-notification-interaction-assert.sh` passes all 5 sections with FAIL=0 FINDINGS=0.
8. `./arch/dots-hyprland.sh verify --strict` passes with 0 findings and zero git working tree churn.
</success_criteria>

<output>
Create `.planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-02-SUMMARY.md` when done
</output>
