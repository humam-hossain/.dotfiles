---
phase: 40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase40.1-clock-volume-assert.sh
  - restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml
  - capture/ii/.config/illogical-impulse/config.json
  - stow/hypr/.config/hypr/custom/keybinds.lua
  - restow/quickshell/.config/quickshell/ii/modules/common/Config.qml
  - restow/quickshell/.config/quickshell/ii/services/Audio.qml
autonomous: true
requirements:
  - CLOCK-01
  - VOL-01

estimate:
  tokens: 42000
  raw_tokens: 20000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "ClockWidget.qml status bar clock pill enforces 10px horizontal breathing room via 5px margins on rowLayout plus 5px BarGroup padding, matching the 10px sidebar button and ~9px Resources pill (CLOCK-01, D-01)."
    - "ClockWidget.qml retains the 8px non-glyph spacer (implicitWidth: 8) between time and date strings, preserving second precision (DateTime.time) and full date formatting (DateTime.longDate) (D-02)."
    - "capture/ii/.config/illogical-impulse/config.json defines volumeCeiling: 1.5 under 'audio' as the single canonical source of truth for the desktop environment (VOL-01, D-03)."
    - "stow/hypr/.config/hypr/custom/keybinds.lua unbinds upstream XF86AudioRaiseVolume, parses config.json in pure Lua at startup/reload with fallback 1.5, and binds 'wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+ -l <ceiling>' with zero subshell overhead during key repeats (VOL-01, D-04, T-40.1-02)."
    - "restow/quickshell/.config/quickshell/ii/modules/common/Config.qml declares 'property real volumeCeiling: 1.5' on JsonObject audio, enabling Quickshell JsonAdapter deserialization (VOL-01, D-05)."
    - "restow/quickshell/.config/quickshell/ii/services/Audio.qml exposes 'readonly property real maxVolume: Config.options?.audio?.volumeCeiling ?? 1.5' as the central property used across Quickshell (VOL-01, D-05)."
    - "All QML modifications are deployed as leaf symlinks via restow/quickshell/ without modifying vendor/dots-hyprland (INTG-01, T-40.1-01)."
  artifacts:
    - path: "scripts/phase40.1-clock-volume-assert.sh"
      provides: "Automated 5-section test harness validating symlinks, static AST properties, ceiling contract, volume ergonomics, and strict repository verification"
      contains: "phase40.1-clock-volume-assert"
    - path: "restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml"
      provides: "Status bar clock widget with 10px horizontal breathing room and 8px spacer"
      contains: "anchors.leftMargin: 5"
    - path: "capture/ii/.config/illogical-impulse/config.json"
      provides: "Single source of truth desktop configuration defining audio.volumeCeiling"
      contains: "\"volumeCeiling\": 1.5"
    - path: "stow/hypr/.config/hypr/custom/keybinds.lua"
      provides: "Hyprland keybinding overlay parsing config.json via Lua and binding dynamic volume raise"
      contains: "wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+ -l"
    - path: "restow/quickshell/.config/quickshell/ii/modules/common/Config.qml"
      provides: "Quickshell schema adapter declaring audio.volumeCeiling for reactive binding"
      contains: "property real volumeCeiling: 1.5"
    - path: "restow/quickshell/.config/quickshell/ii/services/Audio.qml"
      provides: "Quickshell PipeWire audio service exposing centralized maxVolume property"
      contains: "readonly property real maxVolume:"
  key_links:
    - from: "capture/ii/.config/illogical-impulse/config.json"
      to: "stow/hypr/.config/hypr/custom/keybinds.lua"
      via: "pure Lua io.open and regex extraction at compositor startup"
      pattern: "\"volumeCeiling\"%s*:%s*([%d%.]+)"
    - from: "capture/ii/.config/illogical-impulse/config.json"
      to: "restow/quickshell/.config/quickshell/ii/modules/common/Config.qml"
      via: "Quickshell.Io.JsonAdapter reactive JSON mapping"
      pattern: "property real volumeCeiling: 1.5"
    - from: "restow/quickshell/.config/quickshell/ii/modules/common/Config.qml"
      to: "restow/quickshell/.config/quickshell/ii/services/Audio.qml"
      via: "Config.options.audio.volumeCeiling binding to Audio.maxVolume"
      pattern: "Config\\.options\\?\\.audio\\?\\.volumeCeiling \\?\\? 1\\.5"
    - from: "scripts/phase40.1-clock-volume-assert.sh"
      to: "arch/dots-hyprland.sh"
      via: "strict repository verification gate invocation"
      pattern: "\\./arch/dots-hyprland\\.sh verify --strict"
---

<objective>
Establish the automated test harness foundation (`scripts/phase40.1-clock-volume-assert.sh`) with strict execution modes and git porcelain baseline tracking, restore comfortable 10px horizontal breathing room to the status bar clock pill in `ClockWidget.qml`, and establish the desktop-wide 150% (1.5) volume ceiling single source of truth across `config.json`, `keybinds.lua`, `Config.qml`, and `Audio.qml`.

Purpose: Eliminate visual cramping on the status bar clock pill and unify the system audio ceiling contract across Hyprland keybinds and Quickshell before wiring UI controls.
Output: Executable test harness `scripts/phase40.1-clock-volume-assert.sh`, updated `ClockWidget.qml`, updated `config.json`, updated `keybinds.lua`, and restow overlays for `Config.qml` and `Audio.qml`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-CONTEXT.md
@.planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-RESEARCH.md
@.planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-PATTERNS.md
@.planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-VALIDATION.md
@restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml
@capture/ii/.config/illogical-impulse/config.json
@stow/hypr/.config/hypr/custom/keybinds.lua
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/Config.qml
@vendor/dots-hyprland/dots/.config/quickshell/ii/services/Audio.qml
@scripts/phase40-notification-interaction-assert.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — Test harness foundation (scripts/phase40.1-clock-volume-assert.sh), restow scaffolding, and baseline symlink/porcelain integrity</name>
  <read_first>
    - .planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-CONTEXT.md
    - .planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-RESEARCH.md
    - .planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-PATTERNS.md
    - scripts/phase40-notification-interaction-assert.sh
    - arch/dots-hyprland.sh
  </read_first>
  <files>scripts/phase40.1-clock-volume-assert.sh</files>
  <action>
    Establish the phase test harness scaffold at `scripts/phase40.1-clock-volume-assert.sh` following the proven 5-section architecture from `scripts/phase40-notification-interaction-assert.sh` (per INTG-01, INTG-03, T-40.1-01, T-40.1-04):

    1. Construct `scripts/phase40.1-clock-volume-assert.sh`:
       - Enforce bash strict mode (`set -euo pipefail`), non-root guard (`[[ "${EUID:-$(id -u)}" -ne 0 ]] || { echo "Error: Do not run as root" >&2; exit 1; }`).
       - CLI argument parsing supporting `--section <1-5>` (`-s <1-5>`), `--syntax` (`-c`), `--help` (`-h`).
       - Trap cleanup (`trap cleanup EXIT`) tracking temporary files in `TMP_FILES` array.
       - Git porcelain snapshot tracking (`PORCELAIN_BEFORE` and `PORCELAIN_AFTER`) using `git status --porcelain` to detect and fail on any working-tree churn (T-40.1-04).
       - Implement Section 1 (Restow Isolation & Symlink Integrity):
         * Assert git submodule `vendor/dots-hyprland` has 0 uncommitted changes (`git -C vendor/dots-hyprland status --porcelain`).
         * Assert `restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml` exists as a regular file.
         * Assert live `~/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml` is a valid symlink resolving into `restow/quickshell/`.
         * Assert live `~/.config/hypr/custom/keybinds.lua` is a valid symlink resolving into `stow/hypr/`.
         * Assert directories `~/.config/quickshell/ii/modules/ii/bar`, `~/.config/quickshell/ii/modules/common`, and `~/.config/quickshell/ii/services` are real directories without directory folding.
       - Implement Section 2 baseline (Clock Pill Geometry AST):
         * Check `ClockWidget.qml` contains required layout margins and properties.
       - Implement Section 3 baseline (Single Source of Truth Ceiling Contract):
         * Check `config.json`, `keybinds.lua`, `Config.qml`, and `Audio.qml` ceiling declarations.
       - Implement Section 5 baseline (Repository & Submodule Porcelain Cleanliness):
         * Execute `./arch/dots-hyprland.sh verify --strict` requiring `FAIL=0 FINDINGS=0`.
         * Assert post-run porcelain matches pre-run porcelain snapshot byte-for-byte.
       - Make `scripts/phase40.1-clock-volume-assert.sh` executable (`chmod +x scripts/phase40.1-clock-volume-assert.sh`).
  </action>
  <verify>
    <automated>test -x scripts/phase40.1-clock-volume-assert.sh &amp;&amp; ./scripts/phase40.1-clock-volume-assert.sh --section 1</automated>
    <fails_when>non-zero exit code or output containing [FAIL]</fails_when>
    <automated>./scripts/phase40.1-clock-volume-assert.sh --syntax</automated>
    <fails_when>non-zero exit code or AST validation error</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/phase40.1-clock-volume-assert.sh` exists, is executable, and passes Section 1 and `--syntax` checks.
    - CLI supports `--section 1` through `--section 5`, `--syntax`, and `--help`.
    - Git porcelain before/after snapshot mechanism is functional.
    - Submodule `vendor/dots-hyprland` is verified completely pristine.
  </acceptance_criteria>
  <done>Tracer slice is proven: test harness foundation is operational, Section 1 symlink and packaging integrity checks pass, and syntax validation succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Status bar clock pill horizontal padding in ClockWidget.qml (CLOCK-01, D-01, D-02)</name>
  <read_first>
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/Resources.qml
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml
    - .planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-RESEARCH.md
    - .planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-PATTERNS.md
  </read_first>
  <files>restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml, scripts/phase40.1-clock-volume-assert.sh</files>
  <action>
    Adjust the geometry of `restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml` to enforce 10px horizontal breathing room while preserving second precision and the 8px non-glyph spacer (per CLOCK-01, D-01, D-02):

    1. In `restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml`:
       - Replace `implicitWidth: rowLayout.implicitWidth` with:
         `implicitWidth: rowLayout.implicitWidth + rowLayout.anchors.leftMargin + rowLayout.anchors.rightMargin` (matching the `Resources.qml` pattern).
       - In `RowLayout` (`id: rowLayout`), replace `anchors.centerIn: parent` with:
         `anchors.fill: parent`
         `anchors.leftMargin: 5`
         `anchors.rightMargin: 5`
       - This 5px left/right margin on `RowLayout` combines with `BarGroup.qml`'s container `padding: 5` to create exactly `5 + 5 = 10px` total horizontal breathing room on each side, perfectly matching the 10px left sidebar button and ~9px Resources pill (D-01).
       - Verify the 8px non-glyph spacer `Item { visible: root.showDate; implicitWidth: 8; implicitHeight: 1 }` between time and date strings remains intact (D-02).
       - Verify `DateTime.time` (second precision) and `DateTime.longDate` formatting remain intact.
       - Reversibility: reversible by adjusting margin values.

    2. Update Section 2 in `scripts/phase40.1-clock-volume-assert.sh`:
       - Assert `ClockWidget.qml` defines `anchors.leftMargin: 5` and `anchors.rightMargin: 5`.
       - Assert `ClockWidget.qml` defines `implicitWidth: rowLayout.implicitWidth + rowLayout.anchors.leftMargin + rowLayout.anchors.rightMargin`.
       - Assert `ClockWidget.qml` contains the 8px spacer `implicitWidth: 8`.
       - Assert `ClockWidget.qml` references `DateTime.time` and `DateTime.longDate`.
  </action>
  <verify>
    <automated>./scripts/phase40.1-clock-volume-assert.sh --section 2</automated>
    <fails_when>non-zero exit code or Section 2 geometry assertions fail</fails_when>
    <automated>grep -q "anchors.leftMargin: 5" restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml &amp;&amp; grep -q "anchors.rightMargin: 5" restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml</automated>
    <fails_when>exit code 1 or missing 5px margins in ClockWidget.qml</fails_when>
    <automated>grep -q "implicitWidth: 8" restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml</automated>
    <fails_when>exit code 1 or missing 8px non-glyph spacer</fails_when>
  </verify>
  <acceptance_criteria>
    - `ClockWidget.qml` specifies `anchors.fill: parent`, `anchors.leftMargin: 5`, `anchors.rightMargin: 5`, and dynamic `implicitWidth` calculation.
    - Combined horizontal breathing room inside `BarGroup` equals 10px on each side.
    - 8px non-glyph spacer between time and date strings is preserved.
    - Second precision (`DateTime.time`) and full date (`DateTime.longDate`) are preserved.
    - Section 2 of `scripts/phase40.1-clock-volume-assert.sh` passes cleanly.
  </acceptance_criteria>
  <done>ClockWidget horizontal breathing room is expanded to 10px, non-glyph spacer is preserved, and Section 2 verification passes.</done>
</task>

<task type="auto">
  <name>Task 3: Single source of truth volume ceiling across config.json, keybinds.lua, Config.qml, and Audio.qml (VOL-01, D-03, D-04, D-05)</name>
  <read_first>
    - capture/ii/.config/illogical-impulse/config.json
    - stow/hypr/.config/hypr/custom/keybinds.lua
    - vendor/dots-hyprland/dots/.config/hypr/hyprland/keybinds.lua
    - vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/Config.qml
    - vendor/dots-hyprland/dots/.config/quickshell/ii/services/Audio.qml
    - .planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-RESEARCH.md
    - .planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-PATTERNS.md
  </read_first>
  <files>capture/ii/.config/illogical-impulse/config.json, stow/hypr/.config/hypr/custom/keybinds.lua, restow/quickshell/.config/quickshell/ii/modules/common/Config.qml, restow/quickshell/.config/quickshell/ii/services/Audio.qml, scripts/phase40.1-clock-volume-assert.sh</files>
  <action>
    Establish the canonical single source of truth for the desktop-wide 150% (1.5) volume ceiling across configuration, keybinds, schema adapter, and audio service (per VOL-01, D-03, D-04, D-05, T-40.1-02):

    1. Update `capture/ii/.config/illogical-impulse/config.json`:
       - Under `"audio"`, add `"volumeCeiling": 1.5` alongside the existing `"protection"` object (D-03).
       - Sync the updated file to the live path `~/.config/illogical-impulse/config.json`.
       - Reversibility gate: costly — downstream components (Hyprland keybinds and Quickshell) consume this schema path.

    2. Update `stow/hypr/.config/hypr/custom/keybinds.lua`:
       - In the unbinds section, unbind upstream `XF86AudioRaiseVolume`:
         `hl.unbind("XF86AudioRaiseVolume")` (D-04).
       - Near the top of the file, define the pure Lua startup configuration parser:
         ```lua
         local function get_volume_ceiling()
             local default_ceiling = "1.5"
             local config_path = HOME .. "/.config/illogical-impulse/config.json"
             local f = io.open(config_path, "r")
             if not f then return default_ceiling end
             local content = f:read("*a")
             f:close()
             if not content then return default_ceiling end
             local ceiling = content:match('"volumeCeiling"%s*:%s*([%d%.]+)')
             if ceiling and tonumber(ceiling) and tonumber(ceiling) > 0 then
                 return ceiling
             end
             return default_ceiling
         end
         local volume_ceiling = get_volume_ceiling()
         ```
       - Bind `XF86AudioRaiseVolume` using the resolved `volume_ceiling`:
         `hl.bind("XF86AudioRaiseVolume", hl.dsp.exec_cmd("wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+ -l " .. volume_ceiling), { locked = true, repeating = true, description = "Audio: Raise volume" })` (D-04).
       - Zero subprocess forks (`jq` or subshells) are spawned during key repeats, preventing latency and audio stutter (T-40.1-02).
       - Reversibility: reversible.

    3. Author `restow/quickshell/.config/quickshell/ii/modules/common/Config.qml`:
       - Create parent directory `restow/quickshell/.config/quickshell/ii/modules/common/` if needed.
       - Base on upstream `vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/Config.qml`.
       - Under `property JsonObject audio: JsonObject {`, declare:
         `property real volumeCeiling: 1.5` (D-05).
       - Pitfall prevention: Quickshell's C++ `JsonAdapter` requires explicit property declaration to deserialize `volumeCeiling` from `config.json`.
       - Deploy via GNU Stow: if `~/.config/quickshell/ii/modules/common/Config.qml` exists as a regular file, back it up to `Config.qml.bak` before stowing. Run `stow --verbose=5 --no-folding -d restow -t ~ quickshell`.
       - Reversibility gate: costly — standardizes volume ceiling contract across Quickshell QML components.

    4. Author initial `restow/quickshell/.config/quickshell/ii/services/Audio.qml`:
       - Create parent directory `restow/quickshell/.config/quickshell/ii/services/` if needed.
       - Base on upstream `vendor/dots-hyprland/dots/.config/quickshell/ii/services/Audio.qml`.
       - On `root`, declare:
         `readonly property real maxVolume: Config.options?.audio?.volumeCeiling ?? 1.5` (D-05).
       - Deploy via GNU Stow: if `~/.config/quickshell/ii/services/Audio.qml` exists as a regular file, back it up to `Audio.qml.bak` before stowing. Run `stow --verbose=5 --no-folding -d restow -t ~ quickshell`.
       - Reversibility: costly — standardizes volume ceiling contract across Quickshell QML components.

    5. Update Section 3 in `scripts/phase40.1-clock-volume-assert.sh`:
       - Assert `capture/ii/.../config.json` contains `"volumeCeiling": 1.5` under `"audio"`.
       - Assert `~/.config/illogical-impulse/config.json` contains `"volumeCeiling": 1.5`.
       - Assert `keybinds.lua` unbinds `XF86AudioRaiseVolume` and binds `wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+ -l` with dynamic ceiling.
       - Assert pure Lua test execution evaluates `volume_ceiling` to `"1.5"`.
       - Assert `Config.qml` contains `property real volumeCeiling: 1.5`.
       - Assert `Audio.qml` contains `readonly property real maxVolume: Config.options?.audio?.volumeCeiling ?? 1.5`.
  </action>
  <verify>
    <automated>./scripts/phase40.1-clock-volume-assert.sh --section 3</automated>
    <fails_when>non-zero exit code or Section 3 contract assertions fail</fails_when>
    <automated>jq -e '.audio.volumeCeiling == 1.5' capture/ii/.config/illogical-impulse/config.json &amp;&amp; jq -e '.audio.volumeCeiling == 1.5' "$HOME/.config/illogical-impulse/config.json"</automated>
    <fails_when>exit code 1 or volumeCeiling is not 1.5 in config.json</fails_when>
    <automated>grep -q "XF86AudioRaiseVolume" stow/hypr/.config/hypr/custom/keybinds.lua &amp;&amp; grep -q "wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+ -l " stow/hypr/.config/hypr/custom/keybinds.lua</automated>
    <fails_when>exit code 1 or missing dynamic volume keybinding</fails_when>
    <automated>grep -q "property real volumeCeiling: 1.5" restow/quickshell/.config/quickshell/ii/modules/common/Config.qml &amp;&amp; grep -q "readonly property real maxVolume:" restow/quickshell/.config/quickshell/ii/services/Audio.qml</automated>
    <fails_when>exit code 1 or missing QML volume ceiling properties</fails_when>
  </verify>
  <acceptance_criteria>
    - `config.json` in repository and live `$HOME` declares `"volumeCeiling": 1.5` under `"audio"`.
    - `keybinds.lua` parses `config.json` in Lua without subprocess overhead and binds `XF86AudioRaiseVolume` with `-l <ceiling>`.
    - `Config.qml` overlay declares `property real volumeCeiling: 1.5` on `JsonObject audio`.
    - `Audio.qml` overlay declares `readonly property real maxVolume: Config.options?.audio?.volumeCeiling ?? 1.5`.
    - Live symlinks for `Config.qml` and `Audio.qml` resolve into `restow/quickshell/` without folding or submodule drift.
    - Section 3 of `scripts/phase40.1-clock-volume-assert.sh` passes cleanly.
  </acceptance_criteria>
  <done>Single source of truth volume ceiling contract is deployed across config.json, Hyprland keybinds, Config.qml, and Audio.qml, with Section 3 verification passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File Configuration Parser | Lua evaluation in `keybinds.lua` reading user-writable `~/.config/illogical-impulse/config.json`. |
| Subprocess Execution | Hyprland keybinding dispatch executing `wpctl` with arguments. |
| Filesystem & Stow Deployment | Leaf symlinks deployed by GNU Stow from `restow/quickshell/` and `stow/hypr/` to `$HOME/.config/`. |
| Git Submodule Boundary | Pristine `vendor/dots-hyprland` tree tracked as git submodule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-01 | Tampering | Git submodule `vendor/dots-hyprland` | high | mitigate | Author all QML modifications exclusively in `restow/quickshell/` and Hyprland bindings in `stow/hypr/`. Assert zero submodule drift via `arch/dots-hyprland.sh verify --strict` (INTG-01, INTG-03). |
| T-40.1-02 | Elevation of Privilege / Command Injection | Lua `config.json` parser in `keybinds.lua` | medium | mitigate | Use strict regex pattern `[%d%.]+` and `tonumber(ceiling) > 0` validation before concatenating into the `wpctl` command string, falling back to safe `"1.5"` on any invalid input (D-04). |
| T-40.1-03 | Denial of Service / Acoustic Shock | PipeWire audio output | medium | mitigate | Enforce volume ceiling limit at 1.5 (150%) across WirePlumber (`-l <ceiling>`) and Quickshell (`Audio.maxVolume`). Preserve `hardMaxValue: 2.00` safety clamp in `Audio.qml`. |
| T-40.1-04 | Working-Tree Churn | Test harness execution | low | mitigate | Test harness uses isolated temporary files in `/tmp` tracked via `TMP_FILES` with `trap cleanup EXIT`. Compare `git status --porcelain` before and after execution to guarantee zero drift. |
</threat_model>

## Artifacts this phase produces

- `scripts/phase40.1-clock-volume-assert.sh`: Automated 5-section test harness validating symlinks, static AST properties, ceiling contract, volume ergonomics, and strict repository verification.
- `restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml`: Status bar clock widget enforcing 10px horizontal breathing room and 8px non-glyph spacer.
- `capture/ii/.config/illogical-impulse/config.json`: Canonical desktop configuration defining `"volumeCeiling": 1.5` under `"audio"`.
- `stow/hypr/.config/hypr/custom/keybinds.lua`: Hyprland keybinding overlay parsing `config.json` via pure Lua and binding dynamic volume raise.
- `restow/quickshell/.config/quickshell/ii/modules/common/Config.qml`: Quickshell schema adapter declaring `property real volumeCeiling: 1.5` for reactive binding.
- `restow/quickshell/.config/quickshell/ii/services/Audio.qml`: Quickshell PipeWire audio service exposing centralized `readonly property real maxVolume`.

## Multi-Source Coverage Audit

| Source | Item ID / Reference | Description | Status | Covered By | Notes |
|---|---|---|---|---|---|
| REQ | CLOCK-01 | Top status bar clock/date pill provides comfortable horizontal padding matching adjacent pills while retaining custom second precision and full date string formatting. | COVERED | Task 2 | 5px RowLayout margins + 5px BarGroup padding = 10px horizontal breathing room, retaining DateTime.time and DateTime.longDate. |
| REQ | VOL-01 | Single source of truth defines the desktop-wide volume ceiling (150% / 1.5) consumed consistently across Hyprland keybinds and Quickshell services without hardcoded magic numbers. | COVERED | Task 3 | Added volumeCeiling: 1.5 to config.json, parsed in keybinds.lua, exposed in Config.qml and Audio.maxVolume. |
| CONTEXT | D-01 | 10px horizontal breathing room in ClockWidget.qml via 5px margins on rowLayout + 5px BarGroup padding. | COVERED | Task 2 | RowLayout anchors.leftMargin: 5 and anchors.rightMargin: 5. |
| CONTEXT | D-02 | Retain 8px non-glyph spacer between time and date strings. | COVERED | Task 2 | Item with implicitWidth: 8 preserved. |
| CONTEXT | D-03 | volumeCeiling: 1.5 in config.json under "audio": {"volumeCeiling": 1.5}. | COVERED | Task 3 | Synced to repo and live $HOME. |
| CONTEXT | D-04 | keybinds.lua unbinds upstream XF86AudioRaiseVolume, parses config.json in Lua, binds wpctl -l <ceiling>. | COVERED | Task 3 | Pure Lua io.open and pattern match with zero key repeat forks. |
| CONTEXT | D-05 | Audio.qml exposes maxVolume; Config.qml declares property real volumeCeiling: 1.5. | COVERED | Task 3 | Deployed as restow overlays. |
| RESEARCH | T-40.1-01 | Submodule tampering prevention. | COVERED | Task 1, Task 2, Task 3 | Zero edits to vendor/dots-hyprland; leaf symlinks only. |
| RESEARCH | T-40.1-02 | Command injection prevention in Lua config parser. | COVERED | Task 3 | tonumber() and regex validation before exec_cmd. |
| RESEARCH | T-40.1-04 | Working-tree churn prevention. | COVERED | Task 1 | Porcelain snapshots before and after assert execution. |

<verification>
Execute the tracer and contract test suites:
`./scripts/phase40.1-clock-volume-assert.sh --section 1`
`./scripts/phase40.1-clock-volume-assert.sh --section 2`
`./scripts/phase40.1-clock-volume-assert.sh --section 3`
Verify static syntax:
`./scripts/phase40.1-clock-volume-assert.sh --syntax`
</verification>

<success_criteria>
1. `scripts/phase40.1-clock-volume-assert.sh` exists, is executable, and runs Sections 1, 2, 3, and `--syntax` with zero failures.
2. `ClockWidget.qml` provides 10px horizontal breathing room, retaining the 8px spacer and date/time formatting.
3. `capture/ii/.config/illogical-impulse/config.json` and live `$HOME` declare `"volumeCeiling": 1.5`.
4. `keybinds.lua` resolves `volume_ceiling` via Lua and binds `XF86AudioRaiseVolume` with `-l <ceiling>`.
5. `Config.qml` and `Audio.qml` restow overlays are active and expose `Audio.maxVolume`.
6. `vendor/dots-hyprland` submodule remains 100% pristine with zero git diff.
</success_criteria>

<output>
Create `.planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-01-SUMMARY.md` when done
</output>
