---
phase: 38-power-profiles-daemon-system-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - arch/pkglist-native.txt
  - bootstrap.sh
  - scripts/phase38-power-profiles-assert.sh
autonomous: true
requirements:
  - POWER-01
  - POWER-02
  - POWER-03

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "power-profiles-daemon is installed on Arch Linux and power-profiles-daemon.service is actively running and enabled at system level (POWER-01, D-06)."
    - "Quickshell's existing upstream quick-toggle (PowerProfilesToggle.qml) cycles Power Saver -> Balanced -> Performance via native Quickshell.Services.UPower talking to net.hadess.PowerProfiles, updating button icon and label without custom OSDs (POWER-02, D-01, D-02, D-03)."
    - "Package manifests (arch/pkglist-native.txt) and ./bootstrap.sh are updated to ensure idempotent installation and service activation on fresh machines (POWER-03, D-04, D-05, D-06)."
    - "Systemd unit power-profiles-daemon.service is active and enabled; if the daemon is stopped or restarted, Quickshell's UPower service reconnects to D-Bus interface net.hadess.PowerProfiles upon daemon availability without crashing the shell session (POWER-01 edge coverage)."
    - "Repeated clicks or rapid toggling across power profiles transition deterministically through the 3-state cycle (Power Saver -> Balanced -> Performance) without race conditions, and setting the active profile to its current value is an idempotent no-op (POWER-02 idempotency edge coverage, D-02)."
    - "Concurrent profile change requests via Quickshell UI and external CLI (powerprofilesctl set) are serialized atomically by power-profiles-daemon over system D-Bus, and Quickshell's property bindings reactively synchronize state changes from external triggers without desynchronization (POWER-02 concurrency edge coverage, D-02, D-03)."
    - "bootstrap.sh step_packages execution is strictly idempotent: running repeatedly on an already-configured machine performs zero-overhead status checks with zero pacman reinstalls or redundant systemd mutations (POWER-03 idempotency edge coverage, D-05, D-06)."
    - "Repository verification via arch/dots-hyprland.sh verify --strict exits 0 with 0 findings and zero git working tree churn, maintaining strict focus on symlinks and git repository drift with zero runtime coupling (POWER-03, D-07)."
  artifacts:
    - path: "arch/pkglist-native.txt"
      provides: "Authoritative native Arch Linux package manifest including power-profiles-daemon in alphabetical order"
      contains: "power-profiles-daemon"
    - path: "bootstrap.sh"
      provides: "Root bootstrap orchestrator with idempotent power-profiles-daemon check and activation"
      contains: "power-profiles-daemon"
    - path: "scripts/phase38-power-profiles-assert.sh"
      provides: "5-section automated assertion test harness validating package, service, D-Bus, bootstrap idempotency, and upstream zero-override invariant"
      contains: "phase38-power-profiles-assert"
  key_links:
    - from: "bootstrap.sh"
      to: "arch/pkglist-native.txt"
      via: "package presence in step_packages matching manifest entry"
      pattern: "pacman -Q power-profiles-daemon"
    - from: "vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/models/quickToggles/PowerProfilesToggle.qml"
      to: "net.hadess.PowerProfiles"
      via: "Quickshell.Services.UPower D-Bus client binding"
      pattern: "Quickshell\\.Services\\.UPower"
    - from: "scripts/phase38-power-profiles-assert.sh"
      to: "arch/dots-hyprland.sh"
      via: "strict repository verification gate invocation"
      pattern: "\\./arch/dots-hyprland\\.sh verify --strict"
---

<objective>
Install and activate `power-profiles-daemon` on Arch Linux, record it in repository package manifests (`arch/pkglist-native.txt`, `bootstrap.sh`), preserve strict zero-override upstream QML parity (D-01), and establish an automated 5-section assertion test harness (`scripts/phase38-power-profiles-assert.sh`) verifying live D-Bus `net.hadess.PowerProfiles` communication, Quickshell UPower service binding, and repository integrity via `arch/dots-hyprland.sh verify --strict`.

Purpose: Enable system-level power management profile switching (Power Saver, Balanced, Performance) and wire it seamlessly into Quickshell's existing upstream sidebar toggle without local QML drift or maintenance overhead.
Output: Active system service `power-profiles-daemon.service`, updated `arch/pkglist-native.txt`, hardened `bootstrap.sh`, and executable test harness `scripts/phase38-power-profiles-assert.sh`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-power-profiles-daemon-system-integration/38-CONTEXT.md
@.planning/phases/38-power-profiles-daemon-system-integration/38-PATTERNS.md
@arch/pkglist-native.txt
@bootstrap.sh
@arch/dots-hyprland.sh
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/models/quickToggles/PowerProfilesToggle.qml
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/sidebarRight/quickToggles/androidStyle/AndroidPowerProfileToggle.qml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — Power Profiles Daemon installation, service activation, and D-Bus / Quickshell UPower end-to-end verification</name>
  <read_first>
    - .planning/phases/38-power-profiles-daemon-system-integration/38-CONTEXT.md
    - .planning/phases/38-power-profiles-daemon-system-integration/38-PATTERNS.md
    - vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/models/quickToggles/PowerProfilesToggle.qml
    - scripts/phase37-voice-pill-assert.sh
  </read_first>
  <files>scripts/phase38-power-profiles-assert.sh</files>
  <action>
    Establish the phase test harness scaffold at `scripts/phase38-power-profiles-assert.sh` and execute the end-to-end tracer slice proving the complete power profile control stack from OS daemon to live Quickshell UPower integration (per D-02, D-03, D-08, POWER-01, POWER-02).

    1. Install `power-profiles-daemon` via `sudo pacman -S --needed --noconfirm power-profiles-daemon`.
    2. Enable and start `power-profiles-daemon.service` via `sudo systemctl enable --now power-profiles-daemon.service`.
    3. Verify system daemon and D-Bus responsiveness:
       - Run `systemctl is-active power-profiles-daemon.service` and assert output is `active`.
       - Run `busctl status net.hadess.PowerProfiles` and assert system service is responding.
       - Run `powerprofilesctl list` and assert available profiles include `power-saver`, `balanced`, and `performance` (if supported by hardware/kernel).
    4. Construct the initial scaffold of `scripts/phase38-power-profiles-assert.sh`:
       - Model after `scripts/phase37-voice-pill-assert.sh` with bash strict mode (`set -euo pipefail`), non-root guard (`EUID != 0`), CLI argument parsing for `--section <1-5>` (`-s <1-5>`) and `--syntax` (`-c`), cleanup trap for temporary files, and git porcelain snapshot tracking (`PORCELAIN_BEFORE` and `PORCELAIN_AFTER`).
       - Implement Section 1 (Package Manifest & Installation Verification):
         * Assert `pacman -Q power-profiles-daemon` exits 0.
       - Implement Section 2 (Systemd Service & D-Bus Bus Verification):
         * Assert `systemctl is-active --quiet power-profiles-daemon.service` exits 0.
         * Assert `busctl status net.hadess.PowerProfiles` exits 0.
         * Assert `powerprofilesctl list` exits 0 and outputs profile details.
    5. Ensure `scripts/phase38-power-profiles-assert.sh` is executable (`chmod +x scripts/phase38-power-profiles-assert.sh`).
  </action>
  <verify>
    <automated>systemctl is-active power-profiles-daemon.service &amp;&amp; busctl status net.hadess.PowerProfiles &amp;&amp; powerprofilesctl list</automated>
    <fails_when>non-zero exit code, inactive output from systemctl, or busctl error indicating service not found</fails_when>
    <automated>./scripts/phase38-power-profiles-assert.sh --section 1 &amp;&amp; ./scripts/phase38-power-profiles-assert.sh --section 2</automated>
    <fails_when>non-zero exit code or output containing [FAIL]</fails_when>
  </verify>
  <acceptance_criteria>
    - `power-profiles-daemon` package is installed on the system (`pacman -Q power-profiles-daemon` exits 0).
    - `power-profiles-daemon.service` is active (`systemctl is-active power-profiles-daemon.service` outputs `active`).
    - D-Bus destination `net.hadess.PowerProfiles` is reachable on the system bus (`busctl status net.hadess.PowerProfiles` exits 0).
    - `powerprofilesctl list` outputs profile listings successfully without error.
    - `scripts/phase38-power-profiles-assert.sh` exists, has executable permissions, and passes Section 1 and Section 2.
  </acceptance_criteria>
  <done>The tracer slice is proven: power-profiles-daemon is active, running, communicating on system D-Bus, and passing initial assertion checks.</done>
</task>

<task type="auto">
  <name>Task 2: Repository manifest tracking in pkglist-native.txt and idempotent bootstrap.sh integration</name>
  <read_first>
    - arch/pkglist-native.txt
    - bootstrap.sh
    - .planning/phases/38-power-profiles-daemon-system-integration/38-PATTERNS.md
    - .planning/phases/38-power-profiles-daemon-system-integration/38-CONTEXT.md
  </read_first>
  <files>arch/pkglist-native.txt, bootstrap.sh</files>
  <action>
    Record `power-profiles-daemon` into the authoritative native package manifest and automate its idempotent installation and activation in the root bootstrap orchestrator (per D-04, D-05, D-06, D-07, POWER-03).

    1. In `arch/pkglist-native.txt`:
       - Insert `power-profiles-daemon` in strict alphabetical order (`LC_ALL=C` collation) directly between `playerctl` and `python` (per D-04).
       - Maintain header metadata comment invariants.
    2. In `bootstrap.sh`:
       - In `step_packages()`, within the `DRY_RUN -eq 1` guard, add dry-run preview messaging: `echo "[DRY-RUN] Checking and activating power-profiles-daemon..."` (per D-05, D-06).
       - In `step_packages()`, after prerequisite checks, add an idempotent package presence check using `pacman -Q power-profiles-daemon &amp;&gt;/dev/null`. If missing, output `[INFO] power-profiles-daemon not installed; installing via pacman...` and install via `sudo pacman -S --needed --noconfirm power-profiles-daemon` (per D-05).
       - In `step_packages()`, add idempotent systemd service enablement and activation: check if `command -v systemctl &amp;&gt;/dev/null`. If present, check `if ! systemctl is-active --quiet power-profiles-daemon.service 2&gt;/dev/null; then echo "[CONFIG] Enabling and starting power-profiles-daemon.service..."; sudo systemctl enable --now power-profiles-daemon.service; else echo "[PASS] power-profiles-daemon.service is already active."; fi` (per D-06).
       - Ensure non-root operator invariant (`CURRENT_EUID != 0`) and sudo elevation pattern are strictly maintained.
       - Preserve `arch/dots-hyprland.sh verify --strict` invariant: ensure no runtime service checks or daemon dependencies are added to `arch/dots-hyprland.sh` verify subcommand (per D-07).
  </action>
  <verify>
    <automated>LC_ALL=C sort -c &lt;(grep -v '^#' arch/pkglist-native.txt)</automated>
    <fails_when>non-zero exit code indicating unsorted lines in arch/pkglist-native.txt</fails_when>
    <automated>grep -x 'power-profiles-daemon' arch/pkglist-native.txt</automated>
    <fails_when>exit code 1 or empty output indicating power-profiles-daemon is missing</fails_when>
    <automated>bash -n bootstrap.sh</automated>
    <fails_when>non-zero exit code indicating bash syntax error</fails_when>
    <automated>./bootstrap.sh --dry-run | grep 'power-profiles-daemon'</automated>
    <fails_when>exit code 1 or empty output indicating dry-run branch does not report power-profiles-daemon</fails_when>
  </verify>
  <acceptance_criteria>
    - `arch/pkglist-native.txt` contains `power-profiles-daemon` strictly sorted in `LC_ALL=C` alphabetical order between `playerctl` and `python`.
    - `LC_ALL=C sort -c` over non-comment lines in `arch/pkglist-native.txt` succeeds with exit 0.
    - `bootstrap.sh` passes `bash -n` syntax check.
    - `./bootstrap.sh --dry-run` displays power-profiles-daemon preview messages and exits 0.
    - `bootstrap.sh` performs idempotent checks before invoking `sudo pacman` or `sudo systemctl`.
  </acceptance_criteria>
  <done>Package manifest tracking and idempotent bootstrap orchestration are integrated and verified against syntax, sort, and dry-run gates.</done>
</task>

<task type="auto">
  <name>Task 3: Complete 5-section assertion test harness, zero-override invariant, and strict repository verification</name>
  <read_first>
    - scripts/phase38-power-profiles-assert.sh
    - scripts/phase37-voice-pill-assert.sh
    - vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/models/quickToggles/PowerProfilesToggle.qml
    - vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/sidebarRight/quickToggles/androidStyle/AndroidPowerProfileToggle.qml
    - .planning/phases/38-power-profiles-daemon-system-integration/38-CONTEXT.md
    - .planning/phases/38-power-profiles-daemon-system-integration/38-PATTERNS.md
  </read_first>
  <files>scripts/phase38-power-profiles-assert.sh</files>
  <action>
    Complete `scripts/phase38-power-profiles-assert.sh` across all 5 verification sections, assert zero-override upstream QML parity (per D-01), test live Quickshell UPower binding, and run strict repository verification (per D-01 through D-08, POWER-01, POWER-02, POWER-03).

    1. In `scripts/phase38-power-profiles-assert.sh`:
       - Finalize Section 1 (Package Manifest & Sorting):
         * Assert `pacman -Q power-profiles-daemon` succeeds.
         * Assert `power-profiles-daemon` is present in `arch/pkglist-native.txt`.
         * Assert `arch/pkglist-native.txt` non-comment lines satisfy `LC_ALL=C sort -c`.
       - Finalize Section 2 (Systemd Service & D-Bus Interface):
         * Assert `systemctl is-active --quiet power-profiles-daemon.service` succeeds.
         * Assert `systemctl is-enabled --quiet power-profiles-daemon.service` succeeds.
         * Assert `busctl status net.hadess.PowerProfiles` succeeds.
         * Assert `powerprofilesctl list` succeeds.
       - Implement Section 3 (Upstream Parity & Zero Local QML Overrides):
         * Assert that no local override file exists under `restow/quickshell/.config/quickshell/ii/modules/common/models/quickToggles/PowerProfilesToggle.qml` (per D-01).
         * Assert that no local override file exists under `restow/quickshell/.config/quickshell/ii/modules/ii/sidebarRight/quickToggles/androidStyle/AndroidPowerProfileToggle.qml` (per D-01).
         * Assert that upstream `PowerProfilesToggle.qml` uses `Quickshell.Services.UPower` and defines cycling across `PowerProfile.PowerSaver`, `PowerProfile.Balanced`, and `PowerProfile.Performance` (per D-02).
       - Implement Section 4 (Bootstrap Static Analysis & Idempotency):
         * Assert that `bootstrap.sh` contains the idempotent check `pacman -Q power-profiles-daemon` inside `step_packages` (per D-05).
         * Assert that `bootstrap.sh` contains `systemctl is-active --quiet power-profiles-daemon.service` before invoking `systemctl enable --now` (per D-06).
         * Assert that `./bootstrap.sh --dry-run` reports power-profiles-daemon checking without mutating system state.
       - Implement Section 5 (Headless Quickshell UPower Integration & Repository Integrity):
         * Define `run_qs_test()` running headless Quickshell (`quickshell -p`) on a temporary QML test file.
         * Execute a QML test snippet importing `QtQuick`, `Quickshell`, and `Quickshell.Services.UPower`, binding to `PowerProfiles.profile`, reading the active profile string, and exiting with return code 0 when `PowerProfiles` object resolves cleanly.
         * Assert that `./arch/dots-hyprland.sh verify --strict` exits 0 with `0 findings`.
       - Verify that the git porcelain bracket (`PORCELAIN_BEFORE` / `PORCELAIN_AFTER`) detects zero untracked or modified repository files created during test execution.
    2. Make `scripts/phase38-power-profiles-assert.sh` executable and run the full suite.
  </action>
  <verify>
    <automated>./scripts/phase38-power-profiles-assert.sh</automated>
    <fails_when>non-zero exit code, output containing [FAIL], or findings count greater than 0</fails_when>
    <automated>./arch/dots-hyprland.sh verify --strict</automated>
    <fails_when>non-zero exit code or output containing [FAIL] or non-zero findings</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/phase38-power-profiles-assert.sh` runs all 5 sections cleanly and outputs `[PASS]` for all assertions with `FAIL=0 FINDINGS=0`.
    - No QML override files exist for `PowerProfilesToggle.qml` or `AndroidPowerProfileToggle.qml` in `restow/quickshell/` (D-01 verified).
    - `PowerProfilesToggle.qml` upstream model imports `Quickshell.Services.UPower` and defines 3-state cycling (D-02 verified).
    - Headless Quickshell snippet successfully instantiates `PowerProfiles` from `Quickshell.Services.UPower`.
    - `./arch/dots-hyprland.sh verify --strict` passes with 0 findings and zero git working tree churn.
  </acceptance_criteria>
  <done>All 5 assertion sections pass, upstream zero-drift parity is confirmed, and repository integrity is verified strictly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell UI → System D-Bus (`net.hadess.PowerProfiles`) | Quickshell running in unprivileged user desktop session queries and invokes profile switching on system-level `power-profiles-daemon` over the system D-Bus. |
| Bootstrap Script → System Management (`pacman` / `systemctl`) | Non-root operator running `./bootstrap.sh` elevates privileges via `sudo` on-demand to install OS packages and enable systemd units. |
| Package Management → Upstream Arch Repositories | `pacman` downloads official Arch Linux binary packages and verifies PGP signatures against Arch Linux keyring. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-01 | Elevation of Privilege | `bootstrap.sh` `sudo systemctl` / `sudo pacman` | medium | mitigate | Perform non-root idempotent pre-checks (`pacman -Q`, `systemctl is-active`) before invoking `sudo` to minimize root privilege exposure; scope service commands strictly to `enable --now power-profiles-daemon.service` (per D-06). |
| T-38-02 | Denial of Service | `PowerProfilesToggle.qml` rapid clicking / D-Bus spam | low | mitigate | Upstream toggle uses synchronous property binding to `Quickshell.Services.UPower.PowerProfiles`; `power-profiles-daemon` serializes D-Bus requests internally without queue exhaustion. |
| T-38-03 | Tampering | Package manifest `arch/pkglist-native.txt` injection | medium | mitigate | Enforce `LC_ALL=C` alphabetical sorting and pacman database package integrity; verify `pacman -Q power-profiles-daemon` in automated test harness (per D-04). |
| T-38-SC | Tampering | Arch Linux pacman package repository | high | mitigate | Install only standard official Arch Linux repository package `power-profiles-daemon` verified with pacman PGP keyring signatures; avoid untrusted third-party AUR wrappers for core power management. |
</threat_model>

## Artifacts this phase produces

- `arch/pkglist-native.txt`: Tracked native package manifest with `power-profiles-daemon` inserted in sorted alphabetical order.
- `bootstrap.sh`: Root bootstrap orchestrator with idempotent `power-profiles-daemon` package checking and service activation in `step_packages`.
- `scripts/phase38-power-profiles-assert.sh`: Executable 5-section test harness asserting package installation, systemd service and D-Bus status, zero QML overrides, bootstrap idempotency, and live Quickshell UPower integration.

## Multi-Source Coverage Audit

| Source | Item ID / Reference | Description | Status | Covered By | Notes |
|---|---|---|---|---|---|
| GOAL | ROADMAP §Phase 38 | Establish system-level power profile switching support on Arch Linux and connect it to Quickshell's power profile quick-toggle. | COVERED | Task 1, Task 2, Task 3 | Full stack established from OS daemon to Quickshell UPower toggle. |
| REQ | POWER-01 | `power-profiles-daemon` package installed and `power-profiles-daemon.service` enabled on Arch Linux. | COVERED | Task 1, Task 3 | Package installed, service enabled, verified via systemctl and assert harness Section 2. |
| REQ | POWER-02 | Quickshell power profile quick-toggle (`PowerProfilesToggle.qml`) cycles through profiles (Power Saver, Balanced, Performance) with matching icons and live state feedback. | COVERED | Task 1, Task 3 | Verified via upstream toggle QML analysis, UPower D-Bus communication, and headless runner Section 5. |
| REQ | POWER-03 | Package manifests (`arch/pkglist-native.txt`, `dots-hyprland.sh`, `bootstrap.sh`) updated to guarantee `power-profiles-daemon` on fresh machine installs. | COVERED | Task 2, Task 3 | Added to `pkglist-native.txt`, automated in `bootstrap.sh` `step_packages`, verified with zero drift in `dots-hyprland.sh verify --strict`. |
| RESEARCH | PATTERNS §1 | `arch/pkglist-native.txt` alphabetical insertion between `playerctl` and `python`. | COVERED | Task 2, Task 3 | `LC_ALL=C` sorting verified. |
| RESEARCH | PATTERNS §2 | `bootstrap.sh` `step_packages` dry-run, idempotent pacman check, and idempotent systemctl activation. | COVERED | Task 2, Task 3 | Non-root check with sudo elevation on demand. |
| RESEARCH | PATTERNS §3 | `scripts/phase38-power-profiles-assert.sh` 5-section assert harness with porcelain bracket and headless runner. | COVERED | Task 1, Task 3 | Built and executed with FAIL=0 FINDINGS=0. |
| CONTEXT | D-01 | Strict Upstream Parity — Zero QML Modifications in `restow/quickshell/`. | COVERED | Task 3 | Section 3 explicitly asserts zero local QML overrides exist. |
| CONTEXT | D-02 | Native Upstream 3-State Cycle (Power Saver -> Balanced -> Performance) via `Quickshell.Services.UPower`. | COVERED | Task 1, Task 3 | Tested via D-Bus interface and headless Quickshell instantiation. |
| CONTEXT | D-03 | Sidebar-Only Visual Feedback (icon + status text updates; no popups or OSDs). | COVERED | Task 1, Task 3 | Upstream `PowerProfilesToggle.qml` visual bindings confirmed. |
| CONTEXT | D-04 | Package Manifest Tracking in `arch/pkglist-native.txt`. | COVERED | Task 2, Task 3 | Added alphabetically and validated via `sort -c`. |
| CONTEXT | D-05 | Bootstrap Integration in `step_packages`. | COVERED | Task 2, Task 3 | Automated package verification and install in `bootstrap.sh`. |
| CONTEXT | D-06 | Idempotent Systemd Service Activation before sudo. | COVERED | Task 2, Task 3 | `systemctl is-active --quiet` pre-check implemented in `bootstrap.sh`. |
| CONTEXT | D-07 | Repository Verification Invariant: `dots-hyprland.sh verify --strict` maintains focus on symlinks/drift without runtime coupling. | COVERED | Task 2, Task 3 | `dots-hyprland.sh` left unaltered; verified to exit 0 with 0 findings. |
| CONTEXT | D-08 | Default Upstream Policy: manual switching via UI/CLI; standard daemon state persistence across reboots. | COVERED | Task 1, Task 3 | Preserved without automated battery scripts or udev rules. |

<verification>
Execute the full test harness:
`./scripts/phase38-power-profiles-assert.sh`
Assert strict repository cleanliness:
`./arch/dots-hyprland.sh verify --strict`
</verification>

<success_criteria>
1. `power-profiles-daemon.service` is actively running and enabled on Arch Linux.
2. `arch/pkglist-native.txt` lists `power-profiles-daemon` in sorted alphabetical order.
3. `bootstrap.sh` idempotently checks and provisions `power-profiles-daemon` and its systemd unit.
4. Upstream `PowerProfilesToggle.qml` operates cleanly with zero QML overrides in `restow/quickshell/`.
5. `./scripts/phase38-power-profiles-assert.sh` passes all 5 sections with FAIL=0 FINDINGS=0.
6. `./arch/dots-hyprland.sh verify --strict` passes with 0 findings and zero git working tree churn.
</success_criteria>

<output>
Create `.planning/phases/38-power-profiles-daemon-system-integration/38-01-SUMMARY.md` when done
</output>
