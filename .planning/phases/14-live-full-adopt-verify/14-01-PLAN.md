---
phase: 14-live-full-adopt-verify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase14-preflight.sh
  - docs/phase14-adopt-runbook.md
  - .planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt
  - .gitignore
  - .config/hypr/hyprland.conf
  - .config/hypr/hyprland.conf.bak
  - .config/hypr/hyprland-gui.conf
  - .config/dolphinrc
  - .config/kdeglobals
  - arch/dots-hyprland.sh
autonomous: true
requirements: [ADOPT-01, ADOPT-04]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 6
  confidence: low

must_haves:
  truths:
    - "The operator can read one committed document that gives one unambiguous order for the whole adopt window and states both the go conditions and the no-go conditions (D-18, D-20, D-21)."
    - "Running `./scripts/phase14-preflight.sh` with no arguments reports every mechanical go condition and exits 0 when they all hold, and it mutates nothing under $HOME (D-02, D-13, RESEARCH Pitfall 2)."
    - "Rotating `~/ii-original-dots-backup` happens only when the operator passes `--rotate-backup`, never as a side effect of a check run (D-27)."
    - "The runbook forbids `-f`/`--force`, `--skip-backup`, `-F`/`--firstrun`, and `--skip-hyprland-entry` by name (D-22)."
    - "The runbook's rollback section names all three wrapper-owned tiers and never reaches for the upstream uninstall subcommand (ADOPT-04, D-23, D-25)."
    - "The pre-adopt sha256 and byte-size facts for `hyprland.conf`, `hyprlock.conf`, and `hypridle.conf` are recorded in the repo before any mutation, so D-36 and D-37 stay provable after the install (RESEARCH Pattern 3)."
    - "The repo holds a faithful pre-adopt archive of the live hypr tree plus `dolphinrc` and `kdeglobals`, and the D-07 sync left `.config/hypr/custom/` untouched (D-03, Phase 13 D-03)."
    - "`waybar` and `swaync` are gone from `PROTECT_EXPLICIT`, `hyprpaper` remains, and the Phase 12 smoke suite is still green (D-28)."
    - "The working tree is clean and `origin/main` is current, so the runbook the operator reads at a TTY is the version on GitHub (D-15, D-25)."
  artifacts:
    - scripts/phase14-preflight.sh
    - docs/phase14-adopt-runbook.md
    - .planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt
    - .config/hypr/hyprland.conf.bak
    - .config/hypr/hyprland-gui.conf
    - .config/dolphinrc
    - .config/kdeglobals
  key_links:
    - "`docs/phase14-adopt-runbook.md` go/no-go section cites `./scripts/phase14-preflight.sh`'s exit code as one input, not as the gate itself (D-18)."
    - "`14-PRE-ADOPT-BASELINE.txt` is the only source `scripts/phase14-verify.sh` (plan 14-02) can compare D-36 and D-37 against; after the install the live pre-adopt conf no longer exists."
    - "The runbook's overlay-apply step points at `13-SOT-APPLY.md` §\"Apply command (D-18)\" rather than restating the `cp -a` fence (RESEARCH §Don't Hand-Roll)."
    - "`arch/dots-hyprland.sh` `PROTECT_EXPLICIT` feeds `protect --install-missing`, which is rollback tier 3; dropping `waybar`/`swaync` there is what stops tier 3 resurrecting torn-down bar packages (D-28)."
  prohibitions:
    - requirement_id: ADOPT-01
      category: safety
      status: resolved
      verification: judgment
      statement: "No agent-runnable artifact this plan produces may carry a mutating `install --full` invocation; the only agent-runnable form is the gate-fed `--dry-run` (D-01, D-05). The operator pulls the trigger."
    - requirement_id: ADOPT-01
      category: safety
      status: resolved
      verification: judgment
      statement: "The preflight script must not mutate the operator's home directory as a side effect of running checks. Any destructive step is opt-in behind its own flag and is never reached on the default path (D-13, D-27)."
    - requirement_id: ADOPT-04
      category: safety
      status: resolved
      verification: judgment
      statement: "Rollback guidance must not exist only in a location that requires a working desktop to read. It is pushed to GitHub and copied to a plain file the operator can reach from a bare TTY (D-25)."
---

<objective>
Produce every artifact the operator needs before the live full adopt, and make the two prep
mutations the agent is permitted to make.

Purpose: Phase 14 is the first phase that mutates the live machine, and the mutation is
operator-executed (D-01). This plan is the whole of the agent's contribution to the adopt
window: a runbook that reads correctly at a bare TTY with no browser, a non-mutating
preflight script that turns the go conditions into an exit code, a recorded-facts fixture
without which D-36 and D-37 are unprovable after the fact, the D-07 live-to-repo `.config`
archive, and the D-28 `PROTECT_EXPLICIT` edit.

Output: `scripts/phase14-preflight.sh`, `docs/phase14-adopt-runbook.md`,
`14-PRE-ADOPT-BASELINE.txt`, a `.gitignore` entry for `.gsd/`, five archived `.config` files,
a two-line deletion in `arch/dots-hyprland.sh`, and a clean tree pushed to `origin/main`.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-live-full-adopt-verify/14-CONTEXT.md
@.planning/phases/14-live-full-adopt-verify/14-RESEARCH.md
@.planning/phases/14-live-full-adopt-verify/14-PATTERNS.md
@.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end go/no-go path — preflight script proves three go conditions, runbook cites its exit code</name>
  <files>scripts/phase14-preflight.sh, docs/phase14-adopt-runbook.md</files>
  <read_first>
    - `scripts/phase12-full-smoke.sh` — the assert-script preamble (`:1-16`), the gate-fed dry-run block (`:63-69`), and the exit contract (`:159-163`). Copy the shape.
    - `scripts/phase10-inventory-assert.sh` — the `-h`/flag parse loop and the `XDG="${XDG_CONFIG_HOME:-$HOME/.config}"` indirection.
    - `docs/dots-hyprland-workflow.md` — title/Purpose/DRY-pointer/numbered-outline shape to mirror in the runbook.
    - `arch/dots-hyprland.sh` lines 100-120 (documented usage examples) so the dry-run invocation matches the wrapper's own form.
    - `.planning/phases/14-live-full-adopt-verify/14-PATTERNS.md` — every excerpt is already extracted there.
  </read_first>
  <action>
    Wire ONE go/no-go path through every layer this plan touches: repo script -> live filesystem probe -> wrapper invocation -> operator document.

    Create `scripts/phase14-preflight.sh`, executable, `#!/usr/bin/env bash`, `set -euo pipefail`,
    with the repo's standard preamble: a header block carrying a one-line purpose, an explicit
    `# Constraints (Phase 14):` list stating that the default path performs no `mv`, `rm`, `cp`,
    or `rsync` under `$HOME` and never invokes the wrapper without `--dry-run`, a
    `Usage (from REPO_ROOT):` block, and the exit-code contract. Then `REPO_ROOT` resolution and
    `cd "$REPO_ROOT"`, the `FAIL` accumulator with `pass()` and `fail()`, and a third level
    `FINDINGS` with `finding()` that prints `[FINDING]` and never touches `FAIL`.
    Declare named constants before the checks: `XDG="${XDG_CONFIG_HOME:-$HOME/.config}"`,
    `BACKUP_DIR="$HOME/ii-original-dots-backup"`, `WRAP="./arch/dots-hyprland.sh"`,
    `VENDOR="vendor/dots-hyprland"`.

    Implement exactly three hard checks in this task — the thin end-to-end slice, production
    quality, not a stub:
    1. D-34 not-firstrun marker. Label the check so its text contains `installed_true`.
       `test -f "$XDG/illogical-impulse/installed_true"`. On absence this is a hard `fail` and the
       script prints the reason: without it, `INSTALL_FIRSTRUN` flips true and Phase 11 D-24 stops
       holding. Follow the `scripts/phase10-inventory-assert.sh:53-60` early-abort shape: print the
       done line and exit 1 immediately, because every later check is meaningless without it.
    2. D-12 submodule pin recorded and clean. Label text must contain `submodule`. Run
       `git -C "$VENDOR" rev-parse HEAD` and echo the pin; run `git -C "$VENDOR" status --porcelain`
       (including `--recurse-submodules` behaviour via `git -C "$VENDOR" submodule status --recursive`)
       and `fail` if either reports anything. Also `fail` if `git status --porcelain -- "$VENDOR"`
       is non-empty, which means the parent records a different pin than the checkout.
    3. D-05 gate-fed dry-run. Capture into a `mktemp` file cleaned by a `trap ... EXIT`, then run
       `printf 'yes\n' | "$WRAP" install --full --dry-run` redirecting stdout and stderr into it.
       The `printf` pipe is mandatory, not decorative: `backup_gate` reads stdin and exits 1 on any
       non-`yes` token before argv is ever assembled. `pass` when the capture contains `would exec`
       and does not contain `--skip-hyprland`; `fail` otherwise and dump the first 40 lines of the
       capture, matching the repo convention of printing evidence on failure.

    Close with the standard `echo "=== done: FAIL=${FAIL} ==="` line, additionally echoing
    `FINDINGS=${FINDINGS}`, then `exit 1` when `FAIL` is greater than zero, else `exit 0`.
    `FINDINGS` must never influence the exit code.

    Create `docs/phase14-adopt-runbook.md` with only its frame in this task: the title, a one-line
    role statement, a `## Purpose` block, a DRY pointer telling the reader to run
    `./arch/dots-hyprland.sh help` for full flag semantics rather than restating them, a numbered
    `## Outline` with anchor links, and one populated section
    `## 3. Go / no-go gate (ADOPT-01, D-18..D-22)`. That section states that the gate is this
    checklist and that `./scripts/phase14-preflight.sh`'s exit code is input 1 to it, not the gate
    itself. Name the script by path so the link is greppable. Leave the remaining outline entries
    as headings only — Task 6 fills them.

    Throughout this file and every file this plan creates, write `Waybar/rofi/swaync` when naming
    the bar stack. The browser, when it must be named, is written only as its full binary name
    `google-chrome-stable` (D-39).
  </action>
  <verify>
    <automated>bash -n scripts/phase14-preflight.sh && test -x scripts/phase14-preflight.sh && ./scripts/phase14-preflight.sh && grep -q 'scripts/phase14-preflight.sh' docs/phase14-adopt-runbook.md && grep -q 'Go / no-go gate' docs/phase14-adopt-runbook.md && grep -q 'Waybar/rofi/swaync' docs/phase14-adopt-runbook.md</automated>
    <fails_when>non-zero exit — `bash -n` prints a syntax error, the script is not executable, the script prints a `[FAIL]` line and exits 1 because `installed_true` is absent / the vendor submodule is dirty / the gate-fed dry-run capture lacks `would exec`, or any of the three runbook greps prints nothing</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/phase14-preflight.sh` exists, is mode `755`, and its first line is `#!/usr/bin/env bash`.
    - The header contains a `# Constraints (Phase 14):` block naming the no-mutation-under-`$HOME` rule.
    - The file defines `pass`, `fail`, and `finding`, and `finding` does not increment `FAIL`.
    - Running the script with no arguments prints at least three `[PASS]` lines and exits 0 on this machine today.
    - `grep -c 'printf .yes' scripts/phase14-preflight.sh` is at least 1 and every wrapper invocation in the file is followed by `--dry-run`.
    - `docs/phase14-adopt-runbook.md` contains the literal `scripts/phase14-preflight.sh` and a heading whose text contains `Go / no-go gate`.
    - Running the script twice in a row leaves `stat -c %Y "$HOME/ii-original-dots-backup"` unchanged.
  </acceptance_criteria>
  <done>One go/no-go path runs end to end: the script probes live state, invokes the wrapper in its only agent-safe form, returns an exit code, and the runbook names that exit code as input 1 to the gate.</done>
</task>

<task type="auto">
  <name>Task 2: Record the pre-adopt baseline fixture and stop `.gsd/` invalidating the clean-tree assert</name>
  <files>.planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt, .gitignore</files>
  <read_first>
    - `.planning/phases/14-live-full-adopt-verify/14-RESEARCH.md` §"Pattern 3: Baseline-fixture comparison instead of live re-derivation" — the generator block and why the comparison is circular without it.
    - `.gitignore` — the existing pattern list, to append in the same style.
    - `.planning/phases/14-live-full-adopt-verify/14-RESEARCH.md` §"Common Pitfalls" Pitfall 1 — the exact dirty entries and the unignored `.gsd/`.
  </read_first>
  <action>
    Write `.planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt` as plain
    `key=value` lines, one per line, no quoting, so the plan 14-02 verify script can grep it.
    This must be written before any mutation in this plan lands, because after the operator's
    install the live pre-adopt `hyprland.conf` no longer exists and the only other copy is inside
    the backup directory that D-36 exists to distrust.

    Required keys, each derived by running the command against the live machine now:
    `baseline_captured` (an ISO-8601 UTC timestamp), `hyprland_conf_sha256`,
    `hyprland_conf_bytes`, `hyprlock_conf_sha256`, `hyprlock_conf_bytes`,
    `hypridle_conf_sha256`, `hypridle_conf_bytes`, `hyprpaper_conf_sha256`,
    `submodule_pin` (from `git -C vendor/dots-hyprland rev-parse HEAD`),
    `configProvider_pre` (from `hyprctl -j status | jq -r .configProvider`),
    `hyprland_version` (from `hyprctl -j version | jq -r .version`),
    `dp1_scale_pre`, `dp1_mode_pre`, `dp1_transform_pre` (from `hyprctl -j monitors all`
    filtered to the entry named `DP-1`),
    `hdmi_a2_present_pre` (`true` or `false`, from the same query filtered to `HDMI-A-2`),
    `backup_dir_present_pre`, `backup_dir_hyprland_conf_sha256` and
    `backup_dir_hyprland_conf_mtime` for `$HOME/ii-original-dots-backup/.config/hypr/hyprland.conf`,
    `graphical_session_target_pre` (from `systemctl --user is-active graphical-session.target`),
    and `screencast_source_types_pre` (from the `busctl --user get-property` query for
    `org.freedesktop.portal.ScreenCast` `AvailableSourceTypes`, recorded verbatim or the literal
    `unavailable` if the call fails).

    Expected values on this machine as of 2026-09-04, for cross-checking rather than hard-coding:
    `hyprlock_conf_bytes=554`, `hypridle_conf_bytes=359`,
    `submodule_pin=1a9ffb78f0c272a45f82342587dc3bec72762233`,
    `configProvider_pre=hyprlang`, `dp1_scale_pre=1`, `hdmi_a2_present_pre=false`. If any observed
    value differs from these, write the observed value and add a `# NOTE:` comment line naming the
    drift — do not write the expected value.

    Then append `.gsd/` to `.gitignore` in the existing style. Without it, `git status --porcelain`
    reports an untracked, unignorable entry forever and the D-15/D-35 clean-tree condition can never
    be satisfied. Also stage-and-review the six other currently-dirty entries so the tree can reach
    clean in Task 6: the modified `.planning/PROJECT.md` and `stow/zsh/.zshrc`, and the untracked
    Phase 13 artifacts `13-EDGE-COVERAGE.json`, `13-PATTERNS.md`, `13-RESEARCH.md`, `COVERAGE.md`.
    Commit those as their own housekeeping commit separate from the D-07 archive commit.
  </action>
  <verify>
    <automated>test -s .planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt && grep -q '^hyprland_conf_sha256=[0-9a-f]\{64\}$' .planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt && grep -q '^hyprlock_conf_bytes=554$' .planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt && grep -q '^hypridle_conf_bytes=359$' .planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt && grep -q '^submodule_pin=1a9ffb78f0c272a45f82342587dc3bec72762233$' .planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt && grep -q '^configProvider_pre=hyprlang$' .planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt && grep -q '^dp1_scale_pre=' .planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt && grep -q '^hyprlock_conf_sha256=[0-9a-f]\{64\}$' .planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt && grep -q '^hypridle_conf_sha256=[0-9a-f]\{64\}$' .planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt && git check-ignore -q .gsd</automated>
    <fails_when>non-zero exit — the fixture is empty, any of the nine anchored keys is absent, a sha256 key holds something other than 64 hex characters, a recorded value contradicts the 2026-09-04 observation without a `# NOTE:` drift line, or `git check-ignore` reports `.gsd` is still not ignored</fails_when>
  </verify>
  <acceptance_criteria>
    - `14-PRE-ADOPT-BASELINE.txt` contains all 20 required keys, one `key=value` per line.
    - `grep -c '^[a-z0-9_]*=' .planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt` is at least 20.
    - `git check-ignore -q .gsd` exits 0.
    - `git status --porcelain` no longer lists `.gsd/`, `.planning/PROJECT.md`, `stow/zsh/.zshrc`, or any of the four Phase 13 artifacts.
    - The fixture is committed; it is an input to plan 14-02, not a scratch file.
  </acceptance_criteria>
  <done>The pre-adopt facts D-36 and D-37 depend on are recorded in git before any mutation, and the clean-tree condition is now reachable.</done>
</task>

<task type="auto">
  <name>Task 3: Run the D-07 live-to-repo `.config` archive sync as its own commit, with `hypr/custom/` excluded</name>
  <files>.config/hypr/hyprland.conf, .config/hypr/hyprland.conf.bak, .config/hypr/hyprland-gui.conf, .config/dolphinrc, .config/kdeglobals</files>
  <read_first>
    - `.planning/phases/11-disposition-decisions/11-CONTEXT.md` — D-07 (the sync itself) and D-08 (repo is archive SoT; `~/.config/quickshell` is excluded).
    - `.planning/phases/14-live-full-adopt-verify/14-RESEARCH.md` §"The D-03 sync is much smaller than sync all of `~/.config`" — the enumerated delta list and Open Question 2.
    - `.config/hypr/hyprland.conf` — the current repo copy, so the diff against live is understood before it is overwritten.
    - `.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md` §"Authoring SoT" — why the repo copy of `custom/` must not be written by a live-to-repo flow.
  </read_first>
  <action>
    Copy an enumerated, literal list of live files into the repo. Never an open-ended tree sync,
    and never `rsync --delete` — a delete-sync would remove `.config/hypr/custom/general.lua`,
    whose repo copy is the one-way authoring source of truth (Phase 13 D-03).

    Copy exactly these five, live source on the left, repo destination on the right, with
    `cp -a` on each named path:
    `$XDG/hypr/hyprland.conf` to `.config/hypr/hyprland.conf`;
    `$XDG/hypr/hyprland.conf.bak` to `.config/hypr/hyprland.conf.bak`;
    `$XDG/hypr/hyprland-gui.conf` to `.config/hypr/hyprland-gui.conf`;
    `$XDG/dolphinrc` to `.config/dolphinrc`;
    `$XDG/kdeglobals` to `.config/kdeglobals`.
    `$XDG` is `${XDG_CONFIG_HOME:-$HOME/.config}`.

    Do not touch `.config/hypr/custom/` in any way. Do not copy `$XDG/quickshell` (Phase 11 D-08),
    `$XDG/hypr/hyprland/` (already byte-identical in the repo and dispositioned accept-upstream by
    Phase 11 D-18), `$XDG/hypr/hyprlock.conf`, `$XDG/hypr/hypridle.conf`, or
    `$XDG/hypr/hyprpaper.conf` — those three are already tracked and unchanged.

    `hyprland.conf.bak` and `hyprland-gui.conf` are mode 0600 on live. Read both in full before
    staging them and confirm neither holds a credential, token, or private host detail. This repo
    is public. If either does, do not add it: record the exclusion and the reason in the commit
    body instead.

    After copying, normalise permissions on the two newly added files to 0644 so they match the
    rest of the tracked tree, then commit these five paths alone with a message naming D-07 and
    D-03. This is deliberately its own commit (D-03), separate from Task 2's housekeeping commit
    and Task 4's wrapper edit, so the pre-adopt archive is a single revertible unit.
  </action>
  <verify>
    <automated>cmp -s .config/hypr/hyprland.conf "${XDG_CONFIG_HOME:-$HOME/.config}/hypr/hyprland.conf" && cmp -s .config/hypr/hyprland-gui.conf "${XDG_CONFIG_HOME:-$HOME/.config}/hypr/hyprland-gui.conf" && cmp -s .config/dolphinrc "${XDG_CONFIG_HOME:-$HOME/.config}/dolphinrc" && cmp -s .config/kdeglobals "${XDG_CONFIG_HOME:-$HOME/.config}/kdeglobals" && test -z "$(git diff --name-only HEAD~1 -- .config/hypr/custom)" && ./scripts/phase13-d19-assert.sh</automated>
    <fails_when>non-zero exit — any archived repo file differs byte-for-byte from its live counterpart, `git diff --name-only HEAD~1 -- .config/hypr/custom` prints any path (the sync reached the overlays), or `scripts/phase13-d19-assert.sh` prints a `[FAIL]` line and exits 1</fails_when>
  </verify>
  <acceptance_criteria>
    - `git ls-files .config` lists `.config/dolphinrc` and `.config/kdeglobals`, which it did not before.
    - `.config/hypr/hyprland.conf` is byte-identical to the live file (`cmp -s` exits 0).
    - `git log --oneline -1 -- .config/dolphinrc` and `git log --oneline -1 -- .config/hypr/hyprland.conf` name the same single commit, and that commit touches no path under `.config/hypr/custom/`.
    - `grep -c 'hl.monitor' .config/hypr/custom/general.lua` is still exactly 2 and `grep -c 'hl.workspace_rule' .config/hypr/custom/general.lua` is still exactly 11.
    - `.config/hypr/custom/env.lua` and `.config/hypr/custom/execs.lua` still exist and still hold no non-comment content.
  </acceptance_criteria>
  <done>The repo holds a faithful pre-adopt archive of every personal config upstream is about to overwrite, in one commit, with the Phase 13 overlays provably untouched.</done>
</task>

<task type="auto">
  <name>Task 4: Drop the two bar-stack packages from `PROTECT_EXPLICIT` so rollback tier 3 cannot resurrect them</name>
  <files>arch/dots-hyprland.sh</files>
  <reversibility rating="costly">Re-adding dual-run later is a new decision, not an undo (Phase 11 D-11, D-09). The packages stay installed and tier-1 rollback stays coherent, so undo is a list edit plus a fresh disposition — but it touches the protection contract every later `protect --install-missing` inherits.</reversibility>
  <read_first>
    - `arch/dots-hyprland.sh` lines 228-300 — the whole `PROTECT_EXPLICIT` array with its section comments, so the deletion lands on the right two lines and the surrounding comment can be judged.
    - `arch/dots-hyprland.sh` lines 340-420 — `protect_explicit_packages`, to confirm it only ever adds `--asexplicit` and can therefore not remove or demote anything.
    - `scripts/phase12-full-smoke.sh` — the regression suite this edit must leave green.
    - `.planning/phases/14-live-full-adopt-verify/14-PATTERNS.md` §"`arch/dots-hyprland.sh` — D-28 edit" — the verified line numbers.
  </read_first>
  <action>
    Delete exactly two array entries from `PROTECT_EXPLICIT` in `arch/dots-hyprland.sh`: the
    two-space-indented entry for the bar daemon at line 251 and the two-space-indented entry for
    the notification daemon at line 263. Both are bare package names on their own lines. Delete
    nothing else. `hyprpaper` at line 234 stays — it is hypr-stack, not bar chrome. The launcher
    package was never in the array, so there is no third deletion.

    The section comment above the deleted bar entry currently justifies the block by the retired
    dual-run bar. Reword it so it names the surviving justification instead: `curl`, `jq`, `bc`,
    `python`, `iputils`, `playerctl`, `pavucontrol`, `networkmanager`, `btop`, `nautilus`, and
    `kitty` are shared CLI and session tools used outside the bar stack, and the comment should say
    that. Do not delete the comment line and do not renumber or reorder the rest of the array.

    Record in the commit body that this is a protection-list change only: `protect_explicit_packages`
    exclusively adds `--asexplicit`, so removing these two entries installs nothing, removes nothing,
    and demotes nothing already marked. Its whole effect is that a later
    `./arch/dots-hyprland.sh protect --install-missing` — rollback tier 3 — will not reinstall the
    two packages the adopt window intentionally stops autostarting.
  </action>
  <verify>
    <automated>bash -n arch/dots-hyprland.sh && test "$(grep -cE '^  waybar$' arch/dots-hyprland.sh)" -eq 0 && test "$(grep -cE '^  swaync$' arch/dots-hyprland.sh)" -eq 0 && test "$(grep -cE '^  hyprpaper$' arch/dots-hyprland.sh)" -eq 1 && test "$(grep -cE '^  kitty$' arch/dots-hyprland.sh)" -eq 1 && ./scripts/phase12-full-smoke.sh</automated>
    <fails_when>non-zero exit — `bash -n` prints a syntax error, either deleted package still matches its anchored bare-array-line pattern, `hyprpaper` or `kitty` no longer matches exactly once (an over-broad deletion), or `scripts/phase12-full-smoke.sh` prints `=== done: FAIL=` with a count above zero and exits 1</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --stat -- arch/dots-hyprland.sh` shows 2 deletions plus at most 1 changed comment line.
    - `./scripts/phase12-full-smoke.sh` prints `=== done: FAIL=0 ===` and exits 0.
    - `printf 'yes\n' | ./arch/dots-hyprland.sh install --full --dry-run` still prints `would exec` and still plans the protect-list re-mark.
    - The array still contains `hyprland`, `hyprpaper`, `hyprlock`, `hypridle`, `kitty`, `fish`, and `starship`.
  </acceptance_criteria>
  <done>The two bar-stack packages are out of the protection list, everything else in it is untouched, and the Phase 12 contract suite is still green.</done>
</task>

<task type="auto">
  <name>Task 5: Expand the preflight to the full go-condition set and add the opt-in `--rotate-backup` mutation</name>
  <files>scripts/phase14-preflight.sh</files>
  <read_first>
    - `scripts/phase14-preflight.sh` — the file created in Task 1, so the expansion extends it rather than rewriting it.
    - `scripts/phase10-inventory-assert.sh` lines 30-60 — the `-h`/flag parse loop shape and the early-abort shape.
    - `vendor/dots-hyprland/sdata/subcmd-install/3.files.sh` lines 12-38 — `auto_backup_configs` and its `ask`-dependent branches, which is why the backup directory's mere existence is a hazard.
    - `arch/dots-hyprland.sh` lines 58-95 — the documented `uninstall` and `protect` flag surface, so the rollback-input probes call them in their real form.
    - `.planning/phases/14-live-full-adopt-verify/14-RESEARCH.md` §"Common Pitfalls" Pitfall 2 — why rotation must not be implicit.
  </read_first>
  <action>
    Add a flag-parse loop ahead of the checks accepting exactly three forms: no arguments (check
    only), `--rotate-backup`, and `-h`/`--help`. Any other argument prints a `[FAIL] unknown arg:`
    line to stderr and exits 1. `--help` prints a usage block that names `--rotate-backup` and
    states plainly that it is the only mutating path in the script, then exits 0.

    Extend the check set. Every new check uses `pass`/`fail` except where noted, and every label
    must contain the literal token named below so the output is greppable:

    - ADOPT-01 artifact gate. Label text contains `INV-01` and `DISP-01`. Assert the Phase 10 and
      Phase 11 source-of-truth artifacts exist and are non-empty:
      `.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md` and
      `.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md`. Also run
      `./scripts/phase10-inventory-assert.sh` and `./scripts/phase11-dispositions-assert.sh` and
      fail on a non-zero exit from either.
    - Overlay readiness. Assert `.config/hypr/custom/general.lua` is non-empty, its `hl.monitor`
      count is 2 and its `hl.workspace_rule` count is 11, and `env.lua` and `execs.lua` exist.
      Delegate to `./scripts/phase13-d19-assert.sh` and fail on a non-zero exit.
    - D-15/D-35 clean and pushed tree. Label text must contain `porcelain`. Fail when
      `git status --porcelain` is non-empty, and fail separately when
      `git rev-list --count origin/main..HEAD` is greater than zero, with a message naming
      `git push` as the remediation. The runbook the operator reads at a TTY has to be the version
      on GitHub.
    - D-13/D-27 backup directory report. Label text must contain `ii-original-dots-backup`. Always
      print what the directory currently contains: its top-level entries, its total size, and the
      mtime of `.config/hypr/hyprland.conf` inside it if present. Assert the parent is writable.
      Then compare that inner file's mtime against live `$XDG/hypr/hyprland.conf`; when the backup
      copy is older, this is a hard `fail` whose message states the exact remediation command,
      `./scripts/phase14-preflight.sh --rotate-backup`, and explains that an existing directory
      makes the upstream backup conditional on an answer the operator may not control.
    - D-26 rollback inputs. Two labels, one containing `uninstall --dry-run` and one containing
      `protect --dry-run`. Run `./arch/dots-hyprland.sh uninstall --dry-run` and
      `./arch/dots-hyprland.sh protect --dry-run`, each captured into its own `mktemp` file under
      the existing `trap`, and fail on a non-zero exit from either. Also assert the tier-1 repo
      archive input exists: `.config/hypr/hyprland.conf` is present and non-empty. Prove the inputs,
      never rehearse the restore (D-26).
    - Runbook presence. Assert `docs/phase14-adopt-runbook.md` exists and is non-empty, so the gate
      cannot pass without the document it is defined by.

    Implement `--rotate-backup` as a separate function that runs only when the flag is present and
    only after every check has run. It performs one operation: `mv` of `$BACKUP_DIR` to
    `${BACKUP_DIR}.<UTC timestamp>`, using literal paths built from the `BACKUP_DIR` constant, with
    no computed path in a delete position and no `rm` or `rsync` anywhere. It refuses and returns
    non-zero if `$BACKUP_DIR` does not exist or if the timestamped destination already exists. It
    prints the old and new paths. Nothing on the default path may call it.
  </action>
  <verify>
    <automated>bash -n scripts/phase14-preflight.sh && ./scripts/phase14-preflight.sh --help 2>&1 | grep -q -- '--rotate-backup' && ! ./scripts/phase14-preflight.sh --bogus-arg 2>/dev/null && M0=$(stat -c %Y "$HOME/ii-original-dots-backup") && { ./scripts/phase14-preflight.sh >/tmp/p14-pre.txt 2>&1 || true; } && test "$(stat -c %Y "$HOME/ii-original-dots-backup")" = "$M0" && grep -q 'installed_true' /tmp/p14-pre.txt && grep -q 'submodule' /tmp/p14-pre.txt && grep -q 'INV-01' /tmp/p14-pre.txt && grep -q 'DISP-01' /tmp/p14-pre.txt && grep -q 'porcelain' /tmp/p14-pre.txt && grep -q 'ii-original-dots-backup' /tmp/p14-pre.txt && grep -q 'uninstall --dry-run' /tmp/p14-pre.txt && grep -q 'protect --dry-run' /tmp/p14-pre.txt</automated>
    <fails_when>non-zero exit — `bash -n` prints a syntax error, `--help` output lacks the flag name, an unknown argument is accepted instead of rejected, the backup directory's mtime changed across a default-path run (the script mutated `$HOME`), or any of the eight required label tokens is absent from the captured output</fails_when>
  </verify>
  <acceptance_criteria>
    - `./scripts/phase14-preflight.sh --help` exits 0 and its output names `--rotate-backup` as the only mutating path.
    - `./scripts/phase14-preflight.sh --bogus-arg` exits non-zero and prints a line beginning `[FAIL] unknown arg:`.
    - A default-path run leaves `$HOME/ii-original-dots-backup` present with an unchanged mtime and unchanged top-level entry list.
    - The captured output contains labels holding each of: `installed_true`, `submodule`, `INV-01`, `DISP-01`, `porcelain`, `ii-original-dots-backup`, `uninstall --dry-run`, `protect --dry-run`.
    - `grep -cE 'rm -rf|rsync .*--delete' scripts/phase14-preflight.sh` is 0.
    - Every `mv` in the file is inside the rotate function, and that function is called from exactly one place, guarded by the flag.
  </acceptance_criteria>
  <done>The preflight reports every mechanical go condition, its default path is provably read-only against `$HOME`, and the one destructive step exists only behind its own flag.</done>
</task>

<task type="auto">
  <name>Task 6: Write the full runbook body, then commit and push so the operator reads it from GitHub</name>
  <files>docs/phase14-adopt-runbook.md</files>
  <read_first>
    - `docs/phase14-adopt-runbook.md` — the frame written in Task 1, so the sections are filled rather than the file rewritten.
    - `docs/dots-hyprland-workflow.md` — numbered-section shape, fenced `bash` blocks with inline `# expect:` comments, and the `## See also` footer.
    - `.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md` §"Apply command (D-18)" — the overlay apply fence to point at, not restate.
    - `arch/dots-hyprland.sh` lines 58-95 — the `uninstall` and `protect` usage text, so the three rollback tiers are written in their real invocation form.
    - `.planning/phases/14-live-full-adopt-verify/14-RESEARCH.md` §"Runtime State Inventory" and §"Common Pitfalls" 5, 6, 7 — the known-loss facts.
    - `.planning/phases/11-disposition-decisions/11-CONTEXT.md` — D-24 (lock/idle no-touch) and D-28 (accept-upstream), which the runbook's known-loss list must agree with.
  </read_first>
  <action>
    Fill every remaining section of `docs/phase14-adopt-runbook.md`. The D-21 sequence appears
    exactly once, as one numbered order, and nowhere else in the file is a second ordering implied.
    Sections, in this order:

    1. Prerequisites and what this window covers. One gate covers install, overlay apply, reboot,
       first login, and verification (D-21).
    2. Start the recording. `script(1)`, output-only, writing to
       `.planning/phases/14-live-full-adopt-verify/14-ADOPT-TRANSCRIPT.txt`. State explicitly that
       `--log-in` and `--log-io` must not be used, because the install runs `sudo pacman` and those
       modes record terminal input including passwords.
    3. Go / no-go gate — already present from Task 1. Extend it with the go checklist and the no-go
       list. Go inputs: `./scripts/phase14-preflight.sh` exits 0; the operator has read
       `11-DISPOSITIONS.md`; a physical TTY is reachable; the backup directory has been rotated if
       preflight said so. No-go conditions, all hard blockers: preflight exits non-zero;
       `installed_true` is absent; the tree is dirty or unpushed; the vendor submodule is dirty; the
       operator has less than a two-hour window; there is no second device able to reach GitHub.
       State that the go decision is spoken aloud so it lands in the transcript (D-19) and that no
       separate signed-off file is produced.
    4. Banned flags. Name all four and say what each would break: `-f` / `--force` sets `ask` false
       and defeats the backup; `--skip-backup` is refused bare by the wrapper; `-F` / `--firstrun`
       makes upstream replace live `hyprlock.conf` and `hypridle.conf` instead of writing `.new`
       sidecars; `--skip-hyprland-entry` skips installing `hyprland.lua` and silently defeats
       ADOPT-02. Add a fifth prohibition that is not a flag: never type `yesforall` at any prompt,
       and answer `y` at the greeting, because either alternative sets `ask` false before the
       backup runs.
    5. Rotate the backup if preflight said so: `./scripts/phase14-preflight.sh --rotate-backup`.
    6. Stop Hyprland and switch to a bare TTY. The install must not run from inside a live session:
       upstream's stage 4 ends by asking the compositor to reload a config file it has just renamed
       away, which is harmless with nothing running and wrong inside a session (D-06).
    7. Run the install. Write out the invocation the operator types: the `./arch/dots-hyprland.sh`
       wrapper, its `install` subcommand, the `--full` meta flag, and nothing else. Do not add
       `--dry-run` to this one — this is the real mutating run and it is the operator's to type, not
       the executor's to run. Walk the operator through what
       they will see, in upstream's own execution order — wrapper preflight and backup gate,
       greeting, deps (a `pacman -Syu` that may pull a kernel), setups, then the files stage:
       backup, misc, quickshell, fish, fontconfig, hypr. Inside the hypr block name what happens:
       the `hyprland/` tree is replaced, `hyprland.conf` is renamed to `hyprland.conf.old`,
       `hyprlock.conf` and `hypridle.conf` get `.new` sidecars rather than being replaced,
       `hyprland.lua` is installed, and `custom/` is seeded only because live has none. Then the
       wrapper's post-install protect-list re-mark and ii hook enable. State the failure rule: if it
       dies partway, capture the output, do not hand-patch, do not roll back, re-run the identical
       command (D-07).
    8. Apply the Phase 13 overlay, before relogin (D-08). Point at
       `.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md` §"Apply command (D-18)"
       and say the fence there is authoritative — do not restate it. Add the post-apply sanity gate
       `luac -p ~/.config/hypr/custom/*.lua`. State the one thing that must never happen: the apply
       stays a named-file copy and never becomes a delete-sync, because upstream's seed also lands
       `keybinds.lua`, `rules.lua`, `variables.lua`, and a wallpaper-restore script that ii's own
       startup references.
    9. Re-stow kitty: `stow -R kitty` from the repo root (D-17, the one named exception to Phase 11
       D-28). Upstream's kitty sync replaces the stow symlink with its own file and adds two kitten
       scripts; the repo file is never touched, so the re-stow restores the personal config and the
       upstream scripts sit inert alongside. Note the deliberate asymmetry — `starship.toml` is not
       protected, its repo copy is overwritten in place through the symlink, and that is an accepted
       loss (D-17). Fish stays accept-upstream.
    10. Reboot. Unconditional, regardless of whether a kernel landed (D-10).
    11. Log in at a TTY and run `start-hyprland`. This machine has no display manager, so upstream's
        closing warning about session-picker entries does not apply here (D-16).
    12. Verify: run `./scripts/phase14-verify.sh` and record results per plan 14-02. Ask the operator
        to connect HDMI-A-2 first if they want the dual-head half checked; it is currently not
        attached, and the script degrades to single-head with an informational line rather than a
        failure.
    13. Known losses (D-38). Split into two lists so nothing reads as breakage. Replaced by ii, not
        losses: the polkit agent (Quickshell provides one), cliphist text and image, wallpaper,
        cursor theme, and the Qt platform theme. Actually lost and accepted: the personal
        `hyprland-session.service` autostart — say clearly that the unit file itself survives under
        `stow/systemd/` and only its autostart line dies, so screen share may break and that is a
        recorded finding, not a blocker; `wl-clip-persist`; the four workspace-pinned autostarts;
        `hyprpaper` becomes stopped-but-installed because its autostart shared the same line as the
        bar stack; and `hyprland/scripts/launch_first_available.sh` is deleted from live but is
        already tracked identically in the repo. Also note the launcher package has no autostart at
        all — only its keybinds go away with the renamed conf, and ii's own launcher replaces them.
    14. Rollback (ADOPT-04, D-23, D-24). Trigger: no usable desktop after one honest attempt — not
        any single failed criterion. Three escalating tiers, in this order. Tier 1, config restore,
        with its three independent sources ordered cheapest and most certain first: move
        `~/.config/hypr/hyprland.conf.old` back to `hyprland.conf`; then this repo's pre-adopt
        archive at `.config/hypr/hyprland.conf`; then `~/ii-original-dots-backup/`. Tier 2, the
        wrapper's own safe removal: `./arch/dots-hyprland.sh uninstall --configs-only` or
        `./arch/dots-hyprland.sh uninstall --packages-only`, which removes only
        `illogical-impulse-*` meta packages with `pacman -R`, never runs an AUR-helper recursive
        removal, and never sweeps orphans. Tier 3: `./arch/dots-hyprland.sh protect --install-missing`.
        Add an explicit prohibition line: the upstream vendor tree ships its own removal subcommand
        and it must never be used, because it removes meta packages recursively and can cascade into
        the compositor package itself. Write that prohibition by describing it — do not write the
        two-word invocation anywhere in this file, because the verification gate greps for its
        absence.
    15. `## See also` footer linking `docs/dots-hyprland-workflow.md`,
        `.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md`, and
        `.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md`.

    Immediately after section 2, add a numbered step instructing the operator to copy this document
    to a plain file outside the config tree — `cp docs/phase14-adopt-runbook.md ~/phase14-rollback.txt`
    — so section 14 is readable from a bare TTY even if the desktop and the network are both
    unavailable. This is the belt to D-25's braces.

    When naming the bar stack anywhere in this file write `Waybar/rofi/swaync`. Never use the
    generic collective noun for it; the browser this machine autostarts shares that word, so an
    instruction using it would be genuinely ambiguous at a TTY (D-39). Where the browser must be
    named, write only `google-chrome-stable`.

    Finish the task by committing the runbook and pushing every commit from this plan to
    `origin/main`. The push is a hard requirement, not housekeeping: sections 14 and the go/no-go
    checklist are only reachable from a phone if GitHub is current (D-15, D-25).
  </action>
  <verify>
    <automated>grep -q -- '--force' docs/phase14-adopt-runbook.md && grep -q -- '--skip-backup' docs/phase14-adopt-runbook.md && grep -q -- '--firstrun' docs/phase14-adopt-runbook.md && grep -q -- '--skip-hyprland-entry' docs/phase14-adopt-runbook.md && grep -q 'yesforall' docs/phase14-adopt-runbook.md && grep -q 'uninstall --configs-only' docs/phase14-adopt-runbook.md && grep -q 'uninstall --packages-only' docs/phase14-adopt-runbook.md && grep -q 'protect --install-missing' docs/phase14-adopt-runbook.md && grep -q 'hyprland.conf.old' docs/phase14-adopt-runbook.md && grep -q 'start-hyprland' docs/phase14-adopt-runbook.md && grep -q '13-SOT-APPLY.md' docs/phase14-adopt-runbook.md && grep -q 'Waybar/rofi/swaync' docs/phase14-adopt-runbook.md && test "$(grep -cE '(\./)?setup uninstall' docs/phase14-adopt-runbook.md)" -eq 0 && test -z "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' docs/phase14-adopt-runbook.md | grep -vx 'google-chrome-stable' || true)" && test -z "$(git status --porcelain)" && test "$(git rev-list --count origin/main..HEAD)" -eq 0 && ./scripts/phase14-preflight.sh</automated>
    <fails_when>non-zero exit — any of the twelve required literals is absent from the runbook, the forbidden upstream-removal phrase appears at least once, any chrome-bearing token other than the exact string `google-chrome-stable` appears, `git status --porcelain` prints any line, `git rev-list --count origin/main..HEAD` prints a number above 0 (commits not pushed), or the preflight prints a `[FAIL]` line and exits 1</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/phase14-adopt-runbook.md` contains exactly one numbered adopt sequence; `grep -c '^## [0-9]' docs/phase14-adopt-runbook.md` matches the outline entry count.
    - All four banned flags and the `yesforall` token are named in the file.
    - The rollback section names all three tiers in their real invocation form and contains zero occurrences of the upstream removal phrase.
    - The file contains `Waybar/rofi/swaync` and every chrome-bearing token in it is exactly `google-chrome-stable`.
    - `git status --porcelain` prints nothing and `git rev-list --count origin/main..HEAD` prints `0`.
    - `./scripts/phase14-preflight.sh` exits 0 and prints no `[FAIL]` line.
  </acceptance_criteria>
  <done>The runbook is complete, unambiguous, reachable from GitHub and from a plain local file, and the preflight now passes end to end because the tree is clean and pushed.</done>
</task>

</tasks>

<!-- planner-discipline-allow: (\./)?setup uninstall -->
<!-- planner-discipline-allow: [A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]* -->
<!-- planner-discipline-allow: ^  waybar$ -->
<!-- planner-discipline-allow: ^  swaync$ -->
<!-- planner-discipline-allow: rm -rf|rsync .*--delete -->

<artifacts_produced>
## Artifacts this phase produces

New files created by this plan:

- `scripts/phase14-preflight.sh`
- `docs/phase14-adopt-runbook.md`
- `.planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt`
- `.config/hypr/hyprland.conf.bak` (repo copy; the live file already existed)
- `.config/hypr/hyprland-gui.conf` (repo copy)
- `.config/dolphinrc` (repo copy)
- `.config/kdeglobals` (repo copy)
- `~/phase14-rollback.txt` (created by the operator, from a runbook step — not tracked)

New shell symbols defined in `scripts/phase14-preflight.sh`:

- functions: `pass`, `fail`, `finding`, `usage`, `rotate_backup`
- variables/constants: `REPO_ROOT`, `FAIL`, `FINDINGS`, `XDG`, `BACKUP_DIR`, `WRAP`, `VENDOR`
- CLI flags: `--rotate-backup`, `-h`, `--help`

New keys defined in `14-PRE-ADOPT-BASELINE.txt`:

- `baseline_captured`, `hyprland_conf_sha256`, `hyprland_conf_bytes`, `hyprlock_conf_sha256`,
  `hyprlock_conf_bytes`, `hypridle_conf_sha256`, `hypridle_conf_bytes`, `hyprpaper_conf_sha256`,
  `submodule_pin`, `configProvider_pre`, `hyprland_version`, `dp1_scale_pre`, `dp1_mode_pre`,
  `dp1_transform_pre`, `hdmi_a2_present_pre`, `backup_dir_present_pre`,
  `backup_dir_hyprland_conf_sha256`, `backup_dir_hyprland_conf_mtime`,
  `graphical_session_target_pre`, `screencast_source_types_pre`

New markdown section headings other artifacts grep for:

- `docs/phase14-adopt-runbook.md` — `## 3. Go / no-go gate (ADOPT-01, D-18..D-22)`, `## See also`

Modified, not created: `arch/dots-hyprland.sh` (two `PROTECT_EXPLICIT` deletions plus one comment
reword), `.gitignore` (one appended pattern), `.config/hypr/hyprland.conf` (archive refresh).
</artifacts_produced>

<flagged_assumptions>
## Flagged assumptions — spec-less edge probe (unresolved rows)

Phase 14 has no SPEC, so the edge probe ran against the phase requirement IDs. Every row came back
`unclassified` and therefore stays `unresolved` — none was auto-resolved with a backstop marker.
These are surfaced, not dropped. Two of the four belong to this plan; the other two are carried in
`14-02-PLAN.md`.

| Requirement | Probe status | Assumption this plan proceeds on |
|---|---|---|
| ADOPT-01 | unresolved (unclassified) | That "satisfied" for INV-* and DISP-* means the artifacts exist, their assert scripts exit 0, and the operator has read them — not that any further boundary condition on the inventory or disposition content is checked. The probe proposed no edge category, so no boundary/adjacency/encoding condition was derived; if one exists it is unhandled. |
| ADOPT-04 | unresolved (unclassified) | That "rollback guidance exists" is satisfied by a written, pushed, locally-copied procedure whose inputs are proven to exist (D-26) — not by any rehearsal, and not by any guarantee that the guidance succeeds. The probe derived no edge condition on partial-restore or interrupted-restore states; those are unhandled by design (D-26). |
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo artifact → operator at a bare TTY | The runbook is executable instruction with no interactive review; a wrong or ambiguous line becomes a wrong command on a live machine. |
| agent → live `$HOME` | Prep runs as the operator's own user with full write access to `~/.config` and `~/ii-original-dots-backup`. |
| live `$HOME` → public git repo | The D-07 archive moves live config files, two of them mode 0600, into a public repository. |
| repo wrapper → upstream `./setup` | `arch/dots-hyprland.sh` composes flags around third-party install logic pinned at a submodule SHA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | agent-runnable artifacts (this plan, `scripts/phase14-preflight.sh`) | critical | mitigate | The only wrapper invocation any artifact in this plan may carry is `printf 'yes\n' \| ./arch/dots-hyprland.sh install --full --dry-run`. Task 1 acceptance requires every wrapper call in the script to be followed by `--dry-run`; the mutating form appears only as operator instruction in Task 6's runbook section 7. |
| T-14-02 | Denial of service | `scripts/phase14-preflight.sh` backup rotation | high | mitigate | Task 5 puts the `mv` behind `--rotate-backup`, calls it from exactly one guarded site after all checks, and the task verify proves a default-path run leaves the backup directory's mtime unchanged. `grep -cE 'rm -rf\|rsync .*--delete'` on the script must be 0. |
| T-14-03 | Denial of service | `docs/phase14-adopt-runbook.md` rollback section | high | mitigate | Task 6 writes only the three wrapper-owned tiers; the verify carries a file-wide negative grep for the upstream removal phrase, which is correct here because ADOPT-04 bans it from the whole document, prose included. |
| T-14-04 | Tampering | upstream `./setup` flag surface | high | mitigate | Task 6 section 4 names all four banned flags plus the `yesforall` token and states what each breaks; Task 5's `installed_true` hard-fail is the mechanical half (D-34). |
| T-14-06 | Information disclosure | `.config/hypr/hyprland.conf.bak`, `.config/hypr/hyprland-gui.conf` | medium | mitigate | Task 3 requires reading both mode-0600 files in full and confirming no credential, token, or private host detail before staging; a positive finding excludes the file and is recorded in the commit body. |
| T-14-07 | Denial of service | D-07 live-to-repo sync | high | mitigate | Task 3 enumerates five literal source and destination paths and uses `cp -a` on each. No `rsync`, no `--delete`, no glob, no computed path in a delete position. The verify asserts `git diff --name-only HEAD~1 -- .config/hypr/custom` is empty and re-runs the Phase 13 D-19 fence. |
| T-14-12 | Tampering | `PROTECT_EXPLICIT` cascade-protection list | medium | mitigate | Task 4 deletes exactly two anchored array lines; the verify asserts `hyprpaper` and `kitty` still match exactly once each (catching an over-broad deletion) and that `scripts/phase12-full-smoke.sh` is still green. |
| T-14-10 | Spoofing | upstream package set | medium | transfer | Package selection is upstream's, pinned at `vendor/dots-hyprland` SHA `1a9ffb78f0c272a45f82342587dc3bec72762233`. Task 1's D-12 check fails when the submodule is dirty or the parent records a different pin. No package is chosen by this phase, so no legitimacy audit applies. |
| T-14-13 | Information disclosure | `.gitignore` change | low | accept | Adding `.gsd/` hides agent scratch state from git rather than exposing anything; the risk is only that a wanted file is later ignored, which is visible in `git status --ignored`. |

No `npm`, `pip`, `cargo`, or `pacman` install appears in any deliverable of this plan, so no
package-legitimacy checkpoint is required (`14-RESEARCH.md` §Package Legitimacy Audit).
</threat_model>

<verification>
Run from the repo root, in order:

1. `bash -n scripts/phase14-preflight.sh` — syntax gate.
2. `./scripts/phase14-preflight.sh` — exits 0 with no `[FAIL]` line.
3. `./scripts/phase12-full-smoke.sh` — prints `=== done: FAIL=0 ===` (guards the D-28 edit).
4. `./scripts/phase13-d19-assert.sh` — exits 0 (guards the D-03 sync against the overlays).
5. `./scripts/phase10-inventory-assert.sh` and `./scripts/phase11-dispositions-assert.sh` — exit 0.
6. `test -z "$(git status --porcelain)" && test "$(git rev-list --count origin/main..HEAD)" -eq 0`.
7. `test -s .planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt`.

Nothing in this list mutates the live machine. No step invokes the wrapper without `--dry-run`.
</verification>

<success_criteria>
- `scripts/phase14-preflight.sh` exists, is executable, exits 0 today, and leaves `$HOME` unchanged on its default path.
- `docs/phase14-adopt-runbook.md` carries one unambiguous adopt sequence, the go/no-go checklist, all four banned flags, the three rollback tiers, and the known-loss list — and never the forbidden upstream removal phrase.
- `14-PRE-ADOPT-BASELINE.txt` records all 20 pre-adopt keys and is committed.
- The repo `.config` tree archives the live pre-adopt hypr files plus `dolphinrc` and `kdeglobals`, in one commit, with `.config/hypr/custom/` untouched.
- `waybar` and `swaync` are absent from `PROTECT_EXPLICIT`; `hyprpaper` remains; `scripts/phase12-full-smoke.sh` is green.
- `git status --porcelain` is empty and `origin/main` is current.
</success_criteria>

<output>
Create `.planning/phases/14-live-full-adopt-verify/14-01-SUMMARY.md` when done
</output>
