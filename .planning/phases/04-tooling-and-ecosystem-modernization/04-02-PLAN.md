---
phase: 04-tooling-and-ecosystem-modernization
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - .config/nvim/lua/plugins/conform.lua
  - .config/nvim/lua/plugins/blink-cmp.lua
  - .config/nvim/lua/plugins/fzflua.lua
  - .config/nvim/lua/plugins/neotree.lua
  - .config/nvim/lua/plugins/git.lua
  - .config/nvim/lua/core/keymaps/registry.lua
  - .config/nvim/lua/core/keymaps/attach.lua
autonomous: true
requirements:
  - PLUG-02
  - TOOL-02
tags: [neovim, formatting, completion, search, tree, git]

must_haves:
  truths:
    - "Save-time formatting is enabled by default for normal file buffers, with explicit exclusions for unsafe or noisy cases"
    - "Completion remains richer by default, including docs/signature/inline help, without regressing normal editing"
    - "Search, file tree, and git workflows still work through centrally managed mappings after modernization"
    - "The search/tree/git integrations keep working on both Linux and Windows because OS-specific behavior remains behind guarded helpers"
  artifacts:
    - path: ".config/nvim/lua/plugins/conform.lua"
      provides: "Safe format-on-save policy with clear exclusions and fallback behavior"
      contains: "format_on_save"
    - path: ".config/nvim/lua/plugins/blink-cmp.lua"
      provides: "Productivity-first completion defaults aligned with the migrated LSP stack"
      contains: "documentation"
    - path: ".config/nvim/lua/core/keymaps/registry.lua"
      provides: "Central ownership of workflow-facing search/tree/git/format mappings"
      contains: "code.format"
  key_links:
    - from: ".config/nvim/lua/plugins/conform.lua"
      to: ".config/nvim/lua/core/keymaps/registry.lua"
      via: "manual format and save-without-format paths continue to target the same formatter policy"
      pattern: "conform|format"
    - from: ".config/nvim/lua/plugins/neotree.lua"
      to: ".config/nvim/lua/core/keymaps/attach.lua"
      via: "neo-tree buffer-local mappings continue to come from the central registry rather than ad hoc plugin-local globals"
      pattern: "neo%-tree|apply_explorer"
---

<objective>
Modernize the workflow-facing integrations that sit on top of the new LSP baseline: formatting, completion, search, tree, and git. This plan turns on the productivity-first defaults chosen in Phase 4, defines a safe format-on-save policy, and cleans integration edges without re-scattering mappings out of the central keymap architecture.

Purpose: Phase 4 success is workflow-facing, not just architectural. Users need the modernized stack to feel better day to day without breaking search, tree, git, completion, or save behavior.

Output: updated formatting/completion/workflow plugin modules plus any central keymap registry adjustments needed to keep ownership clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-tooling-and-ecosystem-modernization/04-CONTEXT.md
@.planning/phases/04-tooling-and-ecosystem-modernization/04-RESEARCH.md
@.planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md
@.planning/phases/03-plugin-audit-and-validation-harness/03-VALIDATION.md
@.config/nvim/lua/plugins/lsp.lua
@.config/nvim/lua/plugins/conform.lua
@.config/nvim/lua/plugins/blink-cmp.lua
@.config/nvim/lua/plugins/fzflua.lua
@.config/nvim/lua/plugins/neotree.lua
@.config/nvim/lua/plugins/git.lua
@.config/nvim/lua/core/keymaps.lua
@scripts/nvim-validate.sh

<interfaces>
Current workflow contracts:

```lua
-- conform.lua
return {
	"stevearc/conform.nvim",
	opts = {
		formatters_by_ft = { ... },
		-- format_on_save = { timeout_ms = 500, lsp_format = "fallback" },
	},
}
```

```lua
-- fzflua.lua
return {
	"ibhagwan/fzf-lua",
	opts = {},
	keys = require("core.keymaps.lazy").search_keys(),
}
```

```lua
-- neotree.lua
local lazy = require("core.keymaps.lazy")
local explorer_keys = lazy.explorer_keys()
for _, key_map in ipairs(explorer_keys) do
	vim.keymap.set("n", key_map[1], key_map[2], { ... })
end
```

The end-of-file global `vim.keymap.set(...)` loop in `neotree.lua` is a direct pressure point against D-16 and should be normalized back through the central keymap system.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Turn on safe format-on-save and tune completion for productivity-first defaults</name>
  <files>.config/nvim/lua/plugins/conform.lua, .config/nvim/lua/plugins/blink-cmp.lua, .config/nvim/lua/core/keymaps/registry.lua</files>
  <action>
    Update formatting and completion behavior per D-10 through D-13. This task should deliver the default editing experience, not leave the policy commented out:

    1. In `.config/nvim/lua/plugins/conform.lua`, enable save-time formatting for normal file buffers with an explicit function-based safety policy rather than a blanket static table.
    2. The format-on-save policy must:
       - run only for normal file buffers (`buftype == ""`, modifiable file buffers)
       - skip clearly unsafe/noisy filetypes such as `gitcommit`, `text`, and `markdown`
       - skip plugin/special buffers and unnamed scratch buffers
       - use predictable fallback behavior (`lsp_format = "fallback"` or the conform equivalent for the installed version)
       - keep formatter routing centralized in `conform.lua`, not pushed into server config
    3. Preserve the existing explicit "save without formatting" path in the keymap registry. If the registry needs a small wording or desc cleanup to reflect the new default, do that in `.config/nvim/lua/core/keymaps/registry.lua`; do not introduce plugin-local user mappings.
    4. In `.config/nvim/lua/plugins/blink-cmp.lua`, keep blink.cmp as the engine but tune it for a rich-by-default workflow per D-12 and D-13:
       - keep docs auto-show, signature help, and ghost text on unless validation shows clear noise regressions
       - clean up option structure/comments so the final config reads like the chosen baseline rather than an upstream sample dump
       - keep fallback behavior friendly on machines without the Rust matcher by preserving or clarifying the current fuzzy implementation choice

    Do not turn this into a plugin replacement plan. Keep `conform.nvim` and `blink.cmp`, modernize their behavior, and make the save policy explicit and checker-friendly.
  </action>
  <verify>
    <automated>rg -n 'format_on_save' .config/nvim/lua/plugins/conform.lua && rg -n 'auto_show\s*=\s*true' .config/nvim/lua/plugins/blink-cmp.lua && rg -n 'signature' .config/nvim/lua/plugins/blink-cmp.lua && rg -n 'ghost_text' .config/nvim/lua/plugins/blink-cmp.lua && ./scripts/nvim-validate.sh startup && ./scripts/nvim-validate.sh smoke && ./scripts/nvim-validate.sh health</automated>
  </verify>
  <acceptance_criteria>
    - `.config/nvim/lua/plugins/conform.lua` has an active `format_on_save` policy (verified by `rg 'format_on_save'`)
    - The format-on-save implementation is function-based or otherwise explicit enough to encode buffer/filetype exclusions
    - The policy excludes at least `gitcommit`, `text`, and `markdown`, plus non-file/special buffers
    - `.config/nvim/lua/plugins/blink-cmp.lua` keeps docs/signature/ghost text enabled by default (verified by grep)
    - The central keymap registry still contains a manual format path and a save-without-format path
    - `./scripts/nvim-validate.sh startup`, `./scripts/nvim-validate.sh smoke`, and `./scripts/nvim-validate.sh health` all exit 0
  </acceptance_criteria>
  <done>Formatting and completion defaults now match the Phase 4 productivity posture without hiding the safety policy in comments or breaking the registry architecture.</done>
</task>

<task type="auto">
  <name>Task 2: Modernize search, tree, and git integrations while keeping central keymap ownership</name>
  <files>.config/nvim/lua/plugins/fzflua.lua, .config/nvim/lua/plugins/neotree.lua, .config/nvim/lua/plugins/git.lua, .config/nvim/lua/core/keymaps/registry.lua, .config/nvim/lua/core/keymaps/attach.lua</files>
  <action>
    Modernize the remaining workflow integrations around the new baseline per D-14, D-15, and D-16:

    1. Keep `fzf-lua`, `neo-tree`, `gitsigns`, and `vim-fugitive` per the Phase 3 audit. This task is a keep-and-modernize pass, not a novelty replacement pass.
    2. In `.config/nvim/lua/plugins/fzflua.lua`, normalize the plugin spec so it remains a thin adapter onto the central search key set and any opts changes are there to improve the default workflow, not to re-home mappings.
    3. In `.config/nvim/lua/plugins/neotree.lua`, remove the end-of-file global `vim.keymap.set(...)` loop and route explorer mappings back through the central keymap attach/registry path. Keep OS-specific external open behavior behind `core.open` only, per D-15.
    4. In `.config/nvim/lua/plugins/git.lua`, tune defaults only where they improve daily workflow predictability (for example blame timing, preview borders, or attach scope), but keep the git stack itself intact.
    5. If a workflow adjustment requires a new user-facing mapping or a changed description, implement it in `.config/nvim/lua/core/keymaps/registry.lua` and associated attach helpers, never inline in plugin specs.
    6. Ensure `neo-tree` and `fzf-lua` still cooperate with the current cross-platform helper paths instead of reintroducing shell-specific or Linux-only command assumptions.

    This task should leave the workflow plugins easier to reason about and better aligned with the central control plane built in Phase 2.
  </action>
  <verify>
    <automated>! rg -n 'vim\\.keymap\\.set\\(' .config/nvim/lua/plugins/neotree.lua && ./scripts/nvim-validate.sh startup && ./scripts/nvim-validate.sh smoke</automated>
  </verify>
  <acceptance_criteria>
    - `.config/nvim/lua/plugins/neotree.lua` no longer sets global user mappings directly via a plugin-local loop
    - Search, explorer, and git user-facing mappings remain owned by the central keymap modules
    - `.config/nvim/lua/plugins/fzflua.lua`, `.config/nvim/lua/plugins/neotree.lua`, and `.config/nvim/lua/plugins/git.lua` still load under `./scripts/nvim-validate.sh smoke`
    - No new Linux-only command paths are introduced for search/tree/git actions
    - `./scripts/nvim-validate.sh startup` and `./scripts/nvim-validate.sh smoke` both exit 0
  </acceptance_criteria>
  <done>The core workflow plugins stay modern and usable while the keymap/control-plane architecture remains centralized and portable.</done>
</task>

</tasks>

<verification>
Automated:
- `./scripts/nvim-validate.sh startup`
- `./scripts/nvim-validate.sh smoke`
- `./scripts/nvim-validate.sh health`
- `rg -n 'format_on_save' .config/nvim/lua/plugins/conform.lua`
- `rg -n 'auto_show\s*=\s*true' .config/nvim/lua/plugins/blink-cmp.lua`
- `rg -n 'ghost_text' .config/nvim/lua/plugins/blink-cmp.lua`
- `! rg -n 'vim\.keymap\.set\(' .config/nvim/lua/plugins/neotree.lua`

Manual workflow checks:
1. Save representative Lua, Python, and TypeScript files and confirm safe format-on-save behavior works while `gitcommit`, `markdown`, and `text` buffers are excluded.
2. Trigger completion in a code buffer and confirm completion docs, signature help, and ghost text still appear and feel stable.
3. Run file search and live grep through the existing central mappings, then open Neo-tree and git status flows through their central mappings to confirm no major regression in navigation.
</verification>

<success_criteria>
- Formatting is on by default where safe, with documented exclusions instead of commented-out intent.
- Completion remains richer by default and tied cleanly to the Phase 4 LSP baseline.
- Search, tree, and git workflows still function without keymap ownership drifting back into plugin files.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tooling-and-ecosystem-modernization/04-02-SUMMARY.md`.
</output>
