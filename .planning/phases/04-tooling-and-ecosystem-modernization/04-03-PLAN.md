---
phase: 04-tooling-and-ecosystem-modernization
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - .config/nvim/lua/plugins/notify.lua
  - .config/nvim/lua/plugins/lualine.lua
  - .config/nvim/lazy-lock.json
  - .config/nvim/README.md
autonomous: true
requirements:
  - PLUG-02
  - TOOL-02
tags: [neovim, cleanup, plugin-specs, lockfile, docs]

must_haves:
  truths:
    - "The final modernized plugin stack is internally consistent, with the remaining UI/message specs using cleaner supported patterns instead of leftover drift"
    - "Phase 4 finishes without changing the validation harness probe set, so the final harness still validates the same plugin identities it audited in Phase 3"
    - "The refreshed lockfile matches the final modernized plugin set rather than historical drift"
    - "Maintainer can read the README and understand the current modern baseline, validation flow, and expected daily workflows"
  artifacts:
    - path: ".config/nvim/lua/plugins/notify.lua"
      provides: "Normalized message/notification stack config without opening a new replacement branch"
      contains: "noice"
    - path: ".config/nvim/lua/plugins/lualine.lua"
      provides: "Statusline wiring aligned with the kept notification/message stack"
      contains: "lualine"
    - path: ".config/nvim/lazy-lock.json"
      provides: "Lockfile refreshed against the final plugin set after modernization"
      contains: "{"
    - path: ".config/nvim/README.md"
      provides: "Updated documentation for the Phase 4 modern baseline, validation commands, and workflow expectations"
      contains: "nvim-validate.sh"
  key_links:
    - from: ".config/nvim/lua/plugins/notify.lua"
      to: "scripts/nvim-validate.sh"
      via: "04-03 preserves the harness-probed notify/noice stack, so final validation keeps the Phase 3 probe contract intact"
      pattern: "noice"
    - from: ".config/nvim/lazy-lock.json"
      to: ".config/nvim/lua/plugins/*.lua"
      via: "lockfile is regenerated after final spec cleanup so audited/modernized plugins and pins match"
      pattern: "catppuccin|blink|neo%-tree|conform|mason"
    - from: ".config/nvim/README.md"
      to: "scripts/nvim-validate.sh"
      via: "docs point maintainers to the repo-owned validation contract after modernization"
      pattern: "nvim%-validate%.sh"
---

<objective>
Finish the modernization wave by cleaning up the remaining weak or outdated patterns in the UI/message layer, normalizing the final plugin specs that still need cleanup, and refreshing docs plus lockfile so the repo ends Phase 4 in a coherent state without changing the validation harness probe contract.

Purpose: The first two plans modernize behavior. This plan makes the result maintainable: no dangling outdated patterns in the remaining UI/message stack, no undocumented stack drift, and no stale lockfile left behind after the ecosystem update.

Output: normalized `notify.lua` / `lualine.lua`, refreshed `lazy-lock.json`, and updated `.config/nvim/README.md` that documents the final modern baseline and verification flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-tooling-and-ecosystem-modernization/04-CONTEXT.md
@.planning/phases/04-tooling-and-ecosystem-modernization/04-RESEARCH.md
@.planning/phases/03-plugin-audit-and-validation-harness/03-PLUGIN-AUDIT.md
@.planning/phases/03-plugin-audit-and-validation-harness/03-VALIDATION.md
@.config/nvim/lua/plugins/lsp.lua
@.config/nvim/lua/plugins/conform.lua
@.config/nvim/lua/plugins/blink-cmp.lua
@.config/nvim/lua/plugins/fzflua.lua
@.config/nvim/lua/plugins/neotree.lua
@.config/nvim/lua/plugins/git.lua
@.config/nvim/lua/core/keymaps.lua
@scripts/nvim-validate.sh

<interfaces>
Normalization rules for this plan:
- Prefer keep-and-modernize over novelty replacement, per Phase 4 research.
- `scripts/nvim-validate.sh` currently hardcodes `notify` / `noice` probes, so this plan must preserve those plugin identities because the harness file is not in this plan's ownership.
- User-facing mappings remain centralized; this plan must not move mappings back into plugin specs while normalizing the remaining UI/message modules.
- `./scripts/nvim-validate.sh` remains the canonical automation surface for final validation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the remaining UI/message plugin specs without changing harness-probed plugin identities</name>
  <files>.config/nvim/lua/plugins/notify.lua, .config/nvim/lua/plugins/lualine.lua</files>
  <action>
    Perform the final cleanup pass per D-07 through D-09 and D-17, but narrow this plan to normalization rather than replacement. The executor must keep the current `notify.nvim` / `noice.nvim` / `lualine.nvim` stack because `scripts/nvim-validate.sh` still probes `notify` and `noice`, and this plan does not own the harness file.

    1. Normalize `.config/nvim/lua/plugins/notify.lua` and `.config/nvim/lua/plugins/lualine.lua` to cleaner supported patterns:
       - prefer `opts = {}` / `opts = function()` for declarative plugins
       - keep `config = function()` only where imperative setup is actually required
       - remove stale upstream tutorial comments, obsolete compatibility notes, and dead branches left over from the migration
       - keep one plugin/domain per file unless the file already intentionally groups a domain
    2. Use an explicit go/no-go rule for `noice.nvim`: keep it and simplify only its surrounding configuration. Replacement is out of scope for this plan revision. If the executor discovers a defect that can only be fixed by replacing `noice.nvim`, stop and surface that as follow-up work rather than improvising a stack change mid-plan.
    3. Do not reopen LSP/format/search/tree/git architecture decisions already settled in 04-01 and 04-02.
    4. Preserve central keymap ownership while simplifying the UI/message modules. No inline user-facing `keys = { ... }` migrations are allowed just to make the cleanup easier.

    The goal is not "more churn." The goal is a calmer, more supportable final plugin surface that still matches the Phase 3 validation contract.
  </action>
  <verify>
    <automated>./scripts/nvim-validate.sh startup && ./scripts/nvim-validate.sh smoke && ./scripts/nvim-validate.sh health</automated>
  </verify>
  <acceptance_criteria>
    - `.config/nvim/lua/plugins/notify.lua` and `.config/nvim/lua/plugins/lualine.lua` no longer carry obsolete migration notes, commented-out dead paths, or avoidable sample-config noise
    - `noice.nvim` is retained in this plan, so the existing Phase 3 harness probe list remains valid without editing `scripts/nvim-validate.sh`
    - The resulting `notify.lua` / `lualine.lua` coupling is simpler and easier to reason about than the starting point
    - Central keymap ownership is preserved while the UI/message modules are normalized
    - `./scripts/nvim-validate.sh startup`, `smoke`, and `health` all exit 0 after the cleanup pass
  </acceptance_criteria>
  <done>The remaining UI/message plugin surface is cleaner and more internally consistent, and the harness still validates the same plugin identities as before.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh lazy-lock.json and document the final Phase 4 baseline</name>
  <files>.config/nvim/lazy-lock.json, .config/nvim/README.md</files>
  <action>
    Finish the phase with the final lockfile and repo docs:

    1. Regenerate `.config/nvim/lazy-lock.json` against the final plugin set after Task 1. The lockfile must reflect the final Phase 4 cleanup state, not the pre-modernization state.
    2. Update `.config/nvim/README.md` so it documents:
       - the Neovim `0.11+` baseline
       - Mason-first / system-binary fallback policy
       - the chosen formatting, completion, search, tree, and git baseline
       - the save-format safety policy and its exclusions
       - the central keymap ownership rule for user-facing mappings
       - the repo-owned validation commands, including `./scripts/nvim-validate.sh`
    3. The README update should describe the actual final stack after Task 1, including that the Phase 4 finalization kept the existing notify/noice stack and finished by normalizing it rather than swapping message plugins.
    4. Run the full validation harness after the lockfile refresh. If the refresh changes plugin load behavior, fix the config or docs rather than skipping validation.

    This task closes the phase: final plugin set, final docs, final verification.
  </action>
  <verify>
    <automated>./scripts/nvim-validate.sh all</automated>
  </verify>
  <acceptance_criteria>
    - `.config/nvim/lazy-lock.json` is regenerated after the final spec cleanup
    - `.config/nvim/README.md` documents the actual Neovim 0.11+ baseline and the final modern workflow stack
    - The README explicitly mentions the save-format policy, central keymap rule, `./scripts/nvim-validate.sh`, and that the validation harness still probes the kept notify/noice stack
    - `./scripts/nvim-validate.sh sync` and `./scripts/nvim-validate.sh all` both exit 0
  </acceptance_criteria>
  <done>The final modernized stack is pinned, documented, and fully revalidated against the repo-owned harness.</done>
</task>

</tasks>

<verification>
Automated:
- `./scripts/nvim-validate.sh startup`
- `./scripts/nvim-validate.sh smoke`
- `./scripts/nvim-validate.sh health`
- `./scripts/nvim-validate.sh all`

Manual workflow checks:
1. Re-run the Phase 4 daily workflow pass: LSP actions, completion docs/signature help, safe save-time formatting, fzf file search/live grep, Neo-tree navigation/external open, and git status/hunk actions.
2. Confirm the documented README guidance matches the observed behavior of the final stack.
3. Confirm the README and harness assumptions still match the kept notify/noice stack and the final validation commands.
</verification>

<success_criteria>
- The final plugin/tooling stack matches a current documented baseline rather than a partially modernized intermediate state.
- Phase 4 finalization keeps the Phase 3 harness probe contract intact while still normalizing the remaining UI/message specs.
- The repo ends Phase 4 with clean plugin specs, a refreshed lockfile, and a full green validation pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-tooling-and-ecosystem-modernization/04-03-SUMMARY.md`.
</output>
