---
phase: 26-qt-kde-apps-material-you-harmonization
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - scripts/phase26-qt-kde-material-you-assert.sh
autonomous: true
requirements: [QT-01, QT-02]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Qt 6 Darkly style engine plugin /usr/lib/qt6/plugins/styles/darkly6.so exists on disk and ~/.config/kdeglobals declares widgetStyle=Darkly per upstream dots-hyprland standard (D-01)"
    - "~/.config/Kvantum/ is verified guarded in guard-paths.tsv as vendor_theme and ~/.config/darklyrc is retained as an upstream-installed configuration (D-02, D-03)"
    - "guard-paths.tsv entry for $XDG_CONFIG_HOME/kde-material-you-colors is strictly tab-separated (D-04, INTG-01)"
    - "kde-material-you-colors dynamic execution updates ~/.config/kdeglobals and writes ColorScheme=MaterialYouDark with [Colors:Window] and [Colors:View] tokens (QT-02, D-05, D-06, D-09)"
    - "Upstream icon theme breeze-plus-dark is retained in ~/.config/kde-material-you-colors/config.conf (D-07)"
    - "kdeglobals window and view background luminance conforms to dark palette with relative luminance < 0.25 and RGB components < 60 (D-13)"
    - "Sections 3 and 4 of scripts/phase26-qt-kde-material-you-assert.sh pass with FAIL=0 (QT-01, QT-02)"
  artifacts:
    - scripts/phase26-qt-kde-material-you-assert.sh
  key_links:
    - "Darkly::Style reads kdeglobals directly to style Qt applications without requiring Kvantum"
    - "switchwall.sh triggers kde-material-you-colors-wrapper.sh to update kdeglobals on wallpaper change"
  prohibitions:
    - "Must not edit files under vendor/dots-hyprland/ (permanent invariant)"
    - "Must not touch closed-phase assert scripts under scripts/ (D-20 frozen history rule)"
    - "Must not install or mandate kvantum package (D-01 upstream Darkly standard)"
    - "Must not assert exit code 0 on kde-material-you-colors due to KWin DBus reload exception (D-09)"
---

<objective>
Verify Qt style engine alignment with `Darkly::Style` and guard contracts in `guard-paths.tsv` (Section 3 per D-01, D-02, D-03, D-04, QT-01), and execute and validate the dynamic Material You color generation pipeline updating `~/.config/kdeglobals` with dark tokens and compliant background luminance (Section 4 per D-05, D-06, D-07, D-09, D-13, QT-02).

Purpose: Ensure Qt applications render via the upstream Darkly style engine while dynamically inheriting Material You color palettes from wallpaper updates without requiring Kvantum or manual file edits.
Output: Validated Section 3 and Section 4 in `scripts/phase26-qt-kde-material-you-assert.sh` passing with FAIL=0.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-qt-kde-apps-material-you-harmonization/26-CONTEXT.md
@.planning/phases/26-qt-kde-apps-material-you-harmonization/26-RESEARCH.md
@.planning/phases/26-qt-kde-apps-material-you-harmonization/26-PATTERNS.md
@.planning/phases/26-qt-kde-apps-material-you-harmonization/26-VALIDATION.md
@guard-paths.tsv
@scripts/phase26-qt-kde-material-you-assert.sh
</context>

<tasks>

<task type="execute">
  <name>Task 1: Verify Qt style engine Darkly plugin, kdeglobals widgetStyle=Darkly, and guard path contracts for Kvantum and kde-material-you-colors (Section 3)</name>
  <files>scripts/phase26-qt-kde-material-you-assert.sh</files>
  <read_first>
    - scripts/phase26-qt-kde-material-you-assert.sh (lines 247-293: Section 3 implementation)
    - guard-paths.tsv (lines 13-22: Kvantum and kde-material-you-colors entries)
    - collision-map.tsv (lines 62, 77: darklyrc and Kvantum dispositions)
    - .planning/phases/26-qt-kde-apps-material-you-harmonization/26-CONTEXT.md (D-01, D-02, D-03, D-04)
  </read_first>
  <action>
    Implement and execute Section 3 of scripts/phase26-qt-kde-material-you-assert.sh verifying Qt style engine alignment and guard path contracts per D-01, D-02, D-03, D-04, and INTG-01:
    1. Darkly style plugin verification (D-01):
       - Assert that Qt 6 style plugin `/usr/lib/qt6/plugins/styles/darkly6.so` exists on disk.
       - Confirm that Qt applications load `Darkly::Style` pulling Material You colors directly from `kdeglobals`, affirming upstream dots-hyprland architecture without requiring the kvantum package per D-01.
    2. kdeglobals widgetStyle verification (D-01):
       - Assert that `~/.config/kdeglobals` exists and declares `widgetStyle=Darkly` in its `[KDE]` configuration group.
    3. Upstream darklyrc retention (D-03):
       - Confirm `~/.config/darklyrc` is retained as an upstream-installed configuration registered in collision-map.tsv as restow cp-through.
    4. Guard paths verification (D-02, D-04, INTG-01):
       - Assert that `guard-paths.tsv` contains `$XDG_CONFIG_HOME/Kvantum` registered as vendor_theme per D-02.
       - Assert that `guard-paths.tsv` contains `$XDG_CONFIG_HOME/kde-material-you-colors` registered as generated_theme per D-04 and INTG-01.
       - Assert that the `kde-material-you-colors` entry in `guard-paths.tsv` contains zero space characters and is strictly tab-separated (`IFS=$'\t'`).
    5. Run `bash scripts/phase26-qt-kde-material-you-assert.sh --section 3` and verify it passes with FAIL=0.
  </action>
  <acceptance_criteria>
    - test -f /usr/lib/qt6/plugins/styles/darkly6.so returns 0.
    - grep -q "^widgetStyle=Darkly$" "$HOME/.config/kdeglobals" returns 0.
    - bash scripts/phase26-qt-kde-material-you-assert.sh --section 3 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase26-qt-kde-material-you-assert.sh --section 3</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>Section 3 style engine and guard path assertions pass with 0 failures.</done>
</task>

<task type="execute">
  <name>Task 2: Validate dynamic Material You palette generation via kde-material-you-colors, dark palette inheritance, and background luminance compliance (Section 4)</name>
  <files>scripts/phase26-qt-kde-material-you-assert.sh</files>
  <read_first>
    - scripts/phase26-qt-kde-material-you-assert.sh (lines 294-367: Section 4 implementation)
    - vendor/dots-hyprland/dots/.config/matugen/templates/kde/kde-material-you-colors-wrapper.sh (wrapper logic, -d flag, -sv 5)
    - vendor/dots-hyprland/dots/.config/quickshell/ii/scripts/colors/switchwall.sh (dynamic hook invocation)
    - .planning/phases/26-qt-kde-apps-material-you-harmonization/26-CONTEXT.md (D-05, D-06, D-07, D-09, D-13)
  </read_first>
  <action>
    Implement and execute Section 4 of scripts/phase26-qt-kde-material-you-assert.sh validating dynamic color generation and dark palette compliance per D-05, D-06, D-07, D-09, and D-13:
    1. Trigger Material You generation probe:
       - Read active seed color from `~/.local/state/quickshell/user/generated/color.txt` (falling back to default seed `#82d3e1` if unpopulated).
       - Record `~/.config/kdeglobals` modification timestamp before execution.
       - Execute `$HOME/.local/state/quickshell/.venv/bin/kde-material-you-colors -d --color "$SEED_COLOR" -sv 5` per D-05 (dark mode following) and D-06 (standard TonalSpot variant 5).
       - Enforce D-09: Safely catch the expected non-fatal DBusException from `kwin_utils.reload()` under Hyprland with `|| true`, validating output deliverables rather than script exit code.
    2. Deliverables and schema validation:
       - Assert that `~/.config/kdeglobals` modification timestamp was updated by generator execution.
       - Assert that `~/.config/kdeglobals` contains `ColorScheme=MaterialYouDark`.
       - Assert that `~/.config/kdeglobals` contains both `[Colors:Window]` and `[Colors:View]` sections.
       - Assert that `~/.config/kde-material-you-colors/config.conf` retains the upstream icon theme `breeze-plus-dark` per D-07.
    3. Programmatic relative luminance check (D-13):
       - Execute Python probe reading `BackgroundNormal` from `[Colors:Window]` and `[Colors:View]` in `~/.config/kdeglobals`.
       - Compute relative luminance: `(0.2126*R + 0.7152*G + 0.0722*B) / 255.0`.
       - Assert that luminance for both window and view backgrounds is strictly < 0.25, and all RGB component values are < 60 (well within the < 50-60/255 dark range) per D-13.
    4. Run `bash scripts/phase26-qt-kde-material-you-assert.sh --section 4` and confirm it passes with FAIL=0.
  </action>
  <acceptance_criteria>
    - test -f "$HOME/.config/kdeglobals" returns 0.
    - grep -q "^ColorScheme=MaterialYouDark$" "$HOME/.config/kdeglobals" returns 0.
    - bash scripts/phase26-qt-kde-material-you-assert.sh --section 4 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase26-qt-kde-material-you-assert.sh --section 4</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>Section 4 dynamic Material You generation and dark luminance check verified pass with FAIL=0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wallpaper Seed Color → Generator CLI Arguments | Parsing hex color strings from user state files and passing as command line flags to `kde-material-you-colors` |
| Upstream Generator Script → Hyprland Session DBus | Execution of KDE Plasma utility under Hyprland compositor attempting KWin DBus reloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Tampering / Denial of Service | `kde-material-you-colors` CLI execution | high | mitigate | Strictly sanitize and quote seed color inputs (`"$SEED_COLOR"`); catch non-fatal KWin DBus exceptions with `|| true` so absence of Plasma KWin daemon does not abort theming pipeline or verification (D-09). |
| T-26-02 | Information Disclosure / Git Drift | `~/.config/kdeglobals` & `~/.config/Kvantum` | medium | mitigate | Validate that `kdeglobals` and `Kvantum/` are excluded from git tracking via `guard-paths.tsv` to eliminate repository churn on wallpaper transitions (D-02, D-04, INTG-01). |
</threat_model>

<verification>
Per-task automated checks:
1. `bash scripts/phase26-qt-kde-material-you-assert.sh --section 3`
2. `bash scripts/phase26-qt-kde-material-you-assert.sh --section 4`
</verification>

<success_criteria>
1. `Darkly::Style` plugin `/usr/lib/qt6/plugins/styles/darkly6.so` is present and `widgetStyle=Darkly` is set in `kdeglobals`.
2. `guard-paths.tsv` guards `Kvantum/` and `kde-material-you-colors` with strict tab separation.
3. `kde-material-you-colors` dynamic execution refreshes `kdeglobals` with `ColorScheme=MaterialYouDark`.
4. Window and view background colors have dark luminance (< 0.25) conforming to dark palette.
5. `bash scripts/phase26-qt-kde-material-you-assert.sh --section 3` passes with FAIL=0.
6. `bash scripts/phase26-qt-kde-material-you-assert.sh --section 4` passes with FAIL=0.
</success_criteria>

<output>
Create `.planning/phases/26-qt-kde-apps-material-you-harmonization/26-02-SUMMARY.md` when done
</output>

## Multi-Source Coverage Audit

| Requirement | REQUIREMENTS.md | ROADMAP.md | VALIDATION.md | Plan & Task | Status |
|---|---|---|---|---|---|
| **QT-01** | Line 17 | Phase 26 Criterion 1 | Task 26-02-01 | Plan 26-02 Task 1 | Planned (Wave 2) |
| **QT-02** | Line 18 | Phase 26 Criterion 2 | Task 26-02-02 | Plan 26-02 Task 2 | Planned (Wave 2) |

## Artifacts this phase produces
- Assert harness refinement: `scripts/phase26-qt-kde-material-you-assert.sh` (Sections 3 & 4)
- Plan summary: `.planning/phases/26-qt-kde-apps-material-you-harmonization/26-02-SUMMARY.md`
