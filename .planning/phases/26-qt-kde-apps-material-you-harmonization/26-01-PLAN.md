---
phase: 26-qt-kde-apps-material-you-harmonization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guard-paths.tsv
  - scripts/phase26-qt-kde-material-you-assert.sh
autonomous: true
requirements: [QT-01, INTG-01]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "guard-paths.tsv registers $XDG_CONFIG_HOME/kde-material-you-colors as generated_theme with tab separation, preventing upstream directory sync git churn (D-04, INTG-01)"
    - "scripts/phase26-qt-kde-material-you-assert.sh exists at 0755 permissions and implements fail-closed dotfiles assert harness conventions (set -euo pipefail, cleanup trap, --section parsing, and porcelain snapshot check) (D-16)"
    - "kde-material-you-colors executable exists in $XDG_STATE_HOME/quickshell/.venv/bin/ and reports valid version (D-08)"
    - "~/.config/hypr/hyprland/env.lua declares QT_QPA_PLATFORMTHEME=\"kde\" and QT_QPA_PLATFORM=\"wayland;xcb\" (D-14)"
    - "stow/hypr/.config/hypr/custom/env.lua contains zero QT_STYLE_OVERRIDE declarations and live session leaves QT_STYLE_OVERRIDE unset (D-15)"
    - "Sections 1 and 2 of scripts/phase26-qt-kde-material-you-assert.sh pass with FAIL=0 (QT-01, INTG-01)"
  artifacts:
    - guard-paths.tsv
    - scripts/phase26-qt-kde-material-you-assert.sh
  key_links:
    - "scripts/phase26-qt-kde-material-you-assert.sh is the gating test harness verifying all Phase 26 deliverables across Sections 1 to 5"
    - "guard-paths.tsv registration ensures arch/dots-hyprland.sh verify --strict recognizes ~/.config/kde-material-you-colors as a guarded theme path"
  prohibitions:
    - "Must not edit files under vendor/dots-hyprland/ (permanent invariant)"
    - "Must not touch closed-phase assert scripts under scripts/ (D-20 frozen history rule)"
    - "Must not declare QT_STYLE_OVERRIDE in custom/env.lua (D-15 collision avoidance)"
---

<objective>
Scaffold the Phase 26 assertion harness (`scripts/phase26-qt-kde-material-you-assert.sh`) with fail-closed structure covering Sections 1 through 5, register `$XDG_CONFIG_HOME/kde-material-you-colors` in `guard-paths.tsv` to protect the KDE color generator configuration from repository churn (INTG-01, D-04), and execute automated verification for Section 1 (quickshell virtualenv and `kde-material-you-colors` binary readiness per D-08) and Section 2 (Qt environment variables in `env.lua` with zero `QT_STYLE_OVERRIDE` collision per D-14, D-15).

Purpose: Establish the automated test harness gating all Phase 26 deliverables, protect dynamic KDE generator configs from git drift, and verify base Qt session environment readiness.
Output: Tracked `guard-paths.tsv` update and executable `scripts/phase26-qt-kde-material-you-assert.sh` passing Sections 1 and 2 with FAIL=0.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-qt-kde-apps-material-you-harmonization/26-CONTEXT.md
@.planning/phases/26-qt-kde-apps-material-you-harmonization/26-RESEARCH.md
@.planning/phases/26-qt-kde-apps-material-you-harmonization/26-PATTERNS.md
@.planning/phases/26-qt-kde-apps-material-you-harmonization/26-VALIDATION.md
@guard-paths.tsv
@scripts/phase25-gtk-material-you-assert.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Register kde-material-you-colors in guard-paths.tsv and scaffold Phase 26 assertion harness (scripts/phase26-qt-kde-material-you-assert.sh)</name>
  <files>guard-paths.tsv, scripts/phase26-qt-kde-material-you-assert.sh</files>
  <read_first>
    - guard-paths.tsv (lines 1-21: structure, headers, existing guarded theme entries)
    - scripts/phase25-gtk-material-you-assert.sh (lines 1-75: runner scaffolding, trap cleanup, --section parsing, porcelain snapshots)
    - .planning/phases/26-qt-kde-apps-material-you-harmonization/26-PATTERNS.md (Pattern 1: guard-paths.tsv and Pattern 2: phase26 assert harness)
    - .planning/phases/26-qt-kde-apps-material-you-harmonization/26-CONTEXT.md (D-04, D-16: guard-paths registration and phase assert harness specification)
  </read_first>
  <action>
    Update guard-paths.tsv and scaffold scripts/phase26-qt-kde-material-you-assert.sh with permissions 0755 per D-04, D-16, and INTG-01:
    1. Update guard-paths.tsv: Append `$XDG_CONFIG_HOME/kde-material-you-colors\tgenerated_theme\tkde-material-you-colors\tUpstream directory sync` strictly tab-separated to guard-paths.tsv per D-04 and INTG-01. Ensure no spaces are used between columns.
    2. Scaffold scripts/phase26-qt-kde-material-you-assert.sh adhering strictly to dotfiles assert harness patterns per D-16:
       - Scaffolding: Use #!/usr/bin/env bash, set -euo pipefail, resolve REPO_ROOT and ASSERT_SELF, initialize FAIL=0 and FINDINGS=0 counters, define pass(), fail(), finding(), and info() helpers.
       - Cleanup trap: Initialize TMP_FILES and SCRATCH_ROOTS arrays, define cleanup() function removing temporary files and scratch directories, and register trap cleanup EXIT.
       - CLI argument parsing: Support --section <1-5> and -h|--help flags, validating integer range 1 to 5 with fail-closed exit 1 on invalid arguments.
       - Working tree snapshots: Implement porcelain_snapshot_raw and porcelain_snapshot functions filtering ephemeral noise (.commandcode, __pycache__), snapshotting git status before test execution into PORCELAIN_BEFORE to ensure zero repository churn.
       - Implement Section 1 (Virtualenv readiness & generator binary assertion per D-08): Assert $HOME/.local/state/quickshell/.venv exists, assert $HOME/.local/state/quickshell/.venv/bin/kde-material-you-colors exists and is executable, assert generator reports valid version matching 1.[0-9]+.
       - Implement Section 2 (Qt environment variables in env.lua and unset QT_STYLE_OVERRIDE per D-14, D-15): Assert ~/.config/hypr/hyprland/env.lua declares QT_QPA_PLATFORMTHEME="kde" and QT_QPA_PLATFORM="wayland;xcb", assert stow/hypr/.config/hypr/custom/env.lua contains no QT_STYLE_OVERRIDE declarations, assert live environment variable QT_STYLE_OVERRIDE is unset or empty.
       - Implement Section 3 (Style engine & guard path contracts per D-01, D-02, D-03, D-04, INTG-01): Assert Qt 6 Darkly plugin /usr/lib/qt6/plugins/styles/darkly6.so exists, assert ~/.config/kdeglobals configures widgetStyle=Darkly, assert guard-paths.tsv guards both $XDG_CONFIG_HOME/Kvantum and $XDG_CONFIG_HOME/kde-material-you-colors with strict tab separation.
       - Implement Section 4 (Dynamic palette generation & dark luminance invariant per D-05, D-06, D-07, D-09, D-13): Read seed color from ~/.local/state/quickshell/user/generated/color.txt (fallback #82d3e1), execute generator with -d, seed color, and -sv 5 (TonalSpot), catching non-fatal KWin DBus reload error with || true per D-09, assert kdeglobals mtime updated, assert ColorScheme=MaterialYouDark and [Colors:Window] and [Colors:View] present, verify icon theme breeze-plus-dark in config.conf, and execute programmatic Python luminance check asserting relative luminance < 0.25 and RGB < 60 per D-13.
       - Implement Section 5 (Desktop portal integration & strict verifier per D-10, D-11, D-12, INTG-02): Assert ~/.config/xdg-desktop-portal/hyprland-portals.conf (or portals.conf) maps FileChooser to kde per D-12, assert /usr/bin/dolphin and /usr/bin/gwenview are installed and executable per D-10, assert git status in stow/ and restow/ is clean, and execute arch/dots-hyprland.sh verify --strict asserting exit 0 with zero findings per INTG-02.
       - Closing porcelain comparison and summary: Capture PORCELAIN_AFTER, cmp -s against PORCELAIN_BEFORE failing if git status changed across run, print === done: FAIL=X FINDINGS=Y ===, and exit 1 if FAIL > 0, exit 0 otherwise.
  </action>
  <acceptance_criteria>
    - test -x scripts/phase26-qt-kde-material-you-assert.sh returns 0.
    - bash scripts/phase26-qt-kde-material-you-assert.sh --help returns 0 and outputs Usage: .* --section <1-5>.
    - grep -qF '$XDG_CONFIG_HOME/kde-material-you-colors' guard-paths.tsv returns 0.
    - bash scripts/phase26-qt-kde-material-you-assert.sh --section 99 exits 1 with error message.
  </acceptance_criteria>
  <verify>
    <automated>test -x scripts/phase26-qt-kde-material-you-assert.sh && bash scripts/phase26-qt-kde-material-you-assert.sh --help | grep -q -- '--section <1-5>'</automated>
    <fails_when>non-zero exit code or missing --section help text</fails_when>
  </verify>
  <done>guard-paths.tsv updated with kde-material-you-colors guard entry and scripts/phase26-qt-kde-material-you-assert.sh scaffolded with 0755 permissions and complete 5-section structure.</done>
</task>

<task type="execute">
  <name>Task 2: Implement and verify Section 1 (virtualenv readiness & generator binary) and Section 2 (Qt environment variables in env.lua)</name>
  <files>scripts/phase26-qt-kde-material-you-assert.sh</files>
  <read_first>
    - scripts/phase26-qt-kde-material-you-assert.sh (lines 1-246: Sections 1 and 2 implementation)
    - vendor/dots-hyprland/dots/.config/hypr/hyprland/env.lua (lines 10-16: QT_QPA_PLATFORMTHEME and venv path)
    - stow/hypr/.config/hypr/custom/env.lua (verify absence of QT_STYLE_OVERRIDE)
    - .planning/phases/26-qt-kde-apps-material-you-harmonization/26-CONTEXT.md (D-08, D-14, D-15)
  </read_first>
  <action>
    Validate Section 1 and Section 2 automated tests and ensure runtime compliance per D-08, D-14, and D-15:
    1. Section 1 validation (D-08):
       - Test that `$HOME/.local/state/quickshell/.venv` exists as a valid directory.
       - Test that `$HOME/.local/state/quickshell/.venv/bin/kde-material-you-colors` is an executable binary.
       - Execute `$HOME/.local/state/quickshell/.venv/bin/kde-material-you-colors --version` and verify it reports a version string matching `1.[0-9]+` (empirically `1.10.1`).
    2. Section 2 validation (D-14, D-15):
       - Verify that `~/.config/hypr/hyprland/env.lua` defines `QT_QPA_PLATFORMTHEME = "kde"` and `QT_QPA_PLATFORM = "wayland;xcb"` per D-14.
       - Verify that `stow/hypr/.config/hypr/custom/env.lua` contains no `QT_STYLE_OVERRIDE` declarations per D-15.
       - Verify that the active process environment leaves `QT_STYLE_OVERRIDE` unset or empty to prevent collision with Darkly style engine.
    3. Run `bash scripts/phase26-qt-kde-material-you-assert.sh --section 1` and `bash scripts/phase26-qt-kde-material-you-assert.sh --section 2`.
    4. Confirm both sections pass with zero failures (FAIL=0).
  </action>
  <acceptance_criteria>
    - bash scripts/phase26-qt-kde-material-you-assert.sh --section 1 exits 0 with FAIL=0.
    - bash scripts/phase26-qt-kde-material-you-assert.sh --section 2 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase26-qt-kde-material-you-assert.sh --section 1 && bash scripts/phase26-qt-kde-material-you-assert.sh --section 2</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>Section 1 (virtualenv and generator readiness) and Section 2 (Qt environment variables) verified pass with FAIL=0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Script Execution → Python Virtual Environment (`~/.local/state/quickshell/.venv`) | Calling external Python binary inside virtual environment with shell-interpolated arguments |
| Assert Harness Execution → Git Working Tree State | Verification script execution mutating git tracked files or leaving temporary artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Tampering / Elevation of Privilege | `scripts/phase26-qt-kde-material-you-assert.sh` | high | mitigate | Strictly quote all file paths, seed colors, and CLI arguments in shell wrappers and assertions; restrict virtualenv execution to the user-owned `$XDG_STATE_HOME/quickshell/.venv/bin/kde-material-you-colors` binary (D-08, D-16). |
| T-26-02 | Accidental Secret or Git Churn Exposure | `guard-paths.tsv` & porcelain checks | medium | mitigate | Enforce git status porcelain snapshot comparison before and after harness execution, and register `$XDG_CONFIG_HOME/kde-material-you-colors` in `guard-paths.tsv` to prevent repository tracking of dynamic generator state (D-04, INTG-01). |
</threat_model>

<verification>
Per-task automated checks:
1. `test -x scripts/phase26-qt-kde-material-you-assert.sh && bash scripts/phase26-qt-kde-material-you-assert.sh --help | grep -q -- '--section <1-5>'`
2. `bash scripts/phase26-qt-kde-material-you-assert.sh --section 1 && bash scripts/phase26-qt-kde-material-you-assert.sh --section 2`
</verification>

<success_criteria>
1. `guard-paths.tsv` contains `$XDG_CONFIG_HOME/kde-material-you-colors` tab-separated as `generated_theme`.
2. `scripts/phase26-qt-kde-material-you-assert.sh` exists with mode 0755, supporting `--section <1-5>` with clean porcelain snapshots.
3. `bash scripts/phase26-qt-kde-material-you-assert.sh --section 1` passes with FAIL=0.
4. `bash scripts/phase26-qt-kde-material-you-assert.sh --section 2` passes with FAIL=0.
</success_criteria>

<output>
Create `.planning/phases/26-qt-kde-apps-material-you-harmonization/26-01-SUMMARY.md` when done
</output>

## Multi-Source Coverage Audit

| Requirement | REQUIREMENTS.md | ROADMAP.md | VALIDATION.md | Plan & Task | Status |
|---|---|---|---|---|---|
| **QT-01** | Line 17 | Phase 26 Criterion 1 | Task 26-01-02 | Plan 26-01 Task 2 | Planned (Wave 1) |
| **INTG-01** | Line 34 | Phase 29 Criterion 1 | Task 26-01-01 | Plan 26-01 Task 1 | Planned (Wave 1) |

## Artifacts this phase produces
- Test assert harness: `scripts/phase26-qt-kde-material-you-assert.sh`
- Guard path contract: `guard-paths.tsv` (`$XDG_CONFIG_HOME/kde-material-you-colors`)
- Plan summary: `.planning/phases/26-qt-kde-apps-material-you-harmonization/26-01-SUMMARY.md`
