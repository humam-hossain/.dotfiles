---
phase: 41-end-to-end-verification-repository-integrity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase41-interactions-assert.sh
autonomous: true
requirements:
  - INTG-01
  - INTG-02

estimate:
  tokens: 42000
  raw_tokens: 20000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Test harness scripts/phase41-interactions-assert.sh exists, is executable, enforces root EUID safety, and supports CLI flags --section, --quick, --syntax, and --live-notify (INTG-02, D-01, D-03, D-05, T-41-01)."
    - "Prerequisite binary check asserts bash, jq, node, lua, luac, powerprofilesctl, wpctl, qs, and git on PATH at execution start (D-05)."
    - "Section 1 asserts all 11 restow/quickshell/ overlays exist on disk and in $HOME as valid leaf symlinks with zero directory folding (INTG-01, D-02)."
    - "Section 1 asserts vendor/dots-hyprland submodule has zero uncommitted modifications (INTG-01, D-09, T-41-01)."
    - "Section 2 verifies power-profiles-daemon package, manifest entry, zero local QML overrides, and live profile cycling with atomic trap rollback to INITIAL_PROFILE on EXIT, INT, and TERM (POWER-01..03, INTG-02, D-02, D-07, T-41-01)."
    - "Section 3 verifies MediaControls.qml and GlobalStates.qml dynamic positioning AST tokens and proves coordinate boundary clamping across standard, clamped, and fallback geometries via headless JS evaluation (MEDIA-01..02, INTG-02, D-02)."
    - "Two-tier gating passes hard static checks while gracefully soft-skipping live D-Bus, Wayland (hyprctl), and Quickshell (qs -c ii) probes when headless (D-04, D-06)."
  artifacts:
    - path: "scripts/phase41-interactions-assert.sh"
      provides: "Milestone v0.8 Integration & Verification Engine covering CLI parsing, prerequisite gating, Sections 1-3, and signal-trapped rollback"
      contains: "phase41-interactions-assert"
  key_links:
    - from: "scripts/phase41-interactions-assert.sh"
      to: "restow/quickshell/"
      via: "Section 1 leaf symlink and directory folding integrity checks"
      pattern: "restow/quickshell/"
    - from: "scripts/phase41-interactions-assert.sh"
      to: "vendor/dots-hyprland"
      via: "git status --porcelain submodule cleanliness check"
      pattern: "git -C vendor/dots-hyprland status --porcelain"
    - from: "scripts/phase41-interactions-assert.sh"
      to: "powerprofilesctl"
      via: "D-Bus power profile transitions with atomic trap cleanup"
      pattern: "powerprofilesctl set \"\\$INITIAL_PROFILE\""
    - from: "scripts/phase41-interactions-assert.sh"
      to: "restow/quickshell/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml"
      via: "AST token analysis and headless coordinate clamping simulation"
      pattern: "GlobalStates\\.mediaPillCenterX"
---

<objective>
Scaffold the Milestone v0.8 Integration & Verification Engine (`scripts/phase41-interactions-assert.sh`) and implement its core preflight foundation, Section 1 (Restow Symlink Isolation), Section 2 (Power Profiles Daemon & Safe Rollback), and Section 3 (Dynamic Media Popup Positioning & Clamping).

Purpose: Establish the end-to-end assertion harness architecture, prove restow symlink isolation and submodule cleanliness, and verify power profile state transitions and media popup boundary clamping.
Output: Executable test runner `scripts/phase41-interactions-assert.sh` passing `--syntax`, `--section 1`, `--section 2`, and `--section 3` with `FAIL=0 FINDINGS=0`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-end-to-end-verification-repository-integrity/41-CONTEXT.md
@.planning/phases/41-end-to-end-verification-repository-integrity/41-RESEARCH.md
@.planning/phases/41-end-to-end-verification-repository-integrity/41-VALIDATION.md
@scripts/phase38-power-profiles-assert.sh
@scripts/phase39-media-popup-assert.sh
@scripts/phase40.1-clock-volume-assert.sh
@arch/dots-hyprland.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — Scaffold scripts/phase41-interactions-assert.sh with CLI flag parsing, prerequisite checks, logging helpers, signal traps, and baseline syntax checking</name>
  <read_first>
    - .planning/phases/41-end-to-end-verification-repository-integrity/41-CONTEXT.md
    - .planning/phases/41-end-to-end-verification-repository-integrity/41-RESEARCH.md
    - scripts/phase40.1-clock-volume-assert.sh
    - scripts/phase38-power-profiles-assert.sh
  </read_first>
  <files>scripts/phase41-interactions-assert.sh</files>
  <action>
    Author the executable test harness scaffold at `scripts/phase41-interactions-assert.sh` with `chmod +x` permissions, implementing the integration engine foundation per INTG-01, INTG-02, INTG-03, D-01, D-03, D-04, D-05, D-07, T-41-01, T-41-04:

    1. Script Header and Safety Prologue:
       - Shebang `#!/usr/bin/env bash` with `set -euo pipefail`.
       - Non-root EUID check failing closed: `[[ "${EUID:-$(id -u)}" -ne 0 ]] || { echo "Error: Do not run as root" >&2; exit 1; }` (ASVS V4, T-41-01).
       - Determine REPO_ROOT anchored to the script's directory parent: `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd -P)"` and cd into it.
       - Initialize counters: `FAIL=0` and `FINDINGS=0`.
       - Standard output helpers: `pass()`, `fail()` (increments FAIL), `finding()` (increments FINDINGS), `info()`, and `soft()` (two-tier soft skips per D-04).

    2. State Cleanup and Signal Traps (D-07, T-41-01, T-41-04):
       - Track temporary files in array `TMP_FILES=()`.
       - Global variable `INITIAL_PROFILE=""`.
       - Implement `cleanup_power()`: if `INITIAL_PROFILE` is set, query current profile and restore `powerprofilesctl set "$INITIAL_PROFILE"` if different.
       - Implement `cleanup()`: invoke `cleanup_power()` and remove all temporary files registered in `TMP_FILES`.
       - Register signal trap across all exit events: `trap cleanup EXIT INT TERM`.

    3. CLI Argument Parsing (D-01, D-03, D-08):
       - Default flags: `RUN_SECTION=0`, `QUICK_MODE=0`, `SYNTAX_ONLY=0`, `LIVE_NOTIFY=0`.
       - Parse while arguments remain:
         * `--section <1-6>` or `-s <1-6>`: validate argument matches integer 1-6; set `RUN_SECTION` or exit 1 on invalid argument.
         * `--quick`, `--standalone`, or `-q`: set `QUICK_MODE=1` (skips sub-harnesses per D-01).
         * `--syntax` or `-c`: set `SYNTAX_ONLY=1` (static AST and syntax checks only per D-03).
         * `--live-notify`, `--live`, or `-l`: set `LIVE_NOTIFY=1` (transient visual notification per D-08).
         * `-h` or `--help`: print usage banner and options, then exit 0.
         * Unknown arguments: print error and exit 1 (ASVS V5 input validation).

    4. Prerequisite Binaries Gate (D-05):
       - Iterate over required tools: `bash`, `jq`, `node`, `lua`, `luac`, `powerprofilesctl`, `wpctl`, `qs`, `git`.
       - Check `command -v "$bin" >/dev/null 2>&1`.
       - If found, log `pass "Prereq: Command '$bin' is available on PATH"`.
       - If missing, log `fail "Prereq: Required command '$bin' is MISSING on PATH (D-05)"`.

    5. Working-Tree Porcelain Baseline Snapshot (D-09, T-41-04):
       - Define `porcelain_snapshot_raw() { git status --porcelain --ignored || true; }`.
       - Define `porcelain_snapshot() { porcelain_snapshot_raw | grep -v -E '^!! (\.commandcode/|scripts/__pycache__/)$' || true; }`.
       - Allocate `PORCELAIN_BEFORE="$(mktemp /tmp/p41-porcelain-before-XXXXXX)"` and `PORCELAIN_AFTER="$(mktemp /tmp/p41-porcelain-after-XXXXXX)"`, registering both into `TMP_FILES`.
       - Populate `porcelain_snapshot > "$PORCELAIN_BEFORE"`.

    6. Syntax Validation Branch (`--syntax`):
       - When `SYNTAX_ONLY == 1`, test `bash -n "$0"` and validate `capture/ii/.config/illogical-impulse/config.json` via `jq .`.
       - Print syntax summary banner and exit with code 0 if FAIL==0, else exit 1.
  </action>
  <verify>
    <automated>test -x scripts/phase41-interactions-assert.sh && ./scripts/phase41-interactions-assert.sh --syntax</automated>
    <fails_when>test fails if scripts/phase41-interactions-assert.sh does not exist, lacks execute permissions, fails bash syntax validation, or fails prerequisite binary checks on PATH</fails_when>
  </verify>
  <acceptance_criteria>
    - File `scripts/phase41-interactions-assert.sh` exists with execute permissions (`test -x`).
    - Running `./scripts/phase41-interactions-assert.sh --syntax` exits 0 with FAIL=0.
    - Running `./scripts/phase41-interactions-assert.sh --help` prints usage with all supported flags and exits 0.
    - All 9 required binaries (`bash`, `jq`, `node`, `lua`, `luac`, `powerprofilesctl`, `wpctl`, `qs`, `git`) pass prerequisite presence check.
    - Initial git porcelain baseline is recorded to a secure temporary file in `/tmp`.
  </acceptance_criteria>
  <done>
    The tracer foundation is established and executable, providing robust CLI parsing, logging, prerequisite gating, signal traps, and baseline syntax checking.
  </done>
</task>

<task type="auto">
  <name>Task 2: Section 1 — Restow Symlink Isolation & Tree Topology (INTG-01, D-02, D-09, T-41-01)</name>
  <read_first>
    - .planning/phases/41-end-to-end-verification-repository-integrity/41-CONTEXT.md
    - .planning/phases/41-end-to-end-verification-repository-integrity/41-RESEARCH.md
    - arch/dots-hyprland.sh
    - scripts/phase40.1-clock-volume-assert.sh
  </read_first>
  <files>scripts/phase41-interactions-assert.sh</files>
  <action>
    Implement Section 1 (Restow Symlink Isolation) in `scripts/phase41-interactions-assert.sh`, executed when `RUN_SECTION == 0` or `RUN_SECTION == 1`, enforcing INTG-01, D-02, D-09, T-41-01:

    1. Submodule Cleanliness Check:
       - Run `git -C vendor/dots-hyprland status --porcelain 2>/dev/null`.
       - If output is empty, log `pass "S1: vendor/dots-hyprland submodule has 0 uncommitted changes"`.
       - If non-empty, log `fail "S1: vendor/dots-hyprland submodule has uncommitted changes"` with finding (INTG-01, D-09).

    2. Overlay Leaf Symlink Assertions:
       - Define array of all 11 milestone QML overlays:
         * `restow/quickshell/.config/quickshell/ii/GlobalStates.qml`
         * `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml`
         * `restow/quickshell/.config/quickshell/ii/modules/ii/verticalBar/VerticalBarContent.qml`
         * `restow/quickshell/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml`
         * `restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml`
         * `restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml`
         * `restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml`
         * `restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml`
         * `restow/quickshell/.config/quickshell/ii/modules/common/Config.qml`
         * `restow/quickshell/.config/quickshell/ii/services/Audio.qml`
         * `restow/quickshell/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml`
       - For each overlay, check that the repo source file exists (`test -f "$REPO_ROOT/$rel"`).
       - For each overlay, determine the live target path in `$HOME/${rel#restow/quickshell/}`.
       - Assert that the live target is a symbolic link (`test -L "$live"`).
       - Assert that the canonical link target (`readlink -f "$live"`) points directly to the expected repository source file (`*"/$rel"*`).

    3. Directory Folding Guard:
       - Verify that parent directories in the quickshell tree are genuine physical directories and NOT folded directory symlinks:
         * `$HOME/.config/quickshell/ii`
         * `$HOME/.config/quickshell/ii/modules`
         * `$HOME/.config/quickshell/ii/modules/ii`
         * `$HOME/.config/quickshell/ii/modules/ii/bar`
         * `$HOME/.config/quickshell/ii/modules/ii/mediaControls`
         * `$HOME/.config/quickshell/ii/modules/ii/sidebarRight`
         * `$HOME/.config/quickshell/ii/modules/ii/verticalBar`
         * `$HOME/.config/quickshell/ii/modules/common`
         * `$HOME/.config/quickshell/ii/modules/common/functions`
         * `$HOME/.config/quickshell/ii/modules/common/widgets`
         * `$HOME/.config/quickshell/ii/services`
       - For each directory, assert `[[ -d "$d" && ! -L "$d" ]]`. Fail with finding if any directory is folded.
  </action>
  <verify>
    <automated>./scripts/phase41-interactions-assert.sh --section 1</automated>
    <fails_when>test fails if any of the 11 restow overlays are missing, if live symlinks are invalid or broken, if directory folding is detected, or if vendor/dots-hyprland submodule has uncommitted changes</fails_when>
  </verify>
  <acceptance_criteria>
    - Running `./scripts/phase41-interactions-assert.sh --section 1` passes completely with `FAIL=0 FINDINGS=0`.
    - All 11 restow overlay files exist in the repository tree.
    - All 11 live paths in `$HOME` are verified as symbolic links targeting their respective repository sources.
    - All 11 tested parent directories are confirmed as real directories (no directory folding).
    - Submodule `vendor/dots-hyprland` has 0 uncommitted changes.
  </acceptance_criteria>
  <done>
    Section 1 is fully functional and verifies restow symlink isolation, directory folding prevention, and submodule cleanliness per INTG-01.
  </done>
</task>

<task type="auto">
  <name>Task 3: Section 2 & Section 3 — Power Profiles Daemon with Safe Rollback & Media Popup Positioning and Clamping (POWER-01..03, MEDIA-01..02, INTG-02, D-02, D-04, D-06, D-07, T-41-01)</name>
  <read_first>
    - scripts/phase38-power-profiles-assert.sh
    - scripts/phase39-media-popup-assert.sh
    - restow/quickshell/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml
    - restow/quickshell/.config/quickshell/ii/GlobalStates.qml
    - arch/pkglist-native.txt
  </read_first>
  <files>scripts/phase41-interactions-assert.sh</files>
  <action>
    Implement Section 2 (Power Profiles Daemon) and Section 3 (Dynamic Media Popup Positioning & Clamping) in `scripts/phase41-interactions-assert.sh`, executed when `RUN_SECTION` is 0, 2, or 3, enforcing POWER-01..03, MEDIA-01..02, INTG-02, D-02, D-04, D-06, D-07, T-41-01:

    1. Section 2 — Power Profiles Management:
       - Pacman package verification: Assert `pacman -Q power-profiles-daemon >/dev/null 2>&1` passes (POWER-01).
       - Package manifest verification: Assert `grep -qx "power-profiles-daemon" "$REPO_ROOT/arch/pkglist-native.txt"` passes (POWER-03).
       - Upstream toggle parity check: Assert zero local overrides exist for `PowerProfilesToggle.qml` or `AndroidPowerProfileToggle.qml` in `restow/quickshell/...` (POWER-02).
       - Two-tier gating for live D-Bus state & atomic cycle test (D-04, D-07):
         * When `SYNTAX_ONLY == 0`, check if `systemctl is-active --quiet power-profiles-daemon.service`.
         * If active: capture `INITIAL_PROFILE="$(powerprofilesctl get 2>/dev/null || echo "")"`.
         * If `INITIAL_PROFILE` is non-empty, record it, then loop through `power-saver`, `balanced`, and `performance`. For each supported profile, execute `powerprofilesctl set "$p"`, confirm `powerprofilesctl get` reflects the change, and pass.
         * Restore `powerprofilesctl set "$INITIAL_PROFILE"`, assert restored state matches `$INITIAL_PROFILE`.
         * If `power-profiles-daemon.service` is inactive or system is in a non-D-Bus session, emit `soft "S2: power-profiles-daemon.service inactive; soft-skipping live transition test"` without failing hard (D-04).

    2. Section 3 — Dynamic Media Popup Positioning & Clamping:
       - Static AST Analysis of `MediaControls.qml`: Assert presence of required anchoring and boundary clamping tokens: `GlobalStates.mediaPillScreen`, `GlobalStates.mediaPillCenterX`, `Math.round`, `hyprlandGapsOut`, `Math.max`, and `Math.min` (MEDIA-01, MEDIA-02).
       - Static AST Analysis of `GlobalStates.qml`: Assert declarations of `mediaPillCenterX`, `mediaPillCenterY`, and `mediaPillScreen` exist.
       - Headless JavaScript Simulation: Using `node -e`, test coordinate clamping calculation function `calculatePopupX(pillCenterX, pillWidth, popupWidth, screenX, screenWidth, gaps)` across 3 geometry scenarios:
         * Standard monitor centered: pillCenterX=960, pillWidth=120, popupWidth=400, screenX=0, screenWidth=1920, gaps=10 -> expected 760 (centered).
         * Extreme right edge clamping: pillCenterX=1900, pillWidth=100, popupWidth=400, screenX=0, screenWidth=1920, gaps=10 -> expected Math.min(1700, 1920 - 400 - 10) = 1510 (clamped).
         * Sentinel -1 fallback: pillCenterX=-1 -> expected center fallback Math.round(0 + (1920 - 400) / 2) = 760.
       - Two-Tier Live Desktop Probing (D-04, D-06):
         * If `WAYLAND_DISPLAY` is present and `hyprctl` is on PATH, test `hyprctl monitors -j >/dev/null 2>&1`; pass on success, soft-skip on failure.
         * Test live Quickshell process using `qs -c ii list 2>/dev/null | grep -q "quickshell/ii/shell.qml"` (D-06); pass on success, soft-skip if inactive.
  </action>
  <verify>
    <automated>./scripts/phase41-interactions-assert.sh --section 2 && ./scripts/phase41-interactions-assert.sh --section 3</automated>
    <fails_when>test fails if power-profiles-daemon is missing or unmanifested, if local toggle overrides are present, if power profile state cycling or restoration fails, if MediaControls.qml AST tokens are absent, or if headless coordinate clamping math produces incorrect offsets</fails_when>
  </verify>
  <acceptance_criteria>
    - Running `./scripts/phase41-interactions-assert.sh --section 2` passes with `FAIL=0 FINDINGS=0`.
    - Running `./scripts/phase41-interactions-assert.sh --section 3` passes with `FAIL=0 FINDINGS=0`.
    - Active power profile transitions cycle through available profiles and unconditionally restore the initial system profile.
    - Headless JS simulation validates media popup coordinate clamping and centering across all 3 scenarios.
    - Two-tier gating cleanly reports `[PASS]` or `[SOFT]` without unexpected errors.
  </acceptance_criteria>
  <done>
    Sections 2 and 3 are fully implemented, verifying power profile management with atomic rollback and media popup positioning with coordinate boundary clamping.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Desktop → System D-Bus | Test harness requests power profile changes via `powerprofilesctl` crossing into systemd/polkit D-Bus. |
| Test Harness → Local Filesystem | Test harness creates temporary test logs and porcelain diff snapshots in `/tmp`. |
| Repository → Live Submodule | Test harness inspects `vendor/dots-hyprland` submodule status to guarantee zero unauthorized mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Tampering / Privilege Escalation | Root execution / Submodule edits | high | mitigate | Fail closed if run as root (`EUID != 0`). Assert zero changes in `vendor/dots-hyprland` submodule and deploy overlays exclusively via `restow/quickshell/` leaf symlinks (INTG-01, D-05, ASVS V4). |
| T-41-02 | Denial of Service / Hardware State Pollution | Power profile switching | medium | mitigate | Capture `INITIAL_PROFILE` before cycling and bind unconditional restoration in `trap cleanup EXIT INT TERM` handler, guaranteeing initial power state is preserved on normal exit or interrupt (D-07). |
| T-41-03 | Denial of Service / Clamping Math Errors | Media popup coordinate calculation | low | mitigate | Run headless Node.js unit matrix evaluating boundary clamping math across standard, narrow/clamped, and fallback sentinel geometries (MEDIA-01, MEDIA-02). |
| T-41-04 | Working-Tree Churn | Test harness temporary files | low | mitigate | Store temporary outputs in `/tmp` using randomized `mktemp` templates registered in `TMP_FILES` array and cleaned up via signal trap. Snapshot git porcelain before and after execution (D-09, D-10, ASVS V14). |
</threat_model>

## Artifacts this phase produces

- `scripts/phase41-interactions-assert.sh`: Milestone v0.8 Integration & Verification Engine supporting CLI flags, prerequisite checking, signal trap rollback, Section 1 (Restow Symlink Isolation), Section 2 (Power Profiles Management), and Section 3 (Dynamic Media Popup Positioning & Clamping).

## Multi-Source Coverage Audit

| Source | Item ID / Reference | Description | Status | Covered By | Notes |
|---|---|---|---|---|---|
| REQ | INTG-01 | All QML modifications deployed via `restow/quickshell/` leaf symlinks without modifying `vendor/dots-hyprland`. | COVERED | Task 2 | Section 1 asserts all 11 overlays, checks no directory folding, and asserts submodule clean. |
| REQ | INTG-02 | Automated assertion test harness validates media popup positioning, power profile cycling, notification dismissal, link opening, and OTP parsing. | COVERED | Task 3 | Section 2 covers power profiles; Section 3 covers media popup positioning and clamping. |
| CONTEXT | D-01 | Milestone v0.8 assert harness architecture with sub-harness chaining, `--quick`, and `--section <1-6>`. | COVERED | Task 1 | Flag parsing and execution structure in tracer scaffold. |
| CONTEXT | D-02 | 6 modular sections matching milestone domains. | COVERED | Task 2, Task 3 | Section 1 in Task 2; Sections 2 & 3 in Task 3. |
| CONTEXT | D-03 | Standard GSD assert CLI flags (`--section`, `--syntax`, `--quick`, `--live-notify`), logging helpers, exit code `$FAIL`. | COVERED | Task 1 | Handled in tracer scaffold. |
| CONTEXT | D-04 | Two-tier gating (Hard AST + Soft Live). | COVERED | Task 1, Task 3 | Hard fail on static AST/symlinks; soft skip on inactive live sessions. |
| CONTEXT | D-05 | Prerequisite binaries check on PATH (`bash`, `jq`, `node`, `lua`, `luac`, `powerprofilesctl`, `wpctl`, `qs`, `git`). | COVERED | Task 1 | Checked at execution start before test sections. |
| CONTEXT | D-06 | Quickshell process check targets `-c ii` profile. | COVERED | Task 3 | Evaluates `qs -c ii list` matching `quickshell/ii/shell.qml`. |
| CONTEXT | D-07 | Live power profile cycling with atomic trap rollback to `$INITIAL_PROFILE`. | COVERED | Task 1, Task 3 | Signal trap on EXIT INT TERM restores initial profile unconditionally. |
| RESEARCH | T-41-01 | Root EUID guard and submodule tampering prevention. | COVERED | Task 1, Task 2 | EUID guard in prologue; submodule git status check in Section 1. |
| RESEARCH | T-41-04 | Working-tree churn prevention and porcelain tracking. | COVERED | Task 1 | Porcelain baseline snapshot in `/tmp`. |

<verification>
Execute the tracer, syntax, and section verification commands:
`test -x scripts/phase41-interactions-assert.sh && ./scripts/phase41-interactions-assert.sh --syntax`
`./scripts/phase41-interactions-assert.sh --section 1`
`./scripts/phase41-interactions-assert.sh --section 2`
`./scripts/phase41-interactions-assert.sh --section 3`
</verification>

<success_criteria>
1. `scripts/phase41-interactions-assert.sh` exists, is executable, and runs `--syntax` with zero errors.
2. Section 1 validates all 11 restow leaf symlinks, confirms no directory folding, and asserts `vendor/dots-hyprland` has 0 uncommitted changes.
3. Section 2 validates `power-profiles-daemon` package, manifest, zero local overrides, and tests profile switching with automatic rollback to `$INITIAL_PROFILE`.
4. Section 3 validates `MediaControls.qml` AST tokens and proves coordinate clamping math across standard, clamped, and fallback geometries.
5. All executed sections pass with `FAIL=0 FINDINGS=0`.
</success_criteria>

<output>
Create `.planning/phases/41-end-to-end-verification-repository-integrity/41-01-SUMMARY.md` when done
</output>
