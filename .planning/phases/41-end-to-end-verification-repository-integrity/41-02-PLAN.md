---
phase: 41-end-to-end-verification-repository-integrity
plan: "02"
type: execute
wave: 2
depends_on:
  - "41-01"
files_modified:
  - scripts/phase41-interactions-assert.sh
  - .planning/phases/41-end-to-end-verification-repository-integrity/41-VERIFICATION.md
autonomous: true
requirements:
  - INTG-01
  - INTG-02
  - INTG-03

estimate:
  tokens: 48000
  raw_tokens: 24000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Section 4 asserts NotificationGroup.qml closeButton visible, toast popup close button suppressed, NotificationItem.qml OTP copy chip, smart body click, and passes full 19-case OTP test matrix in Node.js VM (NOTIF-01..02, NAV-01..02, OTP-01..02, INTG-02, D-02, D-08)."
    - "Section 4 enforces URL scheme sanitization to https?://, rejecting javascript:, file:, and data: schemes (NAV-02, T-41-03)."
    - "Section 5 asserts ClockWidget.qml 10px breathing room (5px left/right margins), config.json volumeCeiling: 1.5 single source of truth, keybinds.lua wpctl raise binding, Config.qml and Audio.qml maxVolume contract, and QuickSliders.qml 100% stop notch (CLOCK-01, VOL-01..02, INTG-02, D-02)."
    - "Section 6 executes ./arch/dots-hyprland.sh verify --strict and asserts exit code 0 with FAIL=0 FINDINGS=0 (INTG-03, D-02, D-10)."
    - "Default harness execution orchestrates all 4 underlying sub-harnesses (phase38, phase39, phase40, phase40.1) and asserts exit code 0 for all (INTG-02, D-01)."
    - "Git porcelain snapshots before and after execution assert zero net working-tree churn across test runs (INTG-03, D-09, T-41-04)."
    - "41-VERIFICATION.md documents authoritative verification results and cross-phase summary table covering all 17 milestone v0.8 requirements (D-11)."
  artifacts:
    - path: "scripts/phase41-interactions-assert.sh"
      provides: "Completed Milestone v0.8 Integration Engine covering Sections 1-6, sub-harness orchestration, and zero-churn diffing"
      contains: "phase41-interactions-assert"
    - path: ".planning/phases/41-end-to-end-verification-repository-integrity/41-VERIFICATION.md"
      provides: "Milestone v0.8 End-to-End Verification Audit and 17-requirement traceability matrix"
      contains: "Milestone v0.8 Verification Audit"
  key_links:
    - from: "scripts/phase41-interactions-assert.sh"
      to: "arch/dots-hyprland.sh"
      via: "Section 6 strict verification invocation"
      pattern: "\\./arch/dots-hyprland\\.sh verify --strict"
    - from: "scripts/phase41-interactions-assert.sh"
      to: "scripts/phase38-power-profiles-assert.sh"
      via: "sub-harness orchestration chaining"
      pattern: "scripts/phase38-power-profiles-assert\\.sh"
    - from: "scripts/phase41-interactions-assert.sh"
      to: "scripts/phase39-media-popup-assert.sh"
      via: "sub-harness orchestration chaining"
      pattern: "scripts/phase39-media-popup-assert\\.sh"
    - from: "scripts/phase41-interactions-assert.sh"
      to: "scripts/phase40-notification-interaction-assert.sh"
      via: "sub-harness orchestration chaining"
      pattern: "scripts/phase40-notification-interaction-assert\\.sh"
    - from: "scripts/phase41-interactions-assert.sh"
      to: "scripts/phase40.1-clock-volume-assert.sh"
      via: "sub-harness orchestration chaining"
      pattern: "scripts/phase40\\.1-clock-volume-assert\\.sh"
    - from: ".planning/phases/41-end-to-end-verification-repository-integrity/41-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "100% milestone v0.8 requirement traceability mapping"
      pattern: "INTG-01"
---

<objective>
Implement Section 4 (Notification Dismissal, URL Navigation & OTP Parsing), Section 5 (Clock Padding & Volume Ceiling Contract), Section 6 (Repository Integrity & Strict Verification), Sub-Harness Orchestration, and Git Porcelain Zero-Churn Verification in `scripts/phase41-interactions-assert.sh`. Execute the comprehensive test suite to validate all 17 milestone requirements and author `41-VERIFICATION.md`.

Purpose: Provide an authoritative 100% verification verdict for Milestone v0.8 with zero git working-tree churn.
Output: Completed `scripts/phase41-interactions-assert.sh` passing all 6 sections and all 4 sub-harnesses, and comprehensive verification report `41-VERIFICATION.md`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-end-to-end-verification-repository-integrity/41-CONTEXT.md
@.planning/phases/41-end-to-end-verification-repository-integrity/41-RESEARCH.md
@.planning/phases/41-end-to-end-verification-repository-integrity/41-VALIDATION.md
@.planning/phases/41-end-to-end-verification-repository-integrity/41-01-PLAN.md
@scripts/phase41-interactions-assert.sh
@scripts/phase38-power-profiles-assert.sh
@scripts/phase39-media-popup-assert.sh
@scripts/phase40-notification-interaction-assert.sh
@scripts/phase40.1-clock-volume-assert.sh
@arch/dots-hyprland.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section 4 & Section 5 — Notification Center Ergonomics, Smart OTP, Clock Padding, and Volume Ceiling (NOTIF-01..02, NAV-01..02, OTP-01..02, CLOCK-01, VOL-01..02, INTG-02, D-02, D-04, D-08, T-41-03)</name>
  <read_first>
    - scripts/phase40-notification-interaction-assert.sh
    - scripts/phase40.1-clock-volume-assert.sh
    - restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml
    - restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml
    - restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml
    - capture/ii/.config/illogical-impulse/config.json
    - stow/hypr/.config/hypr/custom/keybinds.lua
    - restow/quickshell/.config/quickshell/ii/modules/common/Config.qml
    - restow/quickshell/.config/quickshell/ii/services/Audio.qml
    - restow/quickshell/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml
  </read_first>
  <files>scripts/phase41-interactions-assert.sh</files>
  <action>
    Implement Section 4 (Notification Center, Navigation & Smart OTP) and Section 5 (Clock Padding & Unified Volume Ceiling) in `scripts/phase41-interactions-assert.sh`, executed when `RUN_SECTION` is 0, 4, or 5 (per NOTIF-01..02, NAV-01..02, OTP-01..02, CLOCK-01, VOL-01..02, INTG-02, D-02, D-04, D-08, T-41-03):

    1. Section 4 — Notification Dismissal, URL Navigation & OTP Parsing:
       - Static AST Validation:
         * `NotificationGroup.qml`: Assert header closeButton exists (`id:\s*closeButton`) and is explicitly visible (`visible:\s*true`) per NOTIF-01.
         * `NotificationPopup.qml` in vendor tree: Assert toast popup suppresses header close button (`visible: !root.popup` or absent) per NOTIF-02.
         * `NotificationItem.qml`: Assert OTP action chip rendering (`Copy` or `otpCode`) and smart body click action invoking `notification.dismiss()` and `default` action (NAV-01, OTP-02).
         * `NotificationUtils.qml`: Assert definitions for `extractOtpCode` and `extractUrl`.
       - Sandboxed Node.js VM Evaluation of `extractOtpCode()`:
         * Load regex logic from `NotificationUtils.qml`.
         * Execute against the full 19-case test matrix from Phase 40:
           (1) Standard 6-digit with 'code': "Your verification code is 123456" -> "123456"
           (2) Hyphenated 6-digit: "Your code is 123-456" -> "123456"
           (3) 4-digit PIN: "Your login PIN is 4321" -> "4321"
           (4) 8-digit security code: "Security code: 87654321" -> "87654321"
           (5) Keyword 'OTP': "Use OTP 567890 to log in" -> "567890"
           (6) Keyword 'verification': "verification: 987654" -> "987654"
           (7) Keyword 'auth': "auth code is 345678" -> "345678"
           (8) Prefix keyword: "passcode: 654321" -> "654321"
           (9) Code preceding keyword: "123456 is your code" -> "123456"
           (10) Multi-word space keyword: "security code is 889900" -> "889900"
           (11) Embedded punctuation: "Code: [ 123456 ]" -> "123456"
           (12) Rejection - 3 digits: "Code 123" -> null
           (13) Rejection - 9 digits: "Code 123456789" -> null
           (14) Rejection - unanchored number: "You have 5000 unread messages" -> null
           (15) Rejection - date: "Meeting on 2026-09-24" -> null
           (16) Rejection - time: "Alarm set for 14:30:00" -> null
           (17) Rejection - currency: "Amount: $10000" -> null
           (18) Rejection - hex token: "Error code 0xDEADBEEF" -> null
           (19) Case insensitivity: "YOUR CODE IS 778899" -> "778899"
       - Sandboxed Node.js VM Evaluation of `extractUrl()`:
         * Test HTML anchor extraction: `<a href="https://example.com/verify">Click here</a>` -> `https://example.com/verify`.
         * Test raw URL extraction: `Please visit https://github.com/test for details` -> `https://github.com/test`.
         * Enforce URL Scheme Sanitization (T-41-03): assert regex strictly allows `https?://` schemes and rejects `javascript:`, `file:`, `data:`, or relative paths.
       - Optional Live Notification (D-08): When `LIVE_NOTIFY == 1` and `SYNTAX_ONLY == 0`, if `notify-send` is on PATH, run `notify-send -t 800 -a "Phase41Test" "Test OTP: 582910" "Verification code for Phase 41"`. If absent or command fails, emit soft skip (D-04).

    2. Section 5 — Clock Padding & Unified Volume Ceiling Contract:
       - Static AST Validation of `ClockWidget.qml`: Assert `anchors.leftMargin:\s*5` and `anchors.rightMargin:\s*5` on rowLayout (10px horizontal breathing room per CLOCK-01, D-01), `implicitWidth:\s*8` spacer, `DateTime.time`, and `DateTime.longDate`.
       - Desktop Config Single Source of Truth: Assert `capture/ii/.config/illogical-impulse/config.json` defines `.audio.volumeCeiling == 1.5` using `jq -r '.audio.volumeCeiling'` (VOL-01).
       - Hyprland Keybinds Validation: Assert `stow/hypr/.config/hypr/custom/keybinds.lua` unbinds upstream `XF86AudioRaiseVolume`, parses `config.json` via pure Lua pattern matching (`"volumeCeiling"%s*:%s*([%d%.]+)`), validates `tonumber(ceiling) > 0`, and binds `wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+ -l <ceiling>`. Validate syntax via `luac -p`.
       - Quickshell Audio Schema & Service AST:
         * `Config.qml`: Assert `property real volumeCeiling: 1.5` exists on JsonObject audio.
         * `Audio.qml`: Assert `readonly property real maxVolume: Config.options?.audio?.volumeCeiling ?? 1.5` and dynamic clamping in `incrementVolume()` (`Math.min(root.maxVolume, Audio.sink.audio.volume + step)`).
       - Sidebar Slider AST in `QuickSliders.qml`: Assert `to: Audio.maxVolume`, `stopIndicatorValues: [1.0]`, and `tooltipContent: `${Math.round(value * 100)}%`` (VOL-02).
       - Headless Node.js Volume Step Simulation: Validate 2% step size (1% below 10%), auto-unmute when raised, and clamping at `root.maxVolume` (1.5).
       - Two-tier live PipeWire query: If `wpctl status` succeeds, query `@DEFAULT_AUDIO_SINK@` volume; if PipeWire is inactive, soft-skip (D-04).
  </action>
  <verify>
    <automated>./scripts/phase41-interactions-assert.sh --section 4 && ./scripts/phase41-interactions-assert.sh --section 5</automated>
    <fails_when>test fails if notification close button AST is missing, if OTP extraction fails any of the 19 test cases, if URL extraction allows unvetted schemes, if ClockWidget.qml margins differ from 5px, if volumeCeiling in config.json is not 1.5, or if QuickSliders.qml lacks Audio.maxVolume binding or [1.0] stop indicator</fails_when>
  </verify>
  <acceptance_criteria>
    - Running `./scripts/phase41-interactions-assert.sh --section 4` passes with `FAIL=0 FINDINGS=0`.
    - Running `./scripts/phase41-interactions-assert.sh --section 5` passes with `FAIL=0 FINDINGS=0`.
    - All 19 OTP regex test cases pass headless Node.js evaluation.
    - URL extraction passes with proper scheme sanitization.
    - Clock padding margins (5px left/right) and volume ceiling (1.5 / 150%) contracts are verified.
  </acceptance_criteria>
  <done>
    Sections 4 and 5 are fully implemented, verifying notification center ergonomics, OTP parsing, clock padding, and the unified volume ceiling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Section 6 — Repository Integrity, Strict Verification, Sub-Harness Orchestration, and Zero-Churn Baseline Diffing (INTG-01, INTG-02, INTG-03, D-01, D-02, D-09, D-10, T-41-01, T-41-04)</name>
  <read_first>
    - arch/dots-hyprland.sh
    - scripts/phase38-power-profiles-assert.sh
    - scripts/phase39-media-popup-assert.sh
    - scripts/phase40-notification-interaction-assert.sh
    - scripts/phase40.1-clock-volume-assert.sh
  </read_first>
  <files>scripts/phase41-interactions-assert.sh</files>
  <action>
    Implement Section 6 (Repository Integrity), sub-harness orchestration, and post-execution git porcelain diffing in `scripts/phase41-interactions-assert.sh` (per INTG-01, INTG-02, INTG-03, D-01, D-02, D-09, D-10, T-41-01, T-41-04):

    1. Section 6 — Repository Integrity & Strict Verification (INTG-03, D-10):
       - Executed when `RUN_SECTION == 0` or `RUN_SECTION == 6`.
       - When `SYNTAX_ONLY == 0`:
         * Allocate a temporary log file: `STRICT_LOG="$(mktemp /tmp/p41-strict-XXXXXX)"` and register it in `TMP_FILES`.
         * Execute `./arch/dots-hyprland.sh verify --strict >"$STRICT_LOG" 2>&1`.
         * Check return code: must be 0.
         * Grep stdout log for completion banner: must match `=== done: FAIL=0 FINDINGS=0 ===`.
         * If both pass, log `pass "S6: ./arch/dots-hyprland.sh verify --strict passed with FAIL=0 FINDINGS=0"`.
         * If either check fails, log `fail "S6: ./arch/dots-hyprland.sh verify --strict failed"`.

    2. Sub-Harness Orchestration (D-01):
       - Executed when `RUN_SECTION == 0`, `QUICK_MODE == 0`, and `SYNTAX_ONLY == 0`.
       - Define array of the 4 milestone sub-harnesses:
         * `scripts/phase38-power-profiles-assert.sh`
         * `scripts/phase39-media-popup-assert.sh`
         * `scripts/phase40-notification-interaction-assert.sh`
         * `scripts/phase40.1-clock-volume-assert.sh`
       - For each sub-harness:
         * Confirm file exists and is executable (`test -x "$REPO_ROOT/$sub"`).
         * Execute with `--syntax` flag to verify static integrity.
         * Execute full sub-harness (`"$REPO_ROOT/$sub"`).
         * If execution exits 0, log `pass "Sub-harness $sub passed completely"`.
         * If execution fails, log `fail "Sub-harness $sub failed (exit code $?)"`.

    3. Pre/Post Git Porcelain Zero-Churn Verification (D-09, T-41-04):
       - Populate post-execution snapshot: `porcelain_snapshot > "$PORCELAIN_AFTER"`.
       - Compare pre-run and post-run snapshots: `DIFF_OUT="$(diff -u "$PORCELAIN_BEFORE" "$PORCELAIN_AFTER" || true)"`.
       - If `DIFF_OUT` is non-empty, log `fail "Working tree porcelain drift detected during test execution:\n$DIFF_OUT"`.
       - If `DIFF_OUT` is empty, log `pass "Working tree porcelain is clean (zero execution drift)"`.

    4. Final Milestone Verdict & Exit Code:
       - Print summary banner:
         `echo "=========================================="`
         `echo "Phase 41 Test Results: FAIL=$FAIL FINDINGS=$FINDINGS"`
         `echo "=========================================="`
       - Exit with code 0 if `FAIL == 0`, else exit 1.
  </action>
  <verify>
    <automated>./scripts/phase41-interactions-assert.sh --section 6 && ./scripts/phase41-interactions-assert.sh --quick</automated>
    <fails_when>test fails if ./arch/dots-hyprland.sh verify --strict returns non-zero or reports findings > 0, if any section 1-6 check fails in quick mode, or if any git working tree porcelain drift is detected</fails_when>
  </verify>
  <acceptance_criteria>
    - Running `./scripts/phase41-interactions-assert.sh --section 6` passes with `FAIL=0 FINDINGS=0`.
    - Running `./scripts/phase41-interactions-assert.sh --quick` executes all Sections 1 through 6 in < 6 seconds with `FAIL=0 FINDINGS=0`.
    - Working tree porcelain drift check confirms zero net changes from test execution.
    - Script exits with code 0 when all checks pass.
  </acceptance_criteria>
  <done>
    Section 6, sub-harness orchestration, and zero working-tree churn verification are completely implemented and proven green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full Milestone Verification Execution & Test Matrix Validation (INTG-01, INTG-02, INTG-03, D-01, D-11)</name>
  <read_first>
    - .planning/phases/41-end-to-end-verification-repository-integrity/41-CONTEXT.md
    - .planning/phases/41-end-to-end-verification-repository-integrity/41-RESEARCH.md
    - .planning/phases/41-end-to-end-verification-repository-integrity/41-VALIDATION.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>.planning/phases/41-end-to-end-verification-repository-integrity/41-VERIFICATION.md</files>
  <action>
    Execute the full Phase 41 assertion harness including all sub-harnesses (`./scripts/phase41-interactions-assert.sh`), verify that all tests pass with `FAIL=0 FINDINGS=0`, and author `41-VERIFICATION.md` (per INTG-01..03, D-11):

    1. Execute `./scripts/phase41-interactions-assert.sh` capturing complete stdout transcript and execution time.
    2. Confirm that all 6 high-level integration sections passed:
       - Section 1: Restow Symlink Isolation (INTG-01) — 11 overlays, 11 real directories, clean submodule.
       - Section 2: Power Profiles Management (POWER-01..03) — daemon, service, D-Bus cycling, state rollback.
       - Section 3: Media Popup Positioning & Clamping (MEDIA-01..02) — AST tokens, coordinate clamping math across 3 geometries.
       - Section 4: Notification Center & Smart OTP (NOTIF-01..02, NAV-01..02, OTP-01..02) — close button visibility, 19-case OTP test matrix, URL scheme sanitization.
       - Section 5: Clock Padding & Volume Ceiling (CLOCK-01, VOL-01..02) — 10px breathing room, config single source of truth, keybinds, slider, and mouse scroll ergonomics.
       - Section 6: Repository Integrity (INTG-03) — `arch/dots-hyprland.sh verify --strict` zero findings, zero porcelain churn.
    3. Confirm that all 4 milestone sub-harnesses passed:
       - `scripts/phase38-power-profiles-assert.sh`
       - `scripts/phase39-media-popup-assert.sh`
       - `scripts/phase40-notification-interaction-assert.sh`
       - `scripts/phase40.1-clock-volume-assert.sh`
    4. Compile `41-VERIFICATION.md` with:
       - Verification status (`passed`), date, domain, and scope overview.
       - Execution command transcripts for `--quick` and full suite runs.
       - Complete 17-requirement traceability matrix mapping each milestone requirement:
         * MEDIA-01 (Phase 39, MediaControls.qml dynamic pill anchoring)
         * MEDIA-02 (Phase 39, MediaControls.qml coordinate boundary clamping)
         * POWER-01 (Phase 38, power-profiles-daemon installation & systemd service)
         * POWER-02 (Phase 38, PowerProfilesToggle.qml 3-state profile cycle)
         * POWER-03 (Phase 38, package manifest & bootstrap idempotency)
         * NOTIF-01 (Phase 40, NotificationGroup.qml sidebar header 'X' close button)
         * NOTIF-02 (Phase 40, NotificationPopup.qml toast close button suppression)
         * NAV-01 (Phase 40, Notification card body click default action execution)
         * NAV-02 (Phase 40, Notification URL extraction & browser launching)
         * OTP-01 (Phase 40, NotificationUtils.qml 4-8 digit regex OTP extraction)
         * OTP-02 (Phase 40, NotificationItem.qml "Copy [Code]" action chip)
         * CLOCK-01 (Phase 40.1, ClockWidget.qml 10px horizontal breathing room)
         * VOL-01 (Phase 40.1, config.json single source of truth volume ceiling)
         * VOL-02 (Phase 40.1, QuickSliders.qml slider & Audio.qml scroll 150% ceiling)
         * INTG-01 (Phase 41, restow/quickshell leaf symlinks & pristine vendor submodule)
         * INTG-02 (Phase 41, scripts/phase41-interactions-assert.sh end-to-end harness)
         * INTG-03 (Phase 41, arch/dots-hyprland.sh verify --strict zero findings & zero churn)
       - Analysis of Reversibility & Signal Traps (proving safe power profile restoration).
       - Zero-churn working tree validation details.
  </action>
  <verify>
    <automated>./scripts/phase41-interactions-assert.sh && test -f .planning/phases/41-end-to-end-verification-repository-integrity/41-VERIFICATION.md</automated>
    <fails_when>test fails if full assertion suite returns non-zero, if any sub-harness fails, or if 41-VERIFICATION.md is missing or does not document 100% requirement coverage</fails_when>
  </verify>
  <acceptance_criteria>
    - `./scripts/phase41-interactions-assert.sh` runs the complete milestone test suite and exits 0 with `FAIL=0 FINDINGS=0`.
    - `41-VERIFICATION.md` is authored, comprehensive, and confirms all 17 milestone requirements are verified green.
    - All milestone features are proven integrated with zero working-tree churn.
  </acceptance_criteria>
  <done>
    Milestone v0.8 end-to-end verification is complete, authoritative, and documented in `41-VERIFICATION.md`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sub-Harness Execution | Test runner delegates execution to existing milestone test scripts via bash invocation. |
| External Content → Browser URL Launch | Notification text containing embedded URLs crosses into browser navigation. |
| Shell Execution → Git Working Tree | Sub-harnesses and strict verification inspect git status and repository integrity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Tampering / Privilege Escalation | Root execution / Submodule edits | high | mitigate | Fail closed if run as root (`EUID != 0`). Assert zero changes in `vendor/dots-hyprland` submodule and deploy overlays exclusively via `restow/quickshell/` leaf symlinks (INTG-01, D-05, ASVS V4). |
| T-41-02 | Denial of Service / Hardware State Pollution | Power profile switching | medium | mitigate | Capture `INITIAL_PROFILE` before cycling and bind unconditional restoration in `trap cleanup EXIT INT TERM` handler, guaranteeing initial power state is preserved on normal exit or interrupt (D-07). |
| T-41-03 | Elevation of Privilege / Malicious URL Injection | Notification URL extractor | medium | mitigate | Validate regex strictly requires `https?://` schemes and rejects `javascript:`, `file:`, `data:`, or shell metacharacters before handing off to browser launcher (NAV-02, ASVS V5). |
| T-41-04 | Working-Tree Churn | Test harness execution | low | mitigate | Store temporary outputs in `/tmp` using randomized `mktemp` templates registered in `TMP_FILES` array and cleaned up via signal trap. Snapshot git porcelain before and after execution (D-09, D-10, ASVS V14). |
</threat_model>

## Artifacts this phase produces

- `scripts/phase41-interactions-assert.sh`: Completed Milestone v0.8 Integration & Verification Engine covering Sections 1-6, sub-harness orchestration, and zero-churn diffing.
- `.planning/phases/41-end-to-end-verification-repository-integrity/41-VERIFICATION.md`: Milestone v0.8 End-to-End Verification Audit and 17-requirement traceability matrix.

## Multi-Source Coverage Audit

| Source | Item ID / Reference | Description | Status | Covered By | Notes |
|---|---|---|---|---|---|
| REQ | INTG-01 | All QML modifications deployed via `restow/quickshell/` leaf symlinks without modifying `vendor/dots-hyprland`. | COVERED | Task 2, Task 3 | Section 1 in sub-harnesses and final verification audit. |
| REQ | INTG-02 | Automated assertion test harness validates media popup positioning, power profile cycling, notification dismissal, link opening, and OTP parsing. | COVERED | Task 1, Task 2, Task 3 | Sections 4 & 5 in Task 1; Sub-harnesses in Task 2; Full execution in Task 3. |
| REQ | INTG-03 | `arch/dots-hyprland.sh verify --strict` passes with 0 findings and zero git working-tree churn. | COVERED | Task 2, Task 3 | Section 6 and pre/post porcelain snapshot comparison in Task 2; audit in Task 3. |
| CONTEXT | D-01 | Milestone v0.8 assert harness architecture with sub-harness chaining, `--quick`, and `--section <1-6>`. | COVERED | Task 2 | Chaining of phase38, 39, 40, 40.1 in Task 2. |
| CONTEXT | D-02 | 6 modular sections matching milestone domains. | COVERED | Task 1, Task 2 | Section 4 & 5 in Task 1; Section 6 in Task 2. |
| CONTEXT | D-03 | Standard GSD assert CLI flags (`--section`, `--syntax`, `--quick`, `--live-notify`), logging helpers, exit code `$FAIL`. | COVERED | Task 2 | Full summary reporting and exit code in Task 2. |
| CONTEXT | D-04 | Two-tier gating (Hard AST + Soft Live). | COVERED | Task 1 | Headless Node.js evaluation hard, live D-Bus/PipeWire soft-skip. |
| CONTEXT | D-08 | Default to headless AST/regex; optional `--live-notify` sends transient notification. | COVERED | Task 1 | Section 4 evaluates regex headlessly in Node.js VM; optional `--live-notify` supported. |
| CONTEXT | D-09 | Dual-layer git churn check (porcelain snapshot diff before/after, submodule clean). | COVERED | Task 2 | Pre/post porcelain snapshot diffing in Task 2. |
| CONTEXT | D-10 | Section 6 runs `./arch/dots-hyprland.sh verify --strict` asserting exit 0 and `FAIL=0 FINDINGS=0`. | COVERED | Task 2 | Section 6 implementation in Task 2. |
| CONTEXT | D-11 | Author `41-VERIFICATION.md` with cross-phase summary table covering Phases 38, 39, 40, 40.1, 41. | COVERED | Task 3 | Compiled in Task 3. |
| RESEARCH | T-41-03 | Malicious URL scheme injection prevention. | COVERED | Task 1 | Strict regex scheme validation in Section 4. |
| RESEARCH | T-41-04 | Working-tree churn prevention and porcelain tracking. | COVERED | Task 2 | Diff comparison asserting empty drift in Task 2. |

<verification>
Execute the section verification, quick test, and full milestone test commands:
`./scripts/phase41-interactions-assert.sh --section 4`
`./scripts/phase41-interactions-assert.sh --section 5`
`./scripts/phase41-interactions-assert.sh --section 6`
`./scripts/phase41-interactions-assert.sh --quick`
`./scripts/phase41-interactions-assert.sh`
Verify that `41-VERIFICATION.md` exists:
`test -f .planning/phases/41-end-to-end-verification-repository-integrity/41-VERIFICATION.md`
</verification>

<success_criteria>
1. `scripts/phase41-interactions-assert.sh` is complete, executable, and passes all 6 sections.
2. Section 4 validates notification close button visibility, suppresses toast close button, and passes all 19 OTP regex test cases.
3. Section 5 validates ClockWidget 10px breathing room, config.json volume ceiling 1.5, keybinds, and slider ergonomics.
4. Section 6 executes `./arch/dots-hyprland.sh verify --strict` with 0 findings and exit code 0.
5. All 4 sub-harnesses (`phase38`, `phase39`, `phase40`, `phase40.1`) execute and pass completely.
6. Git working tree porcelain comparison proves zero net execution churn.
7. `41-VERIFICATION.md` documents 100% verification across all 17 milestone requirements.
</success_criteria>

<output>
Create `.planning/phases/41-end-to-end-verification-repository-integrity/41-02-SUMMARY.md` when done
</output>
