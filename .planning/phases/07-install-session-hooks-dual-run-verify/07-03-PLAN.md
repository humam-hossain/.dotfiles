---
phase: 07-install-session-hooks-dual-run-verify
plan: 03
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - .config/hypr/hyprland.conf
  - ~/.config/hypr/hyprland.conf
autonomous: false
requirements:
  - LIVE-02
  - LIVE-03
  - LIVE-04
user_setup:
  - service: hyprland-session
    why: "LIVE-04 requires operator visual confirmation of ii shell chrome on screen"
    env_vars: []
    dashboard_config:
      - task: "After hyprctl reload + qs restart, confirm Material/ii bar or shell chrome is visible alongside Waybar"
        location: "Hyprland session display"

must_haves:
  truths:
    - "Repo .config/hypr/hyprland.conf has env = ILLOGICAL_IMPULSE_VIRTUAL_ENV,~/.local/state/quickshell/.venv (D-12) and exec-once = qs -c ii (D-13) inline (D-10), committed (D-11)"
    - "waybar exec-once line still present; qs -c ii is additive dual-run (D-15 / LIVE-02 adjacency — both bars OK even if they overlap)"
    - "Live ~/.config/hypr/hyprland.conf matches repo via cmp -s after single-file sync (LIVE-02 ordering: hooks committed and live synced before LIVE-04 verify per D-14)"
    - "After hyprctl reload + qs restart with env prefix (D-17 / A1): waybar still running (LIVE-03); qs running with -c ii; qs /proc environ has ILLOGICAL_IMPULSE_VIRTUAL_ENV (LIVE-04 parts 1–2)"
    - "Operator confirms visible ii shell chrome on screen (LIVE-04 part 3 / D-16 three-part bar) — flagged assumption: visual chrome is human-needed"
    - "LIVE-03 dual-run process bar: waybar required; swaync optional if configured (flagged assumption: waybar + optional swaync are the verify bar)"
    - "venv path still present at ~/.local/state/quickshell/.venv (LIVE-02 empty edge continuity from 07-02)"
  artifacts:
    - path: ".config/hypr/hyprland.conf"
      provides: "Versioned personal hypr SoT with ii env + qs -c ii exec-once"
      contains: "env = ILLOGICAL_IMPULSE_VIRTUAL_ENV,~/.local/state/quickshell/.venv; exec-once = qs -c ii; existing waybar line"
    - path: "~/.config/hypr/hyprland.conf"
      provides: "Live session conf matching repo"
      contains: "Same two hooks; cmp -s equal to repo"
  key_links:
    - from: "repo hyprland.conf hooks"
      to: "live ~/.config/hypr/hyprland.conf"
      via: "cp -f + cmp -s single-file sync"
    - from: "exec-once = qs -c ii"
      to: "~/.config/quickshell/ii/shell.qml"
      via: "qs -c ii config name after LIVE-01 tree"
    - from: "env = ILLOGICAL_IMPULSE_VIRTUAL_ENV"
      to: "~/.local/state/quickshell/.venv"
      via: "mid-session env-prefix on qs (A1); compositor-level on next full Hyprland start"
    - from: "waybar exec-once"
      to: "LIVE-03 dual-run"
      via: "unchanged CORE UTILS line + additive qs line"
  prohibitions:
    - "Must NOT remove waybar exec-once (LIVE-03 / D-15)"
    - "Must NOT install or replace full ii hyprland.lua tree (D-09)"
    - "Must NOT add source = snippet files for hooks (D-10 inline only)"
    - "Must NOT retire in-repo .config/quickshell this phase (D-04 / Phase 8)"
    - "Must NOT run arch/quickshell.sh"
    - "Must NOT assume hyprctl reload alone re-exports env = into compositor environ (A1) — use env-prefixed qs for mid-session LIVE-04"
    - "Must NOT treat dual-run bar overlap as failure (D-15)"
    - "Must NOT skip hooks and only manually launch qs for LIVE-04 (D-14 hooks first then verify)"
---

<objective>
Add personal Hyprland session hooks (venv env + `qs -c ii` exec-once), commit and sync live conf, apply mid-session via `hyprctl reload` + env-prefixed qs restart, then verify LIVE-02 / LIVE-03 / LIVE-04 dual-run — per D-09..D-17.

Purpose: Own session startup for installed ii beside Waybar without ii hypr takeover; prove dual-run and visible chrome.

Output: Versioned hooks in `.config/hypr/hyprland.conf`; live conf match; waybar + qs -c ii running; operator chrome confirmation; phase success criteria 2–4 true.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-CONTEXT.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-RESEARCH.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-PATTERNS.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-VALIDATION.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-01-SUMMARY.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-02-SUMMARY.md
@.config/hypr/hyprland.conf
</context>

## Phase Goal

**Land a running illogical-impulse shell beside Waybar using personal session ownership.** This plan wires personal session hooks and closes LIVE-02..04 dual-run verify.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Real installed QS tree | `~/.config/quickshell/ii/shell.qml` | 07-02 |
| Personal hypr hooks (repo SoT) | `.config/hypr/hyprland.conf` | **07-03** |
| Live hypr conf sync | `~/.config/hypr/hyprland.conf` | **07-03** |
| Dual-run LIVE-02..04 evidence | process/env asserts + operator chrome | **07-03** |
| Plan SUMMARY | `07-03-SUMMARY.md` | **07-03** |

## Tasks

<task type="auto">
  <name>Task 1: Inline env + exec-once hooks in personal hyprland.conf; commit (D-09..D-13, D-11)</name>
  <files>.config/hypr/hyprland.conf</files>
  <read_first>
    - .config/hypr/hyprland.conf (AUTOSTART CORE UTILS ~line 64 waybar; ENVIRONMENT VARIABLES ~lines 98–105)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-CONTEXT.md (D-09, D-10, D-11, D-12, D-13, D-15)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-RESEARCH.md (Pattern 3 Personal hypr hooks; recommended lines and placement)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-PATTERNS.md (hypr conf placement pattern; dual-run session policy)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-02-SUMMARY.md (LIVE-01 green prerequisite)
  </read_first>
  <action>
    Keep personal hypr as SoT (D-09). Add two hooks **inline** only (D-10) — no separate sourced snippet, no ii hyprland.lua install:

    1. Under `### ENVIRONMENT VARIABLES ###` near existing `env = XCURSOR_*` lines, add (match comma style — no space after comma, per personal conf):
       - Comment: illogical-impulse Phase 7 LIVE-02 — required when skip-hyprland skips ii env.lua
       - Exact line: `env = ILLOGICAL_IMPULSE_VIRTUAL_ENV,~/.local/state/quickshell/.venv` (D-12)
    2. Under `# INFO: CORE UTILS`, **immediately after** the existing `exec-once = waybar & swaync & hyprpaper &` line (do not remove or alter that line — D-13, D-15, LIVE-03):
       - Comment: Live ii shell dual-run with waybar — Phase 7 LIVE-02
       - Exact line: `exec-once = qs -c ii`
    3. Do not remove waybar, swaync, hyprpaper, clipboard, polkit, or other existing exec-once lines.
    4. Do not add `source =` includes. Do not bulk-copy ii hypr tree.
    5. Commit hooks into `.dotfiles` this phase (D-11) so LIVE-02 is versioned — commit message e.g. `feat(07-03): add ii venv env + qs -c ii exec-once to hyprland.conf`. Include only the hypr conf change unless SUMMARY needs separate docs commit via gsd-tools.
    6. Grep self-check on repo file before live sync (Task 2): env line, `exec-once = qs -c ii`, and waybar exec-once all present.
    7. After commit: assert D-11 via `git log -1 --oneline -- .config/hypr/hyprland.conf` (non-empty) and `git status --porcelain -- .config/hypr/hyprland.conf` empty.
  </action>
  <verify>
    <automated>grep -E '^\s*env\s*=\s*ILLOGICAL_IMPULSE_VIRTUAL_ENV,\s*~/.local/state/quickshell/.venv' .config/hypr/hyprland.conf && grep -E '^\s*exec-once\s*=\s*qs -c ii\b' .config/hypr/hyprland.conf && grep -E '^\s*exec-once\s*=\s*waybar' .config/hypr/hyprland.conf && test -n "$(git log -1 --oneline -- .config/hypr/hyprland.conf)" && test -z "$(git status --porcelain -- .config/hypr/hyprland.conf)" && echo "[VERIFY] OK LIVE-02 repo conf hooks + D-11 commit"</automated>
  </verify>
  <acceptance_criteria>
    - Repo conf contains `env = ILLOGICAL_IMPULSE_VIRTUAL_ENV,~/.local/state/quickshell/.venv` (optional whitespace after comma still greppable; prefer no space).
    - Repo conf contains `exec-once = qs -c ii` as its own line.
    - Repo conf still contains an `exec-once` line that starts waybar.
    - No new `source =` hook snippet file was added for this purpose.
    - Change is committed to git in this plan (D-11): `git log -1 --oneline -- .config/hypr/hyprland.conf` non-empty and `git status --porcelain -- .config/hypr/hyprland.conf` empty.
  </acceptance_criteria>
  <done>Versioned personal hypr hooks present; waybar line preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Sync live conf; hyprctl reload; restart qs with env prefix (D-14, D-17, A1)</name>
  <files>~/.config/hypr/hyprland.conf</files>
  <read_first>
    - .config/hypr/hyprland.conf (after Task 1)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-CONTEXT.md (D-14, D-17, D-16)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-RESEARCH.md (Pattern 4 Mid-session apply; Pitfall 4; Assumptions A1)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-PATTERNS.md (live single-file sync; mid-session apply block)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-VALIDATION.md (07-03 row; LIVE-02 cmp)
  </read_first>
  <action>
    Order is hooks first, then apply, then verify (D-14) — do not skip to manual qs-only without hooks on disk:

    1. Single-file live sync (prefer over full `arch/hyprland.sh` which also pulls packages/swaync):
       `cp -f .config/hypr/hyprland.conf "$HOME/.config/hypr/hyprland.conf"`
       `cmp -s .config/hypr/hyprland.conf "$HOME/.config/hypr/hyprland.conf"` must succeed.
    2. Apply session changes (D-17):
       - `hyprctl reload`
       - Capture `hyprctl configerrors` to `/tmp/p7-hypr-errors.txt`. Fail if the file is non-empty after stripping blank lines, or if it mentions `ILLOGICAL_IMPULSE` / `qs -c ii` / `quickshell` parse failures.
    3. Mid-session env semantics (A1): `hyprctl reload` does **not** re-export `env =` into the running Hyprland process. For LIVE-04 mid-session, restart qs with env prefix:
       - `pkill -x qs 2>/dev/null || true`
       - `ILLOGICAL_IMPULSE_VIRTUAL_ENV="${HOME}/.local/state/quickshell/.venv" qs -c ii -d`
       - Note in SUMMARY: full compositor-level env inheritance is verified on next Hyprland start (logout/login optional; do not block phase on re-login given D-17).
    4. Do not remove waybar process. Do not run `arch/quickshell.sh`. Do not retire in-repo product.
  </action>
  <verify>
    <automated>cmp -s .config/hypr/hyprland.conf "$HOME/.config/hypr/hyprland.conf" && hyprctl configerrors 2>/dev/null | tee /tmp/p7-hypr-errors.txt && test -z "$(grep -v '^[[:space:]]*$' /tmp/p7-hypr-errors.txt)" && pgrep -x qs >/dev/null && pgrep -a qs | grep -E -- '-c ii|\bii\b' && tr '\0' '\n' < /proc/$(pgrep -n -x qs)/environ | grep -q ILLOGICAL_IMPULSE_VIRTUAL_ENV && echo "[VERIFY] OK live sync + qs env + clean configerrors"</automated>
  </verify>
  <acceptance_criteria>
    - `cmp -s` repo vs live hyprland.conf exits 0.
    - `hyprctl configerrors` output is empty (no parse errors); file `/tmp/p7-hypr-errors.txt` has no non-blank lines after capture.
    - At least one `qs` process is running with config `ii` in cmdline.
    - qs process environ contains `ILLOGICAL_IMPULSE_VIRTUAL_ENV` pointing at the quickshell `.venv` path.
    - waybar was not intentionally killed by this task.
  </acceptance_criteria>
  <done>Live conf matches repo; qs running under ii with venv env for mid-session verify.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: LIVE-02..04 dual-run automated bar + operator chrome (D-15, D-16)</name>
  <files>.config/hypr/hyprland.conf, ~/.config/hypr/hyprland.conf</files>
  <read_first>
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-VALIDATION.md (LIVE-02..04 map)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-RESEARCH.md (LIVE assert Code Examples)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-CONTEXT.md (D-15, D-16)
  </read_first>
  <what-built>
    Personal hypr hooks committed and live-synced; hyprctl reloaded; qs restarted with `ILLOGICAL_IMPULSE_VIRTUAL_ENV` and `-c ii`. Automated LIVE-02 conf greps, LIVE-03 waybar process, LIVE-04 process+env asserts should already be runnable; this checkpoint closes the three-part LIVE-04 bar with visible chrome (D-16) and dual-run policy (D-15).
  </what-built>
  <action>
    Run the full LIVE-02..04 automated assert block from REPO_ROOT (conf greps, cmp, pgrep waybar, pgrep qs with ii, qs environ ILLOGICAL_IMPULSE_VIRTUAL_ENV, .venv directory). Then obtain operator confirmation that Material/ii shell chrome is visible on screen alongside Waybar (D-16 part 3). Dual-run bar overlap is success not failure (D-15). Do not remove waybar. Do not re-run arch/quickshell.sh. Do not retire in-repo .config/quickshell. Record results and operator approval in 07-03-SUMMARY.

    LIVE-03 session pieces policy (explicit non-assert rationale):
    - **waybar** — hard required process assert (`pgrep -x waybar`); line preserved in conf.
    - **swaync** — soft optional: if the CORE UTILS `exec-once` still launches swaync, run `pgrep -x swaync || pgrep -a swaync` and note result in SUMMARY; do not fail LIVE-03 if swaync is absent (operator may have disabled notifications).
    - **rofi** — on-demand launcher (keybind), not a session-long daemon; **no process assert** (would false-fail when not open).
    Flagged assumptions (specless probe): LIVE-03 hard bar is waybar (+ optional swaync soft-check); LIVE-04 visual chrome is human-needed.
  </action>
  <how-to-verify>
    Run from REPO_ROOT (automated portion — executor should run these before asking):

    1. LIVE-02 conf:
       - `grep -E 'env = ILLOGICAL_IMPULSE_VIRTUAL_ENV,' .config/hypr/hyprland.conf`
       - `grep -E 'exec-once = qs -c ii' .config/hypr/hyprland.conf`
       - `grep -E 'exec-once = waybar' .config/hypr/hyprland.conf`
       - `cmp -s .config/hypr/hyprland.conf ~/.config/hypr/hyprland.conf`
    2. LIVE-03 dual-run:
       - `pgrep -x waybar` must succeed (required hard bar).
       - Soft: `pgrep -x swaync || pgrep -a swaync` — record in SUMMARY; do not fail if missing.
       - Rofi: no process assert (launch-on-keybind only; not always-on session chrome).
       - Overlap of waybar + ii bar is **OK** (D-15) — do not fail for two bars.
    3. LIVE-04 (1)(2):
       - `pgrep -a qs | grep -E -- '-c ii|\bii\b'`
       - `tr '\0' '\n' < /proc/$(pgrep -n -x qs)/environ | grep ILLOGICAL_IMPULSE_VIRTUAL_ENV`
       - `test -d ~/.local/state/quickshell/.venv`
    4. LIVE-04 (3) operator visual (manual — required):
       - Look at the Hyprland session screen.
       - Confirm Material/ii bar or shell chrome from `qs -c ii` is **visible**.
       - Waybar may still be primary chrome; both visible is success even if they overlap.
  </how-to-verify>
  <verify>
    <automated>grep -E 'env = ILLOGICAL_IMPULSE_VIRTUAL_ENV,' .config/hypr/hyprland.conf && grep -E 'exec-once = qs -c ii' .config/hypr/hyprland.conf && grep -E 'exec-once = waybar' .config/hypr/hyprland.conf && cmp -s .config/hypr/hyprland.conf "$HOME/.config/hypr/hyprland.conf" && pgrep -x waybar >/dev/null && pgrep -a qs | grep -E -- '-c ii|\bii\b' && tr '\0' '\n' < /proc/$(pgrep -n -x qs)/environ | grep -q ILLOGICAL_IMPULSE_VIRTUAL_ENV && test -d "$HOME/.local/state/quickshell/.venv" && echo "[VERIFY] OK LIVE-02..04 automated (chrome is human; swaync soft; rofi no process assert)"</automated>
  </verify>
  <resume-signal>Type "approved" if LIVE-02..04 hold including visible ii chrome, or describe what failed</resume-signal>
  <acceptance_criteria>
    - Automated LIVE-02 conf greps and cmp succeed.
    - `pgrep -x waybar` succeeds (LIVE-03 hard bar).
    - swaync soft-check result recorded in SUMMARY when CORE UTILS still launches it; absence is not a LIVE-03 failure.
    - rofi has no process assert (on-demand keybind launcher only).
    - qs process shows ii config and has ILLOGICAL_IMPULSE_VIRTUAL_ENV in environ.
    - Operator confirms visible ii chrome on screen (D-16 part 3).
    - Dual-run overlap was not treated as failure (D-15).
  </acceptance_criteria>
  <done>LIVE-02, LIVE-03, LIVE-04 all three D-16 parts green; Phase 7 dual-run verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo hypr SoT → live `~/.config/hypr` | Single-file copy must not wipe unrelated live-only hypr files |
| Hyprland conf → session processes | exec-once / env affect autostart and child environ |
| Operator visual confirm → LIVE-04 sign-off | Human is sole authority for on-screen chrome |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-02 | Tampering | hypr conf takeover / rename | high | mitigate | D-09 personal SoT only; additive hooks; no ii hyprland.lua; --skip-hyprland already held from 07-02 |
| T-7-01 | Tampering | QS path regression to symlink | high | mitigate | Do not run arch/quickshell.sh; LIVE-01 tree remains real dir |
| T-7-06 | Tampering | re-symlink installer | high | mitigate | Explicit forbid arch/quickshell.sh in actions and prohibitions |
| T-7-03 | Tampering / data loss | skip-backup | medium | accept | No install in this plan; residual owned by 07-02 policy |
| T-7-05 | Tampering | command injection | low | accept | No new flag parsing; shell commands are fixed literals |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this plan (N/A) |
</threat_model>

<verification>
Repo+live conf greps and cmp (LIVE-02); pgrep waybar (LIVE-03); pgrep qs -c ii + /proc environ ILLOGICAL_IMPULSE_VIRTUAL_ENV (LIVE-04 1–2); operator approved visible chrome (LIVE-04 3).
</verification>

<success_criteria>
Phase 7 ROADMAP success criteria 2–4 true: personal conf sets venv env and starts qs -c ii; Waybar dual-run intact; operator observes installed ii chrome. Combined with 07-02 LIVE-01, all LIVE-01..04 satisfied.
</success_criteria>

<output>
Create `.planning/phases/07-install-session-hooks-dual-run-verify/07-03-SUMMARY.md` when done
</output>
