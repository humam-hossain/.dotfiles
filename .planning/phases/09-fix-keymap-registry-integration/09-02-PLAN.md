---
phase: 09-fix-keymap-registry-integration
plan: "02"
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .config/nvim/lua/core/keymaps/registry.lua
autonomous: true
requirements:
  - KEY-01
  - KEY-02
  - KEY-03

must_haves:
  truths:
    - "All <leader> keys use snacks pickers"
    - "LSP nav keys use Snacks.picker.lsp_*"
    - "No remaining neo-tree command references"
  artifacts:
    - path: ".config/nvim/lua/core/keymaps/registry.lua"
      provides: "updated lazy keymap entries"
      contains: "Snacks.picker"
  key_links:
    - from: "registry.lua lazy entries"
      to: "Snacks.picker"
      via: "action function calls"
      pattern: "Snacks.picker"
---

<objective>
Update all remaining lazy keymap entries to use Snacks pickers, completing the snacks-conventional key layout (D-09 through D-14).

Purpose: Replace remaining neo-tree references with snacks equivalents, add LSP nav keys, adopt full snacks key convention.
Output: Complete snacks-powered keymap system.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-fix-keymap-registry-integration/09-CONTEXT.md

Snacks picker APIs:
- Snacks.picker.files() — file search
- Snacks.picker.grep() — live grep
- Snacks.picker.buffers() — buffer list
- Snacks.picker.git_status() — git status (replaces neo-tree)
- Snacks.picker.lsp_definitions() — gd
- Snacks.picker.lsp_references() — gr  
- Snacks.picker.lsp_implementations() — gI
- Snacks.picker.lsp_type_definitions() — gy
- Snacks.picker.lsp_symbols() — document symbols
- Snacks.picker.lsp_workspace_symbols() — workspace symbols
- Snacks.picker.lines() — grep current buffer

Current keys needing update (from registry.lua M.lazy):
- explorer.toggle → Snacks.explorer()
- explorer.reveal → (remove, no snacks equivalent?)
- explorer.git_status → Snacks.picker.git_status()
- explorer.buffers → Snacks.picker.buffers()
- explorer.reveal_file → (remove or Snacks.explorer with reveal)
- git.commits → (remove, no snacks equivalent?)

New keys to add (D-13):
- gd → Snacks.picker.lsp_definitions()
- gr → Snacks.picker.lsp_references()
- gI → Snacks.picker.lsp_implementations()
- gy → Snacks.picker.lsp_type_definitions()

New git keys (D-14):
- <leader>gl → Snacks.picker.git_log()?
- <leader>gb → branches
- <leader>gd → diff hunks
- <leader>gf → git log file
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update explorer entries to use Snacks</name>
  <files>.config/nvim/lua/core/keymaps/registry.lua</files>
  <action>
Update M.lazy explorer entries to use snacks:

1. explorer.toggle (already partially addressed in plan 01):
   - Ensure action = `function() Snacks.explorer() end`
   - plugin = "folke/snacks.nvim"

2. explorer.git_status (was <leader>ngs → neo-tree):
   - lhs changes to "<leader>gs" per D-11
   - domain changes to "g" (git domain)
   - action = `function() Snacks.picker.git_status() end`

3. explorer.buffers (was <leader>nb → neo-tree):
   - lhs changes to "<leader>," per D-12
   - domain = "b" (buffer domain)
   - This may conflict with <leader><leader>! See Task 2.

4. explorer.reveal, explorer.reveal_file:
   - Remove — no direct snacks equivalent
   - User can still use Snacks.explorer() and navigate

5. git.commits:
   - Remove — no snacks equivalent, lazygit handles most git actions
  </action>
  <verify>
    <automated>grep -c "Snacks.picker\|Snacks.explorer" .config/nvim/lua/core/keymaps/registry.lua</automated>
  </verify>
  <done>All explorer entries use Snacks APIs</done>
</task>

<task type="auto">
  <name>Task 2: Resolve key conflict D-15</name>
  <files>.config/nvim/lua/core/keymaps/registry.lua</files>
  <action>
Resolve key conflict: D-15 mentions:
- Current `<leader><leader>` → search.buffers (snacks picker)
- New `<leader>,` → buffers picker (per D-12)

Decision needed: Choose ONE buffer picker key:

Option A (keep <leader><leader>, remove <leader>,):
- Keep current `<leader><leader>` as buffers picker
- Remove `<leader>,` entry

Option B (keep <leader>, remove <leader><leader>):
- Remove `<leader><leader>` entry  
- Add new `<leader>,` as buffers picker

Per D-12 text: "'<leader>,' → Snacks.picker.buffers()" — this seems to be the decision.

IMPLEMENTATION:
- Remove search.buffers entry for `<leader><leader>` (or change its lhs to something else)
- Add/update buffers entry for `<leader>,`:
  ```lua
  {
    id = "buffers.list",
    lhs = "<leader>,",
    mode = "n",
    desc = "Find existing buffers",
    domain = "b",
    scope = "lazy",
    plugin = "folke/snacks.nvim",
    action = function() Snacks.picker.buffers() end,
  },
  ```
  </action>
  <verify>
    <automated>grep -E "buffers.*<leader>,|<leader>,.*buffers" .config/nvim/lua/core/keymaps/registry.lua</automated>
  </verify>
  <done>Buffer picker key resolved — 1 key bound to Snacks.picker.buffers()</done>
</task>

<task type="auto">
  <name>Task 3: Add LSP nav keys (D-13)</name>
  <files>.config/nvim/lua/core/keymaps/registry.lua</files>
  <action>
Add LSP navigation keys to M.lazy (these are in D-13, already present but verify they're correct):

1. gd → Snacks.picker.lsp_definitions()
2. gr → Snacks.picker.lsp_references() 
3. gI → Snacks.picker.lsp_implementations()
4. gy → Snacks.picker.lsp_type_definitions()

These should already exist in M.buffer (scope = "buffer", attached on LspAttach):
- lsp.definition
- lsp.references
- lsp.implementations
- lsp.typedefs

Verify they use Snacks.picker.* OR if they should be lazy keys instead:
- Check current actions
- If using vim.lsp.buf.*, update to Snacks.picker.*
  </action>
  <verify>
    <automated>grep -E "lsp_(definition|references|implementations|typedefs)" .config/nvim/lua/core/keymaps/registry.lua | wc -l</automated>
  </verify>
  <done>4 LSP nav keys present, using Snacks.picker.lsp_*</done>
</task>

<task type="auto">
  <name>Task 4: Add git picker keys (D-14)</name>
  <files>.config/nvim/lua/core/keymaps/registry.lua</files>
  <action>
Add additional git picker keys to M.lazy:

Per D-14: `<leader>gl` (git log), `<leader>gb` (branches), `<leader>gd` (diff hunks), `<leader>gf` (git log file)

Check if snacks picker supports these:
- Snacks.picker.git_log() — may not exist directly
- For branches: might use terminal or lazygit

IMPLEMENTATION:
- Research: check snacks picker available functions
- Add entries that work:
  - `<leader>gl`: Try `Snacks.picker.lazygit()` with git log view, or use lazygit
  - `<leader>gb`: may not have native picker — try or remove
  - `<leader>gd`: hunk picker may exist in gitsigns or snacks
  - `<leader>gf`: git log file

Conservative approach: Add only working entries, mark others for future update if snacks adds support.
  </action>
  <verify>
    <automated>grep -E "git_log|branches|diff.hunks" .config/nvim/lua/core/keymaps/registry.lua</automated>
  </verify>
  <done>Working git picker keys added</done>
</task>

</tasks>

<verification>
- [ ] explorer entries updated to Snacks
- [ ] Key conflict resolved
- [ ] LSP nav using Snacks.picker
- [ ] Git picker keys available (subset)
</verification>

<success_criteria>
All keymap entries use Snacks APIs, no neo-tree references remain, buffer picker key resolved consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/09-fix-keymap-registry-integration/09-02-SUMMARY.md`
</output>