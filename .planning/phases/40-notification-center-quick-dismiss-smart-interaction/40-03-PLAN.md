---
phase: 40-notification-center-quick-dismiss-smart-interaction
plan: 03
type: execute
wave: 1
gap_closure: true
gap_ids:
  - G-40-10
files_modified:
  - restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml
  - restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml
  - restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml
  - scripts/phase40-notification-interaction-assert.sh
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - OTP-01
  - OTP-02
  - NAV-01
  - INTG-02

estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "NotificationUtils.extractOtpCode executes cleanly without SyntaxError in Qt Quick QV4 JavaScript engine by eliminating RegExp lookbehind syntax (?<!...)."
    - "NotificationUtils.extractOtpCode passes 100% of the 19 test cases (14 positive, 5 negative) in both Node.js and Quickshell environments."
    - "NotificationGroup.qml imports qs to ensure GlobalStates is defined during notification activation."
    - "NotificationGroup.qml displays 1-click 'X' close button on single notifications across both desktop toasts and sidebar cards (visible: !root.multipleNotifications), while expandButton is visible exclusively for multiple notifications (visible: root.multipleNotifications)."
    - "NotificationGroup.qml and NotificationItem.qml eliminate height animation from 0 on initial component creation, rendering toast popups immediately at target height without lag or stutter."
    - "Full 5-section test suite scripts/phase40-notification-interaction-assert.sh and ./arch/dots-hyprland.sh verify --strict pass with FAIL=0 FINDINGS=0."
  artifacts:
    - path: "restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml"
      provides: "QV4-compatible regex OTP extraction without lookbehinds"
      contains: "extractOtpCode"
    - path: "restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml"
      provides: "Toast and sidebar single-notification quick dismiss with 'import qs' and smooth non-lagging height initialization"
      contains: "closeButton"
    - path: "restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml"
      provides: "Instant item height initialization and reliable OTP chip rendering"
      contains: "collapsedOtpChip"
    - path: "scripts/phase40-notification-interaction-assert.sh"
      provides: "Test harness with QV4 regex syntax guard and updated AST property assertions"
      contains: "phase40-notification-interaction-assert"
  key_links:
    - from: "restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml"
      to: "GlobalStates"
      via: "import qs"
      pattern: "import qs"
    - from: "restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml"
      to: "restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml"
      via: "NotificationUtils.extractOtpCode returning valid OTP without SyntaxError"
      pattern: "NotificationUtils\\.extractOtpCode"
---

# Phase 40 Plan 03: Gap Closure — Fix OTP QV4 SyntaxError, Desktop Toast 'X' Button, and Rendering Lag (G-40-10)

## Objective

Resolve UAT Gap `G-40-10`:
1. Fix `NotificationUtils.extractOtpCode` by eliminating RegExp lookbehinds `(?<!...)` unsupported by Qt Quick's QV4 engine, restoring OTP chip rendering.
2. Enable the 1-click 'X' close button on single desktop toast popups (`NotificationGroup.qml`) and add missing `import qs`.
3. Eliminate initial height animation from 0 on toast creation (`NotificationGroup.qml` and `NotificationItem.qml`) preventing toast rendering lag, surface mask thrashing, and visual freeze.
4. Update `scripts/phase40-notification-interaction-assert.sh` to validate QV4 compatibility and assert all 5 sections pass.

## Execution Context

@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-UAT.md
@.planning/debug/notification-interaction-gaps.md
@restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml
@restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml
@restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml
@scripts/phase40-notification-interaction-assert.sh

## Tasks

<tasks>

<task type="auto">
  <name>Task 1: Replace lookbehind regex in NotificationUtils.qml with QV4-compatible pattern (OTP-01, G-40-10)</name>
  <read_first>
    - restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml
    - .planning/debug/notification-interaction-gaps.md
    - scripts/phase40-notification-interaction-assert.sh
  </read_first>
  <action>
    1. In `NotificationUtils.qml`, refactor `extractOtpCode(body, summary)`:
       - Qt Quick QV4 engine throws `SyntaxError: Invalid regular expression` on `(?<!...)` negative lookbehinds.
       - Replace the lookbehind patterns `(?<![-/0-9])` with standard capturing groups or boundary tokens:
         * Use `(?:^|[^0-9\-\/])` as a prefix non-capturing or capturing group, e.g.:
           `(?:^|[^0-9\-\/])(` + codePattern + `)(?![-/0-9])`
         * Adjust `reKeywordBefore`, `reCodeBefore`, `reProximity`, and `reProximityReverse` so the captured group index accurately extracts the code string.
         * Trim and sanitize the matched code string.
       - Verify that all 19 test cases in Section 3 continue to pass:
         * 14 positive cases: 4-digit PIN, 6-digit standard, 8-digit auth, hyphenated (123-456), service-prefixed (G-829104), proximity, keywords (one-time password, passcode, verify, OTP, auth code, security code), HTML formatted body.
         * 5 negative cases: calendar dates, timestamps, phone numbers, order IDs without security keywords, counter metrics.
    2. Deploy via restow:
       `stow --verbose=5 --no-folding -d restow quickshell`
  </action>
  <verify>
    ./scripts/phase40-notification-interaction-assert.sh --section 3
  </verify>
  <commit_msg>fix(40-03): replace lookbehind regex in NotificationUtils with QV4 compatible pattern</commit_msg>
</task>

<task type="auto">
  <name>Task 2: Fix NotificationGroup.qml and NotificationItem.qml toast close button, imports, and initial height lag (NOTIF-01, NOTIF-02, NAV-01, G-40-10)</name>
  <read_first>
    - restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationGroup.qml
    - restow/quickshell/.config/quickshell/ii/modules/common/widgets/NotificationItem.qml
    - .planning/debug/notification-interaction-gaps.md
  </read_first>
  <action>
    1. In `NotificationGroup.qml`:
       - Add `import qs` at the top of the file so `GlobalStates` is in scope for `activateNotification()`.
       - Update `closeButton.visible`: change from `!root.multipleNotifications && !root.popup` to `!root.multipleNotifications` so single notifications on toast popups also have the 1-click 'X' close button.
       - Update `expandButton.visible`: change from `root.multipleNotifications || root.popup` to `root.multipleNotifications`.
       - Fix height animation jank / initial freeze:
         * In `NotificationGroup.qml`, ensure `implicitHeightAnim.enabled` is `false` initially, and is only enabled when `toggleExpanded()` is invoked (already set, verify no other trigger enables it on component mount).
    2. In `NotificationItem.qml`:
       - On component creation, `implicitHeight: expanded ? (contentColumn.implicitHeight + padding * 2) : contentColumn.implicitHeight`.
       - `Behavior on implicitHeight` currently animates from 0 over 500ms using `Appearance.animation.elementMove` (bouncy cubic bezier). This causes continuous surface mask resizing in `NotificationPopup.qml` (`mask: Region { item: listview.contentItem }`).
       - Add a property `property bool initialized: false` with `Component.onCompleted: { Qt.callLater(() => { initialized = true; }); }`.
       - Gate `Behavior on implicitHeight`: `enabled: root.initialized` so initial item height resolves immediately on mount without animating from 0.
    3. Deploy via restow:
       `stow --verbose=5 --no-folding -d restow quickshell`
  </action>
  <verify>
    ./scripts/phase40-notification-interaction-assert.sh --section 1
  </verify>
  <commit_msg>fix(40-03): enable toast close button, fix GlobalStates import, and remove initial height animation lag</commit_msg>
</task>

<task type="auto">
  <name>Task 3: Update assertion test harness and verify 100% pass rate (INTG-02, G-40-10)</name>
  <read_first>
    - scripts/phase40-notification-interaction-assert.sh
  </read_first>
  <action>
    1. In `scripts/phase40-notification-interaction-assert.sh`:
       - In Section 2:
         * Update closeButton check: assert `visible:\s*!root\.multipleNotifications\b` without `!root.popup`.
         * Update expandButton check: assert `visible:\s*root\.multipleNotifications\b`.
         * Add check asserting `import qs\b` in `NotificationGroup.qml`.
         * Add check asserting NO RegExp lookbehinds `\(\?<!` or `\(\?<=` exist in `NotificationUtils.qml`.
       - In Section 3:
         * Ensure all 19 test cases run and pass.
    2. Run full test suite:
       `./scripts/phase40-notification-interaction-assert.sh`
    3. Run strict repository verification:
       `./arch/dots-hyprland.sh verify --strict`
  </action>
  <verify>
    ./scripts/phase40-notification-interaction-assert.sh && ./arch/dots-hyprland.sh verify --strict
  </verify>
  <commit_msg>test(40-03): update phase40 assertion harness with QV4 regex guard and new visibility rules</commit_msg>
</task>

</tasks>
