---
phase: 40-notification-center-quick-dismiss-smart-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml
  - scripts/phase40-notification-interaction-assert.sh
autonomous: true
requirements:
  - OTP-01
  - NAV-02

estimate:
  tokens: 38000
  raw_tokens: 18000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "scripts/phase40-notification-interaction-assert.sh provides a 5-section test harness with --section and --syntax flags, non-root check, and porcelain baseline tracking."
    - "NotificationUtils.qml restow overlay exports extractOtpCode(body, summary) detecting 4-8 digit standalone, hyphenated, and service-prefixed verification codes anchored to security keywords (OTP-01, D-07)."
    - "extractOtpCode strictly rejects non-OTP numerical sequences such as calendar dates, timestamps, phone numbers, order numbers, and counter metrics (T-40-01, D-07)."
    - "NotificationUtils.qml restow overlay exports extractUrl(body) extracting destination URLs from Chromium HTML anchors (<a href='...'>) and raw http(s) URLs with trailing punctuation removal (NAV-02, D-05)."
    - "extractUrl enforces an http/https scheme whitelist, rejecting dangerous protocols such as javascript:, file:, data:, or shell payloads (T-40-03)."
    - "NotificationUtils.qml is deployed as a leaf symlink via restow/quickshell/ without directory folding or vendor/dots-hyprland submodule churn (INTG-01, D-10)."
  artifacts:
    - path: "scripts/phase40-notification-interaction-assert.sh"
      provides: "Automated 5-section test harness validating symlinks, static AST properties, OTP regex test matrix, URL extraction, and strict dots verification"
      contains: "phase40-notification-interaction-assert"
    - path: "restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml"
      provides: "Singleton utility overlay providing extractOtpCode and extractUrl helper functions alongside upstream notification formatting"
      contains: "extractOtpCode"
  key_links:
    - from: "restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml"
      to: "scripts/phase40-notification-interaction-assert.sh"
      via: "Node.js and headless Quickshell test matrix evaluation"
      pattern: "extractOtpCode"
    - from: "scripts/phase40-notification-interaction-assert.sh"
      to: "arch/dots-hyprland.sh"
      via: "strict repository verification gate invocation"
      pattern: "\\./arch/dots-hyprland\\.sh verify --strict"
---

<objective>
Establish the foundation assertion test harness (`scripts/phase40-notification-interaction-assert.sh`) with strict execution modes and git-porcelain tracking, author the `NotificationUtils.qml` restow overlay implementing robust OTP verification code regex extraction (`extractOtpCode`) and safe URL parsing (`extractUrl`), and validate both algorithms against comprehensive positive/negative test matrices in headless runner tests.

Purpose: Provide the deterministic parsing foundation for 2FA verification code extraction and web link resolution before UI layer integration, ensuring zero false positives on dates, phone numbers, or invalid URL schemes.
Output: Restow overlay `NotificationUtils.qml` and executable test harness `scripts/phase40-notification-interaction-assert.sh` passing Sections 1, 3, 4, and `--syntax`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-CONTEXT.md
@.planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-RESEARCH.md
@.planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-PATTERNS.md
@.planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-VALIDATION.md
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml
@scripts/phase39-media-popup-assert.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — Test harness foundation (scripts/phase40-notification-interaction-assert.sh), restow scaffolding, and baseline symlink/porcelain integrity</name>
  <read_first>
    - .planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-CONTEXT.md
    - .planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-RESEARCH.md
    - .planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-PATTERNS.md
    - vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml
    - scripts/phase39-media-popup-assert.sh
  </read_first>
  <files>restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml, scripts/phase40-notification-interaction-assert.sh</files>
  <action>
    Establish the phase test harness scaffold at `scripts/phase40-notification-interaction-assert.sh` and execute the end-to-end tracer slice deploying the `NotificationUtils.qml` restow overlay via GNU Stow leaf symlinks (per D-10, INTG-01, INTG-02).

    1. Construct `scripts/phase40-notification-interaction-assert.sh`:
       - Follow the proven 5-section architecture from `scripts/phase39-media-popup-assert.sh` and `scripts/phase38-power-profiles-assert.sh`.
       - Enforce bash strict mode (`set -euo pipefail`), non-root guard (`EUID != 0`), CLI argument parsing for `--section <1-5>` (`-s <1-5>`) and `--syntax` (`-c`), cleanup trap for temporary files, and git porcelain snapshot tracking (`PORCELAIN_BEFORE` and `PORCELAIN_AFTER`).
       - Implement `run_js_test()` helper running Node.js (`node -e "..."`) on extracted utility functions.
       - Implement `run_qs_test()` helper running headless Quickshell (`quickshell -p`) on a temporary QML test file with timeout.
       - Implement Section 1 (Symlink & Packaging Integrity):
         * Assert that `restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml` exists as a regular file.
         * Assert that `~/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml` is a symlink resolving into `restow/quickshell/...`.
         * Assert that `~/.config/quickshell/ii/modules/common/functions` is a real directory (no folding).
         * Assert that git submodule `vendor/dots-hyprland` has zero git diff or untracked changes.
       - Implement Section 2 baseline (Static AST & QML Property Verification):
         * Assert `NotificationUtils.qml` declares `pragma Singleton` and `Singleton { id: root`.
       - Implement Section 5 baseline (Repository Integrity & Strict Verification):
         * Execute `./arch/dots-hyprland.sh verify --strict` requiring `FAIL=0 FINDINGS=0`.
         * Assert porcelain after execution matches porcelain before execution.
       - Make `scripts/phase40-notification-interaction-assert.sh` executable (`chmod +x scripts/phase40-notification-interaction-assert.sh`).

    2. Author initial `restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml`:
       - Create directory `restow/quickshell/.config/quickshell/ii/modules/common/functions`.
       - Seed file from `vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml` preserving `findSuitableMaterialSymbol`, `getFriendlyNotifTimeString`, and `processNotificationBody`.

    3. Deploy `NotificationUtils.qml` via GNU Stow:
       - Note reversibility: check if `~/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml` exists as a regular file; if so, preserve it by moving to `NotificationUtils.qml.bak` before stowing.
       - Ensure `~/.config/quickshell/ii/modules/common/functions` is a real directory (no folding).
       - Run `stow --verbose=5 --no-folding -d "$REPO_ROOT/restow" -t "$HOME" quickshell`.
       - Verify that `~/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml` is now a valid leaf symlink pointing into `restow/quickshell/`.
  </action>
  <verify>
    <automated>test -x scripts/phase40-notification-interaction-assert.sh &amp;&amp; ./scripts/phase40-notification-interaction-assert.sh --section 1</automated>
    <fails_when>non-zero exit code or output containing [FAIL]</fails_when>
    <automated>./scripts/phase40-notification-interaction-assert.sh --syntax</automated>
    <fails_when>non-zero exit code or AST validation error</fails_when>
    <automated>test -L "$HOME/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml"</automated>
    <fails_when>exit code 1 or target file is not a symlink</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/phase40-notification-interaction-assert.sh` exists, is executable, and passes Section 1 and `--syntax` checks.
    - `restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml` exists and contains upstream functions.
    - `~/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml` is a valid leaf symlink into `restow/quickshell/`.
    - `vendor/dots-hyprland` remains 100% pristine with zero git diff.
  </acceptance_criteria>
  <done>Tracer slice is proven: test harness foundation is operational, NotificationUtils.qml is seeded and stowed as a leaf symlink, and Section 1 assertions pass cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement OTP code extraction (extractOtpCode) and URL extraction (extractUrl) in NotificationUtils.qml with Section 3 &amp; 4 test matrix coverage</name>
  <read_first>
    - restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml
    - .planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-RESEARCH.md
    - .planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-PATTERNS.md
    - scripts/phase40-notification-interaction-assert.sh
  </read_first>
  <files>restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml, scripts/phase40-notification-interaction-assert.sh</files>
  <action>
    Implement pure extraction functions in `NotificationUtils.qml` and build out the full test matrices in Sections 3 and 4 of `scripts/phase40-notification-interaction-assert.sh` (per D-05, D-07, OTP-01, NAV-02, T-40-01, T-40-03).

    1. In `restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml`, add `extractOtpCode(body = "", summary = "")`:
       - Combine `summary` and `body` into a single searchable string, returning empty string if both are empty.
       - Strip HTML markup using regex replace `/<[^>]*>/g` with spaces.
       - Define security keywords alternation: `code|otp|verify|verification|pin|auth|2fa|security|one-time|password|passcode`.
       - Define code pattern matching: standalone 4 to 8 digits (`\b\d{4,8}\b`), hyphenated 3-4 digits on each side (e.g. `\d{3,4}-\d{3,4}` like `123-456`), or service-prefixed codes (e.g. `[A-Za-z]{1,2}-\d{4,8}` like `G-123456`).
       - Enforce negative lookarounds `(?<![-/0-9])` and `(?![-/0-9])` preventing matches inside calendar dates (`2026-09-24`), timestamps (`14:30`), phone numbers (`+1-800-555-0199`), and large byte counts.
       - Use bounded non-greedy regex matching (`{0,30}?`, `{1,60}?`) with no nested unbounded quantifiers to prevent ReDoS (T-40-01).
       - Evaluate keyword-before-code pattern, code-before-keyword pattern, and proximity matching within sentence bounds.
       - Return trimmed extracted code string or empty string if no valid match is found.

    2. In `NotificationUtils.qml`, add `extractUrl(body = "")`:
       - Return empty string if body is empty or undefined.
       - First, search for Chromium notification HTML anchors using regex `/<a\s+[^>]*href=["']([^"']+)["']/i` (matching web notifications from WhatsApp Web, YouTube, etc.). Unescape XML entities (`&amp;` to `&`).
       - Second, search for standalone raw URLs using pattern `/\bhttps?:\/\/[^\s<>"'()]+[^\s<>"'().,;:!?]/i` which explicitly strips trailing punctuation (periods, commas, semicolons, exclamation marks).
       - Enforce scheme whitelist: verify extracted URL begins with `http://` or `https://`. Reject `javascript:`, `file:`, `data:`, or shell payloads, returning empty string on violation (T-40-03).
       - Return trimmed URL string.

    3. Expand `scripts/phase40-notification-interaction-assert.sh`:
       - In Section 2 (Static AST):
         * Assert `NotificationUtils.qml` contains function signatures `extractOtpCode` and `extractUrl`.
         * Assert presence of security keywords regex string and negative lookaround bounds.
         * Assert presence of `https?://` scheme whitelist check.
       - In Section 3 (OTP Regex Extraction Test Matrix):
         * Implement Node.js assertion runner testing 14 positive cases: 4-digit PIN (`PIN is 9482`), 6-digit standard (`verification code: 482910`), 8-digit auth (`Your security code: 84920192`), hyphenated code (`123-456 is your code`), service prefix (`Google: G-829104`), proximity before/after (`Use 582910 for 2FA auth`), keyword variations (`one-time password`, `passcode`, `verify`).
         * Test 5 negative cases verifying empty string return: calendar dates (`Meeting on 2026-09-24`), timestamps (`Event at 14:30`), phone numbers (`Call +1-800-555-0199`), order IDs without keywords (`Order #9482103`), counter metrics (`Downloaded 1048576 bytes`).
       - In Section 4 (URL Extraction Test Suite):
         * Test Chromium HTML anchor `<a href="https://web.whatsapp.com">web.whatsapp.com</a>\n\nNew message` extracts `https://web.whatsapp.com`.
         * Test YouTube HTML anchor `<a href="https://youtube.com/watch?v=123&amp;t=10">` extracts unescaped query parameter `https://youtube.com/watch?v=123&t=10`.
         * Test raw URL `Check https://github.com/pera.` correctly excludes trailing period.
         * Test untrusted schemes (`javascript:alert(1)`, `file:///etc/passwd`) return empty string.
  </action>
  <verify>
    <automated>./scripts/phase40-notification-interaction-assert.sh --section 3</automated>
    <fails_when>non-zero exit code or any OTP test case failure</fails_when>
    <automated>./scripts/phase40-notification-interaction-assert.sh --section 4</automated>
    <fails_when>non-zero exit code or any URL extraction failure</fails_when>
    <automated>grep -q "function extractOtpCode" restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml &amp;&amp; grep -q "function extractUrl" restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml</automated>
    <fails_when>exit code 1 or missing function definitions</fails_when>
  </verify>
  <acceptance_criteria>
    - `NotificationUtils.extractOtpCode` passes all 14 positive and 5 negative test cases with 100% accuracy.
    - `NotificationUtils.extractUrl` parses Chromium HTML anchor tags, unescapes XML entities, strips trailing punctuation from raw URLs, and rejects unsafe protocols.
    - Regex patterns contain bounded quantifiers with zero ReDoS vulnerability.
    - Sections 3 and 4 of `scripts/phase40-notification-interaction-assert.sh` pass cleanly with zero failures.
  </acceptance_criteria>
  <done>OTP and URL extraction functions are implemented in NotificationUtils.qml and validated against the full test matrix in headless assertion execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FreeDesktop Notification Text Ingestion | Untrusted notification body and summary strings originating from arbitrary third-party D-Bus applications. |
| Regex Execution Engine | String parsing executed synchronously in Quickshell JavaScript environment. |
| Browser / External URL Dispatch | External URL strings passed to system handler via `Qt.openUrlExternally()`. |
| Filesystem & Stow Deployment | Leaf symlinks deployed by GNU Stow from `restow/quickshell/` to `~/.config/quickshell/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Denial of Service | `NotificationUtils.extractOtpCode` regex engine | high | mitigate | Eliminate nested unbounded quantifiers; use bounded quantifiers (`\d{4,8}`, `{0,30}?`, `{1,60}?`) with negative lookarounds `(?<![-/0-9])` and `(?![-/0-9])` to guarantee linear regex execution time. |
| T-40-03 | Elevation of Privilege / Command Injection | `NotificationUtils.extractUrl` URL handler | high | mitigate | Enforce strict protocol whitelist (`http://` or `https://` only); explicitly reject dangerous URI schemes (`javascript:`, `file:`, `data:`, `sh:`) before returning URL for external launching. |
| T-40-04 | Tampering | Git submodule `vendor/dots-hyprland` | high | mitigate | Author all modifications exclusively in `restow/quickshell/`; assert zero submodule drift via `arch/dots-hyprland.sh verify --strict` (INTG-01, INTG-03). |
</threat_model>

## Artifacts this phase produces

- `restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml`: Singleton utility overlay providing `extractOtpCode(body, summary)` and `extractUrl(body)` helper functions alongside upstream notification formatting.
- `scripts/phase40-notification-interaction-assert.sh`: 5-section test harness validating symlink packaging, AST properties, OTP regex test matrix, URL extraction, and strict dots-hyprland verification.

## Multi-Source Coverage Audit

| Source | Item ID / Reference | Description | Status | Covered By | Notes |
|---|---|---|---|---|---|
| GOAL | ROADMAP §Phase 40 | Regex OTP verification code extraction and web link resolution. | COVERED | Task 1, Task 2 | Parsing foundation established in `NotificationUtils.qml`. |
| REQ | OTP-01 | Regex parser in `NotificationUtils.qml` scans incoming notification text for 4-8 digit verification codes anchored to security keywords. | COVERED | Task 2 | `extractOtpCode` implements 4-8 digit standalone, hyphenated, and service-prefixed matching anchored to security keywords. |
| REQ | NAV-02 | Notification URL extraction parses links from Chromium notifications (`<a href="...">`), YouTube, WhatsApp, and raw URLs in body. | COVERED | Task 2 | `extractUrl` implements Chromium anchor and raw URL parsing with punctuation stripping and scheme whitelisting. |
| REQ | INTG-01 | All QML modifications deployed via `restow/quickshell/` leaf symlinks without modifying `vendor/dots-hyprland`. | COVERED | Task 1 | `NotificationUtils.qml` deployed via GNU Stow; verified in Section 1. |
| REQ | INTG-02 | Automated assertion test harness validates notification dismissal, link opening, and OTP parsing. | COVERED | Task 1, Task 2 | Implemented in `scripts/phase40-notification-interaction-assert.sh` across Sections 1, 3, and 4. |
| CONTEXT | D-05 | URL fallback extraction for Chromium and raw URLs. | COVERED | Task 2 | `extractUrl` extracts HTML anchors and raw HTTP/HTTPS URLs. |
| CONTEXT | D-07 | OTP regex heuristics matching 4-8 digits, hyphenated codes, service prefixes anchored to keywords. | COVERED | Task 2 | `extractOtpCode` matches codes anchored to keywords with negative lookarounds. |
| CONTEXT | D-10 | Deploy modified components under `restow/quickshell/` via GNU Stow leaf symlinks. | COVERED | Task 1 | Stowed with `--no-folding`. |
| RESEARCH | PATTERNS §1 | `NotificationUtils.qml` Singleton Structure Pattern. | COVERED | Task 1, Task 2 | Preserves upstream functions and exports pure extraction methods. |
| RESEARCH | PATTERNS §4 | `scripts/phase40-notification-interaction-assert.sh` 5-Section Test Harness Pattern. | COVERED | Task 1, Task 2 | Implements sections 1, 3, 4, syntax, and porcelain tracking. |

<verification>
Execute the OTP and URL test suites:
`./scripts/phase40-notification-interaction-assert.sh --section 1`
`./scripts/phase40-notification-interaction-assert.sh --section 3`
`./scripts/phase40-notification-interaction-assert.sh --section 4`
Verify static syntax:
`./scripts/phase40-notification-interaction-assert.sh --syntax`
</verification>

<success_criteria>
1. `scripts/phase40-notification-interaction-assert.sh` exists, is executable, and runs Sections 1, 3, 4, and `--syntax` with zero failures.
2. `NotificationUtils.extractOtpCode` achieves 100% accuracy across 14 positive and 5 negative test cases, rejecting dates, times, phone numbers, and counters.
3. `NotificationUtils.extractUrl` correctly parses Chromium HTML anchors and raw URLs, unescapes entities, strips trailing punctuation, and enforces the HTTP/HTTPS whitelist.
4. `restow/quickshell/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml` is deployed as a leaf symlink to `~/.config/quickshell/ii/modules/common/functions/NotificationUtils.qml`.
5. `vendor/dots-hyprland` submodule remains 100% pristine with zero git diff.
</success_criteria>

<output>
Create `.planning/phases/40-notification-center-quick-dismiss-smart-interaction/40-01-SUMMARY.md` when done
</output>
