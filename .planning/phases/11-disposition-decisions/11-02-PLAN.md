---
phase: 11-disposition-decisions
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md
  - .planning/phases/11-disposition-decisions/11-VALIDATION.md
autonomous: true
requirements:
  - DISP-01
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "§3 Axis A of 11-DISPOSITIONS.md is complete for all non-chrome hypr HIGH inventory paths with D-03 columns and inventory cites (D-04)"
    - "hyprland.conf primary session entry is accept-upstream (conf→.old path); must-keep categories only monitors/workspaces/env are migrate-to-hypr-custom (D-15/D-16) — primary entry is not keep-personal"
    - "hyprland/ dir (rsync --delete) and hyprland/scripts/ (folded or explicit) are accept-upstream (D-18)"
    - "hyprland.lua is accept-upstream (D-19)"
    - "hypr/custom/ allows ii seed on first install then Phase 13 only D-16 must-keeps (D-20) — no overlay files written this phase"
    - "Autostart/binds/chrome exec-once categories are accept-upstream/drop not migrate (D-17)"
    - "hyprlock.conf (HIGH) and hypridle.conf (MED–HIGH) have disposition rows keep-personal no-touch (D-24); deeper lock residual narrative may still live in §7 for 11-04"
    - "Every Axis A disposition cell uses only the five-value enum: keep-personal | migrate-to-hypr-custom | accept-upstream | merge | defer"
    - "§1 pre-flight and §2 flag profile from 11-01 remain intact (this plan only expands §3 Axis A)"
  artifacts:
    - path: ".planning/phases/11-disposition-decisions/11-DISPOSITIONS.md"
      provides: "Complete Axis A disposition table(s) for DISP-01 hypr HIGH coverage"
      contains: "hyprland.conf split rows, hyprland/, hyprland.lua, custom/, scripts cite, hyprlock.conf, hypridle.conf"
  key_links:
    - from: "11-DISPOSITIONS.md §3 Axis A rows"
      to: "10-INVENTORY.md Axis drop --skip-hyprland"
      via: "Inventory source column"
      pattern: "10-INVENTORY.md"
    - from: "migrate-to-hypr-custom rows"
      to: ".config/hypr/hyprland.conf monitors/workspaces/env"
      via: "Rationale + optional line cites (D-16)"
      pattern: "migrate-to-hypr-custom"
    - from: "hyprlock.conf / hypridle.conf rows"
      to: "D-24 no-touch policy"
      via: "Disposition keep-personal + rationale"
      pattern: "keep-personal"
  prohibitions:
    - statement: "no live full install"
      status: active
      verification: "Docs-only edits to 11-DISPOSITIONS.md; no setup/install"
    - statement: "no XDG mutation"
      status: active
      verification: "No rsync/cp/mv/rm into ~/.config"
    - statement: "no wrapper default full"
      status: active
      verification: "arch/dots-hyprland.sh not in files_modified; §2 residual left intact"
    - statement: "no invented surfaces"
      status: active
      verification: "All Axis A Paths from 10-INVENTORY.md or explicit fold cite under inventory parent path"
    - statement: "no hypr/custom creation this phase"
      status: active
      verification: "D-20 documents seed+Phase 13 only; no new overlay files under hypr/custom"
    - statement: "no delete chrome from repo"
      status: active
      verification: "This plan does not touch chrome trees or §6 beyond leaving 11-01 stubs"
---

<objective>
Expand `11-DISPOSITIONS.md` **§3 Axis A only** from 11-01 sample rows into a **complete hypr HIGH (+ MED–HIGH decision) disposition table** for DISP-01: every inventory Axis A high-risk path has enum + short rationale + inventory cite, with must-migrate limited to monitors/workspaces/env (D-15/D-16).

Purpose: Give Phase 13 (overlays) and Phase 14 (adopt) an unambiguous hypr decision set before misc/packages/chrome polish.

Output: Complete §3 Axis A in `11-DISPOSITIONS.md`; VALIDATION DISP-01 hypr rows advanced.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-disposition-decisions/11-CONTEXT.md
@.planning/phases/11-disposition-decisions/11-RESEARCH.md
@.planning/phases/11-disposition-decisions/11-PATTERNS.md
@.planning/phases/11-disposition-decisions/11-PLAN-OUTLINE.md
@.planning/phases/11-disposition-decisions/11-01-PLAN.md
@.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md
@.config/hypr/hyprland.conf
@scripts/phase11-dispositions-assert.sh
</context>

## Phase Goal

Every high-risk change has an explicit human decision before tooling or live adopt. This plan completes **DISP-01 for Axis A (hypr)** so no HIGH hypr inventory path lacks a disposition.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Disposition assert harness | `scripts/phase11-dispositions-assert.sh` | 11-01 |
| Disposition set | `11-DISPOSITIONS.md` | 11-01 scaffold; **11-02 completes §3 Axis A**; 11-03 B/C; 11-04 chrome/lock residual/UNKNOWN |
| Validation contract | `11-VALIDATION.md` | seeded; Wave 0 in 11-01; **11-02 updates DISP-01 hypr gates** |

## Section ownership (hard)

- **This plan edits only §3 Axis A** (and may add a one-line cross-ref under §7 that lock rows are seeded in §3 — optional).
- **Do not rewrite** §1 pre-flight or §2 flag profile from 11-01.
- **Do not expand** §4 Axis B, §5 Axis C, §6 chrome, §8 UNKNOWN extras (`.bak` / gui conf) — those are 11-03 / 11-04.
- hyprpaper may appear as a short Axis A or lock-adjacent row with D-21 `accept-upstream` / no investment if listed under Axis A inventory; full lock residual narrative remains 11-04 §7.

## Decisions locked in this plan

- D-03 / D-04: uniform columns + inventory cites on every row
- D-15: conf primary `accept-upstream` (not keep-personal primary)
- D-16: migrate **only** monitors, workspaces, env (incl. cursor / `ILLOGICAL_IMPULSE_VIRTUAL_ENV` listed under env migrate per RESEARCH recommendation)
- D-17: autostart / personal binds / chrome exec-once → accept-upstream / drop (not migrate)
- D-18: `hyprland/` + scripts subset → accept-upstream; pre-flight captures personal scripts
- D-19: `hyprland.lua` → accept-upstream
- D-20: `custom/` seed OK; Phase 13 only D-16
- D-21: hyprpaper → accept-upstream / no investment (if row included here)
- D-24: hyprlock.conf + hypridle.conf → keep-personal no-touch (HIGH checklist advances; 11-04 deepens §7 narrative + DISP-04 product wording)

## Assumptions

- 11-01 left sample Axis A rows and eight-section skeleton; executor **replaces/expands** sample §3 into complete table rather than duplicating conflicting rows.
- Scripts HIGH may be one explicit row or a fold line under `hyprland/` with inventory cite — must not be missing from grep for `hyprland/scripts` or an explicit “covered by hyprland/ dir” note that still contains the path string for assert `rg -F`.

<tasks>

<task type="auto" id="11-02-01" tdd="false">
  <name>Task 1: Complete Axis A session paths — conf split, dir, lua, custom, scripts, D-17 drop</name>
  <files>.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md</files>
  <read_first>
    - .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md (§3 sample rows from 11-01 — expand in place)
    - .planning/phases/11-disposition-decisions/11-CONTEXT.md (D-15..D-20, D-17)
    - .planning/phases/11-disposition-decisions/11-RESEARCH.md (HIGH path map rows 1–6, 13; must-migrate conf evidence lines; dual-row conf pattern)
    - .planning/phases/11-disposition-decisions/11-PATTERNS.md (D-03 columns; must-migrate only table; HIGH path checklist Axis A)
    - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md (Axis drop --skip-hyprland effect table rows for hyprland/, hyprland.conf, hyprland.lua, custom/, hyprland/scripts/)
    - .config/hypr/hyprland.conf (monitors ~29-30, workspaces ~76-87, env/cursor ~106-111 — cite categories only)
  </read_first>
  <action>
    Edit **only §3 Axis A** in `11-DISPOSITIONS.md`. Preserve §1–§2 and other sections unchanged aside from removing “sample only / expand in 11-02” notes under Axis A.

    Ensure one D-03 table (or tightly grouped tables) with columns: Path | Inventory risk | Disposition | Rationale | Flag stage | Inventory source.

    **Required rows / coverage (locked seeds):**

    1. **`~/.config/hypr/hyprland.conf` primary session entry** — Inventory risk HIGH; Disposition `accept-upstream`; Rationale: full adopt `mv` to `.old` so lua/hyprland tree becomes session entry (D-15); Flag stage `full-profile` / drop-skip-hyprland; Inventory source cite `10-INVENTORY.md` Axis A hyprland.conf row. Must not use `keep-personal` on this primary-entry row.

    2. **Must-keep categories from conf** — Path may be labeled e.g. `hyprland.conf must-keeps (monitors|workspaces|env)` or three sub-rows; Disposition `migrate-to-hypr-custom` **only** for monitors (DP-1 / HDMI-A-2), workspaces layout pins, env machine paths including cursor theme/size and `ILLOGICAL_IMPULSE_VIRTUAL_ENV` (D-16; list VIRTUAL_ENV under env migrate). Flag stage same full-profile hypr axis. Inventory source conf categories + inventory hyprland.conf row. Rationale: Phase 13 minimal hypr/custom overlays — no autostart/binds here.

    3. **`~/.config/hypr/hyprland/`** — HIGH; `accept-upstream`; rsync `--delete` from dots hyprland tree (D-18); cite inventory hyprland/ row; Flag stage full-profile / drop-skip-hyprland.

    4. **`~/.config/hypr/hyprland/scripts/`** — HIGH subset: either dedicated row `accept-upstream` with cite, or single fold row that still includes the path string `hyprland/scripts` and states covered by hyprland/ dir sync + pre-flight repo capture (D-18). Prefer explicit path string for DISP-01 `rg -F` gates.

    5. **`~/.config/hypr/hyprland.lua`** — HIGH; `accept-upstream`; install entry required for dots-hyprland session (D-19); cite inventory.

    6. **`~/.config/hypr/custom/`** — HIGH overlay strategy; Disposition: use `accept-upstream` for allowing ii `ignore_existing` seed on first install **or** `defer` only if wording needs Phase 13 fill — prefer locked D-20: allow seed, then Phase 13 populates **only** D-16 must-keeps (rationale must say Phase 13 only D-16; no extra custom fluff). Do not create any hypr/custom files in this task.

    7. **D-17 drop note** — rows or a clearly labeled subsection/table note: autostart apps (Chrome, kitty+tmux, btop special, vesktop/discord), personal tool binds (define.sh, hyprshot, cliphist-rofi, special workspace binds), chrome-related exec-once → `accept-upstream` / drop — **not** `migrate-to-hypr-custom`. These are category dispositions from personal conf, cited as conf categories under inventory hyprland.conf surface (not invented install targets).

    Flag stage vocabulary consistent with §2 (`full-profile` or `drop-skip-hyprland` for these rows).

    Enum cells only: `keep-personal` | `migrate-to-hypr-custom` | `accept-upstream` | `merge` | `defer`.

    Do not invent paths absent from inventory except category labels clearly tied to hyprland.conf inventory row.
  </action>
  <verify>
    <automated>rg -n 'hyprland\.conf|hyprland\.lua|migrate-to-hypr-custom|hyprland/scripts|accept-upstream' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -F 'hyprland.conf' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -F 'hyprland.lua' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -F 'migrate-to-hypr-custom' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -F 'hyprland/scripts' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches OR `rg -F 'hyprland/scripts/'` matches
    - `rg -n 'monitors|workspaces' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches must-keep rationale or path labels
    - `rg -n 'custom' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches Axis A custom row
    - Section §2 still contains `SAFE_DEFAULTS` (11-01 residual intact): `rg -F 'SAFE_DEFAULTS' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
  </acceptance_criteria>
  <done>Axis A session/install targets fully dispositioned with D-15..D-20 and D-17 drop scope.</done>
</task>

<task type="auto" id="11-02-02">
  <name>Task 2: Axis A lock/idle HIGH rows (D-24) + hyprpaper seed + assert/VALIDATION</name>
  <files>.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md, .planning/phases/11-disposition-decisions/11-VALIDATION.md</files>
  <read_first>
    - .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md (§3 after Task 1; §7 stub from 11-01)
    - .planning/phases/11-disposition-decisions/11-CONTEXT.md (D-21, D-23, D-24)
    - .planning/phases/11-disposition-decisions/11-RESEARCH.md (HIGH row hyprlock.conf; MED–HIGH hypridle; hyprpaper D-21)
    - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md (hyprlock.conf, hypridle.conf, hyprpaper.conf rows; not-firstrun auto_backup note)
    - scripts/phase11-dispositions-assert.sh
    - .planning/phases/11-disposition-decisions/11-VALIDATION.md (11-DISP-01 row)
  </read_first>
  <action>
    Still within **§3 Axis A** (and optionally a single cross-reference sentence in §7 pointing to §3 seeds — do not fully rewrite §7; 11-04 owns lock residual narrative depth, chrome, UNKNOWN).

    Add or complete disposition rows:

    1. **`~/.config/hypr/hyprlock.conf`** — Inventory risk HIGH (session lock); Disposition `keep-personal`; Rationale: operator no-touch / no boot-risk lock changes; host not-firstrun already writes `*.new` sidecars only — do not promote `.new` (D-24); product still hyprlock mechanism if anything locks, no Quickshell lock investment (D-23 one-liner OK); Flag stage full-profile / drop-skip-hyprland (effect axis) or `n/a` with note install writes sidecar only on this host; Inventory source cite Axis A hyprlock.conf row.

    2. **`~/.config/hypr/hypridle.conf`** — MED–HIGH; Disposition `keep-personal`; same no-touch rationale (D-24); cite inventory.

    3. **`~/.config/hypr/hyprpaper.conf`** (optional but recommended here for inventory completeness) — MED; `accept-upstream` / no investment (D-21); cite inventory; not a must-migrate.

    Do **not** disposition `hyprlock/` dir UNKNOWN fully here beyond optional one-line “see §8 / 11-04 defer (D-26)” if needed — UNKNOWN extras owned by 11-04. Do not expand `.bak` / `hyprland-gui.conf` (D-22 → 11-04 §8).

    Confirm `migrate-to-hypr-custom` appears only for D-16 categories (monitors/workspaces/env), not for lock, paper, autostart, or binds.

    Run `./scripts/phase11-dispositions-assert.sh` and fix §3 only if assert fails due to Axis A gaps (do not break §1–§2 or chrome stubs).

    Update `11-VALIDATION.md`: mark 11-DISP-01 progress for hypr HIGH path coverage (hyprland.conf, lua, hyprland/, custom, hyprlock) toward green where `rg -F` would pass; leave Axis B/C and chrome/DISP-03/04 for later plans; do not claim full phase nyquist_compliant unless assert already requires only what exists.
  </action>
  <verify>
    <automated>./scripts/phase11-dispositions-assert.sh && rg -n 'hyprlock\.conf|hypridle\.conf|keep-personal' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -F 'hyprlock.conf' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -F 'hypridle.conf' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -n 'keep-personal' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches lock/idle rows
    - `rg -F 'hyprland.conf' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -F 'hyprland.lua' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `./scripts/phase11-dispositions-assert.sh` exits 0
    - `rg -n 'migrate-to-hypr-custom' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` still matches D-16 categories
    - files_modified this plan do not include `arch/dots-hyprland.sh` or any path under `.config/hypr/custom`
  </acceptance_criteria>
  <done>All Axis A HIGH (+ hypridle MED–HIGH) inventory paths dispositioned; assert green; DISP-01 hypr coverage advanced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor → live XDG | Read conf for category cites only; no write/install |
| §3 dispositions → Phase 13/14 | Must-migrate set is contract; over-migrate expands blast radius |
| Inventory cites → operators | Paths must match Phase 10 SoT |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | live install or XDG sync during Axis A docs | high | mitigate | Docs-only; files_modified limited to planning markdown |
| T-11-06 | Tampering | `keep-personal` on hyprland.conf primary blocking ADOPT-02 | high | mitigate | D-15 required accept-upstream primary row; acceptance greps conf + accept-upstream split |
| T-11-07 | Tampering | Expanding migrate-to-hypr-custom beyond monitors/workspaces/env | medium | mitigate | D-16/D-17 explicit; acceptance requires must-keep category labels only for migrate rows |
| T-11-08 | Tampering | Writing hypr/custom overlays “early” | medium | mitigate | D-20 docs only; prohibition no hypr/custom creation |
| T-11-04 | Information Disclosure | pasting full conf secrets into dispositions | low | mitigate | Category + line-range cites only |
| T-11-05 | Elevation of Privilege | inventing hypr paths not in inventory | medium | mitigate | D-04 inventory source column required |
| T-11-SC | Tampering | supply chain | low | accept | No packages this plan |
</threat_model>

<verification>
From REPO_ROOT after both tasks:
`rg -F` each of: `hyprland.conf`, `hyprland.lua`, `hyprland/scripts` (or scripts path), `hyprlock.conf`, `hypridle.conf`, `migrate-to-hypr-custom`, `keep-personal` in `11-DISPOSITIONS.md`;
`./scripts/phase11-dispositions-assert.sh` exits 0;
`rg -F SAFE_DEFAULTS` still hits (§2 intact).
</verification>

<success_criteria>
§3 Axis A complete for DISP-01 hypr HIGH paths; conf primary accept-upstream + D-16-only migrate; lock/idle keep-personal no-touch rows present; no host/wrapper/overlay mutation; 11-01 §1–§2 preserved.
</success_criteria>

<output>
Create `.planning/phases/11-disposition-decisions/11-02-SUMMARY.md` when done
</output>
