---
phase: 11-disposition-decisions
plan: 04
type: execute
wave: 3
depends_on:
  - "11-02"
  - "11-03"
files_modified:
  - .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md
  - .planning/phases/11-disposition-decisions/11-VALIDATION.md
  - scripts/phase11-dispositions-assert.sh
autonomous: true
requirements:
  - DISP-03
  - DISP-04
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "§6 Dual-run chrome names waybar, rofi, and swaync with emerged-surface note (omitted from 10-INVENTORY by Phase 10 D-15)"
    - "DISP-03 / D-11: chrome disposition is explicit accept-remove on full adopt — override language states this accepts otherwise vs REQUIREMENTS/ROADMAP default-keep"
    - "Chrome disposition cells use accept-upstream enum only; rationale carries accept-remove / D-11 (no sixth enum token)"
    - "D-12: chrome configs stay in repo as archive; success does not delete .config/{waybar,rofi,swaync}"
    - "D-13/D-14: rely on dots-hyprland defaults for launcher/notification binds; chrome stops in same adopt window as full files (not migrated into must-keeps)"
    - "§7 lock/idle/paper: hyprlock.conf and hypridle.conf keep-personal no-touch (D-24); product keeps hyprlock mechanism with no Quickshell lock investment (D-23); *.new sidecars defer (D-25); hyprpaper accept-upstream/no investment if not already final (D-21)"
    - "§8 UNKNOWN/extra: hyprlock/ dir defer (D-26); hyprland.conf.bak keep-personal; hyprland-gui.conf defer (D-22); optional PARTIAL asdeps note"
    - "Full HIGH-path cross-check: all 13 unique HIGH inventory effect paths appear in 11-DISPOSITIONS.md with disposition enum"
    - "./scripts/phase11-dispositions-assert.sh exits 0 including chrome-require and residual SAFE_DEFAULTS gates"
    - "11-VALIDATION.md marks DISP-01..04 automated gates green / nyquist path complete for docs phase"
  artifacts:
    - path: ".planning/phases/11-disposition-decisions/11-DISPOSITIONS.md"
      provides: "Final eight-section disposition SoT with chrome, lock residual, UNKNOWN, and complete HIGH coverage"
      contains: "waybar, rofi, swaync, accept-remove, hyprlock, hypridle, keep-personal, UNKNOWN, Sources"
    - path: ".planning/phases/11-disposition-decisions/11-VALIDATION.md"
      provides: "Phase validation sign-off for DISP-01..04 structural gates"
    - path: "scripts/phase11-dispositions-assert.sh"
      provides: "Final green Nyquist harness (may tighten HIGH list / chrome override checks if needed)"
  key_links:
    - from: "§6 chrome rows"
      to: "Phase 10 D-15 omit + .config/hypr/hyprland.conf exec-once/binds"
      via: "Emerged-surface note + Inventory source column"
      pattern: "accept-remove"
    - from: "§7 lock/idle rows"
      to: "10-INVENTORY.md hyprlock/hypridle + D-23/D-24"
      via: "Disposition keep-personal"
      pattern: "keep-personal"
    - from: "HIGH cross-check task"
      to: "10-INVENTORY.md HIGH effect rows"
      via: "rg -F each path in 11-DISPOSITIONS.md"
      pattern: "Disposition"
  prohibitions:
    - statement: "no live full install"
      status: active
      verification: "Docs/assert only; no setup/install/pacman"
    - statement: "no XDG mutation"
      status: active
      verification: "No rsync/cp/mv/rm into ~/.config"
    - statement: "no wrapper default full"
      status: active
      verification: "arch/dots-hyprland.sh not edited; SAFE_DEFAULTS residual language remains"
    - statement: "no invented surfaces"
      status: active
      verification: "Chrome only via emerged note; extras from inventory UNKNOWN/host rows"
    - statement: "no hypr/custom creation this phase"
      status: active
      verification: "No overlay files written"
    - statement: "no delete chrome from repo"
      status: active
      verification: "D-12 archive; no rm of waybar/rofi/swaync trees"
---

<objective>
Close Phase 11 by completing **§6 dual-run chrome (DISP-03)**, **§7 lock/idle/paper residual (DISP-04)**, and **§8 UNKNOWN/extra surfaces**, then run a **full HIGH-path cross-check** and VALIDATION/assert sign-off so `11-DISPOSITIONS.md` is the committed gate for Phases 12–14.

Purpose: Record explicit chrome accept-remove (D-11 override), lock no-touch policy, and prove every HIGH inventory path has a disposition before wrapper full-profile or live adopt work.

Output: Finalized sections §6–§8 in `11-DISPOSITIONS.md`, assert green, `11-VALIDATION.md` sign-off for DISP-01..04 structural completeness.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-disposition-decisions/11-CONTEXT.md
@.planning/phases/11-disposition-decisions/11-RESEARCH.md
@.planning/phases/11-disposition-decisions/11-PATTERNS.md
@.planning/phases/11-disposition-decisions/11-PLAN-OUTLINE.md
@.planning/phases/11-disposition-decisions/11-VALIDATION.md
@.planning/phases/11-disposition-decisions/11-01-PLAN.md
@.planning/phases/11-disposition-decisions/11-02-PLAN.md
@.planning/phases/11-disposition-decisions/11-03-PLAN.md
@.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md
@.config/hypr/hyprland.conf
@.planning/codebase/INTEGRATIONS.md
@scripts/phase11-dispositions-assert.sh
</context>

## Phase Goal

Every high-risk change has an explicit human decision and staged flag profile before tooling or live adopt. This plan closes **DISP-03** (chrome accept-remove override) and **DISP-04** (lock/idle), and finalizes **DISP-01** via HIGH cross-check.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Disposition assert harness | `scripts/phase11-dispositions-assert.sh` | 11-01; **11-04 may tighten final gates** |
| Disposition set | `11-DISPOSITIONS.md` | 11-01..03 partial; **11-04 completes §6–§8 + cross-check** |
| Validation contract | `11-VALIDATION.md` | **11-04 sign-off** |

## Section ownership (hard)

- Edit **§6 chrome**, **§7 lock/idle/paper**, **§8 UNKNOWN/extra** (and Sources footer if needed).
- **HIGH cross-check** may add missing disposition rows only where a HIGH path is absent — prefer fixing the owning section (A/B/C) with minimal surgical inserts rather than rewriting §1–§5.
- Do not change first full-adopt flag profile intent (D-05) or residual SAFE_DEFAULTS claim (D-10).
- Do not implement wrapper full profile, hypr/custom overlays, or live pre-flight sync.

## Decisions locked in this plan

- D-11: chrome **explicit accept-remove** on full adopt (overrides DISP-03 / ROADMAP SC3 default-keep)
- D-12: chrome configs **archive in repo**; do not delete from repo
- D-13: launcher/notification binds rely on dots-hyprland defaults after chrome removal
- D-14: chrome stops in **same adopt window** as full files; not carried into must-keep overlays
- D-21: hyprpaper accept-upstream / no investment (ensure final in §7 if not already)
- D-22: `.bak` keep-personal; `hyprland-gui.conf` defer
- D-23: hyprlock mechanism; no QS lock investment
- D-24: live hyprlock.conf / hypridle.conf keep-personal no-touch
- D-25: `*.new` sidecars defer
- D-26: hyprlock/ dir UNKNOWN defer
- Enum: chrome uses `accept-upstream` + accept-remove rationale (no sixth token)

<tasks>

<task type="auto" id="11-04-01">
  <name>Task 1: §6 Dual-run chrome accept-remove (DISP-03 / D-11..D-14)</name>
  <files>.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md</files>
  <read_first>
    - .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md (§6 stub from 11-01)
    - .planning/phases/11-disposition-decisions/11-CONTEXT.md (D-11, D-12, D-13, D-14)
    - .planning/phases/11-disposition-decisions/11-RESEARCH.md (chrome emerged-surface template; conf evidence lines; enum carrier note)
    - .planning/phases/11-disposition-decisions/11-PATTERNS.md (chrome accept-remove pattern; anti-pattern sixth enum)
    - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md (scope note that chrome was omitted — Phase 10 D-15)
    - .config/hypr/hyprland.conf (exec-once waybar/swaync ~64; binds ~259,270,277 — cite only)
    - .planning/codebase/INTEGRATIONS.md (dual-run chrome narrative for emerged cites)
    - .planning/REQUIREMENTS.md (DISP-03 default-keep wording — override must be explicit)
    - .planning/ROADMAP.md (Phase 11 SC3)
  </read_first>
  <action>
    Replace §6 stub with a complete **Dual-run chrome (DISP-03)** section.

    **Required prose:**
    1. **Emerged surface note:** Waybar / rofi / swaync were **omitted** from `10-INVENTORY.md` by Phase 10 D-15; dispositioned here by operator decision, not invented install-effect rows.
    2. **Explicit override statement:** This **explicitly accepts otherwise** versus DISP-03 / ROADMAP success criterion default-keep; disposition = accept-remove on full adopt (D-11).
    3. **Archive policy (D-12):** `.config/{waybar,rofi,swaync}` remain in repo as archive (pre-flight captures live); do not delete from repo as Phase 11–14 success.
    4. **Timing (D-14):** chrome stops in the same adopt window as full files install (conf→`.old` / lua entry); personal waybar/rofi/swaync exec-once are not carried into must-keep overlays.
    5. **Binds (D-13):** after removal, rely on dots-hyprland defaults — no must-remap bind list required in dispositions.

    **Required D-03 table rows** (Path | Inventory risk | Disposition | Rationale | Flag stage | Inventory source):
    - waybar session chrome — risk `n/a (emerged)` — Disposition `accept-upstream` — Rationale must include accept-remove and D-11 — Flag stage `full-profile` — Inventory source: emerged Phase 10 D-15; conf exec-once cite; optional INTEGRATIONS.
    - rofi launcher chrome — same pattern (binds / cliphist-rofi cite OK).
    - swaync notifications — same pattern.

    Do **not** introduce a sixth enum token such as a bare `accept-remove` disposition cell. Enum cell = `accept-upstream` only; accept-remove lives in rationale/prose.

    Do not delete repo chrome directories. Do not edit live XDG. Do not expand §4/§5.
  </action>
  <verify>
    <automated>rg -ni 'waybar|rofi|swaync' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md && rg -ni 'accept-remove|explicitly accepts otherwise|overrides? DISP-03|accepted otherwise' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -i waybar .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -i rofi .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -i swaync .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -ni 'accept-remove' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -ni 'DISP-03|default-keep|accepted otherwise|explicit' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` shows override intent
    - `rg -n 'emerged|D-15|omitted' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches emerged-surface note
    - `rg -n 'archive' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches D-12 archive language
    - Chrome table Disposition cells use `accept-upstream` (rationale carries accept-remove)
  </acceptance_criteria>
  <done>§6 chrome complete with DISP-03 explicit accept-remove override and archive-in-repo policy.</done>
</task>

<task type="auto" id="11-04-02">
  <name>Task 2: §7 lock/idle/paper (DISP-04) + §8 UNKNOWN/extra (D-22..D-26)</name>
  <files>.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md</files>
  <read_first>
    - .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md (§7/§8 stubs; §3 lock seeds from 11-02 if present)
    - .planning/phases/11-disposition-decisions/11-CONTEXT.md (D-21, D-22, D-23, D-24, D-25, D-26)
    - .planning/phases/11-disposition-decisions/11-RESEARCH.md (lock/idle section outline; UNKNOWN table)
    - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md (hyprlock.conf, hypridle.conf, hyprpaper, hyprlock/ UNKNOWN, .bak, hyprland-gui.conf, not-firstrun *.new behavior)
  </read_first>
  <action>
    Complete **§7 Lock / idle / paper residual (DISP-04)** and **§8 UNKNOWN / extra surfaces**.

    **§7 required content:**
    - Product statement: lock mechanism remains **hyprlock** if anything locks; **no Quickshell lock screen investment** (D-23).
    - `~/.config/hypr/hyprlock.conf` — HIGH — `keep-personal` — no-touch / no boot-risk changes; host not-firstrun writes `*.new` only — do not promote `.new` to live; do not force firstrun replace (D-24). Inventory cite.
    - `~/.config/hypr/hypridle.conf` — MED–HIGH — `keep-personal` no-touch (D-24). Inventory cite.
    - `hyprlock.conf.new` / `hypridle.conf.new` (or `*.new` lock/idle sidecars) — `defer` (D-25).
    - `~/.config/hypr/hyprpaper.conf` — `accept-upstream` / no investment (D-21) if not already finalized elsewhere — ensure one clear final row or cross-ref.
    - Align with any §3 lock seeds from 11-02: prefer single authoritative wording in §7 with §3 cross-ref, or consistent duplicate rows — no conflicting dispositions (must remain keep-personal for live confs).

    **§8 required content:**
    - `~/.config/hypr/hyprlock/` dir gap — UNKNOWN — `defer` (D-26); not a blocker under no-touch lock policy. Cite inventory UNKNOWN / research notes.
    - `~/.config/hypr/hyprland.conf.bak` — LOW — `keep-personal` (D-22).
    - `~/.config/hypr/hyprland-gui.conf` — LOW — `defer` (D-22).
    - Optional: PARTIAL `previous_dependencies.conf` ∩ explicit pkgs — accept residual under D-29; no per-name invent.
    - Optional: `--exp-files` out of default scope note (inventory legacy SoT).

    Use D-03 columns on table rows. Enum five tokens only. Do not rewrite §6 beyond fixing conflicts if Task 1 already landed. Do not create hypr/custom. Do not mutate XDG.
  </action>
  <verify>
    <automated>rg -n 'hyprlock|hypridle|keep-personal|Quickshell lock|\.new|hyprpaper|hyprland\.conf\.bak|hyprland-gui|UNKNOWN|defer' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -F 'hyprlock.conf' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -F 'hypridle.conf' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -n 'keep-personal' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches lock/idle
    - `rg -ni 'Quickshell lock|no Quickshell|QS lock' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches D-23 intent
    - `rg -n '\.new|defer' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches D-25
    - `rg -n 'hyprlock/' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches D-26 UNKNOWN
    - `rg -n 'hyprland.conf.bak|hyprland-gui' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches D-22
  </acceptance_criteria>
  <done>§7 DISP-04 and §8 UNKNOWN/extra complete with locked D-22..D-26 seeds.</done>
</task>

<task type="auto" id="11-04-03">
  <name>Task 3: Full HIGH-path cross-check + assert green + VALIDATION sign-off</name>
  <files>.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md, .planning/phases/11-disposition-decisions/11-VALIDATION.md, scripts/phase11-dispositions-assert.sh</files>
  <read_first>
    - .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md (full file after Tasks 1–2)
    - .planning/phases/11-disposition-decisions/11-RESEARCH.md (13 unique HIGH path checklist)
    - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md (HIGH effect rows Axes A/B/C)
    - scripts/phase11-dispositions-assert.sh
    - .planning/phases/11-disposition-decisions/11-VALIDATION.md
    - .planning/phases/11-disposition-decisions/11-PATTERNS.md (final assert chrome require + HIGH samples)
  </read_first>
  <action>
    **HIGH-path cross-check (DISP-01 final):** Verify each of the 13 unique HIGH inventory effect paths appears in `11-DISPOSITIONS.md` with a disposition enum nearby:

    Axis A: `hyprland/` (dir), `hyprland.conf`, `hyprland.lua`, `hyprlock.conf`, `custom/`, `hyprland/scripts` (or scripts path string)
    Axis B: `fish`, `fontconfig`, `kitty`, `starship.toml`
    Axis C: full install/install-deps pipeline wording, `pacman -Syu` or `Syu`, `implicitize` or `asdeps`

    If any HIGH path is missing, add a minimal D-03 row in the correct section (§3/§4/§5) using locked CONTEXT seeds — do not invent new policies.

    Also spot-check MED–HIGH decision rows still present: hypridle.conf, mpv, illogical-impulse-hyprland (or illogical-impulse metas).

    Confirm §2 still states residual SAFE_DEFAULTS unchanged on default install (D-10) and first full-adopt drops all three (D-05).

    **Assert:** Run `./scripts/phase11-dispositions-assert.sh`. If chrome override or HIGH sample gates are soft/missing, tighten the script to hard-require: waybar+rofi+swaync, accept-remove/override language, residual SAFE_DEFAULTS, and the HIGH path samples from PATTERNS — still read-only, no XDG writes. Script must exit 0 at end.

    **VALIDATION.md:** Mark per-task map rows 11-DISP-01..04, 11-D10, 11-PROCESS toward green; set `wave_0_complete: true`; set `nyquist_compliant: true` only when assert exits 0 and HIGH checklist is documented done; check off Wave 0 / sign-off boxes that apply to docs structural completeness.

    Do not run live install. Do not rsync XDG. Do not edit `arch/dots-hyprland.sh`. Do not delete chrome from repo.
  </action>
  <verify>
    <automated>./scripts/phase11-dispositions-assert.sh</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/phase11-dispositions-assert.sh` exits 0
    - For each HIGH token, `rg -F` hits in 11-DISPOSITIONS.md: hyprland.conf, hyprland.lua, hyprlock.conf, fish, fontconfig, kitty, starship.toml, and (pacman -Syu or Syu), and (asdeps or implicitize)
    - `rg -i waybar .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` and rofi and swaync all match
    - `rg -ni 'accept-remove' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -F 'SAFE_DEFAULTS' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches
    - `rg -n 'keep-personal' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` matches lock policy
    - `11-VALIDATION.md` frontmatter has `wave_0_complete: true` and `nyquist_compliant: true` after green assert
    - No files under `.config/hypr/custom` created by this plan; `arch/dots-hyprland.sh` untouched
  </acceptance_criteria>
  <done>Phase 11 disposition artifact complete: DISP-01 HIGH cross-check green, DISP-03/04 closed, assert and VALIDATION signed off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chrome dispositions → Phase 14 session | accept-remove means stop launching dual-run chrome — not delete-from-repo or blind rm live without adopt plan |
| Lock dispositions → boot path | no-touch prevents boot-risk lock migration operator rejected |
| Assert script → host | Read repo markdown only (optional test -e); no XDG writes |
| Final dispositions → Phases 12–15 | Single SoT contract; must not claim wrapper already full |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | live full install or XDG mutation during closeout | high | mitigate | Docs/assert only; process prohibition active |
| T-11-02 | Tampering | chrome accept-remove misread as delete repo/live configs | medium | mitigate | D-12 archive language required; rationale stop-launch/not-migrate |
| T-11-12 | Repudiation | missing DISP-03 override language so default-keep appears unmet or contradicted silently | medium | mitigate | Task 1 requires explicit accepts-otherwise / D-11 override prose; assert greps accept-remove |
| T-11-13 | Tampering | promoting hyprlock *.new or forcing firstrun lock replace | medium | mitigate | D-24/D-25 rows; no-touch + defer sidecars |
| T-11-06 | Tampering | last-minute keep-personal on hyprland.conf primary | high | mitigate | Cross-check must not regress D-15; assert/sample still expect accept-upstream conf path |
| T-11-03 | Tampering | editing SAFE_DEFAULTS while finalizing full profile docs | high | mitigate | Wrapper not in intentional edit set; D-10 residual must remain |
| T-11-04 | Information Disclosure | pasting full conf/bind bodies with secrets | low | mitigate | Path and line cites only |
| T-11-SC | Tampering | supply chain | low | accept | No package installs |
</threat_model>

<verification>
From REPO_ROOT:
`./scripts/phase11-dispositions-assert.sh` exits 0;
`rg` confirms waybar/rofi/swaync + accept-remove + hyprlock/hypridle keep-personal + SAFE_DEFAULTS + all HIGH path tokens;
`11-VALIDATION.md` nyquist_compliant true.
</verification>

<success_criteria>
§6 chrome accept-remove override complete (DISP-03); §7 lock no-touch + no QS lock (DISP-04); §8 UNKNOWN/extras dispositioned; all 13 HIGH inventory paths present; assert green; VALIDATION signed off; no host/wrapper/chrome-delete/hypr-custom mutation.
</success_criteria>

<output>
Create `.planning/phases/11-disposition-decisions/11-04-SUMMARY.md` when done
</output>
