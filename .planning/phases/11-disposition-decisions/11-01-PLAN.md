---
phase: 11-disposition-decisions
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - scripts/phase11-dispositions-assert.sh
  - .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md
  - .planning/phases/11-disposition-decisions/11-VALIDATION.md
autonomous: true
requirements:
  - DISP-02
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "scripts/phase11-dispositions-assert.sh exists, is executable, passes bash -n, and gates 11-DISPOSITIONS.md structure (D-01 path, eight-section skeleton, D-03 columns, DISP-02 flag profile)"
    - "D-01: 11-DISPOSITIONS.md exists only under .planning/phases/11-disposition-decisions/ (phase-dir SoT; not under docs/)"
    - "D-02: Single multi-section document with eight section intents — pre-flight, flag profile, Axis A, Axis B, Axis C, chrome, lock/idle, UNKNOWN/extra"
    - "D-03: At least one disposition table uses columns Path | Inventory risk | Disposition | Rationale | Flag stage | Inventory source"
    - "DISP-02 / D-05: Flag profile records three independent axes and first full-adopt profile drops all three residuals (--skip-hyprland, --core, --skip-sysupdate)"
    - "D-10: Residual SAFE_DEFAULTS on default install/install-files remains the triple; Phase 11 does not change wrapper defaults; full profile is opt-in intent for Phase 12"
    - "D-07: Pre-flight section documents live→repo archive sync gate before any full files install, with candidate classes from RESEARCH (live-only + both-PRESENT + chrome archive)"
    - "Sample Axis A rows exist with locked seeds (hyprland.conf split accept-upstream primary + migrate-to-hypr-custom categories; hyprland/ accept-upstream) proving end-to-end D-03 columns"
    - "Assert never mutates XDG and never invokes non-dry-run setup/install"
  artifacts:
    - path: "scripts/phase11-dispositions-assert.sh"
      provides: "Wave 0 Nyquist structural/lint harness for 11-DISPOSITIONS.md"
      contains: "DISP path, FAIL counter, section gates, D-03 header, residual SAFE_DEFAULTS, full-profile triple drop, progressive HIGH/chrome checks"
    - path: ".planning/phases/11-disposition-decisions/11-DISPOSITIONS.md"
      provides: "Committed disposition SoT (scaffold + §1 pre-flight + §2 flag profile complete + sample Axis A)"
  key_links:
    - from: "scripts/phase11-dispositions-assert.sh"
      to: "11-DISPOSITIONS.md"
      via: "structural grep/rg gates"
      pattern: "SAFE_DEFAULTS"
    - from: "11-DISPOSITIONS.md §2 flag profile"
      to: "arch/dots-hyprland.sh"
      via: "Source cites for residual injection"
      pattern: "SAFE_DEFAULTS=(--core --skip-hyprland --skip-sysupdate)"
    - from: "11-DISPOSITIONS.md sample Axis A rows"
      to: "10-INVENTORY.md"
      via: "Inventory source column cites"
      pattern: "10-INVENTORY.md"
  prohibitions:
    - statement: "no live full install"
      status: active
      verification: "Tasks document decisions only; no non-dry-run arch/dots-hyprland.sh install or ./setup"
    - statement: "no XDG mutation"
      status: active
      verification: "No rsync/cp/mv/rm into ~/.config or XDG; pre-flight lists commands as Phase 14 work only"
    - statement: "no wrapper default full"
      status: active
      verification: "D-10 residual language present; arch/dots-hyprland.sh SAFE_DEFAULTS array not edited"
    - statement: "no invented surfaces"
      status: active
      verification: "Every Path cites 10-INVENTORY.md row/UNKNOWN or explicit emerged-surface note"
    - statement: "no hypr/custom creation this phase"
      status: active
      verification: "No new files under hypr/custom or .config/hypr/custom"
    - statement: "no delete chrome from repo"
      status: active
      verification: "No rm of .config/{waybar,rofi,swaync}; D-12 archive policy only"
---

<objective>
Tracer slice for Phase 11: land Wave 0 disposition assert harness + create `11-DISPOSITIONS.md` with the D-02 eight-section skeleton, **complete pre-flight (D-07) and full-adopt flag profile (DISP-02 / D-05 / D-10)**, and **sample Axis A rows** that prove D-03 columns end-to-end before later plans fill remaining HIGH paths.

Purpose: Prove the disposition artifact path, column contract, flag-profile Phase 12 contract, and automated gates without mutating the host or wrapper.

Output: `scripts/phase11-dispositions-assert.sh`, scaffolded `11-DISPOSITIONS.md` with §1–§2 complete + sample §3, VALIDATION.md Wave 0 boxes updated.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-disposition-decisions/11-CONTEXT.md
@.planning/phases/11-disposition-decisions/11-RESEARCH.md
@.planning/phases/11-disposition-decisions/11-PATTERNS.md
@.planning/phases/11-disposition-decisions/11-VALIDATION.md
@.planning/phases/11-disposition-decisions/11-PLAN-OUTLINE.md
@.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md
@arch/dots-hyprland.sh
@scripts/phase10-inventory-assert.sh
</context>

## Phase Goal

Every high-risk change has an explicit human decision and staged flag profile before tooling or live adopt. This plan delivers the **DISP-02 flag-profile contract** (triple residual still default; first full-adopt drops all three) plus the harness and skeleton later plans expand.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Disposition assert harness | `scripts/phase11-dispositions-assert.sh` | **11-01** |
| Disposition set | `11-DISPOSITIONS.md` | **11-01** scaffold + §1–§2 + sample Axis A; filled by 11-02..04 |
| Validation contract | `11-VALIDATION.md` | seeded plan-phase; Wave 0 boxes in **11-01** |

## Decisions locked in this plan

- D-01: phase-dir SoT path for `11-DISPOSITIONS.md`
- D-02: one multi-section markdown document (eight sections)
- D-03: uniform disposition columns + five-value enum
- D-04: cite inventory / UNKNOWN / emerged only (sample rows must cite `10-INVENTORY.md`)
- D-05 / D-32: first full-adopt profile drops all three SAFE_DEFAULTS residuals
- D-07 / D-08: pre-flight live→repo archive gate; vendor remains product SoT
- D-10: residual SAFE_DEFAULTS on default `install` / `install-files` unchanged this phase
- Sample Axis A seeds only: D-15/D-16 conf split, D-18 hyprland/ dir (full Axis A = 11-02)

## Assumptions (specless probe)

- **EDGE_ABSENT / PROHIB_ABSENT:** no SPEC.md; edges from DISP-01..04 + CONTEXT D-*
- **DISP-02 edge:** flag profile must remain true after later plans — later plans must not delete residual or rewrite first full profile without CONTEXT change
- **Progressive assert:** chrome + full HIGH path gates may fail until 11-04 / 11-02–03 if implemented hard; tracer plants keyword stubs under later sections so default assert mode can exit 0 after 11-01, OR assert uses progressive soft checks for chrome/HIGH until a `--strict` mode (prefer hard section presence + keyword stubs in skeleton so default exit 0)

<tasks>

<task type="tracer" id="11-01-01">
  <name>Task 1: Create phase11 dispositions assert harness</name>
  <files>scripts/phase11-dispositions-assert.sh</files>
  <read_first>
    - scripts/phase10-inventory-assert.sh (PASS/FAIL pattern, REPO_ROOT, exit FAIL count, progressive section gates)
    - .planning/phases/11-disposition-decisions/11-PATTERNS.md (phase11 assert deltas vs phase10; invert chrome rule)
    - .planning/phases/11-disposition-decisions/11-RESEARCH.md (Validation Architecture; optional assert skeleton)
    - .planning/phases/11-disposition-decisions/11-VALIDATION.md (Wave 0 requirements; 11-W0 row)
    - .planning/phases/11-disposition-decisions/11-CONTEXT.md (D-01, D-03, D-05, D-10, D-11)
  </read_first>
  <action>
    Create executable `scripts/phase11-dispositions-assert.sh` (chmod +x) with `set -euo pipefail`, REPO_ROOT from BASH_SOURCE, labeled pass/fail helpers matching phase10 style.

    **DISP path constant:** `$REPO_ROOT/.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md`

    **Header comment constraints:** script is structural/lint only; never target HOME/XDG with copy/move/remove tools; never call `./setup` or `arch/dots-hyprland.sh` except if a future dry-run is explicitly documented (do not add install invocation in this task).

    **Default mode (no args) — hard gates for tracer after Task 2:**
    1. File exists at D-01 path.
    2. Required section intent keys present (flexible wording OK): Pre-flight or repo sync; flag profile or SAFE_DEFAULTS or full-adopt; Axis A or hyprland or skip-hyprland; Axis B or drop --core or misc; Axis C or sysupdate or packages; chrome or waybar; hyprlock or hypridle or lock; UNKNOWN or extra surface.
    3. At least one markdown table header matching Path + Inventory risk + Disposition + Rationale + Flag stage + Inventory source (case-insensitive; pipe tables OK).
    4. DISP-02 / D-10 residual: file contains `SAFE_DEFAULTS` and the three tokens `--core`, `--skip-hyprland`, `--skip-sysupdate`, plus language that default install still injects residual / remains safe (match remains|still|unchanged|default near SAFE_DEFAULTS or residual).
    5. DISP-02 / D-05 first full profile: language that first full-adopt drops all three residuals (require all three flag tokens near drop/full-profile/full-adopt wording — not only residual restatement).
    6. Enum tokens: require presence of `keep-personal`, `migrate-to-hypr-custom`, `accept-upstream`, and `defer` somewhere in file (sample rows + stubs supply them). Treat `merge` as allowed if present; do not fail if `merge` unused.
    7. Sample Axis A keywords: require `hyprland.conf` and at least one of `hyprland/` or `hyprland.lua` plus `migrate-to-hypr-custom`.
    8. Chrome progressive: require literal substrings waybar, rofi, and swaync (skeleton stubs plant them for later 11-04 fill — inverse of phase10 chrome ban). Also require accept-remove or explicit remove or DISP-03 override / accepted otherwise language once chrome section exists (stub may include one override sentence).
    9. Do not implement host write operations. Optional `--full` mode may only `test -e` read-only paths if desired; not required for 11-01 green.

    Success: print a summary line like `phase11 dispositions asserts OK` and exit 0 when hard gates pass; otherwise exit non-zero with `[FAIL]` lines.
  </action>
  <verify>
    <automated>test -x scripts/phase11-dispositions-assert.sh && bash -n scripts/phase11-dispositions-assert.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/phase11-dispositions-assert.sh` succeeds
    - `bash -n scripts/phase11-dispositions-assert.sh` exits 0
    - Script references path `11-disposition-decisions/11-DISPOSITIONS.md`
    - Script greps for SAFE_DEFAULTS residual and all three flag tokens
    - Script requires waybar and rofi and swaync (chrome present, not banned)
    - Script body does not invoke rsync against `$HOME/.config` or `$XDG_CONFIG_HOME`
  </acceptance_criteria>
  <done>Executable Wave 0 assert script ready to gate disposition content after Task 2.</done>
</task>

<task type="auto" id="11-01-02">
  <name>Task 2: Scaffold 11-DISPOSITIONS.md — complete pre-flight + flag profile + sample Axis A</name>
  <files>.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md, .planning/phases/11-disposition-decisions/11-VALIDATION.md</files>
  <read_first>
    - .planning/phases/11-disposition-decisions/11-CONTEXT.md (D-01..D-10, D-15/D-16 seeds for samples, D-07 pre-flight)
    - .planning/phases/11-disposition-decisions/11-RESEARCH.md (recommended eight-section outline; pre-flight candidate table; flag-profile Phase 12 block; sample row pattern)
    - .planning/phases/11-disposition-decisions/11-PATTERNS.md (scope banner; SAFE_DEFAULTS residual narrative; D-03 header; eight-section skeleton)
    - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md (Axis A HIGH rows for cites; residual section for cross-check)
    - arch/dots-hyprland.sh (SAFE_DEFAULTS at line ~12; needs_safe_defaults ~127-131; injection ~1399-1407)
    - scripts/phase11-dispositions-assert.sh (gates this task must satisfy)
  </read_first>
  <action>
    Create `.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` (D-01). Structure per D-02 / RESEARCH outline:

    **Banner / scope:** Committed disposition set for full-adopt planning. Consumes `10-INVENTORY.md` as path SoT (D-04). States Phase 11 does not edit wrapper, does not write hypr/custom overlays, does not mutate live home. Artifact SoT is this phase-dir file.

    **§1 Pre-flight repo sync gate (D-07) — fully written, not stub:**
    - Purpose: before any full files install, sync live PRESENT personal configs into repo `.config/` so nothing personal exists only on live; repo is fresh-reinstall bootstrap.
    - Sequencing: capture then adopt (Phase 14 executes; this section documents only).
    - Candidate table columns e.g. Path | Live | Repo | Sync action with RESEARCH classes: live-only (`fontconfig/`, `mpv/`, `dolphinrc`, `kdeglobals`, `hypr/hyprland.conf.bak`, `hypr/hyprland-gui.conf`); both-PRESENT verify-diff set (`hypr/hyprland.conf`, hyprland tree, lock/idle/paper confs, fish, kitty, starship, waybar, rofi, swaync); chrome included for archive (D-12).
    - D-08: do not treat live `quickshell/` as personal product SoT after adopt; vendor/submodule remains product SoT.
    - List example command shapes as documentation (`diff -rq`, then live→repo archive copy) without running them this task.

    **§2 Full-adopt flag profile (DISP-02 / D-05 / D-10 / D-32) — fully written:**
    - Residual default block: `SAFE_DEFAULTS=(--core --skip-hyprland --skip-sysupdate)` with Source cite `arch/dots-hyprland.sh` (line ~12); injection for `install` and `install-files` only; residual **unchanged** this phase; default dual-run-safe install remains available (D-10).
    - Independent axes table: drop `--skip-hyprland` | drop `--core` | allow sysupdate (unset skip-sysupdate) — each axis decided **and** documented separately (ROADMAP SC2 / DISP-02 “not assumed all three” satisfied by writing all three).
    - **First full-adopt profile:** drop all three residuals (D-05/D-32 North Star). State Phase 12 encodes opt-in; backup gate and protect re-mark still required; default install must not accidentally use full profile (FULL-02 boundary).
    - Flag stage vocabulary for row tables: e.g. `full-profile` | `drop-skip-hyprland` | `drop-core` | `allow-sysupdate` | `residual-safe` | `n/a`.

    **§3 Axis A — sample rows only (prove D-03; full fill = 11-02):**
    - Table header exactly intent of D-03: Path | Inventory risk | Disposition | Rationale | Flag stage | Inventory source.
    - At minimum three sample rows with locked seeds:
      1. `~/.config/hypr/hyprland.conf` primary session entry → `accept-upstream` (conf→`.old`; not keep-personal primary) — D-15; Flag stage full-profile / drop-skip-hyprland; Inventory source cite Axis A hyprland.conf row in `10-INVENTORY.md`.
      2. Must-keep categories (monitors / workspaces / env including cursor and `ILLOGICAL_IMPULSE_VIRTUAL_ENV`) → `migrate-to-hypr-custom` only those categories — D-16; same inventory cite + conf line evidence optional.
      3. `~/.config/hypr/hyprland/` dir sync → `accept-upstream` — D-18; cite inventory hyprland/ row.
    - Optional fourth sample: `hyprland.lua` → `accept-upstream` (D-19) if needed for assert keyword coverage.
    - Note under table: remaining Axis A HIGH paths completed in plan 11-02.

    **§4–§8 skeleton stubs** with headings + minimal keyword-bearing content so assert section gates pass until expanded:
    - §4 Axis B: heading + one-line note “expand in 11-03” and mention `fish` / `kitty` / `starship` as FUTURE row names (or single stub table row each with disposition `accept-upstream` and inventory cite if cheap — prefer real locked D-27 seed one-liners over empty TODO).
    - §5 Axis C: mention `pacman -Syu`, `illogical-impulse`, asdeps / full install-deps pipeline as future rows (11-03).
    - §6 Dual-run chrome: **Emerged surface** note (omitted from inventory by Phase 10 D-15). Stub rows or prose naming waybar, rofi, swaync with disposition `accept-upstream` and rationale containing accept-remove / D-11 / explicit override of DISP-03 default-keep; archive-in-repo (D-12). Full chrome section polish = 11-04.
    - §7 Lock/idle/paper: stub naming hyprlock and hypridle with `keep-personal` no-touch seed (D-24) and no Quickshell lock investment (D-23); expand 11-04.
    - §8 UNKNOWN/extra: stub hyprlock/ dir `defer` (D-26); `.bak` / gui conf seeds optional.
    - **Sources** footer: `10-INVENTORY.md`, `arch/dots-hyprland.sh`, CONTEXT D-*, personal conf path as needed.

    **Enum discipline:** disposition cells use only `keep-personal` | `migrate-to-hypr-custom` | `accept-upstream` | `merge` | `defer`. Chrome accept-remove uses `accept-upstream` + rationale text (no sixth enum token).

    Update `11-VALIDATION.md`: mark Wave 0 harness / 11-W0 toward green where assert exists; set task rows for DISP-02 toward green after flag profile lands; set `wave_0_complete: true` only if assert script exists and `./scripts/phase11-dispositions-assert.sh` exits 0; leave `nyquist_compliant` true only when that gate passes.

    Do not edit `arch/dots-hyprland.sh`. Do not create hypr/custom files. Do not mutate `~/.config`. Do not delete repo chrome trees.
  </action>
  <verify>
    <automated>./scripts/phase11-dispositions-assert.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` succeeds
    - `./scripts/phase11-dispositions-assert.sh` exits 0
    - File contains SAFE_DEFAULTS and tokens `--core`, `--skip-hyprland`, `--skip-sysupdate`
    - Flag profile states first full-adopt drops all three residuals
    - Residual/default install still-safe or still-default language present (D-10)
    - Pre-flight section present with live→repo archive intent
    - At least one D-03 header line and sample rows for hyprland.conf + migrate-to-hypr-custom
    - waybar, rofi, and swaync strings present (chrome stub or section)
    - `rg -n 'Disposition|SAFE_DEFAULTS|migrate-to-hypr-custom' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` finds matches
  </acceptance_criteria>
  <done>Tracer dispositions exist with complete §1–§2 (DISP-02), sample Axis A D-03 rows, eight-section skeleton, and assert green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor → live XDG / session | Docs + optional read-only `test -e` only; no install or write |
| Dispositions markdown → Phases 12–14 operators | Decision contract; must not double as destructive runbook |
| Assert script → host | May read repo files; must not write under HOME config |
| Personal conf cites → artifact | Path/category cites only; no secret material |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | executor “helpfully” running full install or pre-flight sync | high | mitigate | Plan bans non-dry-run setup/install and XDG writes; assert is read-only; D-10 residual language required |
| T-11-02 | Tampering | dispositions framed as delete-from-repo chrome or rm live configs | medium | mitigate | D-12 archive-in-repo; accept-remove = stop launching / do not migrate; stub chrome uses accept-upstream + rationale |
| T-11-03 | Tampering | accidental edit of wrapper SAFE_DEFAULTS making full the default | high | mitigate | D-10; files_modified excludes arch/dots-hyprland.sh; residual still-default claim gated |
| T-11-04 | Information Disclosure | pasting tokens/secrets from hypr conf into dispositions | low | mitigate | Cite path categories and line ranges only; no env secret values beyond known non-secret path vars already in repo conf |
| T-11-05 | Elevation of Privilege | inventing surfaces that justify broader live mutation later | medium | mitigate | D-04 inventory/emerged cite rule on every Path |
| T-11-SC | Tampering | supply chain / new packages | low | accept | No package installs this phase (Package Legitimacy N/A) |
</threat_model>

<verification>
From REPO_ROOT: `bash -n scripts/phase11-dispositions-assert.sh` and `./scripts/phase11-dispositions-assert.sh` exit 0 with §1–§2 complete dispositions present. Quick: `rg -n 'SAFE_DEFAULTS|full-adopt|Pre-flight|migrate-to-hypr-custom' .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md`.
</verification>

<success_criteria>
Wave 0 harness exists; `11-DISPOSITIONS.md` exists with complete pre-flight + DISP-02 flag profile (drop all three on first full-adopt; residual SAFE_DEFAULTS still default); sample Axis A D-03 rows committed; eight-section skeleton present; assert exits 0; no host or wrapper mutation.
</success_criteria>

<output>
Create `.planning/phases/11-disposition-decisions/11-01-SUMMARY.md` when done
</output>
