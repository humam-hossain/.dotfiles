# Phase 40.1: Clock Pill Padding and Unified Volume Ceiling Ergonomics - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-24
**Phase:** 40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics
**Areas discussed:** Clock Pill Padding & Visual Alignment, Single Source of Truth for Volume Ceiling, Sidebar Volume Slider Ergonomics, Mouse Scroll Volume Increment & OSD Behavior

---

## Clock Pill Padding & Visual Alignment

| Option | Description | Selected |
|--------|-------------|----------|
| 10px horizontal padding | Sets BarGroup padding to 10px or adds 10px horizontal padding, matching the 10px sidebar button and ~9px Resources pill | ✓ |
| 12px horizontal padding | Slightly more generous breathing room for a balanced focal center-right pill | |
| 8px horizontal padding | Compact padding matching smaller utility pills | |

**User's choice:** 10px horizontal padding on the Clock pill.

| Option | Description | Selected |
|--------|-------------|----------|
| ClockWidget.qml scope | Set horizontal padding in ClockWidget.qml so horizontal breathing room is guaranteed without altering BarGroup vertical height/centering | ✓ |
| BarContent.qml scope | Set padding: 10 on rightCenterGroupContent in BarContent.qml (BarGroup level override) | |
| Combined scope | BarGroup padding: 8 + ClockWidget rowLayout margin: 2 | |

**User's choice:** Set horizontal padding in ClockWidget.qml.

| Option | Description | Selected |
|--------|-------------|----------|
| 8px spacer | Retain 8px spacer between time and date (matches Phase 32 COMP-03 / D-08 decision) | ✓ |
| 10px spacer | Increase spacer to 10px | |
| Configurable spacer | Dynamic or configurable spacer | |

**User's choice:** Retain 8px spacer between time and date.

---

## Single Source of Truth for Volume Ceiling (150% / 1.5)

| Option | Description | Selected |
|--------|-------------|----------|
| config.json | config.json as single source of truth (under "audio": {"volumeCeiling": 1.5}), read natively by Quickshell and consumed by Hyprland keybinds | ✓ |
| Environment/State file | ~/.config/environment.d/audio.conf or shared env var DESKTOP_VOLUME_CEILING=1.5 | |
| Dedicated Lua/QML module | Synchronized constants module | |

**User's choice:** config.json as the single source of truth.

| Option | Description | Selected |
|--------|-------------|----------|
| Lua evaluation at startup/reload | In stow/hypr/.config/hypr/custom/keybinds.lua, parse config.json once and bind with resolved ceiling (zero key-repeat overhead) | ✓ |
| Lightweight shell helper script | scripts/audio-control.sh invoked by keybinds | |
| Inline subshell | wpctl with inline jq subshell | |

**User's choice:** Lua evaluation at Hyprland startup/reload.

| Option | Description | Selected |
|--------|-------------|----------|
| Audio.maxVolume singleton property | Expose readonly property real maxVolume: Config.options?.audio?.volumeCeiling ?? 1.5 on Audio.qml singleton | ✓ |
| Direct Config.options access | Directly read Config.options in each individual QML component | |

**User's choice:** Expose readonly property real maxVolume on Audio.qml singleton.

---

## Sidebar Volume Slider Ergonomics (QuickSliders.qml)

| Option | Description | Selected |
|--------|-------------|----------|
| Dynamic binding to Audio.maxVolume | Dynamically bind to: Audio.maxVolume on QuickSlider | ✓ |
| Fixed to 1.5 | Hardcoded 1.5 on QuickSlider component | |

**User's choice:** Dynamically bind to: Audio.maxVolume on the volume QuickSlider.

| Option | Description | Selected |
|--------|-------------|----------|
| 100% stop indicator | Add visual stop indicator at 100% (1.0) via stopIndicatorValues: [1.0 / Audio.maxVolume] | ✓ |
| No stop indicator | Continuous smooth bar from 0 to 150% | |

**User's choice:** Add a visual stop indicator at 100% (1.0) via stopIndicatorValues: [1.0 / Audio.maxVolume].

| Option | Description | Selected |
|--------|-------------|----------|
| Percentage tooltip + adjusted icon | Show live percentage tooltip up to 150%, adjust nearFull threshold | |
| Percentage tooltip only | Tooltip without adjusting icon | |
| Keep default slider behavior | Keep default slider behavior without tooltip customization | ✓ |

**User's choice:** Keep default slider behavior without tooltip customization.

---

## Mouse Scroll Volume Increment & OSD Behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Dynamic clamp at Audio.maxVolume | Clamp with Math.min(root.maxVolume, Audio.sink.audio.volume + step) in Audio.qml | ✓ |
| Hardcoded 1.5 clamp | Fixed 1.5 clamp in incrementVolume() | |

**User's choice:** Clamp with Math.min(root.maxVolume, Audio.sink.audio.volume + step) in Audio.qml.

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-unmute on volume increase | Audio.sink.audio.muted = false if volume is raised, preserving Phase 03 behavior | ✓ |
| Keep mute state | Do not clear mute when incrementing volume | |

**User's choice:** Auto-unmute when incrementing volume.

| Option | Description | Selected |
|--------|-------------|----------|
| 2% step size (1% below 10%) | Keep 2% step size for mouse scroll, maintaining parity with Hyprland's 2% keybind step | ✓ |
| 5% step size | 5% step size across the board | |

**User's choice:** Keep 2% step size (1% below 10% volume) for mouse scroll.

---

## the agent's Discretion

None — all decisions explicitly directed by the user.

## Deferred Ideas

None — discussion remained focused on Phase 40.1 scope.
