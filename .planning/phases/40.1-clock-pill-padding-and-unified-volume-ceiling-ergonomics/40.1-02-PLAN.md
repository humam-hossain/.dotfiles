---
phase: 40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics
plan: "02"
type: execute
wave: 2
depends_on:
  - "40.1-01"
files_modified:
  - restow/quickshell/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml
  - restow/quickshell/.config/quickshell/ii/services/Audio.qml
  - scripts/phase40.1-clock-volume-assert.sh
autonomous: true
requirements:
  - VOL-02

estimate:
  tokens: 42000
  raw_tokens: 20000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "QuickSliders.qml volume slider dynamically binds upper limit to: Audio.maxVolume, allowing smooth drag from 0% to 150% (VOL-02, D-06)."
    - "QuickSliders.qml volume slider renders a tactile/visual stop indicator at 100% volume via stopIndicatorValues: [1.0] without double normalization (VOL-02, D-07)."
    - "QuickSliders.qml volume slider tooltip explicitly displays true volume percentage via tooltipContent: `${Math.round(value * 100)}%` (VOL-02, D-08)."
    - "Audio.qml incrementVolume() dynamically clamps volume increase to root.maxVolume via Math.min(root.maxVolume, Audio.sink.audio.volume + step), allowing mouse wheel scrolling to reach 150% (VOL-02, D-09)."
    - "Audio.qml incrementVolume() automatically unmutes audio (Audio.sink.audio.muted = false) whenever volume is raised (VOL-02, D-10)."
    - "Audio.qml volume adjustments enforce 2% step size (1% below 10% volume) matching Hyprland keybind granularity (VOL-02, D-11)."
    - "All QML modifications are deployed as leaf symlinks via restow/quickshell/ without modifying vendor/dots-hyprland (INTG-01, T-40.1-01)."
    - "Automated test harness scripts/phase40.1-clock-volume-assert.sh and ./arch/dots-hyprland.sh verify --strict pass with FAIL=0 FINDINGS=0 (INTG-01, INTG-03)."
  artifacts:
    - path: "restow/quickshell/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml"
      provides: "Right sidebar quick sliders overlay binding volume slider to Audio.maxVolume with 100% stop notch and percentage tooltip"
      contains: "to: Audio.maxVolume"
    - path: "restow/quickshell/.config/quickshell/ii/services/Audio.qml"
      provides: "PipeWire audio service overlay with dynamic maxVolume clamp, auto-unmute on increment, and 2% step size"
      contains: "Math.min(root.maxVolume"
    - path: "scripts/phase40.1-clock-volume-assert.sh"
      provides: "Completed 5-section automated assertion test harness validating symlinks, clock AST, ceiling contract, slider/scroll ergonomics, and strict verification"
      contains: "phase40.1-clock-volume-assert"
  key_links:
    - from: "restow/quickshell/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml"
      to: "restow/quickshell/.config/quickshell/ii/services/Audio.qml"
      via: "reactive property binding to: Audio.maxVolume and Audio.sink.audio.volume"
      pattern: "to: Audio\\.maxVolume"
    - from: "restow/quickshell/.config/quickshell/ii/services/Audio.qml"
      to: "scripts/phase40.1-clock-volume-assert.sh"
      via: "Section 4 headless JS simulation verifying step logic, auto-unmute, and dynamic clamping"
      pattern: "incrementVolume"
    - from: "scripts/phase40.1-clock-volume-assert.sh"
      to: "arch/dots-hyprland.sh"
      via: "strict repository verification gate invocation"
      pattern: "\\./arch/dots-hyprland\\.sh verify --strict"
---

<objective>
Implement the right sidebar volume slider ergonomics in `QuickSliders.qml` with dynamic 0-150% ceiling, 100% visual stop indicator, and accurate percentage tooltip, update `Audio.qml` mouse scroll volume controls with dynamic 150% clamping and auto-unmute, and complete the comprehensive automated verification suite across all 5 sections.

Purpose: Deliver full volume slider and mouse wheel ergonomics up to the 150% software amplification limit with zero acoustic shock hazard and pristine repository verification.
Output: Restow overlay for `QuickSliders.qml`, updated `Audio.qml`, and complete 5-section pass in `scripts/phase40.1-clock-volume-assert.sh` alongside clean `./arch/dots-hyprland.sh verify --strict`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-CONTEXT.md
@.planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-RESEARCH.md
@.planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-PATTERNS.md
@.planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-VALIDATION.md
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml
@vendor/dots-hyprland/dots/.config/quickshell/ii/widgets/StyledSlider.qml
@restow/quickshell/.config/quickshell/ii/services/Audio.qml
@scripts/phase40.1-clock-volume-assert.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Right sidebar volume slider in QuickSliders.qml with dynamic 0-150% ceiling, [1.0] stop indicator, and tooltip percentage (VOL-02, D-06, D-07, D-08)</name>
  <read_first>
    - vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml
    - vendor/dots-hyprland/dots/.config/quickshell/ii/widgets/StyledSlider.qml
    - restow/quickshell/.config/quickshell/ii/services/Audio.qml
    - .planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-RESEARCH.md
    - .planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-PATTERNS.md
  </read_first>
  <files>restow/quickshell/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml, scripts/phase40.1-clock-volume-assert.sh</files>
  <action>
    Author the `QuickSliders.qml` overlay under `restow/quickshell/` to bind the volume slider to `Audio.maxVolume`, add a tactile stop notch at 100% volume, and display the true volume percentage (per VOL-02, D-06, D-07, D-08, T-40.1-01, T-40.1-03):

    1. Author `restow/quickshell/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml`:
       - Create parent directory `restow/quickshell/.config/quickshell/ii/modules/ii/sidebarRight/` if needed.
       - Base on `vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml`.
       - In the volume slider Loader (`active: Config.options.sidebar.quickSliders.showVolume`), update `sourceComponent: QuickSlider`:
         * Bind upper limit: `to: Audio.maxVolume` (D-06).
         * Set stop indicator: `stopIndicatorValues: [1.0]` (D-07).
           **Pitfall Prevention:** Supply raw value `[1.0]` because `StyledSlider.TrackDot` internally calculates `normalizedValue = (value - root.from) / (root.to - root.from)`. Supplying `[1.0 / Audio.maxVolume]` would cause double normalization and position the notch at 44.4% (67% volume) instead of 66.7% (100% volume).
         * Set explicit tooltip: `tooltipContent: `${Math.round(value * 100)}%`` (D-08).
           **Pitfall Prevention:** Default `StyledSlider` tooltip computes `((value - from) / (to - from)) * 100`, which would show "67%" at volume 1.0. Explicitly formatting `value * 100` guarantees accurate 0% to 150% readouts.
         * Retain safe property navigation: `value: Audio.sink?.audio?.volume ?? 0`.
         * In `onMoved`: `if (Audio.sink?.audio) { Audio.sink.audio.volume = value }`.
       - Reversibility: reversible.

    2. Deploy `QuickSliders.qml` via GNU Stow:
       - Check if live `~/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml` is a regular file.
       - If regular file, move to `QuickSliders.qml.bak` before stowing.
       - Run `stow --verbose=5 --no-folding -d "$REPO_ROOT/restow" -t "$HOME" quickshell`.
       - Verify `~/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml` is a valid symlink to `restow/quickshell/...`.

    3. Update Section 4 in `scripts/phase40.1-clock-volume-assert.sh`:
       - Assert `QuickSliders.qml` volume slider contains `to: Audio.maxVolume`.
       - Assert `QuickSliders.qml` volume slider contains `stopIndicatorValues: [1.0]`.
       - Assert `QuickSliders.qml` volume slider contains `tooltipContent: \`${Math.round(value * 100)}%\``.
  </action>
  <verify>
    <automated>./scripts/phase40.1-clock-volume-assert.sh --section 4</automated>
    <fails_when>non-zero exit code or Section 4 slider assertions fail</fails_when>
    <automated>test -L "$HOME/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml"</automated>
    <fails_when>exit code 1 or QuickSliders.qml is not a valid symlink</fails_when>
    <automated>grep -q "to: Audio.maxVolume" restow/quickshell/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml &amp;&amp; grep -q "stopIndicatorValues: \[1.0\]" restow/quickshell/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml</automated>
    <fails_when>exit code 1 or missing volume slider bindings in QuickSliders.qml</fails_when>
  </verify>
  <acceptance_criteria>
    - `QuickSliders.qml` volume slider binds `to: Audio.maxVolume`.
    - `QuickSliders.qml` sets `stopIndicatorValues: [1.0]` without double normalization.
    - `QuickSliders.qml` sets `tooltipContent: `${Math.round(value * 100)}%``.
    - Live `QuickSliders.qml` is a valid leaf symlink resolving to `restow/quickshell/`.
    - Section 4 assertions for `QuickSliders.qml` pass cleanly.
  </acceptance_criteria>
  <done>Right sidebar volume slider has dynamic 0-150% ceiling, 100% stop notch, and percentage tooltip, deployed cleanly via restow.</done>
</task>

<task type="auto">
  <name>Task 2: Mouse scroll volume increment clamping, auto-unmute, and 2% step in Audio.qml (VOL-02, D-09, D-10, D-11)</name>
  <read_first>
    - restow/quickshell/.config/quickshell/ii/services/Audio.qml
    - vendor/dots-hyprland/dots/.config/quickshell/ii/services/Audio.qml
    - .planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-RESEARCH.md
    - .planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-PATTERNS.md
    - scripts/phase40.1-clock-volume-assert.sh
  </read_first>
  <files>restow/quickshell/.config/quickshell/ii/services/Audio.qml, scripts/phase40.1-clock-volume-assert.sh</files>
  <action>
    Update `incrementVolume()` and `decrementVolume()` in `restow/quickshell/.config/quickshell/ii/services/Audio.qml` to clamp dynamically to `root.maxVolume`, unmute on increment, and enforce 2% step size, and build out headless JS verification in the test harness (per VOL-02, D-09, D-10, D-11, T-40.1-03):

    1. In `restow/quickshell/.config/quickshell/ii/services/Audio.qml`:
       - Update `incrementVolume()`:
         ```qml
         function incrementVolume() {
             if (Audio.sink?.audio) {
                 Audio.sink.audio.muted = false; // Auto-unmute on increment (D-10)
                 const currentVolume = Audio.value;
                 const step = currentVolume < 0.1 ? 0.01 : 0.02; // 2% step size (D-11)
                 Audio.sink.audio.volume = Math.min(root.maxVolume, Audio.sink.audio.volume + step); // Dynamic clamp (D-09)
             }
         }
         ```
       - Update `decrementVolume()`:
         ```qml
         function decrementVolume() {
             if (Audio.sink?.audio) {
                 const currentVolume = Audio.value;
                 const step = currentVolume < 0.1 ? 0.01 : 0.02; // 2% step size (D-11)
                 Audio.sink.audio.volume = Math.max(0, Audio.sink.audio.volume - step);
             }
         }
         ```
       - Preserve `hardMaxValue: 2.00` safety invariant on `root`.
       - Reversibility: reversible.

    2. In `scripts/phase40.1-clock-volume-assert.sh`, implement headless JS simulation in Section 4:
       - Use `node -e "..."` or headless QML to test `incrementVolume()` and `decrementVolume()` logic:
         * Test 1: Increment from 0.05 steps by 0.01 (result 0.06).
         * Test 2: Increment from 0.50 steps by 0.02 (result 0.52).
         * Test 3: Increment from 1.49 with `maxVolume = 1.5` clamps exactly to 1.50 without exceeding.
         * Test 4: Increment while `muted = true` sets `muted = false`.
         * Test 5: Decrement from 0.01 with step 0.01 clamps to 0.00 without going negative.
  </action>
  <verify>
    <automated>./scripts/phase40.1-clock-volume-assert.sh --section 4</automated>
    <fails_when>non-zero exit code or headless JS simulation fails</fails_when>
    <automated>grep -q "Audio.sink.audio.muted = false" restow/quickshell/.config/quickshell/ii/services/Audio.qml &amp;&amp; grep -q "Math.min(root.maxVolume" restow/quickshell/.config/quickshell/ii/services/Audio.qml</automated>
    <fails_when>exit code 1 or missing auto-unmute / dynamic clamp in Audio.qml</fails_when>
  </verify>
  <acceptance_criteria>
    - `Audio.qml` `incrementVolume()` dynamically clamps to `root.maxVolume`.
    - `Audio.qml` `incrementVolume()` sets `Audio.sink.audio.muted = false`.
    - Step size is 2% (0.02) above 10% and 1% (0.01) below 10%.
    - Headless JS simulation in Section 4 passes all step, clamp, and unmute assertions.
  </acceptance_criteria>
  <done>Audio.qml incrementVolume dynamically clamps to 150%, automatically unmutes audio, and passes headless JS simulation.</done>
</task>

<task type="auto">
  <name>Task 3: Full assertion test harness completion, restow verification, and strict repository porcelain check (INTG-01, INTG-03)</name>
  <read_first>
    - scripts/phase40.1-clock-volume-assert.sh
    - arch/dots-hyprland.sh
    - .planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-VALIDATION.md
    - .planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-RESEARCH.md
  </read_first>
  <files>scripts/phase40.1-clock-volume-assert.sh</files>
  <action>
    Finalize all sections in `scripts/phase40.1-clock-volume-assert.sh` and execute the complete verification suite verifying zero findings and zero git working-tree churn (per INTG-01, INTG-03, T-40.1-01, T-40.1-04):

    1. Complete all 5 sections of `scripts/phase40.1-clock-volume-assert.sh`:
       - Section 1: Restow Isolation & Deployment Verification
         * All 4 QML files (`ClockWidget.qml`, `Config.qml`, `Audio.qml`, `QuickSliders.qml`) exist in `restow/quickshell/`.
         * Live symlinks in `~/.config/quickshell/` resolve to `restow/quickshell/`.
         * Submodule `vendor/dots-hyprland` has 0 changes.
       - Section 2: Clock Pill Padding & Geometry AST Verification
         * 10px breathing room (5px widget + 5px BarGroup), 8px non-glyph spacer, second precision, and full date.
       - Section 3: Single Source of Truth Ceiling Contract
         * `config.json` declares `"volumeCeiling": 1.5` in repo and live `$HOME`.
         * `keybinds.lua` unbinds `XF86AudioRaiseVolume` and parses ceiling via pure Lua with `-l <ceiling>`.
         * `Config.qml` declares `property real volumeCeiling: 1.5`.
         * `Audio.qml` defines `maxVolume: Config.options?.audio?.volumeCeiling ?? 1.5`.
       - Section 4: Quickshell Volume Ergonomics & Behavior
         * `QuickSliders.qml` volume slider binds `to: Audio.maxVolume`, `stopIndicatorValues: [1.0]`, and percentage tooltip.
         * `Audio.qml` dynamic clamp, auto-unmute, 2% step.
         * Headless JS simulation asserting step math, boundary clamping, and auto-unmute.
       - Section 5: Strict Repository & Submodule Porcelain Verification
         * Run `./arch/dots-hyprland.sh verify --strict` requiring `FAIL=0 FINDINGS=0`.
         * Assert git porcelain diff before vs after test run is byte-identical.

    2. Run full suite:
       - `./scripts/phase40.1-clock-volume-assert.sh`
       - `./arch/dots-hyprland.sh verify --strict`
  </action>
  <verify>
    <automated>./scripts/phase40.1-clock-volume-assert.sh</automated>
    <fails_when>non-zero exit code or FAIL > 0 or FINDINGS > 0</fails_when>
    <automated>./arch/dots-hyprland.sh verify --strict</automated>
    <fails_when>non-zero exit code or verify findings detected</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/phase40.1-clock-volume-assert.sh` runs all 5 sections and exits 0 with FAIL=0 FINDINGS=0.
    - `./arch/dots-hyprland.sh verify --strict` exits 0 with FAIL=0 FINDINGS=0.
    - `git -C vendor/dots-hyprland status --porcelain` is completely empty.
    - Zero unexpected modifications or untracked files left in working tree.
  </acceptance_criteria>
  <done>All 5 sections of phase40.1-clock-volume-assert.sh and strict repository verification pass with zero failures and zero findings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slider Value Normalization | StyledSlider / TrackDot component math calculating dot positions from user/config input. |
| Audio Gain / Hardware Output | PipeWire sink volume state driven by slider drag and scroll wheel increments. |
| Filesystem & Stow Deployment | Leaf symlinks deployed by GNU Stow from `restow/quickshell/` to `$HOME/.config/quickshell/`. |
| Git Submodule Boundary | Pristine `vendor/dots-hyprland` tree tracked as git submodule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40.1-01 | Tampering | Git submodule `vendor/dots-hyprland` | high | mitigate | Author all QML modifications exclusively in `restow/quickshell/`. Upstream submodule remains untouched (INTG-01, INTG-03). |
| T-40.1-02 | Elevation of Privilege / Command Injection | Lua `config.json` parser | medium | mitigate | Strict regex pattern `[%d%.]+` with `tonumber(ceiling) > 0` validation before binding into `wpctl` command string (D-04). |
| T-40.1-03 | Denial of Service / Acoustic Shock | PipeWire audio output | medium | mitigate | Clamp volume ceiling strictly to 1.5 (150%) across WirePlumber (`-l <ceiling>`) and Quickshell (`Audio.maxVolume`). Preserve `hardMaxValue: 2.00` safety clamp in `Audio.qml`. |
| T-40.1-04 | Working-Tree Churn | Test harness execution | low | mitigate | Test harness uses isolated temporary files in `/tmp` tracked via `TMP_FILES` with `trap cleanup EXIT`. Compare `git status --porcelain` before and after execution to guarantee zero drift. |
</threat_model>

## Artifacts this phase produces

- `restow/quickshell/.config/quickshell/ii/modules/ii/sidebarRight/QuickSliders.qml`: Right sidebar quick sliders overlay binding volume slider to `Audio.maxVolume` with 100% stop notch and percentage tooltip.
- `restow/quickshell/.config/quickshell/ii/services/Audio.qml`: PipeWire audio service overlay with dynamic `maxVolume` clamp, auto-unmute on increment, and 2% step size.
- `scripts/phase40.1-clock-volume-assert.sh`: Completed 5-section automated assertion test harness validating symlinks, clock AST, ceiling contract, slider/scroll ergonomics, and strict verification.

## Multi-Source Coverage Audit

| Source | Item ID / Reference | Description | Status | Covered By | Notes |
|---|---|---|---|---|---|
| REQ | VOL-02 | Right sidebar volume slider and mouse scroll volume increments allow reaching the 150% upper ceiling defined by the single source of truth. | COVERED | Task 1, Task 2 | QuickSliders.qml volume slider bound to Audio.maxVolume; Audio.qml incrementVolume clamped to root.maxVolume. |
| CONTEXT | D-06 | QuickSliders.qml volume slider dynamically binds to: Audio.maxVolume. | COVERED | Task 1 | Dynamic ceiling across 0.0 to 1.5. |
| CONTEXT | D-07 | QuickSliders.qml sets stopIndicatorValues: [1.0]. | COVERED | Task 1 | Raw 1.0 boundary value avoids double normalization in TrackDot. |
| CONTEXT | D-08 | QuickSliders.qml sets explicit tooltipContent: `${Math.round(value * 100)}%`. | COVERED | Task 1 | Displays accurate volume percentage from 0% to 150%. |
| CONTEXT | D-09 | Audio.qml incrementVolume() dynamically clamps to root.maxVolume. | COVERED | Task 2 | Math.min(root.maxVolume, Audio.sink.audio.volume + step). |
| CONTEXT | D-10 | Audio.qml incrementVolume() automatically unmutes (Audio.sink.audio.muted = false). | COVERED | Task 2 | Preserves auto-unmute ergonomics. |
| CONTEXT | D-11 | 2% step size retained for volume increments. | COVERED | Task 2 | const step = currentVolume < 0.1 ? 0.01 : 0.02. |
| RESEARCH | T-40.1-01 | Submodule tampering prevention. | COVERED | Task 1, Task 3 | All changes in restow/quickshell/; dots-hyprland verify --strict passes. |
| RESEARCH | T-40.1-03 | Audio spike / acoustic shock prevention. | COVERED | Task 1, Task 2 | Software amplification capped at 150%; hardMaxValue: 2.00 preserved. |
| RESEARCH | T-40.1-04 | Working-tree churn prevention. | COVERED | Task 3 | Porcelain diff before/after execution verified byte-identical. |

<verification>
Execute the full test harness:
`./scripts/phase40.1-clock-volume-assert.sh`
Execute strict submodule and symlink verification:
`./arch/dots-hyprland.sh verify --strict`
</verification>

<success_criteria>
1. `QuickSliders.qml` volume slider smoothly drags up to 150% (1.5) bound to `Audio.maxVolume`.
2. Stop indicator notch is rendered at exactly 100% volume on the slider track without double normalization.
3. Slider tooltip displays accurate volume percentage from 0% to 150%.
4. `Audio.qml` `incrementVolume()` dynamically clamps at 150%, automatically unmutes audio, and enforces 2% steps.
5. `scripts/phase40.1-clock-volume-assert.sh` passes all 5 sections with FAIL=0 FINDINGS=0.
6. `./arch/dots-hyprland.sh verify --strict` exits 0 with FAIL=0 FINDINGS=0 and zero git working-tree churn.
</success_criteria>

<output>
Create `.planning/phases/40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics/40.1-02-SUMMARY.md` when done
</output>
