---
phase: 40.1-clock-pill-padding-and-unified-volume-ceiling-ergonomics
plan: "01"
subsystem: ui-audio-bar
tags: [quickshell, qml, hyprland, lua, volume, clock, padding, pipewire, restow]

requires:
  - phase: 40-notification-center-quick-dismiss-smart-interaction
    provides: Restow overlay pattern, 5-section test harness architecture, strict submodule verification
provides:
  - scripts/phase40.1-clock-volume-assert.sh test harness with 5 sections and porcelain tracking
  - ClockWidget.qml status bar clock pill with 10px horizontal breathing room
  - Single source of truth volumeCeiling: 1.5 in config.json
  - Pure Lua config.json parser in keybinds.lua with zero repeat overhead and dynamic wpctl ceiling
  - Config.qml restow overlay declaring property real volumeCeiling: 1.5
  - Audio.qml restow overlay exposing readonly property real maxVolume
affects:
  - 40.1-02

actuals:
  tokens: 12500
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - Pure Lua regex file parsing at startup/reload with fallback default
    - Quickshell JsonAdapter schema declaration for custom properties
    - Leaf symlink deployment via GNU Stow with pre-stow backup (.bak) guards
    - 5-section automated test harness with git porcelain baseline snapshot diffing

key-files:
  created:
    - scripts/phase40.1-clock-volume-assert.sh
    - restow/quickshell/.config/quickshell/ii/modules/common/Config.qml
    - restow/quickshell/.config/quickshell/ii/services/Audio.qml
  modified:
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml
    - capture/ii/.config/illogical-impulse/config.json
    - stow/hypr/.config/hypr/custom/keybinds.lua

key-decisions:
  - "D-01: Adjusted ClockWidget.qml to enforce 10px horizontal breathing room via 5px left/right margins on rowLayout plus BarGroup's 5px padding."
  - "D-02: Retained 8px non-glyph spacer (implicitWidth: 8) between time and date strings preserving DateTime.time second precision and DateTime.longDate."
  - "D-03: Added volumeCeiling: 1.5 to capture/ii/.config/illogical-impulse/config.json and synced to live ~/.config/illogical-impulse/config.json."
  - "D-04: In keybinds.lua, unbound upstream XF86AudioRaiseVolume, parsed config.json in pure Lua at startup, and bound wpctl set-volume with dynamic -l ceiling with zero subshell overhead during key repeats."
  - "D-05: Declared property real volumeCeiling: 1.5 in Config.qml and exposed readonly property real maxVolume in Audio.qml as centralized Quickshell ceiling contract."

patterns-established:
  - "Dynamic implicitWidth calculation accounting for RowLayout anchor margins: rowLayout.implicitWidth + rowLayout.anchors.leftMargin + rowLayout.anchors.rightMargin"
  - "Subprocess-free Lua startup parsing of desktop JSON configurations"

requirements-completed:
  - CLOCK-01
  - VOL-01

coverage:
  - id: D1
    description: "Phase 40.1 assertion test harness with 5 sections, CLI flags, non-root check, and porcelain baseline tracking"
    requirement: "INTG-03"
    verification:
      - kind: integration
        ref: "./scripts/phase40.1-clock-volume-assert.sh --section 1"
        status: pass
    human_judgment: false
  - id: D2
    description: "ClockWidget.qml horizontal breathing room expanded to 10px with preserved 8px spacer and second precision"
    requirement: "CLOCK-01"
    verification:
      - kind: unit
        ref: "./scripts/phase40.1-clock-volume-assert.sh --section 2"
        status: pass
    human_judgment: false
  - id: D3
    description: "Single source of truth volume ceiling (1.5 / 150%) across config.json, keybinds.lua, Config.qml, and Audio.qml"
    requirement: "VOL-01"
    verification:
      - kind: integration
        ref: "./scripts/phase40.1-clock-volume-assert.sh --section 3"
        status: pass
    human_judgment: false

duration: 10min
completed: 2026-09-24
status: complete
---

# Phase 40.1 Plan 01: Clock Pill Padding and Single Source of Truth Volume Ceiling Summary

**Established automated test harness `scripts/phase40.1-clock-volume-assert.sh`, restored 10px horizontal breathing room to `ClockWidget.qml`, and established desktop-wide 150% (1.5) volume ceiling single source of truth across `config.json`, `keybinds.lua`, `Config.qml`, and `Audio.qml`.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-09-24T22:01:00Z
- **Completed:** 2026-09-24T22:11:00Z
- **Tasks:** 3 completed
- **Files modified/created:** 6 files

## Accomplishments

- Established `scripts/phase40.1-clock-volume-assert.sh` 5-section test harness supporting `--section <1-5>`, `--syntax`, non-root execution checks, and git porcelain baseline tracking.
- Updated `ClockWidget.qml` with `anchors.leftMargin: 5` and `anchors.rightMargin: 5` on `RowLayout` along with dynamic `implicitWidth` calculation, providing 10px total horizontal breathing room matching adjacent status bar pills while retaining the 8px non-glyph spacer, second precision, and full date strings.
- Added `"volumeCeiling": 1.5` under `"audio"` in `config.json` (repository capture and live `$HOME`), establishing the single canonical volume ceiling.
- Updated `keybinds.lua` to unbind upstream `XF86AudioRaiseVolume`, parse `config.json` via pure Lua once at compositor startup/reload with safe fallback, and bind `wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+ -l <ceiling>` with zero subprocess overhead during key repeats.
- Authored restow overlays for `Config.qml` (declaring `property real volumeCeiling: 1.5`) and `Audio.qml` (exposing `readonly property real maxVolume: Config.options?.audio?.volumeCeiling ?? 1.5`).
- Deployed overlays via GNU Stow into `~/.config/quickshell/ii/` as valid leaf symlinks with backup guards and zero submodule drift.

## Task Commits

Each task was committed atomically:

1. **Task 1: Tracer — Test harness foundation and symlink integrity checks** - `551db27` (test)
2. **Task 2: Status bar clock pill horizontal padding in ClockWidget.qml** - `23c91ab` (feat)
3. **Task 3: Single source of truth volume ceiling across config, keybinds, and quickshell** - `5d171ab` (feat)

## Self-Check: PASSED

- `scripts/phase40.1-clock-volume-assert.sh --section 1`: PASSED
- `scripts/phase40.1-clock-volume-assert.sh --section 2`: PASSED
- `scripts/phase40.1-clock-volume-assert.sh --section 3`: PASSED
- `scripts/phase40.1-clock-volume-assert.sh --syntax`: PASSED
- `vendor/dots-hyprland` submodule: Pristine (0 diff)
