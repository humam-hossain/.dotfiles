---
phase: 06-runtime-failure-inventory
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [.planning/phases/06-runtime-failure-inventory/CHECKLIST.md]
autonomous: false
requirements: [BUG-01, BUG-02, BUG-03]

must_haves:
  truths:
    - "CHECKLIST.md contains repro steps for each Confirmed failure"
    - "Manual verification session promotes Discovered → Confirmed"
    - "CHECKLIST.md entry format includes BUG-ID + numbered repro steps + expected outcome"
  artifacts:
    - path: ".planning/phases/06-runtime-failure-inventory/CHECKLIST.md"
      provides: "Reproduction checklist for confirmed failures"
      min_lines: 30
  key_links:
    - from: "CHECKLIST.md"
      to: "FAILURES.md"
      via: "reference"
      pattern: "FAILURES\\.md"
---

<objective>
Create reproduction checklist from FAILURES.md Discovered entries after manual verification session.

Purpose: Convert Discovered entries to Confirmed via manual interactive verification, produce CHECKLIST.md for Phase 7-9.
Output: CHECKLIST.md with repro steps for Confirmed failures
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-runtime-failure-inventory/06-CONTEXT.md
@.planning/phases/06-runtime-failure-inventory/06-RESEARCH.md

/scripts/nvim-audit-failures.sh — output consumed by manual verification
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual verification of Discovered failures</name>
  <files>N/A (manual interactive session)</files>
  <action>
Per D-09 and D-10: Run manual verification session to promote Discovered → Confirmed.

1. Open FAILURES.md and review each Discovered entry
2. For each Discovered entry, attempt to reproduce in interactive Neovim:
   - Follow the Repro Steps if provided
   - If no repro steps, create step-by-step instructions
   - Document actual behavior vs expected behavior
3. Mark each as:
   - Confirmed: script + manual both reproduce
   - Won't Fix: intentional behavior (D-25)
   - By Design: expected behavior (D-25)
   - Duplicate: already listed elsewhere (D-25)
   - Out of Scope: not in phase scope (D-25)

4. Update FAILURES.md Status column accordingly
</action>
  <verify>
    <automated>MISSING — manual interactive verification required</automated>
  </verify>
  <done>
FAILURES.md shows Confirmed entries (from manual verification) in Status column
</done>

<task type="auto">
  <name>Task 2: Generate CHECKLIST.md from Confirmed failures</name>
  <files>.planning/phases/06-runtime-failure-inventory/CHECKLIST.md</files>
  <action>
Create CHECKLIST.md per D-26 and D-59 through D-61:

1. Read FAILURES.md, filter entries where Status = Confirmed (D-24)
2. Also include Won't Fix / By Design entries with note (per D-26 context)

3. Format each entry:
   ```markdown
   ## BUG-NNN — [Description]

   **Owner:** [Owner from FAILURES.md]
   **Status:** Confirmed
   **Provenance:** [source(s)]

   ### Reproduction Steps

   1. [Step 1]
   2. [Step 2]
   ...

   ### Expected Outcome

   [What should happen when bug is fixed]
   ```

4. Won't Fix / By Design format:
   ```markdown
   ## BUG-NNN — [Description]

   > Note: Won't Fix — [reason from D-25]
   ```

5. Header: Arch Linux, Neovim version, tool versions (same as FAILURES.md)
6. Footer: Total Confirmed, Total Won't Fix, etc.
</action>
  <verify>
    <automated>
# Verify CHECKLIST.md exists with expected structure
grep -q "^## BUG-" .planning/phases/06-runtime-failure-inventory/CHECKLIST.md
grep -q "Expected Outcome" .planning/phases/06-runtime-failure-inventory/CHECKLIST.md
</automated>
  </verify>
  <done>
CHECKLIST.md exists with:
- Header with environment info
- At least one BUG-NNN section with numbered repro steps
- Expected Outcome for each entry
- Won't Fix/By Design entries include > Note: block
</done>

</tasks>

<verification>
- [ ] Manual verification session complete
- [ ] FAILURES.md updated with Confirmed/Won't Fix/By Design/...
- [ ] CHECKLIST.md generated with repro steps
- [ ] Each CHECKLIST entry has BUG-ID + numbered repro steps + expected outcome
</verification>

<success_criteria>
CHECKLIST.md created with:
- All Confirmed entries from FAILURES.md present
- Repro steps are actionable and numbered
- Won't Fix entries include developer note
- Count summary in footer
</success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-failure-inventory/06-02-SUMMARY.md`
</output>