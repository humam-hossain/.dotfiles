---
phase: 01-reliability-and-portability-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .config/nvim/lua/core/keymaps.lua
  - .config/nvim/lua/plugins/bufferline.lua
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
must_haves:
  truths:
    - "Pressing `<C-q>` closes the current buffer instead of implicitly exiting Neovim."
    - "Modified buffers require normal confirm/save behavior before they are closed."
    - "Autosave runs only on a conservative trigger and skips special or unsupported buffers."
    - "Bufferline close actions follow the same buffer-first semantics as keyboard close actions."
  artifacts:
    - path: ".config/nvim/lua/core/keymaps.lua"
      provides: "Buffer-first close mapping, conservative autosave policy, and explicit split close behavior"
    - path: ".config/nvim/lua/plugins/bufferline.lua"
      provides: "Bufferline close command aligned with buffer-first non-forced close semantics"
  key_links:
    - from: ".config/nvim/lua/core/keymaps.lua"
      to: "Neovim built-in `:confirm bdelete`"
      via: "`<C-q>` mapping"
      pattern: "confirm bdelete"
    - from: ".config/nvim/lua/plugins/bufferline.lua"
      to: ".config/nvim/lua/core/keymaps.lua"
      via: "matching non-forced buffer close behavior"
      pattern: "bdelete"
---

<objective>
Simplify save/quit behavior to the locked buffer-first lifecycle model and reduce autosave to one safe path.

Purpose: Implement D-01 through D-09 and align mouse-driven buffer closes with keyboard-driven closes so core editing flow is predictable.
Output: Updated keymap/autocmd behavior in `core.keymaps` and aligned bufferline close semantics.
</objective>

<execution_context>
@/home/pera/.codex/get-shit-done/workflows/execute-plan.md
@/home/pera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reliability-and-portability-baseline/01-CONTEXT.md
@.planning/phases/01-reliability-and-portability-baseline/01-RESEARCH.md
@.config/nvim/lua/core/keymaps.lua
@.config/nvim/lua/core/options.lua
@.config/nvim/lua/plugins/bufferline.lua

<interfaces>
From `.config/nvim/lua/core/keymaps.lua`:
```lua
vim.keymap.set("n", "<C-q>", function()
	local buf_count = #vim.fn.filter(vim.fn.range(1, vim.fn.bufnr("$")), "buflisted(v:val)")
	local win_count = vim.fn.winnr("$")
	...
end, { desc = "Smart quit" })
```

```lua
vim.api.nvim_create_autocmd("FocusLost", { command = "silent!wa" })
vim.api.nvim_create_autocmd("BufLeave", { command = "silent! w" })
vim.api.nvim_create_autocmd("TextChanged", { callback = function() ... end })
vim.api.nvim_create_autocmd("InsertLeave", { callback = function() ... end })
```

From `.config/nvim/lua/plugins/bufferline.lua`:
```lua
close_command = "Bdelete! %d"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace smart quit and aggressive autosave with the Phase 1 lifecycle policy</name>
  <files>.config/nvim/lua/core/keymaps.lua</files>
  <read_first>
.planning/phases/01-reliability-and-portability-baseline/01-CONTEXT.md
.planning/phases/01-reliability-and-portability-baseline/01-RESEARCH.md
.config/nvim/lua/core/options.lua
.config/nvim/lua/core/keymaps.lua
  </read_first>
  <action>Rewrite `.config/nvim/lua/core/keymaps.lua` so `<C-q>` executes buffer-first close semantics per D-01 through D-06: use `vim.cmd("confirm bdelete")` or equivalent API call that prompts on unsaved changes and never branches to `quit` or `close`. Keep split closing on `<leader>xs` as the explicit window-only action to preserve D-02 and D-03. Remove the `BufLeave`, `TextChanged`, and `InsertLeave` autosave autocmds entirely. Replace the current `FocusLost` `silent!wa` with one callback-based `FocusLost` autocmd that writes only the current buffer when all of these concrete guards pass: `vim.bo.buftype == ""`, `vim.bo.modifiable`, `vim.bo.modified`, `vim.api.nvim_buf_get_name(bufnr) ~= ""`, and `vim.fn.filereadable(bufname) == 1 or vim.fn.bufexists(bufnr) == 1`. Skip terminal, quickfix, prompt, nofile, help, and unnamed buffers by virtue of those guards; do not call `wa`.</action>
  <verify>
    <automated>rg -n "<C-q>|confirm bdelete|BufLeave|TextChanged|InsertLeave|FocusLost|silent!wa|silent! w|quit\\)|vim\\.cmd\\(\"quit|vim\\.cmd\\(\"close" .config/nvim/lua/core/keymaps.lua</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "confirm bdelete" .config/nvim/lua/core/keymaps.lua` matches at least one line.
- `rg -n "vim\\.cmd\\(\"quit|vim\\.cmd\\(\"close|vim\\.cmd\\(\"bdelete\"\\)" .config/nvim/lua/core/keymaps.lua` returns no matches inside the `<C-q>` mapping implementation.
- `rg -n "BufLeave|TextChanged|InsertLeave" .config/nvim/lua/core/keymaps.lua` returns no matches for autosave autocmd declarations.
- `rg -n "FocusLost" .config/nvim/lua/core/keymaps.lua` matches exactly one autocmd declaration.
- `rg -n "buftype == \"\"|modifiable|modified|nvim_buf_get_name" .config/nvim/lua/core/keymaps.lua` matches the FocusLost callback guards.
  </acceptance_criteria>
  <done>`<C-q>` is buffer-only and confirmable, explicit split close remains separate, and autosave is reduced to one guarded `FocusLost` path for normal file buffers.</done>
</task>

<task type="auto">
  <name>Task 2: Align bufferline close actions with non-forced buffer-first close behavior</name>
  <files>.config/nvim/lua/plugins/bufferline.lua</files>
  <read_first>
.planning/phases/01-reliability-and-portability-baseline/01-CONTEXT.md
.planning/phases/01-reliability-and-portability-baseline/01-RESEARCH.md
.config/nvim/lua/core/keymaps.lua
.config/nvim/lua/plugins/bufferline.lua
  </read_first>
  <action>Update `.config/nvim/lua/plugins/bufferline.lua` so `close_command` no longer uses `Bdelete! %d`. Replace it with a function-based close handler that runs a confirmable non-forced buffer delete for the clicked buffer number, for example `vim.cmd(("confirm bdelete %d"):format(bufnr))`. Keep the existing plugin and visuals unchanged. The goal is strict semantic alignment: keyboard `<C-q>` and bufferline mouse close must both close buffers, never tabs/windows/sessions, and must both respect unsaved changes instead of force-discarding.</action>
  <verify>
    <automated>rg -n "close_command|Bdelete!|confirm bdelete" .config/nvim/lua/plugins/bufferline.lua</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "close_command = function" .config/nvim/lua/plugins/bufferline.lua` matches at least one line.
- `rg -n "confirm bdelete" .config/nvim/lua/plugins/bufferline.lua` matches at least one line.
- `rg -n "Bdelete!" .config/nvim/lua/plugins/bufferline.lua` returns no matches.
  </acceptance_criteria>
  <done>Bufferline close actions now match the Phase 1 keyboard close policy and no longer force-delete modified buffers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| buffer lifecycle keymaps -> Neovim close commands | User keypresses trigger destructive editor actions that must stay scoped to the current buffer |
| autosave autocmd -> current buffer write path | Automatic writes cross from editor events into filesystem changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | D | `.config/nvim/lua/core/keymaps.lua` | mitigate | Remove the smart-quit branch that can escalate a buffer close into `quit`, and bind `<C-q>` to `confirm bdelete` only. |
| T-01-05 | T | `.config/nvim/lua/core/keymaps.lua` | mitigate | Restrict autosave to one `FocusLost` callback with explicit buffer guards so special buffers are never written implicitly. |
| T-01-06 | T | `.config/nvim/lua/plugins/bufferline.lua` | mitigate | Replace `Bdelete!` with confirmable non-forced delete so mouse close behavior cannot silently discard unsaved changes. |
</threat_model>

<verification>
- Run `nvim --headless "+qa"` to ensure the revised keymaps and bufferline config load cleanly.
- Run `rg -n "BufLeave|TextChanged|InsertLeave|Bdelete!|smart quit|xdg-open" .config/nvim/lua` and confirm no stale lifecycle behavior remains.
</verification>

<success_criteria>
- `<C-q>` is no longer a session-exit state machine.
- Autosave is limited to one guarded `FocusLost` path for normal file buffers only.
- Bufferline close behavior no longer diverges from keyboard close behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/01-reliability-and-portability-baseline/01-02-SUMMARY.md`
</output>
