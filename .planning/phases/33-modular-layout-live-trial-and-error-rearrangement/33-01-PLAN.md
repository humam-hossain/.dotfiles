---
phase: "33"
plan: "01"
title: "Automated Validation Harness & Symlink Baseline"
type: "tracer"
wave: 1
depends_on: []
files_modified:
  - scripts/phase33-layout-assert.sh
autonomous: true
requirements: [LAYOUT-01]

must_haves:
  truths:
    - "scripts/phase33-layout-assert.sh exists at mode 0755 implementing fail-closed conventions (set -euo pipefail, cleanup trap, --section parsing, and porcelain snapshot check) (D-12)"
    - "~/.config/quickshell/ii/modules/ii/bar/BarContent.qml is a leaf symlink to restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml without directory folding (LAYOUT-01)"
    - "Section 1 of scripts/phase33-layout-assert.sh validates leaf symlink integrity, non-folded ancestor directories, BarGroup.qml symlink, and submodule cleanliness"
    - "Section 2 of scripts/phase33-layout-assert.sh validates AST component distribution across Left, Center, and Right zones per D-01, D-02, D-03, D-04"
    - "Section 3 of scripts/phase33-layout-assert.sh validates pill geometry (spacing 4), Media width clamp (200px), Clock side-by-side format, and absence of legacy center width clamps (D-05, D-06, D-07, D-13, D-14)"
    - "Section 4 of scripts/phase33-layout-assert.sh validates dual-monitor runtime parity (DP-1 and HDMI-A-1/HDMI-A-2), running Quickshell process, Phase 32 token retention, and dots-hyprland strict verification"
  artifacts:
    - scripts/phase33-layout-assert.sh
  key_links:
    - "scripts/phase33-layout-assert.sh is the authoritative test harness gating LAYOUT-01, LAYOUT-02, and LAYOUT-03 across Sections 1 to 4"
    - "restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml is the tracked repository source mirror for ~/.config/quickshell/ii/modules/ii/bar/BarContent.qml"
  prohibitions:
    - "Must not edit or modify files in vendor/dots-hyprland/"
    - "Must not create folded directory symlinks under ~/.config/quickshell/"
    - "Must not commit untracked runtime artifacts or generated caches to git"
    - "Must not run dotfiles operations or test suites as root (EUID == 0)"
---

<objective>
Scaffold the Phase 33 automated validation harness (`scripts/phase33-layout-assert.sh`) with fail-closed structure covering Sections 1 through 4: symlink integrity, AST component zone partitioning, pill geometry & space defense, and multi-monitor runtime parity. Verify that Section 1 passes immediately with `FAIL=0` while preserving submodule cleanliness.

Purpose: Deliver the foundational Nyquist-compliant test harness gating all subsequent QML layout reorganization and visual trial-and-error tasks.
Output: Executable test harness `scripts/phase33-layout-assert.sh` with working `--section <1-4>` interface, two-phase git porcelain invariant enforcement, and green Section 1 baseline.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-CONTEXT.md
@.planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-RESEARCH.md
@.planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-PATTERNS.md
@.planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-VALIDATION.md
@scripts/phase32-component-formatting-assert.sh
@restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
</context>

<assumptions>
- Quickshell status bar modules are deployed via GNU Stow with `--no-folding` under `restow/quickshell/`.
- `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml` is the tracked git source of truth; `~/.config/quickshell/ii/modules/ii/bar/BarContent.qml` is the live leaf symlink.
- Submodule `vendor/dots-hyprland` must remain 100% clean with zero git diff or untracked file churn.
- Non-root execution check (`[[ "${EUID:-$(id -u)}" -ne 0 ]]`) prevents root permission pollution across dotfiles and test runs.
- Temporary files created during assertion runs must be registered in `TMP_FILES` and cleaned up on EXIT.
</assumptions>

<tasks>

<task id="33-01-01" type="tracer">
  <name>Scaffold scripts/phase33-layout-assert.sh CLI interface and Section 1 (Symlink integrity & packaging verification)</name>
  <read_first>
    scripts/phase32-component-formatting-assert.sh
    .planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-VALIDATION.md
    .planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-PATTERNS.md
    .planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-RESEARCH.md
  </read_first>
  <files>
    scripts/phase33-layout-assert.sh
  </files>
  <action>
    Scaffold `scripts/phase33-layout-assert.sh` with permissions 0755 adhering strictly to repository assert harness conventions:
    1. Base scaffolding: Use `#!/usr/bin/env bash`, `set -euo pipefail`. Resolve `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"` and `ASSERT_SELF`. Export default `XDG_CONFIG_HOME` and `XDG_STATE_HOME`. Enforce non-root execution check (`[[ "${EUID:-$(id -u)}" -ne 0 ]] || { echo "Error: Do not run as root" >&2; exit 1; }`). Initialize `FAIL=0` and `FINDINGS=0`, and define `pass()`, `fail()`, `finding()`, and `info()` helpers.
    2. Cleanup trap: Initialize `TMP_FILES=()` and `SCRATCH_ROOTS=()`. Implement `cleanup()` removing temporary files with `rm -f` and scratch directories with `rm -rf`, and register `trap cleanup EXIT`.
    3. CLI argument parsing: Support `--section <1-4>` and `-h|--help`, validating integer range 1 to 4 with fail-closed exit 1 on invalid arguments.
    4. Two-phase working tree snapshots: Implement `porcelain_snapshot_raw` and `porcelain_snapshot` filtering ephemeral noise (`.commandcode/`, `scripts/__pycache__/`), capturing git status before test execution into `$PORCELAIN_BEFORE`.
    5. Section 1 implementation: Symlink Integrity & Packaging Verification (LAYOUT-01, D-12):
       - Assert leaf symlink: `~/.config/quickshell/ii/modules/ii/bar/BarContent.qml` must be a symlink pointing to `$REPO_ROOT/restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml` (`readlink -f`).
       - Assert ancestor directories are real directories (not folded symlinks): `$HOME/.config`, `$HOME/.config/quickshell`, `$HOME/.config/quickshell/ii`, `.../modules`, `.../ii`, `.../bar`.
       - Assert `BarGroup.qml` leaf symlink exists in `$HOME/.config/quickshell/ii/modules/ii/bar/BarGroup.qml` pointing to `$REPO_ROOT/restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml`.
       - Assert `vendor/dots-hyprland` submodule cleanliness: `git -C "$REPO_ROOT/vendor/dots-hyprland" status --porcelain` is empty.
    6. Closing porcelain invariant check: Capture `$PORCELAIN_AFTER`, compare with `$PORCELAIN_BEFORE` (`cmp -s`), print `=== done: FAIL=$FAIL FINDINGS=$FINDINGS ===`, and exit 1 if FAIL > 0, else exit 0.
  </action>
  <acceptance_criteria>
    test -x scripts/phase33-layout-assert.sh exits 0.
    bash scripts/phase33-layout-assert.sh --help exits 0 and outputs Usage: .* --section <1-4>.
    bash scripts/phase33-layout-assert.sh --section 99 exits 1 with error message on stderr.
    bash scripts/phase33-layout-assert.sh --section 1 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>test -x scripts/phase33-layout-assert.sh && bash scripts/phase33-layout-assert.sh --help | grep -q -- '--section <1-4>' && bash scripts/phase33-layout-assert.sh --section 1</automated>
    <fails_when>non-zero exit code, file not executable, or [FAIL] in Section 1 output</fails_when>
  </verify>
  <done>scripts/phase33-layout-assert.sh exists at mode 0755, parses CLI options, enforces porcelain snapshot integrity, and verifies symlinks with Section 1 passing with FAIL=0.</done>
</task>

<task id="33-01-02" type="execute">
  <name>Implement Section 2 (AST & Component zone partitioning assertions: Left, Center, Right)</name>
  <read_first>
    scripts/phase33-layout-assert.sh
    .planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-PATTERNS.md
    .planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-RESEARCH.md
    .planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-CONTEXT.md
  </read_first>
  <files>
    scripts/phase33-layout-assert.sh
  </files>
  <action>
    Implement Section 2 in `scripts/phase33-layout-assert.sh` checking the AST component partitioning and declaration ordering across Left, Center, and Right zones in `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml`:
    1. Left Section AST Assertions:
       - Assert `barLeftSideMouseArea` exists and anchors `left: parent.left` and `right: middleSection.left`.
       - Assert `leftSectionRowLayout` uses `RowLayout` with `spacing: 4`.
       - Assert declaration order: `LeftSidebarButton` appears before `resourcesGroup`; `resourcesGroup` appears before `utilButtonsGroup` (D-01).
       - Assert `LeftSidebarButton` has `colBackground` hover binding and `Layout.leftMargin: Appearance.rounding.screenRounding` (D-03).
       - Assert `resourcesGroup` wraps `Resources` inside a `BarGroup` pill (D-03).
       - Assert `utilButtonsGroup` wraps `UtilButtons` inside a `BarGroup` pill with visibility guarded by `Config.options.bar.verbose && root.useShortenedForm === 0` (D-03, D-08).
       - Assert trailing flexible spacer `Item { Layout.fillWidth: true }` inside `leftSectionRowLayout`.
    2. Center Section AST Assertions:
       - Assert `middleSection` uses `Row` anchored strictly to `anchors.horizontalCenter: parent.horizontalCenter` (D-13).
       - Assert `middleSection` has `spacing: 4` (D-05).
       - Assert declaration order: `weatherGroup` appears before `middleCenterGroup` (`Workspaces`); `middleCenterGroup` appears before `rightCenterGroup` (`ClockWidget`) (D-01, D-02).
       - Assert absence of `VerticalBarSeparator` inside `middleSection` (D-02, D-05).
       - Assert `weatherGroup` wraps `WeatherBar` in a `Loader` with `sourceComponent: BarGroup` (D-02).
       - Assert `middleCenterGroup` wraps `Workspaces` in a `BarGroup` (D-02).
       - Assert `rightCenterGroup` wraps `ClockWidget` in a `BarGroup` (D-02).
    3. Right Section AST Assertions:
       - Assert `barRightSideMouseArea` exists and anchors `left: middleSection.right` and `right: parent.right`.
       - Assert `rightSectionRowLayout` uses `RowLayout` with `spacing: 4`.
       - Assert standard reading order (absence of `layoutDirection: Qt.RightToLeft` in `rightSectionRowLayout` per Pitfall 3).
       - Assert leading flexible spacer `Item { Layout.fillWidth: true }` exists before `mediaLoader` to absorb variable width and push pills to the right edge.
       - Assert declaration order: `mediaLoader` -> `updatesLoader` -> `batteryLoader` -> `sysTrayGroup` -> `rightSidebarButton` (D-01, D-04).
       - Assert dynamic auto-collapsing: `mediaLoader`, `updatesLoader`, and `batteryLoader` are conditional `Loader` items wrapping `BarGroup` (D-04).
       - Assert `sysTrayGroup` is a standalone `BarGroup` with `SysTray { showSeparator: false ... }` (D-04).
       - Assert `rightSidebarButton` contains `indicatorsRowLayout` with `Appearance.rounding.screenRounding` margin.
  </action>
  <acceptance_criteria>
    bash -n scripts/phase33-layout-assert.sh exits 0.
    grep -q 'Section 2: Component AST & Section Distribution' scripts/phase33-layout-assert.sh exits 0.
    grep -q 'leftSectionRowLayout' scripts/phase33-layout-assert.sh exits 0.
    grep -q 'middleSection' scripts/phase33-layout-assert.sh exits 0.
    grep -q 'rightSectionRowLayout' scripts/phase33-layout-assert.sh exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/phase33-layout-assert.sh && grep -q 'Section 2: Component AST & Section Distribution' scripts/phase33-layout-assert.sh && grep -q 'leftSectionRowLayout' scripts/phase33-layout-assert.sh && grep -q 'rightSectionRowLayout' scripts/phase33-layout-assert.sh</automated>
    <fails_when>non-zero exit code or missing Section 2 assertions in assert script</fails_when>
  </verify>
  <done>scripts/phase33-layout-assert.sh contains complete Section 2 assertions verifying component AST distribution and declaration order across Left, Center, and Right zones.</done>
</task>

<task id="33-01-03" type="execute">
  <name>Implement Section 3 (Pill boundary geometry, center anchoring, and Option 1 collision defense assertions)</name>
  <read_first>
    scripts/phase33-layout-assert.sh
    .planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-PATTERNS.md
    .planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-RESEARCH.md
    .planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-CONTEXT.md
  </read_first>
  <files>
    scripts/phase33-layout-assert.sh
  </files>
  <action>
    Implement Section 3 in `scripts/phase33-layout-assert.sh` checking pill geometry, center anchoring, and Option 1 dynamic space collision defense invariants:
    1. Spacing assertions:
       - Assert `spacing: 4` is specified on `leftSectionRowLayout`, `middleSection`, and `rightSectionRowLayout` (D-05).
       - Assert absence of vertical divider lines (`VerticalBarSeparator`) in `BarContent.qml`.
    2. Media sizing & elision assertions (D-06, D-14):
       - Assert `Media` inside `mediaLoader` has `Layout.maximumWidth: (root.useShortenedForm === 1) ? 140 : 200`.
       - Assert `Media` retains `visible: root.useShortenedForm < 2`.
       - Assert `Media.qml` uses `Text.ElideRight` for track title elision without overflow.
    3. Clock & Date layout preservation (D-07):
       - Assert `ClockWidget.qml` retains side-by-side time and date format `[ hh:mm:ss AP   ddd, dd-MM-yyyy ]` with non-glyph spacer (`implicitWidth: 8`) and no unicode bullet glyph (`•`).
    4. Dynamic sizing & absence of artificial clamps (D-13, D-14):
       - Assert absence of `implicitWidth: root.centerSideModuleWidth` in `BarContent.qml`.
       - Assert absence of `anchors.*` on child elements directly inside `RowLayout` containers (`leftSectionRowLayout`, `rightSectionRowLayout`) preventing anchor loops (Pitfall 1).
    5. Mathematical safety buffer invariant (D-14):
       - Calculate remaining buffer on standard 1080p display (1920px width) under worst-case expanded pill sizes: half screen width (960px) - half center width (~230px) - max right cluster (~550px) = >= 180px buffer. Assert that collision is mathematically impossible under Option 1 dynamic defense.
  </action>
  <acceptance_criteria>
    bash -n scripts/phase33-layout-assert.sh exits 0.
    grep -q 'Section 3: Pill Geometry, Anchors & Space Defense' scripts/phase33-layout-assert.sh exits 0.
    grep -q 'centerSideModuleWidth' scripts/phase33-layout-assert.sh exits 0.
    grep -q 'Layout.maximumWidth' scripts/phase33-layout-assert.sh exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/phase33-layout-assert.sh && grep -q 'Section 3: Pill Geometry, Anchors & Space Defense' scripts/phase33-layout-assert.sh && grep -q 'centerSideModuleWidth' scripts/phase33-layout-assert.sh</automated>
    <fails_when>non-zero exit code or missing Section 3 in assert script</fails_when>
  </verify>
  <done>scripts/phase33-layout-assert.sh contains complete Section 3 assertions verifying pill geometry, 4px spacing, Media width clamp, center anchoring, and Option 1 collision defense.</done>
</task>

<task id="33-01-04" type="execute">
  <name>Implement Section 4 (Dual-monitor display parity on DP-1 and HDMI-A-1/HDMI-A-2, and strict repository compliance)</name>
  <read_first>
    scripts/phase33-layout-assert.sh
    scripts/phase32-component-formatting-assert.sh
    arch/dots-hyprland.sh
    .planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-PATTERNS.md
    .planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-RESEARCH.md
  </read_first>
  <files>
    scripts/phase33-layout-assert.sh
  </files>
  <action>
    Implement Section 4 in `scripts/phase33-layout-assert.sh` checking runtime environment, multi-monitor configuration, Phase 32 regression baseline, and repository strict verification:
    1. Multi-monitor configuration & display checks (LAYOUT-02, LAYOUT-03, D-09, D-10, D-11):
       - Query active monitors with `hyprctl monitors -j`.
       - Verify primary ultrawide output `DP-1` (3440x1440) is detected.
       - Check for secondary output `HDMI-A-1` or `HDMI-A-2` (1920x1080) or output definitions in `~/.config/hypr/custom/general.lua`.
       - Validate that both detected widths satisfy `useShortenedForm === 0` (width >= 1200px threshold), ensuring full uncompromised bar rendering across all screens.
    2. Quickshell process check:
       - Assert that `quickshell` process is running (`pgrep -f "qs -c ii"` or `pgrep -x quickshell`).
    3. Submodule cleanliness check:
       - Assert that `vendor/dots-hyprland` submodule is clean (`git -C "$REPO_ROOT/vendor/dots-hyprland" status --porcelain` is empty).
    4. Phase 32 regression check:
       - Execute `bash "$REPO_ROOT/scripts/phase32-component-formatting-assert.sh"`, asserting exit 0 to guarantee zero regression on Phase 32 formatting tokens (RAM GB, Clock format, Privacy revealers, UpdatesButton launcher, Tray spacing).
    5. Repository strict verifier check:
       - Execute `"$REPO_ROOT/arch/dots-hyprland.sh" verify --strict`, asserting exit 0 and 0 findings (`FINDINGS=0`).
    6. Complete closing self-check:
       - Compare `$PORCELAIN_BEFORE` and `$PORCELAIN_AFTER` ensuring test execution left zero working tree changes.
    7. Execute `bash scripts/phase33-layout-assert.sh --section 1` to verify Section 1 passes cleanly.
  </action>
  <acceptance_criteria>
    bash -n scripts/phase33-layout-assert.sh exits 0.
    grep -q 'Section 4: Dual-Monitor Runtime Parity' scripts/phase33-layout-assert.sh exits 0.
    bash scripts/phase33-layout-assert.sh --section 1 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/phase33-layout-assert.sh && grep -q 'Section 4: Dual-Monitor Runtime Parity' scripts/phase33-layout-assert.sh && bash scripts/phase33-layout-assert.sh --section 1</automated>
    <fails_when>non-zero exit code or Section 1 fails</fails_when>
  </verify>
  <done>scripts/phase33-layout-assert.sh is fully scaffolded across Sections 1 to 4, verified syntactically, and Section 1 passes with FAIL=0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Assertion Script → Local System | Assertion script queries system telemetry, D-Bus, and Hyprland IPC safely without privileged execution |
| Test Assert Harness → Git Working Tree | Assertion script performing clean inspection without mutating working tree status or leaving scratch artifacts |
| User Privileges → System Files | Dotfiles maintenance and assertion tests executed under non-root unprivileged user account |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Tampering / Test Harness Churn | scripts/phase33-layout-assert.sh | medium | mitigate | Enforce two-phase porcelain snapshots comparing `$PORCELAIN_BEFORE` and `$PORCELAIN_AFTER` with cleanup traps on exit (D-12). |
| T-33-02 | Elevation of Privilege / Root Execution | Environment | medium | mitigate | Guard assert script with non-root check `[[ "${EUID:-$(id -u)}" -ne 0 ]]`. |
| T-33-03 | Denial of Service / Hanging Subprocesses | scripts/phase33-layout-assert.sh | low | mitigate | Limit commands with timeouts and pass-through non-blocking queries (`pgrep`, `hyprctl`, `cmp`). |
</threat_model>

<verification>
Automated verification commands:
1. `test -x scripts/phase33-layout-assert.sh && bash scripts/phase33-layout-assert.sh --help | grep -q -- '--section <1-4>'`
2. `bash scripts/phase33-layout-assert.sh --section 1`
</verification>

<success_criteria>
1. `scripts/phase33-layout-assert.sh` exists at mode 0755, parses `--section <1-4>`, and implements domain-partitioned fail-closed assertions for all 4 sections.
2. Section 1 passes with `FAIL=0` and 0 findings.
3. Git working tree remains completely clean after test execution.
</success_criteria>

## Artifacts this phase produces
- Test assertion harness: `scripts/phase33-layout-assert.sh`
- Plan summary: `.planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-01-SUMMARY.md`

<output>
Create `.planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-01-SUMMARY.md` when done
</output>
