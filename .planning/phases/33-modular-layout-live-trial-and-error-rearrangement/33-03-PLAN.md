---
phase: "33"
plan: "03"
title: "Live Dual-Monitor Trial-and-Error Visual Evaluation & Strict Compliance"
wave: 3
depends_on: ["33-02"]
files_modified:
  - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
autonomous: false
requirements: [LAYOUT-02, LAYOUT-03]

must_haves:
  truths:
    - "Quickshell status bar renders full uncompromised layout across active monitors (DP-1 3440x1440 and HDMI-A-1/HDMI-A-2 1920x1080) with zero clipping or text overflow (LAYOUT-02, LAYOUT-03, D-09, D-11)"
    - "Option 1 Dynamic Space Collision Defense guarantees minimum spacing buffer >= 180px between center section and side clusters on standard 1080p displays (D-14)"
    - "Live interactive trial-and-error visual evaluation is conducted with the user via Ctrl+Super+R hot-reloads, locking in the finalized layout (LAYOUT-02)"
    - "Workspaces widget adheres to native monitor filtering per Hyprland.monitorFor (workspaces 1-5 on DP-1, 6-10 on HDMI-A-2) (D-10)"
    - "The complete validation suite (scripts/phase33-layout-assert.sh, scripts/phase32-component-formatting-assert.sh, and arch/dots-hyprland.sh verify --strict) exits 0 with FAIL=0 and 0 findings"
  artifacts:
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
    - scripts/phase33-layout-assert.sh
  key_links:
    - "restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml is verified live across physical displays"
    - "arch/dots-hyprland.sh verify --strict enforces zero-drift repository contract"
  prohibitions:
    - "Must not complete phase without human visual evaluation and confirmation of candidate bar layout"
    - "Must not leave working tree dirty or introduce uncommitted files to vendor/dots-hyprland/"
    - "Must not bypass strict verification checks"
---

<objective>
Conduct interactive live trial-and-error visual testing with the user across dual monitors (`DP-1` 3440x1440 ultrawide and `HDMI-A-1`/`HDMI-A-2` 1920x1080), evaluating candidate component placement, pill balance, spacing, and responsive scaling. Execute any fine-tuning adjustments requested during live review, and prove zero regressions across `scripts/phase33-layout-assert.sh`, `scripts/phase32-component-formatting-assert.sh`, and `arch/dots-hyprland.sh verify --strict`.

Purpose: Satisfy LAYOUT-02 and LAYOUT-03 through live human visual confirmation and automated regression-free verification.
Output: Human-approved modular status bar layout, finalized `BarContent.qml`, and 100% green verification suite.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-CONTEXT.md
@.planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-RESEARCH.md
@.planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-PATTERNS.md
@.planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-VALIDATION.md
@scripts/phase33-layout-assert.sh
@scripts/phase32-component-formatting-assert.sh
@arch/dots-hyprland.sh
@restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
</context>

<assumptions>
- User is logged into active Hyprland session with primary monitor `DP-1` (3440x1440) and secondary output `HDMI-A-1` / `HDMI-A-2` (1920x1080).
- `Ctrl+Super+R` cleanly reloads Quickshell in < 1s via `~/.config/hypr/hyprland/keybinds.lua`.
- Human visual evaluation verifies subjective aesthetic balance, pill proportions, and readability that automated AST parsing cannot inspect alone.
- Repository strict verification (`./arch/dots-hyprland.sh verify --strict`) requires `FINDINGS=0` and a clean working tree.
</assumptions>

<tasks>

<task id="33-03-01" type="execute">
  <name>Verify dual-monitor display and responsive scaling across DP-1 (3440x1440) and HDMI-A-1/HDMI-A-2 (1920x1080) with zero clipping</name>
  <read_first>
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
    scripts/phase33-layout-assert.sh
    .planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-CONTEXT.md
    .planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-RESEARCH.md
  </read_first>
  <files>
    scripts/phase33-layout-assert.sh
  </files>
  <action>
    Inspect live multi-monitor status bar runtime across both outputs:
    1. Query Hyprland monitor status via `hyprctl monitors -j` and verify outputs:
       - Primary: `DP-1` (3440x1440)
       - Secondary: `HDMI-A-1` or `HDMI-A-2` (1920x1080) or output definitions in `~/.config/hypr/custom/general.lua`
    2. Confirm that Quickshell daemon is active (`pgrep -f "qs -c ii"` or `pgrep -x quickshell`) and has bound to active displays.
    3. Verify that on both 3440px and 1920px screen widths, `useShortenedForm === 0` holds true, rendering uncompromised full pill layouts.
    4. Run Section 4 of assertion harness:
       ```bash
       bash scripts/phase33-layout-assert.sh --section 4
       ```
    5. Check system journal or Quickshell logs for any QML warnings, property binding errors, or anchor loop notices:
       ```bash
       journalctl --user -b --grep="quickshell" -n 50 --no-pager 2>/dev/null || true
       ```
  </action>
  <acceptance_criteria>
    bash scripts/phase33-layout-assert.sh --section 4 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase33-layout-assert.sh --section 4</automated>
    <fails_when>non-zero exit code or [FAIL] in Section 4 output</fails_when>
  </verify>
  <done>Dual-monitor runtime status is verified on DP-1 and secondary outputs with Section 4 passing with FAIL=0 and zero QML anchor loop warnings.</done>
</task>

<task id="33-03-02" type="checkpoint:human-verify">
  <name>Conduct interactive live trial-and-error visual evaluation with user (checkpoint:human-verify)</name>
  <read_first>
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
    .planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-CONTEXT.md
    .planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-PATTERNS.md
  </read_first>
  <files>
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
  </files>
  <action>
    Present the candidate modular bar layout to the user for live visual inspection and interactive feedback:
    1. Highlight layout organization on physical displays (`DP-1` and `HDMI-A-1`/`HDMI-A-2`):
       - Left Section: Bare launcher button on screen edge, `[ Resources: CPU %, RAM GB, dynamic Swap ]` pill, `[ UtilButtons: Screen snip, record, color picker, mic toggle ]` pill.
       - Center Section: Anchored strictly to true physical screen center. 3 standalone pills: `[ Weather ]` (click opens forecast), `[ Workspaces ]` (monitor-filtered: 1-5 on DP-1, 6-10 on HDMI-A-2; right-click toggles overview), `[ Clock & Date ]` (12h format `hh:mm:ss AP` with date `ddd, dd-MM-yyyy`, hover calendar, click opens right sidebar).
       - Right Section: Pushed firmly to the right edge via flexible spacer: `[ Media ]` pill (auto-collapsing when idle, capped at 200px with elision), `[ Updates ]` pill (auto-collapsing when 0), `[ Battery ]` pill (auto-hidden on desktop), `[ SysTray ]` pill (clean icons with 4px spacing), `[ Status & RightSidebar ]` cluster (Privacy Amber mic and Red screen share, Audio mute, Keyboard layout, Notifications count, Network, Bluetooth).
       - Inter-Pill Spacing: Clean 4px gaps without divider lines.
    2. Ask the user:
       - Does the arrangement feel visually balanced on both ultrawide (DP-1) and secondary (HDMI) screens?
       - Are there any candidate rearrangements desired (e.g. swapping pill order, tweaking spacing or margins)?
    3. If changes are requested:
       - Edit `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml`.
       - Trigger live reload (`Ctrl+Super+R` or `quickshell --reload`).
       - Re-evaluate until user explicitly approves.
    4. Execute automated verification:
       ```bash
       bash scripts/phase33-layout-assert.sh
       ```
  </action>
  <acceptance_criteria>
    User visually confirms approval of the modular status bar layout.
    bash scripts/phase33-layout-assert.sh exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase33-layout-assert.sh</automated>
    <fails_when>non-zero exit code or [FAIL] in assert output</fails_when>
  </verify>
  <done>User completed interactive visual trial-and-error evaluation and approved the modular status bar layout across dual monitors.</done>
</task>

<task id="33-03-03" type="execute">
  <name>Run full automated verification suite (scripts/phase33-layout-assert.sh + ./arch/dots-hyprland.sh verify --strict) ensuring 0 working tree drift</name>
  <read_first>
    scripts/phase33-layout-assert.sh
    scripts/phase32-component-formatting-assert.sh
    arch/dots-hyprland.sh
  </read_first>
  <files>
    scripts/phase33-layout-assert.sh
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
  </files>
  <action>
    Execute full end-to-end automated verification and strict repository validation:
    1. Run Phase 33 layout assertion suite across all 4 sections:
       ```bash
       bash scripts/phase33-layout-assert.sh
       ```
    2. Run Phase 32 component formatting assertion suite:
       ```bash
       bash scripts/phase32-component-formatting-assert.sh
       ```
    3. Run dots-hyprland strict system verifier:
       ```bash
       ./arch/dots-hyprland.sh verify --strict
       ```
    4. Check git status to ensure working tree has zero untracked artifacts or uncommitted changes outside tracked files:
       ```bash
       git status --porcelain
       ```
  </action>
  <acceptance_criteria>
    bash scripts/phase33-layout-assert.sh exits 0 with FAIL=0.
    bash scripts/phase32-component-formatting-assert.sh exits 0 with FAIL=0.
    ./arch/dots-hyprland.sh verify --strict exits 0 with FINDINGS=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase33-layout-assert.sh && bash scripts/phase32-component-formatting-assert.sh && ./arch/dots-hyprland.sh verify --strict</automated>
    <fails_when>non-zero exit code, [FAIL] in assert output, or FINDINGS > 0 in verify --strict</fails_when>
  </verify>
  <done>All 4 sections of phase33-layout-assert.sh, all sections of phase32-component-formatting-assert.sh, and dots-hyprland verify --strict pass with 0 findings and zero git drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Feedback / Trial-and-Error → Configuration State | Human operator evaluating visual appearance and confirming ergonomics before finalizing state |
| Repository Strict Verifier → Filesystem State | arch/dots-hyprland.sh verify --strict validating symlink topology and tree integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-08 | Repudiation / Unconfirmed Layout Deployment | BarContent.qml | medium | mitigate | Enforce blocking human checkpoint (checkpoint:human-verify) requiring explicit user sign-off before closing phase. |
| T-33-09 | Tampering / Working Tree Drift | Git Repository | medium | mitigate | Run ./arch/dots-hyprland.sh verify --strict and assert zero porcelain diff at close. |
| T-33-10 | Regression / Prior Phase Invariant Invalidation | BarContent.qml | high | mitigate | Run full scripts/phase32-component-formatting-assert.sh suite as part of final gate. |
</threat_model>

<verification>
Automated verification commands:
1. `bash scripts/phase33-layout-assert.sh --section 4`
2. `bash scripts/phase33-layout-assert.sh`
3. `bash scripts/phase32-component-formatting-assert.sh`
4. `./arch/dots-hyprland.sh verify --strict`
</verification>

<success_criteria>
1. User visually reviews and signs off on the candidate status bar layout across dual monitors.
2. `bash scripts/phase33-layout-assert.sh` passes all 4 sections with `FAIL=0` and 0 findings.
3. `bash scripts/phase32-component-formatting-assert.sh` passes with `FAIL=0`.
4. `./arch/dots-hyprland.sh verify --strict` passes with `FINDINGS=0`.
5. Working tree has zero drift and remains cleanly committed.
</success_criteria>

## Artifacts this phase produces
- Visual verification transcript & layout sign-off: `.planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-03-SUMMARY.md`

<output>
Create `.planning/phases/33-modular-layout-live-trial-and-error-rearrangement/33-03-SUMMARY.md` when done
</output>
