---
phase: "31"
plan: "02"
type: "execute"
wave: 2
depends_on: ["31-01"]
files_modified:
  - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
  - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml
  - scripts/phase31-overlay-pill-assert.sh
autonomous: true
requirements: [PILL-02, PILL-03, PILL-04]

must_haves:
  truths:
    - "restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml eliminates artificial implicitWidth: root.centerSideModuleWidth clamps on leftCenterGroup and rightCenterGroup (D-01, D-04, PILL-02)"
    - "restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml propagates implicitWidth and implicitHeight from rightCenterGroupContent to rightCenterGroup (D-01, PILL-03)"
    - "restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml defines Behavior on implicitWidth with Appearance.animationCurves.emphasizedDecel over 250ms duration enabled on horizontal bars (D-02, PILL-03)"
    - "restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml preserves 100% upstream visual fidelity with Appearance.rounding.small (12px), padding: 5, and borderless background color handling (D-03, PILL-02, PILL-04)"
    - "Quickshell is reloaded via process signaling matching Ctrl+Super+R and runs cleanly without QML syntax errors (D-07, PILL-01)"
    - "scripts/phase31-overlay-pill-assert.sh passes all 4 sections with FAIL=0 and arch/dots-hyprland.sh verify --strict exits 0 with 0 findings (D-08, INTG-02)"
  artifacts:
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml
    - scripts/phase31-overlay-pill-assert.sh
  key_links:
    - "restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml is symlinked into ~/.config/quickshell/ii/modules/ii/bar/BarContent.qml"
    - "restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml is symlinked into ~/.config/quickshell/ii/modules/ii/bar/BarGroup.qml"
    - "scripts/phase31-overlay-pill-assert.sh and arch/dots-hyprland.sh verify --strict provide authoritative multi-tier verification"
  prohibitions:
    - "Must not edit or modify files under vendor/dots-hyprland/"
    - "Must not introduce hardcoded pixel width clamps (e.g. Math.max, Math.min, or fixed implicitWidth) (D-04)"
    - "Must not alter default rounding (12px), padding (5px), or spacing (4px) tokens (D-03)"
    - "Must not invoke qmllint or qmlformat in automated bash assertions"
---

<objective>
Unlock dynamic content-driven pill widths in `BarContent.qml` by removing artificial width clamps on center groups and propagating inner BarGroup dimensions (D-01, D-04, PILL-02, PILL-03), add fluid width resizing animations to `BarGroup.qml` using Material 3 emphasized deceleration curves while preserving 100% upstream visual style fidelity (D-02, D-03, PILL-02, PILL-03, PILL-04), implement Sections 2, 3, and 4 in `scripts/phase31-overlay-pill-assert.sh` (D-08), reload live Quickshell via the `Ctrl+Super+R` reload sequence (D-07), and execute the full test suite and `arch/dots-hyprland.sh verify --strict` confirming zero defects and zero findings (D-08, INTG-02).

Purpose: Fulfill the core user requirement of allowing status bar pills to dynamically expand and contract fluidly to accommodate detailed text without popping, clipping, or visual drift.
Output: Dynamic `BarContent.qml`, smoothly animated `BarGroup.qml`, complete 4-section assertion harness `scripts/phase31-overlay-pill-assert.sh`, and verified live Quickshell reload.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-CONTEXT.md
@.planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-RESEARCH.md
@.planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-PATTERNS.md
@.planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-VALIDATION.md
@restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
@restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml
@scripts/phase31-overlay-pill-assert.sh
@arch/dots-hyprland.sh
</context>

<assumptions>
- In QtQuick 6, removing implicitWidth overrides inside a Row layout allows children to evaluate their native implicitWidth, causing the Row to lay out child items based on dynamic content size.
- BarGroup's native calculation implicitWidth: vertical ? Appearance.sizes.baseVerticalBarWidth : (gridLayout.implicitWidth + padding * 2) calculates width accurately from grid child items.
- In rightCenterGroup (MouseArea wrapping BarGroup), setting implicitWidth: rightCenterGroupContent.implicitWidth and implicitHeight: rightCenterGroupContent.implicitHeight propagates dimension changes to the parent layout and maintains correct mouse hitboxes.
- Adding Behavior on implicitWidth with Appearance.animationCurves.emphasizedDecel (250ms) interpolates container size smoothly when child text metrics expand or contract.
- Standalone qmllint/qmlformat fails on ECMAScript optional chaining (Config.options?.bar.borderless); syntax and structure assertions must use regex token verification and live process validation instead.
- Reloading Quickshell via killall qs quickshell 2>/dev/null || true; nohup qs -c ii >/dev/null 2>&1 & restarts the status bar cleanly without interrupting Hyprland or active window sessions.
</assumptions>

<tasks>

<task id="31-02-01" type="execute">
  <name>Unlock dynamic content-driven pill width in BarContent.qml and verify Section 2</name>
  <read_first>
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
    vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/bar/BarContent.qml
    scripts/phase31-overlay-pill-assert.sh
    .planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-PATTERNS.md
    .planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-CONTEXT.md
  </read_first>
  <files>
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
    scripts/phase31-overlay-pill-assert.sh
  </files>
  <action>
    Unlock dynamic content-driven pill widths in `BarContent.qml` per D-01, D-04, PILL-02, and PILL-03:
    1. In `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml`:
       - In `leftCenterGroup` (around line 114), remove `implicitWidth: root.centerSideModuleWidth`. Allow `BarGroup`'s native calculation `implicitWidth: vertical ? Appearance.sizes.baseVerticalBarWidth : (gridLayout.implicitWidth + padding * 2)` to dictate container width directly without artificial ceilings (D-01, D-04).
       - In `rightCenterGroup` (around line 159), remove `implicitWidth: root.centerSideModuleWidth`. Add explicit implicit property propagation: `implicitWidth: rightCenterGroupContent.implicitWidth` and `implicitHeight: rightCenterGroupContent.implicitHeight` so the outer `MouseArea` expands fluidly to encapsulate the inner `rightCenterGroupContent` `BarGroup` (D-01).
       - Retain all other existing properties in `BarContent.qml` including `middleCenterGroup` Workspaces, `VerticalBarSeparator` bindings, and mouse click handlers.
    2. In `scripts/phase31-overlay-pill-assert.sh`, implement Section 2 assertions:
       - Check that `leftCenterGroup` block does NOT contain `implicitWidth: root.centerSideModuleWidth`.
       - Check that `rightCenterGroup` block does NOT contain `implicitWidth: root.centerSideModuleWidth`.
       - Check that `rightCenterGroup` block contains `implicitWidth: rightCenterGroupContent.implicitWidth`.
       - Check that `rightCenterGroup` block contains `implicitHeight: rightCenterGroupContent.implicitHeight`.
    3. Run `bash scripts/phase31-overlay-pill-assert.sh --section 2` to verify Section 2 passes with FAIL=0.
  </action>
  <acceptance_criteria>
    grep -q 'implicitWidth: root.centerSideModuleWidth' restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml exits 1.
    grep -q 'implicitWidth: rightCenterGroupContent.implicitWidth' restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml exits 0.
    grep -q 'implicitHeight: rightCenterGroupContent.implicitHeight' restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml exits 0.
    bash scripts/phase31-overlay-pill-assert.sh --section 2 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase31-overlay-pill-assert.sh --section 2</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>BarContent.qml center groups are free of artificial width clamps and dynamically propagate child pill dimensions, verified by Section 2 passing with FAIL=0.</done>
</task>

<task id="31-02-02" type="execute">
  <name>Implement smooth width animation and upstream visual fidelity in BarGroup.qml and verify Section 3</name>
  <read_first>
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml
    vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/Appearance.qml
    scripts/phase31-overlay-pill-assert.sh
    .planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-PATTERNS.md
    .planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-CONTEXT.md
  </read_first>
  <files>
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml
    scripts/phase31-overlay-pill-assert.sh
  </files>
  <action>
    Add fluid width resizing animation and enforce visual styling purity in `BarGroup.qml` per D-02, D-03, PILL-02, PILL-03, and PILL-04:
    1. In `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml`:
       - Add a `Behavior on implicitWidth` block:
         - Set `enabled: !root.vertical` so vertical bars (if configured) do not animate horizontal width.
         - Add `NumberAnimation` with `duration: 250`, `easing.type: Easing.BezierSpline`, and `easing.bezierCurve: Appearance.animationCurves.emphasizedDecel` (D-02).
       - Enforce 100% upstream visual fidelity to dots-hyprland tokens per user directive and D-03:
         - Corner radius: `radius: Appearance.rounding.small` (12px per PILL-02).
         - Internal padding: `property real padding: 5` (5px per PILL-03).
         - Background margins: topMargin/bottomMargin 4, leftMargin/rightMargin 0 for horizontal (4px per PILL-03).
         - Grid layout: `columnSpacing: 4`, `rowSpacing: 12`.
         - Container background color: `Config.options?.bar.borderless ? "transparent" : Appearance.colors.colLayer1` (PILL-04).
    2. In `scripts/phase31-overlay-pill-assert.sh`, implement Section 3 assertions:
       - Check that `BarGroup.qml` contains `Behavior on implicitWidth` with `Appearance.animationCurves.emphasizedDecel`, duration 250, and `enabled: !root.vertical`.
       - Check that `BarGroup.qml` preserves `Appearance.rounding.small` (12px).
       - Check that `BarGroup.qml` preserves `property real padding: 5`.
       - Check that `BarGroup.qml` preserves borderless background condition `Config.options?.bar.borderless ? "transparent" : Appearance.colors.colLayer1`.
       - Check that `BarContent.qml` preserves middleSection `spacing: 4` and `VerticalBarSeparator` borderless binding.
    3. Run `bash scripts/phase31-overlay-pill-assert.sh --section 3` to verify Section 3 passes with FAIL=0.
  </action>
  <acceptance_criteria>
    grep -q 'Behavior on implicitWidth' restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml exits 0.
    grep -q 'Appearance.animationCurves.emphasizedDecel' restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml exits 0.
    grep -q 'radius: Appearance.rounding.small' restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml exits 0.
    grep -q 'property real padding: 5' restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml exits 0.
    grep -q 'Config.options?.bar.borderless ? "transparent" : Appearance.colors.colLayer1' restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml exits 0.
    bash scripts/phase31-overlay-pill-assert.sh --section 3 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase31-overlay-pill-assert.sh --section 3</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>BarGroup.qml smoothly animates width transitions over 250ms with Material 3 emphasized deceleration while strictly preserving upstream visual styling tokens, verified by Section 3 passing with FAIL=0.</done>
</task>

<task id="31-02-03" type="execute">
  <name>Implement Section 4, reload live Quickshell, verify strict repository integrity, and run full suite</name>
  <read_first>
    arch/dots-hyprland.sh
    scripts/phase31-overlay-pill-assert.sh
    ~/.config/hypr/hyprland/keybinds.lua
    .planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-PATTERNS.md
    .planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-CONTEXT.md
  </read_first>
  <files>
    scripts/phase31-overlay-pill-assert.sh
  </files>
  <action>
    Implement Section 4 assertions, reload live Quickshell, and run strict verification per D-06, D-07, D-08, and INTG-02:
    1. In `scripts/phase31-overlay-pill-assert.sh`, implement Section 4 assertions:
       - Check that `restow/README.md` Section 3 table matches `./scripts/gen-collision-map.sh --restow-table` byte-for-byte.
       - Assert `quickshell` package entry is present in `restow/README.md` with `rsync-replace` tag and recovery command.
       - Execute `./arch/dots-hyprland.sh verify --strict`, asserting exit 0 and 0 findings (INTG-02).
       - Assert `git status --porcelain stow/ restow/ capture/` is empty with zero uncommitted packaging drift.
    2. Trigger live Quickshell reload via process signaling (`killall qs quickshell 2>/dev/null || true; nohup qs -c ii >/dev/null 2>&1 &`) matching the `Ctrl+Super+R` keybinding contract in `~/.config/hypr/hyprland/keybinds.lua:56` per D-07. Verify that Quickshell starts and runs without crashes.
    3. Run the full test suite: `bash scripts/phase31-overlay-pill-assert.sh` (exercising all 4 sections) and `./arch/dots-hyprland.sh verify --strict`. Confirm both commands exit 0 with `FAIL=0 FINDINGS=0`.
  </action>
  <acceptance_criteria>
    bash scripts/phase31-overlay-pill-assert.sh exits 0 with FAIL=0 FINDINGS=0.
    ./arch/dots-hyprland.sh verify --strict exits 0 with FINDINGS=0.
    quickshell list --all shows running ii shell instance.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase31-overlay-pill-assert.sh && ./arch/dots-hyprland.sh verify --strict</automated>
    <fails_when>non-zero exit code, "[FAIL]" in assert output, or findings in verify --strict</fails_when>
  </verify>
  <done>Live Quickshell reloaded and verified running, full 4-section assertion harness and arch/dots-hyprland.sh verify --strict exit 0 with 0 findings, and git working tree remains clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QML Property Bindings → Quickshell QML Runtime | Unclamped width calculations evaluated by QML engine without layout overflows or infinite binding loops |
| Live Reload Hook → Desktop Window Management | Signaling Quickshell without crashing Hyprland or disrupting user application windows |
| Strict Repository Verifier → File System State | Verifying symlinks and permissions without modifying live dotfiles |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-05 | Denial of Service / QML Layout Crash | BarContent.qml and BarGroup.qml | high | mitigate | Propagate implicit dimensions cleanly between MouseArea and inner BarGroup; avoid circular dependencies or undefined properties (D-01, PILL-02, PILL-03). |
| T-31-06 | Denial of Service / Shell Reload Failure | Live Quickshell process reload | medium | mitigate | Use standard Hyprland reload sequence `killall qs quickshell 2>/dev/null || true; nohup qs -c ii >/dev/null 2>&1 &` matching `Ctrl+Super+R` binding without disrupting active desktop sessions (D-07). |
| T-31-07 | Tampering / Layout Drift | BarGroup.qml visual tokens | medium | mitigate | Enforce exact upstream token names (`Appearance.rounding.small`, `padding: 5`, `colLayer1`) in assertions to prevent rogue CSS or style drift (D-03, PILL-04). |
</threat_model>

<verification>
Automated verification commands:
1. `bash scripts/phase31-overlay-pill-assert.sh --section 2`
2. `bash scripts/phase31-overlay-pill-assert.sh --section 3`
3. `bash scripts/phase31-overlay-pill-assert.sh && ./arch/dots-hyprland.sh verify --strict`
</verification>

<success_criteria>
1. `BarContent.qml` center groups are free of fixed width constraints (`implicitWidth: root.centerSideModuleWidth`) and propagate inner BarGroup dimensions.
2. `BarGroup.qml` animates horizontal width transitions over 250ms with `Appearance.animationCurves.emphasizedDecel`.
3. `BarGroup.qml` preserves upstream styling tokens (`rounding.small: 12px`, `padding: 5`, `colLayer1`, `borderless`).
4. Live Quickshell process reloads cleanly with zero QML evaluation errors.
5. Complete test harness `scripts/phase31-overlay-pill-assert.sh` passes all 4 sections with `FAIL=0 FINDINGS=0`.
6. `./arch/dots-hyprland.sh verify --strict` exits 0 with `FAIL=0 FINDINGS=0`.
</success_criteria>

## Artifacts this phase produces
- Personal Quickshell bar content overlay: `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml`
- Personal Quickshell bar group container overlay: `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml`
- Complete Phase 31 assertion harness: `scripts/phase31-overlay-pill-assert.sh`
- Plan summary: `.planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-02-SUMMARY.md`

<output>
Create `.planning/phases/31-overlay-infrastructure-pill-geometry-foundation/31-02-SUMMARY.md` when done
</output>
