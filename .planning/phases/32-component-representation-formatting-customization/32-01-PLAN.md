---
phase: "32"
plan: "01"
type: "tracer"
wave: 1
depends_on: []
files_modified:
  - scripts/phase32-component-formatting-assert.sh
  - capture/ii/.config/illogical-impulse/config.json
autonomous: true
requirements: [COMP-03, COMP-05, COMP-06, COMP-09]

must_haves:
  truths:
    - "scripts/phase32-component-formatting-assert.sh exists at mode 0755 implementing fail-closed conventions (set -euo pipefail, cleanup trap, --section parsing, and porcelain snapshot check) (D-08)"
    - "capture/ii/.config/illogical-impulse/config.json configures 12h time with seconds (hh:mm:ss AP), date pattern (ddd, dd-MM-yyyy), and secondPrecision: true (D-06, D-07, COMP-03)"
    - "capture/ii/.config/illogical-impulse/config.json configures weather widget for static Dhaka city in metric Celsius (useUSCS: false) (D-19, COMP-05)"
    - "capture/ii/.config/illogical-impulse/config.json enables full utility buttons suite with showScreenRecord: true (D-17, COMP-06)"
    - "capture/ii/.config/illogical-impulse/config.json configures resource warning thresholds: RAM 80%, CPU 60%, Swap 70% (D-05, COMP-01, COMP-02)"
    - "capture/ii/.config/illogical-impulse/config.json and ~/.config/illogical-impulse/config.json are synchronized with identical valid JSON content, preserving active live sidebar additions"
    - "Section 1 of scripts/phase32-component-formatting-assert.sh passes with FAIL=0 while vendor/dots-hyprland submodule remains completely clean"
  artifacts:
    - scripts/phase32-component-formatting-assert.sh
    - capture/ii/.config/illogical-impulse/config.json
  key_links:
    - "scripts/phase32-component-formatting-assert.sh is the authoritative test harness gating COMP-01 through COMP-10 across Sections 1 to 4"
    - "capture/ii/.config/illogical-impulse/config.json is the tracked repository source mirror for ~/.config/illogical-impulse/config.json"
  prohibitions:
    - "Must not edit or modify files in vendor/dots-hyprland/"
    - "Must not introduce invalid JSON syntax or trailing commas into config.json"
    - "Must not commit untracked runtime artifacts or generated caches to git"
    - "Must not run dotfiles operations or test suites as root (EUID == 0)"
---

<objective>
Scaffold the Phase 32 Nyquist assertion harness (`scripts/phase32-component-formatting-assert.sh`) with fail-closed structure covering Sections 1 through 4, configure native Tier 1 dots-hyprland options in `capture/ii/.config/illogical-impulse/config.json` (12h clock with seconds, date format, Dhaka weather in metric Celsius, full utility buttons suite with Screen Recording, and custom resource warning thresholds), synchronize to live active configuration (`~/.config/illogical-impulse/config.json`), and verify Section 1 passes with `FAIL=0` while preserving submodule cleanliness.

Purpose: Establish the tracer slice for Phase 32, securing native configuration quick-wins and delivering the automated test harness gating all subsequent QML overlay tasks.
Output: Executable test harness `scripts/phase32-component-formatting-assert.sh`, updated repository config `capture/ii/.config/illogical-impulse/config.json`, synchronized active config `~/.config/illogical-impulse/config.json`, and green Section 1 assertions.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-component-representation-formatting-customization/32-CONTEXT.md
@.planning/phases/32-component-representation-formatting-customization/32-RESEARCH.md
@.planning/phases/32-component-representation-formatting-customization/32-PATTERNS.md
@.planning/phases/32-component-representation-formatting-customization/32-VALIDATION.md
@capture/ii/.config/illogical-impulse/config.json
@scripts/phase31-overlay-pill-assert.sh
</context>

<assumptions>
- In dots-hyprland architecture, native options in config.json (Tier 1) govern DateTime format, weather location/units, utility button activation, and base resource thresholds without requiring QML modifications.
- capture/ii/.config/illogical-impulse/config.json is the tracked repository source mirror; ~/.config/illogical-impulse/config.json is the live active config read by Quickshell on initialization and hot-reloads.
- Standalone jq parses and validates config.json syntax and property values reliably across both files.
- Submodule vendor/dots-hyprland must remain 100% clean with zero git diff or untracked file churn.
- Non-root execution check (EUID != 0) prevents root permission pollution across dotfiles and test runs.
</assumptions>

<tasks>

<task id="32-01-01" type="tracer">
  <name>Scaffold Phase 32 4-section assertion harness (scripts/phase32-component-formatting-assert.sh)</name>
  <read_first>
    scripts/phase31-overlay-pill-assert.sh
    .planning/phases/32-component-representation-formatting-customization/32-VALIDATION.md
    .planning/phases/32-component-representation-formatting-customization/32-PATTERNS.md
    .planning/phases/32-component-representation-formatting-customization/32-RESEARCH.md
  </read_first>
  <files>
    scripts/phase32-component-formatting-assert.sh
  </files>
  <action>
    Scaffold `scripts/phase32-component-formatting-assert.sh` with permissions 0755 adhering strictly to repository assert harness conventions:
    1. Base scaffolding: Use `#!/usr/bin/env bash`, `set -euo pipefail`. Resolve `REPO_ROOT` and `ASSERT_SELF`. Export default `XDG_CONFIG_HOME` and `XDG_STATE_HOME`. Enforce non-root execution check (`[[ "${EUID:-$(id -u)}" -ne 0 ]] || { echo "Error: Do not run as root" >&2; exit 1; }`). Initialize `FAIL=0` and `FINDINGS=0`, and define `pass()`, `fail()`, `finding()`, and `info()` helpers.
    2. Cleanup trap: Initialize `TMP_FILES=()` and `SCRATCH_ROOTS=()`. Implement `cleanup()` removing temporary files with `rm -f` and scratch directories with `rm -rf`, and register `trap cleanup EXIT`.
    3. CLI argument parsing: Support `--section <1-4>` and `-h|--help`, validating integer range 1 to 4 with fail-closed exit 1 on invalid arguments.
    4. Two-phase working tree snapshots: Implement `porcelain_snapshot_raw` and `porcelain_snapshot` filtering ephemeral noise (`.commandcode/`, `scripts/__pycache__/`), capturing git status before test execution into `$PORCELAIN_BEFORE`.
    5. Section 1 implementation: Tier 1 Native JSON Configuration Integrity (COMP-03, COMP-05, COMP-06, COMP-09, D-05, D-06, D-07, D-17, D-19, D-20).
       - Validate JSON syntax with `jq empty` on `capture/ii/.config/illogical-impulse/config.json` and `$HOME/.config/illogical-impulse/config.json`.
       - Assert `time.format == "hh:mm:ss AP"` (D-06, COMP-03).
       - Assert `time.secondPrecision == true` (D-06, COMP-03).
       - Assert `time.dateFormat == "ddd, dd-MM-yyyy"` (D-07, COMP-03).
       - Assert `bar.utilButtons.showScreenRecord == true` (D-17, COMP-06).
       - Assert `bar.utilButtons.showScreenSnip == true` and `bar.utilButtons.showColorPicker == true`.
       - Assert `bar.weather.city == "Dhaka"` and `bar.weather.useUSCS == false` (D-19, COMP-05).
       - Assert `bar.resources.memoryWarningThreshold == 80`, `cpuWarningThreshold == 60`, `swapWarningThreshold == 70` (D-05, COMP-01, COMP-02).
       - Assert `sidebar.quickSliders.showBrightness == true` and active quick toggles are preserved.
       - Assert `capture/ii/.../config.json` matches `$HOME/.config/illogical-impulse/config.json` via `cmp -s`.
    6. Section 2 implementation: System Resources Overlays & Symlinks (COMP-01, COMP-02, D-01, D-02, D-03, D-04, D-05).
       - Assert leaf symlinks in `$HOME/.config/quickshell/ii/`:
         - `modules/ii/bar/Resource.qml` -> `$REPO_ROOT/restow/quickshell/.config/quickshell/ii/modules/ii/bar/Resource.qml`
         - `modules/ii/bar/Resources.qml` -> `$REPO_ROOT/restow/quickshell/.config/quickshell/ii/modules/ii/bar/Resources.qml`
       - Assert ancestor directories (`~/.config`, `quickshell`, `ii`, `modules`, `bar`) are real directories, not folded symlinks.
       - Assert `Resource.qml` rules:
         - Contains `property string customText: ""` and `property int criticalThreshold: 100` (D-04).
         - Synchronous recoloring via `alertColor` on `ClippedFilledCircularProgress.colPrimary`, `MaterialSymbol.color`, and `StyledText.color` (D-04).
         - Dynamic `implicitWidth` calculation (`customText.length > 0 ? percentageText.implicitWidth : fullPercentageTextMetrics.width`) (D-01).
       - Assert `Resources.qml` rules:
         - RAM definite gigabytes format `${(ResourceUsage.memoryUsed / (1024 * 1024)).toFixed(1)}/${(ResourceUsage.memoryTotal / (1024 * 1024)).toFixed(1)} GB (${Math.round(ResourceUsage.memoryUsedPercentage * 100)}%)` (D-01, COMP-01).
         - Dynamic swap reveal `shown: ResourceUsage.swapUsed > 0` with definite format (D-03, COMP-02).
         - Custom thresholds: RAM (80/90), Swap (70/85), CPU (60/90) (D-05).
         - CPU icon set to `"planner_review"` (D-02).
    7. Section 3 implementation: Status, Clock, Media, Privacy & Tray Overlays & Symlinks (COMP-03, COMP-07, COMP-08, COMP-09, D-08, D-13, D-18, D-20).
       - Assert leaf symlinks in `$HOME/.config/quickshell/ii/`:
         - `modules/ii/bar/ClockWidget.qml` -> `$REPO_ROOT/restow/quickshell/.config/quickshell/ii/modules/ii/bar/ClockWidget.qml`
         - `modules/ii/bar/SysTray.qml` -> `$REPO_ROOT/restow/quickshell/.config/quickshell/ii/modules/ii/bar/SysTray.qml`
         - `modules/ii/bar/UpdatesButton.qml` -> `$REPO_ROOT/restow/quickshell/.config/quickshell/ii/modules/ii/bar/UpdatesButton.qml`
         - `services/Privacy.qml` -> `$REPO_ROOT/restow/quickshell/.config/quickshell/ii/services/Privacy.qml`
         - `services/Updates.qml` -> `$REPO_ROOT/restow/quickshell/.config/quickshell/ii/services/Updates.qml`
       - Assert ancestor directory (`services`) is a real directory, not folded symlink.
       - Assert `ClockWidget.qml` rules: Absence of unicode bullet glyph `"•"`, presence of non-glyph spacer item (`implicitWidth: 8`) (D-08, COMP-03).
       - Assert `SysTray.qml` rules: `columnSpacing: 4` in `GridLayout` (D-20, COMP-09).
       - Assert `Privacy.qml` rules: Uses `.some(` predicate returning primitive booleans for `micActive` and `screenSharing` (D-13, COMP-08).
       - Assert `Updates.qml` rules: Aggregated non-blocking `checkupdates` + `yay -Qua` check (D-18, COMP-07).
       - Assert `UpdatesButton.qml` rules: Dedicated pending updates status pill rendering `system_update_alt` Material Symbol and executing `kitty -1 --hold=yes fish -i -c 'yay -Syu'` on click (D-18, COMP-07).
    8. Section 4 implementation: BarContent Integration, Quickshell Reload & Verification Engine (COMP-04, COMP-08, COMP-10, INTG-02, D-10..16).
       - Assert leaf symlinks in `$HOME/.config/quickshell/ii/`:
         - `modules/ii/bar/BarContent.qml` -> `$REPO_ROOT/restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml`
       - Assert `BarContent.qml` rules:
         - Absence of active/uncommented `ActiveWindow` (D-15).
         - Absence of `onScrollDown` and `onScrollUp` on left/right mouse areas, and absence of `ScrollHint` (D-16).
         - Dynamic Media player auto-collapse bound to `isPlaying` (`MprisController.activePlayer?.isPlaying`) (D-12, COMP-04).
         - Presence of `Privacy.micActive` (Amber `#FFA000`) and `Privacy.screenSharing` (Red `colError`) animated revealers (D-13, COMP-08).
         - Mounts `UpdatesButton` inside a `BarGroup` loader (D-18, COMP-07).
       - Assert Quickshell running process (`pgrep -f "qs -c ii"` or `pgrep -x quickshell`).
       - Assert submodule cleanliness: `git -C vendor/dots-hyprland status --porcelain` is clean (D-09).
       - Executes `./arch/dots-hyprland.sh verify --strict`, asserting exit 0 and 0 findings (INTG-02).
       - Asserts `restow/README.md` package table matches fresh `./scripts/gen-collision-map.sh --restow-table`.
    9. Closing porcelain invariant check: Capture `$PORCELAIN_AFTER`, compare with `$PORCELAIN_BEFORE` (`cmp -s`), print `=== done: FAIL=$FAIL FINDINGS=$FINDINGS ===`, and exit 1 if FAIL > 0, else exit 0.
  </action>
  <acceptance_criteria>
    test -x scripts/phase32-component-formatting-assert.sh exits 0.
    bash scripts/phase32-component-formatting-assert.sh --help exits 0 and outputs Usage: .* --section <1-4>.
    bash scripts/phase32-component-formatting-assert.sh --section 99 exits 1 with error message on stderr.
  </acceptance_criteria>
  <verify>
    <automated>test -x scripts/phase32-component-formatting-assert.sh && bash scripts/phase32-component-formatting-assert.sh --help | grep -q -- '--section <1-4>'</automated>
    <fails_when>non-zero exit code, file not executable, or missing '--section <1-4>' in help output</fails_when>
  </verify>
  <done>scripts/phase32-component-formatting-assert.sh exists at mode 0755, handles --section <1-4>, enforces porcelain snapshot integrity, and implements fail-closed assertions for Sections 1 through 4 matching validation domains.</done>
</task>

<task id="32-01-02" type="execute">
  <name>Configure native JSON options in capture/ii/.../config.json and sync live runtime config</name>
  <read_first>
    capture/ii/.config/illogical-impulse/config.json
    ~/.config/illogical-impulse/config.json
    scripts/phase32-component-formatting-assert.sh
    .planning/phases/32-component-representation-formatting-customization/32-PATTERNS.md
    .planning/phases/32-component-representation-formatting-customization/32-CONTEXT.md
  </read_first>
  <files>
    capture/ii/.config/illogical-impulse/config.json
  </files>
  <action>
    Configure Tier 1 native dots-hyprland options in `capture/ii/.config/illogical-impulse/config.json` and synchronize with live configuration per D-05, D-06, D-07, D-17, D-19, D-20, COMP-03, COMP-05, COMP-06, ensuring existing active live customizations are strictly preserved:
    1. Ingest and preserve live config additions first:
       - Live `~/.config/illogical-impulse/config.json` contains active user customizations: `"showBrightness": true` under `"sidebar"."quickSliders"` and 9 custom toggles under `"sidebar"."quickToggles"."android"."toggles"` (`easyEffects`, `cloudflareWarp`, `screenSnip`, `colorPicker`, `onScreenKeyboard`, `powerProfile`, `antiFlashbang`, `musicRecognition`).
       - Ingest live additions into `capture/ii/.config/illogical-impulse/config.json` (e.g. by copying `~/.config/illogical-impulse/config.json` to `capture/ii/.config/illogical-impulse/config.json` prior to applying Phase 32 Tier 1 changes, or merging the live delta) to guarantee no live sidebar customizations are overwritten or lost.
    2. Apply Tier 1 native dots-hyprland modifications in `capture/ii/.config/illogical-impulse/config.json`:
       - Under `"time"`:
         - Set `"format": "hh:mm:ss AP"` (12h format with seconds, e.g. `07:25:10 AM` per D-06, COMP-03).
         - Set `"secondPrecision": true` (per D-06, COMP-03).
         - Set `"dateFormat": "ddd, dd-MM-yyyy"` (e.g. `Sun, 20-09-2026` per D-07, COMP-03).
       - Under `"bar"."resources"`:
         - Set `"memoryWarningThreshold": 80` (per D-05, COMP-01).
         - Set `"cpuWarningThreshold": 60` (per D-05, COMP-02).
         - Set `"swapWarningThreshold": 70` (per D-05, COMP-02).
         - Set `"alwaysShowCpu": true`.
         - Set `"alwaysShowSwap": true`.
       - Under `"bar"."utilButtons"`:
         - Set `"showScreenRecord": true` (activates Screen Recording button alongside Snip, Color Picker, Keyboard, Mic, Performance Profile per D-17, COMP-06).
         - Set `"showScreenSnip": true`.
         - Set `"showColorPicker": true`.
         - Set `"showKeyboardToggle": true`.
         - Set `"showMicToggle": true`.
         - Set `"showPerformanceProfileToggle": true`.
       - Under `"bar"."weather"`:
         - Set `"city": "Dhaka"` (per D-19, COMP-05).
         - Set `"useUSCS": false` (metric Celsius per D-19, COMP-05).
         - Set `"enable": true`.
       - Under `"tray"`:
         - Set `"monochromeIcons": true` (monochrome Material You tinting per D-20, COMP-09).
         - Set `"filterPassive": true`.
       - Ensure `"sidebar"."quickSliders"."showBrightness"` remains `true` and all 9 custom quick toggles remain intact under `"sidebar"."quickToggles"."android"."toggles"`.
    3. Verify valid JSON formatting using `jq empty capture/ii/.config/illogical-impulse/config.json`.
    4. Synchronize `capture/ii/.config/illogical-impulse/config.json` to live active runtime path `$HOME/.config/illogical-impulse/config.json` (`cp -p capture/ii/.config/illogical-impulse/config.json ~/.config/illogical-impulse/config.json`).
    5. Run `bash scripts/phase32-component-formatting-assert.sh --section 1` to verify Section 1 passes with `FAIL=0`.
  </action>
  <acceptance_criteria>
    jq -e '.time.format == "hh:mm:ss AP" and .time.secondPrecision == true and .time.dateFormat == "ddd, dd-MM-yyyy"' capture/ii/.config/illogical-impulse/config.json exits 0.
    jq -e '.bar.utilButtons.showScreenRecord == true and .bar.weather.city == "Dhaka" and .bar.weather.useUSCS == false' capture/ii/.config/illogical-impulse/config.json exits 0.
    jq -e '.bar.resources.memoryWarningThreshold == 80 and .bar.resources.cpuWarningThreshold == 60 and .bar.resources.swapWarningThreshold == 70' capture/ii/.config/illogical-impulse/config.json exits 0.
    jq -e '.sidebar.quickSliders.showBrightness == true' capture/ii/.config/illogical-impulse/config.json exits 0.
    jq -e '.sidebar.quickToggles.android.toggles | length >= 10' capture/ii/.config/illogical-impulse/config.json exits 0.
    cmp -s capture/ii/.config/illogical-impulse/config.json ~/.config/illogical-impulse/config.json exits 0.
    bash scripts/phase32-component-formatting-assert.sh --section 1 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase32-component-formatting-assert.sh --section 1</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>capture/ii/.config/illogical-impulse/config.json and live active ~/.config/illogical-impulse/config.json are synchronized with valid Tier 1 configuration options while preserving live sidebar additions, verified by Section 1 passing with FAIL=0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON Configuration → Quickshell Bar Runtime | Native configuration parameters parsed by Quickshell JSON parser without schema corruption or invalid type casting |
| Test Assert Harness → Git Working Tree | Assertion script performing clean inspection without mutating working tree status or leaving scratch artifacts |
| User Privileges → System Files | Dotfiles maintenance and assertion tests executed under non-root unprivileged user account |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Tampering / Test Harness Churn | scripts/phase32-component-formatting-assert.sh | medium | mitigate | Enforce two-phase porcelain snapshots comparing `$PORCELAIN_BEFORE` and `$PORCELAIN_AFTER` with cleanup traps on exit (D-08). |
| T-32-02 | Tampering / JSON Malformation | config.json | high | mitigate | Validate syntax strictly with `jq empty` before synchronization and assert explicit values in Section 1. |
| T-32-03 | Elevation of Privilege / Root Execution | Environment | medium | mitigate | Guard assert script with non-root check `[[ "${EUID:-$(id -u)}" -ne 0 ]]`. |
| T-32-04 | Loss of State / Live Config Overwrite | config.json | high | mitigate | Ingest live additions (showBrightness and custom quick toggles) into repository mirror before applying Tier 1 modifications, and assert preservation in Section 1. |
</threat_model>

<verification>
Automated verification commands:
1. `test -x scripts/phase32-component-formatting-assert.sh && bash scripts/phase32-component-formatting-assert.sh --help | grep -q -- '--section <1-4>'`
2. `bash scripts/phase32-component-formatting-assert.sh --section 1`
</verification>

<success_criteria>
1. `scripts/phase32-component-formatting-assert.sh` exists at mode 0755, parses `--section <1-4>`, and implements domain-partitioned fail-closed assertions for all 4 sections.
2. `capture/ii/.config/illogical-impulse/config.json` contains valid 12h time with seconds, full date format, Dhaka metric weather, full utility buttons suite with `showScreenRecord: true`, and custom resource warning thresholds (80/60/70) while preserving active live sidebar additions.
3. `capture/ii/.config/illogical-impulse/config.json` matches live `~/.config/illogical-impulse/config.json` byte-for-byte.
4. `vendor/dots-hyprland` submodule remains completely clean with zero uncommitted changes.
5. `bash scripts/phase32-component-formatting-assert.sh --section 1` exits 0 with `FAIL=0`.
</success_criteria>

## Artifacts this phase produces
- Test assertion harness: `scripts/phase32-component-formatting-assert.sh`
- Tier 1 native configuration source: `capture/ii/.config/illogical-impulse/config.json`
- Plan summary: `.planning/phases/32-component-representation-formatting-customization/32-01-SUMMARY.md`

<output>
Create `.planning/phases/32-component-representation-formatting-customization/32-01-SUMMARY.md` when done
</output>
