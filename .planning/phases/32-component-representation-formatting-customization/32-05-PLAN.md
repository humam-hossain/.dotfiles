---
phase: "32"
plan: "05"
type: "execute"
wave: 5
gap_closure: true
gap_ids: [G-32-3]
depends_on: ["32-01", "32-02", "32-03", "32-04"]
files_modified:
  - restow/quickshell/.config/quickshell/ii/scripts/videos/record.sh
  - restow/quickshell/.config/quickshell/ii/services/Privacy.qml
  - capture/ii/.config/illogical-impulse/config.json
  - scripts/phase32-component-formatting-assert.sh
autonomous: true
requirements: [COMP-06, COMP-08]

must_haves:
  truths:
    - "restow/quickshell/.../record.sh is deployed via GNU Stow as a leaf symlink, saves the active recording target to a runtime state file on start, and emits a completion notification containing the saved recording file path on stop (COMP-06)"
    - "restow/quickshell/.../Privacy.qml incorporates reactive wf-recorder telemetry so the red screen recording/sharing privacy revealer in BarContent.qml reveals during active screen recordings (COMP-08)"
    - "capture/ii/.../config.json and live config provide clear power profile handling and guidance for power-profiles-daemon (COMP-06)"
    - "scripts/phase32-component-formatting-assert.sh passes all 4 sections with FAIL=0 and arch/dots-hyprland.sh verify --strict exits 0 with 0 findings"
  artifacts:
    - restow/quickshell/.config/quickshell/ii/scripts/videos/record.sh
    - restow/quickshell/.config/quickshell/ii/services/Privacy.qml
    - scripts/phase32-component-formatting-assert.sh
  key_links:
    - "GNU Stow deploys record.sh and Privacy.qml as leaf symlinks without directory folding"
    - "record.sh start sets the recording state, and Privacy.qml dynamically reveals the red status indicator during recording"
  prohibitions:
    - "Must not edit or modify files under vendor/dots-hyprland/"
    - "Must not use banned stow --adopt"
    - "Must not omit --no-folding when invoking GNU Stow"
---

<objective>
Close UAT gap G-32-3 by:
1. Overlaying `record.sh` in `restow/quickshell/.config/quickshell/ii/scripts/videos/record.sh` to track the active recording file path across invocations and emit a completion notification with `Saved to: <filepath>`.
2. Enhancing `restow/quickshell/.config/quickshell/ii/services/Privacy.qml` with reactive screen recording detection (`wf-recorder`) so `Privacy.screenSharing` reveals the red indicator in `BarContent.qml` during active screen recordings.
3. Providing clear resolution and documentation for the `power-profiles-daemon` requirement for power profile toggling.
4. Synchronizing test assertions in `scripts/phase32-component-formatting-assert.sh` and ensuring 100% strict repository verification.

Purpose: Address the user's UAT feedback regarding screen recording notifications, privacy icon visibility, and power profile controls.
Output: Fixed QML privacy service, overlay record script, updated assert harness, and clean verification.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/32-component-representation-formatting-customization/32-UAT.md
@.planning/debug/screen-recording-power-profiles-notification.md
</context>

<tasks>

<task type="auto">
  <name>Overlay record.sh with active file tracking and notification path</name>
  <description>
  Create `restow/quickshell/.config/quickshell/ii/scripts/videos/record.sh` based on upstream `vendor/dots-hyprland/dots/.config/quickshell/ii/scripts/videos/record.sh`.
  Modify the script so that:
  1. On recording start: it stores the full target recording path into `/tmp/quickshell-current-recording.txt`.
  2. On recording stop: it reads `/tmp/quickshell-current-recording.txt`, deletes the temporary file, and sends `notify-send "Recording Stopped" "Saved to: $saved_path" -a 'Recorder'`.
  3. Deploy the script via GNU Stow (`stow -v -t ~ --no-folding quickshell`) ensuring `~/.config/quickshell/ii/scripts/videos/record.sh` is an executable leaf symlink.
  </description>
  <files>
    restow/quickshell/.config/quickshell/ii/scripts/videos/record.sh
  </files>
  <action>
  1. Create directory `restow/quickshell/.config/quickshell/ii/scripts/videos` if not already present.
  2. Copy upstream `record.sh` as the base.
  3. Update start logic to record `$RECORDING_DIR/$filename` to `/tmp/quickshell-current-recording.txt`.
  4. Update stop logic to read `/tmp/quickshell-current-recording.txt` and report `Saved to: $last_file`.
  5. Make `restow/quickshell/.config/quickshell/ii/scripts/videos/record.sh` executable (`chmod +x`).
  6. Restow quickshell without folding and verify leaf symlink.
  </action>
  <verify>
  `test -x restow/quickshell/.config/quickshell/ii/scripts/videos/record.sh` exits 0.
  `test -L ~/.config/quickshell/ii/scripts/videos/record.sh` exits 0.
  `grep -q 'Saved to:' restow/quickshell/.config/quickshell/ii/scripts/videos/record.sh` exits 0.
  </verify>
  <done>record.sh is deployed as an executable leaf symlink, tracks target video path in /tmp/quickshell-current-recording.txt, and reports 'Saved to: <path>' on stop</done>
</task>

<task type="auto">
  <name>Add reactive screen recording telemetry to Privacy.qml</name>
  <description>
  In `restow/quickshell/.config/quickshell/ii/services/Privacy.qml`, add reactive detection for `wf-recorder` (screen recording) and integrate it into `screenSharing`:
  ```qml
  property bool screenRecording: false
  readonly property bool screenSharing: screenRecording || Pipewire.linkGroups.values.some(pwlg => pwlg.source?.type === PwNodeType.VideoSource)
  ```
  Add a periodic `Process` or state check that polls `pgrep -x wf-recorder` so that when `wf-recorder` runs, `screenRecording` becomes `true` and triggers the red alert icon in `BarContent.qml`.
  </description>
  <files>
    restow/quickshell/.config/quickshell/ii/services/Privacy.qml
  </files>
  <action>
  1. Import `Quickshell.Io` in `restow/quickshell/.config/quickshell/ii/services/Privacy.qml`.
  2. Add `property bool screenRecording: false`.
  3. Add a `Process` checking `pgrep -x wf-recorder` and a 1-second `Timer`.
  4. Update `readonly property bool screenSharing: screenRecording || Pipewire.linkGroups.values.some(pwlg => pwlg.source?.type === PwNodeType.VideoSource)`.
  5. Verify that Quickshell parses and reloads `Privacy.qml` without errors.
  </action>
  <verify>
  `grep -q 'screenRecording' restow/quickshell/.config/quickshell/ii/services/Privacy.qml` exits 0.
  `grep -q 'wf-recorder' restow/quickshell/.config/quickshell/ii/services/Privacy.qml` exits 0.
  `pgrep -x quickshell >/dev/null` exits 0.
  </verify>
  <done>Privacy.qml detects active wf-recorder execution and activates the red screen recording alert revealer in BarContent.qml</done>
</task>

<task type="auto">
  <name>Power profiles daemon integration and configuration</name>
  <description>
  Document and verify power profiles handling on Arch Linux.
  Clarify that Quickshell's PowerProfiles module communicates via D-Bus with `power-profiles-daemon`.
  Verify `capture/ii/.../config.json` retains `showPerformanceProfileToggle: true`, and provide the required user systemctl command (`sudo pacman -S power-profiles-daemon && sudo systemctl enable --now power-profiles-daemon.service`) for native power profile management.
  </description>
  <files>
    capture/ii/.config/illogical-impulse/config.json
  </files>
  <action>
  1. Verify `capture/ii/.config/illogical-impulse/config.json` and `~/.config/illogical-impulse/config.json` keep `.bar.utilButtons.showPerformanceProfileToggle: true`.
  2. Ensure documentation and verification scripts reflect that `power-profiles-daemon` is required for switching power states on Arch Linux.
  </action>
  <verify>
  `jq -e '.bar.utilButtons.showPerformanceProfileToggle == true' capture/ii/.config/illogical-impulse/config.json` exits 0.
  `cmp capture/ii/.config/illogical-impulse/config.json ~/.config/illogical-impulse/config.json` exits 0.
  </verify>
  <done>Power profiles configuration and system daemon dependencies are validated</done>
</task>

<task type="auto">
  <name>Update Nyquist assertion harness and verify repository integrity</name>
  <description>
  Update `scripts/phase32-component-formatting-assert.sh` to:
  1. Verify `restow/quickshell/.../scripts/videos/record.sh` exists, is leaf symlinked, executable, and contains `Saved to:`.
  2. Verify `Privacy.qml` checks `screenRecording` and `wf-recorder`.
  3. Execute all 4 sections with `FAIL=0 FINDINGS=0`.
  4. Run `./arch/dots-hyprland.sh verify --strict` to ensure 0 findings.
  </description>
  <files>
    scripts/phase32-component-formatting-assert.sh
  </files>
  <action>
  1. Add assertion in Section 3 for `record.sh` symlink, execution bit, and `Saved to:` stop notification.
  2. Add assertion in Section 3 for `Privacy.qml` `screenRecording` telemetry.
  3. Run `./scripts/phase32-component-formatting-assert.sh` across all 4 sections.
  4. Run `./arch/dots-hyprland.sh verify --strict`.
  </action>
  <verify>
  `./scripts/phase32-component-formatting-assert.sh` outputs `FAIL=0 FINDINGS=0`.
  `./arch/dots-hyprland.sh verify --strict` exits 0 with 0 findings.
  </verify>
  <done>All Nyquist assertions and repository strict verification pass with 0 errors</done>
</task>

</tasks>
