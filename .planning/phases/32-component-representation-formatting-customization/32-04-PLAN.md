---
phase: "32"
plan: "04"
type: "execute"
wave: 4
gap_closure: true
gap_ids: [G-32-2]
depends_on: ["32-01", "32-02", "32-03"]
files_modified:
  - capture/ii/.config/illogical-impulse/config.json
  - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
  - restow/quickshell/.config/quickshell/ii/modules/ii/bar/Resource.qml
  - restow/quickshell/.config/quickshell/ii/modules/ii/bar/Resources.qml
  - restow/quickshell/.config/quickshell/ii/scripts/system-update.sh
  - scripts/phase32-component-formatting-assert.sh
autonomous: true
requirements: [COMP-01, COMP-02, COMP-04, COMP-06, COMP-07]

must_haves:
  truths:
    - "capture/ii/.../config.json and ~/.config/illogical-impulse/config.json set showMicToggle: true under .bar.utilButtons so center utility buttons mic toggle is retained (COMP-06)"
    - "restow/quickshell/.../BarContent.qml removes the Audio.source.muted white mic_off Revealer from indicatorsRowLayout while retaining the amber Privacy.micActive alert (COMP-06)"
    - "restow/quickshell/.../scripts/system-update.sh and config.json apps.update execute yay -Syu --noconfirm && sudo pacman -Sc --noconfirm && yay -Sc --noconfirm to clean both official pacman and AUR caches (COMP-07)"
    - "restow/quickshell/.../Resource.qml sets 6px spacing between circular progress icon and metric text, and Resources.qml provides 8px inter-resource margins (COMP-01, COMP-02)"
    - "restow/quickshell/.../BarContent.qml clamps Media with Layout.maximumWidth so long titles truncate with elide: Text.ElideRight per upstream dots-hyprland behavior (COMP-04)"
    - "scripts/phase32-component-formatting-assert.sh passes all 4 sections with FAIL=0 and arch/dots-hyprland.sh verify --strict exits 0 with 0 findings"
  artifacts:
    - capture/ii/.config/illogical-impulse/config.json
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/Resource.qml
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/Resources.qml
    - restow/quickshell/.config/quickshell/ii/scripts/system-update.sh
    - scripts/phase32-component-formatting-assert.sh
  key_links:
    - "GNU Stow deploys updated BarContent.qml, Resource.qml, Resources.qml, and system-update.sh as leaf symlinks without directory folding"
    - "UpdatesButton.qml dispatches system-update.sh which invokes unattended upgrades followed by pacman and yay cache cleanups"
  prohibitions:
    - "Must not edit or modify files under vendor/dots-hyprland/"
    - "Must not use banned stow --adopt"
    - "Must not omit --no-folding when invoking GNU Stow"
---

<objective>
Close UAT gap G-32-2 by:
1. Retaining the center utility buttons mic toggle (`showMicToggle: true`) and removing the redundant white `mic_off` indicator from the right sidebar status indicators cluster (retaining the amber recording alert).
2. Updating `system-update.sh` and `config.json` with the `--noconfirm` flag and chaining both pacman and yay stale cache cleanups (`yay -Syu --noconfirm && sudo pacman -Sc --noconfirm && yay -Sc --noconfirm`).
3. Adding a 6px visual gap between the circular progress icon and the metric text in `Resource.qml`, and maintaining 8px inter-resource margins in `Resources.qml`.
4. Clamping `Media` layout width in `BarContent.qml` so long audio/video titles truncate with an ellipsis (`elide: Text.ElideRight`) matching upstream dots-hyprland behavior while preserving dynamic pill sizing for shorter titles.

Purpose: Address the user's UAT feedback and visual refinement requirements.
Output: Updated QML components, shell scripts, and JSON configs, with synchronized test assertions.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/32-component-representation-formatting-customization/32-UAT.md
@.planning/debug/tray-mic-updates-media-truncation.md
</context>

<tasks>

<task type="auto" name="Re-enable utility mic toggle and remove status cluster muted mic indicator">
  <description>
  Restore `"showMicToggle": true` under `.bar.utilButtons` in `capture/ii/.config/illogical-impulse/config.json` and sync to `$HOME/.config/illogical-impulse/config.json`. In `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml`, remove the `Audio.source?.audio?.muted` `mic_off` `Revealer` from `indicatorsRowLayout` (lines 282-294), while keeping the `Privacy.micActive` amber alert intact. Deploy via Stow `--no-folding`.
  </description>
  <files>
    capture/ii/.config/illogical-impulse/config.json
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
  </files>
  <action>
  1. In `capture/ii/.config/illogical-impulse/config.json`:
     Change `"showMicToggle": false` back to `"showMicToggle": true`.
  2. Copy `capture/ii/.config/illogical-impulse/config.json` to `$HOME/.config/illogical-impulse/config.json`.
  3. In `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml`:
     Locate and remove the following block from `indicatorsRowLayout`:
     ```qml
                    Revealer {
                        reveal: Audio.source?.audio?.muted ?? false
                        Layout.fillHeight: true
                        Layout.rightMargin: reveal ? indicatorsRowLayout.realSpacing : 0
                        Behavior on Layout.rightMargin {
                            animation: Appearance.animation.elementMoveFast.numberAnimation.createObject(this)
                        }
                        MaterialSymbol {
                            text: "mic_off"
                            iconSize: Appearance.font.pixelSize.larger
                            color: rightSidebarButton.colText
                        }
                    }
     ```
     Ensure the `Privacy.micActive` amber Revealer remains untouched above it.
  4. Deploy changes via GNU Stow `--no-folding`:
     `cd restow && stow --verbose=5 --no-folding -t ~ quickshell`
  </action>
  <verify>
  `jq -r '.bar.utilButtons.showMicToggle' capture/ii/.config/illogical-impulse/config.json` outputs `true`.
  `cmp capture/ii/.config/illogical-impulse/config.json ~/.config/illogical-impulse/config.json` exits 0.
  `grep -q 'mic_off' restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml` exits non-zero (1).
  `grep -q 'Privacy.micActive' restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml` exits 0.
  </verify>
</task>

<task type="auto" name="Update system-update.sh with unattended flags and pacman + yay cache cleanups">
  <description>
  Update `restow/quickshell/.config/quickshell/ii/scripts/system-update.sh` to add `--noconfirm` to `yay -Syu` and chain `sudo pacman -Sc --noconfirm && yay -Sc --noconfirm` to purge stale package archives from both pacman's system cache and yay's AUR cache. Update `config.json` `.apps.update` command to match. Deploy via Stow.
  </description>
  <files>
    restow/quickshell/.config/quickshell/ii/scripts/system-update.sh
    capture/ii/.config/illogical-impulse/config.json
  </files>
  <action>
  1. In `restow/quickshell/.config/quickshell/ii/scripts/system-update.sh`:
     Replace `'yay -Syu'` with `'yay -Syu --noconfirm && sudo pacman -Sc --noconfirm && yay -Sc --noconfirm'`.
  2. In `capture/ii/.config/illogical-impulse/config.json`:
     Update `"update"` under `.apps` to `"kitty -1 --hold=yes fish -i -c 'yay -Syu --noconfirm && sudo pacman -Sc --noconfirm && yay -Sc --noconfirm'"`.
  3. Sync `config.json` to `$HOME/.config/illogical-impulse/config.json`.
  4. Deploy via Stow `--no-folding`:
     `cd restow && stow --verbose=5 --no-folding -t ~ quickshell`
  </action>
  <verify>
  `grep -q -- 'sudo pacman -Sc --noconfirm && yay -Sc --noconfirm' restow/quickshell/.config/quickshell/ii/scripts/system-update.sh` exits 0.
  `jq -r '.apps.update' capture/ii/.config/illogical-impulse/config.json` contains `sudo pacman -Sc`.
  </verify>
</task>

<task type="auto" name="Add icon-to-text spacing in Resource.qml and verify inter-resource margins">
  <description>
  In `restow/quickshell/.config/quickshell/ii/modules/ii/bar/Resource.qml`, increase `spacing` inside `resourceRowLayout` from `2` to `6` to provide comfortable visual margin between the circular progress icon and the text metric across RAM, Swap, and CPU. In `Resources.qml`, ensure inter-resource margins (`Layout.leftMargin: shown ? 8 : 0`) provide balanced pill separation.
  </description>
  <files>
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/Resource.qml
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/Resources.qml
  </files>
  <action>
  1. In `restow/quickshell/.config/quickshell/ii/modules/ii/bar/Resource.qml`:
     Under `RowLayout { id: resourceRowLayout`:
     Change `spacing: 2` to `spacing: 6`.
  2. In `restow/quickshell/.config/quickshell/ii/modules/ii/bar/Resources.qml`:
     Update `Layout.leftMargin: shown ? 6 : 0` on Swap and CPU to `Layout.leftMargin: shown ? 8 : 0`.
  3. Deploy via Stow `--no-folding`:
     `cd restow && stow --verbose=5 --no-folding -t ~ quickshell`
  </action>
  <verify>
  `grep -q 'spacing: 6' restow/quickshell/.config/quickshell/ii/modules/ii/bar/Resource.qml` exits 0.
  `grep -q 'Layout.leftMargin: shown ? 8 : 0' restow/quickshell/.config/quickshell/ii/modules/ii/bar/Resources.qml` exits 0.
  </verify>
</task>

<task type="auto" name="Clamp Media layout width for text truncation and update test assertions">
  <description>
  In `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml`, add `Layout.maximumWidth: (root.useShortenedForm === 1) ? 140 : 200` to the `Media` component in `leftCenterGroup`. Update all corresponding assertions in `scripts/phase32-component-formatting-assert.sh` (Sections 1, 2, 3, 4) and verify all test suites pass.
  </description>
  <files>
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
    scripts/phase32-component-formatting-assert.sh
  </files>
  <action>
  1. In `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml`:
     Under `leftCenterGroup`:
     ```qml
            Media {
                // Media widget retained with upstream dots-hyprland visibility (COMP-04)
                visible: root.useShortenedForm < 2
                Layout.fillWidth: true
                Layout.maximumWidth: (root.useShortenedForm === 1) ? 140 : 200
            }
     ```
  2. In `scripts/phase32-component-formatting-assert.sh`:
     - Section 1: Assert `showMicToggle` is `true`.
     - Section 2: Assert `spacing: 6` in `Resource.qml`.
     - Section 3: Assert `system-update.sh` contains `--noconfirm` and `pacman -Sc`; assert `BarContent.qml` does NOT contain `mic_off` revealer.
     - Section 4: Assert `BarContent.qml` contains `Layout.maximumWidth` on Media.
  3. Deploy via GNU Stow `--no-folding`:
     `cd restow && stow --verbose=5 --no-folding -t ~ quickshell`
  4. Reload live Quickshell process.
  5. Run complete test suites:
     `scripts/phase32-component-formatting-assert.sh`
     `arch/dots-hyprland.sh verify --strict`
  </action>
  <verify>
  `grep -q 'Layout.maximumWidth' restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml` exits 0.
  `./scripts/phase32-component-formatting-assert.sh` exits 0 with FAIL=0 across all 4 sections.
  `./arch/dots-hyprland.sh verify --strict` exits 0 with 0 findings.
  </verify>
</task>

</tasks>
