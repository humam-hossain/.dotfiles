---
phase: 17-unblock-stow-and-restore-the-session-target
plan: 05
type: execute
wave: 5
depends_on: ["17-04"]
files_modified:
  - .config/hypr/custom/execs.lua
  - docs/dots-hyprland-workflow.md
  - scripts/phase17-unblock-assert.sh
  - .planning/phases/17-unblock-stow-and-restore-the-session-target/17-HANDOFF.md
autonomous: true
requirements: [START-02, START-03]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The repo copy of `custom/execs.lua` contains the literal `systemctl --user start hyprland-session.service` inside an `hl.on(\"hyprland.start\", ...)` block, and carries the Phase 13 authoring-source-of-truth header"
    - "The repo copy and the live copy of `custom/execs.lua` are byte-identical, proven by `cmp -s` — the hand-sync window stays closed on every commit"
    - "`hyprland-session.service` is in state `linked`, never `enabled`; the assert compares the printed state string rather than the exit status, because `is-enabled` prints `linked` and exits 1"
    - "Starting `hyprland-session.service` takes `graphical-session.target` from inactive to active through the unit's `Wants=` dependency; the target itself refuses a manual start and can only be reached that way"
    - "The live session is left in its as-found state after the mechanism is proven once by hand, so criterion 5's post-re-login evidence is not contaminated by a hand start"
    - "When the target is inactive the assert reports `[INFO]` naming the operator re-login as the required step, never `[FAIL]` — an agent cannot end the session, so a red here would be permanently red through no defect"
    - "The operator docs name the `systemctl --user disable` footgun, its recovery command, and `mask` as the safe alternative for a stow-managed unit"
    - "The playbook's expected-output lines for the phase-14 verify script are updated for the finding count that flips once the session target is restored, so the playbook does not assert output that is about to become stale"
    - statement: "START-02's edge behaviour was surfaced by the probe as `unclassified`; the predicates above were authored from the five sub-claims the research decomposition produced rather than from a probe category"
      verification: backstop
    - statement: "START-03's edge behaviour was surfaced by the probe as `unclassified`; the predicate above is a documentation-presence claim, and whether the documented recovery actually restores the link is asserted by mechanism review rather than by executing `disable` against the real unit"
      verification: backstop
  artifacts:
    - path: ".config/hypr/custom/execs.lua"
      provides: "the one startup entry this phase restores, in the vendor-sanctioned custom overlay slot"
      contains: "systemctl --user start hyprland-session.service"
    - path: "docs/dots-hyprland-workflow.md"
      provides: "the START-03 warning block with recovery command and safe alternative, plus the corrected known-losses ownership sentence and expected-output lines"
      contains: "systemctl --user mask"
    - path: ".planning/phases/17-unblock-stow-and-restore-the-session-target/17-HANDOFF.md"
      provides: "the Phase 18 handoff rows this phase generates — folded directories, the still-tracked repo-root generated theme file, and the authoring-tree row for the redistribution table"
      contains: "Phase 18"
    - path: "scripts/phase17-unblock-assert.sh"
      provides: "criterion 5 sections 5a-5c and 5e, and criterion 6 sections 6a-6c"
      contains: "graphical-session.target"
  key_links:
    - from: ".config/hypr/custom/execs.lua"
      to: "hyprland-session.service"
      via: "hl.exec_cmd inside the hyprland.start handler, registered after the vendor handler by hyprland.lua's guarded require of custom.execs"
      pattern: "hl.exec_cmd"
    - from: "hyprland-session.service"
      to: "graphical-session.target"
      via: "the unit's Wants= dependency — the only reachable path, since the target refuses manual start"
      pattern: "Wants="
    - from: "scripts/phase17-unblock-assert.sh"
      to: "the repo and live copies of custom/execs.lua"
      via: "cmp -s byte-identity gate plus a fixed-string grep for the command literal"
      pattern: "cmp -s"
  prohibitions:
    - statement: "Must NOT run `systemctl --user enable` on the session unit. Enabling moves it from state `linked` to `enabled` and creates wants-directory entries — which is precisely the state in which the `disable` footgun this phase documents becomes reachable. Start only; the unit is oneshot with RemainAfterExit so one start suffices"
      status: resolved
      verification: judgment
    - statement: "Must NOT leave the session target hand-started at the end of the wave. The post-re-login check would then report a green it did not earn, and the one observation this phase genuinely cannot schedule would be silently faked"
      status: resolved
      verification: judgment
    - statement: "Must NOT edit anything under the vendor-owned live `~/.config/hypr/hyprland/` tree. The only live write this plan makes is the applied copy of `custom/execs.lua`; the whole point of the custom overlay slot is that the vendor tree stays byte-identical to the pin"
      status: resolved
      verification: judgment
---

<objective>
Restore the session bootstrap lost at the Phase 14 adopt — one startup entry in the custom
overlay slot, applied to the live tree and proven byte-identical — and write down the
`systemctl --user disable` footgun with its recovery command before anyone can hit it.

Purpose: `graphical-session.target` is inactive today, which is what the
`xdg-desktop-portal` ScreenCast path depends on. The unit file, its symlink and its
dependency wiring all survive; only the line that starts it was lost. Separately, the
footgun that deletes that symlink is undocumented, and the recovery is not obvious.
Output: one restored startup entry, one warning block, one handoff document, seven assert
sections.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-CONTEXT.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-RESEARCH.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-PATTERNS.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-VALIDATION.md
@.config/hypr/custom/general.lua
@stow/systemd/.config/systemd/user/hyprland-session.service
@scripts/phase14-verify.sh
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restore the session-bootstrap entry in the custom overlay and apply it live</name>
  <files>.config/hypr/custom/execs.lua</files>
  <reversibility rating="costly">Undoing D-18's authoring-source-of-truth placement means relocating the authoring tree and updating every document that names `.config/hypr/custom/` as the source of truth, including the header comment already live in `general.lua`.</reversibility>
  <read_first>
    - `.config/hypr/custom/execs.lua` — the repo copy, currently a single newline.
    - `.config/hypr/custom/general.lua` line 1 — the authoring-source-of-truth header comment to copy verbatim.
    - `~/.config/hypr/hyprland/execs.lua` lines 1-6 — vendor-owned, read-only reference for the handler block shape and the comment stating that former startup commands go inside the function.
    - `~/.config/hypr/hyprland.lua` lines 15 and 22-24 — the guarded require that loads the custom overlay after the vendor one.
    - `stow/systemd/.config/systemd/user/hyprland-session.service` — the unit, its header comment naming how it is started, and its `Wants=` / `Before=` / `Type=oneshot` / `RemainAfterExit=yes` shape.
    - `.config/hypr/hyprland.conf` line 57 — the pre-adopt line being ported.
    - `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-CONTEXT.md` D-15 through D-18.
  </read_first>
  <action>
    Author the repo copy at `.config/hypr/custom/execs.lua` — that is the authoring source of
    truth (D-18), and `~/.config/hypr/custom/execs.lua` is the applied copy produced from it by
    one `cp`. Give it three things, in order: the authoring-source-of-truth header comment
    copied verbatim from `general.lua` line 1; a short comment naming the screen-share fix and
    stating that the target refuses a manual start so the unit's `Wants=` is what pulls it up;
    and one `hl.on("hyprland.start", function () ... end)` block containing exactly one call,
    `hl.exec_cmd("systemctl --user start hyprland-session.service")`.

    The call goes **inside** the handler function, matching the vendor file's own comment that
    former startup commands belong there — a startup command must fire once per session, not on
    every reload. Register against `hyprland.start`, the same event the vendor handler uses;
    both handlers coexist because the overlay is required after the vendor file, which is the
    entire purpose of the custom slot. That is D-16: the entry goes inside the overlay's own
    handler block and the vendor file stays untouched.

    Exactly one entry lands here (D-15). The six other startup entries lost at the adopt belong
    to START-01, which the requirements map to Phase 20 and which Phase 20 criterion 4 verifies.
    Keeping them out is also what keeps the workspace-pinning spelling question open for the
    experiment Phase 20 plans, rather than pre-deciding it here.

    Start only — never enable (D-17). The unit's own header says it is started, and `start`
    leaves it in state `linked`. Enabling would create wants-directory entries and move it to
    state `enabled`, which is exactly the state where the footgun Task 2 documents becomes
    reachable.

    Then perform the apply hop: `cp ./.config/hypr/custom/execs.lua ~/.config/hypr/custom/execs.lua`,
    followed by `cmp -s` on the two paths to confirm byte identity. This is a deliberate,
    operator-visible duplication — `stow/hypr/` does not exist until Phase 20, so the two copies
    must be hand-synced until Phase 18's FIX-03 and Phase 20's HYPR-01 close the window. Write
    nothing anywhere else under `~/.config/hypr/`; the vendor tree is untouched.

    Finally, prove the mechanism once, by hand, and then revert. Record
    `systemctl --user is-active graphical-session.target` before, run
    `systemctl --user start hyprland-session.service`, record the target as active and the unit
    still in state `linked`, then run `systemctl --user stop hyprland-session.service` and
    confirm both are inactive again. Quote all five observations in the plan summary. This one
    mutating check is run here and deliberately kept out of the assert script, which carries a
    non-mutating guarantee. Reverting matters: leaving the target hand-started would make the
    post-re-login check report a green it did not earn.
  </action>
  <acceptance_criteria>
    - `.config/hypr/custom/execs.lua` contains the literal `systemctl --user start hyprland-session.service` exactly once.
    - Its first line is the authoring-source-of-truth header comment, byte-identical to line 1 of `.config/hypr/custom/general.lua`.
    - The command call sits inside an `hl.on("hyprland.start", function ()` block that is closed by `end)`.
    - `cmp -s .config/hypr/custom/execs.lua "$HOME/.config/hypr/custom/execs.lua"` exits 0.
    - The file contains none of the strings `wl-clip-persist`, `google-chrome-stable`, `hyprpaper`, `vesktop`, `discord`, `btop` — those six entries belong to Phase 20.
    - `.config/hypr/custom/execs.lua` contains no `systemctl --user enable`.
    - `git status --porcelain` shows no modification under `vendor/`, and `git diff --name-only HEAD -- .config/hypr/` lists only `custom/execs.lua`.
    - At task end: `systemctl --user is-active graphical-session.target` prints `inactive`, and `systemctl --user is-enabled hyprland-session.service` prints `linked`.
  </acceptance_criteria>
  <verify>
    <automated>cmp -s .config/hypr/custom/execs.lua "$HOME/.config/hypr/custom/execs.lua" &amp;&amp; grep -Fq 'systemctl --user start hyprland-session.service' .config/hypr/custom/execs.lua &amp;&amp; echo SYNCED</automated>
    <fails_when>non-zero exit, or `SYNCED` is not printed — either the two copies diverged or the command literal is absent from the repo copy</fails_when>
    <automated>grep -cE 'wl-clip-persist|google-chrome-stable|hyprpaper|vesktop|systemctl --user enable' .config/hypr/custom/execs.lua || true</automated>
    <fails_when>the printed count is anything other than `0` — a Phase 20 entry or an enable call leaked into this phase's single-line overlay</fails_when>
    <automated>systemctl --user is-enabled hyprland-session.service 2>/dev/null || true</automated>
    <fails_when>the printed state is anything other than `linked` — `enabled` means the footgun was armed, `not-found` means the stow symlink is gone</fails_when>
  </verify>
  <done>The repo copy carries the one startup entry with its header, the live copy is byte-identical, the mechanism was proven once and reverted, and the unit is still `linked`.</done>
</task>

<task type="auto">
  <name>Task 2: Document the disable footgun, correct the stale playbook claims, and write the Phase 18 handoff</name>
  <files>docs/dots-hyprland-workflow.md, .planning/phases/17-unblock-stow-and-restore-the-session-target/17-HANDOFF.md</files>
  <read_first>
    - `docs/dots-hyprland-workflow.md` lines 318-350 — §7's expected-output block at lines 327-328 and the observed-counts sentence at line 331, §8 "Known losses after the full adopt" beginning at line 335, the session-bootstrap bullet at line 339, and the ownership sentence at line 344.
    - `docs/dots-hyprland-workflow.md` lines 18-19 and line 358 — the house blockquote shape and the recovery-subsection shape to imitate.
    - `scripts/phase14-verify.sh` lines 438-465 — the finding block whose count flips once the session target is restored, and the session-unit path check.
    - `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-RESEARCH.md` §F-12 and §F-3.
    - `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-CONTEXT.md` D-19 — the warning-block placement, its recovery command, and the requirement that `mask` be named as the safe alternative.
    - `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-03-SUMMARY.md` — the handoff row it recorded.
    - `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-01-SUMMARY.md` — the folded directories its `[INFO]` audit reported.
  </read_first>
  <action>
    Add a warning block to `docs/dots-hyprland-workflow.md` beside the session-bootstrap
    narrative in §8, using the file's own `> **Label:** …` blockquote shape. It must state four
    things: that `systemctl --user disable` removes *all* symlinks to a unit from
    `~/.config/systemd/user/`, including manually created ones, which is exactly what a stow
    link is; that the repo copy under `stow/systemd/` survives and only the link dies; the
    recovery, which is `stow --verbose=5 --no-folding -t ~ systemd` followed by
    `systemctl --user daemon-reload`; and that `systemctl --user mask` is the safe alternative
    for a stow-managed unit, with `systemctl --user stop` as the lighter option. Write the
    re-stow recovery as applying after any `disable`, `mask` or `unmask`, unconditionally —
    whether `mask` replaces an existing stow link was not probed, and an unconditional recovery
    instruction is correct either way. Also state that this unit is never enabled: it stays in
    state `linked` and is started from the custom overlay at session start.

    Correct the ownership sentence in §8 that currently says restoring the session bootstrap and
    the other startup entries is unowned work with no owning phase. It is owned now — this phase
    owns the session bootstrap under START-02, and Phase 20 owns the rest under START-01. The
    same sentence points at a Phase 15 sweep record under an unarchived path; Phase 15 now lives
    under the archived milestone directory, so fix the path while the sentence is being rewritten.

    Update §7's expected-output line and the observed-counts sentence that follow the phase-14
    verify command. That script emits its one allowed finding precisely because the session
    target is inactive; once the operator re-logs in and the target comes up, the script's
    informational branch fires instead and its finding count drops to zero. Leaving the playbook
    asserting the current count would make it wrong at the exact moment the phase succeeds.
    State both the pre-re-login and post-re-login expected outputs and say which condition
    selects which, rather than replacing one stale number with another.

    Record the pinned invocation form for `arch/hyprland.sh` next to the live-run instructions:
    an absolute path, `bash "$PWD/arch/hyprland.sh"` from the repo root, or `cd arch && ./hyprland.sh`.
    An operator who invokes it by a repo-root-relative path gets a red run at the second
    directory change that looks like the phase failed. Note that this is a documented invocation
    constraint, not a code fix — the directory-change idiom is kept verbatim per D-01.

    Then create `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-HANDOFF.md`
    as a short table of the rows this phase hands to Phase 18, each naming the row, the owning
    Phase 18 requirement, and why Phase 17 declined to act: the two folded directory symlinks
    from plan 17-01's audit, which the tree taxonomy must classify before they can be unfolded;
    the repo-root generated-theme file that remains tracked so its new ignore pattern governs
    future writes only, which is a redistribution decision under FIX-03; and the authoring copy
    of `custom/execs.lua`, which must appear in FIX-03's redistribution table as moving into
    `stow/hypr/` under HYPR-01. That third row is load-bearing — if it goes missing, the
    hand-sync window this phase opened is never closed.
  </action>
  <acceptance_criteria>
    - `docs/dots-hyprland-workflow.md` contains all three of the literals `systemctl --user disable`, `systemctl --user mask`, and `stow --verbose=5 --no-folding -t ~ systemd`.
    - It contains the literal `systemctl --user daemon-reload` within the recovery block.
    - It no longer contains the phrase `unowned work with no owning phase`, and no longer contains the path `.planning/phases/15-playbook-safe-vs-full/`.
    - It names both the pre-re-login and post-re-login expected finding counts for the phase-14 verify script, with the condition that selects each.
    - It contains one of the literals `bash "$PWD/arch/hyprland.sh"` or `cd arch && ./hyprland.sh`.
    - `17-HANDOFF.md` exists with exactly three rows, each naming a Phase 18 requirement id, and one of them names `custom/execs.lua`.
    - `bash -n` is not applicable, but the recovery command in the doc is byte-identical to the flag pair landed in plan 17-01: `grep -c -- '--verbose=5 --no-folding' docs/dots-hyprland-workflow.md` is at least 1.
  </acceptance_criteria>
  <verify>
    <automated>grep -Fq 'systemctl --user disable' docs/dots-hyprland-workflow.md &amp;&amp; grep -Fq 'systemctl --user mask' docs/dots-hyprland-workflow.md &amp;&amp; grep -Fq 'stow --verbose=5 --no-folding -t ~ systemd' docs/dots-hyprland-workflow.md &amp;&amp; echo DOC_OK</automated>
    <fails_when>non-zero exit, or `DOC_OK` is not printed — one of the three required literals is missing from the playbook</fails_when>
    <automated>grep -c 'unowned work with no owning phase' docs/dots-hyprland-workflow.md || true</automated>
    <fails_when>the printed count is anything other than `0` — the stale ownership claim survived</fails_when>
    <automated>test -f .planning/phases/17-unblock-stow-and-restore-the-session-target/17-HANDOFF.md &amp;&amp; grep -c 'execs.lua' .planning/phases/17-unblock-stow-and-restore-the-session-target/17-HANDOFF.md</automated>
    <fails_when>non-zero exit, or the printed count is `0` — the handoff row that closes this phase's hand-sync window is missing</fails_when>
  </verify>
  <done>The footgun, its recovery and its safe alternative are written down; the two stale playbook claims are corrected; the invocation form is pinned; and the three Phase 18 handoff rows are on disk.</done>
</task>

<task type="auto">
  <name>Task 3: Assert criteria 5 and 6 in the harness, with the inactive-target branch reported as INFO</name>
  <files>scripts/phase17-unblock-assert.sh</files>
  <read_first>
    - `scripts/phase17-unblock-assert.sh` — the harness and every section added so far.
    - `scripts/phase14-verify.sh` lines 438-465 — the prior-art probe shape for the session target and the session-unit path check. Reuse the probe, not the finding helper.
    - `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-PATTERNS.md` § CONTRACT DIVERGENCE — the reclassification of that condition from a finding to an informational line.
    - `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-CONTEXT.md` D-22 and D-23.
  </read_first>
  <action>
    Add criterion 5 sections to `scripts/phase17-unblock-assert.sh`.

    Section 5a: a fixed-string grep for `systemctl --user start hyprland-session.service` in the
    repo copy of the overlay, preceded by a guard asserting that file exists and is non-empty.
    This catches a partial edit that a byte-comparison alone would pass, because a comparison
    passes happily when both copies are equally wrong.

    Section 5b: `cmp -s` between the repo copy and the live copy, reported as pass or fail.
    Scope it to that one file. Do not widen it to a directory comparison — the live custom
    directory holds four files the repo copy does not, and a directory comparison would go red
    on a difference this phase does not own.

    Section 5c: assert the unit's state is `linked`. Compare the printed **string**, not the
    exit status: the command prints `linked` and exits 1 on this machine, so a status-based
    assertion reports the opposite of the truth, and under the script's `set -euo pipefail` an
    unguarded command substitution would abort the run outright. Capture it with a trailing
    `|| true` inside the substitution and compare the captured word.

    Section 5e: probe the session target with the same `is-active` invocation the phase-14
    script uses, redirecting output. On success emit `[PASS]`. On failure emit `[INFO]` — never
    `[FAIL]` — with a message naming the operator re-login as the required step. A failure here
    would be permanently red through no defect, because an agent cannot end the session.

    Make that informational message distinguish two different states, which costs two lines and
    is the difference between a pending operator step and a real regression: read
    `systemctl --user show hyprland-session.service -p ActiveEnterTimestamp` and say whether it
    is empty, meaning the unit has never started this boot and the re-login is simply still
    pending, or populated, meaning it started and stopped and something else is wrong. That probe
    is read-only.

    Do not add the mutating start-and-check to this script under any flag. It was run once by
    hand in Task 1 and re-proving it on every run costs the script its non-mutating guarantee.

    Add criterion 6 sections: fixed-string greps over the playbook for the disable footgun, for
    the recovery command including its flag pair, and for the safe alternative — each preceded by
    a guard asserting the playbook exists and is non-empty, so none of the three can pass over a
    missing file.

    Finally, attach the post-re-login verification as a human check on this task so it is
    harvested at end of phase rather than blocking the wave.
  </action>
  <acceptance_criteria>
    - The assert script emits `[PASS]` for 5a, 5b, 5c, 6a, 6b and 6c, and either `[PASS]` or `[INFO]` for 5e — never `[FAIL]` for 5e.
    - The 5c section compares the captured state word to `linked`; it does not branch on the exit status of `systemctl --user is-enabled`.
    - The 5e informational message names the operator re-login and reports whether the unit has started this boot.
    - The assert script contains no `systemctl --user start`, no `systemctl --user stop`, no `systemctl --user enable`, and no `systemctl --user disable`.
    - Emptying the repo overlay file makes 5a report `[FAIL]` rather than passing vacuously — confirm once by hand during the task and restore the file.
    - `./scripts/phase17-unblock-assert.sh` closes `=== done: FAIL=0 ===`.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase17-unblock-assert.sh</automated>
    <fails_when>non-zero exit, or the closing line is not `=== done: FAIL=0 ===`, or no line mentioning `graphical-session.target` appears in stdout</fails_when>
    <automated>grep -cE 'systemctl --user (start|stop|enable|disable)' scripts/phase17-unblock-assert.sh || true</automated>
    <fails_when>the printed count is anything other than `0` — a mutating systemd call entered the non-mutating assert script</fails_when>
    <automated>./scripts/phase14-verify.sh</automated>
    <fails_when>non-zero exit, or the closing line does not report `FAIL=0` — this wave touches the playbook that script cross-checks, and it asserts a clean working tree, so commit first</fails_when>
    <human-check>
      After the phase completes, log out of the Hyprland session and log back in. Then run
      `./scripts/phase17-unblock-assert.sh` and confirm the criterion 5e line has flipped from
      `[INFO]` to `[PASS]`, meaning `graphical-session.target` came up from the custom overlay
      at login rather than from a hand start. Then run `./scripts/phase14-verify.sh` and confirm
      its finding count has dropped to zero, which is the independent corroboration that the
      known loss is closed. If 5e is still `[INFO]` after a real re-login, that is a genuine
      regression, not a pending step.
    </human-check>
  </verify>
  <done>Criteria 5a-5c and 6a-6c are hard assertions, 5e reports informationally with a message that distinguishes pending from broken, and the script is still non-mutating.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hyprland session start → systemd user manager | the overlay issues a command to the user manager at every login |
| repo authoring copy → live applied copy | a hand-sync window where the two can silently diverge until Phase 20 closes it |
| operator command → stow symlink in the unit configuration directory | a single documented-elsewhere command deletes a link the repo owns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-05 | Tampering | `systemctl --user disable` against a stow-managed unit in state `linked` | medium | mitigate | the footgun, its recovery re-stow plus daemon-reload, and `mask` as the safe alternative are written into the operator playbook; the unit is never enabled, which keeps the state where the footgun bites out of reach (D-17, D-19) |
| T-17-16 | Tampering | divergence between the repo authoring copy and the live applied copy | medium | mitigate | byte-identity gate on every assert run, scoped to the one file; the duplication is recorded as a Phase 18 handoff row so it is closed rather than forgotten |
| T-17-17 | Spoofing | a hand-started session target reporting a green the re-login did not earn | medium | mitigate | the mechanism proof in Task 1 reverts the session to its as-found state, and the assert reports informationally rather than passing while the target is inactive |
| T-17-18 | Elevation of Privilege | `graphical-session.target` reachable by manual start | low | accept | the target refuses manual start by its own configuration; the only path is the unit's `Wants=` dependency, and starting the unit starts nothing else because the target's reverse dependencies propagate stop and restart but never start |
</threat_model>

<verification>
`./scripts/phase17-unblock-assert.sh` after every task commit, closing `=== done: FAIL=0 ===`.
At wave close also `./scripts/phase14-verify.sh`, which is coupled to this wave twice — it
emits its one allowed finding because the session target is inactive, and it asserts a clean
working tree, so commit first.
</verification>

<success_criteria>
- `systemctl --user is-active graphical-session.target` returns active after a fresh login, started from the custom overlay, and the repo copy of the overlay is byte-identical to the live one (ROADMAP criterion 5; sub-claims 5a-5d land here, 5e is the operator re-login).
- The `systemctl --user disable` footgun is written down in the operator docs with its recovery command (ROADMAP criterion 6).
- The three Phase 18 handoff rows exist on disk, including the one that closes this phase's hand-sync window.
</success_criteria>

## Artifacts this phase produces

New symbols created by this plan:

- The `hl.on("hyprland.start", ...)` handler block and its single `hl.exec_cmd` call in `.config/hypr/custom/execs.lua` — the file exists but is empty before this plan.
- `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-HANDOFF.md` — new file.
- The START-03 warning block, the pinned-invocation-form note, and the dual expected-output lines in `docs/dots-hyprland-workflow.md` — new content.
- Criterion 5 sections (5a, 5b, 5c, 5e) and criterion 6 sections (6a, 6b, 6c) inside `scripts/phase17-unblock-assert.sh`.

## Flagged planner assumptions

- **START-02 edge probe returned `unclassified`.** No boundary category was produced for a session-bootstrap requirement. The predicates above were authored from the five-way sub-claim decomposition in the research document — command present, copies identical, unit `linked`, mechanism proven, target active after re-login — rather than from a probe category. If an edge exists outside those five, this plan does not assert it.
- **START-03 edge probe returned `unclassified`.** The documented recovery is asserted as documentation presence plus mechanism review of the unit configuration directory layout. Whether `mask` on an already-stow-linked unit replaces that link was not probed; the warning block is written so the unconditional re-stow recovery is correct under either answer, but that specific behaviour remains unverified.

## Prohibition recall breadcrumbs

- Generic privilege-escalation and session-management canon is covered by `/gsd-secure-phase`; the three bespoke prohibitions minted above concern enabling the unit, faking the post-re-login green, and writing into the vendor tree.

<output>
Create `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-05-SUMMARY.md` when done.
Quote the five before/during/after systemd observations from Task 1 verbatim, record that the
session was returned to its as-found state, and list the three handoff rows.
</output>
