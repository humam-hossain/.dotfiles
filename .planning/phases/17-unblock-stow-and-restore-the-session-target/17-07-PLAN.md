---
phase: 17-unblock-stow-and-restore-the-session-target
plan: 07
type: execute
wave: 7
depends_on: ["17-06"]
files_modified:
  - .planning/phases/17-unblock-stow-and-restore-the-session-target/17-LIVE-RUN.md
autonomous: false
requirements: [FIX-02]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`arch/hyprland.sh` runs end-to-end against the live session and exits 0, using a pinned invocation form rather than a working-directory-relative one"
    - "After the run, `hyprctl -j status` still reports the Lua configuration provider — the installer did not revert the Phase 14 adopt"
    - "After the run, the live `~/.config/hypr/hyprland.conf` is still absent"
    - "After the run, the two stow links the script places are present and resolve into the repo"
    - "The run is captured in a terminal transcript with the configuration provider recorded immediately before and immediately after, because the run is not repeatable and the transcript is therefore the record"
    - "The durable privilege change the script makes — adding the operator to a hardware-access group — is surfaced to the operator before the run rather than discovered afterwards"
    - statement: "This run is one-way: it installs packages, refreshes the package database without a full upgrade, and widens group membership. There is no undo, only a forward full upgrade. A second run to re-observe anything is not available"
      verification: backstop
  artifacts:
    - path: ".planning/phases/17-unblock-stow-and-restore-the-session-target/17-LIVE-RUN.md"
      provides: "the one-way run's evidence record — pinned invocation form, exit status, before/after provider, post-run link state, and the transcript path"
      contains: "configProvider"
  key_links:
    - from: ".planning/phases/17-unblock-stow-and-restore-the-session-target/17-LIVE-RUN.md"
      to: "the terminal transcript produced by the run"
      via: "the transcript path recorded verbatim, because the observation cannot be reproduced"
      pattern: "transcript"
  prohibitions:
    - statement: "Must NOT run the installer before the stow flag fix has landed and been committed. Everything the script does before its first stow call is irreversible system mutation, so an early run pays the whole irreversible cost and then aborts on a flag the phase already knows is wrong"
      status: resolved
      verification: judgment
    - statement: "Must NOT re-run the installer to re-observe a result that was mis-captured. The run reinstalls packages and re-refreshes the package database; if the transcript is incomplete, record the gap honestly rather than paying the one-way cost a second time to produce a tidier record"
      status: resolved
      verification: judgment
---

<objective>
Prove criterion 2's live half: `arch/hyprland.sh` runs end-to-end, exits 0, and leaves the
session still driven by the Lua configuration provider — captured once, in a transcript,
because the run cannot be repeated.

Purpose: every static half of criterion 2 is already asserted by plan 17-06. What remains is
the one claim a grep cannot make — that the script actually executes. This is the phase's
terminal wave and its only one-way action.
Output: one evidence document and one terminal transcript.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-CONTEXT.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-RESEARCH.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-VALIDATION.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-06-SUMMARY.md
@docs/phase14-adopt-runbook.md
@arch/hyprland.sh
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Checkpoint: accept or defer the one-way live run</name>
  <decision>Accept the one-way cost of running `arch/hyprland.sh` against the live daily-driver session now, or defer criterion 2's live half</decision>
  <context>
    Every other criterion in this phase is closed. This is the last one, and it is the only
    action in the phase with no undo.

    Running the script does five things that cannot be reversed. It installs packages. It
    refreshes the package database five times with a partial-refresh form that has no upgrade
    flag, which on this distribution is the documented way to end up linking new packages
    against older libraries already on disk — recoverable only by a forward full upgrade, never
    by going back. It runs an equivalent refresh through the AUR helper for one more package. It
    adds the operator to a hardware-access group, which is a durable privilege change that
    survives logout. And it runs against the production desktop session, which is this machine's
    only one.

    The package-install and group-membership lines map to no Phase 17 requirement and were
    deliberately left unfixed; they are recorded as deferred work with no owning phase yet. This
    checkpoint is not asking you to re-decide that. It is asking whether now is the moment to
    pay the cost, because the phase records the door being walked through rather than walking
    through it unattended.
  </context>
  <options>
    <option id="run-now">
      <name>Run it now, under a transcript, after a full system upgrade</name>
      <pros>Criterion 2 closes in this phase as the roadmap intends. Running a full upgrade immediately beforehand makes the five partial refreshes a no-op, which neutralises the one hazard that has no bounded blast radius. Everything else in the phase is already committed and green, so a failure is attributable to this run alone.</pros>
      <cons>Packages are installed and group membership widens permanently. If something in the run breaks the session, recovery is the documented clean-reinstall route, not a rollback.</cons>
    </option>
    <option id="defer">
      <name>Defer the live run; close the phase with criterion 2's static half only</name>
      <pros>No irreversible action. The three static assertions already prove the destructive copy is gone, which is the part that threatened the milestone's capture trees.</pros>
      <cons>Criterion 2 is only partly satisfied and the phase cannot be recorded as fully verified. The claim that the installer runs at all stays untested, and the second defect the research found — the directory change evaluated from the wrong base — stays unproven in either direction. A later phase inherits an unrun installer.</cons>
    </option>
  </options>
  <resume-signal>Reply `run-now` or `defer`. If `defer`, the remaining tasks are skipped and the deferral is recorded against criterion 2 in the phase summary.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: confirm the six preconditions immediately before the run</name>
  <what-built>
    Nothing new — this gate confirms six preconditions are all true at this moment, immediately
    before the one-way run. They are checked together because the run is not repeatable and a
    missing precondition discovered mid-run cannot be fixed by starting over.
  </what-built>
  <how-to-verify>
    Confirm all six, in this order. Each is a command you can run now.

    1. **Clean working tree.** `git status --porcelain` prints nothing. The run's damage, if
       any, must be attributable — and for the parts that are file changes, revertible.
    2. **The stow flag fix is committed.** `grep -c -- '--verbose=5 --no-folding' arch/hyprland.sh`
       prints `2`, and `git log -1 --oneline -- arch/hyprland.sh` shows the phase's commit. Both
       of this script's stow calls are among the sites that were broken; an uncommitted fix means
       the run aborts after the package operations have already happened.
    3. **The phase assert is green.** `./scripts/phase17-unblock-assert.sh` closes
       `=== done: FAIL=0 ===`.
    4. **A full system upgrade has just completed.** Run `sudo pacman -Syu` and let it finish.
       This is what makes the script's five partial database refreshes a no-op instead of a
       partial-upgrade hazard. Do this immediately before, not yesterday.
    5. **A transcript is ready.** You will run the installer wrapped in `script`, writing to a
       file under this phase's directory. Decide the path now.
    6. **The current configuration provider is captured.** `hyprctl -j status | jq -r .configProvider`
       prints `lua`. Record the output; it is the before half of the phase's binding comparison.

    The pinned invocation form is an absolute path — `bash "$PWD/arch/hyprland.sh"` from the repo
    root, or `cd arch && ./hyprland.sh`. Do not invoke it by a repo-root-relative path; that form
    fails at the second directory change.

    One thing to know before you approve: the script adds your user to a hardware-access group.
    That is a permanent change to your group membership and it survives logout.
  </how-to-verify>
  <resume-signal>Type `approved` once all six are confirmed, or name the one that is not yet true.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Execute the transcripted run and record its evidence</name>
  <files>.planning/phases/17-unblock-stow-and-restore-the-session-target/17-LIVE-RUN.md</files>
  <reversibility rating="one-way">The run installs packages, refreshes the package database without an upgrade, and permanently widens the operator's group membership; the only forward path from a bad outcome is a full system upgrade, and there is no route back.</reversibility>
  <precondition>`git status --porcelain` is empty and `hyprctl -j status` reports the Lua configuration provider — the two facts the before/after comparison is measured against. If either is false, halt.</precondition>
  <read_first>
    - `arch/hyprland.sh` — all of it, as it stands after plan 17-06, so the expected stanza sequence in the transcript is known before the run rather than interpreted after.
    - `docs/phase14-adopt-runbook.md` — the transcript convention this repo already uses for operator-owned live runs, which D-04 requires for this run.
    - `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-CONTEXT.md` D-04 — the operator picks the moment, the run starts from a clean working tree, the configuration provider is captured before and after, and the partial-refresh hazard is recorded as a finding rather than fixed here.
    - `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-06-SUMMARY.md` — the checkpoint outcome that decided whether the directory-change expressions were pinned or hoisted, which determines which invocation forms are valid.
  </read_first>
  <action>
    Capture the before state first: `hyprctl -j status | jq -r .configProvider` and the presence
    or absence of `~/.config/hypr/hyprland.conf`. Record both verbatim.

    Then run the installer wrapped in `script`, writing the transcript to
    `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-CRITERION2-TRANSCRIPT.txt`,
    using the pinned invocation form — an absolute path from the repo root, or by changing into
    the installer directory first. Not a repo-root-relative path.

    Capture the exit status of the wrapped command. `script` reports the wrapped command's
    status when asked to; read it deliberately rather than inferring success from the transcript
    ending without a visible error, because the script's trace setting makes a transcript that
    ends mid-stanza look superficially similar to one that completed.

    Then capture the after state: the configuration provider again, the continued absence of the
    live `hyprland.conf`, and the two stow links the script places — assert each is a symbolic
    link that resolves into the repo working tree, not merely that a file of that name exists.

    Write `17-LIVE-RUN.md` recording, in order: the invocation form used, the before provider,
    the exit status, the after provider, the post-run link state, the transcript path, and the
    group-membership change as an explicit note. If anything went wrong, record what and stop —
    do not re-run the installer to produce a cleaner record. A second run pays the whole
    irreversible cost again and the second transcript would not be evidence of the first run.

    Finally re-run `./scripts/phase17-unblock-assert.sh` and the sibling suites and record their
    closing lines, so the phase gate is measured after the live run rather than before it.
  </action>
  <acceptance_criteria>
    - The transcript file exists at the recorded path and is non-empty.
    - The wrapped command's exit status is recorded in `17-LIVE-RUN.md` and is `0`.
    - `hyprctl -j status | jq -r .configProvider` prints `lua` after the run.
    - `~/.config/hypr/hyprland.conf` does not exist after the run.
    - `~/.config/systemd/user/hyprland-session.service` is a symbolic link resolving under the repo working tree, and the swaync package's links are likewise present.
    - `17-LIVE-RUN.md` records the before provider, the after provider, the exit status, the invocation form, the transcript path, and the group-membership note.
    - `./scripts/phase17-unblock-assert.sh` closes `=== done: FAIL=0 ===` after the run.
  </acceptance_criteria>
  <verify>
    <automated>hyprctl -j status | jq -r .configProvider</automated>
    <fails_when>the printed value is anything other than `lua` — the installer reverted the session to the pre-adopt configuration provider</fails_when>
    <automated>test ! -e "$HOME/.config/hypr/hyprland.conf" &amp;&amp; echo CONF_ABSENT</automated>
    <fails_when>non-zero exit, or `CONF_ABSENT` is not printed — a pre-adopt configuration file was restored into the live tree</fails_when>
    <automated>test -L "$HOME/.config/systemd/user/hyprland-session.service" &amp;&amp; readlink -f "$HOME/.config/systemd/user/hyprland-session.service"</automated>
    <fails_when>non-zero exit, or the resolved path does not lie under the repo working tree — the session unit stopped being a stow link into the repo</fails_when>
    <automated>./scripts/phase17-unblock-assert.sh</automated>
    <fails_when>non-zero exit, or the closing line is not `=== done: FAIL=0 ===`</fails_when>
    <human-check>
      Read the transcript at
      `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-CRITERION2-TRANSCRIPT.txt`
      end to end and confirm two things a status code cannot tell you. First, that every stanza
      the script contains appears in the transcript in order and none was skipped. Second, that
      nothing between the first package-database refresh and the end of the run mentions a
      library version mismatch or an unresolvable dependency — that is the signature of the
      partial-upgrade hazard landing, and it is the one failure mode that would not show up as a
      non-zero exit. Also confirm your desktop session still behaves normally: the bar is
      running, notifications work, and screen sharing offers sources.
    </human-check>
  </verify>
  <done>The installer ran end-to-end to exit 0 under a transcript, the session is still driven by the Lua provider, the stow links resolve into the repo, and the evidence record names everything the run cost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installer → elevated privilege | five package operations and one group-membership change run with elevated privilege |
| package database refresh → installed library set | a refresh without an upgrade lets a newly installed package link against older libraries already present |
| installer → live desktop session | the run mutates the production session this machine depends on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-06 | Tampering | partial package-database refresh in `arch/hyprland.sh` | medium | accept | maps to no Phase 17 requirement and is recorded as deferred; the checkpoint requires a full system upgrade immediately before the run, which makes the five refreshes a no-op; the human check reads the transcript for the mismatch signature that a zero exit status would hide |
| T-17-04 | Elevation of Privilege | hardware-access group membership widened for the operator | medium | accept | pre-existing and unowned by any Phase 17 requirement; surfaced explicitly in the blocking checkpoint before approval and recorded as a note in the evidence document, so the operator knows their groups change rather than discovering it later |
| T-17-21 | Tampering | the installer reverting the session configuration provider | high | mitigate | the destructive restore was deleted in plan 17-06 and its absence is asserted statically; this plan adds the direct observation — the provider is read immediately before and immediately after, and the live pre-adopt configuration file is asserted still absent |
| T-17-22 | Tampering | the run replacing the session unit stow link with a plain file | medium | mitigate | the post-run assertion checks link-ness and resolved target rather than mere file presence, which is the only form of the check that can detect a symlink being overwritten through |
</threat_model>

<verification>
`./scripts/phase17-unblock-assert.sh` after the run, closing `=== done: FAIL=0 ===`, plus the
full phase gate: `./scripts/phase16-retire-assert.sh`, `./scripts/phase13-d19-assert.sh`,
`./scripts/phase12-full-smoke.sh`, `./scripts/phase11-dispositions-assert.sh`,
`./scripts/phase10-inventory-assert.sh` and `./scripts/phase14-verify.sh` — each on a clean
tree, with its closing line quoted verbatim into the phase summary. All six filenames were
resolved against `scripts/` at planning time and exist. The phase-14 script is expected to
report one finding until the operator re-login lands, and zero afterwards.
</verification>

<success_criteria>
- Running `arch/hyprland.sh` end-to-end exits 0 and leaves `hyprctl -j status` still reporting the Lua configuration provider (ROADMAP criterion 2, live half).
- The evidence is a transcript plus a record document, because the observation is not repeatable.
- The full pre-existing assert suite is still green after the live run.
</success_criteria>

## Artifacts this phase produces

New symbols created by this plan:

- `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-LIVE-RUN.md` — new file.
- `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-CRITERION2-TRANSCRIPT.txt` — new file.

## Flagged planner assumptions

- **The transcript path and the exact `script` invocation are decided at the checkpoint, not here.** The plan pins the transcript's destination directory and the requirement that the wrapped command's exit status be read deliberately; the precise flag spelling for reporting that status is resolved by the executor against the installed `script` at run time, because a wrong spelling would silently substitute the wrapper's status for the installer's.

## Prohibition recall breadcrumbs

- Privilege-escalation canon is covered by `/gsd-secure-phase` and by the register row above; the two bespoke prohibitions minted here concern ordering (never before the flag fix lands) and non-repeatability (never re-run to tidy the record).

<output>
Create `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-07-SUMMARY.md` when done.
Quote the before and after configuration provider verbatim, the exit status, the closing line of
every assert script in the phase gate, and — if the checkpoint answered `defer` — record criterion
2's live half as explicitly deferred with the static half noted as satisfied.
</output>
