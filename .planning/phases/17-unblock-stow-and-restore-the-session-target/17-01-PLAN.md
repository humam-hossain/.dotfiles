---
phase: 17-unblock-stow-and-restore-the-session-target
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase17-unblock-assert.sh
  - arch/btop.sh
  - arch/alacritty.sh
  - arch/define.sh
  - arch/fish.sh
  - arch/hyprland.sh
  - arch/kitty.sh
  - arch/nvim.sh
  - arch/rofi.sh
  - arch/tmux.sh
  - arch/wezterm.sh
  - arch/xterm.sh
  - arch/yazi.sh
  - arch/zsh.sh
  - arch/zsh_powerlevel.sh
  - docs/phase14-adopt-runbook.md
autonomous: true
requirements: [FIX-01, CAP-04]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every one of the 15 stow call sites under arch/ carries the literal `--verbose=5 --no-folding`, and a repo-wide count of that literal across arch/*.sh equals exactly 15"
    - "The invalid short-verbosity spelling survives at zero sites under arch/ and zero sites under docs/"
    - "`bash -n` exits 0 on all 14 arch/*.sh files that hold a stow call site"
    - "A real GNU Stow 2.4.1 simulate run with the new flag pair exits 0 against an already-stowed package, proving the spelling is accepted by the installed stow and not merely by a grep"
    - "The flag pair is written in a single fixed order (`--verbose=5` then `--no-folding`) at all 15 arch/ sites and the one docs/ site, so a single literal count is a complete audit"
    - "A stow destination directory that already exists as a real directory is left a real directory by a run carrying `--no-folding`; the two pre-existing folded directory symlinks are enumerated as `[INFO]` and handed to Phase 18 rather than unfolded here (D-02)"
    - statement: "A stow invocation with no package argument, or over a package directory containing no files, is not exercised by any call site in this repo — every one of the 16 invocations names exactly one existing package"
      verification: backstop
    - statement: "GNU Stow is not transactional; an interrupted run leaves a partially-linked tree, and nothing in this phase adds a rollback for that. Recovery is a re-run of the same command, which is idempotent"
      verification: backstop
    - statement: "CAP-04's scope phrase `every stow invocation in the repo` is satisfied at the arch/ and docs/ trees only; the copy inside the frozen planning artifact .planning/research/PITFALLS.md is deliberately left unedited under the Phase 16 frozen-record precedent"
      verification: backstop
  artifacts:
    - path: "scripts/phase17-unblock-assert.sh"
      provides: "Phase 17 contract assert harness — D-20 three-prefix contract, one FAIL counter, criterion sections 1a-1e plus the D-02 folding audit"
      contains: "=== done: FAIL="
    - path: "arch/btop.sh"
      provides: "the tracer call site — first site converted to the valid flag pair"
      contains: "--verbose=5 --no-folding"
    - path: "docs/phase14-adopt-runbook.md"
      provides: "the 16th invocation (the documented kitty re-stow recovery command), corrected while preserving -R"
      contains: "--verbose=5 --no-folding"
  key_links:
    - from: "scripts/phase17-unblock-assert.sh"
      to: "arch/*.sh"
      via: "counted grep of the literal flag pair, plus a ban grep scoped to arch/ and docs/ only"
      pattern: "--verbose=5 --no-folding"
    - from: "scripts/phase17-unblock-assert.sh"
      to: "GNU Stow 2.4.1"
      via: "a real non-mutating simulate run of the btop package with the new flag pair"
      pattern: "stow --verbose=5 --no-folding -n"
  prohibitions:
    - statement: "Must NOT introduce `stow --adopt` at any call site while editing all 15 of them — adopt replaces repo content with live content and exits 0, silently destroying authored configuration with no error to notice"
      status: resolved
      verification: judgment
    - statement: "Must NOT edit any frozen .planning/ research or milestone artifact to make a ban-grep pass — a green gate obtained by rewriting the historical record is a false green, and the Phase 16 precedent treats those files as history"
      status: resolved
      verification: judgment
---

<objective>
Land the phase's verification harness and the FIX-01 / CAP-04 flag sweep in one pass:
every `stow` invocation in `arch/` and `docs/` moves from the invalid short-verbosity
spelling to `--verbose=5 --no-folding`, and `scripts/phase17-unblock-assert.sh` is created
with the D-20 contract and grows its criterion-1 sections alongside the edits.

Purpose: `arch/hyprland.sh` holds two of the 15 broken call sites, and everything before
them in that file is irreversible system mutation. Criterion 2's live run cannot happen
until this lands. Phase 18 also depends on `--no-folding` being universal before any tree
is stowed.
Output: a new assert script, 14 edited installer scripts, one edited operator doc.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-CONTEXT.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-RESEARCH.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-PATTERNS.md
@.planning/phases/17-unblock-stow-and-restore-the-session-target/17-VALIDATION.md
@scripts/phase16-retire-assert.sh
@scripts/phase14-verify.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a defect is fixed and the harness proves it" — one call site only</name>
  <files>scripts/phase17-unblock-assert.sh, arch/btop.sh</files>
  <read_first>
    - `scripts/phase16-retire-assert.sh` — the D-20 harness model: header + non-mutating note (lines 1-19), preamble and `REPO_ROOT`/`FAIL`/`pass()`/`fail()` (lines 21-28), banner (line 43), syntax-check shape (lines 45-50), vacuity-guarded ban-grep (lines 251-264), scope-limited extraction grep (lines 286-302), tail (lines 303-307).
    - `scripts/phase14-verify.sh` line 41 only — the single `info()` helper line D-20's third prefix needs. Take nothing else from that file.
    - `arch/btop.sh` — the call site being converted; line 8 is the canonical one-liner shared by all 15 sites.
    - `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-PATTERNS.md` § "CONTRACT DIVERGENCE — do not mix the two assert contracts".
  </read_first>
  <action>
    Create `scripts/phase17-unblock-assert.sh`, executable, with the D-20 harness copied
    structurally from `scripts/phase16-retire-assert.sh`: `#!/usr/bin/env bash`, a header
    comment block naming Phase 17 and D-20, a non-mutating constraint note stating that the
    script runs only greps, `bash -n`, stow simulate runs and a sourced-subshell fixture and
    never runs a live install, a live uninstall or any package operation, then
    `set -euo pipefail`, `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"`,
    `cd "$REPO_ROOT"`, `FAIL=0`, and the three helpers `pass`, `fail`, `info` printing
    `[PASS] %s\n`, `[FAIL] %s\n`, `[INFO] %s\n` with `fail` incrementing `FAIL`. Banner:
    `=== Phase 17 unblock contract (non-mutating) ===`. Tail: `echo "=== done: FAIL=${FAIL} ==="`,
    then `if [[ "$FAIL" -gt 0 ]]; then exit 1; fi`, then `exit 0`.

    Exactly three prefix helpers and exactly one counter. Do not copy the fourth prefix helper
    or the second counter from `scripts/phase14-verify.sh`; the closing line carries one
    counter only. D-20 governs this and the CONTEXT `<code_context>` description of the other
    script's contract does not apply here.

    Add one criterion section, headed by a comment naming criterion 1e / FIX-01: a behavioral,
    non-mutating proof that the installed GNU Stow accepts the new spelling. Run
    `( cd "$REPO_ROOT/stow" && stow --verbose=5 --no-folding -n -t ~ btop )` with stdout and
    stderr discarded, inside an `if ... then pass ... else fail ... fi`, so the expected
    non-zero case cannot trip `set -e`. On the failure branch re-run the same command without
    discarding output, suffixed `|| true`, so the `[FAIL]` prints the evidence that produced
    it — that evidence-printing pairing is house style at every `fail()` in the model script.

    Then convert the single call site at `arch/btop.sh` line 8 to
    `cd "$(dirname "${BASH_SOURCE[0]}")/../stow" && stow --verbose=5 --no-folding -t ~ btop`.
    Flags only: keep the `cd "$(dirname "${BASH_SOURCE[0]}")/../stow" &&` prefix verbatim per
    D-01, keep `-t ~`, keep the package name, add no `-d`, no `--restow`, and no `--adopt`.
    Touch no other call site in this task.
  </action>
  <acceptance_criteria>
    - `scripts/phase17-unblock-assert.sh` exists, is executable (`test -x`), and its first line is `#!/usr/bin/env bash`.
    - The file defines exactly three helper functions named `pass`, `fail`, `info`, and `grep -c 'FINDINGS' scripts/phase17-unblock-assert.sh` returns `0`.
    - The file's closing echo emits the literal `=== done: FAIL=` followed by the counter and ` ===`.
    - `arch/btop.sh` contains the literal `--verbose=5 --no-folding` exactly once and no longer contains the invalid short-verbosity spelling.
    - `bash -n arch/btop.sh` exits 0.
    - Running the script prints exactly one `[PASS]` line and zero `[FAIL]` lines, and exits 0.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase17-unblock-assert.sh</automated>
    <fails_when>non-zero exit, or the closing line is not `=== done: FAIL=0 ===`, or no `[PASS]` line mentioning the stow simulate run appears in stdout</fails_when>
    <automated>bash -n arch/btop.sh &amp;&amp; grep -c -- '--verbose=5 --no-folding' arch/btop.sh</automated>
    <fails_when>non-zero exit, or the printed count is not `1`</fails_when>
    <automated>grep -c 'FINDINGS' scripts/phase17-unblock-assert.sh || true</automated>
    <fails_when>the printed count is anything other than `0` — a second counter means the phase-14 contract leaked into the D-20 script</fails_when>
  </verify>
  <done>The harness exists with the D-20 contract, one real GNU Stow simulate run passes through it end to end, one call site is converted, and the script exits 0. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the remaining 14 stow call sites and the documented recovery command</name>
  <files>arch/alacritty.sh, arch/define.sh, arch/fish.sh, arch/hyprland.sh, arch/kitty.sh, arch/nvim.sh, arch/rofi.sh, arch/tmux.sh, arch/wezterm.sh, arch/xterm.sh, arch/yazi.sh, arch/zsh.sh, arch/zsh_powerlevel.sh, docs/phase14-adopt-runbook.md, scripts/phase17-unblock-assert.sh</files>
  <read_first>
    - `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-RESEARCH.md` §F-1 — the 15 sites quoted verbatim with file and line, and §F-7 — the 16th invocation outside `arch/`.
    - `arch/alacritty.sh`, `arch/define.sh`, `arch/fish.sh`, `arch/hyprland.sh`, `arch/kitty.sh`, `arch/nvim.sh`, `arch/rofi.sh`, `arch/tmux.sh`, `arch/wezterm.sh`, `arch/xterm.sh`, `arch/yazi.sh`, `arch/zsh.sh`, `arch/zsh_powerlevel.sh` — the files holding the remaining 14 sites.
    - `docs/phase14-adopt-runbook.md` around line 247 — the documented kitty re-stow recovery command.
    - `scripts/phase17-unblock-assert.sh` — created in Task 1.
  </read_first>
  <action>
    <!-- planner-discipline-allow: -v=5 -->
    Apply the same flags-only conversion to the remaining 14 call sites. The invalid spelling
    `-v=5` is replaced by `--verbose=5 --no-folding` in that order at every site; everything
    else on the line — the `cd "$(dirname "${BASH_SOURCE[0]}")/../stow" &&` prefix, `-t ~`,
    and the package name — stays byte-identical (D-01). The sites are: `arch/alacritty.sh:9`
    (alacritty), `arch/define.sh:7` (define), `arch/fish.sh:26` (fish), `arch/hyprland.sh:29`
    (systemd), `arch/hyprland.sh:33` (swaync), `arch/kitty.sh:10` (kitty), `arch/nvim.sh:20`
    (nvim), `arch/rofi.sh:10` (rofi), `arch/tmux.sh:17` (tmux), `arch/wezterm.sh:10` (wezterm),
    `arch/xterm.sh:10` (xterm), `arch/yazi.sh:10` (yazi), `arch/zsh.sh:43` (zsh), and
    `arch/zsh_powerlevel.sh:61` (zsh).

    Decompose by call site, never by package: `arch/zsh.sh` and `arch/zsh_powerlevel.sh` both
    stow the package named `zsh`, and `arch/hyprland.sh` holds two sites. A one-edit-per-package
    pass silently misses two sites.

    Then fix the 16th invocation at `docs/phase14-adopt-runbook.md` line 247, the documented
    kitty re-stow recovery command. Preserve its `-R` restow flag and its `cd stow` prefix —
    that is a documentation instruction and the doc already states packages live under `stow/`.
    The corrected line reads `cd stow && stow -R --verbose=5 --no-folding -t ~ kitty`. Do not
    touch the identical string inside `.planning/research/PITFALLS.md`; planning artifacts are
    frozen history under the Phase 16 precedent, which is why the assert grep below is scoped
    to `arch/` and `docs/` by explicit path list rather than recursing from the repo root.

    Add three sections to `scripts/phase17-unblock-assert.sh`, each headed by a comment naming
    its criterion. Criterion 1a: a ban grep for the invalid spelling scoped to the explicit path
    list `arch/ docs/`, preceded by a vacuity guard asserting both directories exist and hold at
    least one `.sh` / `.md` file so the ban cannot pass over nothing. Criterion 1b: a counted
    grep asserting `grep -ho -- '--verbose=5 --no-folding' arch/*.sh | wc -l` equals exactly 15.
    Criterion 1d: the same ban grep scoped to `docs/` alone, so the operator-doc scope is a
    named claim rather than a side effect of 1a. Every failure branch re-prints the offending
    grep output suffixed `|| true`.
  </action>
  <acceptance_criteria>
    - `grep -rn -- '-v=5' arch/ docs/` finds no match (exit 1). <!-- planner-discipline-allow: -v=5 -->
    - `grep -ho -- '--verbose=5 --no-folding' arch/*.sh | wc -l` prints exactly `15`.
    - `grep -c -- '--verbose=5 --no-folding' arch/hyprland.sh` prints exactly `2`.
    - `docs/phase14-adopt-runbook.md` contains the literal `stow -R --verbose=5 --no-folding -t ~ kitty`.
    - `.planning/research/PITFALLS.md` is unmodified — `git diff --name-only HEAD -- .planning/research/PITFALLS.md` prints nothing.
    - `grep -c -- '--adopt' arch/*.sh docs/phase14-adopt-runbook.md` finds no match.
    - The assert script prints `[PASS]` for criteria 1a, 1b and 1d and exits 0.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase17-unblock-assert.sh</automated>
    <fails_when>non-zero exit, or the closing line is not `=== done: FAIL=0 ===`, or fewer than four `[PASS]` lines appear in stdout</fails_when>
    <automated>grep -ho -- '--verbose=5 --no-folding' arch/*.sh | wc -l</automated>
    <fails_when>the printed count is anything other than `15`</fails_when>
    <automated>git diff --name-only HEAD -- .planning/research/PITFALLS.md</automated>
    <fails_when>any path is printed — a frozen planning artifact was edited to satisfy a grep</fails_when>
  </verify>
  <done>All 15 arch/ sites and the one docs/ site carry the valid flag pair in a fixed order, the frozen planning copy is untouched, and the assert script proves all three claims.</done>
</task>

<task type="auto">
  <name>Task 3: Syntax gate over the 14 files, folding audit, and the sibling-suite regression run</name>
  <files>scripts/phase17-unblock-assert.sh</files>
  <read_first>
    - `scripts/phase17-unblock-assert.sh` — the sections added in Tasks 1 and 2.
    - `scripts/phase16-retire-assert.sh` lines 45-50 — the `bash -n` section shape to loop.
    - `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-RESEARCH.md` §F-10 — the folding audit already run, and the two directory symlinks it found.
    - `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-CONTEXT.md` D-02 — audit and record only, hand the list to Phase 18.
  </read_first>
  <action>
    Add a criterion 1c section to `scripts/phase17-unblock-assert.sh`: loop `bash -n` over the
    14 files that hold a stow call site — `arch/alacritty.sh`, `arch/btop.sh`, `arch/define.sh`,
    `arch/fish.sh`, `arch/hyprland.sh`, `arch/kitty.sh`, `arch/nvim.sh`, `arch/rofi.sh`,
    `arch/tmux.sh`, `arch/wezterm.sh`, `arch/xterm.sh`, `arch/yazi.sh`, `arch/zsh.sh`,
    `arch/zsh_powerlevel.sh` — emitting one `pass`/`fail` per file, and precede the loop with a
    guard asserting all 14 paths exist so a renamed file cannot make the loop pass vacuously.

    Add a D-02 folding-audit section that is read-only and reports `[INFO]` only, never
    `[PASS]`/`[FAIL]`: run `find "$HOME/.config" -maxdepth 2 -type l -lname '*.dotfiles*'`,
    test each result with `-d`, and emit one `[INFO]` line per result that resolves to a
    directory, plus one summary `[INFO]` line naming Phase 18 as the owner of what each folded
    directory becomes. Two such directories are expected today — `~/.config/smartmontools` and
    `~/.config/qBittorrent` — and neither is stowed by any `arch/*.sh` script, which is why they
    folded. This phase does not unfold them; `--no-folding` governs new runs only (D-02).

    Then run the sibling suite this wave could disturb and record the result in the plan
    summary: `./scripts/phase16-retire-assert.sh` must still close `FAIL=0`. It runs
    `bash -n arch/dots-hyprland.sh` and captures dry-run argv, neither of which this wave
    changes, so a red result means a real regression rather than an expected re-pin.
  </action>
  <acceptance_criteria>
    - The assert script emits 14 `[PASS]` lines from the `bash -n` loop, one per file, and zero `[FAIL]`.
    - The assert script emits at least one `[INFO]` line naming a folded directory symlink and one naming Phase 18 as its owner.
    - The folding-audit section performs no write: the section contains no `stow ` invocation without `-n`, no `rm`, no `mv`, and no `ln`.
    - `./scripts/phase16-retire-assert.sh` closes `=== done: FAIL=0 ===`.
    - `./scripts/phase17-unblock-assert.sh` closes `=== done: FAIL=0 ===`.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase17-unblock-assert.sh</automated>
    <fails_when>non-zero exit, or the closing line is not `=== done: FAIL=0 ===`, or fewer than 14 `[PASS]` lines mentioning `bash -n` appear in stdout</fails_when>
    <automated>./scripts/phase16-retire-assert.sh</automated>
    <fails_when>non-zero exit, or the closing line is not `=== done: FAIL=0 ===`</fails_when>
    <automated>./scripts/phase17-unblock-assert.sh | grep -c '^\[INFO\]'</automated>
    <fails_when>the printed count is `0` — the D-02 folding audit produced no record to hand to Phase 18</fails_when>
  </verify>
  <done>The syntax gate covers all 14 files, the folding audit is recorded as live `[INFO]` output rather than a frozen note, and the Phase 16 suite is still green.</done>
</task>

</tasks>

<threat_model>
**ASVS level: L1**, blocking on `high`. Dispositions below are assigned at OWASP ASVS
Level 1 under the project's configured `security_block_on: high`: no row rated `high` or
`critical` may be dispositioned `accept` — every such row carries a `mitigate` with a named
implementation. `accept` is available only to `medium` and `low` rows, each with a written
rationale.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → `$HOME/.config` | `stow` creates symlinks from the repo into the operator's live config tree; a folded directory would invert this and let external writers reach the working tree |
| operator doc → operator shell | `docs/phase14-adopt-runbook.md` holds copy-pasteable commands executed with the operator's privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-08 | Tampering | stow destination directories under `~/.config` | medium | mitigate | `--no-folding` at all 16 invocations so no destination directory becomes a symlink into the working tree; the two pre-existing folded directories are enumerated as `[INFO]` and handed to Phase 18 (D-02) |
| T-17-09 | Tampering | `stow --adopt` in any `arch/` script | medium | mitigate | `--adopt` is verified absent from all 15 sites today and the acceptance criteria re-assert its absence after the sweep; adopt would replace repo content with live content at exit 0 |
| T-17-10 | Tampering | frozen `.planning/` artifacts | low | mitigate | the ban grep is scoped by explicit path list to `arch/ docs/`; an acceptance criterion asserts `.planning/research/PITFALLS.md` is unmodified |
</threat_model>

<verification>
Run `./scripts/phase17-unblock-assert.sh` after every task commit; it must close
`=== done: FAIL=0 ===`. At wave close also run `./scripts/phase16-retire-assert.sh`,
which must close `=== done: FAIL=0 ===`.
</verification>

<success_criteria>
- No `stow` call site in `arch/` carries the invalid short-verbosity spelling; all 15 sites carry both `--verbose=5` and `--no-folding`; `bash -n` passes on every one of the 14 files (ROADMAP criterion 1).
- The 16th invocation in `docs/phase14-adopt-runbook.md` carries the same flag pair with `-R` preserved (CAP-04).
- `scripts/phase17-unblock-assert.sh` exists with the D-20 three-prefix, one-counter contract and covers criteria 1a-1e plus the D-02 folding audit.
</success_criteria>

## Artifacts this phase produces

New symbols created by this plan (excluded from drift verification because they do not exist before it runs):

- `scripts/phase17-unblock-assert.sh` — new script.
- `pass()`, `fail()`, `info()` — new shell functions defined inside that new script.
- The literal flag pair `--verbose=5 --no-folding` — new at 15 `arch/` call sites and one `docs/` site.
- `REPO_ROOT` and `FAIL` — new globals inside the new script (they shadow nothing; each assert script defines its own).

## Flagged planner assumptions

None in this plan. The three backstop truths above are recorded probe outcomes, not assumptions.

## Prohibition recall breadcrumbs

- Path-traversal and symlink-escape prohibitions are canon security — covered by `/gsd-secure-phase` and the STRIDE register in plan `17-02`; not minted here.

<output>
Create `.planning/phases/17-unblock-stow-and-restore-the-session-target/17-01-SUMMARY.md` when done.
Quote the closing line of both assert scripts verbatim, and list the folded directories the
`[INFO]` audit reported so Phase 18 inherits them.
</output>
