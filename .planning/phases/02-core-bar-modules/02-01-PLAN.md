---
phase: 02-core-bar-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase02-config-assert.py
  - .planning/phases/02-core-bar-modules/02-VALIDATION.md
autonomous: true
requirements:
  - BAR-01
  - BAR-02
  - BAR-03
  - BAR-04
must_haves:
  truths:
    - Wave 0 live-config assert script exists and is runnable without network
    - Assert script covers workspaces shown/showAppIcons/monochromeIcons, weather.enable, time.format/secondPrecision, tray.monochromeIcons/invertPinnedItems/pinnedItems
    - VALIDATION.md Wave 0 checkbox for the assert script is marked complete and task IDs reference this plan
  artifacts:
    - path: scripts/phase02-config-assert.py
      provides: "Live illogical-impulse config.json key asserts for Phase 2"
      min_lines: 20
  key_links:
    - from: scripts/phase02-config-assert.py
      to: ~/.config/illogical-impulse/config.json
      via: "python Path.home() read + assert on bar/time/tray keys"
      pattern: "illogical-impulse/config.json"
  prohibitions:
    - Do not enable weather in the assert expectations
    - Do not introduce a new test framework (pytest/jest) for this phase
    - Do not modify QML sources in this plan
---

<objective>
Introduce Wave 0 Nyquist validation harness for Phase 2: a reusable live-config assert script plus VALIDATION.md wiring so later plans can dual-write Config defaults and prove BAR-01..04 config keys without manual JSON dumps.

Purpose: Phase 1 taught that persisted `~/.config/illogical-impulse/config.json` wins over `Config.qml` defaults — every config change plan needs a one-command assert.
Output: `scripts/phase02-config-assert.py` + updated `02-VALIDATION.md` Wave 0 / task map.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-bar-modules/02-CONTEXT.md
@.planning/phases/02-core-bar-modules/02-RESEARCH.md
@.planning/phases/02-core-bar-modules/02-VALIDATION.md
@.planning/phases/01-shell-foundation-theme/01-LEARNINGS.md
</context>

<tasks>

<task type="auto" id="02-01-01">
  <name>Task 1: Create phase02 live config assert script</name>
  <files>scripts/phase02-config-assert.py</files>
  <read_first>
    - .planning/phases/02-core-bar-modules/02-VALIDATION.md (Wave 0 suggested python assert block)
    - .planning/phases/02-core-bar-modules/02-RESEARCH.md (Validation Architecture + dual-write pitfall)
    - scripts/ (existing shell script style)
  </read_first>
  <action>
    Create `scripts/phase02-config-assert.py` as a standalone Python 3 script (stdlib only: json, pathlib, sys).

    Behavior:
    1. Load `Path.home() / ".config/illogical-impulse/config.json"`. On missing file, print a clear error to stderr and exit non-zero.
    2. Assert the following live keys (match VALIDATION.md Wave 0 snippet and CONTEXT D-01..D-03, D-05, D-13, D-14, D-16):
       - bar.workspaces.shown == 10 (D-02)
       - bar.workspaces.showAppIcons is True (D-01)
       - bar.workspaces.monochromeIcons is True (D-03)
       - bar.weather.enable is False (D-16)
       - time.secondPrecision is True (D-05)
       - time.format contains the substring "ss" AND contains "AP" or "ap" (D-05 seconds + AM/PM)
       - tray.monochromeIcons is False (D-13 full-color)
       - tray.invertPinnedItems is True (D-14)
       - tray.pinnedItems contains the string "Fcitx" (D-14)
    3. On success print exactly one line: `config asserts OK` and exit 0. On failure raise AssertionError (or print failure and exit 1).
    4. Make the file executable (`chmod +x`) and use a shebang `#!/usr/bin/env python3`.
    5. Do not soft-pass when keys are still pre-Phase-2 values — this script is expected to FAIL until plan 02-02 dual-writes config. That red state is intentional for Wave 0.

    Do not edit QML, Network.qml, or install packages.
  </action>
  <acceptance_criteria>
    - File `scripts/phase02-config-assert.py` exists and starts with `#!/usr/bin/env python3`
    - Script imports only stdlib modules (json, pathlib, sys at most)
    - Running `python3 scripts/phase02-config-assert.py` exits 0 only when all asserts pass; currently live host values (format hh:mm, secondPrecision false, tray monochrome true) must cause non-zero exit until plan 02-02
    - Source contains the path segment `illogical-impulse/config.json`
    - Source asserts `shown` == 10, `showAppIcons`, workspace monochrome true, weather enable false, secondPrecision true, tray monochrome false, invertPinnedItems true, and Fcitx pin membership
  </acceptance_criteria>
  <verify>
    <automated>test -f scripts/phase02-config-assert.py &amp;&amp; python3 -m py_compile scripts/phase02-config-assert.py &amp;&amp; (python3 scripts/phase02-config-assert.py; echo EXIT:$?)</automated>
  </verify>
  <done>Wave 0 assert script exists, compiles, and encodes all Phase 2 live-config predicates.</done>
</task>

<task type="auto" id="02-01-02">
  <name>Task 2: Wire Wave 0 into VALIDATION.md task map</name>
  <files>.planning/phases/02-core-bar-modules/02-VALIDATION.md</files>
  <read_first>
    - .planning/phases/02-core-bar-modules/02-VALIDATION.md
    - scripts/phase02-config-assert.py
  </read_first>
  <action>
    Update `02-VALIDATION.md` only:

    1. Mark Wave 0 requirement for live config assert as done (checkbox true) and point the command at `python3 scripts/phase02-config-assert.py`.
    2. Replace TBD task IDs in the Per-Task Verification Map with concrete IDs from this phase plan set:
       - 02-01-01 / 02-01-02 → Wave 0 harness
       - 02-02-* → config dual-write (time/tray/workspaces)
       - 02-03-* → left/center layout
       - 02-04-* → right layout + indicators
       - 02-05-* → static + smoke gates
    3. Set `wave_0_complete: true` in YAML frontmatter when the script path is documented. Leave `nyquist_compliant: false` until plan 02-05 proves green end-to-end (do not claim full compliance here).
    4. Keep Manual-Only Verifications table; do not invent unit tests.

    Do not change QML or live config.json in this task.
  </action>
  <acceptance_criteria>
    - `02-VALIDATION.md` frontmatter has `wave_0_complete: true`
    - Body references `scripts/phase02-config-assert.py` as the automated config assert command
    - Per-Task Verification Map no longer has Task ID cell value `TBD` for BAR-01..BAR-04 rows
    - Wave 0 Requirements checkbox for the live config assert is checked
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'phase02-config-assert|wave_0_complete|02-0[1-5]' .planning/phases/02-core-bar-modules/02-VALIDATION.md | head -40</automated>
  </verify>
  <done>VALIDATION.md documents Wave 0 harness and maps tasks to plans 02-01..02-05.</done>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/phase02-config-assert.py` — live config.json asserts for BAR-01..04 keys
- Updated `.planning/phases/02-core-bar-modules/02-VALIDATION.md` Wave 0 wiring

## Assumptions (specless edges)

- BAR-01..03 probe items were unclassified; treated as config + layout + static/UAT truths covered across 02-01..02-05.
- BAR-04 concurrency probe: Network service is existing singleton event-driven nmcli monitor; Phase 2 does not add parallel writers — guarantee is stock ii single Process monitor, verified by not expanding Network.qml surface.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → host config.json | Plan only creates a read-only assert script; no secrets written |
| Script → filesystem | Reads user config path under $HOME only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | scripts/phase02-config-assert.py | low | accept | Script is read-only assert; no Process/nmcli; malformed JSON fails closed (exit non-zero) |
| T-02-02 | Information disclosure | config.json dump | low | accept | Assert prints pass/fail only; does not echo full config or SSIDs |
| T-02-SC | Tampering | package installs | low | accept | No npm/pip/cargo installs in this plan |
</threat_model>

<verification>
- `python3 -m py_compile scripts/phase02-config-assert.py` succeeds
- VALIDATION.md references the script and wave_0_complete true
</verification>

<success_criteria>
- Wave 0 harness exists and encodes D-01..D-03, D-05, D-13, D-14, D-16 predicates
- Later plans can run one command for live config regression
</success_criteria>

<output>
Create `.planning/phases/02-core-bar-modules/02-01-SUMMARY.md` when done
</output>
