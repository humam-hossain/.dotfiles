---
phase: 02-core-bar-modules
plan: 07
type: execute
wave: 6
gap_closure: true
gap_ids: [G-02-7a]
depends_on:
  - "02-05"
files_modified:
  - .config/quickshell/modules/ii/bar/BarContent.qml
autonomous: true
requirements:
  - BAR-01
must_haves:
  truths:
    - Left bar region has no ActiveWindow module
    - Left order is LeftSidebarButton → Workspaces → Resources
    - Workspaces retain stock click/wheel dispatch
  artifacts:
    - path: .config/quickshell/modules/ii/bar/BarContent.qml
      provides: "ActiveWindow removed from left section"
      contains: "Workspaces"
  key_links:
    - from: leftSectionRowLayout
      to: Workspaces
      via: "sidebar then workspaces then resources — no ActiveWindow"
  prohibitions:
    - Do not remove Workspaces, Resources, or LeftSidebarButton
    - Do not change workspace dispatch in Workspaces.qml
    - Do not re-enable weather
---

<objective>
Close G-02-7a: remove ActiveWindow from the left bar region so workspaces have room.

Purpose: UAT rejected ActiveWindow as space-wasting and unnecessary (overrides Phase 2 D-17 for ActiveWindow only).
Output: leftSectionRowLayout without ActiveWindow.
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-bar-modules/02-UAT.md
@.planning/debug/remove-active-window.md
@.planning/phases/02-core-bar-modules/02-CONTEXT.md
@.config/quickshell/modules/ii/bar/BarContent.qml
</context>

<tasks>

<task type="auto" id="02-07-01">
  <name>Task 1: Remove ActiveWindow from leftSectionRowLayout</name>
  <files>.config/quickshell/modules/ii/bar/BarContent.qml</files>
  <read_first>
    - .config/quickshell/modules/ii/bar/BarContent.qml (leftSectionRowLayout ~lines 80–122)
    - .planning/debug/remove-active-window.md
  </read_first>
  <action>
    In BarContent.qml leftSectionRowLayout:

    1. Delete the entire `ActiveWindow { ... }` block (including Layout.fillWidth and visibility gates).
    2. Ensure order is: LeftSidebarButton → Workspaces → Resources.
    3. Adjust Workspaces left margin if it relied on ActiveWindow spacing — keep a sensible margin after LeftSidebarButton (e.g. existing Workspaces Layout.rightMargin; left side may use leftSidebarButton margins only).
    4. Do not remove ActiveWindow.qml file from disk — only stop instantiating it on the bar.
    5. Leave a short comment: `// ActiveWindow removed — UAT G-02-7a (space)` so future readers know it was intentional.

    Smoke: Configuration Loaded.
  </action>
  <acceptance_criteria>
    - `rg -n 'ActiveWindow' .config/quickshell/modules/ii/bar/BarContent.qml` returns no ActiveWindow component instantiation (comment-only OK)
    - leftSectionRowLayout still contains LeftSidebarButton, Workspaces, Resources
    - Workspaces block still present with MouseArea right-click overview
    - `timeout 4 quickshell 2>&1 | rg -c 'Configuration Loaded'` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'ActiveWindow|LeftSidebarButton|Workspaces|Resources' .config/quickshell/modules/ii/bar/BarContent.qml | head -40; timeout 4 quickshell 2>&amp;1 | rg 'Configuration Loaded|Error|ReferenceError' | head -15</automated>
  </verify>
  <done>ActiveWindow gone from bar left; workspaces + resources remain; smoke green.</done>
</task>

</tasks>

<success_criteria>
- G-02-7a closed: no window title strip on left bar
- Workspaces still interactive
</success_criteria>
