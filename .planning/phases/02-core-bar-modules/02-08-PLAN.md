---
phase: 02-core-bar-modules
plan: 08
type: execute
wave: 6
gap_closure: true
gap_ids: [G-02-7b, G-02-8]
depends_on:
  - "02-05"
files_modified:
  - .config/quickshell/modules/ii/bar/BarContent.qml
autonomous: true
requirements:
  - BAR-04
must_haves:
  truths:
    - Indicators pill always shows mute, mic, Bluetooth, Network, notif icons (state glyphs) in D-19 order
    - No large empty gap after Network when other icons are present
    - Whole pill still toggles GlobalStates.sidebarRightOpen
    - Network remains icon-only (no SSID on bar)
  artifacts:
    - path: .config/quickshell/modules/ii/bar/BarContent.qml
      provides: "Always-visible D-19 indicator strip without orphan trailing space"
      contains: "Network.materialSymbol"
  key_links:
    - from: indicatorsRowLayout
      to: Audio / BluetoothStatus / Network / Notifications
      via: "always-visible icons with state-dependent MaterialSymbol text"
  prohibitions:
    - Do not put SSID text on the bar
    - Do not split Network into its own popup button
    - Do not remove the single-pill sidebar toggle
    - Do not enable weather
---

<objective>
Close G-02-7b and G-02-8: indicators pill must show a full always-visible strip (mute → mic → xkb? → Bluetooth → Network → notif), not “only network + empty space”.

Purpose: Stock Revealers hide idle icons; UAT expects a full pill. Trailing margin after Network creates blank space.
Output: Always-visible state icons in D-19 order; clean spacing.
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-bar-modules/02-UAT.md
@.planning/debug/indicators-only-network.md
@.config/quickshell/modules/ii/bar/BarContent.qml
@.config/quickshell/modules/ii/bar/NotificationUnreadCount.qml
@.config/quickshell/modules/ii/bar/HyprlandXkbIndicator.qml
</context>

<tasks>

<task type="auto" id="02-08-01">
  <name>Task 1: Always-visible mute/mic/Bluetooth/Network/notif with clean spacing</name>
  <files>.config/quickshell/modules/ii/bar/BarContent.qml</files>
  <read_first>
    - .config/quickshell/modules/ii/bar/BarContent.qml (indicatorsRowLayout full block)
    - .config/quickshell/modules/ii/bar/NotificationUnreadCount.qml
    - .config/quickshell/modules/ii/bar/UtilButtons.qml (mic glyph pattern: muted ? mic_off : mic)
    - .planning/debug/indicators-only-network.md
  </read_first>
  <action>
    Rewrite indicatorsRowLayout children for always-visible D-19 strip:

    **Order (L→R):** mute → mic → xkb → Bluetooth → Network → notif

    1. **Mute (always visible):** MaterialSymbol, not Revealer.
       - text: `Audio.sink?.audio?.muted ? "volume_off" : "volume_up"` (or volume_down if preferred when unmuted — use volume_up when not muted)
       - color: rightSidebarButton.colText

    2. **Mic (always visible):** MaterialSymbol
       - text: `Audio.source?.audio?.muted ? "mic_off" : "mic"`

    3. **xkb:** Keep HyprlandXkbIndicator as-is (multi-layout only). Do not force single-layout clutter. If inactive it contributes zero width — OK.

    4. **Bluetooth (always visible):** Remove `visible: BluetoothStatus.available` gate.
       - text: same connected/enabled/disabled ternary as today
       - When adapters unavailable, show `bluetooth_disabled`

    5. **Network:** Keep Network.materialSymbol bind (always visible).

    6. **Notif (always visible):** Stop wrapping NotificationUnreadCount in a Revealer that collapses when idle.
       - Always show NotificationUnreadCount (or equivalent MaterialSymbol notifications / notifications_paused)
       - Unread badge may still depend on showUnreadCount config

    7. **Spacing:** Prefer `indicatorsRowLayout.spacing = 15` (or realSpacing) and **remove per-child Layout.rightMargin** that leaves orphan space after Network. No large empty gap after the last icon.

    8. Preserve rightSidebarButton as single RippleButton toggling `GlobalStates.sidebarRightOpen`.

    Smoke: Configuration Loaded.
  </action>
  <acceptance_criteria>
    - indicatorsRowLayout has no Revealer wrapping mute or mic (always-visible MaterialSymbol)
    - Bluetooth MaterialSymbol has no `visible: BluetoothStatus.available` (always shown)
    - NotificationUnreadCount is not gated solely by silent/unread Revealer that collapses to zero width when idle
    - Network.materialSymbol still present
    - D-19 source order: mute-related symbol before mic before HyprlandXkb before bluetooth before Network before notif
    - No Layout.rightMargin on Network that is larger than spacing when it is not needed (prefer layout spacing)
    - `timeout 4 quickshell 2>&1 | rg -c 'Configuration Loaded'` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'indicatorsRowLayout|volume_off|volume_up|mic_off|bluetooth|Network.materialSymbol|NotificationUnreadCount|Revealer|Layout.rightMargin' .config/quickshell/modules/ii/bar/BarContent.qml | head -60; timeout 4 quickshell 2>&amp;1 | rg 'Configuration Loaded|Error|ReferenceError' | head -15</automated>
  </verify>
  <done>Full always-visible indicator strip in D-19 order; no orphan empty space; smoke green.</done>
</task>

</tasks>

<success_criteria>
- G-02-7b and G-02-8 closed: user sees mute, mic, Bluetooth, Network, notif (not just network)
- Sidebar pill click still works
- Network icon-only preserved
</success_criteria>
