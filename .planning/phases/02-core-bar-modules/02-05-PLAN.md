---
phase: 02-core-bar-modules
plan: 05
type: execute
wave: 5
depends_on:
  - "02-04"
files_modified:
  - .planning/phases/02-core-bar-modules/02-VALIDATION.md
autonomous: true
requirements:
  - BAR-01
  - BAR-02
  - BAR-03
  - BAR-04
must_haves:
  truths:
    - Workspaces click/wheel still use stock Hyprland workspace dispatch (D-04); no plugin dispatcher
    - Clock popup path remains ClockWidgetPopup; no external calendar URL in ClockWidget (D-06)
    - System TZ only — no hardcoded timezone in DateTime or Config time (D-07)
    - Live config assert script green (workspaces, time, tray, weather)
    - quickshell smoke reaches Configuration Loaded without reintroducing known bad dispatch warnings
    - Network bar surface remains materialSymbol bind; Network.qml Process surface not expanded this phase
  artifacts:
    - path: .config/quickshell/modules/ii/bar/Workspaces.qml
      provides: "Stock workspace dispatch (verify-only)"
      contains: "workspace"
    - path: .config/quickshell/modules/ii/bar/ClockWidget.qml
      provides: "ClockWidgetPopup click path (verify-only)"
      contains: "ClockWidgetPopup"
    - path: .config/quickshell/modules/ii/bar/BarContent.qml
      provides: "Final D-15/D-19 layout + network icon"
  key_links:
    - from: Workspaces.qml
      to: Hyprland.dispatch
      via: "workspace ${id} and workspace r±1"
      pattern: "Hyprland.dispatch"
    - from: ClockWidget.qml
      to: ClockWidgetPopup
      via: "MouseArea hoverTarget popup"
      pattern: "ClockWidgetPopup"
    - from: BarContent.qml
      to: Network.materialSymbol
      via: "icon-only indicators bind"
      pattern: "Network.materialSymbol"
  prohibitions:
    - Do not reintroduce plugin-based workspace focus dispatchers
    - Do not add Google Calendar URL handlers on the clock
    - Do not enable weather
    - Do not expand nmcli Process commands or add SSID text on the bar
    - Do not strip non-core modules to "simplify" verification
---

<objective>
Close Phase 2 with automated static gates and smoke verification across BAR-01..BAR-04: stock workspace dispatch (D-04), clock popup without external calendar (D-06), system TZ (D-07), live config green, network icon-only bind, layout presence of D-15/D-19 markers, and Configuration Loaded smoke. Update VALIDATION.md nyquist status.

Purpose: Layout/config plans can leave silent regressions; this plan is the automated phase gate before human UAT.
Output: All automated verifies green; VALIDATION.md sampling map status updated; no product QML changes unless a gate fails (fix only then).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-bar-modules/02-CONTEXT.md
@.planning/phases/02-core-bar-modules/02-RESEARCH.md
@.planning/phases/02-core-bar-modules/02-VALIDATION.md
@.config/quickshell/modules/ii/bar/BarContent.qml
@.config/quickshell/modules/ii/bar/Workspaces.qml
@.config/quickshell/modules/ii/bar/ClockWidget.qml
@.config/quickshell/services/DateTime.qml
@.config/quickshell/services/Network.qml
@scripts/phase02-config-assert.py
</context>

<tasks>

<task type="auto" id="02-05-01">
  <name>Task 1: Static gates for workspaces, clock popup, network, layout markers</name>
  <files>.config/quickshell/modules/ii/bar/Workspaces.qml, .config/quickshell/modules/ii/bar/ClockWidget.qml, .config/quickshell/modules/ii/bar/BarContent.qml, .config/quickshell/services/DateTime.qml, .config/quickshell/services/Network.qml</files>
  <read_first>
    - .config/quickshell/modules/ii/bar/Workspaces.qml (switchWorkspaceToHovered, onWheel)
    - .config/quickshell/modules/ii/bar/ClockWidget.qml (popup + DateTime.time)
    - .config/quickshell/modules/ii/bar/BarContent.qml (showDate, Network.materialSymbol, module order markers)
    - .config/quickshell/services/DateTime.qml (no TZ hardcode)
    - .config/quickshell/services/Network.qml (materialSymbol; do not expand)
    - .planning/phases/02-core-bar-modules/02-CONTEXT.md (D-04, D-06, D-07, D-09, D-15, D-19)
  </read_first>
  <action>
    Run and record static verification only. Fix QML only if a gate fails (minimal fix, preserve CONTEXT decisions).

    Gates (all must pass):

    BAR-01 / D-04:
    - Workspaces.qml contains stock dispatch patterns: template workspace with id interpolation AND workspace r+1 / r-1 for wheel.
    - Workspaces.qml must NOT contain the plugin focus dispatcher string used before Phase 1 gap fix (search for the hl.dsp focus form and expect zero matches).

    BAR-02 / D-06 / D-07 / D-08:
    - ClockWidget.qml references ClockWidgetPopup.
    - ClockWidget.qml must not open an external calendar host URL (no calendar.google.com style URL string).
    - BarContent.qml ClockWidget uses showDate: false.
    - DateTime.qml has no hardcoded timezone id assignment.

    BAR-03:
    - BarContent.qml still instantiates SysTray.
    - Live config assert (next task) covers monochrome false + pin policy.

    BAR-04 / D-09 / D-11 / D-19 markers:
    - BarContent.qml binds Network.materialSymbol on the indicators pill.
    - indicators order markers present: volume_off, mic_off, HyprlandXkb, bluetooth glyph source, Network.materialSymbol, NotificationUnreadCount in that relative order (Bluetooth before Network before notif).
    - Network.qml was not given new Process entries this phase (diff vs main/HEAD should be empty for Network.qml unless emergency — prefer empty).

    D-15 markers:
    - leftSectionRowLayout still hosts Workspaces and Resources after ActiveWindow.
    - middle/center hosts ClockWidget + UtilButtons.
    - right hosts Media, BatteryIndicator, SysTray, rightSidebarButton.

    D-16: bar.weather.enable not forced true in BarContent; config assert covers false.

    If all gates already pass, do not churn code — document pass in SUMMARY.
  </action>
  <acceptance_criteria>
    - `rg -n 'workspace \$\{|workspace r\+1|workspace r\-1' .config/quickshell/modules/ii/bar/Workspaces.qml` matches click and wheel paths
    - `rg -n 'hl\.dsp' .config/quickshell/modules/ii/bar/Workspaces.qml` count is 0 (or only appears inside a comment explaining removal — prefer 0 total)
    - `rg -n 'ClockWidgetPopup' .config/quickshell/modules/ii/bar/ClockWidget.qml` ≥ 1
    - `rg -n 'showDate:\s*false' .config/quickshell/modules/ii/bar/BarContent.qml` ≥ 1
    - `rg -n 'Network.materialSymbol' .config/quickshell/modules/ii/bar/BarContent.qml` ≥ 1
    - `rg -n 'SysTray' .config/quickshell/modules/ii/bar/BarContent.qml` ≥ 1
    - Bluetooth MaterialSymbol block appears before Network.materialSymbol block before NotificationUnreadCount in BarContent.qml
    - DateTime.qml has no timedatectl TZ override property set to a region string
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'workspace \$\{|workspace r\+|workspace r\-|hl\.dsp' .config/quickshell/modules/ii/bar/Workspaces.qml; rg -n 'ClockWidgetPopup|showDate:\s*false|Network.materialSymbol|SysTray|volume_off|mic_off|HyprlandXkb|NotificationUnreadCount|bluetooth' .config/quickshell/modules/ii/bar/BarContent.qml .config/quickshell/modules/ii/bar/ClockWidget.qml | head -50</automated>
  </verify>
  <done>Static gates for BAR-01..04 and D-04/D-06/D-08/D-09/D-15/D-19 markers pass.</done>
</task>

<task type="auto" id="02-05-02">
  <name>Task 2: Live config assert + smoke + VALIDATION.md sign-off fields</name>
  <files>.planning/phases/02-core-bar-modules/02-VALIDATION.md</files>
  <read_first>
    - scripts/phase02-config-assert.py
    - .planning/phases/02-core-bar-modules/02-VALIDATION.md
    - .planning/phases/01-shell-foundation-theme/01-LEARNINGS.md (smoke pattern)
  </read_first>
  <action>
    1. Run `python3 scripts/phase02-config-assert.py` — must exit 0.
    2. Run smoke: `timeout 4 quickshell 2>&1 | tee /tmp/qs-phase02-smoke.log | rg 'Configuration Loaded|Error|hl\.dsp'`.
       - Require Configuration Loaded.
       - Fail if smoke log shows Invalid dispatcher involving plugin workspace focus, or hard Error preventing load.
    3. Optional: `hyprctl layers` notes quickshell:bar present (document in SUMMARY; Waybar may still coexist — not a failure).
    4. Update `02-VALIDATION.md`:
       - Mark automated rows for BAR-01..04 config/static/smoke as green where commands passed.
       - Set `nyquist_compliant: true` only if Wave 0 complete AND this plan's automated commands all green AND every task has automated verify coverage.
       - Leave Manual-Only Verifications unchecked (human UAT is `/gsd-verify-work` / UAT later) — do not claim UAT passed.
    5. Do not enable weather, do not cut over Waybar, do not expand Network.qml.

    Manual UAT checklist items (for SUMMARY handoff, not blocking this task): click workspace, wheel cycle, clock seconds tick, clock popup, tray color/click, network icon + sidebar SSID, D-15 L→R visual, D-19 pill visual.
  </action>
  <acceptance_criteria>
    - `python3 scripts/phase02-config-assert.py` exits 0
    - Smoke log contains `Configuration Loaded`
    - Smoke log does not require fixing a hard startup Error
    - `02-VALIDATION.md` frontmatter has `wave_0_complete: true` and `nyquist_compliant: true` when automated gates green
    - Manual-Only table remains for human UAT (not falsely marked all complete)
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/phase02-config-assert.py &amp;&amp; timeout 4 quickshell 2>&amp;1 | rg 'Configuration Loaded|Error|hl\.dsp' | head -20</automated>
  </verify>
  <done>Automated phase gate green; VALIDATION.md reflects Nyquist automated compliance; ready for human UAT.</done>
</task>

</tasks>

## Artifacts this phase produces

- Phase 2 automated verification evidence (config assert + smoke + static rg)
- Updated `02-VALIDATION.md` nyquist/status fields
- Confirm-only reads of Workspaces.qml, ClockWidget.qml, DateTime.qml, Network.qml, BarContent.qml

## Assumptions (specless edges)

- Unresolved probe items for BAR-01..03 are closed via static + config + smoke here; interactive click/wheel/SSID remain Manual-Only (VALIDATION.md).
- BAR-04 concurrency: single Network service monitor; no parallel Phase 2 writers introduced.
- HDMI dual-monitor UAT optional if display detached (RESEARCH A3).

## Prohibitions (descriptor-less)

- No Google Calendar on clock click
- No SSID text on bar
- No weather enable
- No stripping non-core modules
- No new nmcli Process surface
- No building modules from scratch when stock exists
- No plugin workspace dispatch reintroduction

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke launch → session | Local quickshell process only |
| Assert script → config.json | Read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Network.qml nmcli | high | mitigate | Gate requires Network.qml not expanded; bar remains materialSymbol bind only |
| T-02-04 | Denial of service | bad config / QML | medium | mitigate | config assert + Configuration Loaded smoke before phase complete |
| T-02-11 | Tampering | workspace dispatch | medium | mitigate | Static gate forbids plugin dispatcher reintroduction; stock workspace only |
| T-02-SC | Tampering | package installs | low | accept | No installs in verify plan |
</threat_model>

<verification>
- phase02-config-assert.py OK
- static rg gates OK
- Configuration Loaded smoke OK
- VALIDATION.md nyquist_compliant true when automated complete
</verification>

<success_criteria>
- All four requirements BAR-01..BAR-04 have automated evidence paths green
- D-04, D-06, D-07, D-09, D-15/D-19 markers verified statically
- Human UAT remaining items are explicitly manual-only, not silently dropped
</success_criteria>

<output>
Create `.planning/phases/02-core-bar-modules/02-05-SUMMARY.md` when done
</output>
