---
phase: 02-core-bar-modules
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - .config/quickshell/modules/ii/bar/BarContent.qml
autonomous: true
requirements:
  - BAR-03
  - BAR-04
must_haves:
  truths:
    - Right region visual L→R is Media → BatteryIndicator → SysTray → Indicators pill (D-15 right)
    - Indicators pill child order L→R is mute → mic → xkb → Bluetooth → Network → notif (D-19)
    - Full indicators cluster retained: mute, mic, HyprlandXkb, Bluetooth, Network, NotificationUnreadCount (D-18)
    - Network on bar is icon-only MaterialSymbol bound to Network.materialSymbol — no SSID/signal text on bar (D-09, D-10)
    - Whole indicators pill remains one RippleButton toggling GlobalStates.sidebarRightOpen (D-11)
    - SysTray remains present with shortened-form visibility gate (BAR-03 placement)
    - Weather loader stays inactive via Config.options.bar.weather.enable (D-16)
  artifacts:
    - path: .config/quickshell/modules/ii/bar/BarContent.qml
      provides: "Right LTR layout + indicators reorder D-19 + network icon bind"
      contains: "Network.materialSymbol"
  key_links:
    - from: indicatorsRowLayout Network MaterialSymbol
      to: services/Network.qml materialSymbol
      via: "text: Network.materialSymbol icon-only bind"
      pattern: "Network.materialSymbol"
    - from: rightSidebarButton
      to: GlobalStates.sidebarRightOpen
      via: "onPressed toggles right sidebar (wifi panel path)"
      pattern: "sidebarRightOpen"
    - from: SysTray
      to: TrayService / SystemTray
      via: "existing SysTray placement before indicators"
      pattern: "SysTray"
  prohibitions:
    - Do not put SSID or signal percent text on the bar
    - Do not split Network into its own button / NetworkPopup
    - Do not expand Network.qml nmcli Process surface or add shell interpolation of SSID
    - Do not strip Media, Battery, mute/mic/xkb/BT/notif from the pill cluster
    - Do not enable weather
    - Do not change tray pin logic in TrayService (D-14 already config-locked)
---

<objective>
Finish BarContent.qml right-region rewire: LTR module order Media → Battery → SysTray → Indicators (D-15 / BAR-03), indicators pill reorder mute→mic→xkb→Bluetooth→Network→notif (D-19 / BAR-04 icon surface), keep full cluster and single-pill sidebar click (D-18 / D-11), weather still off (D-16).

Purpose: Stock ii uses RTL reverse-declare and wrong indicator order; CONTEXT locks visual L→R and network icon-only on the pill.
Output: Final rightSectionRowLayout + indicatorsRowLayout in BarContent.qml.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-bar-modules/02-CONTEXT.md
@.planning/phases/02-core-bar-modules/02-RESEARCH.md
@.planning/phases/02-core-bar-modules/02-PATTERNS.md
@.config/quickshell/modules/ii/bar/BarContent.qml
@.config/quickshell/services/Network.qml
@.config/quickshell/modules/ii/bar/SysTray.qml
</context>

<tasks>

<task type="auto" id="02-04-01">
  <name>Task 1: Switch right section to LTR and order Media Battery SysTray Indicators</name>
  <files>.config/quickshell/modules/ii/bar/BarContent.qml</files>
  <read_first>
    - .config/quickshell/modules/ii/bar/BarContent.qml (barRightSideMouseArea, rightSectionRowLayout, SysTray, Weather Loader, any Media/Battery parked in plan 02-03)
    - .planning/phases/02-core-bar-modules/02-RESEARCH.md (Pattern 3 LTR preference; layout sketch)
    - .planning/phases/02-core-bar-modules/02-CONTEXT.md (D-15 right row; D-17)
  </read_first>
  <action>
    Finalize rightSectionRowLayout for D-15 right (planner discretion: LTR, not RTL reverse-declare):

    1. Set `layoutDirection` on rightSectionRowLayout to Qt.LeftToRight (remove RightToLeft).
    2. Child structure that yields visual L→R Media → BatteryIndicator → SysTray → Indicators while content still hugs the right screen edge:
       - Leading `Item { Layout.fillWidth: true; Layout.fillHeight: true }` spacer (pushes modules right)
       - Media (preserve useShortenedForm visibility)
       - BatteryIndicator (preserve Battery.available / shortened gates)
       - SysTray (preserve useShortenedForm === 0 visibility, invertSide bind, fillHeight)
       - rightSidebarButton Indicators pill (preserve Layout.rightMargin: Appearance.rounding.screenRounding and existing colors/toggle)
       - Weather Loader last (or after spacer before modules only if inactive) with `active: Config.options.bar.weather.enable` remaining the sole gate — must stay false via config (D-16). Do not place Weather between Media and Indicators in a way that shows when enable is false (Loader inactive is fine at end).
    3. Preserve barRightSideMouseArea volume scroll (onScrollUp/Down) and left-click sidebar toggle on the empty right area.
    4. Ensure Media and BatteryIndicator appear exactly once (clean up any temporary park from 02-03).
    5. Do not edit SysTray.qml internals; monochrome is already config-driven from 02-02 (D-13).

    After edit, confirm QML still loads: Configuration Loaded smoke.
  </action>
  <acceptance_criteria>
    - rightSectionRowLayout does not set layoutDirection to RightToLeft
    - Source child order inside rightSectionRowLayout includes Media before BatteryIndicator before SysTray before rightSidebarButton (allow fill Item and comments)
    - SysTray { block still present with visible gate on useShortenedForm
    - Media { and BatteryIndicator { each count == 1 in BarContent.qml
    - Weather Loader still uses Config.options.bar.weather.enable (not hardcoded true)
    - `timeout 4 quickshell 2>&1 | rg -c 'Configuration Loaded'` ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'layoutDirection|Media|BatteryIndicator|SysTray|rightSidebarButton|weather.enable|rightSectionRowLayout' .config/quickshell/modules/ii/bar/BarContent.qml | head -50; timeout 4 quickshell 2>&amp;1 | rg 'Configuration Loaded|Error|ReferenceError' | head -15</automated>
  </verify>
  <done>Right region LTR order matches D-15; SysTray placed before indicators; smoke green.</done>
</task>

<task type="auto" id="02-04-02">
  <name>Task 2: Reorder indicators pill to D-19 and keep network icon-only</name>
  <files>.config/quickshell/modules/ii/bar/BarContent.qml</files>
  <read_first>
    - .config/quickshell/modules/ii/bar/BarContent.qml (indicatorsRowLayout children ~mute/mic/xkb/notif/Network/Bluetooth)
    - .config/quickshell/services/Network.qml (materialSymbol property only — do not modify)
    - .planning/phases/02-core-bar-modules/02-CONTEXT.md (D-09..D-12, D-18, D-19)
    - .planning/phases/02-core-bar-modules/02-RESEARCH.md (Pitfall 8 spacing after reorder)
  </read_first>
  <action>
    Reorder indicatorsRowLayout children for D-19 visual L→R (layoutDirection default LTR on this inner RowLayout):

    Target order:
    1. mute Revealer (volume_off) — keep reveal bind on Audio.sink muted
    2. mic Revealer (mic_off) — keep reveal bind on Audio.source muted
    3. HyprlandXkbIndicator — keep color bind to rightSidebarButton.colText
    4. Bluetooth MaterialSymbol — move before Network; keep visible: BluetoothStatus.available and connected/enabled/disabled glyph ternary
    5. Network MaterialSymbol — `text: Network.materialSymbol` only (D-09, D-10); no StyledText SSID beside it
    6. NotificationUnreadCount Revealer — move last; keep silent/unread reveal logic

    Spacing (Pitfall 8):
    - After reorder, normalize Layout.leftMargin / Layout.rightMargin so non-revealer icons use indicatorsRowLayout.realSpacing consistently (Bluetooth previously used leftMargin; Network may need rightMargin before notif).
    - Do not remove any of the six cluster members (D-18).

    Click path (D-11):
    - Keep Network as a non-interactive MaterialSymbol child of rightSidebarButton.
    - rightSidebarButton onPressed must still toggle GlobalStates.sidebarRightOpen (whole pill).
    - Do not add a dedicated NetworkPopup or new Process commands.
    - Do not modify services/Network.qml nmcli surface (no new bash -c with SSID).

    D-12: no new tooltip requirements; existing sidebar wifi UI remains the SSID/signal detail path for UAT.
  </action>
  <acceptance_criteria>
    - Inside indicatorsRowLayout, reading top-to-bottom child blocks, order is: mute Revealer, mic Revealer, HyprlandXkbIndicator, Bluetooth MaterialSymbol, Network MaterialSymbol (Network.materialSymbol), NotificationUnreadCount Revealer
    - `rg -n 'Network.materialSymbol' .config/quickshell/modules/ii/bar/BarContent.qml` matches ≥ 1
    - No SSID property text binding appears next to the Network MaterialSymbol (no `Network.active.ssid` or similar StyledText in the pill)
    - rightSidebarButton still toggles sidebarRightOpen onPressed
    - BluetoothStatus and NotificationUnreadCount and HyprlandXkbIndicator still present in the pill
    - services/Network.qml is unchanged in this plan (`git diff --name-only` does not list it)
    - Configuration Loaded smoke still passes
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'volume_off|mic_off|HyprlandXkb|bluetooth|Network.materialSymbol|NotificationUnreadCount|sidebarRightOpen' .config/quickshell/modules/ii/bar/BarContent.qml | head -40; timeout 4 quickshell 2>&amp;1 | rg 'Configuration Loaded|Error' | head -10</automated>
  </verify>
  <done>Indicators match D-19; network icon-only on pill; sidebar click path intact; BAR-03/04 layout complete.</done>
</task>

</tasks>

## Artifacts this phase produces

- Final right-region layout in `BarContent.qml` (D-15 right, BAR-03 SysTray placement)
- Reordered `indicatorsRowLayout` (D-19) with Network.materialSymbol icon-only (BAR-04 / D-09..D-11)
- Weather still config-gated off (D-16)

## Assumptions (specless edges)

- A1: LTR + leading fill Item keeps right-edge hug equivalent to prior RTL (verify margins in smoke/UAT).
- A2: D-11 “wifi panel” = opening right sidebar via pill; auto-opening WifiDialog is out of scope unless UAT fails (RESEARCH open question).
- BAR-04 concurrency: no new writers; stock Network monitor Process only.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bar Network icon → Network.qml service | Read-only property bind; no new user text on bar |
| Indicators pill → sidebar | UI state toggle only |
| Network.qml → nmcli | Pre-existing Process surface — do not expand |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Network.qml nmcli / SSID | high | mitigate | Do not modify Network.qml; do not add shell -c with unescaped SSID in Phase 2; bar uses materialSymbol only |
| T-02-08 | Information disclosure | SSID on bar | medium | mitigate | D-09 icon-only; acceptance forbids SSID StyledText on pill; detail stays in sidebar |
| T-02-09 | Spoofing | dual bar clickjack (waybar + quickshell) | low | accept | Document UAT targets quickshell:bar; cutover is Phase 4 |
| T-02-10 | Elevation of privilege | SysTray left-click activate | low | accept | Desktop SNI norm; full-color icons (D-13) aid recognition |
| T-02-SC | Tampering | package installs | low | accept | No installs |
</threat_model>

<verification>
- Static L→R right module order and indicators order
- Network.materialSymbol bind present; Network.qml untouched
- Configuration Loaded
</verification>

<success_criteria>
- BAR-03 SysTray in D-15 right position
- BAR-04 network icon on indicators pill with D-19 order and D-11 click path
- D-18 full cluster retained; D-16 weather still off
</success_criteria>

<output>
Create `.planning/phases/02-core-bar-modules/02-04-SUMMARY.md` when done
</output>
