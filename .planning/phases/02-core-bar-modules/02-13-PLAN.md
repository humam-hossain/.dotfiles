---
phase: "02"
plan: "13"
type: execute
wave: 1
gap_closure: true
gap_ids: ["G-02-14"]
depends_on: []
files_modified:
  - ".config/quickshell/modules/common/Config.qml"
  - "/home/pera/.config/illogical-impulse/config.json"
  - "scripts/phase02-config-assert.py"
autonomous: true
requirements: []
must_haves:
  truths:
    - Bar workspaces strip shows 4 workspace indicators.
    - Live config and Config defaults both use shown: 4.
    - phase02-config-assert.py expects shown == 4.
---

## Context

**Gap G-02-14 (minor):** Workspaces strip shows 10 slots; user wants 4.

**Root cause:** Phase 2 D-02 locked `bar.workspaces.shown: 10` (dual-written Config + live JSON; assert script expects 10). UAT preference overrides D-02 to **4**.

Debug: `.planning/debug/workspaces-shown-count.md`

## Tasks

### Task 1: Dual-write shown: 4

**File:** `.config/quickshell/modules/common/Config.qml`  
Under `bar.workspaces`:
- Change `property int shown: 10` → `property int shown: 4`

**File:** `/home/pera/.config/illogical-impulse/config.json`  
Under `bar.workspaces`:
- Change `"shown": 10` → `"shown": 4`

### Task 2: Update assert script

**File:** `scripts/phase02-config-assert.py`
- Change assert from `shown == 10` to `shown == 4` (and update the error message string).

## Notes

- Overrides D-02 from `02-CONTEXT.md` (UAT preference). Dual-monitor grouping still uses `shownCount` as group size (was 10, now 4).
- Dual-monitor UAT test 9 remains skipped when HDMI is disconnected.

## Verification

- Reload quickshell; bar shows 4 workspace indicators.
- `python3 scripts/phase02-config-assert.py` exits 0 (or only fails on unrelated keys).
