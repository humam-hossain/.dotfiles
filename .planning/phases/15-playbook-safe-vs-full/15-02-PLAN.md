---
phase: 15-playbook-safe-vs-full
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - docs/dots-hyprland-workflow.md
autonomous: true
requirements: [DOC-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A reader can tell, from one section, what the safe profile injects and what the full profile drops, without opening the wrapper (D-06, D-11)."
    - "A reader learns the wrapper default is still safe and `--full` is opt-in, even though the walkthrough that follows walks full (D-07)."
    - "A cold machine cannot reach `--full` from this document without first passing the inventory → disposition gate, because the gate is written before the install step (D-13)."
    - "The playbook no longer claims dual-run is the destination, nor that wrapper defaults unconditionally leave `hyprland.conf` alone (D-10)."
    - "Every Outline entry resolves to a heading that exists in the file at this commit (D-03, D-23)."
  artifacts:
    - path: "docs/dots-hyprland-workflow.md"
      provides: "Profiles section with the three-row flag-axis table, and the required pre-install gate section"
      contains: "## Profiles: safe vs full"
  key_links:
    - from: "docs/dots-hyprland-workflow.md"
      to: "arch/dots-hyprland.sh"
      via: "the flag-axis table quotes the live SAFE_DEFAULTS array verbatim and names `./arch/dots-hyprland.sh help` as the syntax SoT"
      pattern: "--core --skip-hyprland --skip-sysupdate"
    - from: "docs/dots-hyprland-workflow.md"
      to: ".planning/phases/10-full-install-impact-inventory/10-INVENTORY.md"
      via: "the gate section cites the impact inventory as SoT for what a full install touches"
      pattern: "10-INVENTORY\\.md"
    - from: "docs/dots-hyprland-workflow.md"
      to: ".planning/phases/11-disposition-decisions/11-DISPOSITIONS.md"
      via: "the gate section cites the dispositions as SoT for the per-surface adopt decisions"
      pattern: "11-DISPOSITIONS\\.md"
  prohibitions:
    - statement: "The playbook must not present the full profile as the default or the recommended-by-omission path; the wrapper default is safe and `--full` is opt-in (D-07)."
    - statement: "No operator-facing doc may name a rollback source that does not hold the recoverable pre-adopt config, and no destructive or recovery-destroying command may be documented without its guard."
    - statement: "The retired session surfaces must not be misrepresented: write `Waybar`, `rofi` and `swaync` literally rather than as a collective noun, and state the D-38 screen-share consequence as a possibility rather than a certainty."
    - statement: "Frozen historical planning records must not be rewritten to agree with today's truth; staleness in them is flagged in the sweep record, never edited away (D-22)."
---

<objective>
Deliver the front half of DOC-03: the reader framing, the explicit safe-versus-full definition, the three-row flag-axis table, and the inventory → disposition gate placed where a cold machine actually hits it — before the install step, not after it.

The tracer in `15-01` proved a corrected fact can travel the doc chain and be machine-asserted. This plan expands that proof horizontally across the sections that decide *which* install a reader runs (D-03, D-04, D-06, D-07, D-10, D-11, D-12, D-13).

Purpose: DOC-03's first success criterion — "documents safe vs full profiles, inventory→disposition→adopt sequence, and flag axes".
Output: a rewritten Purpose and Prerequisites, a new `## Profiles: safe vs full` section, a new `## 3. Required gate before any full install` section, and a renumbered spine whose Outline resolves.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-playbook-safe-vs-full/15-CONTEXT.md
@.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md
@.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md
@.planning/phases/15-playbook-safe-vs-full/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Purpose and Prerequisites for a cold machine, deleting two of D-10's three stale claims</name>
  <read_first>
    - `docs/dots-hyprland-workflow.md` — lines 1-34 (`## Purpose`, the DRY callout, `## Prerequisites`, `## Canonical path`), the exact region this task rewrites, in its post-`15-01` state
    - `.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md` — "Doc Sweep Inventory" table for `docs/dots-hyprland-workflow.md` (lines 671-696), which quotes the stale lines verbatim with their line numbers
    - `.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md` — the "DRY / SoT-naming callout" section, which holds the callout sentence verbatim and the runbook's extended variant
    - `.planning/REQUIREMENTS.md` — lines 74 and 82, the two binding Out of Scope rows
  </read_first>
  <files>docs/dots-hyprland-workflow.md</files>
  <action>
Rewrite the opening so a cold-machine reader is addressed first and this machine's current state is a short note rather than the framing (D-04).

**Purpose.** Keep the existing "single product path" bullet list — fork and pin, install entry, live product, retired — it is still true. Replace the sentence that follows it. The current sentence promises that a cold machine can reach dual-run as the destination; delete that destination claim entirely (D-10, first stale claim, quoted at `15-RESEARCH.md:675`). Write in its place a sentence saying this playbook is the Install/Adopt source of truth so a cold machine can reach a working session without tribal knowledge, followed by one sentence naming the two profiles it covers and pointing at the `## Profiles: safe vs full` section. Then add a short, clearly secondary note — two sentences at most — recording that the machine this repo was written on has already taken the full adopt (2026-09-04), and citing `docs/phase14-adopt-runbook.md` for that window's detail (D-02, D-04). This note must not become the framing.
<!-- planner-discipline-allow: dual-run -->

Keep the DRY callout blockquote immediately after Purpose exactly as it reads today — it is the sentence that makes the flag-axis table in Task 2 legal alongside D-12's prohibition on reproducing the flag list. Extend it with the runbook's clause shape (`15-PATTERNS.md`, "DRY / SoT-naming callout"): state that this doc names only the flags whose consequences the reader must weigh, and that `./arch/dots-hyprland.sh help` remains the syntax source of truth.

**Prerequisites.** Four of the five bullets are unchanged. Rewrite only the Hyprland bullet. It currently asserts, unconditionally, that you own a personal `~/.config/hypr` and that wrapper defaults **do not** replace `hyprland.conf` — a claim that holds only under the safe profile (D-10, second stale claim, quoted at `15-RESEARCH.md:676`). Replace it with a bullet that states a Hyprland session is required to run the post-install checks, and that whether your personal `hyprland.conf` survives depends on which profile you run — with the consequence stated in the profiles section, not here. Do not repeat the fate of the file in this bullet; it belongs in the flag-axis table.
<!-- planner-discipline-allow: wrapper defaults **do not** replace -->

Leave `## Canonical path` untouched. Change no numbered section in this task.
  </action>
  <verify>
    <automated>! grep -qF 'wrapper defaults **do not** replace' docs/dots-hyprland-workflow.md && ! grep -qF 'reach dual-run' docs/dots-hyprland-workflow.md && grep -qF './arch/dots-hyprland.sh help' docs/dots-hyprland-workflow.md && grep -q 'phase14-adopt-runbook.md' docs/dots-hyprland-workflow.md && grep -qi 'profiles' docs/dots-hyprland-workflow.md</automated>
    <fails_when>non-zero exit — either D-10 stale fragment still matches (the unconditional `hyprland.conf` claim in Prerequisites, or the dual-run destination phrase in Purpose), the DRY callout lost its `./arch/dots-hyprland.sh help` citation, the post-adopt note does not cite the runbook, or the word "profiles" never appears</fails_when>
    <automated>test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' docs/dots-hyprland-workflow.md | grep -vx 'google-chrome-stable' | wc -l)" -eq 0 && git diff --quiet HEAD -- arch/ scripts/ .config/ stow/ vendor/ .planning/STATE.md .planning/ROADMAP.md</automated>
    <fails_when>the count is above 0 because the rewrite used a collective noun instead of naming `Waybar`, `rofi` and `swaync` literally, or `git diff --quiet` reports an uncommitted change under a fenced directory or in `STATE.md` / `ROADMAP.md`</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/dots-hyprland-workflow.md` contains neither the unconditional Prerequisites claim `wrapper defaults **do not** replace` nor the Purpose destination phrase `reach dual-run`.
    - The Purpose section names both profiles and points at the profiles section.
    - A post-adopt note exists, is at most two sentences, and cites `docs/phase14-adopt-runbook.md`.
    - The DRY callout is still a blockquote beginning `> **Flag / subcommand details:**` and still names `./arch/dots-hyprland.sh help`.
    - `## Canonical path` is byte-identical to its previous state.
    - Non-allowlisted `chrome` token count in the file is 0.
    - No file under `arch/`, `scripts/`, `.config/`, `stow/`, `vendor/` and neither `.planning/STATE.md` nor `.planning/ROADMAP.md` has an uncommitted diff.
  </acceptance_criteria>
  <done>Purpose and Prerequisites address a cold machine, two of D-10's three stale claims are gone from the file, and the DRY callout still names the wrapper help as the syntax SoT.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Profiles section with the three-row flag-axis table</name>
  <read_first>
    - `docs/dots-hyprland-workflow.md` — the `## Prerequisites` / `## Canonical path` boundary where this section is inserted, in its post-Task-1 state
    - `arch/dots-hyprland.sh` — line 12 (`SAFE_DEFAULTS`), lines 177-183 (the full-profile blast-radius echoes), lines 1420-1424 (the `--full` scope guard), lines 1444-1452 (the injection logic). READ ONLY; the phase scope fence forbids editing `arch/`
    - the live output of `./arch/dots-hyprland.sh help` — the safe-defaults paragraph, which is the syntax SoT this table defers to
    - `.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md` — "Flag Axes — Verified Ground Truth" (lines 471-532), which already assembles the three rows from those sources
    - `.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md` — the "Table pattern" and "Quoting an executable SoT verbatim" sections
    - `.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` — section 6, the D-11 accept-remove for Waybar/rofi/swaync and D-10 on SAFE_DEFAULTS remaining the default
  </read_first>
  <files>docs/dots-hyprland-workflow.md</files>
  <action>
Insert a new top-level section headed `## Profiles: safe vs full` between `## Prerequisites` and `## Canonical path`. One section on the one spine — not a second track (D-03, D-06).

**Safe paragraph.** State what the wrapper injects: the array `--core --skip-hyprland --skip-sysupdate`, quoted byte-for-byte as it appears at `arch/dots-hyprland.sh:12`, presented in a fenced `text` block per the repo's literal-value convention. State that injection applies to `install` and `install-files` only, and that `install-deps` and `install-setups` get no injection at all — that scope note is quoted from the live help text. State what safe does not touch: your personal `hyprland.conf` is neither renamed nor replaced, the misc overlay is not applied, and no unattended full system upgrade runs. State that running `Waybar`, `rofi` and `swaync` alongside `qs -c ii` is a property of this profile and of no other (D-09) — never describe it as a milestone goal. Close with one pointer line, not a section, saying the two conf-hook lines this profile relies on are documented in the session section (D-08).

**Full paragraph.** State that `--full` is wrapper-owned meta valid only on `install` and `install-files`, and that the wrapper refuses it on any other subcommand (`arch/dots-hyprland.sh:1420-1424`). State that it drops all three residuals at once. Then the sentence D-07 requires, unmissably and in this section rather than a footnote: the wrapper default is still safe, `--full` is opt-in, and the walkthrough later in this document walks full as a documentation choice, not as a default change. Quote the wrapper's own unambiguous help sentence to that effect. Follow it with one sentence recording that under full, `Waybar`, `rofi` and `swaync` are replaced by `qs -c ii`; that this removal was explicitly accepted by Phase 11 D-11 in `11-DISPOSITIONS.md` section 6 rather than being out of scope; and that the prior trees remain archived under `stow/` per D-12. Do not describe a restore path — nothing has verified one since the adopt, and it is a deferred idea.

**Flag-axis table (D-11).** A three-row, three-column pipe table, left-aligned, header separator without alignment colons, following the repo table pattern. Columns: Axis, Injected (safe default), Dropped (`--full`). Rows, in this order, with content taken from `15-RESEARCH.md` lines 519-524:
- `--skip-hyprland` — injected: personal `hyprland.conf` is neither renamed nor replaced, and the skip is full rather than entry-only; dropped: upstream renames `hyprland.conf` to `.old`, syncs the ii `hypr/hyprland` Lua tree, installs `hyprland.lua`, and writes `.new` sidecars for hyprlock and hypridle.
- `--core` — injected: core install path only, misc overlay not applied; dropped: the misc overlay may overwrite, and the install also reaches fish, kitty, starship and misc.
- `--skip-sysupdate` — injected: no unattended full system upgrade; dropped: `pacman -Syu` may run on the deps portion of `install`.

Immediately under the table, one sentence naming `./arch/dots-hyprland.sh help` as the syntax source of truth and stating this table is narrative about consequences, not a flag reference. Do not reproduce the wrapper's full flag list anywhere (D-12).

Add the matching `## Outline`-adjacent placement only if the Outline already lists unnumbered front sections; it does not today, so leave the Outline to Task 3.
  </action>
  <verify>
    <automated>grep -qF '## Profiles: safe vs full' docs/dots-hyprland-workflow.md && SD="$(grep -oP '^SAFE_DEFAULTS=\(\K[^)]+' arch/dots-hyprland.sh)" && grep -qF -- "$SD" docs/dots-hyprland-workflow.md && for f in skip-hyprland core skip-sysupdate; do grep -q -- "--$f" docs/dots-hyprland-workflow.md || exit 1; done && grep -q -- '--full' docs/dots-hyprland-workflow.md && grep -qiE 'default.*(safe|SAFE_DEFAULTS)' docs/dots-hyprland-workflow.md && grep -q '11-DISPOSITIONS.md' docs/dots-hyprland-workflow.md</automated>
    <fails_when>non-zero exit — the `## Profiles: safe vs full` heading is absent, the doc does not quote the live `SAFE_DEFAULTS` array byte-for-byte (the table has drifted from `arch/dots-hyprland.sh:12`), any one of the three axis flags is unmentioned, `--full` is unmentioned, no sentence pairs the word "default" with safe or `SAFE_DEFAULTS` (the D-07 statement is missing), or `11-DISPOSITIONS.md` is not cited</fails_when>
    <automated>test "$(grep -cE '^\| *`--(skip-hyprland|core|skip-sysupdate)`' docs/dots-hyprland-workflow.md)" -eq 3 && test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' docs/dots-hyprland-workflow.md | grep -vx 'google-chrome-stable' | wc -l)" -eq 0 && test "$(grep -cE '\brofi\b' docs/dots-hyprland-workflow.md)" -ge 1 && grep -q 'swaync' docs/dots-hyprland-workflow.md && grep -q 'Waybar' docs/dots-hyprland-workflow.md</automated>
    <fails_when>the anchored table-row count is not exactly 3 (a row was dropped, duplicated, or written outside the table), the non-allowlisted `chrome` count is above 0, or the word-boundary `rofi` count is 0 / `swaync` / `Waybar` are absent — meaning the trio was named collectively instead of literally. The `\b` boundaries are mandatory here because this section makes the file full of the word "profile", which contains `rofi` as a substring</fails_when>
    <automated>git diff --quiet HEAD -- arch/ scripts/ .config/ stow/ vendor/ .planning/STATE.md .planning/ROADMAP.md && ./scripts/phase13-d19-assert.sh >/tmp/p15-02-d19.txt 2>&1 && test "$(grep -c '^\[FAIL\]' /tmp/p15-02-d19.txt)" -eq 0</automated>
    <fails_when>non-zero exit — an uncommitted change exists under a fenced directory or in `STATE.md` / `ROADMAP.md`, or `scripts/phase13-d19-assert.sh` prints a `[FAIL]` line against its 15-`[PASS]` / 0-`[FAIL]` baseline</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/dots-hyprland-workflow.md` contains the heading `## Profiles: safe vs full`, positioned between `## Prerequisites` and `## Canonical path`.
    - The file quotes the live `SAFE_DEFAULTS` array byte-for-byte, verified by extracting it from `arch/dots-hyprland.sh:12` at check time rather than by a hardcoded string.
    - Exactly three table rows begin with a backticked axis flag: `--skip-hyprland`, `--core`, `--skip-sysupdate`.
    - The file contains a sentence in which the word "default" appears with "safe" or `SAFE_DEFAULTS` — the D-07 statement.
    - The file cites `11-DISPOSITIONS.md` for the accept-remove decision and does not describe the removal as out of scope.
    - `Waybar`, `swaync` and a word-boundary `rofi` each appear at least once; non-allowlisted `chrome` tokens number 0.
    - The wrapper's full flag list is not reproduced: the file names no wrapper flag beyond `--core`, `--skip-hyprland`, `--skip-sysupdate`, `--full`, `--skip-backup`, `--allow-skip-backup`, `--dry-run`, `--force`, `--install-missing`.
    - `./scripts/phase13-d19-assert.sh` exits 0 with zero `[FAIL]` lines.
  </acceptance_criteria>
  <done>A reader can decide between the two profiles from one section: what safe injects, what full drops, what each does to the machine, and the fact that safe is still the wrapper default.</done>
</task>

<task type="auto">
  <name>Task 3: Insert the required pre-install gate and renumber the spine so the Outline resolves</name>
  <read_first>
    - `docs/dots-hyprland-workflow.md` — the full `## Outline` list (lines 36-48) and every `## N.` heading in the file, in its post-Task-2 state
    - `.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md` — the impact inventory this section cites as SoT for what a full install touches (INV-01..04); read its headings, not its full body
    - `.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` — the dispositions this section cites as SoT for per-surface decisions; read the section list and section 6
    - `.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md` — "Recommended structure of the rewritten playbook" (lines 307-330), the target spine
    - `.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md` — the "Heading + numbering pattern" section, which fixes the anchor form
  </read_first>
  <files>docs/dots-hyprland-workflow.md</files>
  <action>
Give the document its final spine and put the gate where a cold machine actually hits it.

**Insert the gate section.** Immediately before the install section, add a new top-level section headed `## 3. Required gate before any full install`. Write it as a gate, not as advice: a cold machine must not reach `--full` from this document without passing it (D-13, ADOPT-01 is a process gate). Three ordered steps in prose or a short numbered list — inventory what a full install would touch on *this* machine; decide a disposition for each surface it would change; only then adopt. Cite `.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md` as the source of truth for what a full install touches, and `.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` as the source of truth for per-surface dispositions, both as bare inline backticked paths in the body per the repo's body-citation form. Name them as this machine's worked instance — the gate was run here in Phases 10 and 11 — so a reader on a different machine understands they are an example of the artifact to produce, not a substitute for producing it. Add one sentence stating that the safe profile does not require this gate, which is what makes the gate specifically a full-profile precondition. Do not reproduce the inventory's or the dispositions' content.

**Renumber the spine.** After the insertion the numbered sections are, in order: `## 1. Clone & recursive submodule init`, `## 2. Verify fork remotes & pin`, `## 3. Required gate before any full install`, `## 4. Install via the thin wrapper` (the section previously numbered 3 and titled for the dry-run-to-live path), `## 5. Session model after a full install` (the section `15-01` retitled to `Session model & verification`), `## 10. Update contract (pin-bump)`, `## 11. Non-goals / non-primary paths`. Renumber only; do not rewrite the bodies of the install, session or update sections in this task — plans `15-04` and `15-05` own those. Numbers 6 through 9 are deliberately not used yet; `15-04` adds sections 6 and 7 and `15-05` adds 8 and 9, so the numbering lands contiguous at the end of the phase without a renumber pass later.

**Rewrite the Outline.** List exactly the **numbered** sections that exist in the file at this commit, in file order, one Outline entry per `## N.` heading and no others. Drop the trailing `See also` entry the Outline carries today: `See also` is a closing reference list, not a step on the spine, and keeping it in the list makes the Outline entry count differ from the section count for no reader benefit. Use the existing numbered-link form with GitHub-slug anchors — lowercase, characters outside `[a-z0-9 -]` dropped, spaces to hyphens, so a dropped `&` or `.` leaves a doubled hyphen exactly as `#2-verify-fork-remotes--pin` does today. Do not list sections 6 through 9 yet; an Outline entry pointing at a heading that does not exist would break in-page link integrity at this commit, and D-23 covers in-page anchors. `15-05` writes the final Outline once every section exists.

**Fix one line inside the install section while renumbering.** The hooks paragraph currently says a successful install gives personal hypr the hook lines in **both** the live `~/.config/hypr/hyprland.conf` and the repo copy. Post-adopt, the live file does not exist, so the wrapper's target list resolves to the repo copy alone. Rewrite that sentence to say the wrapper enables the hooks in whichever of the two target files exist, that after a full adopt only the repo copy remains, and point at section 9 for what that means. Leave the rest of the install section for `15-04`.
  </action>
  <verify>
    <automated>grep -qF '## 3. Required gate before any full install' docs/dots-hyprland-workflow.md && grep -q '10-INVENTORY.md' docs/dots-hyprland-workflow.md && grep -q '11-DISPOSITIONS.md' docs/dots-hyprland-workflow.md && GATE="$(grep -n '10-INVENTORY.md' docs/dots-hyprland-workflow.md | head -1 | cut -d: -f1)" && INST="$(grep -nE '^\s*\./arch/dots-hyprland\.sh install' docs/dots-hyprland-workflow.md | head -1 | cut -d: -f1)" && test -n "$GATE" && test -n "$INST" && test "$GATE" -lt "$INST"</automated>
    <fails_when>non-zero exit — the gate heading is absent, either cited artifact is unmentioned, or the computed line number of the first `10-INVENTORY.md` mention is not strictly less than the line number of the first `./arch/dots-hyprland.sh install` command line, which is what "the gate was written as a postscript instead of a precondition" looks like</fails_when>
    <automated>ok=1; while read -r a; do h="${a#\#}"; grep -oE '^#{2,3} .*' docs/dots-hyprland-workflow.md | sed 's/^#* //' | tr 'A-Z' 'a-z' | sed 's/[^a-z0-9 -]//g; s/ /-/g' | grep -qx "$h" || { echo "MISSING ANCHOR: $h"; ok=0; }; done < <(grep -oE '\(#[a-z0-9-]+\)' docs/dots-hyprland-workflow.md | tr -d '()' | sort -u); test "$ok" -eq 1 && test "$(grep -cE '^## [0-9]+\. ' docs/dots-hyprland-workflow.md)" -eq 7 && test "$(grep -cE '^[0-9]+\. \[' docs/dots-hyprland-workflow.md)" -eq 7</automated>
    <fails_when>a `MISSING ANCHOR:` line is printed (an Outline entry points at a slug no heading produces), or either count is not 7 — the file must hold exactly seven numbered `## N.` sections and seven numbered Outline entries at this commit, so a renumber that skipped a heading or an Outline that lists a section 6 through 9 that does not exist yet both fail here</fails_when>
    <automated>for f in docs/dots-hyprland-workflow.md docs/phase14-adopt-runbook.md README.md; do base="$(dirname "$f")"; grep -o '](\.\{0,2\}/\?[^)#][^)]*)' "$f" | sed 's/](\(.*\))/\1/' | grep -v '^http' | while read -r l; do [ -e "$base/$l" ] || [ -e "$l" ] || { echo "BROKEN $f -> $l"; exit 1; }; done || exit 1; done && git diff --quiet HEAD -- arch/ scripts/ .config/ stow/ vendor/ .planning/STATE.md .planning/ROADMAP.md</automated>
    <fails_when>a `BROKEN <file> -> <path>` line is printed, or `git diff --quiet` reports an uncommitted change under a fenced directory or in `STATE.md` / `ROADMAP.md`</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/dots-hyprland-workflow.md` contains the heading `## 3. Required gate before any full install`.
    - The first line number mentioning `10-INVENTORY.md` is strictly less than the first line number holding a `./arch/dots-hyprland.sh install` command.
    - Both `10-INVENTORY.md` and `11-DISPOSITIONS.md` are cited in the gate section body as bare inline backticked paths.
    - The gate section states that the safe profile does not require it.
    - The file contains exactly seven `## N.` headings, numbered 1, 2, 3, 4, 5, 10, 11 in file order.
    - The `## Outline` holds exactly seven numbered entries, one per existing numbered section, in file order, with no trailing `See also` entry.
    - Every in-page anchor referenced from the Outline resolves to a heading in the file.
    - Every relative Markdown link in the three prose docs resolves.
    - The install section's hooks paragraph no longer asserts that hooks land in both the live and repo `hyprland.conf`, and points at section 9.
    - No file under `arch/`, `scripts/`, `.config/`, `stow/`, `vendor/` and neither `.planning/STATE.md` nor `.planning/ROADMAP.md` has an uncommitted diff.
  </acceptance_criteria>
  <done>The gate is a precondition rather than a postscript — provably, by line number — and the document's spine is numbered with a resolving Outline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published prose → operator's shell | Fenced commands in this section are pasted verbatim into a root-capable shell. |
| published prose → the operator's choice of profile | This plan writes the section a reader uses to decide whether to run `--full` on a machine they care about. Framing here is the control. |
| playbook narrative → executable SoT | The flag-axis table paraphrases `arch/dots-hyprland.sh`; drift between them is silent unless asserted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-06 | Spoofing | `## Profiles: safe vs full` framing | high | mitigate | D-07's explicit statement that safe is still the wrapper default and `--full` is opt-in, asserted by `grep -qiE 'default.*(safe\|SAFE_DEFAULTS)'`; the walkthrough is labelled a documentation choice in the same section, not a footnote |
| T-15-07 | Tampering | flag-axis table drifting from `arch/dots-hyprland.sh:12` | medium | mitigate | The verify extracts `SAFE_DEFAULTS` from the script at check time with `grep -oP` and requires the doc to quote it with `grep -qF`, so a future wrapper change breaks the check rather than the reader |
| T-15-08 | Denial of Service | a reader reaching `--full` without the inventory gate | high | mitigate | The gate is a numbered section placed before the install section, and the ordering is asserted by comparing line numbers rather than by review |
| T-15-09 | Tampering | `arch/`, `scripts/`, `.config/`, `stow/`, `vendor/` | high | mitigate | `git diff --quiet HEAD` scope-fence guard on every task in this plan |
| T-15-10 | Repudiation | Waybar/rofi/swaync removal described as out of scope | medium | mitigate | The section must cite `11-DISPOSITIONS.md` section 6 and state the removal was explicitly accepted; asserted by grep for the citation |
| T-15-SC | Tampering | package-manager installs | low | accept | No `npm` / `pip` / `cargo` / `pacman` install task exists in this phase (`15-RESEARCH.md` § Package Legitimacy Audit: not applicable) |
</threat_model>

## Artifacts this phase produces

Created by **this plan**, all in `docs/dots-hyprland-workflow.md`:

| Artifact | Kind | Anchor |
|----------|------|--------|
| `## Profiles: safe vs full` | new heading | `#profiles-safe-vs-full` |
| the three-row flag-axis table | new table | inside `#profiles-safe-vs-full` |
| `## 3. Required gate before any full install` | new heading | `#3-required-gate-before-any-full-install` |
| `## 4. Install via the thin wrapper` | renamed/renumbered heading | `#4-install-via-the-thin-wrapper` |
| `## 5. Session model after a full install` | renumbered heading | `#5-session-model-after-a-full-install` |
| `## 10. Update contract (pin-bump)` | renumbered heading | `#10-update-contract-pin-bump` |
| `## 11. Non-goals / non-primary paths` | renumbered heading | `#11-non-goals--non-primary-paths` |

## Flagged Assumptions

| # | Item | Status | Why it is flagged |
|---|------|--------|-------------------|
| C-1 | `DOC-03` edge coverage | **unresolved** (`unclassified`) | The edge probe could not classify this requirement; no acceptance criterion was auto-derived from it. DOC-03 coverage in this plan comes from the goal-backward truths above. Not auto-resolved with a backstop, not dropped. |
| C-2 | `DOC-04` edge coverage | **unresolved** (`unclassified`) | Same; DOC-04 is covered by plan `15-04` from `13-SOT-APPLY.md`. |
| A-02-1 | Section numbers 6 through 9 are reserved but unused at the end of this plan | assumption, deliberate | Avoids a second renumber pass in `15-04`/`15-05`. The Outline lists only existing sections at each commit, so in-page link integrity (D-23) holds at every commit rather than only at the end. |
| A-02-2 | The flag-list ceiling — nine flags — is the planner's reading of D-12 | assumption | D-12 forbids reproducing the full wrapper flag list but sets no number. The nine named are exactly those the phase's own decisions require (D-11's three axes, `--full`, D-14's backup pair, plus `--dry-run`, `--force`, `--install-missing` already in the doc). If a section genuinely needs a tenth, raise it rather than silently widening the gate. |

<verification>
- The gate-ordering check is a line-number comparison, not a reading. It is the only mechanical way to assert D-13's "before, not after".
- `\brofi\b` word boundaries are mandatory in every `rofi` assertion in this plan: this is the plan that fills the file with the word "profile", and `profile` contains `rofi`.
- The `SAFE_DEFAULTS` agreement check extracts from the script rather than hardcoding, so it keeps working after a future wrapper change and fails loudly when the doc drifts.
- `./scripts/phase14-verify.sh` is not run here: it asserts a clean working tree (D-35) and would fail against a pre-commit task verify. It runs at the phase gate in `15-06`.
</verification>

<success_criteria>
- A reader can choose a profile from one section and knows safe is still the default.
- A cold machine cannot reach `--full` from this document without passing the gate first, and the ordering is provable.
- Two of D-10's three stale claims are gone; the third dies with section 4's rewrite in `15-04`.
- Every Outline anchor resolves at this commit.
</success_criteria>

<output>
Create `.planning/phases/15-playbook-safe-vs-full/15-02-SUMMARY.md` when done.
</output>