---
phase: 15-playbook-safe-vs-full
plan: 05
type: execute
wave: 4
depends_on: ["15-04"]
files_modified:
  - docs/dots-hyprland-workflow.md
autonomous: true
requirements: [DOC-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A reader learns what a full adopt costs them — the session-bootstrap unit, `wl-clip-persist`, and four workspace-pinned autostarts — stated as a loss that happened, with the screen-share consequence framed as a possibility rather than a certainty (D-17)."
    - "A reader learns that the repo copy of `hyprland.conf` is simultaneously a rollback source, frozen evidence and the wrapper's only remaining hook target, and that escalating a rollback to tier 2 destroys one of those roles unless the file is copied aside first (D-18)."
    - "A reader is told never to rotate the backup after a full adopt, because rotation renames away rollback tier-1 source 3 (D-19)."
    - "The non-goals table no longer claims the Waybar/rofi/swaync cutover and the `hyprland.lua` takeover are out of scope for this milestone — both happened (D-10)."
    - "Every Outline entry, every in-page anchor and every relative link in the finished playbook resolves (D-23)."
  artifacts:
    - path: "docs/dots-hyprland-workflow.md"
      provides: "Known-losses section, the WR-02 three-roles section, a corrected non-goals table, the final Outline and See also"
      contains: "## 9. Three roles of the repo hyprland.conf"
  key_links:
    - from: "docs/dots-hyprland-workflow.md"
      to: "docs/phase14-adopt-runbook.md"
      via: "the three-roles section cites section 14's tier ordering for the hazard it describes, and See also links the runbook"
      pattern: "phase14-adopt-runbook\\.md"
    - from: "docs/dots-hyprland-workflow.md"
      to: "stow/systemd"
      via: "the known-losses section names where the surviving session unit file lives"
      pattern: "stow/"
    - from: "docs/dots-hyprland-workflow.md"
      to: ".planning/ROADMAP.md"
      via: "See also cites this phase's success criteria, not Phase 9's"
      pattern: "ROADMAP\\.md"
  prohibitions:
    - statement: "The playbook must not present the full profile as the default or the recommended-by-omission path; the wrapper default is safe and `--full` is opt-in (D-07)."
    - statement: "No operator-facing doc may name a rollback source that does not hold the recoverable pre-adopt config, and no destructive or recovery-destroying command may be documented without its guard."
    - statement: "The retired session surfaces must not be misrepresented: write `Waybar`, `rofi` and `swaync` literally rather than as a collective noun, and state the D-38 screen-share consequence as a possibility rather than a certainty."
    - statement: "Frozen historical planning records must not be rewritten to agree with today's truth; staleness in them is flagged in the sweep record, never edited away (D-22)."
---

<objective>
Close the playbook: the honest cost of a full adopt (D-17), the one real ordering hazard a recovering operator can walk into (D-18, D-19), a non-goals table that stops claiming two completed things are out of scope (D-10), and a finished Outline and See also whose every link resolves (D-23).

The three-roles section is the phase's answer to the WR-02 concern carried in `STATE.md`. Research demonstrated it live rather than reasoning about it abstractly: post-adopt, live `~/.config/hypr/hyprland.conf` no longer exists, so the wrapper's hook-target list resolves to the repo copy alone, and `uninstall --dry-run` confirms it would delete the ii hooks there. That changes the file's sha256, which is what makes rollback tier 2 destroy rollback tier-1 source 2.

Purpose: the last of DOC-03, plus the link integrity D-23 requires across the rewritten document.
Output: sections 8 and 9 written, section 11 corrected, the final 11-entry Outline, and an updated See also.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-playbook-safe-vs-full/15-CONTEXT.md
@.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md
@.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md
@.planning/phases/15-playbook-safe-vs-full/15-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write section 8 — known losses after the full adopt</name>
  <read_first>
    - `docs/dots-hyprland-workflow.md` — the boundary after `## 7. Verify after login`, in its post-`15-04` state
    - `docs/phase14-adopt-runbook.md` — lines 281-304 (`## 13. Known losses (D-38)`), the phrasing pattern this section follows and the source of the four autostart names
    - `.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md` — "Known losses — Verified Ground Truth" (lines 641-666), the live-state table and the `AvailableSourceTypes` nuance
    - `.planning/phases/14-live-full-adopt-verify/14-VERIFICATION.md` — warning 3, the origin of the still-open D-38 item
    - `.planning/STATE.md` — the "Concerns carried forward" entry recording D-38 as open with no owning phase
    - `.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md` — the "Known losses phrasing pattern" section
  </read_first>
  <files>docs/dots-hyprland-workflow.md</files>
  <action>
Insert a top-level section headed `## 8. Known losses after the full adopt` immediately after section 7. Follow the runbook's known-losses phrasing pattern: lead each item with a bolded noun phrase, then the honest qualifier — what survives, and what exactly was lost — so the reader neither over- nor under-estimates the damage.

Four items:

1. **The personal session-unit autostart.** The `hyprland-session.service` unit file survives — it lives under `stow/systemd/` and the symlink in `~/.config/systemd/user/` is untouched. What died with the renamed conf is the `exec-once` line that started it, so `graphical-session.target` is now inactive. Consequence: the xdg-desktop-portal ScreenCast path depends on that target, so screen share **may** stop working. Write "may", not "is broken": the portal still answers with an unchanged `AvailableSourceTypes`, so what is lost is the session bootstrap, not the portal (research assumption A6). This is not a deletion.

2. **`wl-clip-persist`.** Not running; same cause.

3. **The four workspace-pinned autostarts.** Name them literally, as the runbook does: `google-chrome-stable` on workspace 1, `kitty -e tmux` on workspace 1, `btop` on its special workspace, and `discord` on `special:social`. Do not summarise them as a category noun. `google-chrome-stable` is the only browser token this document may contain.

4. **`hyprpaper`.** Stopped but installed — the binary is on PATH and `hyprpaper.conf` survives; nothing starts it. Wallpaper is Quickshell's job under the ii shell, so this is a changed owner rather than breakage.

Close with two sentences and no more. First: this phase documents these losses and owns no fix — restoring the session bootstrap, `wl-clip-persist` and the four autostarts is unowned work, and the sweep record in `.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md` carries it as a deferred item. Second: `scripts/phase14-verify.sh` reports the session-target loss as a `[FINDING]` rather than a failure, which is why its expected output in section 7 is one finding rather than zero.

Do not write a restore path for `Waybar`, `rofi` or `swaync` — nothing has verified one since the adopt, and it is a deferred idea. Do not describe the losses as a milestone goal or as an intended outcome; they are an accepted cost (Phase 11 D-11).
  </action>
  <verify>
    <automated>grep -qF '## 8. Known losses after the full adopt' docs/dots-hyprland-workflow.md && grep -q 'hyprland-session.service' docs/dots-hyprland-workflow.md && grep -q 'graphical-session.target' docs/dots-hyprland-workflow.md && grep -q 'wl-clip-persist' docs/dots-hyprland-workflow.md && grep -q 'google-chrome-stable' docs/dots-hyprland-workflow.md && grep -q 'btop' docs/dots-hyprland-workflow.md && grep -q 'discord' docs/dots-hyprland-workflow.md && grep -q 'hyprpaper' docs/dots-hyprland-workflow.md && grep -q 'stow/' docs/dots-hyprland-workflow.md</automated>
    <fails_when>non-zero exit — the section heading is absent, or any of the enumerated losses is unstated: the session unit, the inactive target, the clipboard persister, any of the four literally-named autostarts, the wallpaper daemon, or the `stow/` location where the unit file survives</fails_when>
    <automated>grep -qiE 'screen ?share[^.]*\bmay\b|\bmay\b[^.]*screen ?share' docs/dots-hyprland-workflow.md && test "$(grep -ciE 'screen ?share is broken|screen ?share no longer works|breaks screen ?share' docs/dots-hyprland-workflow.md)" -eq 0 && test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' docs/dots-hyprland-workflow.md | grep -vx 'google-chrome-stable' | wc -l)" -eq 0</automated>
    <fails_when>the hedged phrasing is absent (no sentence pairs screen share with "may"), or an overstated phrasing matched with a count above 0, or a non-allowlisted `chrome` token appeared — the four autostarts must be named literally and the browser only by its full binary name</fails_when>
    <automated>test "$(grep -cE '^## [0-9]+\. ' docs/dots-hyprland-workflow.md)" -eq 10 && grep -q '15-DOC-SWEEP.md' docs/dots-hyprland-workflow.md && grep -q 'phase14-verify.sh' docs/dots-hyprland-workflow.md && git diff --quiet HEAD -- arch/ scripts/ .config/ stow/ vendor/ .planning/STATE.md .planning/ROADMAP.md</automated>
    <fails_when>the numbered-heading count is not 10 (sections 1-8, 10, 11), the sweep record is not cited as the carrier of the deferred fix, the verify script is not named as the reporter of the finding, or `git diff --quiet` reports an uncommitted change under a fenced directory or in `STATE.md` / `ROADMAP.md` — note `stow/` is named in prose but must not be modified</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/dots-hyprland-workflow.md` contains `## 8. Known losses after the full adopt`, positioned after section 7.
    - All four losses are stated: the session unit and inactive `graphical-session.target`, `wl-clip-persist`, the four autostarts, `hyprpaper`.
    - The four autostarts are named literally: `google-chrome-stable`, `kitty -e tmux`, `btop`, `discord`.
    - The section states the unit file survives under `stow/systemd/` and that this is not a deletion.
    - A sentence pairs screen share with "may"; no sentence asserts screen share is broken.
    - The section states that this phase owns no fix and that `15-DOC-SWEEP.md` carries the deferred item.
    - The section explains why section 7's expected output is one finding rather than zero, naming `scripts/phase14-verify.sh`.
    - No restore path for `Waybar`, `rofi` or `swaync` is described.
    - The file contains exactly 10 `## N.` headings; non-allowlisted `chrome` tokens number 0.
  </acceptance_criteria>
  <done>A reader of the playbook knows exactly what a full adopt cost, in the same terms `scripts/phase14-verify.sh` reports it, without overstatement and without a fix this phase does not own.</done>
</task>

<task type="auto">
  <name>Task 2: Write section 9 — the three roles of the repo hyprland.conf, and the post-adopt rotate prohibition</name>
  <read_first>
    - `docs/dots-hyprland-workflow.md` — the boundary after `## 8. Known losses after the full adopt`, in its post-Task-1 state
    - `.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md` — "Pitfall 4" (lines 426-454), which demonstrates the tier-2-destroys-tier-1-source-2 mechanism live, and "Pitfall 3" (lines 414-425) for the rotation hazard
    - `.config/hypr/hyprland.conf` — the repo copy itself: the `exec-once` hook at line 67 and the `env` hook at line 111. READ ONLY
    - `arch/dots-hyprland.sh` — `list_hypr_ii_hook_target_files`, the two-candidate list that resolves to the repo copy alone post-adopt. READ ONLY
    - `docs/phase14-adopt-runbook.md` — `## 14. Rollback`, in its post-`15-03` state, for the tier ordering this section cites
    - `.planning/STATE.md` — the WR-02 and IN-11 entries under "Concerns carried forward"
    - `.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md` — the "Prohibition / hazard phrasing pattern" section
  </read_first>
  <files>docs/dots-hyprland-workflow.md</files>
  <action>
Insert a top-level section headed `## 9. Three roles of the repo hyprland.conf` immediately after section 8. This is D-18's disambiguation of the WR-02 concern, and it has a real ordering hazard to describe rather than an abstract role conflict.

**Name the three roles of `.config/hypr/hyprland.conf` in this repo, and say which profile each applies under.**
1. *Rollback source.* It is section 14 tier-1 source 2 in the runbook — the pre-adopt archive committed before anything mutated, whose sha256 matches the recorded fixture.
2. *Frozen evidence.* It is the D-36 record of what the machine's config was before the adopt; that is why it is committed rather than regenerated.
3. *Hook-injection target.* The wrapper enables the ii hooks in whichever of its two candidate files exist — the live `~/.config/hypr/hyprland.conf` and this repo copy. Under the safe profile both exist and both get the hooks. Under the full profile the live file was renamed to `.old`, so this repo copy is the wrapper's **only** remaining target. The full path never uses it as a live file.

**Then the hazard, in the repo's bold-imperative prohibition voice.** Because role 3 is now the only remaining target, `./arch/dots-hyprland.sh uninstall` deletes the two hook lines from this file. Deleting them changes its sha256, and `scripts/phase14-verify.sh`'s tier-1 source 2 check flips to a failure — so escalating a rollback from tier 1 to tier 2 destroys one of tier 1's own sources while you are still recovering. State the mitigation as an ordered instruction: copy this file aside before escalating to tier 2. Add the bounding fact so the reader does not over-react: the content is recoverable from git history, and a forward install reports the hooks as already active and changes nothing, so only `uninstall` triggers this.

**Then the rotation prohibition (D-19), same voice.** After a full adopt, `~/ii-original-dots-backup` is rollback tier-1 source 3. Running `./scripts/phase14-preflight.sh --rotate-backup` now renames it away. State plainly that it must not be run post-adopt. Record that the preflight script still prints that rotation as mandatory remediation, that this is a known stale message tracked as IN-11 with its fix deferred to a phase that owns the script, and that a documentation phase does not edit it. Name `scripts/phase14-preflight.sh` by path.

Cite `docs/phase14-adopt-runbook.md` section 14 for the tiers themselves and copy none of them into this file (D-02, D-14). Do not present the upstream vendor tree's removal subcommand as an alternative anywhere.
  </action>
  <verify>
    <automated>grep -qF '## 9. Three roles of the repo hyprland.conf' docs/dots-hyprland-workflow.md && grep -q 'D-36' docs/dots-hyprland-workflow.md && grep -q -- '--rotate-backup' docs/dots-hyprland-workflow.md && grep -q 'IN-11' docs/dots-hyprland-workflow.md && grep -q 'scripts/phase14-preflight.sh' docs/dots-hyprland-workflow.md && grep -q 'uninstall' docs/dots-hyprland-workflow.md && grep -q 'sha256' docs/dots-hyprland-workflow.md && grep -qF 'phase14-adopt-runbook.md' docs/dots-hyprland-workflow.md</automated>
    <fails_when>non-zero exit — the section heading is absent, the frozen-evidence role is not tied to D-36, the rotation prohibition or its IN-11 provenance is missing, the preflight script is not named by path, the `uninstall` mechanism behind the hazard is unstated, no sha256 identity argument is made, or the runbook is not cited for the tiers</fails_when>
    <automated>B="$(git log -1 --format=%H -- .planning/phases/15-playbook-safe-vs-full/15-01-PLAN.md)" && test -n "$B" && test "$(grep -cE '^## [0-9]+\. ' docs/dots-hyprland-workflow.md)" -eq 11 && test "$(grep -c 'UPSTREAM-UNINSTALL\|upstream-dangerous' docs/dots-hyprland-workflow.md)" -eq 0 && test -z "$(git diff --name-only "$B"..HEAD -- scripts/phase14-preflight.sh)"</automated>
    <fails_when>the numbered-heading count is not 11 (the spine is now complete: 1 through 11), the count of dangerous-override tokens is above 0, or `git diff --name-only "$B"..HEAD` prints `scripts/phase14-preflight.sh` — writing this section is precisely when an executor is tempted to fix the stale message D-19 freezes</fails_when>
    <automated>git diff --quiet HEAD -- arch/ scripts/ .config/ stow/ vendor/ .planning/STATE.md .planning/ROADMAP.md && ./scripts/phase13-d19-assert.sh >/tmp/p15-05-d19.txt 2>&1 && test "$(grep -c '^\[FAIL\]' /tmp/p15-05-d19.txt)" -eq 0</automated>
    <fails_when>non-zero exit — an uncommitted change exists under a fenced directory or in `STATE.md` / `ROADMAP.md` (this section describes `.config/hypr/hyprland.conf` in detail, which is the moment an executor is tempted to edit it), or `scripts/phase13-d19-assert.sh` prints a `[FAIL]` line against its 15-`[PASS]` / 0-`[FAIL]` baseline</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/dots-hyprland-workflow.md` contains `## 9. Three roles of the repo hyprland.conf`, positioned after section 8.
    - All three roles are named, each tied to the profile under which it applies, with role 3 stating that under full this repo copy is the wrapper's only remaining hook target.
    - The tier-2-destroys-tier-1-source-2 mechanism is stated with its cause (`uninstall` deletes the hooks, changing the file's sha256) and its mitigation (copy the file aside before escalating).
    - The bounding facts are present: content recoverable from git history, and a forward install changes nothing.
    - The post-adopt rotation prohibition is stated, names `--rotate-backup` and `scripts/phase14-preflight.sh`, and records IN-11 as a deferred fix belonging to a phase that owns the script.
    - `docs/phase14-adopt-runbook.md` is cited for the tiers, and no tier is reproduced.
    - The file contains exactly 11 `## N.` headings and zero dangerous-override tokens.
    - `git diff --name-only "$B"..HEAD -- scripts/phase14-preflight.sh` prints nothing.
  </acceptance_criteria>
  <done>WR-02 is disambiguated in the operator's own document, with the ordering hazard stated as an instruction rather than a caution, and IN-11 is recorded as deferred rather than silently fixed.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the non-goals table, write the final Outline and See also, and prove every link resolves</name>
  <read_first>
    - `docs/dots-hyprland-workflow.md` — the whole file in its post-Task-2 state, specifically `## Outline`, `## 11. Non-goals / non-primary paths` and `## See also`
    - `.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md` — the `docs/dots-hyprland-workflow.md` sweep table (lines 671-696), which names the two false non-goals rows and the stale cross-reference at line 296
    - `.planning/REQUIREMENTS.md` — lines 74 and 82, the two Out of Scope rows the table must stay aligned with, and the DOC-03 / DOC-04 wording
    - `.planning/ROADMAP.md` — the Phase 15 entry, whose success criteria `See also` must now cite instead of Phase 9's
    - `.planning/phases/11-disposition-decisions/11-DISPOSITIONS.md` — section 6, the explicit acceptance that fired the CUT-01 escape clause
    - `.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md` — the "Non-goals table" and "Citation pattern" sections
  </read_first>
  <files>docs/dots-hyprland-workflow.md</files>
  <action>
Finish the document.

**Non-goals table (section 11).** Keep the table's shape — the Path / idea, Status, Why columns, the bolded first column, and the closing bottom-line sentence about updating with pin-bump rather than exp-merge or an online cache install. Rewrite three rows and leave the rest:

- The row claiming the full `Waybar` / `rofi` / `swaync` cutover is out of scope this milestone is false: the removal was explicitly accepted by Phase 11 D-11, which is the escape clause the `REQUIREMENTS.md` CUT-01 row provides for. Rewrite the row so its status records that the removal happened under the full profile with Phase 11's acceptance, and its Why states that what remains deferred is the Waybar custom module ports (CUST-01 through CUST-04), not the cutover.
<!-- planner-discipline-allow: Out of scope this milestone -->
- The row claiming the full `hyprland.lua` / ii hypr tree takeover is out of scope this milestone is directly false — it is exactly what the Phase 14 adopt did. Rewrite the row so its status records that this is now the full profile's session model, and its Why points at section 5, with one clause noting the safe profile still keeps personal hypr as source of truth via `--skip-hyprland`.
- The wrapper `verify` subcommand row still sends the reader to manual dual-run checks in section 4. Those checks no longer exist there. Point it at section 7 and at `scripts/phase14-verify.sh` instead.

Leave the `exp-merge` / `exp-update` row, the online-cache row, the auto-bump row and the reimplementing-package-lists row exactly as they are — all four are still true. The `exp-merge` mention is prose naming a *refused* path, not a command line, and must stay.

**Final Outline.** Rewrite `## Outline` to list all eleven numbered sections in file order, one entry per `## N.` heading, no `See also` entry, using the GitHub-slug anchor form the file already uses — lowercase, characters outside `[a-z0-9 -]` dropped, spaces to hyphens, so a dropped `&`, `.` or `:` leaves the doubled hyphen the existing anchors already show.

**See also.** Keep the existing entries for `./arch/dots-hyprland.sh help`, `vendor/dots-hyprland`, `PROJECT.md` and the root `README.md`. Change the `ROADMAP.md` gloss from Phase 9's success criteria to this phase's, and the `REQUIREMENTS.md` gloss from DOC-01 / DOC-02 to DOC-03 / DOC-04. Add four linked entries, following the existing backticked-link-text and em-dash-gloss form, using repo-relative `../` paths from `docs/`: `docs/phase14-adopt-runbook.md` (adopt-window record and the three-tier rollback), `10-INVENTORY.md` (what a full install touches), `11-DISPOSITIONS.md` (per-surface adopt dispositions) and `13-SOT-APPLY.md` (overlay authoring source of truth and apply command). Copy the two link lines the runbook already carries for the dispositions and the overlay SoT rather than re-deriving their paths; both documents live in `docs/`, so the depth is identical.
  </action>
  <verify>
    <automated>test "$(grep -c 'Out of scope this milestone' docs/dots-hyprland-workflow.md)" -eq 0 && grep -q 'CUST-01' docs/dots-hyprland-workflow.md && grep -q '11-DISPOSITIONS.md' docs/dots-hyprland-workflow.md && grep -q '10-INVENTORY.md' docs/dots-hyprland-workflow.md && grep -q '13-SOT-APPLY.md' docs/dots-hyprland-workflow.md && grep -q 'DOC-03' docs/dots-hyprland-workflow.md && grep -q 'DOC-04' docs/dots-hyprland-workflow.md && test "$(grep -c 'Phase 9 success criteria' docs/dots-hyprland-workflow.md)" -eq 0 && grep -q 'exp-merge' docs/dots-hyprland-workflow.md</automated>
    <fails_when>non-zero exit — the milestone-scope claim survives on any row, the deferred module ports are not named, any of the three cited planning artifacts is missing from the document, the requirement IDs were not updated from DOC-01/DOC-02, the stale Phase 9 gloss survives in See also, or the `exp-merge` non-goals row was deleted rather than left alone</fails_when>
    <automated>test "$(grep -cE '^## [0-9]+\. ' docs/dots-hyprland-workflow.md)" -eq 11 && test "$(grep -cE '^[0-9]+\. \[' docs/dots-hyprland-workflow.md)" -eq 11 && ok=1; while read -r a; do h="${a#\#}"; grep -oE '^#{2,3} .*' docs/dots-hyprland-workflow.md | sed 's/^#* //' | tr 'A-Z' 'a-z' | sed 's/[^a-z0-9 -]//g; s/ /-/g' | grep -qx "$h" || { echo "MISSING ANCHOR: $h"; ok=0; }; done < <(grep -oE '\(#[a-z0-9-]+\)' docs/dots-hyprland-workflow.md | tr -d '()' | sort -u); test "$ok" -eq 1</automated>
    <fails_when>either count is not 11 — the eleven numbered sections and eleven Outline entries must correspond one to one, so a section without an entry or an entry without a section both fail here — or a `MISSING ANCHOR:` line is printed because an Outline slug does not match any heading under the GitHub slug transform</fails_when>
    <automated>for f in docs/dots-hyprland-workflow.md docs/phase14-adopt-runbook.md README.md; do base="$(dirname "$f")"; grep -o '](\.\{0,2\}/\?[^)#][^)]*)' "$f" | sed 's/](\(.*\))/\1/' | grep -v '^http' | while read -r l; do [ -e "$base/$l" ] || [ -e "$l" ] || { echo "BROKEN $f -> $l"; exit 1; }; done || exit 1; done && for p in .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md .planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md docs/phase14-adopt-runbook.md; do grep -qF "$(basename "$p")" docs/dots-hyprland-workflow.md && { test -f "$p" || { echo "CITED BUT MISSING: $p"; exit 1; }; }; done</automated>
    <fails_when>a `BROKEN <file> -> <path>` line is printed (a relative Markdown link in one of the three prose docs does not resolve), or a `CITED BUT MISSING: <path>` line is printed (the playbook cites a planning artifact by basename that does not exist at its expected path) — this is the D-23 gate, and it is the check that catches a `../` depth error in the four new See also entries</fails_when>
    <automated>test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' docs/dots-hyprland-workflow.md | grep -vx 'google-chrome-stable' | wc -l)" -eq 0 && test "$(grep -cE '\brofi\b' docs/dots-hyprland-workflow.md)" -ge 2 && test "$(grep -c 'rsync' docs/dots-hyprland-workflow.md)" -eq 0 && git diff --quiet HEAD -- arch/ scripts/ .config/ stow/ vendor/ .planning/STATE.md .planning/ROADMAP.md</automated>
    <fails_when>a non-allowlisted `chrome` token exists, the word-boundary `rofi` count is below 2 (the trio is named in the profiles section and the non-goals table, so a collective noun crept into one of them — the `\b` boundaries are mandatory because this document is full of the word "profile"), a `rsync` occurrence exists, or `git diff --quiet` reports an uncommitted change under a fenced directory or in `STATE.md` / `ROADMAP.md`</fails_when>
  </verify>
  <acceptance_criteria>
    - No row of the non-goals table claims anything is out of scope this milestone; the count of that phrase in the file is 0.
    - The cutover row records the accepted removal and names `CUST-01` as what remains deferred; the takeover row records the full profile's session model and points at section 5; the `verify`-subcommand row points at section 7 and `scripts/phase14-verify.sh`.
    - The `exp-merge` / `exp-update`, online-cache, auto-bump and reimplementing-package-lists rows survive unchanged.
    - `## Outline` holds exactly 11 numbered entries matching the file's 11 `## N.` headings one to one, with no `See also` entry.
    - Every in-page anchor referenced from the Outline resolves under the GitHub slug transform.
    - `See also` cites this phase's ROADMAP success criteria and DOC-03 / DOC-04, and adds linked entries for the runbook, `10-INVENTORY.md`, `11-DISPOSITIONS.md` and `13-SOT-APPLY.md`.
    - Every relative Markdown link in the three prose docs resolves; every planning artifact cited by basename exists at its expected path.
    - Non-allowlisted `chrome` tokens: 0. Word-boundary `rofi` occurrences: at least 2. `rsync` occurrences: 0.
  </acceptance_criteria>
  <done>The playbook is finished and internally consistent: eleven sections, an Outline that matches them one to one, a non-goals table that no longer claims completed work is out of scope, and every link — in-page and relative — proven to resolve.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published prose → operator mid-rollback | Section 9 is read by someone whose session is already broken and who is deciding whether to escalate a tier. The ordering instruction here is the control. |
| published prose → operator's expectations | Section 8 sets what the operator believes they lost. Overstating it triggers unnecessary recovery work; understating it hides a real screen-share regression. |
| playbook cross-references → cited artifacts | Four new `See also` entries and three body citations. A wrong `../` depth silently produces a dead link in the document that is supposed to be the source of truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-22 | Tampering | rollback escalation destroying tier-1 source 2 | high | mitigate | Section 9 states the mechanism, the cause and the ordered mitigation (copy aside before escalating); verify asserts the `uninstall` mechanism and the sha256 identity argument are both present |
| T-15-23 | Tampering | `--rotate-backup` run post-adopt | high | mitigate | Explicit prohibition in the operator's own document, with IN-11 recorded as a deferred fix; the preflight script's freeze is asserted with `git diff --name-only "$B"..HEAD` |
| T-15-24 | Repudiation | misrepresenting the D-38 screen-share consequence | medium | mitigate | Hedged phrasing asserted present and three overstated phrasings asserted absent, so the honest wording is machine-enforced rather than reviewed |
| T-15-25 | Spoofing | non-goals table claiming completed work is out of scope | medium | mitigate | The milestone-scope phrase count is pinned at 0 and `11-DISPOSITIONS.md` must be cited, so the accepted removal cannot be re-framed as deferred |
| T-15-26 | Denial of Service | dead cross-references in the source-of-truth document | medium | mitigate | Two-layer D-23 gate: relative-link resolution across all three prose docs, plus a cited-artifact existence check by basename |
| T-15-27 | Elevation of Privilege | upstream removal subcommand offered as a rollback alternative | high | mitigate | Dangerous-override token count asserted at 0; the tiers are cited to the runbook and never reproduced |
| T-15-SC | Tampering | package-manager installs | low | accept | No `npm` / `pip` / `cargo` / `pacman` install task exists in this phase (`15-RESEARCH.md` § Package Legitimacy Audit: not applicable) |
</threat_model>

## Artifacts this phase produces

Created by **this plan**, all in `docs/dots-hyprland-workflow.md`:

| Artifact | Kind | Anchor |
|----------|------|--------|
| `## 8. Known losses after the full adopt` | new heading | `#8-known-losses-after-the-full-adopt` |
| `## 9. Three roles of the repo hyprland.conf` | new heading | `#9-three-roles-of-the-repo-hyprlandconf` |
| the final 11-entry `## Outline` | rewritten list | `#outline` |
| four new linked `See also` entries | new list items | inside `#see-also` |

At the end of this plan the playbook's complete section set is: `Purpose`, `Prerequisites`, `Profiles: safe vs full`, `Canonical path`, `Outline`, sections 1 through 11, `See also`.

## Flagged Assumptions

| # | Item | Status | Why it is flagged |
|---|------|--------|-------------------|
| C-1 | `DOC-03` edge coverage | **unresolved** (`unclassified`) | The edge probe could not classify this requirement; no acceptance criterion was auto-derived. Coverage comes from the goal-backward truths above. Not auto-resolved with a backstop, not dropped. |
| C-2 | `DOC-04` edge coverage | **unresolved** (`unclassified`) | Same; DOC-04 was delivered in plan `15-04` from `13-SOT-APPLY.md`. |
| A6 | "Screen share **may** be affected" is the accurate D-38 wording | resolved, enforced | The portal's `AvailableSourceTypes` is unchanged, so only the session bootstrap is lost. Task 1 asserts the hedge present and the overstatement absent rather than leaving the wording to review. |
| A-05-1 | Dropping the `See also` entry from the Outline | assumption, deliberate | Introduced in `15-02`. It makes the Outline entry count equal the section count, which is what lets a one-to-one correspondence be asserted mechanically. `See also` remains a section; it is simply not a step on the spine. |
| A-05-2 | The `rofi` floor of 2 occurrences | assumption | Derived from where the trio must be named: the profiles section (D-06/D-09) and the non-goals cutover row. If a later edit legitimately reduces this, raise it rather than lowering the floor silently. |
| Open-3 | The safe-profile section does not document a dual-run restore path | resolved per CONTEXT | `15-CONTEXT.md` defers it explicitly and notes nothing has verified that path since the adopt. Section 8 says the trees are archived under `stow/` and stops there. |

<verification>
- The Outline-to-section one-to-one assertion is the mechanical form of D-03's "one linear spine": eleven headings, eleven entries, every anchor resolving.
- The D-23 gate runs in two layers because the playbook cites planning artifacts two ways — as Markdown links in `See also` and as bare backticked paths in the body. A link check alone would miss a body citation whose file was moved.
- `./scripts/phase14-verify.sh` is not run in these task verifies: it asserts a clean working tree (D-35) and would fail pre-commit. It runs at the phase gate in `15-06`.
- `$B` in the range checks is the phase's planning commit, resolved at run time as
  `git log -1 --format=%H -- .planning/phases/15-playbook-safe-vs-full/15-01-PLAN.md`. It is deliberately
  not `origin/main`: `origin/main` predates `15-CONTEXT.md`, so a range anchored there would report that
  frozen artifact as changed by this phase when it was merely created before the branch was pushed. The
  anchor also survives a push mid-phase, which `origin/main` does not. Measured empty for every guarded
  path at planning time.
</verification>

<success_criteria>
- A reader knows what the full adopt cost and is not misled in either direction about screen share.
- A recovering operator is told to copy the repo conf aside before escalating to tier 2, and never to rotate the backup post-adopt.
- The non-goals table describes the world as it is after Phase 14.
- Every link in the finished playbook resolves, in-page and relative.
</success_criteria>

<output>
Create `.planning/phases/15-playbook-safe-vs-full/15-05-SUMMARY.md` when done.
</output>