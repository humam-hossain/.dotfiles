---
phase: 15-playbook-safe-vs-full
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - docs/phase14-adopt-runbook.md
autonomous: true
requirements: [DOC-03]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An operator in a tier-1 rollback is pointed at `~/ii-original-dots-backup`, the directory whose `hyprland.conf` sha256 matches the recorded pre-adopt fixture, and not at the rotated directory holding a July config (D-21, Correction 3)."
    - "An operator reading runbook section 5 post-adopt learns that rotating the backup now would rename away rollback tier-1 source 3, so the section's pre-adopt 'mandatory' framing no longer applies (D-19, D-21)."
    - "The runbook's structure, section list and role as the record of the 2026-09-04 adopt window are unchanged; only factually false lines moved (D-02, D-21)."
    - "`scripts/phase14-preflight.sh` is not edited by this documentation phase, and its stale `--rotate-backup` remediation line is recorded as a deferred fix rather than silently fixed (D-19)."
  artifacts:
    - path: "docs/phase14-adopt-runbook.md"
      provides: "Rollback tier 1 naming the backup directory that actually holds the pre-adopt config, plus post-adopt caveats on the pre-adopt-tense sections"
      contains: "3d17932a"
  key_links:
    - from: "docs/phase14-adopt-runbook.md"
      to: ".planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt"
      via: "tier-1 source 3 cites the recorded conf sha256 as the way to tell the two backup directories apart"
      pattern: "PRE-ADOPT-BASELINE|3d17932a"
    - from: "docs/phase14-adopt-runbook.md"
      to: "scripts/phase14-verify.sh"
      via: "the corrected tier-1 source 3 agrees with the script's BACKUP_DIR default and its D-36 sha256 assertion"
      pattern: "phase14-verify\\.sh"
  prohibitions:
    - statement: "The playbook must not present the full profile as the default or the recommended-by-omission path; the wrapper default is safe and `--full` is opt-in (D-07)."
    - statement: "No operator-facing doc may name a rollback source that does not hold the recoverable pre-adopt config, and no destructive or recovery-destroying command may be documented without its guard."
    - statement: "The retired session surfaces must not be misrepresented: write `Waybar`, `rofi` and `swaync` literally rather than as a collective noun, and state the D-38 screen-share consequence as a possibility rather than a certainty."
    - statement: "Frozen historical planning records must not be rewritten to agree with today's truth; staleness in them is flagged in the sweep record, never edited away (D-22)."
---

<objective>
Correct the four factually false post-adopt lines in `docs/phase14-adopt-runbook.md` (D-21) without moving any content out of it and without altering its structure or its role as the record of the 2026-09-04 adopt window (D-02).

One of the four is the highest-severity finding in the phase: runbook section 14's tier-1 rollback source 3 tells a recovering operator to restore from the rotated directory, which holds a July config, rather than from `~/ii-original-dots-backup`, which holds the genuine pre-adopt conf. That instruction sits inside a recovery procedure, which is where a wrong string costs the most.

This plan runs in parallel with `15-02`; the two touch different files and share no line.

Purpose: the playbook cites this runbook's section 14 as the rollback source of truth (D-02, D-14), so DOC-03's install-path guidance is only as true as the runbook it defers to.
Output: four corrected lines, structure and section list byte-for-byte preserved.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-playbook-safe-vs-full/15-CONTEXT.md
@.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md
@.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md
@.planning/phases/15-playbook-safe-vs-full/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct rollback tier-1 source 3 and add the post-adopt caveat to the rotate-backup section</name>
  <precondition>The Task 1 checkpoint in plan 15-01 was answered `verified` — this task writes strings that deviate from a conservative reading of D-02 and from D-14's literal path.</precondition>
  <read_first>
    - `docs/phase14-adopt-runbook.md` — lines 305-364 (`## 14. Rollback`), and specifically the tier-1 fence at lines 310-337 with its in-fence explanatory comments
    - `docs/phase14-adopt-runbook.md` — lines 142-160 (`## 5. Rotate the stale backup (D-27)`), including the bold mandatory sentence at line 144
    - `.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md` — "Correction 2" (lines 209-240) and "Correction 3" (lines 241-260), which hold the sha256 comparison and the verbatim defective comment
    - `.planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt` — the recorded `hyprland_conf_sha256` value the correction rests on
    - `scripts/phase14-verify.sh` — line 45 (`BACKUP_DIR`) and the D-36 sha256 assertion, the executable statement of the same fact. READ ONLY
    - `scripts/phase14-preflight.sh` — line 264, the stale `--rotate-backup` remediation message. READ ONLY; D-19 forbids editing it in this phase
    - `.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md` — the `docs/phase14-adopt-runbook.md` section, which fixes the in-fence comment voice and the caveat-sentence shape
  </read_first>
  <files>docs/phase14-adopt-runbook.md</files>
  <action>
Two corrections, both written in the voice already used at their site.

**1. Section 14, tier-1 source 3 (lines 334-336).** The three-line in-fence comment currently tells the operator to use the timestamped directory section 5 created, falling back to `~/ii-original-dots-backup/` only if section 5 did not run. That is inverted. Hashing against the recorded fixture shows `~/ii-original-dots-backup/.config/hypr/hyprland.conf` matches the pre-adopt sha256 `3d17932a6d2dd1b61ccc509402a70c224409bb5c24c4ed70a3c55d4f4bcd89b5`, while the timestamped directory holds `c5c65023dcdb0a202c6e73e32320daf8e6343b00a394fc3bfcf8d5be39867bfe` — a July config from an earlier install.
<!-- planner-discipline-allow: the timestamped directory section 5 created -->

Rewrite that comment, staying inside the fence and keeping the existing `# ` comment voice (the tier-1 fence already carries long explanatory comments; do not lift this into prose above the fence). It must: name `~/ii-original-dots-backup/` as source 3; state that the timestamped `ii-original-dots-backup.<UTC timestamp>` directory is the *rotated stale* backup section 5 moved aside and is **not** a rollback source; and give the operator the one-command way to tell them apart rather than trusting the name — compare `sha256sum` of the candidate's `.config/hypr/hyprland.conf` against `hyprland_conf_sha256` recorded in `.planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt`. Add the actual `cp -a` restore line for source 3, matching the shape of sources 1 and 2 immediately above it, which currently source 3 lacks — it is comment-only today, so an operator reaching tier 1 source 3 has no command to run. Keep the ordering rationale that source 3 is last because it is the source D-36 exists to distrust.

Do not change the tier ordering, the step-0 `hyprland.lua` move-aside, sources 1 and 2, tier 2, tier 3, or the Prohibition on the upstream removal subcommand. D-02 preserves this section's structure; D-21 only authorises correcting what is false.

**2. Section 5, line 144.** The bold sentence declares rotation mandatory whenever the directory exists, qualified by a clause asserting that it does exist as of writing. That qualifier was written pre-adopt and is now actively dangerous: post-adopt `~/ii-original-dots-backup` *is* rollback tier-1 source 3, so rotating it renames away a recovery source.
<!-- planner-discipline-allow: which is whenever the directory exists, as it does today -->

Do not delete the section — D-02 preserves structure, and the section is the correct record of what the adopt window did. Replace the pre-adopt qualifier clause and add, immediately after the bold sentence, a bold post-adopt caveat in the file's existing bold-parenthetical style. The caveat states: this section applies **before** a full adopt only; after the adopt of 2026-09-04, `~/ii-original-dots-backup` holds the pre-adopt configs and is tier-1 source 3 of section 14, so `./scripts/phase14-preflight.sh --rotate-backup` must not be run now. Add one further sentence recording that the preflight script itself still prints that rotation as mandatory remediation, that this is a known stale message tracked as IN-11, and that the script is deliberately not edited by a documentation phase (D-19) — name `scripts/phase14-preflight.sh` by path so the reference is greppable, and do not touch the script.
  </action>
  <verify>
    <automated>grep -q '3d17932a' docs/phase14-adopt-runbook.md && grep -q '14-PRE-ADOPT-BASELINE.txt' docs/phase14-adopt-runbook.md && grep -q -- '--rotate-backup' docs/phase14-adopt-runbook.md && grep -q 'IN-11' docs/phase14-adopt-runbook.md && grep -q 'scripts/phase14-preflight.sh' docs/phase14-adopt-runbook.md && ! grep -qF 'the timestamped directory section 5 created' docs/phase14-adopt-runbook.md && ! grep -qF 'which is whenever the directory exists, as it does today' docs/phase14-adopt-runbook.md</automated>
    <fails_when>non-zero exit — the pre-adopt conf sha256 prefix or the baseline fixture path is not cited (the operator has no way to tell the two backup directories apart), `--rotate-backup` / `IN-11` / the preflight script path are unmentioned (the post-adopt caveat is missing), or either stale fragment still matches: the inverted tier-1 source-3 instruction, or section 5's pre-adopt "as it does today" qualifier</fails_when>
    <automated>test "$(grep -c '^## ' docs/phase14-adopt-runbook.md)" -eq 17 && grep -qF '## 14. Rollback (ADOPT-04, D-23, D-24)' docs/phase14-adopt-runbook.md && grep -qF '## 5. Rotate the stale backup (D-27)' docs/phase14-adopt-runbook.md && grep -q 'ii-original-dots-backup/' docs/phase14-adopt-runbook.md && test "$(grep -c 'cp -a' docs/phase14-adopt-runbook.md)" -ge 3</automated>
    <fails_when>the `##` heading count is not exactly 17, or either named heading is absent — both mean the correction changed the runbook's structure, which D-02 forbids; or `~/ii-original-dots-backup/` is unmentioned; or fewer than three `cp -a` restore lines exist, meaning tier-1 source 3 still has no runnable command beside its comment</fails_when>
    <automated>B="$(git log -1 --format=%H -- .planning/phases/15-playbook-safe-vs-full/15-01-PLAN.md)" && test -n "$B" && git diff --quiet HEAD -- arch/ scripts/ .config/ stow/ vendor/ .planning/STATE.md .planning/ROADMAP.md && test -z "$(git diff --name-only "$B"..HEAD -- scripts/phase14-preflight.sh)" && test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' docs/phase14-adopt-runbook.md | grep -vx 'google-chrome-stable' | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit — an uncommitted change exists under a fenced directory or in `STATE.md` / `ROADMAP.md`, `git diff --name-only "$B"..HEAD` prints `scripts/phase14-preflight.sh` (this phase committed an edit to the script D-19 freezes), or a non-allowlisted `chrome` token was introduced into the runbook</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/phase14-adopt-runbook.md` contains the pre-adopt conf sha256 prefix `3d17932a` and cites `14-PRE-ADOPT-BASELINE.txt`.
    - Tier-1 source 3 names `~/ii-original-dots-backup/` and carries a `cp -a` restore line, bringing the file's `cp -a` count to at least 3.
    - The instruction to prefer the timestamped directory as a rollback source is gone from the file.
    - Section 5 no longer carries the pre-adopt "as it does today" qualifier, and does carry a bold post-adopt caveat naming `--rotate-backup`, `IN-11` and `scripts/phase14-preflight.sh`.
    - The file still has exactly 17 `## ` headings, including `## 5. Rotate the stale backup (D-27)` and `## 14. Rollback (ADOPT-04, D-23, D-24)` unchanged in text.
    - `git diff --name-only "$B"..HEAD -- scripts/phase14-preflight.sh` prints nothing.
    - Non-allowlisted `chrome` token count in the file is 0.
  </acceptance_criteria>
  <done>A recovering operator following tier 1 restores the config that matches the recorded pre-adopt fixture, has a command to run for source 3, and cannot mistake the rotated directory for a rollback source. Section 5 no longer reads as a post-adopt instruction.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the two remaining pre-adopt-tense claims in sections 3 and 8</name>
  <read_first>
    - `docs/phase14-adopt-runbook.md` — line 117 (inside `## 3. Go / no-go gate`) and line 199 (inside `## 7. Run the install`), in their post-Task-1 state
    - `.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md` — the `docs/phase14-adopt-runbook.md` sweep table (lines 697-709), which grades these two MEDIUM and LOW with the reason each is now false
    - `.planning/phases/14-live-full-adopt-verify/14-LIVE-VERIFY.md` — the post-adopt state these lines must now agree with
    - `scripts/phase13-d19-assert.sh` — the assert that proves live `custom/` now holds the applied overlay. READ ONLY
  </read_first>
  <files>docs/phase14-adopt-runbook.md</files>
  <action>
Two tense corrections. Both are single sentences inside sections whose structure and role stay untouched (D-02, D-21).

**Line 117, inside the go/no-go gate.** The sentence asserts that `~/ii-original-dots-backup` exists on this machine today and that the `hyprland.conf` inside it is older than the live one. Post-adopt that second half is false: the directory was recreated by the `--full` install and its conf is byte-identical to the pre-adopt fixture, which is exactly why it became tier-1 source 3.
<!-- planner-discipline-allow: inside it is older than the live one -->
Keep the paragraph's argument intact — it explains why the preflight *reports* rather than *fails* on the backup condition, and that reasoning is still correct for anyone preparing a future adopt. Rewrite only the factual clause, scoping it to the pre-adopt window with an explicit tense marker ("at the time this window was prepared"), and add one bracketed post-adopt sentence stating that the directory now holds the pre-adopt configs and is section 14's tier-1 source 3, cross-referencing the caveat Task 1 added to section 5 so a reader cannot land on one without the other.

**Line 199, inside the install-effects list.** The bullet says `custom/` is seeded, and only because live currently has none. That "currently" is a pre-adopt tense; live `~/.config/hypr/custom/` now holds the applied Phase 13 overlay, verified byte-identical to the repo source of truth. Rewrite the bullet to say the seeding happened because live had no `custom/` at the time of this window, and that section 8 then overwrote the three files you own. Keep the sentence about section 8 exactly as it reads. Do not restate the overlay policy here — that belongs to `13-SOT-APPLY.md` and to the playbook's overlay section, and this file only records what the window did.
<!-- planner-discipline-allow: only because live currently has none -->

Neither correction may move content out of the runbook into the playbook (D-02), and neither may change a heading.
  </action>
  <verify>
    <automated>! grep -qF 'inside it is older than the live one' docs/phase14-adopt-runbook.md && ! grep -qF 'only because live currently has none' docs/phase14-adopt-runbook.md && grep -q 'Section 8 then overwrites the three files you own' docs/phase14-adopt-runbook.md && test "$(grep -c '^## ' docs/phase14-adopt-runbook.md)" -eq 17</automated>
    <fails_when>non-zero exit — either pre-adopt-tense fragment still matches, the section-8 sentence that must be preserved verbatim was reworded or dropped, or the `##` heading count is not exactly 17 (a structural change D-02 forbids)</fails_when>
    <automated>for f in docs/dots-hyprland-workflow.md docs/phase14-adopt-runbook.md README.md; do base="$(dirname "$f")"; grep -o '](\.\{0,2\}/\?[^)#][^)]*)' "$f" | sed 's/](\(.*\))/\1/' | grep -v '^http' | while read -r l; do [ -e "$base/$l" ] || [ -e "$l" ] || { echo "BROKEN $f -> $l"; exit 1; }; done || exit 1; done && ok=1; while read -r a; do h="${a#\#}"; grep -oE '^#{2,3} .*' docs/phase14-adopt-runbook.md | sed 's/^#* //' | tr 'A-Z' 'a-z' | sed 's/[^a-z0-9 -]//g; s/ /-/g' | grep -qx "$h" || { echo "MISSING ANCHOR: $h"; ok=0; }; done < <(grep -oE '\(#[a-z0-9-]+\)' docs/phase14-adopt-runbook.md | tr -d '()' | sort -u); test "$ok" -eq 1</automated>
    <fails_when>a `BROKEN <file> -> <path>` line is printed (a relative link stopped resolving), or a `MISSING ANCHOR:` line is printed and the final test exits non-zero (a runbook Outline entry points at a slug no heading produces, which is what an accidental heading edit looks like)</fails_when>
    <automated>B="$(git log -1 --format=%H -- .planning/phases/15-playbook-safe-vs-full/15-01-PLAN.md)" && test -n "$B" && git diff --quiet HEAD -- arch/ scripts/ .config/ stow/ vendor/ .planning/STATE.md .planning/ROADMAP.md && test -z "$(git diff --name-only "$B"..HEAD -- arch/ scripts/ .config/ stow/ vendor/)" && ./scripts/phase13-d19-assert.sh >/tmp/p15-03-d19.txt 2>&1 && test "$(grep -c '^\[FAIL\]' /tmp/p15-03-d19.txt)" -eq 0</automated>
    <fails_when>non-zero exit — an uncommitted change exists under a fenced directory or in `STATE.md` / `ROADMAP.md`, `git diff --name-only "$B"..HEAD` prints any path under the five fenced directories, or `scripts/phase13-d19-assert.sh` prints a `[FAIL]` line against its 15-`[PASS]` / 0-`[FAIL]` baseline</fails_when>
  </verify>
  <acceptance_criteria>
    - Neither pre-adopt-tense fragment survives in `docs/phase14-adopt-runbook.md`.
    - The line-117 paragraph still explains why the preflight reports rather than fails on the backup condition, and now carries a post-adopt sentence cross-referencing section 5's caveat.
    - The line-199 bullet still ends with the unchanged sentence about section 8 overwriting the three files you own.
    - The file still has exactly 17 `## ` headings.
    - Every relative Markdown link in the three prose docs resolves; every runbook in-page anchor resolves to a heading.
    - `git diff --name-only "$B"..HEAD -- arch/ scripts/ .config/ stow/ vendor/` prints nothing.
    - `./scripts/phase13-d19-assert.sh` exits 0 with zero `[FAIL]` lines.
  </acceptance_criteria>
  <done>All four of the runbook's false post-adopt lines are corrected; its 17 sections, its ordering and its role as the adopt-window record are unchanged, and nothing was copied out of it into the playbook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published recovery procedure → operator mid-failure | Section 14 is read by a human whose session is already broken. Every string here is executed under time pressure with no second source to check against. |
| runbook prose → `scripts/phase14-preflight.sh` | The script prints remediation the runbook must now contradict, and the script is frozen this phase (D-19), so the contradiction has to live in the prose. |
| runbook → playbook | The playbook cites section 14 rather than copying it (D-02, D-14), so a false line here propagates to a reader who never opens this file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-11 | Denial of Service | section 14 tier-1 source 3 | high | mitigate | Name `~/ii-original-dots-backup/`, cite the recorded sha256 `3d17932a…` and `14-PRE-ADOPT-BASELINE.txt` as the way to distinguish the two directories by content rather than by name; verify asserts the sha prefix present and the inverted instruction absent |
| T-15-12 | Tampering | section 5's rotate instruction read post-adopt | high | mitigate | Bold post-adopt caveat stating rotation must not be run now because it renames away tier-1 source 3; verify asserts `--rotate-backup`, `IN-11` and the script path are all present |
| T-15-13 | Denial of Service | tier-1 source 3 having no runnable command | medium | mitigate | Add the missing `cp -a` restore line so source 3 matches sources 1 and 2; verify asserts the file's `cp -a` count is at least 3 |
| T-15-14 | Repudiation | rewriting the adopt-window record instead of correcting it | medium | mitigate | The `##` heading count is pinned at 17 and both edited section headings are asserted unchanged, so a structural rewrite fails the gate (D-02) |
| T-15-15 | Tampering | `scripts/phase14-preflight.sh` | high | mitigate | A documentation phase must not fix the script it documents as stale; asserted by `git diff --name-only "$B"..HEAD -- scripts/phase14-preflight.sh` printing nothing (D-19) |
| T-15-SC | Tampering | package-manager installs | low | accept | No `npm` / `pip` / `cargo` / `pacman` install task exists in this phase (`15-RESEARCH.md` § Package Legitimacy Audit: not applicable) |
</threat_model>

## Artifacts this phase produces

This plan creates no new headings or files. It produces corrected content at four existing sites in `docs/phase14-adopt-runbook.md`:

| Site | Section | Correction |
|------|---------|------------|
| tier-1 source 3 comment + new restore line | `## 14. Rollback (ADOPT-04, D-23, D-24)` | names `~/ii-original-dots-backup/`, adds the sha256 discrimination method and the missing `cp -a` |
| bold mandatory sentence + new bold caveat | `## 5. Rotate the stale backup (D-27)` | scopes the section to pre-adopt, records IN-11 as a deferred fix |
| backup-condition paragraph | `## 3. Go / no-go gate (ADOPT-01, D-18..D-22)` | tense-scopes the false clause, cross-references section 5 |
| `custom/` seeding bullet | `## 7. Run the install (D-07)` | tense-scopes "currently" |

## Flagged Assumptions

| # | Item | Status | Why it is flagged |
|---|------|--------|-------------------|
| C-1 | `DOC-03` edge coverage | **unresolved** (`unclassified`) | The edge probe could not classify this requirement; no acceptance criterion was auto-derived. Coverage here comes from the goal-backward truths above. Not auto-resolved with a backstop, not dropped. |
| C-2 | `DOC-04` edge coverage | **unresolved** (`unclassified`) | Same; DOC-04 is covered by plan `15-04`. |
| A2 | Correcting section 14 sits inside D-21 rather than violating D-02 | gated | Resolved by the `checkpoint:decision` in `15-01`; this plan carries a `<precondition>` binding to that answer. |
| A-03-1 | Adding a `cp -a` restore line to tier-1 source 3 is a correction, not a structural change | assumption | Sources 1 and 2 each have one; source 3 is comment-only today, so an operator reaching it has nothing to run. Read as completing the existing shape rather than adding structure. The heading-count and heading-text assertions bound the risk. |
| A-03-2 | IN-11 stays a deferred fix with the runbook prose as its only current mitigation | resolved per D-19 | The correct fix is editing `scripts/phase14-preflight.sh:264`; a documentation phase must not. Plan `15-06` records the deferred fix with an owner in the sweep record. |

<verification>
- Structure preservation is asserted mechanically: `##` heading count pinned at 17 plus two exact heading-text greps. That is the difference between "corrected a false line" and "rewrote the record".
- The `scripts/phase14-preflight.sh` freeze is asserted with `git diff --name-only "$B"..HEAD`, which catches a *committed* edit; `git diff --quiet HEAD` alone would not.
- `./scripts/phase14-verify.sh` is not run in this plan's task verifies: it asserts a clean working tree (D-35) and would fail pre-commit. It runs at the phase gate in `15-06`.
- `$B` in the range checks is the phase's planning commit, resolved at run time as
  `git log -1 --format=%H -- .planning/phases/15-playbook-safe-vs-full/15-01-PLAN.md`. It is deliberately
  not `origin/main`: `origin/main` predates `15-CONTEXT.md`, so a range anchored there would report that
  frozen artifact as changed by this phase when it was merely created before the branch was pushed. The
  anchor also survives a push mid-phase, which `origin/main` does not. Measured empty for every guarded
  path at planning time.
</verification>

<success_criteria>
- An operator in a tier-1 rollback restores the config whose sha256 matches the recorded pre-adopt fixture.
- No section of the runbook still reads as a post-adopt instruction when it was written pre-adopt.
- The runbook's 17 sections, ordering and role are unchanged, and nothing moved out of it.
- `scripts/phase14-preflight.sh` is untouched.
</success_criteria>

<output>
Create `.planning/phases/15-playbook-safe-vs-full/15-03-SUMMARY.md` when done.
</output>