---
phase: 15-playbook-safe-vs-full
plan: 06
type: execute
wave: 5
depends_on: ["15-03", "15-05"]
files_modified:
  - .planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md
autonomous: true
requirements: [DOC-03, DOC-04]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every file in the D-20 sweep scope has an entry in the sweep record — including the one with nothing wrong with it — so the record shows the review happened rather than implying it (D-20, research assumption A3)."
    - "Staleness found in frozen planning artifacts is flagged with file and line, and none of those artifacts is edited (D-22)."
    - "Every deferred fix this phase surfaced has a named owner and a reason it is not fixed here: IN-11, the D-38 restoration, and the dual-run restore path (D-19, D-17)."
    - "The phase gate is proven, not asserted: both existing suites run at their recorded baselines and every scope fence holds across the whole phase, not just per task."
    - "No task in this phase leaves a direct edit to `.planning/STATE.md` or `.planning/ROADMAP.md`; the only writes to either come from the `gsd-tools.cjs` query handlers (D-22)."
  artifacts:
    - path: ".planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md"
      provides: "Complete doc staleness sweep record with per-file findings, flagged-not-edited items, deferred fixes with owners, and the phase-gate transcript"
      contains: "## Deferred fixes"
  key_links:
    - from: ".planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md"
      to: "docs/dots-hyprland-workflow.md"
      via: "per-file findings table recording what was corrected in the rewrite"
      pattern: "dots-hyprland-workflow\\.md"
    - from: ".planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md"
      to: "scripts/phase14-preflight.sh"
      via: "IN-11 recorded as a deferred fix with an owner, naming the script and its stale line"
      pattern: "phase14-preflight\\.sh"
    - from: ".planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md"
      to: ".planning/STATE.md"
      via: "the D-38 ownership answer is recorded as a state change to be applied through the gsd-tools query handlers, never by editing the file"
      pattern: "STATE\\.md"
  prohibitions:
    - statement: "The playbook must not present the full profile as the default or the recommended-by-omission path; the wrapper default is safe and `--full` is opt-in (D-07)."
    - statement: "No operator-facing doc may name a rollback source that does not hold the recoverable pre-adopt config, and no destructive or recovery-destroying command may be documented without its guard."
    - statement: "The retired session surfaces must not be misrepresented: write `Waybar`, `rofi` and `swaync` literally rather than as a collective noun, and state the D-38 screen-share consequence as a possibility rather than a certainty."
    - statement: "Frozen historical planning records must not be rewritten to agree with today's truth; staleness in them is flagged in the sweep record, never edited away (D-22)."
---

<objective>
Finish the phase's second first-class deliverable. The sweep is not a side effect of the rewrite — `15-CONTEXT.md` records the user's steer that every doc needed reviewing — so it gets a written record that names every file in scope, including the one with nothing wrong with it, and every deferred fix with an owner (D-20, D-22).

Then prove the phase, rather than asserting it: both existing assert suites at their recorded baselines, every forbidden string absent from the operator-facing docs, every link resolving, and every scope fence held across the phase's whole commit range rather than only within a task.

Purpose: DOC-03 and DOC-04 are only credible if the documents around them agree, and the `.planning` half of the sweep can only be delivered as a report because D-22 forbids editing what it reviews.
Output: a complete `15-DOC-SWEEP.md` and a recorded phase-gate transcript inside it.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-playbook-safe-vs-full/15-CONTEXT.md
@.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md
@.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md
@.planning/phases/15-playbook-safe-vs-full/15-03-SUMMARY.md
@.planning/phases/15-playbook-safe-vs-full/15-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the docs/ and README sweep, including the file with no findings</name>
  <read_first>
    - `.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md` — the seeded record from `15-01`, whose `## Corrections applied`, `## Reviewed, no findings` and `## Flagged, not edited (D-22)` headings this task fills
    - `.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md` — "Doc Sweep Inventory" (lines 667-757) in full: the per-file tables with line numbers, severities and the `arch/README.md` no-findings determination
    - `arch/README.md` — read in full to confirm the no-findings determination first-hand rather than inheriting it; it is Arch OS bootstrap only. READ ONLY
    - `README.md` and `docs/dots-hyprland-workflow.md` and `docs/phase14-adopt-runbook.md` — in their finished state, to record what was actually corrected rather than what was planned
    - `.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md` — the "Sweep report" section, which fixes the per-file heading plus findings-table shape
  </read_first>
  <files>.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md</files>
  <action>
Fill the operator-facing half of the sweep record. One `###` sub-heading per file inside `## Corrections applied`, each followed by a findings table with columns Line, Stale text, Correction, Severity — the shape `15-RESEARCH.md` uses.

`docs/dots-hyprland-workflow.md`: record the three D-10 stale claims by their pre-rewrite line numbers (14, 23, 196-197) and the additional stale lines the research found inside the rewrite scope (41, 145-149, 168, 174-181, 193, 293, 294, 296). For each, the correction actually made and which plan made it. Note that this file was rewritten in place under D-01 rather than superseded.

`docs/phase14-adopt-runbook.md`: record the four corrections from plan `15-03` at lines 334-336 (HIGH), 144 (MEDIUM), 117 (MEDIUM) and 199 (LOW), each with the reason it was false post-adopt, and state explicitly that the file's structure, its 17 sections and its role as the adopt-window record were preserved (D-02) — only false lines moved.

`README.md`: one row for line 7, the arrow sequence that promised dual-run as the destination.

`arch/README.md`: this file goes under `## Reviewed, no findings`, not under corrections. State that it was read in full, that it is entirely Arch OS bootstrap — pacman base install, `useradd`, `visudo`, `bootctl`, `loader.conf`, `arch.conf`, `efibootmgr`, chroot exit — and that it contains no reference to dots-hyprland, to the wrapper, to `hyprland.conf`, or to `Waybar` / `rofi` / `swaync`. Record the negative result explicitly so the record shows the review happened; a silently absent file reads as an unreviewed one (research assumption A3).

Add a short scope statement at the top of `## Corrections applied` naming the five files D-20 puts in scope, so a reader can see the sweep's boundary without reconstructing it.

Quoting the stale strings verbatim in this record is correct — it is a findings report, not operator instruction. The forbidden-string gates in this phase are scoped to the operator-facing docs precisely so this file can quote them.
  </action>
  <verify>
    <automated>S=.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md; grep -q 'dots-hyprland-workflow.md' "$S" && grep -q 'phase14-adopt-runbook.md' "$S" && grep -qE '^### .*README\.md' "$S" && grep -q 'arch/README.md' "$S" && grep -q '## Reviewed, no findings' "$S" && grep -qi 'bootctl' "$S" && grep -q '334' "$S" && grep -q 'D-02' "$S" && grep -q 'D-10' "$S"</automated>
    <fails_when>non-zero exit — any of the five in-scope files is unmentioned, `arch/README.md` has no explicit no-findings entry with its evidence (the `bootctl` marker proves it was read rather than assumed), the runbook's highest-severity line number is unrecorded, or the D-02 preservation statement / D-10 stale-claim attribution is missing</fails_when>
    <automated>B="$(git log -1 --format=%H -- .planning/phases/15-playbook-safe-vs-full/15-01-PLAN.md)" && test -n "$B" && S=.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md; test "$(grep -cE '^\| *[0-9]' "$S")" -ge 12 && grep -qE 'HIGH|MEDIUM|LOW' "$S" && test -z "$(git diff --name-only "$B"..HEAD -- arch/README.md)"</automated>
    <fails_when>fewer than twelve line-numbered findings rows exist (the research inventory names at least that many across the three corrected files, so a smaller count means findings were dropped rather than recorded), no severity grading appears, or `git diff --name-only "$B"..HEAD` prints `arch/README.md` — a file recorded as having no findings must not also have been edited</fails_when>
    <automated>git diff --quiet HEAD -- arch/ scripts/ .config/ stow/ vendor/ .planning/STATE.md .planning/ROADMAP.md</automated>
    <fails_when>non-zero exit — an uncommitted change exists under a fenced directory or in `STATE.md` / `ROADMAP.md`</fails_when>
  </verify>
  <acceptance_criteria>
    - `15-DOC-SWEEP.md` has a `###` entry for each of `docs/dots-hyprland-workflow.md`, `docs/phase14-adopt-runbook.md` and `README.md` under `## Corrections applied`, each with a line-numbered findings table.
    - At least twelve line-numbered findings rows exist across those tables, and severities are graded.
    - The runbook entry names line 334 among its findings and states that its structure, 17 sections and role were preserved.
    - `arch/README.md` appears under `## Reviewed, no findings` with concrete evidence it was read in full, including at least one bootstrap-specific term such as `bootctl`.
    - A scope statement names the five files D-20 puts in scope.
    - `git diff --name-only "$B"..HEAD -- arch/README.md` prints nothing.
    - No file under `arch/`, `scripts/`, `.config/`, `stow/`, `vendor/` and neither `.planning/STATE.md` nor `.planning/ROADMAP.md` has an uncommitted diff.
  </acceptance_criteria>
  <done>The operator-facing half of the sweep is a record rather than a claim: every corrected line is attributed, and the one file with nothing wrong with it says so explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Record the read-only .planning review and every deferred fix with an owner</name>
  <read_first>
    - `.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md` — its post-Task-1 state, specifically the empty `## Flagged, not edited (D-22)` section
    - `.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md` — the `.planning` prose sweep subsection (lines 724-741), which names the correctable `PROJECT.md` lines, the guarded files and the frozen artifacts, and Open Question 4's recommendation to use a targeted grep rather than an exhaustive read
    - `.planning/phases/15-playbook-safe-vs-full/15-CONTEXT.md` — lines 30, 39, 40 and 127, which carry the Correction 1 and 2 errors and are themselves a frozen artifact
    - `.planning/PROJECT.md` — lines 43, 106 and 127, the collective-noun occurrences outside D-22's correction exception
    - `.planning/STATE.md` — the "Concerns carried forward" block and the Deferred Items row recording D-38 as open with no owning phase. READ ONLY; D-22 forbids editing it
    - `scripts/phase14-preflight.sh` — line 264, the stale `--rotate-backup` remediation message. READ ONLY
  </read_first>
  <files>.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md</files>
  <action>
Fill `## Flagged, not edited (D-22)` and add a new section `## Deferred fixes`.

**Flagged, not edited.** Run the targeted grep the research recommends rather than reading fifteen frozen artifacts line by line: search `.planning/` for the four known stale patterns — the dead compositor-option spelling, the rotated timestamped backup directory, dual-run framed as a destination, and the D-38 item recorded as having no owning phase. Record each hit as a row with columns File, Line, Stale text, Why stale, Disposition. Every disposition in this section is "flag, do not edit", with the reason stated per row rather than once at the top.

At minimum these must appear:
- `.planning/phases/15-playbook-safe-vs-full/15-CONTEXT.md` lines 30, 39, 40 and 127 — this phase's own context artifact carries the Correction 1 and 2 errors. It is a frozen record of what was decided at the time; the playbook was written correctly regardless, and the checkpoint in plan `15-01` is the audit trail for that divergence.
- `.planning/PROJECT.md` lines 43, 106 and 127 — the retired session surfaces referred to by a collective noun rather than named literally. Outside D-22's product-surface correction exception, which is why lines 18 and 28 were corrected and these were not. Note that the phase's gate pins this file's non-allowlisted token count at exactly three, so a later phase that fixes them will need to update that number deliberately.
- `.planning/STATE.md` — the D-38 concern recorded as open with no owning phase. D-17 answers the ownership question in favour of "documented in Phase 15, fix unowned". State that this is a state change and that it goes through the `gsd-tools.cjs` query handlers, never through an edit to the file (D-22, research assumption A5). Name the handler surface; do not run it in this task.

Also state, once, which files were reviewed by targeted grep rather than read in full, and why: an exhaustive line-by-line read of roughly fifteen frozen artifacts is high cost for a report that changes nothing.

**Deferred fixes.** A table with columns Item, What is wrong, Why not fixed here, Owner. Three rows minimum:
- IN-11 — `scripts/phase14-preflight.sh` line 264 still prints backup rotation as mandatory remediation, which post-adopt would rename away rollback tier-1 source 3. Not fixed here because a documentation phase does not edit scripts (D-19). Owner: a future phase that owns the wrapper and its scripts; record that the runbook section 5 caveat and the playbook section 9 prohibition are the current mitigation, and that both are prose, not enforcement.
- D-38 restoration — the `graphical-session.target` / `hyprland-session.service` bootstrap, `wl-clip-persist`, and the four workspace-pinned autostarts. Not fixed here because this phase's criteria are documentation-only (D-17). Owner: unowned; record that `.planning/STATE.md` already carries it and that the fix is one `systemctl --user` invocation or one line in `custom/execs.lua`.
- Dual-run restore path — how to bring the archived `stow/` trees back after a full adopt. Not written because nothing has verified that path since the adopt. Owner: unowned.

Write `Waybar`, `rofi` and `swaync` literally throughout. Edit no file other than the sweep record.
  </action>
  <verify>
    <automated>S=.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md; grep -q '## Deferred fixes' "$S" && grep -q 'IN-11' "$S" && grep -q 'D-38' "$S" && grep -q 'phase14-preflight.sh' "$S" && grep -q '15-CONTEXT.md' "$S" && grep -q 'STATE.md' "$S" && grep -q 'gsd-tools' "$S" && grep -qi 'unowned' "$S" && grep -q 'stow/' "$S"</automated>
    <fails_when>non-zero exit — the deferred-fixes section is absent, either deferred item is unrecorded, the preflight script is not named as IN-11's site, this phase's own context artifact is not flagged, or the STATE.md ownership answer does not name the tooling surface it must go through — an owner recorded as blank rather than explicitly `unowned` also fails the `grep -qi 'unowned'`</fails_when>
    <automated>B="$(git log -1 --format=%H -- .planning/phases/15-playbook-safe-vs-full/15-01-PLAN.md)" && test -n "$B" && S=.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md; grep -q '## Flagged, not edited (D-22)' "$S" && test "$(grep -c 'flag, do not edit' "$S")" -ge 3 && test -z "$(git diff --name-only "$B"..HEAD -- .planning/phases/15-playbook-safe-vs-full/15-CONTEXT.md .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md .planning/phases/11-disposition-decisions/11-DISPOSITIONS.md .planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md)"</automated>
    <fails_when>the flagged section heading is missing, fewer than three rows carry the explicit "flag, do not edit" disposition (a blank disposition column is exactly how a flagged item quietly becomes an edited one), or `git diff --name-only "$B"..HEAD` prints any frozen artifact path — this phase must not have committed a change to its own CONTEXT, the inventory, the dispositions or the overlay SoT</fails_when>
    <automated>test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' .planning/PROJECT.md | grep -vx 'google-chrome-stable' | wc -l)" -eq 3 && git diff --quiet HEAD -- arch/ scripts/ .config/ stow/ vendor/ .planning/STATE.md .planning/ROADMAP.md</automated>
    <fails_when>the `.planning/PROJECT.md` count is not exactly 3 — above means a new collective-noun occurrence was written, below means a flagged-not-edited row was edited away instead of flagged; or `git diff --quiet` reports an uncommitted change under a fenced directory or in `STATE.md` / `ROADMAP.md` — the satisfiable form of D-22's guard, since a direct edit is exactly what an uncommitted diff left behind by this task looks like</fails_when>
  </verify>
  <acceptance_criteria>
    - `15-DOC-SWEEP.md` contains `## Deferred fixes` with at least three rows, each naming an owner explicitly — `unowned` where that is the truth, never blank.
    - IN-11's row names `scripts/phase14-preflight.sh`, its line, D-19 as the reason, and records the two prose mitigations as prose rather than enforcement.
    - D-38's row records the phase's documentation-only scope and the known one-line shapes of a fix.
    - `## Flagged, not edited (D-22)` records at least three rows with an explicit "flag, do not edit" disposition, covering `15-CONTEXT.md` lines 30/39/40/127, `.planning/PROJECT.md` lines 43/106/127, and the `.planning/STATE.md` D-38 ownership answer.
    - The STATE.md row states the change goes through the `gsd-tools.cjs` query handlers and not through an edit.
    - The report states which files were reviewed by targeted grep rather than read in full, and why.
    - `git diff --name-only "$B"..HEAD` prints nothing for `15-CONTEXT.md`, `10-INVENTORY.md`, `11-DISPOSITIONS.md` or `13-SOT-APPLY.md`; `.planning/STATE.md` and `.planning/ROADMAP.md` have no uncommitted diff.
    - The non-allowlisted `chrome` token count in `.planning/PROJECT.md` is exactly 3.
  </acceptance_criteria>
  <done>Everything this phase found but may not fix is written down with an owner, and everything D-22 freezes is provably still frozen.</done>
</task>

<task type="auto">
  <name>Task 3: Run the phase gate and record the transcript in the sweep record</name>
  <read_first>
    - `.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md` — its post-Task-2 state
    - `.planning/phases/15-playbook-safe-vs-full/15-VALIDATION.md` — the per-task verification map and the recorded suite baselines this gate must reproduce
    - `scripts/phase14-verify.sh` — its header rules and the D-35 clean-working-tree assertion, which is why this gate tolerates exactly one specific failure line. READ ONLY
    - `docs/dots-hyprland-workflow.md`, `docs/phase14-adopt-runbook.md`, `README.md`, `.planning/PROJECT.md` — the finished operator-facing set the gate asserts over
  </read_first>
  <files>.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md</files>
  <action>
Prove the phase and write the proof down.

Run the full gate and capture its output, then add a section headed `## Phase gate` to the sweep record holding: the command set that was run, the observed result of each, and the two suite baselines. Report results as `[PASS]` / `[FAIL]` lines in the repo's convention, and never report a pass for something not observed — an unobservable condition is an `[INFO]`, matching `scripts/phase14-verify.sh`'s own header rule.

The gate has five parts:
1. **Existing suites.** `./scripts/phase13-d19-assert.sh` at its baseline of 15 `[PASS]` and 0 `[FAIL]`. `./scripts/phase14-verify.sh` at its baseline of 0 `[FAIL]` and exactly 1 `[FINDING]`, the D-38 loss. Record the caveat that this second script also asserts a clean working tree under D-35, so it is run after commits land; a `[FAIL]` naming a dirty tree is an artifact of when it ran and not a regression, and the gate command tolerates that one line and no other.
2. **Forbidden strings across the operator-facing set only.** The dead compositor-option spelling, the rotated timestamped backup form, and the milestone-scope claim must all be absent from `docs/dots-hyprland-workflow.md`, `docs/phase14-adopt-runbook.md`, `README.md` and `.planning/PROJECT.md`. State in the record why the sweep report itself is excluded from these greps: it quotes the stale strings as findings by design.
3. **Link integrity (D-23).** Every relative Markdown link in the three prose docs resolves, and every planning artifact the playbook cites by basename exists.
4. **Scope fences.** Across the phase's whole commit range, nothing under `arch/`, `scripts/`, `.config/`, `stow/` or `vendor/` changed, and no frozen phase artifact was committed. `.planning/STATE.md` and `.planning/ROADMAP.md` are checked against the working tree rather than the commit range, because `gsd-tools.cjs` legitimately commits to both during execution — record that distinction in the transcript so a later reader does not mistake the weaker check for an oversight.
5. **Requirement coverage.** `docs/dots-hyprland-workflow.md` satisfies the DOC-03 and DOC-04 assertions from `15-VALIDATION.md`'s verification map — both profiles named, all three flag axes named, the gate preceding the first install command by line number, the overlay SoT and one-way direction stated, `13-SOT-APPLY.md` cited.

Where a check cannot be observed, write `[INFO]` with the reason. Do not soften a failure into a finding.
  </action>
  <verify>
    <automated>./scripts/phase13-d19-assert.sh >/tmp/p15-gate-d19.txt 2>&1; test "$(grep -c '^\[FAIL\]' /tmp/p15-gate-d19.txt)" -eq 0 && test "$(grep -c '^\[PASS\]' /tmp/p15-gate-d19.txt)" -eq 15; ./scripts/phase14-verify.sh >/tmp/p15-gate-p14.txt 2>&1 || true; test "$(grep -c '^\[FINDING\]' /tmp/p15-gate-p14.txt)" -eq 1 && test "$(grep '^\[FAIL\]' /tmp/p15-gate-p14.txt | grep -vc 'D-35 working tree is dirty')" -eq 0</automated>
    <fails_when>non-zero exit — `scripts/phase13-d19-assert.sh` prints any `[FAIL]` line or a `[PASS]` count other than 15, or `scripts/phase14-verify.sh` prints a `[FINDING]` count other than 1 (a second finding means something regressed beyond the expected D-38 loss) or prints any `[FAIL]` line other than the D-35 dirty-tree one, which is tolerated because this verify runs before the task's own commit</fails_when>
    <automated>test -z "$(grep -l 'getoption configProvider' docs/dots-hyprland-workflow.md docs/phase14-adopt-runbook.md README.md .planning/PROJECT.md 2>/dev/null)" && ! grep -qE 'ii-original-dots-backup\.[0-9]{8}T' docs/dots-hyprland-workflow.md docs/phase14-adopt-runbook.md README.md .planning/PROJECT.md && test "$(grep -c 'Out of scope this milestone' docs/dots-hyprland-workflow.md)" -eq 0 && test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' docs/dots-hyprland-workflow.md docs/phase14-adopt-runbook.md README.md | sed 's/^[^:]*://' | grep -vx 'google-chrome-stable' | wc -l)" -eq 0</automated>
    <fails_when>non-zero exit — `grep -l` prints a filename because the dead compositor-option spelling survived in an operator-facing doc, the rotated backup form matches anywhere in the four-file set, the milestone-scope claim survives in the playbook, or a non-allowlisted `chrome` token exists in the three prose docs</fails_when>
    <automated>for f in docs/dots-hyprland-workflow.md docs/phase14-adopt-runbook.md README.md; do base="$(dirname "$f")"; grep -o '](\.\{0,2\}/\?[^)#][^)]*)' "$f" | sed 's/](\(.*\))/\1/' | grep -v '^http' | while read -r l; do [ -e "$base/$l" ] || [ -e "$l" ] || { echo "BROKEN $f -> $l"; exit 1; }; done || exit 1; done && ok=1; while read -r a; do h="${a#\#}"; grep -oE '^#{2,3} .*' docs/dots-hyprland-workflow.md | sed 's/^#* //' | tr 'A-Z' 'a-z' | sed 's/[^a-z0-9 -]//g; s/ /-/g' | grep -qx "$h" || { echo "MISSING ANCHOR: $h"; ok=0; }; done < <(grep -oE '\(#[a-z0-9-]+\)' docs/dots-hyprland-workflow.md | tr -d '()' | sort -u); test "$ok" -eq 1</automated>
    <fails_when>a `BROKEN <file> -> <path>` line or a `MISSING ANCHOR:` line is printed — the D-23 gate, covering both relative links across the three prose docs and in-page anchors within the playbook</fails_when>
    <automated>B="$(git log -1 --format=%H -- .planning/phases/15-playbook-safe-vs-full/15-01-PLAN.md)" && test -n "$B" && test -z "$(git diff --name-only "$B"..HEAD -- arch/ scripts/ .config/ stow/ vendor/)" && git diff --quiet HEAD -- .planning/STATE.md .planning/ROADMAP.md && GATE="$(grep -n '10-INVENTORY.md' docs/dots-hyprland-workflow.md | head -1 | cut -d: -f1)" && INST="$(grep -nE '^\s*\./arch/dots-hyprland\.sh install' docs/dots-hyprland-workflow.md | head -1 | cut -d: -f1)" && test "$GATE" -lt "$INST" && grep -qi 'safe' docs/dots-hyprland-workflow.md && grep -q -- '--full' docs/dots-hyprland-workflow.md && for f in skip-hyprland core skip-sysupdate; do grep -q -- "--$f" docs/dots-hyprland-workflow.md || exit 1; done && grep -q '13-SOT-APPLY.md' docs/dots-hyprland-workflow.md && grep -qiE 'one-way|repo *(→|->|to) *live' docs/dots-hyprland-workflow.md && grep -q 'Phase gate' .planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md</automated>
    <fails_when>non-zero exit — `git diff --name-only "$B"..HEAD` prints any path under the five fenced directories (a committed scope violation, which the per-task `git diff --quiet HEAD` checks cannot see), `git diff --quiet HEAD` reports an uncommitted edit to `STATE.md` or `ROADMAP.md`, the D-13 gate-before-install line-number comparison fails, any DOC-03 or DOC-04 assertion from the validation map is unmet, or the sweep record has no `Phase gate` section holding the transcript. `ROADMAP.md` is checked against the working tree rather than the commit range because `gsd-tools.cjs roadmap update-plan-progress` legitimately commits to it during execution; D-22 forbids a *direct* edit, which is what an uncommitted diff left by a task looks like</fails_when>
  </verify>
  <acceptance_criteria>
    - `./scripts/phase13-d19-assert.sh` exits 0 with exactly 15 `[PASS]` and 0 `[FAIL]`.
    - `./scripts/phase14-verify.sh` reports exactly 1 `[FINDING]` and no `[FAIL]` other than the D-35 dirty-tree line.
    - The dead compositor-option spelling, the rotated backup form and the milestone-scope claim are all absent from the four operator-facing files.
    - Non-allowlisted `chrome` tokens across the three prose docs: 0.
    - Every relative Markdown link in the three prose docs resolves; every in-page anchor in the playbook resolves.
    - `git diff --name-only "$B"..HEAD -- arch/ scripts/ .config/ stow/ vendor/` prints nothing, and `git diff --quiet HEAD -- .planning/STATE.md .planning/ROADMAP.md` succeeds.
    - The first `10-INVENTORY.md` mention precedes the first `./arch/dots-hyprland.sh install` command line by line number.
    - The playbook names both profiles, all three flag axes and `--full`, cites `13-SOT-APPLY.md`, and states the one-way repo-to-live direction.
    - `15-DOC-SWEEP.md` contains a `## Phase gate` section recording the command set, the observed result of each, and both suite baselines, with the D-35 caveat explained and the sweep record's own exclusion from the forbidden-string greps stated.
  </acceptance_criteria>
  <done>The phase is proven rather than asserted, and the proof is written where the next reader will find it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sweep record → future maintainer | This file is the only record that a review happened at all. An omission here reads as "reviewed and clean" rather than "not reviewed". |
| documentation phase → frozen planning history | The sweep reviews artifacts it is forbidden to edit. The temptation to correct what it just documented as wrong is the phase's main integrity risk. |
| phase gate → the phase's own commit range | Per-task working-tree checks cannot see a scope violation that was committed. The gate is the only place the whole range is checked. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-28 | Repudiation | frozen phase artifacts edited instead of flagged | high | mitigate | Every flagged row carries an explicit "flag, do not edit" disposition, and `git diff --name-only "$B"..HEAD` over the four named frozen artifacts must print nothing (D-22) |
| T-15-29 | Repudiation | `.planning/STATE.md` / `.planning/ROADMAP.md` edited directly rather than through the query handlers | high | mitigate | `git diff --quiet HEAD` on both files in every task of this plan. A commit-range emptiness check is deliberately not used: `gsd-tools.cjs roadmap update-plan-progress` and the state handlers legitimately write to them during execution, so a range check would be unsatisfiable and would train the executor to ignore the gate |
| T-15-30 | Tampering | a committed change under a fenced directory that per-task checks missed | high | mitigate | The phase gate re-runs the fence over the whole `"$B"..HEAD` range rather than the working tree |
| T-15-31 | Repudiation | a file silently omitted from the sweep record reading as reviewed-and-clean | medium | mitigate | `arch/README.md` gets an explicit no-findings entry with first-hand evidence, and a minimum findings-row count guards against dropped rows |
| T-15-32 | Denial of Service | IN-11 left with no owner, so the stale rotation message stays live indefinitely | medium | mitigate | Recorded in `## Deferred fixes` with an owner, its current prose-only mitigations named as prose rather than enforcement |
| T-15-33 | Tampering | the phase gate softening a real failure into a finding | medium | mitigate | The gate tolerates exactly one specific `[FAIL]` line — the D-35 dirty-tree artifact of pre-commit timing — and no other; the `[FINDING]` count is pinned at 1 |
| T-15-SC | Tampering | package-manager installs | low | accept | No `npm` / `pip` / `cargo` / `pacman` install task exists in this phase (`15-RESEARCH.md` § Package Legitimacy Audit: not applicable) |
</threat_model>

## Artifacts this phase produces

Created by **this plan**, all in `.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md`:

| Artifact | Kind | Anchor |
|----------|------|--------|
| per-file `###` findings entries under `## Corrections applied` | new sub-headings | one per swept file |
| `## Deferred fixes` | new heading | `#deferred-fixes` |
| `## Phase gate` | new heading | `#phase-gate` |

Filled by this plan: `## Reviewed, no findings` and `## Flagged, not edited (D-22)`, both seeded empty by plan `15-01`.

## Flagged Assumptions

| # | Item | Status | Why it is flagged |
|---|------|--------|-------------------|
| C-1 | `DOC-03` edge coverage | **unresolved** (`unclassified`) | The edge probe could not classify this requirement; no acceptance criterion was auto-derived. Task 3's requirement-coverage gate is authored from `15-VALIDATION.md`'s verification map instead. Not auto-resolved with a backstop, not dropped. |
| C-2 | `DOC-04` edge coverage | **unresolved** (`unclassified`) | Same; the DOC-04 assertions in Task 3 come from `13-SOT-APPLY.md` by way of the validation map. |
| A3 | `arch/README.md` is recorded as reviewed-with-no-findings rather than omitted | resolved, enforced | Task 1 requires first-hand evidence in the record (a bootstrap-specific term), so the entry cannot be written from the research summary alone. |
| A5 | The D-38 ownership answer reaches `STATE.md` through the query handlers, never an edit | resolved, enforced | Recorded as a row in the sweep with the handler surface named; the task does not run it, and the gate asserts no direct edit was committed. |
| A-06-1 | Targeted grep over `.planning/` rather than an exhaustive read of every frozen artifact | assumption, deliberate | Research Open Question 4's recommendation. The report must state that this was the method, so a later reader knows the review's depth rather than assuming completeness. |
| A-06-2 | The phase gate tolerates one specific `[FAIL]` line from `scripts/phase14-verify.sh` | assumption, bounded | The script asserts a clean working tree (D-35) and a task verify runs before that task's commit, so the dirty-tree failure is a timing artifact. The tolerance is written as an exact-string exclusion of that one line, never as a relaxed threshold. |

<verification>
- Two scope-fence layers exist for a reason. `git diff --quiet HEAD` on each task catches an uncommitted violation; `git diff --name-only "$B"..HEAD` at the gate catches a committed one. Neither substitutes for the other.
- The forbidden-string gate is deliberately scoped to the four operator-facing files. `15-DOC-SWEEP.md` quotes every stale string it reports, and a repo-wide grep would make the findings report unwritable.
- The `chrome` guard uses the Phase 14 allowlist idiom throughout, and the `.planning/PROJECT.md` count is pinned at exactly 3 so the gate fails on both an addition and a silent deletion.
- `$B` in the range checks is the phase's planning commit, resolved at run time as
  `git log -1 --format=%H -- .planning/phases/15-playbook-safe-vs-full/15-01-PLAN.md`. It is deliberately
  not `origin/main`: `origin/main` predates `15-CONTEXT.md`, so a range anchored there would report that
  frozen artifact as changed by this phase when it was merely created before the branch was pushed. The
  anchor also survives a push mid-phase, which `origin/main` does not. Measured empty for every guarded
  path at planning time.
</verification>

<success_criteria>
- Every file in the D-20 sweep scope has an entry, including the one with no findings.
- Every deferred fix has an owner, `unowned` written explicitly where that is the truth.
- Nothing D-22 freezes was edited, and nothing under the code fences changed anywhere in the phase's commit range.
- Both existing suites are at their recorded baselines and the transcript is in the record.
</success_criteria>

<output>
Create `.planning/phases/15-playbook-safe-vs-full/15-06-SUMMARY.md` when done.
</output>