---
phase: 15-playbook-safe-vs-full
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/dots-hyprland-workflow.md
  - .planning/PROJECT.md
  - .planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md
autonomous: false
requirements: [DOC-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An operator reading the docs runs a compositor probe that exists on this machine and returns `lua`, instead of one that returns `no such option` (D-15, Correction 1)."
    - "Every operator-facing doc that names the adopt backup names `~/ii-original-dots-backup`, the directory whose `hyprland.conf` sha256 matches the recorded pre-adopt fixture (D-14, Correction 2)."
    - "The cold-clone entry pointer in `README.md` no longer promises dual-run as the destination (D-09, D-20)."
    - "A written sweep record exists and names both ground-truth corrections with their evidence (D-20, D-22)."
    - "No file under `arch/`, `scripts/`, `.config/`, `stow/` or `vendor/` is modified by this plan, and `.planning/STATE.md` / `.planning/ROADMAP.md` are untouched (D-19, D-22)."
  artifacts:
    - path: "docs/dots-hyprland-workflow.md"
      provides: "Corrected post-install verification block reachable from the Outline"
      contains: "-j status"
    - path: ".planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md"
      provides: "Doc staleness sweep record, seeded with Corrections 1 and 2"
      contains: "## Corrections applied"
    - path: ".planning/PROJECT.md"
      provides: "Product-surface status lines corrected to post-adopt truth (D-22 exception)"
      contains: "configProvider: lua"
    - path: "README.md"
      provides: "Cold-clone discovery pointer with a profile-neutral step sequence"
      contains: "docs/dots-hyprland-workflow.md"
  key_links:
    - from: "README.md"
      to: "docs/dots-hyprland-workflow.md"
      via: "arrow-sequence pointer line, root-relative link on line 9"
      pattern: "docs/dots-hyprland-workflow\\.md"
    - from: "docs/dots-hyprland-workflow.md"
      to: "scripts/phase14-verify.sh"
      via: "the verification block quotes the probe the script runs and names the script as the executable SoT"
      pattern: "phase14-verify\\.sh"
    - from: ".planning/PROJECT.md"
      to: "docs/phase14-adopt-runbook.md"
      via: "rollback product-surface line cites runbook section 14"
      pattern: "phase14-adopt-runbook\\.md"
  prohibitions:
    - statement: "The playbook must not present the full profile as the default or the recommended-by-omission path; the wrapper default is safe and `--full` is opt-in (D-07)."
    - statement: "No operator-facing doc may name a rollback source that does not hold the recoverable pre-adopt config, and no destructive or recovery-destroying command may be documented without its guard."
    - statement: "The retired session surfaces must not be misrepresented: write `Waybar`, `rofi` and `swaync` literally rather than as a collective noun, and state the D-38 screen-share consequence as a possibility rather than a certainty."
    - statement: "Frozen historical planning records must not be rewritten to agree with today's truth; staleness in them is flagged in the sweep record, never edited away (D-22)."
---

<objective>
Prove the whole documentation chain end to end on one fact: **the compositor is on the Lua entry, and here is the probe that shows it.** That single corrected fact travels every doc layer this phase touches — the cold-clone entry pointer (`README.md`), the operator playbook (`docs/dots-hyprland-workflow.md`), the planning status prose (`.planning/PROJECT.md`), and the new sweep record — and every layer is asserted to agree.

This plan also carries the phase's one gate. Three claims that `15-CONTEXT.md` instructs the playbook to assert are **false on this machine** (D-08, D-14, D-15, and by extension a conservative reading of D-02). Writing them as locked would ship a playbook whose headline verification command fails on first paste and a rollback instruction that points at a stale config. The operator confirms that deviation once, in one look, before any corrected string is written.

Purpose: catch an architectural dead end in the rewrite after one commit rather than after ten — if a prose claim cannot be deterministically asserted against the executable ground truth already in this repo, we learn it here (D-01).
Output: a corrected verification path reachable from `README.md` through the playbook; `.planning/PROJECT.md` product-surface lines true; `15-DOC-SWEEP.md` created with both corrections recorded (D-20, D-22).
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-playbook-safe-vs-full/15-CONTEXT.md
@.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md
@.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Confirm the three ground-truth deviations from locked decisions D-08 / D-14 / D-15</name>
  <decision>Write the verified ground-truth values, or the literal text of locked decisions D-08 / D-14 / D-15?</decision>
  <context>
`15-RESEARCH.md` section "GROUND-TRUTH CORRECTIONS — read before planning" (lines 170-260) found three claims that `15-CONTEXT.md` instructs this phase to assert but which are false on this machine. Each is backed by pasted probe output in that section. Deviating from locked decision text is not a call the planner may make silently.

**Correction 1 (D-08, D-15, and the CONTEXT.md integration-points list).** The compositor-option read named in those decisions returns `no such option` on Hyprland 0.56.2 for all three spellings tried. The probe that works — and the one `scripts/phase14-verify.sh:168-173` actually runs — is `hyprctl -j status` read through `jq -r .configProvider`, returning `lua`. `hyprctl eval 'return 1+1'` returning `ok` is the token-independent backup. The recorded pre-adopt value was `hyprlang` (`14-PRE-ADOPT-BASELINE.txt`). The wrong command already propagated into `.planning/PROJECT.md:18`.

**Correction 2 (D-14).** Both `~/ii-original-dots-backup` and `~/ii-original-dots-backup.20260904T171128Z` exist. Hashing the `hyprland.conf` inside each against the recorded fixture `hyprland_conf_sha256=3d17932a…` shows the **plain** directory holds the genuine pre-adopt conf and the **timestamped** one holds a July config (`c5c65023…`). The timestamp is the moment runbook section 5 rotated the old backup aside, not the moment of the adopt. `arch/dots-hyprland.sh:21` confirms the wrapper's own default has no suffix. The wrong path already propagated into `.planning/PROJECT.md:28`.

**Correction 3 (a D-21 target, and a conservative reading of D-02).** `docs/phase14-adopt-runbook.md:334-336` tells an operator mid-rollback to restore tier-1 source 3 from the timestamped directory — that is, from the stale July config. This is the highest-severity finding in the phase because it sits inside a recovery procedure. D-21 authorises correcting factually false post-adopt lines in the runbook; D-02 preserves its structure and role. Research assumption A2 records that reading.

Cost of choosing wrong: low for the doc edit itself, but an operator who follows a wrong rollback instruction restores a stale config and may then escalate to tier 2, which deletes the ii hooks from repo `.config/hypr/hyprland.conf` and thereby destroys rollback tier-1 source 2.

**Why this is a `checkpoint:decision` rather than a `checkpoint:human-verify`:** `workflow.human_verify_mode` is `end-of-phase` in `.planning/config.json`, so a planner-emitted human-verify is suppressed and harvested only after the phase — which is after the corrected strings would already be written. `checkpoint:decision` is unaffected by that mode and stops before the work, which is what "confirm the deviation before writing it" requires. The divergence from the spawn instruction is recorded under `## Flagged Assumptions` below.
  </context>
  <options>
    <option id="verified">
      <name>Write the verified values (recommended)</name>
      <pros>The playbook's headline verification command actually runs and returns `lua`. The named backup directory actually holds the pre-adopt config. The runbook stops pointing a recovering operator at a stale config. Every claim becomes machine-assertable against `scripts/phase14-verify.sh` and `arch/dots-hyprland.sh`, so the same drift cannot recur silently.</pros>
      <cons>Deviates from the literal text of D-08, D-14 and D-15. `15-CONTEXT.md` lines 30, 39, 40 and 127 keep the errors — it is a frozen artifact under D-22, so it gets flagged in the sweep record rather than edited.</cons>
    </option>
    <option id="literal">
      <name>Write the locked decision text verbatim</name>
      <pros>Zero deviation from CONTEXT.md.</pros>
      <cons>Ships a playbook whose first verification command returns `no such option`, so the first operator to paste it concludes the adopt failed. Ships a backup path that does not hold the recoverable config. Leaves a wrong rollback source inside a recovery procedure.</cons>
    </option>
    <option id="split">
      <name>Write Corrections 1 and 2 here, defer Correction 3 to a phase that owns the runbook</name>
      <pros>Keeps the runbook untouched if D-02 is read as forbidding any edit at all.</pros>
      <cons>Leaves the highest-severity finding — a wrong rollback source — published and unowned. D-21 explicitly authorises the fix, and plan 15-03 is scoped to it.</cons>
    </option>
  </options>
  <resume-signal>Reply `verified`, `literal`, or `split`. Reply `verified` to proceed as planned.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: End-to-end verification path — one corrected fact through README, playbook, PROJECT.md and the sweep record</name>
  <reversibility rating="costly">The corrected probe and backup strings become the operator-facing contract every later section of this phase is written against; changing them afterwards means re-editing four files plus the sweep record. Confirmed at Task 1.</reversibility>
  <read_first>
    - `docs/dots-hyprland-workflow.md` — lines 36-48 (the `## Outline` list) and lines 172-215 (`## 4. Session hooks & dual-run expectations`), the two regions this task edits
    - `README.md` — lines 5-11, the pointer block
    - `.planning/PROJECT.md` — lines 14-31, the "Phase 14 delivered" paragraph and the Product surface list
    - `.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md` — section "GROUND-TRUTH CORRECTIONS" (lines 170-260) and section "Post-install verification — corrected D-15 block" (lines 562-595) for the exact six checks and their observed output
    - `.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md` — the "Command block + expected output pattern" and "Citation pattern" sections, which govern fence style and the `# expect:` idiom
    - `scripts/phase14-verify.sh` — lines 160-200, the probes the doc must agree with (READ ONLY — the phase scope fence forbids editing `scripts/`)
    - `.planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt` — the recorded `configProvider_pre` value and the conf sha256 the corrections rest on
    - `.planning/codebase/CONVENTIONS.md` — repo doc and bash conventions (this repo has no CLAUDE.md)
  </read_first>
  <files>README.md, docs/dots-hyprland-workflow.md, .planning/PROJECT.md, .planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md</files>
  <action>
Thread one corrected fact through every doc layer this phase touches. Four edits; the slice is not done until all four agree.

**1. `README.md` line 7.** Replace the parenthesised arrow sequence so the entry pointer stops promising dual-run as the destination (D-20, D-09). The current sequence names clone, recursive submodule, install, dual-run, pin-bump update. Write it as: clone, recursive submodule, gate, install, session, verify, pin-bump update — same `→` separator, same bold `**Operator playbook**` label, same trailing colon. Keep the blank line and the `→ [docs/dots-hyprland-workflow.md](docs/dots-hyprland-workflow.md)` pointer line exactly as they are; that link is root-relative without `./` and must not change (D-23). Change nothing else in `README.md` — the Neovim and T-MUX sections are out of scope.

**2. `docs/dots-hyprland-workflow.md` — replace the dual-run subsection with the corrected verification block (D-01, D-15).** Retitle the top-level heading `## 4. Session hooks & dual-run expectations` to `## 4. Session model & verification`, and update the matching `## Outline` entry on line 41 to point at `#4-session-model--verification` with link text `Session model & verification`. Then delete the subsection headed `### Dual-run (intentional this milestone)` together with its three bullets and the `Optional soft process checks` fence that immediately follows it, and put in its place a subsection headed `### Verify the session after login`.

That new subsection carries the corrected D-15 checks as a single fenced `bash` block using the repo's in-fence `# expect:` comment idiom (`15-PATTERNS.md`, "Command block + expected output pattern"), in this order, taken from `15-RESEARCH.md` lines 562-595:
(a) `hyprctl -j status` piped through `jq -r .configProvider`, expecting `lua`, with a one-line in-fence note that the value recorded in `14-PRE-ADOPT-BASELINE.txt` before the adopt was `hyprlang`;
(b) `hyprctl eval 'return 1+1'` expecting `ok`, labelled as the token-independent backup;
(c) `pgrep -f 'qs -c ii'` proving the ii shell is running;
(d) three `test` invocations proving `~/.config/hypr/hyprland.lua` is present, `~/.config/hypr/hyprland.conf.old` is present, and `~/.config/hypr/hyprland.conf` is absent so no `.conf` can win over the Lua entry;
(e) an `ls` of `~/.config/hypr/custom/` covering `general.lua`, `env.lua` and `execs.lua`;
(f) `./scripts/phase14-verify.sh`, expecting its `=== done: FAIL=0 FINDINGS=1 ===` line, with the single finding identified in the comment as the D-38 known loss.

Above the fence, one sentence stating these checks run **after login**, matching the runbook's section 11 to section 12 ordering, because the first three need an active Hyprland session. Below the fence, one sentence in prose naming `scripts/phase14-verify.sh` as the executable source of truth for everything in the block, and one sentence pointing failures at `docs/phase14-adopt-runbook.md` section 14 — no failure-branch tree (D-15).

Do not use the compositor-option spelling `getoption configProvider` anywhere in the file. It returns `no such option` on Hyprland 0.56.2 and is the exact defect Correction 1 removes.
<!-- planner-discipline-allow: getoption configProvider -->

Leave the rest of section 4 alone for now — the `### Personal hypr hooks (two lines)` and `### Live product path` subsections stay as they are. Plan `15-04` replaces them with the full session model and demotes the conf-hook method to a single pointer line (D-08); this task deliberately closes only the verification path, so the file is briefly incomplete on the session model but never wrong about verification.

**3. `.planning/PROJECT.md` — two product-surface lines (the D-22 correction exception).** On line 18, inside the "Phase 14 delivered" paragraph, replace the parenthesised probe with the working one, phrased so the file contains the literal `configProvider: lua`. On line 28, in the Product surface list, replace the timestamped rollback directory with `~/ii-original-dots-backup`, keeping the rest of that line — the `hyprland.conf.old` mention and the `docs/phase14-adopt-runbook.md` section 14 citation — intact. Match the tense and density of the already-correct lines 8 and 27. Touch nothing else in this file: the collective-noun occurrences at lines 43, 106 and 127 are historical records outside D-22's correction exception, and are recorded as findings in the sweep record instead.

**4. Create `.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md`.** Seed the sweep record now so the phase has a findings surface from its first commit (D-20, D-22). Follow the per-file structure of `15-RESEARCH.md` section "Doc Sweep Inventory": a title, a one-line scope statement naming D-20 and D-22, then a section headed `## Corrections applied` holding a table with columns File, Line, Stale text, Correction, Evidence. Enter two rows now — `.planning/PROJECT.md` line 18 (wrong compositor probe; evidence: the live probe output and `scripts/phase14-verify.sh:168-173`) and `.planning/PROJECT.md` line 28 (wrong backup directory; evidence: the sha256 comparison against `14-PRE-ADOPT-BASELINE.txt` and `arch/dots-hyprland.sh:21`). Add two empty sections headed `## Reviewed, no findings` and `## Flagged, not edited (D-22)`; plan `15-06` fills both. Quoting stale strings inside this record is correct and expected — it is a findings report, not operator instruction, which is why the forbidden-string assertions in this phase are scoped to the operator-facing docs and never to this file.

Write `Waybar`, `rofi` and `swaync` literally wherever the trio is named, never as a collective noun (D-09, Phase 14 D-39). The only legal browser token this phase may write is `google-chrome-stable`.

Do not edit `arch/`, `scripts/`, `.config/`, `stow/`, `vendor/`, `.planning/STATE.md` or `.planning/ROADMAP.md`.
  </action>
  <verify>
    <automated>grep -q -- '-j status' docs/dots-hyprland-workflow.md && grep -q 'configProvider' docs/dots-hyprland-workflow.md && grep -q 'phase14-verify.sh' docs/dots-hyprland-workflow.md && grep -q 'configProvider: lua' .planning/PROJECT.md && test -z "$(grep -l 'getoption configProvider' docs/dots-hyprland-workflow.md docs/phase14-adopt-runbook.md README.md .planning/PROJECT.md 2>/dev/null)" && ! grep -qE 'ii-original-dots-backup\.[0-9]{8}T' docs/dots-hyprland-workflow.md .planning/PROJECT.md && ! grep -q 'dual-run' README.md && ! grep -qF 'Dual-run (intentional this milestone)' docs/dots-hyprland-workflow.md && ! grep -qF 'both bars OK' docs/dots-hyprland-workflow.md && ! grep -qF 'No Waybar cutover' docs/dots-hyprland-workflow.md && test -s .planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md && grep -q '## Corrections applied' .planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md</automated>
    <fails_when>non-zero exit — the playbook lacks `-j status` / `configProvider` / the `phase14-verify.sh` citation, `.planning/PROJECT.md` lacks `configProvider: lua`, `grep -l` prints any filename because the dead compositor-option spelling survived somewhere, the rotated `ii-original-dots-backup.<timestamp>` form still matches, `README.md` still contains `dual-run`, any of the three deleted stale fragments still matches, or the sweep record is missing/empty/lacks its `## Corrections applied` heading</fails_when>
    <automated>ok=1; while read -r a; do h="${a#\#}"; grep -oE '^#{2,3} .*' docs/dots-hyprland-workflow.md | sed 's/^#* //' | tr 'A-Z' 'a-z' | sed 's/[^a-z0-9 -]//g; s/ /-/g' | grep -qx "$h" || { echo "MISSING ANCHOR: $h"; ok=0; }; done < <(grep -oE '\(#[a-z0-9-]+\)' docs/dots-hyprland-workflow.md | tr -d '()' | sort -u); test "$ok" -eq 1</automated>
    <fails_when>the loop prints a `MISSING ANCHOR:` line and the final `test` exits non-zero — an Outline entry points at a slug no `##`/`###` heading in the file produces (this is what a retitled section without a matching Outline edit looks like)</fails_when>
    <automated>for f in docs/dots-hyprland-workflow.md docs/phase14-adopt-runbook.md README.md; do base="$(dirname "$f")"; grep -o '](\.\{0,2\}/\?[^)#][^)]*)' "$f" | sed 's/](\(.*\))/\1/' | grep -v '^http' | while read -r l; do [ -e "$base/$l" ] || [ -e "$l" ] || { echo "BROKEN $f -> $l"; exit 1; }; done || exit 1; done</automated>
    <fails_when>a `BROKEN <file> -> <path>` line is printed and the loop exits non-zero — a relative Markdown link in one of the three prose docs resolves to a path that does not exist</fails_when>
    <automated>test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' docs/dots-hyprland-workflow.md README.md .planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md | sed 's/^[^:]*://' | grep -vx 'google-chrome-stable' | wc -l)" -eq 0 && test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' .planning/PROJECT.md | grep -vx 'google-chrome-stable' | wc -l)" -eq 3</automated>
    <fails_when>either count is wrong — the first is above 0 because a doc this task wrote used the collective noun instead of naming `Waybar`, `rofi` and `swaync` literally; the second is not exactly 3 because the task added a new occurrence to `.planning/PROJECT.md` (above 3) or edited away one of the three historical records at lines 43/106/127 that D-22 freezes (below 3)</fails_when>
    <automated>B="$(git log -1 --format=%H -- .planning/phases/15-playbook-safe-vs-full/15-01-PLAN.md)" && test -n "$B" && git diff --quiet HEAD -- arch/ scripts/ .config/ stow/ vendor/ .planning/STATE.md .planning/ROADMAP.md && test -z "$(git diff --name-only "$B"..HEAD -- arch/ scripts/ .config/ stow/ vendor/)" && ./scripts/phase13-d19-assert.sh >/tmp/p15-01-d19.txt 2>&1 && test "$(grep -c '^\[FAIL\]' /tmp/p15-01-d19.txt)" -eq 0</automated>
    <fails_when>non-zero exit — `git diff --quiet` reports an uncommitted change under a fenced directory or in `STATE.md`/`ROADMAP.md`, `git diff --name-only "$B"..HEAD` prints any path under the five fenced directories (a documentation phase committed a code change), or `scripts/phase13-d19-assert.sh` prints a `[FAIL]` line — its baseline today is 15 `[PASS]`, 0 `[FAIL]`</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/dots-hyprland-workflow.md` contains the literal `-j status`, the literal `configProvider`, and the literal `phase14-verify.sh`.
    - `.planning/PROJECT.md` contains the literal `configProvider: lua`.
    - The dead compositor-option spelling appears in none of `docs/dots-hyprland-workflow.md`, `docs/phase14-adopt-runbook.md`, `README.md`, `.planning/PROJECT.md`.
    - Neither `docs/dots-hyprland-workflow.md` nor `.planning/PROJECT.md` matches the rotated-backup form `ii-original-dots-backup\.[0-9]{8}T`; `.planning/PROJECT.md` still contains `ii-original-dots-backup`.
    - `README.md` contains no occurrence of `dual-run`, and line 9 still holds `→ [docs/dots-hyprland-workflow.md](docs/dots-hyprland-workflow.md)` unchanged.
    - `docs/dots-hyprland-workflow.md` contains a heading whose text is `4. Session model & verification` and a subheading whose text is `Verify the session after login`.
    - `docs/dots-hyprland-workflow.md` contains none of the three deleted fragments: the dual-run subsection heading text, `both bars OK`, `No Waybar cutover`.
    - Every in-page anchor referenced from the playbook's Outline resolves to a heading in the same file under the GitHub slug transform (lowercase, drop characters outside `[a-z0-9 -]`, spaces to hyphens).
    - Every relative Markdown link in `docs/dots-hyprland-workflow.md`, `docs/phase14-adopt-runbook.md` and `README.md` resolves to an existing path.
    - `.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md` exists, is non-empty, and contains the headings `## Corrections applied`, `## Reviewed, no findings`, `## Flagged, not edited (D-22)`.
    - Non-allowlisted `chrome` tokens: 0 across `docs/dots-hyprland-workflow.md`, `README.md`, `15-DOC-SWEEP.md`; exactly 3 in `.planning/PROJECT.md` (the frozen historical records).
    - `git diff --name-only "$B"..HEAD -- arch/ scripts/ .config/ stow/ vendor/` prints nothing.
    - `./scripts/phase13-d19-assert.sh` exits 0 with zero `[FAIL]` lines.
  </acceptance_criteria>
  <done>One operator-verification path works end to end and is committed: the README pointer leads to the playbook, the playbook's verification block runs the probe that exists on this machine and names `scripts/phase14-verify.sh` as its executable SoT, `.planning/PROJECT.md` states the same two facts, and `15-DOC-SWEEP.md` records both corrections with their evidence. Section 4's safe-profile hook description is deliberately left intact and is demoted to a pointer line by plan `15-04` (D-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published prose → operator's shell | Every fenced command in these docs is pasted verbatim by a human into a root-capable shell on a live machine. A wrong string here is executed, not reviewed. |
| published prose → operator's recovery path | The rollback material named in `.planning/PROJECT.md` and the playbook is what a human reaches for after the session is already broken. A wrong path here is discovered at the worst possible moment. |
| repo prose → frozen planning history | The sweep may only flag phase-scoped artifacts; a doc phase that edits them destroys the decision audit trail (D-22). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Denial of Service | playbook verification block, `.planning/PROJECT.md:18` | high | mitigate | Quote the probe `scripts/phase14-verify.sh:168-173` actually runs (`hyprctl -j status` → `configProvider`); the task verify greps for `-j status` present and the dead `getoption` spelling absent across all four operator-facing files |
| T-15-02 | Tampering | `.planning/PROJECT.md:28` rollback line | high | mitigate | Name `~/ii-original-dots-backup`, the directory whose conf sha256 matches `14-PRE-ADOPT-BASELINE.txt`; the task verify bans the rotated `ii-original-dots-backup.<timestamp>` form by regex in both edited files |
| T-15-03 | Tampering | `arch/`, `scripts/`, `.config/`, `stow/`, `vendor/` | high | mitigate | Two-sided scope-fence guard: `git diff --quiet HEAD` for uncommitted changes plus `git diff --name-only "$B"..HEAD` for committed ones, both empty today |
| T-15-04 | Repudiation | frozen phase artifacts and `.planning/PROJECT.md` historical lines | medium | mitigate | D-22 flag-don't-rewrite; the `chrome`-token count in `.planning/PROJECT.md` is pinned at exactly 3 so silently editing a historical record fails the gate in the same way adding one does |
| T-15-05 | Information Disclosure | operator docs | low | accept | These docs name no secret. The only env var mentioned anywhere in the phase is `ILLOGICAL_IMPULSE_VIRTUAL_ENV`, which is a path, not a credential |
| T-15-SC | Tampering | package-manager installs | low | accept | No `npm` / `pip` / `cargo` / `pacman` install task exists in this phase — it is documentation-only (`15-RESEARCH.md` § Package Legitimacy Audit: not applicable). No supply-chain surface to gate |
</threat_model>

## Artifacts this phase produces

Created or renamed by **this plan**:

| Artifact | Kind | Note |
|----------|------|------|
| `.planning/phases/15-playbook-safe-vs-full/15-DOC-SWEEP.md` | new file | the phase's sweep record (D-20, D-22) |
| `## Corrections applied` | new heading in `15-DOC-SWEEP.md` | anchor `#corrections-applied` |
| `## Reviewed, no findings` | new heading in `15-DOC-SWEEP.md` | anchor `#reviewed-no-findings`; filled by `15-06` |
| `## Flagged, not edited (D-22)` | new heading in `15-DOC-SWEEP.md` | anchor `#flagged-not-edited-d-22`; filled by `15-06` |
| `## 4. Session model & verification` | renamed heading in `docs/dots-hyprland-workflow.md` | anchor `#4-session-model--verification`, replacing `#4-session-hooks--dual-run-expectations` |
| `### Verify the session after login` | new subheading in `docs/dots-hyprland-workflow.md` | promoted to top-level `## 7. Verify after login` by plan `15-04` |

Removed by this plan: the `### Dual-run (intentional this milestone)` subheading and its `Optional soft process checks` fence.

## Flagged Assumptions

Surfaced, not silently resolved. Rows 1-2 are the spec-less edge-probe report; the rest carry `15-RESEARCH.md`'s Assumptions Log forward and record where this plan diverges from a literal instruction.

| # | Item | Status | Why it is flagged |
|---|------|--------|-------------------|
| C-1 | `DOC-03` edge coverage | **unresolved** (`unclassified`) | The edge probe could not classify this requirement into its shape taxonomy; no acceptance criterion was auto-derived from it. DOC-03's must-have coverage in this phase comes from the goal-backward truths above, not from the probe. Not auto-resolved with a backstop, not dropped. |
| C-2 | `DOC-04` edge coverage | **unresolved** (`unclassified`) | Same. DOC-04 coverage is authored in plan `15-04` from `13-SOT-APPLY.md`, not from the probe. |
| A1 | Writing verified values over the literal text of D-08 / D-14 / D-15 | gated | Resolved by Task 1's `checkpoint:decision`, not by the planner. |
| A2 | Correcting runbook section 14 tier-1 source 3 sits inside D-21, not outside D-02 | gated | Same checkpoint; plan `15-03` owns the edit. |
| DIV-1 | The spawn instruction asked for a `checkpoint:human-verify` before the corrected strings are written; this plan emits a `checkpoint:decision` instead | divergence, deliberate | `workflow.human_verify_mode` is `end-of-phase`, which suppresses planner-emitted human-verify tasks and harvests them *after* the phase — too late to gate a write. `checkpoint:decision` is explicitly unaffected by that mode. Same operator, same evidence, same one-look confirmation; correct timing. |
| DIV-2 | The tracer leaves `docs/dots-hyprland-workflow.md` internally incomplete for one commit — the new verification block coexists with the old conf-hook session description | divergence, deliberate | Tracer-first accepts functionality gaps, not architectural ones. Plan `15-04` closes it (D-08). Called out in `<done>` so it is not mistaken for an oversight. |
| A4 | No `scripts/phase15-docs-assert.sh` is created | resolved against research | `15-RESEARCH.md` proposes one; the phase scope fence in `15-CONTEXT.md:11` forbids new scripts, and `15-PATTERNS.md` and `15-VALIDATION.md` both resolve toward inlining. The assertion shapes are inlined into the verify blocks above. |

<verification>
- Both existing suites are the regression floor. `./scripts/phase13-d19-assert.sh` must stay at 15 `[PASS]` / 0 `[FAIL]`. `./scripts/phase14-verify.sh` additionally asserts a clean working tree (D-35), so it is run at the phase gate in plan `15-06` after commits land, not inside a pre-commit task verify.
- The scope fence is checked two ways because a doc phase can violate it either by leaving an uncommitted edit or by committing one.
- The `chrome` guard uses the Phase 14 allowlist idiom (`grep -vx 'google-chrome-stable'`), never a naive `\bchrome\b` ban, which would fire on the legitimate autostart name. Any `rofi` grep added later must use `\brofi\b`, because this phase's playbook contains "profile" many times by construction.
- `$B` in the range checks is the phase's planning commit, resolved at run time as
  `git log -1 --format=%H -- .planning/phases/15-playbook-safe-vs-full/15-01-PLAN.md`. It is deliberately
  not `origin/main`: `origin/main` predates `15-CONTEXT.md`, so a range anchored there would report that
  frozen artifact as changed by this phase when it was merely created before the branch was pushed. The
  anchor also survives a push mid-phase, which `origin/main` does not. Measured empty for every guarded
  path at planning time.
</verification>

<success_criteria>
- An operator following `README.md` reaches the playbook and finds a verification block whose first command returns `lua` on this machine.
- No operator-facing doc names a rollback source that does not hold the recoverable pre-adopt config.
- `15-DOC-SWEEP.md` exists and records both applied corrections with evidence.
- Nothing under `arch/`, `scripts/`, `.config/`, `stow/`, `vendor/`, `.planning/STATE.md`, `.planning/ROADMAP.md` changed.
</success_criteria>

<output>
Create `.planning/phases/15-playbook-safe-vs-full/15-01-SUMMARY.md` when done.
</output>