---
phase: 15-playbook-safe-vs-full
plan: 04
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - docs/dots-hyprland-workflow.md
autonomous: true
requirements: [DOC-03, DOC-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A reader walks the full profile end to end — dry-run, backup gate, live install — from one linear section, and learns at the gate that bare `--skip-backup` is refused without `--allow-skip-backup` (D-05, D-14)."
    - "A reader learns that the backup lands at `~/ii-original-dots-backup` and how to confirm by content rather than by name (D-14, Correction 2)."
    - "A reader learns the full session model: `~/.config/hypr/hyprland.lua` is the entry, `configProvider` reports `lua` where it reported `hyprlang` before, upstream renamed the previous conf to `.old`, and personal overlays live under `~/.config/hypr/custom/` (D-08)."
    - "The safe profile's two conf-hook lines are one pointer line, not a section (D-08)."
    - "A reader learns the overlay policy in the playbook's own words — repo `.config/hypr/custom/` is the authoring SoT, apply is one-way repo to live, named files only, never into `vendor/dots-hyprland` or the fork — with `13-SOT-APPLY.md` named as the SoT it summarises (DOC-04, D-16)."
    - "The last of D-10's three stale claims is gone: the file no longer frames dual-run as intentional for this milestone (D-09, D-10)."
  artifacts:
    - path: "docs/dots-hyprland-workflow.md"
      provides: "Install walkthrough with the inline backup gate, the full session model, the overlay policy section, and the promoted verification section"
      contains: "## 6. Personal overlays: repo, live, fork"
  key_links:
    - from: "docs/dots-hyprland-workflow.md"
      to: ".planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md"
      via: "the overlay section narrates this SoT and names it, per Phase 13 D-05"
      pattern: "13-SOT-APPLY\\.md"
    - from: "docs/dots-hyprland-workflow.md"
      to: "scripts/phase13-d19-assert.sh"
      via: "the overlay section cites the in-repo verify rather than restating its checks"
      pattern: "phase13-d19-assert\\.sh"
    - from: "docs/dots-hyprland-workflow.md"
      to: "arch/dots-hyprland.sh"
      via: "the backup gate paragraph quotes the wrapper's dual-key refusal and its backup-directory default"
      pattern: "allow-skip-backup"
  prohibitions:
    - statement: "The playbook must not present the full profile as the default or the recommended-by-omission path; the wrapper default is safe and `--full` is opt-in (D-07)."
    - statement: "No operator-facing doc may name a rollback source that does not hold the recoverable pre-adopt config, and no destructive or recovery-destroying command may be documented without its guard."
    - statement: "The retired session surfaces must not be misrepresented: write `Waybar`, `rofi` and `swaync` literally rather than as a collective noun, and state the D-38 screen-share consequence as a possibility rather than a certainty."
    - statement: "Frozen historical planning records must not be rewritten to agree with today's truth; staleness in them is flagged in the sweep record, never edited away (D-22)."
---

<objective>
Write the middle of the spine: the install walkthrough that walks full end to end (D-05), the session model that describes what a full install actually produces (D-08), and the overlay section that delivers DOC-04 in the playbook's own words (D-16).

This is where the phase's second requirement lands. DOC-04 asks the playbook to document hypr/custom overlay expectations and the repo/live/fork source-of-truth policy from OVL-03; `13-SOT-APPLY.md` already holds that policy authoritatively, so the playbook narrates it and names it rather than replacing it (Phase 13 D-05).

Purpose: DOC-03's install path and DOC-04 in full.
Output: sections 4, 5, 6 and 7 of the playbook written, and the last of D-10's three stale claims removed.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-playbook-safe-vs-full/15-CONTEXT.md
@.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md
@.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md
@.planning/phases/15-playbook-safe-vs-full/15-02-SUMMARY.md
@.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write section 4 — the full install walkthrough with the backup gate inline</name>
  <read_first>
    - `docs/dots-hyprland-workflow.md` — the whole of `## 4. Install via the thin wrapper` in its post-`15-02` state, including the subcommand table and the hooks paragraph `15-02` already corrected
    - `arch/dots-hyprland.sh` — lines 165-195 (the backup gate and its exact `yes` token), line 21 (`II_BACKUP_DIR`), lines 177-183 (the full-profile blast-radius echoes), lines 1430-1434 (the `--skip-backup` dual-key refusal). READ ONLY
    - the live output of `./arch/dots-hyprland.sh help` — the subcommand allowlist, which the doc's subcommand table must not contradict
    - `.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md` — "Backup gate — Verified Ground Truth" (lines 533-561)
    - `docs/phase14-adopt-runbook.md` — `## 14. Rollback`, in its post-`15-03` state, so the citation this section makes is accurate
    - `.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md` — the "Command block + expected output pattern" and "Literal-value blocks use `text`" sections
  </read_first>
  <files>docs/dots-hyprland-workflow.md</files>
  <action>
Rewrite the body of `## 4. Install via the thin wrapper` so it walks the **full** profile end to end (D-05), on the same spine, with the safe difference stated in place rather than as a fork (D-03).

Keep the section's existing scaffolding: the "only install entry" opening, the preflight paragraph about the wrapper printing the recursive-init fix without auto-initing, the allowlisted-subcommand table, and the hooks paragraph `15-02` corrected. Rewrite the walkthrough between them.

**Dry-run first.** Show `./arch/dots-hyprland.sh install --full --dry-run` as the command the walkthrough uses, in a fenced `bash` block with the `# expect:` idiom. State what a full dry-run shows that a default one does not: the argv carries no `--core`, no `--skip-hyprland`, no `--skip-sysupdate`, and the wrapper prints its full-profile line instead of its safe-defaults line. Immediately follow with one sentence, not a fork, saying that dropping `--full` gives the safe profile and injects the triple instead — with a pointer back to the profiles section for the consequences, per D-07 and D-03.

**Backup gate, inline (D-14).** Write this immediately before the live install, because that is where the operator meets it. State four facts, each traceable to `arch/dots-hyprland.sh`:
1. The gate is interactive and the token is exact — it reads a line from stdin and aborts unless the answer is exactly `yes`, invoking nothing.
2. Upstream backs clashing paths up to the wrapper's `II_BACKUP_DIR` default, `~/ii-original-dots-backup`, quoted in a fenced `text` block per the literal-value convention. Add one sentence saying that if a directory of that name already exists from an earlier install, upstream may skip taking a fresh backup entirely, so confirm the backup by content — compare `sha256sum` of the conf inside it against what you recorded before the run — rather than trusting the directory's presence. Do not name the rotated timestamped form as a backup or as a rollback source; it is the *previous* backup moved aside, and naming it here is the Correction 2 defect.
3. Bare `--skip-backup` is refused. The wrapper exits before the gate unless `--allow-skip-backup` is also present, and the refusal message says a first adoption must not skip backup. State the dual-key requirement and state plainly that this document shows no bare-`--skip-backup` example on purpose.
4. Where the backup is used from: cite `docs/phase14-adopt-runbook.md` section 14 for the three-tier rollback and copy no tier into this file (D-02, D-14).

**Live install.** Show `./arch/dots-hyprland.sh install --full` in a fenced `bash` block, then a short list of what a full run does to the machine, taken from the wrapper's own full-profile echo lines at `arch/dots-hyprland.sh:177-183`: no safe-defaults residual on this path; personal `hyprland.conf` may be renamed to `.old` by the upstream install; the misc overlay may overwrite when the core residual is absent; `pacman -Syu` may run on the deps portion; upstream may back clashing paths up to `~/ii-original-dots-backup`; bare skip-backup is still refused. Close with one sentence sending the reader to section 3's gate if they have not run it, and one sending them to section 7 after they log back in.

Name no wrapper flag beyond `--core`, `--skip-hyprland`, `--skip-sysupdate`, `--full`, `--skip-backup`, `--allow-skip-backup`, `--dry-run`, `--force` and `--install-missing` (D-12). Never present the upstream vendor tree's own removal subcommand, or any dangerous-override token, as an option anywhere in this section.
  </action>
  <verify>
    <automated>grep -q -- '--allow-skip-backup' docs/dots-hyprland-workflow.md && grep -q -- '--skip-backup' docs/dots-hyprland-workflow.md && grep -q 'ii-original-dots-backup' docs/dots-hyprland-workflow.md && ! grep -qE 'ii-original-dots-backup\.[0-9]{8}T' docs/dots-hyprland-workflow.md && grep -q 'sha256sum' docs/dots-hyprland-workflow.md && grep -qF 'phase14-adopt-runbook.md' docs/dots-hyprland-workflow.md && grep -qE '\./arch/dots-hyprland\.sh install --full' docs/dots-hyprland-workflow.md</automated>
    <fails_when>non-zero exit — the dual-key refusal is not documented (either flag missing), the backup directory is unnamed, the rotated timestamped form was reintroduced, no content-based confirmation (`sha256sum`) is offered, the runbook is not cited for rollback, or no `install --full` command line appears</fails_when>
    <automated>HELP="$(./arch/dots-hyprland.sh help 2>&1)"; rc=0; for sub in $(grep -oE '^\s*\./arch/dots-hyprland\.sh [a-z-]+' docs/dots-hyprland-workflow.md | awk '{print $2}' | sort -u); do grep -qE "^  $sub " <<<"$HELP" || { echo "NOT ALLOWLISTED: $sub"; rc=1; }; done; test "$rc" -eq 0</automated>
    <fails_when>a `NOT ALLOWLISTED: <subcommand>` line is printed and the final test exits non-zero — the playbook shows a command line invoking a subcommand `./arch/dots-hyprland.sh help` does not list, which is either an invented subcommand or one the wrapper refuses. The check reads only line-leading invocations inside fences, so `exp-merge` named as a refused path in the non-goals prose is correctly not matched</fails_when>
    <automated>test -z "$(grep -oE '^[[:space:]]*(#[[:space:]]*)?\./arch/dots-hyprland\.sh .*' docs/dots-hyprland-workflow.md | grep -oE '\-\-[a-z][a-z-]*' | sort -u | grep -vxE '\-\-(core|skip-hyprland|skip-sysupdate|full|skip-backup|allow-skip-backup|dry-run|force|install-missing)' || true)" && test "$(grep -c 'UPSTREAM-UNINSTALL\|upstream-dangerous' docs/dots-hyprland-workflow.md)" -eq 0</automated>
    <fails_when>the first `test -z` fails because a wrapper command line in a fence carries a flag outside the nine D-12 permits — that is the doc drifting toward reproducing the help text; or the second test fails because the count is above 0, meaning a dangerous-override token was written into the operator path. The extraction reads only line-leading wrapper invocations, so Outline anchor slugs containing doubled hyphens are correctly not matched</fails_when>
    <automated>test "$(grep -cE '^## [0-9]+\. ' docs/dots-hyprland-workflow.md)" -eq 7 && git diff --quiet HEAD -- arch/ scripts/ .config/ stow/ vendor/ .planning/STATE.md .planning/ROADMAP.md && test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' docs/dots-hyprland-workflow.md | grep -vx 'google-chrome-stable' | wc -l)" -eq 0</automated>
    <fails_when>the numbered-heading count is not 7 — this task rewrites section 4's body and must add no heading and drop none; or `git diff --quiet` reports an uncommitted change under a fenced directory or in `STATE.md` / `ROADMAP.md`; or a non-allowlisted `chrome` token was introduced</fails_when>
  </verify>
  <acceptance_criteria>
    - `## 4. Install via the thin wrapper` shows `./arch/dots-hyprland.sh install --full --dry-run` and `./arch/dots-hyprland.sh install --full` as fenced command lines.
    - The section names `~/ii-original-dots-backup` and does not match the rotated form `ii-original-dots-backup\.[0-9]{8}T`.
    - The section documents the exact-`yes` gate token, the dual-key `--skip-backup` / `--allow-skip-backup` refusal, and offers `sha256sum` as the content-based way to confirm a fresh backup was taken.
    - The section cites `docs/phase14-adopt-runbook.md` for rollback and reproduces none of its three tiers.
    - One sentence states that dropping `--full` gives the safe profile and injects the triple, pointing back to the profiles section rather than forking the walkthrough.
    - Every wrapper subcommand appearing on a fenced command line is present in `./arch/dots-hyprland.sh help`'s allowlist.
    - Every flag on a fenced wrapper command line is one of the nine D-12 permits.
    - The file contains no dangerous-override token and no bare `--skip-backup` example.
    - The file still contains exactly 7 `## N.` headings; non-allowlisted `chrome` tokens number 0.
  </acceptance_criteria>
  <done>A reader walks the full profile from dry-run to live install in one linear section, meets the backup gate where it actually happens, and knows both what full does to the machine and that the safe profile is one dropped flag away.</done>
</task>

<task type="auto">
  <name>Task 2: Write section 5 — the full session model — and promote the verification block to section 7</name>
  <read_first>
    - `docs/dots-hyprland-workflow.md` — all of `## 5. Session model after a full install` in its post-`15-02` state: the `### Personal hypr hooks (two lines)` subsection, the `### Live product path` subsection, the `### Verify the session after login` subsection `15-01` created, and the `### Mid-session reload` subsection
    - `.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md` — "Correction 1" (lines 174-208) for the probe and the recorded pre-adopt value, and "Post-install verification — corrected D-15 block" (lines 562-595)
    - `scripts/phase14-verify.sh` — lines 160-200, the probes this section must agree with. READ ONLY
    - `.planning/phases/14-live-full-adopt-verify/14-PRE-ADOPT-BASELINE.txt` — the recorded `configProvider_pre=hyprlang` value
    - `.planning/phases/15-playbook-safe-vs-full/15-PATTERNS.md` — the "Heading + numbering pattern" section for the anchor form
  </read_first>
  <files>docs/dots-hyprland-workflow.md</files>
  <action>
Replace the safe-era session description with the full model, and lift verification out into its own section.

**Section 5 body (D-08).** Delete the `### Personal hypr hooks (two lines)` subsection — heading, fenced block and the sentence beneath it — and write the full session model in its place as running prose plus at most one fenced `text` block of paths. State: `~/.config/hypr/hyprland.lua` is the session entry after a full install; `configProvider` reports `lua`, where the recorded pre-adopt value on this machine was `hyprlang`; upstream renamed the previous `~/.config/hypr/hyprland.conf` to `hyprland.conf.old` rather than deleting it, and section 9 explains why that matters beyond archival; personal overlays live under `~/.config/hypr/custom/`, with the policy in section 6.

Then one pointer line — a single sentence, not a subsection (D-08) — for the safe profile: under safe, the session model is instead two hook lines the wrapper injects into your own `hyprland.conf`, an `env` line setting `ILLOGICAL_IMPULSE_VIRTUAL_ENV` and an `exec-once` line starting `qs -c ii`, which the wrapper enables on a successful install and deletes on `uninstall`. Name the two line kinds; do not reproduce them as a fenced block, and do not give them a heading.

Add one sentence stating that under the full profile `Waybar`, `rofi` and `swaync` are not part of the session — `qs -c ii` replaces them — and that this is the accepted Phase 11 D-11 outcome, with the losses listed in section 8. Write the three names literally; never as a collective noun (D-09). Do not describe dual-run as anything other than a property of the safe profile, and do not describe a restore path for the archived trees.

Keep `### Live product path` and `### Mid-session reload` as they are; both are still true under either profile.

**Promote verification to section 7 (D-15).** Move the `### Verify the session after login` subsection out of section 5 and make it a top-level section headed `## 7. Verify after login`, placed after section 5. Move it whole — the fence, the after-login sentence above it, the `scripts/phase14-verify.sh` SoT sentence and the runbook pointer below it all travel together, unchanged in content. Change only the heading level and the heading text. Section 6 does not exist yet; Task 3 inserts it between 5 and 7, which is why the numbering skips here and lands contiguous at the end of the task.

Do not touch the `## Outline` in this task — plan `15-05` writes the final Outline once every section exists, and an Outline entry for a section that does not exist yet would break in-page link integrity at this commit (D-23).
  </action>
  <verify>
    <automated>grep -qF '## 7. Verify after login' docs/dots-hyprland-workflow.md && grep -qF '## 5. Session model after a full install' docs/dots-hyprland-workflow.md && ! grep -qF '### Personal hypr hooks (two lines)' docs/dots-hyprland-workflow.md && ! grep -qF '### Verify the session after login' docs/dots-hyprland-workflow.md && grep -q 'hyprland.lua' docs/dots-hyprland-workflow.md && grep -q 'hyprland.conf.old' docs/dots-hyprland-workflow.md && grep -q 'hyprlang' docs/dots-hyprland-workflow.md && grep -q 'ILLOGICAL_IMPULSE_VIRTUAL_ENV' docs/dots-hyprland-workflow.md</automated>
    <fails_when>non-zero exit — section 7 was not created or section 5 lost its heading, the safe-profile hook subsection survived as a subsection instead of being demoted to a pointer line, the verification block was copied rather than promoted (both the `###` and `##` forms present), or any of the four session facts is unstated: the Lua entry, the renamed `.old` conf, the recorded pre-adopt `hyprlang` value, the safe-profile env var</fails_when>
    <automated>test "$(grep -cE '^## [0-9]+\. ' docs/dots-hyprland-workflow.md)" -eq 8 && test "$(grep -c -- '-j status' docs/dots-hyprland-workflow.md)" -ge 1 && test -z "$(grep -l 'getoption configProvider' docs/dots-hyprland-workflow.md 2>/dev/null)" && grep -q 'phase14-verify.sh' docs/dots-hyprland-workflow.md && test "$(grep -cE '\brofi\b' docs/dots-hyprland-workflow.md)" -ge 1 && grep -q 'swaync' docs/dots-hyprland-workflow.md && grep -q 'Waybar' docs/dots-hyprland-workflow.md</automated>
    <fails_when>the numbered-heading count is not 8 (sections 1-5, 7, 10, 11), the working compositor probe was lost in the move, `grep -l` prints the filename because the dead `getoption` spelling reappeared, the `scripts/phase14-verify.sh` SoT citation was dropped in the promotion, or the trio is not named literally — the `\brofi\b` boundary is required because this file is full of the word "profile"</fails_when>
    <automated>git diff --quiet HEAD -- arch/ scripts/ .config/ stow/ vendor/ .planning/STATE.md .planning/ROADMAP.md && ok=1; while read -r a; do h="${a#\#}"; grep -oE '^#{2,3} .*' docs/dots-hyprland-workflow.md | sed 's/^#* //' | tr 'A-Z' 'a-z' | sed 's/[^a-z0-9 -]//g; s/ /-/g' | grep -qx "$h" || { echo "MISSING ANCHOR: $h"; ok=0; }; done < <(grep -oE '\(#[a-z0-9-]+\)' docs/dots-hyprland-workflow.md | tr -d '()' | sort -u); test "$ok" -eq 1</automated>
    <fails_when>`git diff --quiet` reports an uncommitted change under a fenced directory or in `STATE.md` / `ROADMAP.md`, or a `MISSING ANCHOR:` line is printed — retitling section 5 or promoting the verify subsection without checking the Outline is exactly how an in-page link goes dead</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/dots-hyprland-workflow.md` contains the heading `## 7. Verify after login` and no longer contains `### Verify the session after login`.
    - `### Personal hypr hooks (two lines)` is gone; the safe conf-hook method appears as one sentence naming an `env` line for `ILLOGICAL_IMPULSE_VIRTUAL_ENV` and an `exec-once` line for `qs -c ii`, with no heading of its own.
    - Section 5 states all four session facts: `hyprland.lua` entry, `configProvider` reports `lua`, the recorded pre-adopt value was `hyprlang`, previous conf renamed `hyprland.conf.old`.
    - Section 5 points at section 6 for overlay policy and section 9 for the renamed conf's other roles.
    - `### Live product path` and `### Mid-session reload` survive unchanged.
    - The file contains exactly 8 `## N.` headings.
    - The working `-j status` probe survives the promotion, the dead `getoption` spelling is absent, and `scripts/phase14-verify.sh` is still cited.
    - `Waybar`, `swaync` and a word-boundary `rofi` each appear; non-allowlisted `chrome` tokens number 0.
    - Every in-page anchor referenced from the Outline still resolves.
  </acceptance_criteria>
  <done>The playbook describes the session a full install actually produces, the safe method is a pointer line rather than a section, and verification stands on its own as section 7.</done>
</task>

<task type="auto">
  <name>Task 3: Write section 6 — the overlay policy, in the playbook's own words (DOC-04)</name>
  <read_first>
    - `docs/dots-hyprland-workflow.md` — the boundary between `## 5. Session model after a full install` and `## 7. Verify after login`, where this section is inserted
    - `.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md` — read in full: the policy statement at lines 7-13 and 25, the apply command at lines 31-53, the D-19 fence at line 82. This is the SoT this section narrates
    - `.config/hypr/custom/general.lua`, `.config/hypr/custom/env.lua`, `.config/hypr/custom/execs.lua` — the three repo-side files the section describes. READ ONLY
    - `scripts/phase13-d19-assert.sh` — the in-repo verify this section cites rather than restates. READ ONLY
    - `.planning/phases/15-playbook-safe-vs-full/15-RESEARCH.md` — "Overlay SoT — Verified Ground Truth" (lines 596-640), including the live-versus-repo asymmetry
    - `.planning/phases/13-personal-hypr-custom-overlays/13-CONTEXT.md` — D-01 through D-05, the origin of the policy
  </read_first>
  <files>docs/dots-hyprland-workflow.md</files>
  <action>
Insert a new top-level section headed `## 6. Personal overlays: repo, live, fork` between sections 5 and 7. Write DOC-04 in the playbook's own words, narrating `13-SOT-APPLY.md` rather than duplicating it, and name it as the SoT being summarised (D-16, Phase 13 D-05).

**Three locations, three roles.** Repo `.config/hypr/custom/` is the authoring source of truth — the place you edit, and the version that survives a reinstall on the next machine. Live `~/.config/hypr/custom/` is an applied copy, not an editing surface; persist a live tweak by copying it back into the repo. `vendor/dots-hyprland` and the personal fork are product source of truth only; machine overlays are never committed into either.

**Direction.** Apply is one-way, repo to live, after the full files install. There is no sync daemon and no reverse sync.

**Named files only.** The apply copies `general.lua`, `env.lua` and `execs.lua` by name with `cp -a`, overwriting ii seeds where present. It never uses a mirroring delete, and it never touches `keybinds.lua`, `rules.lua` or `variables.lua`. Explain the asymmetry a reader will otherwise find confusing: live `custom/` legitimately holds more files than the repo does, because upstream seeds `keybinds.lua`, `rules.lua`, `variables.lua` and a `scripts/` directory there, and the apply deliberately leaves them alone. Show the apply as a fenced `bash` block in the named-file `cp -a` shape that `13-SOT-APPLY.md` records at its lines 44-48, and cite that SoT alongside it. There is no cite-instead-of-show option: D-16 makes the apply command part of what the playbook documents, and this task's own verify requires `cp -a` to be present in the file. Do not invent a variant, and do not show a mirroring-delete form even as a counter-example.

**Failure mode.** The apply aborts if repo `general.lua` is missing, because the layout is required; `env.lua` and `execs.lua` are optional slots and their absence is a warning, not an abort.

**Citations.** Name `.planning/phases/13-personal-hypr-custom-overlays/13-SOT-APPLY.md` as the source of truth for the full policy and for the in-repo verify, and name `scripts/phase13-d19-assert.sh` as the executable that runs that verify. Both as bare inline backticked paths in the body, per the repo's body-citation form; `15-05` adds the linked `See also` entries.

Add no fenced block that mutates anything outside `~/.config/hypr/custom/`, and do not restate the fork-boundary check's implementation — cite it.
  </action>
  <verify>
    <automated>grep -qF '## 6. Personal overlays: repo, live, fork' docs/dots-hyprland-workflow.md && grep -q '13-SOT-APPLY.md' docs/dots-hyprland-workflow.md && grep -q 'phase13-d19-assert.sh' docs/dots-hyprland-workflow.md && grep -q '.config/hypr/custom' docs/dots-hyprland-workflow.md && grep -q 'vendor/dots-hyprland' docs/dots-hyprland-workflow.md && grep -q 'cp -a' docs/dots-hyprland-workflow.md && grep -qiE 'one-way|repo *(→|->|to) *live' docs/dots-hyprland-workflow.md && grep -q 'general.lua' docs/dots-hyprland-workflow.md && grep -q 'env.lua' docs/dots-hyprland-workflow.md && grep -q 'execs.lua' docs/dots-hyprland-workflow.md</automated>
    <fails_when>non-zero exit — the section heading is absent, either cited SoT is unnamed, the overlay directory or the fork boundary is unmentioned, the named-file `cp -a` apply is absent, no phrasing states the one-way repo-to-live direction, or any of the three named overlay files is unmentioned</fails_when>
    <automated>test "$(grep -c 'rsync' docs/dots-hyprland-workflow.md)" -eq 0 && test "$(grep -cE '^## [0-9]+\. ' docs/dots-hyprland-workflow.md)" -eq 9 && test "$(grep -c 'keybinds.lua' docs/dots-hyprland-workflow.md)" -ge 1 && test "$(grep -c 'variables.lua' docs/dots-hyprland-workflow.md)" -ge 1</automated>
    <fails_when>the `rsync` count is above 0 — a mirroring copy, even shown as a counter-example, is the one apply shape `13-SOT-APPLY.md` forbids and must not appear in an operator doc; or the numbered-heading count is not 9 (sections 1-7, 10, 11); or the never-copied ii seeds are unnamed, meaning the live-versus-repo asymmetry was left unexplained</fails_when>
    <automated>ok=1; while read -r a; do h="${a#\#}"; grep -oE '^#{2,3} .*' docs/dots-hyprland-workflow.md | sed 's/^#* //' | tr 'A-Z' 'a-z' | sed 's/[^a-z0-9 -]//g; s/ /-/g' | grep -qx "$h" || { echo "MISSING ANCHOR: $h"; ok=0; }; done < <(grep -oE '\(#[a-z0-9-]+\)' docs/dots-hyprland-workflow.md | tr -d '()' | sort -u); test "$ok" -eq 1 && git diff --quiet HEAD -- arch/ scripts/ .config/ stow/ vendor/ .planning/STATE.md .planning/ROADMAP.md && ./scripts/phase13-d19-assert.sh >/tmp/p15-04-d19.txt 2>&1 && test "$(grep -c '^\[FAIL\]' /tmp/p15-04-d19.txt)" -eq 0</automated>
    <fails_when>a `MISSING ANCHOR:` line is printed, `git diff --quiet` reports an uncommitted change under a fenced directory (writing this section is exactly when an executor is tempted to "just fix" `.config/hypr/custom/`), or `scripts/phase13-d19-assert.sh` prints a `[FAIL]` line against its 15-`[PASS]` / 0-`[FAIL]` baseline</fails_when>
  </verify>
  <acceptance_criteria>
    - `docs/dots-hyprland-workflow.md` contains `## 6. Personal overlays: repo, live, fork`, positioned between sections 5 and 7.
    - The section states all three roles: repo `.config/hypr/custom/` as authoring SoT, live as applied copy, `vendor/dots-hyprland` and the fork as product SoT that never receives machine overlays.
    - The one-way repo-to-live direction is stated explicitly.
    - `general.lua`, `env.lua` and `execs.lua` are named, the `cp -a` apply shape is present, and no `rsync` occurrence exists anywhere in the file.
    - `keybinds.lua` and `variables.lua` are named as ii seeds the apply never touches, explaining why live holds more files than the repo.
    - The abort-if-`general.lua`-missing behavior and the warn-if-slot-missing behavior are both stated.
    - `13-SOT-APPLY.md` and `scripts/phase13-d19-assert.sh` are both cited as bare inline backticked paths.
    - The file contains exactly 9 `## N.` headings; every Outline anchor resolves.
    - `./scripts/phase13-d19-assert.sh` exits 0 with zero `[FAIL]` lines, and no file under `.config/` was modified.
  </acceptance_criteria>
  <done>DOC-04 is delivered: a reader knows where overlays are authored, which direction they flow, which files move, which never do, and which document is the source of truth they can go read.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published prose → operator's shell | Every fenced command in sections 4, 6 and 7 is pasted verbatim into a root-capable shell during an install or an overlay apply. |
| playbook narrative → `13-SOT-APPLY.md` | The overlay section paraphrases a policy that has an executable verifier; drift is silent unless the citation is asserted present. |
| playbook narrative → `arch/dots-hyprland.sh` | The backup-gate paragraph paraphrases the wrapper's refusal logic; if it softens the dual-key rule the operator can lose their only backup. |
| documentation task → `.config/hypr/custom/` | Writing about the overlays is the moment an executor is most tempted to also *fix* them. The scope fence forbids it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-16 | Denial of Service | backup-gate paragraph in section 4 | high | mitigate | Document the dual-key refusal explicitly and show no bare `--skip-backup` example; verify asserts both flag strings present and no dangerous-override token anywhere |
| T-15-17 | Tampering | backup confirmed by directory name rather than content | high | mitigate | The section instructs `sha256sum` comparison and states that upstream may skip a fresh backup when a directory of that name already exists; verify asserts `sha256sum` present and the rotated form absent |
| T-15-18 | Elevation of Privilege | upstream removal subcommand or dangerous-override token presented as an option | high | mitigate | Verify asserts the count of `UPSTREAM-UNINSTALL` / `upstream-dangerous` tokens is 0, and every subcommand on a fenced command line is checked against the live `help` allowlist |
| T-15-19 | Tampering | overlay apply documented as a mirroring copy | high | mitigate | `13-SOT-APPLY.md` forbids mirroring delete; verify asserts the file contains zero `rsync` occurrences, counter-examples included |
| T-15-20 | Tampering | `.config/hypr/custom/` edited while documenting it | high | mitigate | Scope-fence guard on every task plus `scripts/phase13-d19-assert.sh` green, which asserts repo-side overlay layout directly |
| T-15-21 | Spoofing | full walkthrough read as a default change | medium | mitigate | One sentence in section 4 states dropping `--full` gives the safe profile, pointing back to the profiles section rather than forking the walkthrough (D-07, D-03) |
| T-15-SC | Tampering | package-manager installs | low | accept | No `npm` / `pip` / `cargo` / `pacman` install task exists in this phase (`15-RESEARCH.md` § Package Legitimacy Audit: not applicable) |
</threat_model>

## Artifacts this phase produces

Created by **this plan**, all in `docs/dots-hyprland-workflow.md`:

| Artifact | Kind | Anchor |
|----------|------|--------|
| `## 6. Personal overlays: repo, live, fork` | new heading | `#6-personal-overlays-repo-live-fork` |
| `## 7. Verify after login` | new heading, promoted from a `###` | `#7-verify-after-login` |

Removed: `### Personal hypr hooks (two lines)` and `### Verify the session after login` (the latter promoted, not deleted). Rewritten in place: the body of `## 4. Install via the thin wrapper` and the body of `## 5. Session model after a full install`.

## Flagged Assumptions

| # | Item | Status | Why it is flagged |
|---|------|--------|-------------------|
| C-1 | `DOC-03` edge coverage | **unresolved** (`unclassified`) | The edge probe could not classify this requirement; no acceptance criterion was auto-derived. Coverage comes from the goal-backward truths above. Not auto-resolved with a backstop, not dropped. |
| C-2 | `DOC-04` edge coverage | **unresolved** (`unclassified`) | Same. DOC-04's criteria in Task 3 are authored from `13-SOT-APPLY.md` directly, not from the probe. |
| A-04-1 | Section numbering skips 8 and 9 at the end of this plan | assumption, deliberate | `15-05` adds them, landing the numbering contiguous. The Outline is deliberately untouched here so no entry ever points at a heading that does not exist yet (D-23). |
| A-04-2 | Showing the `cp -a` apply in the playbook is narration, not duplication of `13-SOT-APPLY.md` | assumption, enforced | D-16 says the apply command is part of what the playbook documents, and Phase 13 D-05 keeps the SoT authoritative. The earlier draft offered citing instead of showing as a conservative fallback; that clause is removed, because this task's verify hard-requires `grep -q 'cp -a'` and its acceptance criteria list the shape as mandatory — an executor taking the fallback would have failed its own gate. The SoT is cited alongside the shown block, not instead of it. |
| A6 | "Screen share **may** be affected" is the accurate D-38 wording | carried forward | The portal's `AvailableSourceTypes` is unchanged; only the session-bootstrap target is inactive. Section 8 in plan `15-05` owns the wording; recorded here because the prohibition list travels with every plan. |

<verification>
- The subcommand-allowlist check reads only line-leading wrapper invocations. That distinction matters: `exp-merge` is named in the non-goals prose as a *refused* path and must not trip a check meant to catch invented subcommands.
- The flag-ceiling check is likewise scoped to wrapper command lines, because Outline anchor slugs contain doubled hyphens that a naive `--[a-z-]+` scan reads as flags.
- `./scripts/phase14-verify.sh` is not run in these task verifies: it asserts a clean working tree (D-35) and would fail pre-commit. It runs at the phase gate in `15-06`.
</verification>

<success_criteria>
- A reader walks the full profile end to end and meets the backup gate inline, with the refusal rule stated.
- A reader knows what the full session model is and that the safe conf-hook method is one sentence away.
- DOC-04 is delivered in the playbook's own words with `13-SOT-APPLY.md` named as the SoT.
- No mirroring-delete apply, no dangerous-override token, no bare `--skip-backup` example appears anywhere in the file.
</success_criteria>

<output>
Create `.planning/phases/15-playbook-safe-vs-full/15-04-SUMMARY.md` when done.
</output>