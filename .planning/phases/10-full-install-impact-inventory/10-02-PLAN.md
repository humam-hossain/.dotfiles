---
phase: 10-full-install-impact-inventory
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md
autonomous: true
requirements:
  - INV-02
user_setup: []
# Single-writer rule: only this plan mutates 10-INVENTORY.md in wave 2.
# Plans 03/04 chain after 02 so execute-phase cannot race the same SoT.

must_haves:
  truths:
    - "Hypr axis section lists hyprland.conf → .old, hypr/hyprland rsync --delete sync, hyprland.lua install_file, hyprlock/hypridle auto_backup both firstrun branches, hypr/custom ignore_existing, hyprpaper not-touched-by-legacy"
    - "Every hypr effect row has Path | Effect | Risk | Source | Host present? with concrete Source cite (3.files-legacy.sh / 3.files.sh file:line or symbol)"
    - "Personal hyprland.conf has category annotations (monitors, workspaces, binds, exec-once, env, rules) without disposition verbs"
    - "Host not-firstrun (installed_true) is stated so lock/idle expect *.new not replace on this machine"
    - "No Waybar/rofi/swaync rows; no disposition recommendations"
  artifacts:
    - path: ".planning/phases/10-full-install-impact-inventory/10-INVENTORY.md"
      provides: "Complete Axis A hypr impact tables (INV-02)"
  key_links:
    - from: "10-INVENTORY.md Axis A"
      to: "vendor/dots-hyprland/sdata/subcmd-install/3.files-legacy.sh"
      via: "Source column file:line cites"
      pattern: "hyprland.conf.old"
    - from: "10-INVENTORY.md Axis A"
      to: "vendor/dots-hyprland/sdata/subcmd-install/3.files.sh"
      via: "auto_backup / ignore_existing / rsync_dir__sync helpers"
      pattern: "install_file__auto_backup"
  prohibitions:
    - statement: "Do not invent hyprpaper install behavior under legacy hypr block"
      status: active
      verification: "hyprpaper row Effect states not touched by legacy"
    - statement: "Do not omit firstrun vs non-firstrun auto_backup branch"
      status: active
      verification: "lock/idle rows mention both branches or *.new for this host"
    - statement: "Do not run live full install to observe hypr effects"
      status: active
      verification: "tasks read-only"
    - statement: "Do not write keep/migrate/accept dispositions for personal hypr"
      status: active
      verification: "D-13 category tags only; D-12 lint"
---

<objective>
Expand `10-INVENTORY.md` Axis A (drop `--skip-hyprland`) with full INV-02 personal hypr vs upstream install behavior tables, citing legacy files install sources and live host presence.

Purpose: Operator sees exact hypr blast radius of removing `--skip-hyprland` without dispositions.

Output: Complete hypr axis section in `10-INVENTORY.md`; assert remains green.
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-full-install-impact-inventory/10-CONTEXT.md
@.planning/phases/10-full-install-impact-inventory/10-RESEARCH.md
@.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md
@.planning/REQUIREMENTS.md
@vendor/dots-hyprland/sdata/subcmd-install/3.files-legacy.sh
@vendor/dots-hyprland/sdata/subcmd-install/3.files.sh
@vendor/dots-hyprland/sdata/subcmd-install/options.sh
@scripts/phase10-inventory-assert.sh
</context>

## Artifacts this phase produces

| Artifact | Path | Notes |
|----------|------|-------|
| Hypr axis rows | `10-INVENTORY.md` | INV-02 minimum set + extra live hypr files |

## Assumptions (specless)

- Default files path is **legacy** (`3.files-legacy.sh`), not `--exp-files`
- Hyprland loads `hyprland.lua` after conf rename (ASSUMED — still inventory both)
- Live host scan may differ from research date — re-check Host present?

<tasks>

<task type="auto" id="10-02-01">
  <name>Task 1: Complete Axis A hypr effect tables (INV-02)</name>
  <files>.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md</files>
  <read_first>
    - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md (current scaffold)
    - .planning/phases/10-full-install-impact-inventory/10-RESEARCH.md (Focus 2 table — primary seed; re-verify line numbers)
    - .planning/phases/10-full-install-impact-inventory/10-CONTEXT.md (D-09, D-10, D-13, D-14, D-15)
    - vendor/dots-hyprland/sdata/subcmd-install/3.files-legacy.sh (full hypr case)
    - vendor/dots-hyprland/sdata/subcmd-install/3.files.sh (install_file__auto_backup, rsync_dir__sync, install_dir__ignore_existing, firstrun helpers)
    - Live host: `~/.config/hypr/` listing and `~/.config/illogical-impulse/installed_true` (read-only)
  </read_first>
  <action>
    Replace Axis A stubs in `10-INVENTORY.md` with complete neutral tables:

    **Minimum INV-02 / D-14 rows (Path under ~/.config/hypr unless noted):**
    1. `hypr/hyprland/` — rsync sync with `--delete` from dots; Risk HIGH; Source `3.files-legacy.sh` + `rsync_dir__sync` in `3.files.sh`
    2. `hypr/hyprland.conf` — `mv` to `hyprland.conf.old`; Risk HIGH; Source legacy lines for conf rename
    3. `hypr/hyprland.lua` — `install_file` unless skip entry; ABSENT→would create; Risk HIGH
    4. `hypr/hyprlock.conf` — `install_file__auto_backup`; document firstrun replace vs non-firstrun `*.new`; this host has `installed_true` → expect `.new`
    5. `hypr/hypridle.conf` — same auto_backup behavior
    6. `hypr/custom/` — `install_dir__ignore_existing`: seed if absent, no-op if exists; host ABSENT → would seed
    7. `hypr/hyprpaper.conf` — **not** in legacy hypr install list; Effect = not touched by legacy hypr block; still list as personal surface (INV-02)
    8. Any other live files under `~/.config/hypr` found by read-only `find` (e.g. `.bak`, gui conf) — Effect = not in install list unless proven otherwise; Risk LOW

    **Also document:**
    - UNKNOWN/gap row: upstream `hyprlock/` directory not installed by legacy (only conf) — cite RESEARCH open Q1
    - Optional LOW: Google Sans Flex font side effect under files step (`3.files.sh` font install) — may live outside hypr path
    - Category annotation subsection for personal `hyprland.conf`: monitors, workspaces, binds, exec-once, env, rules — tags/counts only, **no** keep/migrate/accept

    **Independence:** Section is Axis A only — do not imply dropping `--core` or sysupdate together (D-09).

    Re-run host presence with `test -e` for each Path; update Host present? column. Do not mutate XDG. Do not add chrome rows. Keep residual section intact.
  </action>
  <verify>
    <automated>./scripts/phase10-inventory-assert.sh</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/phase10-inventory-assert.sh` exits 0
    - Axis A contains `hyprland.conf.old` or explicit conf→`.old` wording
    - Axis A contains `hyprland.lua`, `hyprlock`, `hypridle`, `custom`, `hyprpaper`
    - Axis A mentions auto_backup or firstrun/`.new` behavior
    - Source column cites `3.files-legacy.sh` or `3.files.sh` for hypr install rows
    - No waybar/rofi/swaync strings
    - Category annotations present without disposition language
  </acceptance_criteria>
  <done>INV-02 hypr axis complete with cited effects and host presence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static setup source → inventory | Trusted in-repo pin; cite only |
| Live hypr conf → inventory | Category tags only; avoid secret paste |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | live install temptation | high | mitigate | Read-only host; static cites only |
| T-10-03 | Information Disclosure | hyprland.conf | low | mitigate | Categories not full conf dump |
| T-10-02 | Tampering | disposition creep | medium | mitigate | D-13 tags only; assert D-12 |
</threat_model>

<verification>
`./scripts/phase10-inventory-assert.sh` green; hypr axis satisfies INV-02 keyword and Source density checks.
</verification>

<success_criteria>
Operator can read Axis A and know exactly what removing `--skip-hyprland` does to personal hypr surfaces on this host.
</success_criteria>

<output>
Create `.planning/phases/10-full-install-impact-inventory/10-02-SUMMARY.md` when done
</output>
