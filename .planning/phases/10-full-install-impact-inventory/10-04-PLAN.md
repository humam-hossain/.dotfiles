---
phase: 10-full-install-impact-inventory
plan: 04
type: execute
wave: 4
depends_on:
  - "10-01"
  - "10-03"
files_modified:
  - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md
autonomous: true
requirements:
  - INV-01
user_setup: []
# Single-writer chain: runs after 10-03 finishes Axis B (same 10-INVENTORY.md SoT).

must_haves:
  truths:
    - "Axis C documents pacman -Syu when SKIP_SYSUPDATE unset with HIGH risk"
    - "Axis C lists coarse illogical-impulse meta PKGBUILD set from install-deps.sh"
    - "Axis C documents remove_deprecated_dependencies and implicitize_old_dependencies (asdeps) side effects"
    - "Axis C separates files-only vs full install/deps pipeline (install-files alone does not Syu)"
    - "Wrapper post-install protect re-mark noted as mitigation for asdeps where applicable"
    - "Every package/sysupdate row has Path|Effect|Risk|Source|Host present? (Host may be package query or N/A)"
  artifacts:
    - path: ".planning/phases/10-full-install-impact-inventory/10-INVENTORY.md"
      provides: "Complete Axis C packages/sysupdate impact tables (INV-01)"
  key_links:
    - from: "10-INVENTORY.md Axis C"
      to: "vendor/dots-hyprland/sdata/dist-arch/install-deps.sh"
      via: "Syu, metas, asdeps, plasmaintg"
      pattern: "pacman -Syu"
    - from: "10-INVENTORY.md Axis C"
      to: "arch/dots-hyprland.sh"
      via: "post protect / SAFE_DEFAULTS interaction notes"
      pattern: "protect"
  prohibitions:
    - statement: "Do not run pacman -Syu, yay -S, or makepkg installs in this phase"
      status: active
      verification: "tasks are documentation-only"
    - statement: "Do not expand every PKGBUILD depend unless needed — coarse metas OK (CONTEXT discretion)"
      status: active
      verification: "meta list from install-deps.sh sufficient"
    - statement: "Do not imply dropping sysupdate requires dropping --core/--skip-hyprland"
      status: active
      verification: "independent Axis C (D-09)"
---

<objective>
Expand `10-INVENTORY.md` Axis C (allow sysupdate / package effects) with coarse-grain package and sysupdate blast radius from `install-deps.sh` (INV-01 package side).

Purpose: Operator sees what removing `--skip-sysupdate` and running deps path does — without performing it.

Output: Complete packages/sysupdate axis; assert green.
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-full-install-impact-inventory/10-CONTEXT.md
@.planning/phases/10-full-install-impact-inventory/10-RESEARCH.md
@.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md
@vendor/dots-hyprland/sdata/dist-arch/install-deps.sh
@arch/dots-hyprland.sh
@scripts/phase10-inventory-assert.sh
</context>

## Artifacts this phase produces

| Artifact | Path | Notes |
|----------|------|-------|
| Packages/sysupdate axis | `10-INVENTORY.md` | INV-01 package/sysupdate effects |

## Assumptions (specless)

- Coarse meta listing satisfies INV-01 without full depends expansion (A3)
- Optional `pacman -Qq` presence for metas is read-only Host present?

<tasks>

<task type="auto" id="10-04-01">
  <name>Task 1: Complete Axis C package/sysupdate effects (INV-01)</name>
  <files>.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md</files>
  <read_first>
    - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md
    - .planning/phases/10-full-install-impact-inventory/10-RESEARCH.md (Focus 4)
    - .planning/phases/10-full-install-impact-inventory/10-CONTEXT.md (D-09, D-10, INV-01 discretion on grain)
    - vendor/dots-hyprland/sdata/dist-arch/install-deps.sh (Syu, deprecated, asdeps, meta loop, plasmaintg)
    - arch/dots-hyprland.sh (SAFE_DEFAULTS injection scope; post protect if present)
    - Optional read-only: `pacman -Qq | rg illogical-impulse` for Host present?
  </read_first>
  <action>
    Replace Axis C stubs with complete neutral tables:

    **Required effect rows:**
    1. `sudo pacman -Syu` when `SKIP_SYSUPDATE` unset — HIGH — cite install-deps.sh
    2. `remove_deprecated_dependencies` (`pacman -Rdd` style list) — MED — always on deps path
    3. `implicitize_old_dependencies` / `yay -D --asdeps` against previous_dependencies — HIGH for dual-run stack — cite script + note wrapper protect re-mark mitigates after wrapper install paths
    4. Meta PKGBUILD build/install loop (`makepkg -Afsi`, `yay -S --asdeps` depends, `--needed`) — list coarse meta names from script (audio, backlight, basic, fonts-themes, kde, portal, python, screencapture, toolkit, widgets, hyprland, microtex-git, quickshell-git, bibata-modern-classic-bin — verify against current pin)
    5. `plasma-browser-integration` optional when not SKIP_PLASMAINTG — MED
    6. Clarifying row: `install-files` alone does **not** run Syu; full `install` / `install-deps` does deps first

    **SAFE_DEFAULTS interaction note (not residual rewrite):** default wrapper injects `--skip-sysupdate` on install/install-files; dropping it is a future full-profile concern (Phase 12) — inventory maps the effect only.

    Use Path|Effect|Risk|Source|Host present? — Host for packages via `pacman -Qq` or "installed (dual-run)" / ABSENT.

    Do not run Syu or installs. Keep other sections intact. Independent axis (D-09).
  </action>
  <verify>
    <automated>./scripts/phase10-inventory-assert.sh</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/phase10-inventory-assert.sh` exits 0
    - Axis C mentions `pacman -Syu` or Syu
    - Axis C mentions `illogical-impulse` metas
    - Axis C mentions `asdeps` or implicitize/previous_dependencies
    - Axis C notes install-files vs full install/deps regarding Syu
    - Source cites `install-deps.sh`
    - No live package transaction commands executed as part of the task (documentation only)
  </acceptance_criteria>
  <done>INV-01 package/sysupdate axis complete with cited effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install-deps.sh → inventory | Static analysis only |
| pacman query → Host present? | Read-only `-Q` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering / DoS | accidental Syu | high | mitigate | Document only; ban pacman -Syu in tasks |
| T-10-02 | Tampering | asdeps demotion surprise | medium | mitigate | Document + wrapper protect note |
| T-10-SC | Supply chain | yay/makepkg | low | accept | No builds this phase |
</threat_model>

<verification>
Assert green; Axis C covers Syu, metas, asdeps.
</verification>

<success_criteria>
Operator understands package/sysupdate blast radius of allowing sysupdate / running deps without performing it.
</success_criteria>

<output>
Create `.planning/phases/10-full-install-impact-inventory/10-04-SUMMARY.md` when done
</output>
