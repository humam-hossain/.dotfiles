---
phase: 10-full-install-impact-inventory
plan: 01
type: tracer
wave: 1
depends_on: []
files_modified:
  - scripts/phase10-inventory-assert.sh
  - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md
  - .planning/phases/10-full-install-impact-inventory/10-VALIDATION.md
autonomous: true
requirements:
  - INV-04
user_setup: []

must_haves:
  truths:
    - "scripts/phase10-inventory-assert.sh exists, is executable, passes bash -n, and exits non-zero until 10-INVENTORY.md meets residual gates"
    - "D-01: 10-INVENTORY.md exists at phase-dir SoT path (not under docs/) with section skeleton for SAFE_DEFAULTS residual + three flag axes + host snapshot + UNKNOWN + Sources"
    - "D-02: One multi-section markdown document only (not split files per flag axis); sections cover residual, hypr, misc/--core, packages/sysupdate, host snapshot"
    - "D-05: Evidence is static setup/wrapper source + live host scan only; no live full install this phase; primary sources 3.files-legacy.sh, options.sh, arch/dots-hyprland.sh"
    - "D-11: SAFE_DEFAULTS residual (INV-04) is the dedicated top section before the three flag axes; states triple --core --skip-hyprland --skip-sysupdate, cites arch/dots-hyprland.sh, and states safe dual-run install remains default/available after this milestone"
    - "D-10: Effect tables use columns Path | Effect | Risk | Source | Host present?"
    - "Assert script forbids calling setup without --dry-run and never rsync/cp/mv/rm into XDG"
    - statement: "Host presence columns refreshed at inventory write time (not only research date)"
      verification: backstop
  artifacts:
    - path: "scripts/phase10-inventory-assert.sh"
      provides: "Wave 0 Nyquist structural/lint harness for 10-INVENTORY.md"
      contains: "INVENTORY path, FAIL counter, INV-04/02/03/01 rg-style gates, D-12/D-15 lint"
    - path: ".planning/phases/10-full-install-impact-inventory/10-INVENTORY.md"
      provides: "Committed neutral impact inventory (scaffold + INV-04 residual complete)"
  key_links:
    - from: "scripts/phase10-inventory-assert.sh"
      to: "10-INVENTORY.md"
      via: "structural rg/grep gates"
      pattern: "SAFE_DEFAULTS"
    - from: "10-INVENTORY.md residual section"
      to: "arch/dots-hyprland.sh"
      via: "Source cites for SAFE_DEFAULTS injection"
      pattern: "SAFE_DEFAULTS=(--core --skip-hyprland --skip-sysupdate)"
  prohibitions:
    - statement: "Do not run live full install or drop SAFE_DEFAULTS for discovery"
      status: active
      verification: "Plans and assert script contain no non-dry-run setup install path"
    - statement: "Do not rsync/cp/mv/rm into ~/.config or ii backup dirs"
      status: active
      verification: "assert script and tasks use read-only host commands only"
    - statement: "Do not write disposition recommendations (keep/migrate/accept) as operator guidance"
      status: active
      verification: "D-12 lint gate in assert"
    - statement: "Do not inventory Waybar/rofi/swaync clash rows"
      status: active
      verification: "D-15 lint gate in assert"
    - statement: "Do not create inventory under docs/ (phase-dir SoT only)"
      status: active
      verification: "D-01 path only"
---

<objective>
Tracer slice for Phase 10: land Wave 0 assert harness + create `10-INVENTORY.md` with full section skeleton and a **complete SAFE_DEFAULTS residual (INV-04)** so the end-to-end deliverable path is proven before axis expansion.

Purpose: Prove the inventory artifact path, column contract, residual claim, and automated gates without mutating the host.

Output: `scripts/phase10-inventory-assert.sh`, scaffolded `10-INVENTORY.md` with INV-04 filled, VALIDATION.md Wave 0 checkboxes updated.
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-full-install-impact-inventory/10-CONTEXT.md
@.planning/phases/10-full-install-impact-inventory/10-RESEARCH.md
@.planning/phases/10-full-install-impact-inventory/10-PATTERNS.md
@.planning/phases/10-full-install-impact-inventory/10-VALIDATION.md
@arch/dots-hyprland.sh
@scripts/phase07-live-smoke.sh
@docs/dots-hyprland-workflow.md
</context>

## Phase Goal

Operator can see exactly what a full install would change before any disposition or mutation. This plan delivers the residual claim (safe path still default) plus the harness that gates later axis fills.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Inventory assert harness | `scripts/phase10-inventory-assert.sh` | **10-01** |
| Impact inventory | `10-INVENTORY.md` | **10-01** scaffold+INV-04; filled by 10-02..05 |
| Validation contract | `10-VALIDATION.md` | seeded plan-phase; Wave 0 boxes in **10-01** |

## Decisions locked in this plan

- D-01: phase-dir SoT path for `10-INVENTORY.md`
- D-02: one multi-section markdown document (not split per axis)
- D-05: static setup/wrapper + live host scan evidence only; no live full install
- D-08: dry-run optional for residual proof
- D-10: column headers Path|Effect|Risk|Source|Host present? on every effect table (including stubs)
- D-11: SAFE_DEFAULTS residual top section before the three axes

## Assumptions (specless probe)

- **EDGE_ABSENT / PROHIB_ABSENT:** no SPEC.md; edges derived from INV-01..04 + CONTEXT
- **INV-04 edge:** residual must remain true after later plans — later plans must not delete residual section
- **Host scan freshness:** research host facts are seed data; executor re-checks presence when filling Host columns (backstop)

<tasks>

<task type="auto" id="10-01-01">
  <name>Task 1: Create phase10 inventory assert harness</name>
  <files>scripts/phase10-inventory-assert.sh</files>
  <read_first>
    - scripts/phase07-live-smoke.sh (PASS/FAIL pattern, REPO_ROOT, exit FAIL count)
    - .planning/phases/10-full-install-impact-inventory/10-RESEARCH.md (Validation Architecture requirement→test map)
    - .planning/phases/10-full-install-impact-inventory/10-VALIDATION.md (Wave 0 requirements)
    - .planning/phases/10-full-install-impact-inventory/10-PATTERNS.md (assert analog notes)
    - .planning/phases/10-full-install-impact-inventory/10-CONTEXT.md (D-01, D-10, D-12, D-15)
  </read_first>
  <action>
    Create executable `scripts/phase10-inventory-assert.sh` (chmod +x) with `set -euo pipefail`, REPO_ROOT from BASH_SOURCE, labeled pass/fail helpers matching phase07 style.

    **INVENTORY path constant:** `$REPO_ROOT/.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md`

    **Default mode (no args):** structural gates only:
    1. File exists.
    2. Required section headings present (flexible wording OK but must match these intent keys): `SAFE_DEFAULTS` residual, drop `--skip-hyprland` or hypr axis, drop `--core` or misc axis, sysupdate/packages axis, Host snapshot, UNKNOWN (or research notes), Sources.
    3. At least one markdown table header line containing Path, Effect, Risk, Source, and Host present (case-insensitive Host present OK).
    4. INV-04: file contains `SAFE_DEFAULTS` and the three tokens `--core`, `--skip-hyprland`, `--skip-sysupdate`, plus language that safe/default dual-run install remains available (match `remains` or `still` near safe/default/SAFE_DEFAULTS).
    5. INV-02 signals (may fail until plan 02 — gate them behind progressive checks OR require placeholders that mention the keywords so tracer can pass with stubs): require strings `hyprland.conf`, `hyprland.lua`, `auto_backup` OR `.new`, `custom`, `hyprpaper` somewhere in file once Axis A exists. For Wave 0 tracer: implement checks as **soft until section filled** is wrong — instead use **required keywords only after residual**: residual-only run must pass residual gates; for hypr/misc/sysupdate use checks that fail loudly when those sections claim completeness. Simplest correct design: always check residual hard; for INV-02/03/01 check hard for keyword presence — Task 2 will plant stub rows containing those keywords under each axis heading so tracer stays green, and later plans expand rows.

    6. INV-03: require `fish`, `kitty`, `starship`, `fontconfig`, and at least two of `fuzzel|matugen|wlogout|mpv|dolphinrc`.
    7. INV-01: require `Syu` or `pacman -Syu`, `illogical-impulse`, and `asdeps` or `skip-sysupdate`.
    8. D-12 lint: fail if `rg -ni 'recommend (keep|migrate|accept)|disposition:'` matches (allowlines containing `Phase 11` only if needed — prefer zero matches).
    9. D-15 lint: fail if waybar|rofi|swaync appear (case-insensitive). Exception: none — omit chrome entirely.

    **`--full` mode:** also print read-only PRESENT/ABSENT for a fixed checklist (hypr INV-02 set + full misc basenames from RESEARCH + firstrun marker path). Use only `test -e` / `printf`. Do not write under XDG. Do not call `./setup` or `arch/dots-hyprland.sh` except optional dry-run is **not** required in assert.

    Script body must not contain rsync/cp/mv/rm targeting HOME config, and must not invoke setup without documenting dry-run-only if ever added.

    Success: print a single summary line like `phase10 inventory asserts OK` and exit 0 when all hard gates pass; otherwise exit non-zero with `[FAIL]` lines.
  </action>
  <verify>
    <automated>test -x scripts/phase10-inventory-assert.sh && bash -n scripts/phase10-inventory-assert.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/phase10-inventory-assert.sh` succeeds
    - `bash -n scripts/phase10-inventory-assert.sh` exits 0
    - Script references path `10-full-install-impact-inventory/10-INVENTORY.md`
    - Script contains D-15 checks for waybar, rofi, swaync
    - Script contains SAFE_DEFAULTS residual checks
    - Script does not call `rsync` into config paths
  </acceptance_criteria>
  <done>Executable Wave 0 assert script ready to gate inventory content.</done>
</task>

<task type="auto" id="10-01-02">
  <name>Task 2: Scaffold 10-INVENTORY.md + complete SAFE_DEFAULTS residual (INV-04)</name>
  <files>.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md, .planning/phases/10-full-install-impact-inventory/10-VALIDATION.md</files>
  <read_first>
    - .planning/phases/10-full-install-impact-inventory/10-CONTEXT.md (D-01..D-17)
    - .planning/phases/10-full-install-impact-inventory/10-RESEARCH.md (Focus 1 SAFE_DEFAULTS; heading skeleton; optional dry-run result)
    - arch/dots-hyprland.sh (SAFE_DEFAULTS at line ~12; injection ~127-131 and ~1399-1407; backup gate ~149-160)
    - docs/dots-hyprland-workflow.md (safe dual-run narrative — secondary)
    - scripts/phase10-inventory-assert.sh (gates this task must satisfy)
  </read_first>
  <action>
    Create `.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md` (D-01). Structure:

    1. Title + date + scope note: neutral impact inventory; no dispositions; no live install this phase; legacy files path (not `--exp-files`) is default SoT.
    2. `## SAFE_DEFAULTS residual (INV-04)` — **fully written**, not stub:
       - Quote/state `SAFE_DEFAULTS=(--core --skip-hyprland --skip-sysupdate)` with Source `arch/dots-hyprland.sh` (line cite).
       - State injection applies to `install` and `install-files` only (not install-deps/install-setups).
       - State Phase 10 does **not** change the wrapper; default `./arch/dots-hyprland.sh install` remains dual-run-safe.
       - State full-profile opt-in is future (Phase 12); residual remains after this milestone.
       - Optional: paste dry-run proof if cheap (`printf 'yes\n' | ./arch/dots-hyprland.sh install --dry-run`) showing SAFE_DEFAULTS in argv — optional per D-08.
       - Include a small residual table with Path|Effect|Risk|Source|Host present? at least for "wrapper default install path" row.
    3. Stub sections with headings and **minimal keyword-bearing placeholder tables** so assert INV-02/03/01 gates pass until expanded:
       - `## Axis: drop --skip-hyprland (hypr files)` — table rows naming hyprland.conf → `.old`, hyprland.lua, auto_backup / `.new`, hypr/custom ignore_existing, hyprpaper not-touched — Effect column may say `TODO expand in plan 10-02` but Source must still cite `3.files-legacy.sh` or RESEARCH — prefer short accurate one-liners from RESEARCH Focus 2 rather than vague TODO if possible (tracer quality).
       - `## Axis: drop --core (misc / fish / fontconfig)` — rows for fish, kitty, starship, fontconfig, fuzzel, matugen, wlogout, mpv at minimum.
       - `## Axis: allow sysupdate / package effects` — rows for pacman -Syu, illogical-impulse metas, asdeps demotion.
       - `## Host snapshot (live ~/.config, YYYY-MM-DD)` — dated stub noting re-scan required in plan 10-05.
       - `## UNKNOWN / research notes` — at least legacy hyprlock/ dir gap from RESEARCH open Q1.
       - `## Sources` — list primary setup/wrapper paths.

    4. Update `10-VALIDATION.md`: check Wave 0 harness box; set task 10-01 rows toward green where applicable; leave `nyquist_compliant` true only if assert script exists and residual inventory passes `./scripts/phase10-inventory-assert.sh`.

    Do not edit vendor tree. Do not mutate `~/.config`. Do not add Waybar/rofi/swaync rows. Do not recommend dispositions.
  </action>
  <verify>
    <automated>./scripts/phase10-inventory-assert.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md`
    - `./scripts/phase10-inventory-assert.sh` exits 0
    - Residual section contains SAFE_DEFAULTS and all three flags `--core`, `--skip-hyprland`, `--skip-sysupdate`
    - Residual states safe dual-run/default install remains available
    - File has no waybar/rofi/swaync strings
    - File has no `recommend keep|migrate|accept` disposition language
  </acceptance_criteria>
  <done>Tracer inventory exists with INV-04 complete and assert green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator / executor → live XDG | Read-only scan only; no install mutation |
| Inventory markdown → future Phase 11 operator | Neutral facts only; must not social-engineer destructive steps |
| Assert script → host | May read HOME config; must not write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | executor “research” | high | mitigate | Ban live setup install; dry-run optional only; assert is read-only |
| T-10-02 | Tampering | inventory content | medium | mitigate | Neutral effects only; D-12 lint; no rm/fix-it recipes |
| T-10-03 | Information Disclosure | host conf paste | low | mitigate | Category tags only for hyprland.conf; no secrets paste |
| T-10-SC | Tampering | supply chain | low | accept | No new packages this phase |
</threat_model>

<verification>
From REPO_ROOT: `bash -n scripts/phase10-inventory-assert.sh` and `./scripts/phase10-inventory-assert.sh` exit 0 with residual inventory present.
</verification>

<success_criteria>
Wave 0 harness exists; `10-INVENTORY.md` exists with complete INV-04 residual and section skeleton; assert exits 0; no host mutation.
</success_criteria>

<output>
Create `.planning/phases/10-full-install-impact-inventory/10-01-SUMMARY.md` when done
</output>
