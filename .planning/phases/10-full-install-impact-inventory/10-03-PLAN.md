---
phase: 10-full-install-impact-inventory
plan: 03
type: execute
wave: 3
depends_on:
  - "10-01"
  - "10-02"
files_modified:
  - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md
autonomous: true
requirements:
  - INV-03
user_setup: []
# Single-writer chain: runs after 10-02 finishes Axis A (same 10-INVENTORY.md SoT).

must_haves:
  truths:
    - "Axis B inventories full ii default misc catalog basenames from dots/.config find loop (not only fish/kitty/starship/fontconfig)"
    - "fish, fontconfig, and plasmaintg/--core package side effects included as part of dropping --core"
    - "Every misc row uses Path | Effect | Risk | Source | Host present? with install mode (file cp vs dir sync --delete vs exclude conf.d)"
    - "Named INV-03 set fish, kitty, starship, fontconfig all present as rows"
    - "No Waybar/rofi/swaync clash rows; quickshell noted as NOT skipped by --core if mentioned"
  artifacts:
    - path: ".planning/phases/10-full-install-impact-inventory/10-INVENTORY.md"
      provides: "Complete Axis B misc/--core impact tables (INV-03)"
  key_links:
    - from: "10-INVENTORY.md Axis B"
      to: "vendor/dots-hyprland/sdata/subcmd-install/3.files-legacy.sh"
      via: "misc/fish/fontconfig install branches"
      pattern: "SKIP_MISCCONF|SKIP_FISH|SKIP_FONTCONFIG"
    - from: "10-INVENTORY.md Axis B"
      to: "vendor/dots-hyprland/sdata/subcmd-install/options.sh"
      via: "--core expansion to SKIP_* set"
      pattern: "--core"
  prohibitions:
    - statement: "Do not limit misc list to the four named INV-03 examples"
      status: active
      verification: "full catalog basenames present (fuzzel/matugen/wlogout/etc.)"
    - statement: "Do not claim --core skips quickshell or hypr"
      status: active
      verification: "options.sh --core = plasmaintg+fish+fontconfig+misc only"
    - statement: "Do not include Waybar/rofi/swaync inventory rows"
      status: active
      verification: "D-15 assert"
    - statement: "Do not live-install misc to discover effects"
      status: active
      verification: "static + host read-only"
---

<objective>
Expand `10-INVENTORY.md` Axis B (drop `--core`) with the **full** ii default misc catalog plus fish/fontconfig/plasmaintg effects and host collision markers (INV-03 / D-16).

Purpose: Operator sees every non-hypr clash candidate a greenfield full install would place, with this host’s presence marked.

Output: Complete misc/`--core` axis in `10-INVENTORY.md`; assert green.
</objective>

<execution_context>
@$HOME/.agents/gsd-core/workflows/execute-plan.md
@$HOME/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-full-install-impact-inventory/10-CONTEXT.md
@.planning/phases/10-full-install-impact-inventory/10-RESEARCH.md
@.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md
@vendor/dots-hyprland/sdata/subcmd-install/3.files-legacy.sh
@vendor/dots-hyprland/sdata/subcmd-install/options.sh
@vendor/dots-hyprland/dots/.config
@scripts/phase10-inventory-assert.sh
</context>

## Artifacts this phase produces

| Artifact | Path | Notes |
|----------|------|-------|
| Misc/`--core` axis | `10-INVENTORY.md` | Full catalog + Host present? |

## Assumptions (specless)

- Misc catalog = `find dots/.config` excluding quickshell/fish/hypr/fontconfig + separate fish/fontconfig branches + konsole share path + plasmaintg package note
- D-17 greenfield vision informs completeness of catalog, not dispositions

<tasks>

<task type="auto" id="10-03-01">
  <name>Task 1: Complete Axis B full misc catalog (INV-03)</name>
  <files>.planning/phases/10-full-install-impact-inventory/10-INVENTORY.md</files>
  <read_first>
    - .planning/phases/10-full-install-impact-inventory/10-INVENTORY.md
    - .planning/phases/10-full-install-impact-inventory/10-RESEARCH.md (Focus 3 full catalog table)
    - .planning/phases/10-full-install-impact-inventory/10-CONTEXT.md (D-09, D-10, D-15, D-16, D-17)
    - vendor/dots-hyprland/sdata/subcmd-install/options.sh (`--core` expansion ~line 90)
    - vendor/dots-hyprland/sdata/subcmd-install/3.files-legacy.sh (misc/fish/fontconfig blocks)
    - vendor/dots-hyprland/dots/.config (re-run find for basenames on current pin)
    - Live host `~/.config` presence for each basename (read-only test -e)
  </read_first>
  <action>
    Replace Axis B stubs with complete tables.

    **Document what `--core` skips today** (so dropping re-enables): from `options.sh`, typically SKIP_PLASMAINTG + SKIP_FISH + SKIP_FONTCONFIG + SKIP_MISCCONF — cite exact expansion.

    **Full misc basenames** — re-run find on pin (research list as of 2026-08-04; verify still complete):
    chrome-flags.conf, code-flags.conf, darklyrc, dolphinrc, foot/, fuzzel/, kdeglobals, kde-material-you-colors/, kitty/, konsolerc, Kvantum/, matugen/, mpv/, starship.toml, thorium-flags.conf, wlogout/, xdg-desktop-portal/, zshrc.d/, plus `~/.local/share/konsole/` if legacy installs it.

    For each: Path, Effect (install_file vs install_dir sync --delete vs non-delete), Risk (HIGH/MED/LOW — personal PRESENT collisions higher), Source (legacy file:line), Host present? (re-scan).

    **Also rows:**
    - `~/.config/fish/` — sync exclude conf.d behavior
    - `~/.config/fontconfig/` — dir sync --delete
    - plasma-browser-integration package optional install note (size warning from install-deps if cited)

    **Clarify non-core:** quickshell is independent of `--core` (already dual-run). Hypr is Axis A. Do not invent chrome rows.

    Independent section only (D-09). Keep residual + Axis A intact.
  </action>
  <verify>
    <automated>./scripts/phase10-inventory-assert.sh</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/phase10-inventory-assert.sh` exits 0
    - Axis B lists fish, kitty, starship, fontconfig
    - Axis B lists at least fuzzel, matugen, wlogout, mpv (full-catalog signal)
    - Axis B states `--core` SKIP set from options.sh
    - Host present? filled for named collision candidates (at least PRESENT for host-present research set: kitty, starship, fish, fontconfig, mpv, dolphinrc, kdeglobals if still present)
    - No waybar/rofi/swaync
  </acceptance_criteria>
  <done>INV-03 full misc/--core axis complete with host collisions marked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vendor dots tree → catalog | Pin SoT for greenfield paths |
| Live XDG → Host present? | Read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | live misc install | high | mitigate | Static+scan only |
| T-10-02 | Tampering | incomplete catalog | medium | mitigate | D-16 full find loop required |
| T-10-SC | Elevation | plasmaintg note | low | mitigate | Document only; no pacman -S |
</threat_model>

<verification>
Assert green; misc catalog completeness vs find basenames.
</verification>

<success_criteria>
Operator sees full greenfield misc impact of dropping `--core` and which paths already exist on this host.
</success_criteria>

<output>
Create `.planning/phases/10-full-install-impact-inventory/10-03-SUMMARY.md` when done
</output>
