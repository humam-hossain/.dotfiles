---
phase: 08-plugin-runtime-hardening
plan: "03"
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - .planning/phases/06-runtime-failure-inventory/FAILURES.md
  - .planning/phases/06-runtime-failure-inventory/CHECKLIST.md
autonomous: false
requirements:
  - BUG-02
  - BUG-03
must_haves:
  truths:
    - "Maintainer has current evidence for search, explorer, git, LSP, UI, tmux-navigation, and external-open workflows after the Phase 8 fixes."
    - "FAILURES.md distinguishes fixed config bugs from remaining environment-only validator noise."
    - "CHECKLIST.md contains Phase 8 regression steps/results, including BUG-017 ownership evidence, that a future maintainer can rerun."
  artifacts:
    - path: ".planning/phases/06-runtime-failure-inventory/FAILURES.md"
      provides: "Updated BUG-001/016/017 dispositions and Phase 8 verification notes"
    - path: ".planning/phases/06-runtime-failure-inventory/CHECKLIST.md"
      provides: "Post-Phase-8 regression matrix for search, explorer, git, LSP, UI, tmux-navigation, and external open"
  key_links:
    - from: "scripts/nvim-validate.sh"
      to: ".planning/phases/06-runtime-failure-inventory/FAILURES.md"
      via: "Headless validation results are recorded with config-vs-environment triage"
      pattern: "startup|health|smoke"
    - from: ".planning/phases/06-runtime-failure-inventory/CHECKLIST.md"
      to: ".planning/phases/06-runtime-failure-inventory/FAILURES.md"
      via: "Interactive workflow outcomes justify bug status changes and regression notes"
      pattern: "BUG-016|BUG-017|search|explorer|git|LSP|which%-key|tmux"
---

<objective>
Re-verify the plugin workflows in milestone scope and leave the failure inventory in a trustworthy post-Phase-8 state.

Purpose: Convert the Phase 8 fixes into durable verification evidence, while explicitly separating real config regressions from sandbox or environment noise.
Output: Updated failure inventory and regression checklist grounded in automated and interactive verification.
</objective>

<execution_context>
@/home/pera/.codex/get-shit-done/workflows/execute-plan.md
@/home/pera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-plugin-runtime-hardening/08-CONTEXT.md
@.planning/phases/08-plugin-runtime-hardening/08-RESEARCH.md
@.planning/phases/08-plugin-runtime-hardening/08-VALIDATION.md
@.planning/phases/08-plugin-runtime-hardening/08-01-SUMMARY.md
@.planning/phases/08-plugin-runtime-hardening/08-02-SUMMARY.md
@.planning/phases/06-runtime-failure-inventory/FAILURES.md
@.planning/phases/06-runtime-failure-inventory/CHECKLIST.md
@scripts/nvim-validate.sh

<interfaces>
From `scripts/nvim-validate.sh`:
```bash
./scripts/nvim-validate.sh startup
./scripts/nvim-validate.sh health
./scripts/nvim-validate.sh smoke
```

From `FAILURES.md`:
```markdown
| BUG-016 | vim.tbl_flatten is deprecated ... | unknown plugin dependency | Discovered |
| BUG-017 | vim-tmux-navigator <C-h/j/k/l> ... | plugins/misc.lua + registry | Discovered |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run headless validation and update inventory triage with Phase 8 outcomes</name>
  <read_first>
    - .planning/phases/08-plugin-runtime-hardening/08-CONTEXT.md
    - .planning/phases/08-plugin-runtime-hardening/08-VALIDATION.md
    - .planning/phases/08-plugin-runtime-hardening/08-01-SUMMARY.md
    - .planning/phases/08-plugin-runtime-hardening/08-02-SUMMARY.md
    - .planning/phases/06-runtime-failure-inventory/FAILURES.md
    - scripts/nvim-validate.sh
  </read_first>
  <files>.planning/phases/06-runtime-failure-inventory/FAILURES.md</files>
  <acceptance_criteria>
    - `./scripts/nvim-validate.sh startup` and `./scripts/nvim-validate.sh health` both run and their outcomes are reflected in `FAILURES.md`.
    - `FAILURES.md` explicitly marks BUG-001 fixed when verified, records BUG-016 as Fixed or Won't Fix per D-08 with the traced plugin named, and marks BUG-017 Fixed only after `CHECKLIST.md` records both mapping ownership evidence and interactive tmux-pane traversal results per D-04.
    - `FAILURES.md` includes a note distinguishing remaining sandbox/environment noise from config defects.
  </acceptance_criteria>
  <action>Run the automated validation commands required by `08-VALIDATION.md` and use the results to update `FAILURES.md` as the living source of truth. Apply D-04, D-08, and D-10 only after evidence exists: mark BUG-017 fixed only after Task 2 records `:verbose nmap <C-h/j/k/l>` ownership and tmux-pane traversal outcomes, BUG-001 fixed once stale neo-tree probing is gone, and BUG-016 fixed or Won't Fix based on the traced-plugin outcome from Plan 08-01. Preserve any residual failures that are clearly sandbox-only or environment-only, but label them as such instead of treating them as config regressions.</action>
  <verify>
    <automated>./scripts/nvim-validate.sh startup && ./scripts/nvim-validate.sh health</automated>
  </verify>
  <done>`FAILURES.md` is current, evidence-backed, and explicit about which remaining warnings are environmental rather than repo bugs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Interactively verify search, explorer, git, LSP, UI, and external-open workflows</name>
  <read_first>
    - .planning/phases/08-plugin-runtime-hardening/08-CONTEXT.md
    - .planning/phases/08-plugin-runtime-hardening/08-VALIDATION.md
    - .planning/phases/06-runtime-failure-inventory/CHECKLIST.md
    - .planning/phases/06-runtime-failure-inventory/FAILURES.md
  </read_first>
  <files>.planning/phases/06-runtime-failure-inventory/CHECKLIST.md, .planning/phases/06-runtime-failure-inventory/FAILURES.md</files>
  <acceptance_criteria>
    - `CHECKLIST.md` contains explicit Phase 8 regression steps/results for Snacks search, Snacks explorer, git workflow, LSP workflow, UI workflow, tmux-navigation ownership/pane traversal, Linux external-open behavior, and Windows external-open behavior per D-04/D-15.
    - `FAILURES.md` and `CHECKLIST.md` agree on the final status of BUG-001, BUG-016, and BUG-017.
    - `./scripts/nvim-validate.sh startup` still passes after the documentation updates.
  </acceptance_criteria>
  <action>Guide the user through the D-15 workflow matrix and record the outcomes in `CHECKLIST.md`, then synchronize `FAILURES.md` with those manual results. The checklist must explicitly cover: Snacks file find/live grep/buffer picker; Snacks explorer open-close-navigation-safe file action; gitsigns preview, blame, and lazygit; LSP definition/hover/diagnostics/format-on-save; UI surfaces including which-key, notification/noice output, and visible statusline/fold rendering during normal editing; tmux-navigation ownership via `:verbose nmap <C-h>`, `:verbose nmap <C-j>`, `:verbose nmap <C-k>`, `:verbose nmap <C-l>` plus interactive pane traversal inside tmux and normal split movement outside tmux; Linux `<C-S-o>` external-open behavior; and Windows-target `<C-S-o>`/`vim.ui.open()` verification recorded from a Windows machine before the phase can be marked complete. If any mandatory workflow fails with a config-caused runtime defect, stop Phase 8 as not complete, record the exact failing step in `CHECKLIST.md` and `FAILURES.md`, and route back into remediation execution rather than treating the phase as documentation-only. Do not replace the interactive verification with guesswork or headless-only checks, and do not mark BUG-017 fixed without the written ownership evidence.</action>
  <what-built>Phase 8 runtime fixes from Plans 08-01 and 08-02, plus current automated validator evidence.</what-built>
  <how-to-verify>
    1. Open Neovim in this repo with the updated config.
    2. Run the search workflow: `<leader>ff`, live grep, and buffer picker. Confirm no error or crash.
    3. Run the explorer workflow: `<leader>e`, navigate, open/close, and a safe file action if available. Confirm no error.
    4. Run the git workflow in a tracked changed file: `<leader>gp`, `<leader>gt`, `<leader>gg`. Confirm preview/blame/lazygit all work without runtime errors.
    5. Run the LSP workflow in a file with an attached client: definition, hover, diagnostics, and save-triggered formatting. Confirm no crash.
    6. Run the UI workflow: trigger which-key, a notification or noice message, and inspect statusline/fold UI during normal editing. Confirm no runtime error or broken render.
    7. Verify tmux-navigation ownership: run `:verbose nmap <C-h>`, `<C-j>`, `<C-k>`, and `<C-l>` and record that the active owner is `vim-tmux-navigator`. Inside tmux, cross from a Neovim split into an adjacent tmux pane and back. Outside tmux, confirm the same keys still move across Neovim splits normally.
    8. Run `<C-S-o>` on a normal file buffer on Linux. Confirm it either opens externally or reports the real error text.
    9. On a Windows target machine using this same config, run `<C-S-o>` on a normal file buffer and record whether `vim.ui.open()` opens the file or reports the correct error text.
    10. Review the updated `CHECKLIST.md` and `FAILURES.md` entries before resuming.
  </how-to-verify>
  <resume-signal>Type `approved` if the workflow matrix passes, or describe the specific failing step so execution can continue with an exact gap.</resume-signal>
  <verify>
    <automated>./scripts/nvim-validate.sh startup</automated>
  </verify>
  <done>The phase has written evidence that the milestone workflows work interactively, including UI surfaces and tmux-navigation ownership, and any remaining gaps are precise enough to route into follow-up execution or gap-closure planning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated validator -> written inventory | Tool output must be interpreted before it becomes phase truth |
| human interactive verification -> written checklist | Manual workflow results become the permanent regression contract for future phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | R | `FAILURES.md` | mitigate | Record exact automated/manual evidence and tie status changes to concrete Phase 8 verification results. |
| T-08-10 | I | validator logs vs docs | mitigate | Separate sandbox/environment noise from config regressions in writing so future maintainers do not chase false failures. |
| T-08-11 | D | interactive workflows | mitigate | Block on human verification for search/explorer/git/LSP/UI/tmux-navigation/external-open so hidden runtime crashes or BUG-017 ownership regressions cannot be marked complete by automation alone. |
</threat_model>

<verification>
Automated baseline: `./scripts/nvim-validate.sh startup` and `./scripts/nvim-validate.sh health`.
Manual baseline: the D-15 workflow matrix plus D-04 tmux-navigation ownership evidence recorded in `CHECKLIST.md`.
</verification>

<success_criteria>
Phase 8 is complete only when the failure inventory and regression checklist both show that core plugin workflows operate without config-caused runtime errors, and remaining warnings are explicitly classified as environment-only noise rather than silently ignored.
</success_criteria>

<output>
After completion, create `.planning/phases/08-plugin-runtime-hardening/08-03-SUMMARY.md`
</output>
