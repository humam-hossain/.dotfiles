---
phase: 08-plugin-runtime-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - .config/nvim/lua/core/open.lua
  - .config/nvim/lua/core/keymaps.lua
  - .config/nvim/lua/core/options.lua
  - .config/nvim/lua/core/keymaps/whichkey.lua
  - .config/nvim/lua/plugins/conform.lua
  - .config/nvim/lua/plugins/lsp.lua
  - .config/nvim/lua/plugins/treesitter.lua
autonomous: true
requirements:
  - BUG-03
  - BUG-02
must_haves:
  truths:
    - "Common save, format, and LSP-attach flows bail out safely on special buffers instead of crashing the editing session."
    - "External open failures surface the real OS/helper error text instead of a generic false-negative notification."
    - "LSP highlight and attach hooks run only when a real client and normal file buffer exist."
  artifacts:
    - path: ".config/nvim/lua/core/open.lua"
      provides: "Direct vim.ui.open tuple handling with real error propagation"
    - path: ".config/nvim/lua/core/keymaps.lua"
      provides: "FocusLost autosave guards that avoid special-buffer writes"
    - path: ".config/nvim/lua/core/options.lua"
      provides: "Autocmd guard fix path if D-14 review finds crash-prone assumptions outside keymaps.lua"
    - path: ".config/nvim/lua/core/keymaps/whichkey.lua"
      provides: "which-key init-order fix path if D-14 review finds UI runtime issues"
    - path: ".config/nvim/lua/plugins/conform.lua"
      provides: "Format-on-save exclusion and buffer-kind guards for crash-prone buffers"
    - path: ".config/nvim/lua/plugins/lsp.lua"
      provides: "LspAttach/highlight safety checks for invalid clients or unsupported buffers"
    - path: ".config/nvim/lua/plugins/treesitter.lua"
      provides: "Treesitter init-order fix path if D-14 review finds reproducible runtime issues"
  key_links:
    - from: ".config/nvim/lua/core/open.lua"
      to: "vim.ui.open"
      via: "M.open() captures `(cmd, err)` directly and reports `err`"
      pattern: "vim\\.ui\\.open"
    - from: ".config/nvim/lua/core/keymaps.lua"
      to: ".config/nvim/lua/plugins/conform.lua"
      via: "autosave and format-on-save now share the same special-buffer assumptions"
      pattern: "buftype|modifiable|bufname"
    - from: ".config/nvim/lua/plugins/lsp.lua"
      to: ".config/nvim/lua/core/keymaps/attach.lua"
      via: "LspAttach callback gates `attach.apply_lsp(event.buf)` behind valid client/buffer checks"
      pattern: "LspAttach|apply_lsp"
---

<objective>
Harden the crash-prone runtime paths that can still break a normal editing session even after the plugin config defects are fixed.

Purpose: Make write, format, external-open, and LSP attach flows fail closed on invalid runtime state rather than crashing or spamming misleading notifications.
Output: Guarded open/autosave/format/LSP paths suitable for BUG-03 re-verification.
</objective>

<execution_context>
@/home/pera/.codex/get-shit-done/workflows/execute-plan.md
@/home/pera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-plugin-runtime-hardening/08-CONTEXT.md
@.planning/phases/08-plugin-runtime-hardening/08-RESEARCH.md
@.planning/phases/08-plugin-runtime-hardening/08-PATTERNS.md
@.planning/phases/08-plugin-runtime-hardening/08-VALIDATION.md
@.planning/phases/08-plugin-runtime-hardening/08-01-SUMMARY.md
@.config/nvim/lua/core/open.lua
@.config/nvim/lua/core/keymaps.lua
@.config/nvim/lua/core/options.lua
@.config/nvim/lua/core/keymaps/whichkey.lua
@.config/nvim/lua/plugins/conform.lua
@.config/nvim/lua/plugins/lsp.lua
@.config/nvim/lua/plugins/snacks.lua
@.config/nvim/lua/plugins/misc.lua
@.config/nvim/lua/plugins/treesitter.lua

<interfaces>
From `.config/nvim/lua/core/open.lua`:
```lua
function M.open(target) ... end
function M.open_current_buffer()
  local target = vim.fn.fnamemodify(buf_name, ":p")
  M.open(target)
end
```

From `.config/nvim/lua/plugins/conform.lua`:
```lua
format_on_save = function(bufnr)
  local bufname = vim.api.nvim_buf_get_name(bufnr)
  local ft = vim.bo[bufnr].filetype
  local buftype = vim.bo[bufnr].buftype
end
```

From `.config/nvim/lua/plugins/lsp.lua`:
```lua
vim.api.nvim_create_autocmd("LspAttach", {
  callback = function(event)
    local client = vim.lsp.get_client_by_id(event.data.client_id)
    if not client then return end
    attach.apply_lsp(event.buf)
  end,
})
```

From `.config/nvim/lua/core/keymaps/whichkey.lua`:
```lua
function M.setup()
  local ok, which_key = pcall(require, "which-key")
  if not ok then
    vim.notify("[keymaps.whichkey] which-key not loaded", vim.log.levels.DEBUG)
    return
  end
  which_key.add(global_specs)
  which_key.add(lazy_specs)
end
```

From `.config/nvim/lua/plugins/treesitter.lua`:
```lua
return {
  "nvim-treesitter/nvim-treesitter",
  build = ":TSUpdate",
  main = "nvim-treesitter.configs",
  opts = { auto_install = true, highlight = { enable = true } },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard external-open, autosave, and format-on-save against special-buffer runtime state</name>
  <read_first>
    - .planning/phases/08-plugin-runtime-hardening/08-CONTEXT.md
    - .planning/phases/08-plugin-runtime-hardening/08-RESEARCH.md
    - .config/nvim/lua/core/open.lua
    - .config/nvim/lua/core/keymaps.lua
    - .config/nvim/lua/plugins/conform.lua
  </read_first>
  <files>.config/nvim/lua/core/open.lua, .config/nvim/lua/core/keymaps.lua, .config/nvim/lua/plugins/conform.lua</files>
  <acceptance_criteria>
    - `rg -n 'local .*err *= *vim\\.ui\\.open|local .*cmd.*,.*err *= *vim\\.ui\\.open' .config/nvim/lua/core/open.lua` matches the direct tuple capture required by D-13.
    - `rg -n 'buftype|modifiable|bufname' .config/nvim/lua/core/keymaps.lua .config/nvim/lua/plugins/conform.lua` shows explicit guards for special/unnamed buffers.
    - `./scripts/nvim-validate.sh startup` passes after the guard changes.
  </acceptance_criteria>
  <action>Implement D-13 exactly in `core/open.lua`: stop wrapping `vim.ui.open()` in `pcall`, capture its return tuple directly, and notify with the real `errmsg` when present. In `core/keymaps.lua` and `plugins/conform.lua`, harden the FocusLost autosave and format-on-save paths for D-14 by rejecting special buffers (`nofile`, fugitive, picker previews, unnamed buffers, non-modifiable buffers, and any other existing excluded filetypes already represented in repo patterns). Keep the guard-first style already used in `conform.lua`; do not add broad `silent!` masking that would hide real failures.</action>
  <verify>
    <automated>./scripts/nvim-validate.sh startup</automated>
  </verify>
  <done>External open exposes the real failure message, and write/format hooks skip buffers that should never be auto-written or formatted during ordinary editing.</done>
</task>

<task type="auto">
  <name>Task 2: Harden LspAttach and attach-time highlight behavior without widening plugin scope</name>
  <read_first>
    - .planning/phases/08-plugin-runtime-hardening/08-CONTEXT.md
    - .planning/phases/08-plugin-runtime-hardening/08-RESEARCH.md
    - .planning/phases/08-plugin-runtime-hardening/08-01-SUMMARY.md
    - .config/nvim/lua/plugins/lsp.lua
    - .config/nvim/lua/core/options.lua
    - .config/nvim/lua/core/keymaps/attach.lua
    - .config/nvim/lua/core/keymaps/whichkey.lua
    - .config/nvim/lua/plugins/snacks.lua
    - .config/nvim/lua/plugins/misc.lua
    - .config/nvim/lua/plugins/treesitter.lua
  </read_first>
  <files>.config/nvim/lua/plugins/lsp.lua, .config/nvim/lua/core/options.lua, .config/nvim/lua/core/keymaps/whichkey.lua, .config/nvim/lua/plugins/treesitter.lua</files>
  <acceptance_criteria>
    - `rg -n 'if not client then|vim\\.api\\.nvim_buf_is_valid|buftype|filetype' .config/nvim/lua/plugins/lsp.lua` shows explicit attach-time guards.
    - `rg -n 'document_highlight|clear_references|LspDetach' .config/nvim/lua/plugins/lsp.lua` still shows the highlight lifecycle, but behind the new safety checks.
    - If D-14 review finds a reproducible issue in `.config/nvim/lua/core/options.lua`, `.config/nvim/lua/core/keymaps/whichkey.lua`, or `.config/nvim/lua/plugins/treesitter.lua`, the fix lands in that file in this plan and `08-02-SUMMARY.md` records the exact bug and remediation.
    - If D-14 review finds no reproducible issue outside `lsp.lua`, `08-02-SUMMARY.md` states that `options.lua`, `whichkey.lua`, and `treesitter.lua` were reviewed with no code change required.
    - `./scripts/nvim-validate.sh startup` passes after the LSP guard changes.
  </acceptance_criteria>
  <action>Per D-14, review the `LspAttach` callback and make it safe for invalid clients, invalid buffers, special buffers, and unsupported filetypes before any buffer-local keymaps or document-highlight autocmds are attached. Use targeted nil/buffer checks around `event.data`, `event.buf`, `vim.lsp.get_client_by_id(...)`, and any highlight setup that assumes a normal file buffer. Expand the mandated review scope to `core/options.lua` autocmd assumptions plus `core/keymaps/whichkey.lua` and `plugins/treesitter.lua` init-order interactions with Snacks/LSP startup; if that review reveals a concrete reproducible bug in one of those files, fix it in this plan instead of deferring it. If no such bug is reproduced, keep edits inside `lsp.lua` and record the clean review result in `08-02-SUMMARY.md`.</action>
  <verify>
    <automated>./scripts/nvim-validate.sh startup</automated>
  </verify>
  <done>The LSP attach path is resilient to invalid runtime state, and D-14's autocmd/init-order review explicitly covers `options.lua`, `whichkey.lua`, and `treesitter.lua` with either a reproduced fix or a written no-regression finding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Neovim events -> repo autocmd callbacks | Untrusted buffer/runtime state crosses into autosave, format, and LSP attach code |
| repo open helper -> OS opener | External command selection and failures come from the host environment, not trusted repo code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | D | `core/keymaps.lua` | mitigate | Refuse autosave on special, unnamed, or non-modifiable buffers so FocusLost cannot crash or mutate unsafe buffers. |
| T-08-06 | D | `plugins/conform.lua` | mitigate | Keep format-on-save behind explicit buffer/filetype guards so picker, fugitive, and nofile buffers do not enter formatter code paths. |
| T-08-07 | I | `core/open.lua` | mitigate | Surface `vim.ui.open()`'s returned error string so the user sees the real failure instead of a misleading generic notification. |
| T-08-08 | D | `plugins/lsp.lua` | mitigate | Gate attach/highlight setup on valid client and buffer checks to prevent attach-time crashes in special or unsupported buffers. |
</threat_model>

<verification>
Run `./scripts/nvim-validate.sh startup` after each task. If startup remains green, reserve deeper functional confirmation for Plan 08-03 rather than adding ad hoc manual checks here.
</verification>

<success_criteria>
The editing-session risk surface is reduced to guarded no-ops: bad buffers are skipped, real external-open errors are reported accurately, and `LspAttach` cannot crash on malformed attach context.
</success_criteria>

<output>
After completion, create `.planning/phases/08-plugin-runtime-hardening/08-02-SUMMARY.md`
</output>
