---
phase: 36-visual-voice-pill-component-dynamic-animations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml
  - scripts/phase36-voice-pill-assert.sh
autonomous: true
requirements:
  - VOICE-01
  - VOICE-02
  - VOICE-03
  - VOICE-04
  - VOICE-05
  - VOICE-06
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "VoicePill.qml renders as a dedicated status bar pill component extending BarGroup.qml with standard rounded rectangle geometry (12px radius, 5px padding, colLayer1 container) and clip: true (D-01, VOICE-01)."
    - "Material Symbol graphic_eq icon renders via MaterialSymbol.qml with Appearance.font.pixelSize.normal (16px), styled in colOnLayer1 during compact idle resting state (D-01, D-02, D-03, VOICE-02)."
    - "Gentle breathing pulse animation on graphic_eq icon (opacity cycling 1.0 <-> 0.5 over 1000ms) runs strictly during active STT recording, resetting cleanly to 1.0 on stop (D-08, VOICE-03)."
    - "Dynamic Material You palette shifts across lifecycle states via Matugen tokens (recording: colPrimary, transcribing: colTertiary, typing/speaking/wrapup: colSecondary, idle: colOnLayer1) with zero hardcoded hex colors (D-09, VOICE-03, VOICE-04)."
    - "Sequential Linear Flow displays live duration (M:SS) while recording/speaking, localized status badges ('Transcribing...', 'Typing...') during transitions, and lingers on final talk duration for 1500ms before returning to idle (D-06, D-07, VOICE-05)."
    - "Root implicitWidth binding bypasses Qt 6.11 GridLayout caching bug and drives fluid 250ms Material 3 emphasized deceleration width resizing between compact (26px) and expanded (62-125px) states (D-04, VOICE-06)."
  artifacts:
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml
    - scripts/phase36-voice-pill-assert.sh
  key_links:
    - "VoicePill.qml -> Voice.overallState and Voice.formattedDuration (services/Voice.qml)"
    - "VoicePill.qml -> BarGroup.qml container geometry and 250ms emphasizedDecel width animation"
    - "VoicePill.qml -> MaterialSymbol.qml (graphic_eq glyph from ttf-material-symbols-variable-git)"
    - "VoicePill.qml -> Appearance.colors (colPrimary, colTertiary, colSecondary, colOnLayer1)"
---

<objective>
Author and validate the dedicated `VoicePill.qml` status bar component in `restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml`, inheriting `BarGroup.qml` pill geometry, Google Material Symbols `graphic_eq` iconography, dynamic Material You color shifts, gentle breathing pulse animation, Sequential Linear Flow state badging with 1.5s wrap-up linger, and fluid 250ms Material 3 emphasized deceleration width expansion.

Purpose: Provide the visual status bar component for voice telemetry that seamlessly reflects real-time STT/TTS state without blocking the UI thread or inducing working-tree drift, ready for bar layout placement in Phase 37.
Output: Production `VoicePill.qml` component, leaf symlink deployed to `~/.config/quickshell/ii/modules/ii/bar/VoicePill.qml`, and an automated 6-section assertion test harness `scripts/phase36-voice-pill-assert.sh`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-visual-voice-pill-component-dynamic-animations/36-CONTEXT.md
@.planning/phases/36-visual-voice-pill-component-dynamic-animations/36-RESEARCH.md
@.planning/phases/36-visual-voice-pill-component-dynamic-animations/36-PATTERNS.md
@.planning/phases/36-visual-voice-pill-component-dynamic-animations/36-VALIDATION.md

# Relevant pattern analogs
@restow/quickshell/.config/quickshell/ii/services/Voice.qml
@restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/MaterialSymbol.qml
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/StyledText.qml
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/Appearance.qml
@restow/quickshell/.config/quickshell/ii/modules/ii/bar/UpdatesButton.qml
@restow/quickshell/.config/quickshell/ii/modules/ii/bar/Resource.qml
@scripts/phase35-voice-telemetry-assert.sh
</context>

## Artifacts this phase produces

- restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml
- scripts/phase36-voice-pill-assert.sh
- VoicePill.previousState
- VoicePill.lastRecordedDuration
- VoicePill.inWrapUp
- VoicePill.effectiveState
- VoicePill.currentColor
- VoicePill.displayText
- VoicePill.isExpanded
- VoicePill.implicitWidth
- VoicePill.wrapUpTimer
- VoicePill.pulseAnimation
- VoicePill.voiceIcon
- VoicePill.voiceLabel
- VoicePill.contentContainer

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end VoicePill component tracer slice, test harness scaffold, and geometry/iconography integration</name>
  <files>
    scripts/phase36-voice-pill-assert.sh,
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml
  </files>
  <read_first>
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarGroup.qml,
    vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/MaterialSymbol.qml,
    vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/StyledText.qml,
    vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/Appearance.qml,
    restow/quickshell/.config/quickshell/ii/services/Voice.qml,
    scripts/phase35-voice-telemetry-assert.sh,
    .planning/phases/36-visual-voice-pill-component-dynamic-animations/36-CONTEXT.md,
    .planning/phases/36-visual-voice-pill-component-dynamic-animations/36-RESEARCH.md,
    .planning/phases/36-visual-voice-pill-component-dynamic-animations/36-PATTERNS.md
  </read_first>
  <action>
    Author the end-to-end tracer slice establishing the automated assertion test harness, the core VoicePill component foundation, leaf symlink deployment, and M3 width resizing:

    1. Automated Assertion Test Harness Scaffold (scripts/phase36-voice-pill-assert.sh):
       - Create scripts/phase36-voice-pill-assert.sh following scripts/phase35-voice-telemetry-assert.sh: bash strict mode (set -euo pipefail), non-root execution check (EUID != 0), cleanup trap on EXIT for temporary files and test runners, git status porcelain snapshot before/after, and CLI section parsing (--section|-s 1-6).
       - Implement run_qs_test helper using headless Quickshell (quickshell -p) with temporary QML runners written into XDG_CONFIG_HOME/quickshell/ii/ and cleaned up safely.
       - Implement Section 1 (VOICE-01, D-01): assert restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml exists, assert it is deployed as a symlink to ~/.config/quickshell/ii/modules/ii/bar/VoicePill.qml without folding ancestor directories, assert root element is BarGroup, assert clip is true, and assert idle compact state displays only icon without text badges.
       - Implement Section 2 (VOICE-02, D-02, D-03, D-05, D-09): assert zero hardcoded hex color codes (grep -E "#[0-9a-fA-F]{3,8}" returns 0), assert text: "graphic_eq" glyph declaration, assert MaterialSymbol usage, assert Material You palette bindings (Appearance.colors.colPrimary, colTertiary, colSecondary, colOnLayer1), assert wrapUpTimer interval is 1500, and assert absence of vertical equalizer wave bar sub-items (D-05).
       - Implement Section 4 (VOICE-06, D-04): headless Quickshell test asserting root implicitWidth expands dynamically from compact resting state (26px = 16px icon + 10px padding) to expanded state (>= 60px) and contracts back to 26px when returning to idle, verifying intermediate samples follow 250ms Material 3 emphasized deceleration (Appearance.animationCurves.emphasizedDecel).
       - Add placeholder stubs for Section 3, Section 5, and Section 6. Make script executable (chmod +x scripts/phase36-voice-pill-assert.sh).

    2. Foundational VoicePill.qml Component (restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml):
       - Declare pragma ComponentBehavior: Bound.
       - Import qs.modules.common, qs.modules.common.widgets, qs.services, QtQuick, QtQuick.Layouts, and Quickshell.
       - Define BarGroup as root container with id: root and clip: true (D-01, Pitfall 5).
       - Define state tracking properties: property string previousState: "idle", property string lastRecordedDuration: "0:00", readonly property bool inWrapUp: wrapUpTimer.running.
       - Define effectiveState property: if inWrapUp return "wrapup"; else return Voice.overallState.
       - Define currentColor property binding (D-09): recording -> Appearance.colors.colPrimary; transcribing -> Appearance.colors.colTertiary; typing, speaking, wrapup -> Appearance.colors.colSecondary; starting -> Appearance.colors.colPrimary; default -> Appearance.colors.colOnLayer1.
       - Define displayText property binding (D-06, D-07): recording, speaking, starting -> Voice.formattedDuration; transcribing -> Translation.tr("Transcribing..."); typing -> Translation.tr("Typing..."); wrapup -> lastRecordedDuration; default -> "".
       - Define isExpanded property: effectiveState !== "idle".
       - Implement explicit root implicitWidth override (D-04, VOICE-06, Pitfall 1): vertical ? Appearance.sizes.baseVerticalBarWidth : (voiceIcon.implicitWidth + (isExpanded ? (voiceLabel.implicitWidth + 4) : 0) + padding * 2). This bypasses Qt 6.11 GridLayout implicitWidth caching bugs and directly drives BarGroup's native Behavior on implicitWidth (250ms emphasizedDecel).
       - Implement wrapUpTimer: Timer with id: wrapUpTimer, interval: 1500, repeat: false.
       - Inside root, declare Item id: contentContainer with implicitWidth: voiceIcon.implicitWidth + (root.isExpanded ? (voiceLabel.implicitWidth + 4) : 0), implicitHeight: Appearance.font.pixelSize.normal, anchors.verticalCenter: parent.verticalCenter (Pitfall 2).
       - Declare MaterialSymbol id: voiceIcon (D-02, D-03): anchors.left: parent.left, anchors.verticalCenter: parent.verticalCenter, text: "graphic_eq", iconSize: Appearance.font.pixelSize.normal, color: root.currentColor. Include Behavior on color with ColorAnimation duration 200ms, easing.type: Easing.BezierSpline, easing.bezierCurve: Appearance.animationCurves.expressiveEffects.
       - Declare StyledText id: voiceLabel (D-06, VOICE-05): anchors.left: voiceIcon.right, anchors.leftMargin: 4, anchors.verticalCenter: parent.verticalCenter, text: root.displayText, font.pixelSize: Appearance.font.pixelSize.small, color: root.currentColor, visible: root.isExpanded, opacity: root.isExpanded ? 1.0 : 0.0. Include Behavior on color (200ms expressiveEffects) and Behavior on opacity (150ms expressiveEffects).

    3. Deployment via GNU Stow:
       - Deploy component to live user configuration using stow -d restow --no-folding -t ~ quickshell.
       - Verify ~/.config/quickshell/ii/modules/ii/bar/VoicePill.qml exists as a valid leaf symlink resolving to restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml without folding parent directories.
  </action>
  <reversibility rating="reversible">
    Component is authored in restow/quickshell/ and deployed as a leaf symlink. Can be unlinked via stow -D without touching upstream vendor/dots-hyprland.
  </reversibility>
  <verify>
    <automated>./scripts/phase36-voice-pill-assert.sh --section 1 && ./scripts/phase36-voice-pill-assert.sh --section 2 && ./scripts/phase36-voice-pill-assert.sh --section 4</automated>
    <fails_when>non-zero exit code or FAIL > 0 or FINDINGS > 0</fails_when>
  </verify>
  <acceptance_criteria>
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml exists and is symlinked to ~/.config/quickshell/ii/modules/ii/bar/VoicePill.qml.
    - Component uses BarGroup root geometry (radius 12px, padding 5px, colLayer1 container) and clip: true.
    - Component renders graphic_eq glyph using MaterialSymbol.qml with zero hardcoded hex colors.
    - Color tokens bind to Appearance.colors.colPrimary, colTertiary, colSecondary, and colOnLayer1.
    - Root implicitWidth animates smoothly across 250ms emphasized deceleration curve without Qt 6.11 GridLayout caching lockups.
    - Sections 1, 2, and 4 in scripts/phase36-voice-pill-assert.sh pass with FAIL=0 and FINDINGS=0.
  </acceptance_criteria>
  <done>
    Tracer slice is verified end-to-end: VoicePill component foundation, graphic_eq icon, responsive M3 implicitWidth calculation, and test harness sections 1, 2, and 4 pass cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Breathing pulse animation with mid-cycle opacity reset and active recording/speaking transitions</name>
  <files>
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml,
    scripts/phase36-voice-pill-assert.sh
  </files>
  <read_first>
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml,
    vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/MaterialSymbol.qml,
    scripts/phase36-voice-pill-assert.sh,
    .planning/phases/36-visual-voice-pill-component-dynamic-animations/36-RESEARCH.md,
    .planning/phases/36-visual-voice-pill-component-dynamic-animations/36-PATTERNS.md
  </read_first>
  <action>
    Implement gentle breathing pulse animation during active recording and test assertions in scripts/phase36-voice-pill-assert.sh:

    1. Pulse Animation Implementation in VoicePill.qml (VOICE-03, D-08, Pitfall 3):
       - Inside MaterialSymbol id: voiceIcon, implement SequentialAnimation id: pulseAnimation.
       - Set running property strictly to: root.effectiveState === "recording".
       - Set loops property to: Animation.Infinite.
       - Implement onRunningChanged handler: if (!running) voiceIcon.opacity = 1.0;. This guarantees that whenever recording stops mid-cycle (e.g. at opacity 0.5 or 0.7), the icon immediately and unconditionally restores full 1.0 opacity without lingering in a dimmed or frozen state (Pitfall 3).
       - Add first child animation: NumberAnimation { target: voiceIcon; property: "opacity"; to: 0.5; duration: 500; easing.type: Easing.InOutSine }.
       - Add second child animation: NumberAnimation { target: voiceIcon; property: "opacity"; to: 1.0; duration: 500; easing.type: Easing.InOutSine }.
       - Total breathing cycle period is exactly 1000ms (500ms down, 500ms up).

    2. Automated Assertion Test Implementation (scripts/phase36-voice-pill-assert.sh Section 5):
       - Implement Section 5 (VOICE-03, D-08) in scripts/phase36-voice-pill-assert.sh:
       - Assert static properties: pulseAnimation declared with running bound to recording state, loops set to Animation.Infinite, cycle duration 500ms + 500ms, opacity range 1.0 to 0.5, and onRunningChanged restoring opacity = 1.0.
       - Launch headless Quickshell test (quickshell -p) driving state transitions:
         - In idle state: assert pulseAnimation.running is false and voiceIcon.opacity is 1.0.
         - Transition to recording: assert pulseAnimation.running is true, sample intermediate opacity values verifying oscillation between 0.5 and 1.0.
         - Abruptly transition to transcribing: assert pulseAnimation.running stops immediately and voiceIcon.opacity resets to 1.0 without delay.
         - Abruptly transition to idle: assert voiceIcon.opacity is 1.0.
  </action>
  <reversibility rating="reversible">
    Animation properties are self-contained within VoicePill.qml and can be adjusted or reverted without external side effects.
  </reversibility>
  <verify>
    <automated>./scripts/phase36-voice-pill-assert.sh --section 5</automated>
    <fails_when>non-zero exit code or FAIL > 0 or FINDINGS > 0</fails_when>
  </verify>
  <acceptance_criteria>
    - SequentialAnimation runs strictly when effectiveState === "recording".
    - Breathing pulse oscillates opacity between 1.0 and 0.5 over a 1000ms period with Easing.InOutSine.
    - onRunningChanged handler guarantees voiceIcon.opacity returns to 1.0 whenever recording terminates.
    - Section 5 of scripts/phase36-voice-pill-assert.sh passes with FAIL=0 and FINDINGS=0.
  </acceptance_criteria>
  <done>
    Breathing pulse animation is fully operational with verified 1000ms cycle timing, smooth sine easing, and fail-safe mid-cycle opacity reset.
  </done>
</task>

<task type="auto">
  <name>Task 3: Sequential Linear Flow state engine, completion wrap-up linger, and full regression verification</name>
  <files>
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml,
    scripts/phase36-voice-pill-assert.sh
  </files>
  <read_first>
    restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml,
    restow/quickshell/.config/quickshell/ii/services/Voice.qml,
    scripts/phase36-voice-pill-assert.sh,
    .planning/phases/36-visual-voice-pill-component-dynamic-animations/36-CONTEXT.md,
    .planning/phases/36-visual-voice-pill-component-dynamic-animations/36-RESEARCH.md,
    .planning/phases/36-visual-voice-pill-component-dynamic-animations/36-PATTERNS.md
  </read_first>
  <action>
    Implement complete Sequential Linear Flow state engine, duration caching, 1.5s wrap-up linger, and finalize test harness sections 3 and 6:

    1. Sequential Linear Flow & Wrap-Up Engine in VoicePill.qml (VOICE-04, VOICE-05, D-06, D-07, Pitfall 4):
       - In VoicePill.qml, implement Connections { target: Voice } to coordinate state shifts and metric caching:
       - Implement function onOverallStateChanged():
         - When Voice.overallState === "recording" or Voice.overallState === "speaking", immediately call wrapUpTimer.stop().
         - When Voice.overallState === "idle", inspect root.previousState: if previousState === "typing" or previousState === "speaking", call wrapUpTimer.restart() to hold the completed talk duration during wrapup; otherwise call wrapUpTimer.stop().
         - For any other state transition (transcribing, typing), call wrapUpTimer.stop().
         - Update root.previousState = Voice.overallState.
       - Implement function onFormattedDurationChanged():
         - When Voice.formattedDuration !== "0:00", update root.lastRecordedDuration = Voice.formattedDuration. This preserves the final duration so that when Voice.qml resets formattedDuration to "0:00" on entering idle, the 1.5s wrap-up display shows the real elapsed speech time instead of "0:00" (Pitfall 4).
       - When wrapUpTimer expires (interval: 1500), inWrapUp evaluates to false, effectiveState returns to "idle", displayText becomes "", isExpanded becomes false, and root implicitWidth smoothly contracts back to 26px via 250ms emphasizedDecel.
       - Ensure TTS playback (speaking) displays Voice.formattedDuration matching recording format (D-07).

    2. Automated Assertion Test Implementation (scripts/phase36-voice-pill-assert.sh Sections 3 and 6):
       - In scripts/phase36-voice-pill-assert.sh, implement Section 3 (VOICE-04, VOICE-05, D-06, D-07):
         - Launch headless Quickshell test driving mock states in sequential order:
           - 1. idle: assert displayText == "", isExpanded == false, currentColor == colOnLayer1.
           - 2. recording: assert displayText == Voice.formattedDuration ("0:05"), currentColor == colPrimary.
           - 3. transcribing: assert displayText == "Transcribing...", currentColor == colTertiary.
           - 4. typing: assert displayText == "Typing...", currentColor == colSecondary.
           - 5. idle transition triggers wrapup: assert inWrapUp is true, displayText == "0:05", currentColor == colSecondary.
           - 6. wait 1600ms for wrapUpTimer expiration: assert inWrapUp is false, effectiveState == "idle", displayText == "", isExpanded == false.
           - 7. speaking (TTS): assert displayText == Voice.formattedDuration ("0:03"), currentColor == colSecondary.
       - Implement Section 6 (Repository Invariance & Strict Verification Gate):
         - Assert git working-tree invariance: diff -u "$PORCELAIN_BEFORE" "$PORCELAIN_AFTER" returns 0.
         - Execute ./arch/dots-hyprland.sh verify --strict and assert output contains "=== done: FAIL=0 FINDINGS=0 ===".
       - Run full test suite ./scripts/phase36-voice-pill-assert.sh across all 6 sections.
  </action>
  <reversibility rating="reversible">
    State bindings and duration caching logic are contained within VoicePill.qml and test harness, completely reversible without touching external files.
  </reversibility>
  <verify>
    <automated>./scripts/phase36-voice-pill-assert.sh</automated>
    <fails_when>non-zero exit code or FAIL > 0 or FINDINGS > 0</fails_when>
  </verify>
  <acceptance_criteria>
    - Sequential Linear Flow executes smoothly: recording -> transcribing -> typing -> wrapup -> idle.
    - Live timer displays during recording and speaking; status badges display during transcribing and typing.
    - Last recorded duration is cached and displayed for 1500ms during wrapup without flashing "0:00".
    - Full assertion harness ./scripts/phase36-voice-pill-assert.sh passes all 6 sections with FAIL=0 and FINDINGS=0.
    - ./arch/dots-hyprland.sh verify --strict passes with 0 findings and zero git working-tree churn.
  </acceptance_criteria>
  <done>
    Full visual voice pill component and dynamic animation suite is implemented and verified end-to-end with zero errors and zero working-tree drift.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Voice.qml Singleton Service -> VoicePill.qml UI bindings | Untrusted or malformed string data from overallState or formattedDuration |
| Appearance.qml / Matugen -> VoicePill.qml color tokens | Dynamic palette token shifts during wallpaper reload |
| QtQuick SceneGraph -> QML UI animation thread | Animation loops, timer intervals, and CPU execution |
| GNU Stow / bash scripts -> Filesystem links | Leaf symlink deployment and test script execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-01 | Denial of Service | VoicePill.qml (pulseAnimation & timers) | medium | mitigate | Constrain pulseAnimation strictly to effectiveState === "recording", reset opacity on stop, use hardware-accelerated scene graph animations, and configure wrapUpTimer as single-shot (repeat: false, 1500ms interval). |
| T-36-02 | Spoofing / Tampering | VoicePill.qml (overallState binding) | medium | mitigate | Implement exhaustive switch statements with default fallbacks returning safe tokens (Appearance.colors.colOnLayer1, "") so malformed or unexpected states fail soft without QML script errors. |
| T-36-03 | Elevation of Privilege | scripts/phase36-voice-pill-assert.sh | high | mitigate | Enforce bash strict mode (set -euo pipefail), refuse execution if run as root (EUID != 0), quote all filesystem paths and variables, and execute commands directly without eval or unescaped shell strings. |
| T-36-04 | Information Disclosure | VoicePill.qml (status labels) | low | mitigate | Component displays strictly generic lifecycle state badges ("Transcribing...", "Typing...") and duration numbers (M:SS); zero raw transcript text or user clipboard data is rendered in the bar widget. |
</threat_model>

<verification>
Automated multi-section test suite in `scripts/phase36-voice-pill-assert.sh`:
- Section 1: Restow leaf symlink integrity without directory folding, BarGroup root geometry (radius 12px, padding 5px, colLayer1 container), and compact idle state (VOICE-01, D-01).
- Section 2: Component AST inspection: zero hardcoded hex colors, graphic_eq glyph rendering via MaterialSymbol.qml, Matugen color token bindings, and absence of vertical wave bars (VOICE-02, D-02, D-03, D-05, D-09).
- Section 3: Headless Quickshell test driving Sequential Linear Flow across all states (idle, recording, transcribing, typing, speaking, wrapup), duration formatting, status badges, and 1500ms wrap-up linger (VOICE-04, VOICE-05, D-06, D-07).
- Section 4: M3 emphasized deceleration width resizing: root implicitWidth calculation bypassing Qt 6.11 GridLayout caching, verifying smooth expansion (26px -> >= 60px) and contraction (VOICE-06, D-04).
- Section 5: Breathing pulse animation: running strictly on recording, 1000ms cycle, 1.0 <-> 0.5 opacity bounds, and reliable mid-cycle reset to 1.0 on stop (VOICE-03, D-08).
- Section 6: Git working-tree invariance (git status --porcelain identical before and after) and strict repository verification (arch/dots-hyprland.sh verify --strict with FAIL=0 FINDINGS=0).
</verification>

<success_criteria>
1. `VoicePill.qml` is authored in `restow/quickshell/.config/quickshell/ii/modules/ii/bar/VoicePill.qml` and deployed as a leaf symlink to `~/.config/quickshell/ii/modules/ii/bar/VoicePill.qml`.
2. Idle state displays a compact pill showing only the `graphic_eq` icon in standard status bar color (`colOnLayer1`).
3. Active STT recording triggers a gentle breathing pulse animation (1.0 <-> 0.5 opacity over 1000ms) that resets cleanly to 1.0 on stop.
4. Material You colors dynamically reflect active wallpaper tokens (`colPrimary`, `colTertiary`, `colSecondary`, `colOnLayer1`) with zero hardcoded hex colors.
5. Sequential Linear Flow coordinates live timer display, status badges ("Transcribing...", "Typing..."), and 1.5s completion wrap-up linger.
6. Pill width expands smoothly from 26px to >= 60px on voice activity and contracts back to 26px via 250ms Material 3 emphasized deceleration without Qt 6.11 caching locks.
7. Full test suite `./scripts/phase36-voice-pill-assert.sh` passes all 6 sections (`FAIL=0 FINDINGS=0`).
8. `./arch/dots-hyprland.sh verify --strict` reports 0 findings with zero git working-tree churn.
</success_criteria>

<output>
Create `.planning/phases/36-visual-voice-pill-component-dynamic-animations/36-01-SUMMARY.md` when done
</output>
