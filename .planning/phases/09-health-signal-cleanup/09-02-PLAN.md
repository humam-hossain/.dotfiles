---
phase: 09-health-signal-cleanup
plan: "02"
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - .config/nvim/lua/core/health.lua
  - .config/nvim/lua/config/health.lua
  - .config/nvim/README.md
autonomous: true
requirements:
  - HEAL-01
  - HEAL-02
must_haves:
  truths:
    - "`:checkhealth config` clearly separates required failures from optional tool or environment warnings."
    - "The existing `core.health` module stops registering a broken provider and instead supplies reusable probe functions and a safe compatibility check path."
    - "Required tools `git` and `rg` are classified in code, while all other tools stay warnings with current install hints."
    - "Maintainers can discover the new validator command from README if the validation-command table is still present."
  artifacts:
    - path: ".config/nvim/lua/core/health.lua"
      provides: "Exported `probe_tool`/`probe_plugin`, `required` metadata, updated tool hints, and a non-broken `M.check` compatibility path"
    - path: ".config/nvim/lua/config/health.lua"
      provides: "Repo-owned `vim.health` provider with version, required tools, optional tools, plugin load, config guards, and known environment gaps sections"
    - path: ".config/nvim/README.md"
      provides: "Validation command table row for `./scripts/nvim-validate.sh checkhealth` if that table remains in use"
  key_links:
    - from: ".config/nvim/lua/config/health.lua"
      to: ".config/nvim/lua/core/health.lua"
      via: "Provider reuses exported probe functions instead of duplicating tool/plugin metadata"
      pattern: "require\\(\"core\\.health\"\\)|probe_tool|probe_plugin"
    - from: ".config/nvim/lua/core/health.lua"
      to: "scripts/nvim-validate.sh"
      via: "Shared tool metadata marks `git` and `rg` as required while the validator preserves the same classification in bash"
      pattern: "required|affected_feature|install_hint"
    - from: ".config/nvim/lua/core/health.lua"
      to: ".config/nvim/lua/config/health.lua"
      via: "`M.check` must no longer error when Neovim discovers `core/health.lua` as a provider"
      pattern: "function M\\.check"
    - from: ".config/nvim/README.md"
      to: "scripts/nvim-validate.sh"
      via: "README validation commands reflect the new `checkhealth` entrypoint"
      pattern: "nvim%-validate%.sh checkhealth"
---

<objective>
Finish the repo-owned health surface so `:checkhealth` becomes both trustworthy and readable for maintainers.

Purpose: Reuse the existing health probe module as shared infrastructure, add a real `config` health provider, and classify required versus optional findings directly in code.
Output: Updated `core.health`, new `lua/config/health.lua`, and README command discoverability.
</objective>

<execution_context>
@/home/pera/.codex/get-shit-done/workflows/execute-plan.md
@/home/pera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-health-signal-cleanup/09-CONTEXT.md
@.planning/phases/09-health-signal-cleanup/09-RESEARCH.md
@.planning/phases/09-health-signal-cleanup/09-VALIDATION.md
@.planning/phases/09-health-signal-cleanup/09-01-SUMMARY.md
@scripts/nvim-validate.sh
@.config/nvim/lua/core/health.lua
@.config/nvim/README.md

<interfaces>
From `.config/nvim/lua/core/health.lua`:
```lua
local function probe_plugin(name)
  return { name = name, loaded = ok and true or false, error = ... }
end

local function probe_tool(name)
  return {
    name = name,
    available = available,
    affected_feature = meta.affected_feature,
    install_hint = meta.install_hint,
  }
end
```

From `scripts/nvim-validate.sh`:
```bash
PLUGIN_LIST="{'snacks','lualine','lspconfig','conform','nvim-treesitter.configs','blink.cmp','gitsigns','ufo','bufferline','which-key','render-markdown'}"
TOOL_LIST="{'stylua','black','isort','prettierd','prettier','clang-format','shfmt','rg','git','node','go','clangd','gopls','lua-language-server'}"
```

From `.config/nvim/README.md`:
```markdown
| `./scripts/nvim-validate.sh health` | Core health snapshot (plugins + tools) |
| `./scripts/nvim-validate.sh all` | Run all validations in order |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor `core.health` into shared probe infrastructure with required classification</name>
  <read_first>
    - .planning/phases/09-health-signal-cleanup/09-CONTEXT.md
    - .planning/phases/09-health-signal-cleanup/09-RESEARCH.md
    - .config/nvim/lua/core/health.lua
    - scripts/nvim-validate.sh
  </read_first>
  <files>.config/nvim/lua/core/health.lua</files>
  <acceptance_criteria>
    - `rg -n 'required=true|required = true' .config/nvim/lua/core/health.lua` matches only the `git` and `rg` metadata entries per D-16.
    - `rg -n 'function M\\.probe_tool|function M\\.probe_plugin|function M\\.check' .config/nvim/lua/core/health.lua` shows the exported probes and the provider-safe `M.check`.
    - `rg -n 'snacks|picker|gitsigns|lazy' .config/nvim/lua/core/health.lua` reflects updated feature descriptions and no longer describes ripgrep as an `fzf-lua` dependency.
    - `rg -n 'TODO: Health snapshot for validation harness' .config/nvim/lua/core/health.lua` returns no matches.
    - `nvim --headless -u .config/nvim/init.lua --cmd \"set rtp^=.config/nvim\" +\"lua local h=require('core.health'); assert(type(h.probe_tool)=='function'); assert(type(h.probe_plugin)=='function'); vim.cmd('qa!')\"` exits 0.
  </acceptance_criteria>
  <action>Implement D-13 through D-19 in `core.health.lua` without changing file ownership reserved for 09-01. Remove the stale TODO banner, add the one-line `required` semantics comment above `TOOL_METADATA`, and update every metadata entry so affected features and install hints match the shipped stack and current Arch/Debian wording. Export `probe_tool` and `probe_plugin` as `M.probe_tool` and `M.probe_plugin`, preserve `snapshot()` as the JSON-oriented headless surface, and add a safe `M.check` implementation so `:checkhealth core` no longer crashes when Neovim auto-discovers `lua/core/health.lua`. That `M.check` should be a compatibility shim, not a second copy of the full report: delegate to `require('config.health').check()` when available, or emit a clear `vim.health.error()` if the new provider is missing.</action>
  <verify>
    <automated>nvim --headless -u .config/nvim/init.lua --cmd "set rtp^=.config/nvim" +"lua local h=require('core.health'); assert(type(h.probe_tool)=='function'); assert(type(h.probe_plugin)=='function'); vim.cmd('qa!')"</automated>
  </verify>
  <done>`core.health` is now reusable infrastructure instead of a broken provider, and the code itself declares which tools are required versus optional.</done>
</task>

<task type="auto">
  <name>Task 2: Create `:checkhealth config`, classify environment gaps, and publish the validator command</name>
  <read_first>
    - .planning/phases/09-health-signal-cleanup/09-CONTEXT.md
    - .planning/phases/09-health-signal-cleanup/09-RESEARCH.md
    - .planning/phases/09-health-signal-cleanup/09-01-SUMMARY.md
    - .config/nvim/lua/core/health.lua
    - .config/nvim/README.md
    - .planning/phases/06-runtime-failure-inventory/FAILURES.md
  </read_first>
  <files>.config/nvim/lua/config/health.lua, .config/nvim/README.md</files>
  <acceptance_criteria>
    - `test -f .config/nvim/lua/config/health.lua` passes.
    - `rg -n 'vim\\.health\\.start|vim\\.health\\.(ok|warn|error)' .config/nvim/lua/config/health.lua` shows the provider uses the Neovim health API directly.
    - `rg -n 'Neovim version|Required tools|Optional tools|Plugin load status|Config guards|Known environment gaps' .config/nvim/lua/config/health.lua` finds the six mandated sections from D-09.
    - `rg -n 'C-h|C-j|C-k|C-l|xdg-open|leader>o|<leader>o' .config/nvim/lua/config/health.lua` shows copy-paste or investigation guidance for the tmux and Linux external-open warnings per D-20 through D-22.
    - `rg -n 'nvim-validate\\.sh checkhealth' .config/nvim/README.md` matches a validation-command row if the README still contains the validation commands table.
    - `./scripts/nvim-validate.sh health && ./scripts/nvim-validate.sh checkhealth` passes after the new provider lands.
  </acceptance_criteria>
  <action>Create `lua/config/health.lua` per D-08 through D-12. Wrap `require('core.health')` and each probe block in `pcall`, use `vim.health.start()` to emit the six required sections, and drive the tool/plugin sections from the exported probes so there is one source of truth. Required tools (`git`, `rg`) must emit `vim.health.error()` when missing, optional tools must emit `vim.health.warn()`, plugin probe failures must stay visible, and the config-guard section must include the Neovim `>= 0.12.0` check plus any repo-specific guard findings surfaced by 09-01's audit. The known-environment section must always render and include plain-language tmux companion-binding guidance plus BUG-020 investigation guidance without BUG IDs. If the README still has the validation commands table, add a row for `./scripts/nvim-validate.sh checkhealth`; do not add a separate prose section for classification because D-25 forbids that.</action>
  <verify>
    <automated>./scripts/nvim-validate.sh health && ./scripts/nvim-validate.sh checkhealth</automated>
  </verify>
  <done>The repo ships a working `:checkhealth config` surface, `:checkhealth core` no longer errors, and maintainers can tell which findings are fix-now versus optional/environment-only without opening extra docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo health provider -> Neovim health UI | Repo code emits severity and guidance that users will trust to decide whether action is required |
| shared probe module -> validator and provider | Drift between bash and Lua classification would make health results inconsistent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | I | `.config/nvim/lua/core/health.lua` | mitigate | Keep one metadata table with exported probes and a compatibility `M.check` so Neovim discovery cannot surface a broken provider. |
| T-09-06 | T | `.config/nvim/lua/config/health.lua` | mitigate | Reuse exported probes and `pcall` every section so provider failures become explicit health errors instead of crashing the report. |
| T-09-07 | R | `.config/nvim/lua/config/health.lua` | mitigate | Encode required-vs-optional severity in code and render environment gaps unconditionally, so maintainers can distinguish repo defects from optional tooling at a glance. |
| T-09-08 | I | `.config/nvim/README.md` | mitigate | Keep the README update limited to the existing validation commands table so command discovery stays accurate without drifting into duplicate narrative docs. |
</threat_model>

<verification>
Run the focused module check after Task 1, then run `./scripts/nvim-validate.sh health && ./scripts/nvim-validate.sh checkhealth` after Task 2. Interactive spot-check: open Neovim and run `:checkhealth config` to confirm the six sections and severity tiers render as intended.
</verification>

<success_criteria>
HEAL-01 and HEAL-02 are satisfied when both repo-owned providers (`core` compatibility shim and `config`) stop emitting config-caused errors, required tools are elevated to failures, optional tools/environment gaps remain warnings, and the new validator command is discoverable.
</success_criteria>

<output>
After completion, create `.planning/phases/09-health-signal-cleanup/09-02-SUMMARY.md`
</output>
