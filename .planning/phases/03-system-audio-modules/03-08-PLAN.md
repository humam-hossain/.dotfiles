---
phase: 03-system-audio-modules
plan: 08
type: execute
wave: 5
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
  - "03-04"
  - "03-05"
  - "03-06"
  - "03-07"
files_modified:
  - .planning/phases/03-system-audio-modules/03-VALIDATION.md
autonomous: true
requirements:
  - BAR-05
  - BAR-06
  - BAR-07
  - BAR-08
must_haves:
  truths:
    - python3 scripts/phase03-config-assert.py exits 0
    - Static gates prove CPU→RAM→Disk order, no swap bar UI, no ResourcesPopup attach, dual thresholds, disk df, Audio maxVolume 1.30, auto-unmute, mute % + volumeMixer, right-bar scroll
    - quickshell smoke reaches Configuration Loaded without hard errors
    - VALIDATION.md task map statuses updated; nyquist_compliant set true only if all automated gates green
  artifacts:
    - path: .planning/phases/03-system-audio-modules/03-VALIDATION.md
      provides: "Phase gate sign-off for automated Nyquist sampling"
  key_links:
    - from: VALIDATION.md automated commands
      to: phase03-config-assert + rg gates + quickshell smoke
      via: "full suite"
      pattern: "phase03-config-assert"
  prohibitions:
    - Do not enable weather or reintroduce brightness/ddcutil
    - Do not claim UAT manual rows green without human (leave manual table pending)
    - Do not strip non-phase modules to force smoke green
---

<objective>
Close Phase 3 with automated static gates, config assert, and smoke verification across BAR-05..08; update VALIDATION.md sampling map and Nyquist frontmatter.

Purpose: Product plans can leave silent regressions; this plan is the automated phase gate before human UAT (`/gsd-verify-work`).
Output: All automated verifies green; VALIDATION.md updated; fix-only product edits if a gate fails.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-system-audio-modules/03-CONTEXT.md
@.planning/phases/03-system-audio-modules/03-RESEARCH.md
@.planning/phases/03-system-audio-modules/03-VALIDATION.md
@scripts/phase03-config-assert.py
@.config/quickshell/modules/ii/bar/Resources.qml
@.config/quickshell/modules/ii/bar/Resource.qml
@.config/quickshell/services/ResourceUsage.qml
@.config/quickshell/services/Audio.qml
@.config/quickshell/modules/ii/bar/BarContent.qml
@.config/hypr/hyprland.conf
</context>

## Artifacts this phase produces (this plan)

| Symbol | Kind | Path |
|--------|------|------|
| Nyquist sign-off frontmatter | docs | 03-VALIDATION.md |
| Gate result notes | docs | 03-08-SUMMARY.md |

## Decisions / prohibitions enforced at gate

- No brightness/ddcutil; no dedicated volume module; no ResourcesPopup; no swap on bar

<tasks>

<task type="auto" id="03-08-01" tdd="false">
  <name>Task 1: Run full automated static + assert + smoke suite</name>
  <files>.planning/phases/03-system-audio-modules/03-VALIDATION.md</files>
  <read_first>
    - .planning/phases/03-system-audio-modules/03-VALIDATION.md
    - scripts/phase03-config-assert.py
    - All product files listed in context
  </read_first>
  <action>
    Run (and fix product code only if a gate fails — no scope expansion):

    1. `python3 scripts/phase03-config-assert.py` → must print `config asserts OK` exit 0

    2. Static resource gates:
       - Resources.qml: no `swap_horiz`; no `ResourcesPopup`; order shows CPU before memory before disk
       - Resource.qml: has `errorThreshold` and `labelText`
       - ResourceUsage.qml: has `diskUsedPercentage` (or equiv) and `df` Process; disk interval not 1s-only

    3. Static audio gates:
       - Audio.qml: `maxVolume` 1.30; auto-unmute (`muted = false` on volume paths); no sole `Math.min(1,` raise cap in incrementVolume
       - ScreenCorners: no Math.min(1, Audio.sink raise
       - hyprland.conf: XF86AudioRaiseVolume uses `-l 1.3`
       - BarContent: volume % + volumeMixer + toggleMute; right-bar incrementVolume/decrementVolume

    4. Prohibition gates:
       - `rg -n 'ddcutil|brightness' .config/quickshell/services/ResourceUsage.qml` empty or no new brightness poll
       - No new dedicated VolumeModule.qml under bar

    5. Smoke: `timeout 4 quickshell 2>&amp;1 | rg 'Configuration Loaded|Error'` must include Configuration Loaded

    Record pass/fail in SUMMARY. Prefer fixing failed product paths over weakening asserts.
  </action>
  <acceptance_criteria>
    - phase03-config-assert.py exits 0
    - All static rg gates listed above pass
    - Smoke shows Configuration Loaded
    - No ddcutil introduced in ResourceUsage
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/phase03-config-assert.py &amp;&amp; rg -n 'swap_horiz|ResourcesPopup' .config/quickshell/modules/ii/bar/Resources.qml; rg -n 'errorThreshold|labelText' .config/quickshell/modules/ii/bar/Resource.qml; rg -n 'diskUsedPercentage|df ' .config/quickshell/services/ResourceUsage.qml; rg -n 'maxVolume|muted = false' .config/quickshell/services/Audio.qml; rg -n 'volumeMixer|toggleMute|incrementVolume' .config/quickshell/modules/ii/bar/BarContent.qml; rg -n 'XF86AudioRaiseVolume' .config/hypr/hyprland.conf; timeout 4 quickshell 2&gt;&amp;1 | rg 'Configuration Loaded|Error' || true</automated>
  </verify>
  <done>Full automated suite green or product fixed to green.</done>
</task>

<task type="auto" id="03-08-02" tdd="false">
  <name>Task 2: Update VALIDATION.md Nyquist sign-off</name>
  <files>.planning/phases/03-system-audio-modules/03-VALIDATION.md</files>
  <read_first>
    - .planning/phases/03-system-audio-modules/03-VALIDATION.md
    - Task 1 gate results
  </read_first>
  <action>
    Update 03-VALIDATION.md only (unless task 1 required product fixes already done):

    1. Mark Per-Task Verification Map automated rows for 03-01..03-07 as ✅ green where gates prove them; leave Manual-Only Verifications as ⬜ pending human UAT.
    2. Check Validation Sign-Off boxes that are truly met by automation; leave human UAT boxes unchecked.
    3. Set frontmatter `nyquist_compliant: true` only if automated sampling continuity + Wave 0 + static/smoke suite all green. If any automated gate still fails, leave false and document gap.
    4. Set `status: draft` still until human UAT, or `validated` only if project convention allows without UAT — prefer keep draft + nyquist_compliant true for automated portion matching Phase 2 pattern if that phase did so.

    Do not mark manual UAT rows passed.
  </action>
  <acceptance_criteria>
    - VALIDATION.md has no TBD task IDs
    - wave_0_complete remains true
    - nyquist_compliant reflects automated suite result (true only if green)
    - Manual-Only table still documents human steps for BAR-05..08
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'nyquist_compliant|wave_0_complete|phase03-config-assert|Manual-Only|03-0[1-8]' .planning/phases/03-system-audio-modules/03-VALIDATION.md | head -50</automated>
  </verify>
  <done>VALIDATION.md records automated phase-gate outcome; ready for human UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Validation docs | Process integrity only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01..05 | various | phase product | high | mitigate | Suite re-checks dual-write, df interval, clamps, no tooltips, volumeMixer path |
| T-03-SC | Tampering | packages | high | accept | Gate forbids package installs |
</threat_model>

<verification>
- Full suite from task 1
- VALIDATION.md frontmatter updated honestly
</verification>

<success_criteria>
- Automated BAR-05..08 gates green
- Phase ready for `/gsd-verify-work` human UAT
</success_criteria>

<output>
Create `.planning/phases/03-system-audio-modules/03-08-SUMMARY.md` when done
</output>
