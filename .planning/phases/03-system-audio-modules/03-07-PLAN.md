---
phase: 03-system-audio-modules
plan: 07
type: execute
wave: 3
depends_on:
  - "03-04"
files_modified:
  - .config/quickshell/modules/ii/bar/BarContent.qml
autonomous: true
requirements:
  - BAR-08
must_haves:
  truths:
    - No dedicated volume module widget added (D-17) — mute/mic stay in indicators pill
    - Output unmuted shows volume icon + volume %; muted shows volume_off only (D-18)
    - Mic unmuted shows mic icon + input volume %; muted shows mic_off only (D-19)
    - Right-bar FocusedScrollMouseArea still calls Audio.incrementVolume/decrementVolume (D-20)
    - Left-click mute/mic toggles mute; middle and/or right opens volumeMixer (pavucontrol) (D-23, D-26)
    - No new volume OSD component (D-24)
  artifacts:
    - path: .config/quickshell/modules/ii/bar/BarContent.qml
      provides: "mute/mic icon+% + multi-button MouseArea + volumeMixer launch"
      contains: "volumeMixer"
  key_links:
    - from: mute/mic MouseArea
      to: Audio.toggleMute / toggleMicMute
      via: "left click"
      pattern: "toggleMute"
    - from: mute/mic MouseArea middle/right
      to: Config.options.apps.volumeMixer
      via: "Quickshell.execDetached bash -c"
      pattern: "volumeMixer"
    - from: barRightSideMouseArea
      to: Audio.incrementVolume / decrementVolume
      via: "onScrollUp/Down"
      pattern: "incrementVolume"
  prohibitions:
    - Do not add a standalone volume module in left/center sections
    - Do not remove right-bar volume scroll
    - Do not build a new OnScreenDisplay for volume
    - Do not interpolate untrusted bar text into shell commands (use Config.volumeMixer only)
---

<objective>
Enhance indicators-pill mute/mic for BAR-08: icon+percent when unmuted, icon-only when muted, multi-button clicks for mute vs pavucontrol, keep right-bar scroll and stock OSD (D-17..D-24, D-26).

Purpose: Indicators are icon-only left-click toggle; CONTEXT requires volume %, mic %, and mixer escape hatch without a dedicated module.
Output: BarContent.qml indicator rows updated only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-system-audio-modules/03-CONTEXT.md
@.planning/phases/03-system-audio-modules/03-RESEARCH.md
@.planning/phases/03-system-audio-modules/03-PATTERNS.md
@.config/quickshell/modules/ii/bar/BarContent.qml
@.config/quickshell/services/Audio.qml
@.config/quickshell/modules/waffle/actionCenter/volumeControl/VolumeControl.qml
@.config/quickshell/modules/common/Config.qml
</context>

## Artifacts this phase produces (this plan)

| Symbol | Kind | Path |
|--------|------|------|
| mute indicator Row (icon + optional %) | QML | BarContent.qml indicators |
| mic indicator Row (icon + optional %) | QML | BarContent.qml indicators |
| multi-button MouseArea acceptedButtons | QML | BarContent.qml |
| volumeMixer launch path | call | BarContent.qml |

## Decisions locked

- D-17 no dedicated module; D-18/D-19 icon+% rules
- D-20 keep right-bar scroll; D-23/D-26 click map; D-24 keep OSD

## Discretion

- Prefer PipeWire **volume level** (not VU peak) for mic % (CONTEXT agent discretion)
- Display percent as Math.round(volume * 100) clamped for UI; values above 100 allowed when boosted

<tasks>

<task type="auto" id="03-07-01" tdd="false">
  <name>Task 1: Mute and mic icon + percent display</name>
  <files>.config/quickshell/modules/ii/bar/BarContent.qml</files>
  <read_first>
    - .config/quickshell/modules/ii/bar/BarContent.qml (indicators RowLayout ~L240-260 and right FocusedScrollMouseArea)
    - .config/quickshell/services/Audio.qml (sink/source volume + muted)
    - .planning/phases/03-system-audio-modules/03-CONTEXT.md (D-17..D-19)
  </read_first>
  <action>
    Replace each bare MaterialSymbol mute/mic block with a small RowLayout (or Item) still inside indicatorsRowLayout, preserving D-19 order mute → mic → xkb → …:

    1. **Output (mute) indicator:**
       - MaterialSymbol: `volume_off` when Audio.sink.audio.muted else `volume_up` (or volume_down if project already branches — keep muted→volume_off)
       - StyledText (or existing bar text component) visible only when NOT muted: show `Math.round((Audio.sink?.audio?.volume ?? 0) * 100) + "%"`
       - When muted: icon only (D-18) — hide percent

    2. **Mic indicator:**
       - MaterialSymbol: `mic_off` when source muted else `mic`
       - Percent from source.audio.volume when unmuted only (D-19) — volume level not peak meter

    3. Match color to rightSidebarButton.colText; iconSize Appearance.font.pixelSize.larger; percent uses small pixelSize so pill does not explode.

    4. Do not add a new bar module outside the indicators pill (D-17).

    Keep right FocusedScrollMouseArea onScrollUp/Down → Audio.incrementVolume/decrementVolume unchanged (D-20).
  </action>
  <acceptance_criteria>
    - BarContent.qml mute indicator area references both volume_off and a percent expression involving Audio.sink
    - Mic indicator references mic_off and Audio.source volume percent when unmuted
    - Percent StyledText (or equivalent) gated on muted state (visible: !muted or text empty when muted)
    - barRightSideMouseArea still has onScrollUp: Audio.incrementVolume and onScrollDown: Audio.decrementVolume
    - No new file under modules/ii/bar named VolumeModule or similar
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'volume_off|volume_up|mic_off|Audio\.sink|Audio\.source|incrementVolume|decrementVolume|FocusedScrollMouseArea' .config/quickshell/modules/ii/bar/BarContent.qml</automated>
  </verify>
  <done>Mute/mic show icon+% unmuted and icon-only muted.</done>
</task>

<task type="auto" id="03-07-02" tdd="false">
  <name>Task 2: Multi-button clicks — toggle mute + open pavucontrol</name>
  <files>.config/quickshell/modules/ii/bar/BarContent.qml</files>
  <read_first>
    - .config/quickshell/modules/ii/bar/BarContent.qml (mute/mic after task 1)
    - .config/quickshell/modules/waffle/actionCenter/volumeControl/VolumeControl.qml (volumeMixer launch)
    - .config/quickshell/modules/common/Config.qml (apps.volumeMixer string)
    - .planning/phases/03-system-audio-modules/03-CONTEXT.md (D-23, D-26)
  </read_first>
  <action>
    On both mute and mic indicator interactive areas:

    1. Use MouseArea with `z: 10`, `anchors.fill` on the indicator row (icon+%), `acceptedButtons: Qt.LeftButton | Qt.MiddleButton | Qt.RightButton`.
    2. Left click: Audio.toggleMute() for output; Audio.toggleMicMute() for mic; `mouse.accepted = true` so parent RippleButton does not also fire sidebar toggle.
    3. Middle and/or right click: launch mixer via existing trusted path:
       `Quickshell.execDetached(["bash", "-c", Config.options.apps.volumeMixer]);`
       Do not invent a new shell string; do not change volumeMixer config value.
    4. Prevent wheel on indicator from breaking right-bar scroll habit — either leave wheel unhandled (propagate) or do not steal wheel; D-20 priority is whole right-bar scroll still works.
    5. Do not create a new OSD component (D-24).

    Smoke Configuration Loaded after edits.
  </action>
  <acceptance_criteria>
    - Mute MouseArea acceptedButtons includes MiddleButton and/or RightButton in addition to LeftButton
    - Source contains toggleMute and toggleMicMute
    - Source contains volumeMixer and execDetached (or Quickshell.execDetached) launch
    - Launch uses Config.options.apps.volumeMixer — not a hard-coded only "pavucontrol" without Config
    - No new OnScreenDisplay.qml file
    - `timeout 4 quickshell 2>&amp;1 | rg 'Configuration Loaded|Error'` shows Configuration Loaded
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'acceptedButtons|toggleMute|toggleMicMute|volumeMixer|execDetached|MiddleButton|RightButton' .config/quickshell/modules/ii/bar/BarContent.qml; timeout 4 quickshell 2&gt;&amp;1 | rg 'Configuration Loaded|Error' || true</automated>
  </verify>
  <done>Click map matches D-23/D-26; mixer opens via Config volumeMixer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config.apps.volumeMixer | User-controlled trusted config string (existing) |
| PipeWire volume display | Local session numbers only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Injection | volumeMixer launch | high | mitigate | Use only Config.options.apps.volumeMixer; no bar text interpolation |
| T-03-04 | Tampering | volume raise | medium | mitigate | Scroll still goes through Audio.incrementVolume (03-04 max 1.30) |
| T-03-SC | Tampering | packages | high | accept | No installs |
</threat_model>

<verification>
- Static gates for % visibility, scroll, volumeMixer clicks
- Smoke Configuration Loaded
</verification>

<success_criteria>
- BAR-08 display + scroll + mute click + mixer escape hatch complete on bar
- No dedicated volume module; no new OSD
</success_criteria>

<output>
Create `.planning/phases/03-system-audio-modules/03-07-SUMMARY.md` when done
</output>
