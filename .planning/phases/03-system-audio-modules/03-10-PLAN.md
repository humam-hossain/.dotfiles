---
phase: 03-system-audio-modules
plan: 10
type: execute
wave: 6
depends_on:
  - "03-08"
gap_closure: true
gap_ids:
  - G-03-4
  - G-03-8
files_modified:
  - .config/hypr/hyprland.conf
  - .config/quickshell/modules/common/Config.qml
  - .config/hypr/hyprland/scripts/launch_first_available.sh
autonomous: true
requirements:
  - BAR-08
must_haves:
  truths:
    - Keyboard XF86AudioRaiseVolume can raise sink volume to ~130% (wpctl -l 1.3) on the config the compositor loads
    - Middle/right mute/mic and sidebar Details open pavucontrol (or pavucontrol-qt if present)
  artifacts:
    - path: .config/hypr/hyprland.conf
      provides: "XF86AudioRaiseVolume -l 1.3 (already present; ensure live applies)"
    - path: .config/quickshell/modules/common/Config.qml
      provides: "working apps.volumeMixer command"
    - path: .config/hypr/hyprland/scripts/launch_first_available.sh
      provides: "optional first-available launcher used by volumeMixer"
  key_links:
    - from: Hyprland XF86AudioRaiseVolume
      to: wpctl set-volume -l 1.3
    - from: BarContent / VolumeDialog
      to: Config.options.apps.volumeMixer
  prohibitions:
    - Do not lower Audio.maxVolume below 1.30
    - Do not re-enable audio.protection unless required
    - Do not hardcode only pavucontrol-qt if pavucontrol is the installed binary
---

<objective>
Close UAT gaps G-03-4 (keyboard volume ceiling) and G-03-8 (pavucontrol never opens).

Purpose: Scroll already reaches 130% via Quickshell; keyboard is still capped at 100% by live Hyprland. Mixer launch points at a missing script.
Output: Live keyboard path respects 130%; volumeMixer launches a real mixer window from bar and sidebar.
</objective>

<context>
@.planning/phases/03-system-audio-modules/03-UAT.md
@.planning/debug/keyboard-volume-ceiling.md
@.planning/debug/pavucontrol-launch-broken.md
@.config/hypr/hyprland.conf
@.config/quickshell/modules/common/Config.qml
@.config/quickshell/modules/ii/bar/BarContent.qml
@.config/quickshell/modules/ii/sidebarRight/volumeMixer/VolumeDialog.qml
</context>

## Root causes (from diagnosis)

| Gap | Root cause |
|-----|------------|
| G-03-4 | Live `~/.config/hypr/hyprland.conf` has `wpctl -l 1`; repo has `-l 1.3` but files are not the same inode |
| G-03-8 | `apps.volumeMixer` → missing `launch_first_available.sh` (exit 127); `pavucontrol` is installed |

## Tasks

<tasks>

<task type="auto" id="03-10-01">
  <name>Task 1: Apply keyboard volume ceiling 130% to live Hyprland</name>
  <files>
    - .config/hypr/hyprland.conf
  </files>
  <read_first>
    - .config/hypr/hyprland.conf (XF86AudioRaiseVolume line ~418)
    - .planning/debug/keyboard-volume-ceiling.md
  </read_first>
  <action>
    1. Confirm repo bind is: `wpctl set-volume -l 1.3 @DEFAULT_AUDIO_SINK@ 5%+` (keep if already correct).
    2. Apply the same `-l 1.3` change to the **live** compositor config the session actually loads (`~/.config/hypr/hyprland.conf` if it is a separate file — do not assume stow/link). Prefer making live track repo (symlink/stow) **or** patch live in place if that is how this machine is managed; document which approach was used in SUMMARY.
    3. Reload Hyprland binds (`hyprctl reload` or equivalent) so the new limit is active without a full logout if possible.
  </action>
  <verify>
    - Live file contains `-l 1.3` on XF86AudioRaiseVolume
    - `wpctl get-volume` can exceed 1.0 after keyboard raise from ~100% (manual or scripted)
  </verify>
  <done>Keyboard path no longer hard-capped at 100%.</done>
</task>

<task type="auto" id="03-10-02">
  <name>Task 2: Fix volumeMixer launch (script + config dual-write)</name>
  <files>
    - .config/hypr/hyprland/scripts/launch_first_available.sh
    - .config/quickshell/modules/common/Config.qml
  </files>
  <read_first>
    - .config/quickshell/modules/common/Config.qml (apps.volumeMixer)
    - .config/quickshell/modules/ii/bar/BarContent.qml (middle/right handlers)
    - .config/quickshell/modules/ii/sidebarRight/volumeMixer/VolumeDialog.qml
    - .planning/debug/pavucontrol-launch-broken.md
  </read_first>
  <action>
    1. Create `.config/hypr/hyprland/scripts/launch_first_available.sh` that: takes one or more command names/paths as args; runs the first that exists on PATH (or is executable); exits non-zero if none found. Make it executable.
    2. Ensure Config default `apps.volumeMixer` still targets that script with `"pavucontrol-qt" "pavucontrol"` **or** switch to an equivalent one-liner that works when the script is present under the path bash expands (`~` is fine inside `bash -c`).
    3. Dual-write live `~/.config/illogical-impulse/config.json` `apps.volumeMixer` to match (same pattern as other Phase 3 config dual-writes).
    4. Smoke: `bash -c "$volumeMixer"` must open pavucontrol (or pavucontrol-qt) without exit 127.
  </action>
  <verify>
    - Script exists and is executable
    - `bash -c` with the configured volumeMixer string exits 0 / launches process
    - Config.qml and live config.json volumeMixer agree
  </verify>
  <done>Bar mute/mic middle-right and sidebar Details can launch the mixer.</done>
</task>

</tasks>

## Acceptance

- [ ] G-03-4: Keyboard volume can go past 100% up to ~130%
- [ ] G-03-8: Middle/right on mute/mic and sidebar Details open pavucontrol
