---
phase: 03-system-audio-modules
plan: 09
type: execute
wave: 6
depends_on:
  - "03-08"
gap_closure: true
gap_ids:
  - G-03-1
  - G-03-2
files_modified:
  - .config/quickshell/modules/common/Appearance.qml
  - .config/quickshell/modules/ii/bar/Resource.qml
  - .config/quickshell/services/ResourceUsage.qml
autonomous: true
requirements:
  - BAR-05
  - BAR-06
must_haves:
  truths:
    - Warning tier ring color is visually distinct from default and from error (D-07)
    - RAM/disk capacity labels use single unit suffix (e.g. 12.3/31.2 GB)
    - Ring↔label spacing for RAM/disk matches CPU visual rhythm
  artifacts:
    - path: .config/quickshell/modules/common/Appearance.qml
      provides: "colWarning (or equivalent distinct warning token)"
    - path: .config/quickshell/modules/ii/bar/Resource.qml
      provides: "isWarning → colWarning; tuned ring↔label spacing"
    - path: .config/quickshell/services/ResourceUsage.qml
      provides: "memoryUsedTotalString / diskFreeTotalString single-unit format"
  key_links:
    - from: Resource isWarning
      to: Appearance.colors.colWarning
      via: "ClippedFilledCircularProgress.colPrimary bind"
  prohibitions:
    - Do not remove dual-threshold logic or collapse to single error color
    - Do not reintroduce ResourcesPopup on hover
    - Do not change CPU default percent label format (still N%)
---

<objective>
Close UAT gaps G-03-1 (missing warning color) and G-03-2 (GB/GB labels + RAM/disk ring spacing).

Purpose: UAT confirmed error tier works but warning is invisible; capacity labels show double units and cramped ring spacing.
Output: Distinct warning ring color; `used/total UNIT` labels; CPU-matching ring↔text spacing for capacity modules.
</objective>

<context>
@.planning/phases/03-system-audio-modules/03-UAT.md
@.planning/debug/cpu-warning-color-missing.md
@.planning/debug/ram-label-spacing.md
@.config/quickshell/modules/ii/bar/Resource.qml
@.config/quickshell/services/ResourceUsage.qml
@.config/quickshell/modules/common/Appearance.qml
</context>

## Root causes (from diagnosis)

| Gap | Root cause |
|-----|------------|
| G-03-1 | `isWarning` uses `colPrimary` — not a perceptible warning color |
| G-03-2 | Double `formatBytes()` units; fixed `spacing: 2` too tight for long labels |

## Tasks

<tasks>

<task type="auto" id="03-09-01">
  <name>Task 1: Distinct warning color token + Resource bind</name>
  <files>
    - .config/quickshell/modules/common/Appearance.qml
    - .config/quickshell/modules/ii/bar/Resource.qml
  </files>
  <read_first>
    - .config/quickshell/modules/common/Appearance.qml (colors section)
    - .config/quickshell/modules/ii/bar/Resource.qml (colPrimary ladder L37–41)
    - .planning/debug/cpu-warning-color-missing.md
  </read_first>
  <action>
    1. Add `Appearance.colors.colWarning` as a **visually distinct** warm/warning color (not equal to colPrimary or colError). Prefer a clear amber/warm token that stays readable on the bar; if inventing a fixed hex, document why m3 tokens were insufficient.
    2. In `Resource.qml`, change the `isWarning` branch from `Appearance.colors.colPrimary` to `Appearance.colors.colWarning`.
    3. Keep error → `colError`, default → `colOnSecondaryContainer`.
  </action>
  <verify>
    - `rg colWarning Appearance.qml Resource.qml` shows definition + bind
    - Warning branch no longer references colPrimary for the ring ladder
  </verify>
  <done>Warning tier uses a dedicated, distinct color token.</done>
</task>

<task type="auto" id="03-09-02">
  <name>Task 2: Single-unit capacity labels</name>
  <files>
    - .config/quickshell/services/ResourceUsage.qml
  </files>
  <read_first>
    - .config/quickshell/services/ResourceUsage.qml (formatBytes, memoryUsedTotalString, diskFreeTotalString)
    - .planning/debug/ram-label-spacing.md
  </read_first>
  <action>
    Change `memoryUsedTotalString` and `diskFreeTotalString` so the unit appears **once** on the right, e.g. `12.3/31.2 GB` or `450.1/512.0 GB` (auto TB when either side needs TB per existing formatBytes rules). Implement a helper if needed (`formatPair(usedBytes, totalBytes)` / free-total pair) so both numbers share one unit.
  </action>
  <verify>
    - Strings no longer match pattern `GB/.*GB` or `TB/.*TB` as two separate unit suffixes
    - Helper or inline logic still uses auto human units (GB/TB)
  </verify>
  <done>Labels read as used/total UNIT (single suffix).</done>
</task>

<task type="auto" id="03-09-03">
  <name>Task 3: Ring↔label spacing for capacity modules</name>
  <files>
    - .config/quickshell/modules/ii/bar/Resource.qml
    - .config/quickshell/modules/ii/bar/Resources.qml
  </files>
  <read_first>
    - .config/quickshell/modules/ii/bar/Resource.qml (RowLayout spacing)
    - .config/quickshell/modules/ii/bar/Resources.qml (CPU vs RAM/Disk margins)
    - .planning/debug/ram-label-spacing.md
  </read_first>
  <action>
    Tune ring↔text spacing so RAM/disk match the CPU visual rhythm. Prefer a small property on Resource (e.g. larger spacing when `labelText` is non-empty) or explicit spacing that keeps inter-module gaps (CPU→RAM) as user-approved. Do not break CPU % layout.
  </action>
  <verify>
    - Resource/Resources still compile-structure intact
    - Capacity modules no longer forced to the same cramped spacing as before without intentional change
  </verify>
  <done>RAM/disk ring↔text spacing aligned with CPU feel.</done>
</task>

</tasks>

## Acceptance

- [ ] G-03-1: Warning band (CPU 40–79%) uses distinct non-red warning color
- [ ] G-03-2: Labels like `x/y GB` (not `x GB/y GB`); spacing fixed for RAM/disk
