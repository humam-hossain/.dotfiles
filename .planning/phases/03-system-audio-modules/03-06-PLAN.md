---
phase: 03-system-audio-modules
plan: 06
type: execute
wave: 4
depends_on:
  - "03-02"
  - "03-03"
  - "03-05"
files_modified:
  - .config/quickshell/modules/ii/bar/Resources.qml
autonomous: true
requirements:
  - BAR-05
  - BAR-06
  - BAR-07
must_haves:
  truths:
    - Resource strip order L→R is CPU → RAM → Disk (D-06, D-12)
    - Swap Resource is not rendered on the bar (D-04)
    - CPU and RAM always shown; no media/MPRIS hide (D-05)
    - CPU uses ring + percent label with %; RAM ring + used/total GB; Disk ring + free/total (D-02, D-03, D-10)
    - Dual thresholds wired from Config for CPU 40/80, RAM 75/95, Disk 80/95 (D-07, D-13)
    - ResourcesPopup not attached; no click/tooltip productization on strip (D-09, D-25)
  artifacts:
    - path: .config/quickshell/modules/ii/bar/Resources.qml
      provides: "CPU→RAM→Disk strip without swap/popup"
      contains: "diskUsedPercentage"
  key_links:
    - from: Resources.qml Resource children
      to: ResourceUsage + Config.bar.resources thresholds
      via: "percentage, labelText, warningThreshold, errorThreshold"
      pattern: "errorThreshold"
  prohibitions:
    - Do not keep swap_horiz Resource on the bar
    - Do not attach ResourcesPopup hoverTarget
    - Do not open btop/nautilus on click
    - Do not put disk in a separate bar region outside Resources cluster
---

<objective>
Productize `Resources.qml` strip for BAR-05..07: CPU→RAM→Disk order, dual-threshold rings, capacity labels, always-visible, no swap UI, no popup (D-01..D-16, D-25).

Purpose: Current strip is Memory→Swap→CPU with single thresholds, media-hide on CPU, and ResourcesPopup hover — fails CONTEXT and Waybar parity goals.
Output: Updated Resources.qml only (leave ResourcesPopup.qml file on disk unused).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-system-audio-modules/03-CONTEXT.md
@.planning/phases/03-system-audio-modules/03-RESEARCH.md
@.planning/phases/03-system-audio-modules/03-PATTERNS.md
@.config/quickshell/modules/ii/bar/Resources.qml
@.config/quickshell/modules/ii/bar/Resource.qml
@.config/quickshell/services/ResourceUsage.qml
</context>

## Artifacts this phase produces (this plan)

| Symbol | Kind | Path |
|--------|------|------|
| CPU Resource child | QML instance | Resources.qml |
| RAM Resource child | QML instance | Resources.qml |
| Disk Resource child | QML instance | Resources.qml |
| (removed) swap Resource | removal | Resources.qml |
| (removed) ResourcesPopup | removal | Resources.qml |

## Decisions locked

- D-01 rings; D-02 CPU %; D-03 RAM used/total; D-04 no swap UI
- D-05 always show; D-06/D-12 order; D-07/D-13 thresholds from Config
- D-09/D-16/D-25 no popup/tooltip/click

<tasks>

<task type="auto" id="03-06-01" tdd="false">
  <name>Task 1: Rebuild strip CPU→RAM→Disk with dual thresholds and labels</name>
  <files>.config/quickshell/modules/ii/bar/Resources.qml</files>
  <read_first>
    - .config/quickshell/modules/ii/bar/Resources.qml (full)
    - .config/quickshell/modules/ii/bar/Resource.qml (errorThreshold, labelText API from 03-03)
    - .config/quickshell/services/ResourceUsage.qml (disk/label helpers from 03-05)
    - .planning/phases/03-system-audio-modules/03-CONTEXT.md (D-01..D-16)
  </read_first>
  <action>
    Rewrite the Resource children inside Resources.qml RowLayout:

    1. **Order** exactly: CPU, then RAM, then Disk (D-06, D-12). Remove swap_horiz Resource entirely from bar UI (D-04).

    2. **CPU Resource:**
       - iconName: keep `planner_review` (or existing CPU icon)
       - percentage: ResourceUsage.cpuUsage
       - shown: true (D-05 — no MPRIS hide)
       - warningThreshold: Config.options.bar.resources.cpuWarningThreshold
       - errorThreshold: Config.options.bar.resources.cpuErrorThreshold
       - labelText: empty string so Resource default shows `N%` OR explicit string with % (D-02)

    3. **RAM Resource:**
       - iconName: `memory`
       - percentage: ResourceUsage.memoryUsedPercentage (ring = used%)
       - shown: true
       - warningThreshold / errorThreshold: memory* Config keys (75/95)
       - labelText: used/total GB string from ResourceUsage helper (D-03) — must not be bare percent

    4. **Disk Resource:**
       - iconName: choose Material disk/storage icon already used in ii if present (e.g. `hard_drive` / `storage`) — pick one existing MaterialSymbol name used elsewhere in project
       - percentage: ResourceUsage.diskUsedPercentage
       - shown: true
       - warningThreshold / errorThreshold: disk* Config keys (80/95) on used% (D-13)
       - labelText: free/total capacity string (D-10, D-15)

    5. Spacing: keep small Layout.leftMargin between shown items consistent with existing strip.

    Do not edit Resource.qml API here unless a compile break requires a one-line fix (prefer fix in 03-03).
  </action>
  <acceptance_criteria>
    - Resources.qml child Resource order in source is CPU then memory then disk (planner_review / memory / disk icon) — not memory-first
    - No `swap_horiz` Resource instance remains in Resources.qml
    - CPU Resource does not use MprisController media-hide shown expression
    - Each of three Resources sets errorThreshold from Config.options.bar.resources
    - RAM and Disk set non-empty labelText bindings (or property binds) for capacity strings
    - Disk percentage binds to ResourceUsage.diskUsedPercentage (or equivalent)
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'Resource \{|iconName:|errorThreshold|labelText|swap_horiz|diskUsedPercentage|MprisController|ResourcesPopup' .config/quickshell/modules/ii/bar/Resources.qml</automated>
  </verify>
  <done>Strip shows CPU→RAM→Disk with dual thresholds and capacity labels.</done>
</task>

<task type="auto" id="03-06-02" tdd="false">
  <name>Task 2: Disable ResourcesPopup and strip interactivity</name>
  <files>.config/quickshell/modules/ii/bar/Resources.qml</files>
  <read_first>
    - .config/quickshell/modules/ii/bar/Resources.qml (after task 1)
    - .planning/phases/03-system-audio-modules/03-CONTEXT.md (D-09, D-25, D-26)
  </read_first>
  <action>
    1. Remove `ResourcesPopup { hoverTarget: root }` instance from Resources.qml (D-09). Leave ResourcesPopup.qml file on disk for future detail phase.
    2. Disable popup affordance on root: set hoverEnabled false and/or convert root from MouseArea to Item if that is cleaner — result must be no hover popup and no click action on the strip (D-25).
    3. Do not wire btop, nautilus, or pavucontrol on resource strip (D-26 audio only on mute/mic).
    4. Smoke: Configuration Loaded.

    Optional: if vertical-bar Resources.qml twin exists and is loaded, apply same no-popup rule only if trivial; primary success surface is ii horizontal bar.
  </action>
  <acceptance_criteria>
    - `rg -n 'ResourcesPopup' .config/quickshell/modules/ii/bar/Resources.qml` returns no matches (or only comments)
    - Resources.qml has no onClicked that launches external apps
    - `timeout 4 quickshell 2>&amp;1 | rg 'Configuration Loaded|Error'` shows Configuration Loaded
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'ResourcesPopup|onClicked|hoverEnabled' .config/quickshell/modules/ii/bar/Resources.qml; timeout 4 quickshell 2&gt;&amp;1 | rg 'Configuration Loaded|Error' || true</automated>
  </verify>
  <done>Resource strip is display-only for Phase 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ResourceUsage metrics → UI labels | Numbers only; no shell in widget |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Info disclosure | disk labels | low | mitigate | free/total numbers only; no mount path in labelText |
| T-03-01 | Tampering | thresholds | medium | mitigate | Thresholds from dual-written Config |
| T-03-SC | Tampering | packages | high | accept | No installs |
</threat_model>

<verification>
- Static order / no swap / no ResourcesPopup / dual thresholds
- Smoke Configuration Loaded
</verification>

<success_criteria>
- BAR-05/06/07 bar surfaces match CONTEXT strip decisions
- No resource hover popup
</success_criteria>

<output>
Create `.planning/phases/03-system-audio-modules/03-06-SUMMARY.md` when done
</output>
