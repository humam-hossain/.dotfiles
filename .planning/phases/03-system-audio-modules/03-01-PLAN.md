---
phase: 03-system-audio-modules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase03-config-assert.py
  - .planning/phases/03-system-audio-modules/03-VALIDATION.md
autonomous: true
requirements:
  - BAR-05
  - BAR-06
  - BAR-07
  - BAR-08
must_haves:
  truths:
    - Wave 0 live-config assert script exists and runs with stdlib Python only
    - Assert covers all Phase 3 dual-write keys for resource thresholds, intervals, alwaysShowCpu, and audio.protection.maxAllowed
    - VALIDATION.md Wave 0 checkbox and per-task map reference concrete plan/task IDs (not TBD)
  artifacts:
    - path: scripts/phase03-config-assert.py
      provides: "Live illogical-impulse config.json asserts for BAR-05..08 keys"
      min_lines: 40
    - path: .planning/phases/03-system-audio-modules/03-VALIDATION.md
      provides: "Wave 0 wired with concrete task IDs"
  key_links:
    - from: scripts/phase03-config-assert.py
      to: ~/.config/illogical-impulse/config.json
      via: "Path.home() read + nested asserts"
      pattern: "illogical-impulse/config.json"
  prohibitions:
    - Do not introduce pytest/jest or any new test framework
    - Do not modify QML or live config.json in this plan (assert is intentionally red until 03-02)
    - Do not claim nyquist_compliant true yet
---

<objective>
Introduce Wave 0 Nyquist validation harness for Phase 3: `scripts/phase03-config-assert.py` plus VALIDATION.md wiring so later dual-write and productization plans can prove BAR-05..08 config keys without manual JSON dumps.

Purpose: Live `~/.config/illogical-impulse/config.json` shadows Config.qml — every threshold/interval/maxAllowed change needs a one-command assert (Phase 2 pattern; T-03-01).
Output: phase03-config-assert.py + updated 03-VALIDATION.md task map / Wave 0 checkboxes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-system-audio-modules/03-CONTEXT.md
@.planning/phases/03-system-audio-modules/03-RESEARCH.md
@.planning/phases/03-system-audio-modules/03-VALIDATION.md
@.planning/phases/03-system-audio-modules/03-PATTERNS.md
@scripts/phase02-config-assert.py
</context>

## Artifacts this phase produces (this plan)

| Symbol / artifact | Kind | Path |
|-------------------|------|------|
| `phase03-config-assert.py` | script | `scripts/phase03-config-assert.py` |
| `main()` exit codes 0/1 | API | assert script |
| Wave 0 task map rows | docs | `03-VALIDATION.md` |

## Decisions locked in this plan

- Dual-write assert keys per D-07, D-08, D-13, D-14, D-22 and VALIDATION Wave 0 list
- Split interval keys dual-written (planner choice): `resources.updateInterval=1000`, `resources.memoryUpdateInterval=3000`, `resources.diskUpdateInterval=10000`

## Assumptions (specless probe)

- BAR-05..08 edge cases covered by CONTEXT D-01..D-26 must_haves rather than SPEC.md
- Assert is red until plan 03-02 dual-writes live JSON (intentional)

<tasks>

<task type="auto" id="03-01-01">
  <name>Task 1: Create phase03 live config assert script</name>
  <files>scripts/phase03-config-assert.py</files>
  <read_first>
    - scripts/phase02-config-assert.py (structure to clone)
    - .planning/phases/03-system-audio-modules/03-VALIDATION.md (Wave 0 key list)
    - .planning/phases/03-system-audio-modules/03-CONTEXT.md (D-07, D-08, D-13, D-14, D-22)
    - .planning/phases/03-system-audio-modules/03-PATTERNS.md (phase03-config-assert.py analog)
  </read_first>
  <action>
    Create `scripts/phase03-config-assert.py` as a standalone Python 3 script (stdlib only: json, pathlib, sys). Clone structure from phase02 assert.

    Behavior:
    1. Load `Path.home() / ".config" / "illogical-impulse" / "config.json"`. On missing file, print clear error to stderr and exit 1.
    2. Assert live keys (exact values; fail loudly on missing keys):
       - `bar.resources.cpuWarningThreshold == 40` (D-07)
       - `bar.resources.cpuErrorThreshold == 80` (D-07)
       - `bar.resources.memoryWarningThreshold == 75` (D-07)
       - `bar.resources.memoryErrorThreshold == 95` (D-07)
       - `bar.resources.diskWarningThreshold == 80` (D-13)
       - `bar.resources.diskErrorThreshold == 95` (D-13)
       - `bar.resources.alwaysShowCpu is True` (D-05)
       - `bar.resources.alwaysShowSwap is False` (D-04 bar hide)
       - `resources.updateInterval == 1000` (D-08 CPU ~1s)
       - `resources.memoryUpdateInterval == 3000` (D-08 RAM ~3s)
       - `resources.diskUpdateInterval == 10000` (D-14 disk ~10s)
       - `audio.protection.maxAllowed >= 130` (D-22; allow exactly 130)
    3. On success print exactly one line `config asserts OK` and exit 0. On KeyError/AssertionError print `config assert FAIL: ...` to stderr and exit 1.
    4. Shebang `#!/usr/bin/env python3`; `chmod +x`.
    5. Do not soft-pass on current host values (cpuWarning 90, maxAllowed 99, interval 3000) — script is expected RED until plan 03-02.

    Do not edit QML, live config, or install packages.
  </action>
  <acceptance_criteria>
    - File `scripts/phase03-config-assert.py` exists and starts with `#!/usr/bin/env python3`
    - Script imports only stdlib (json, pathlib, sys at most)
    - Source contains path segment `illogical-impulse/config.json`
    - Source asserts cpuWarningThreshold 40, cpuErrorThreshold 80, memoryWarningThreshold 75, memoryErrorThreshold 95, diskWarningThreshold 80, diskErrorThreshold 95
    - Source asserts alwaysShowCpu True, alwaysShowSwap False
    - Source asserts updateInterval 1000, memoryUpdateInterval 3000, diskUpdateInterval 10000
    - Source asserts maxAllowed >= 130
    - `python3 -m py_compile scripts/phase03-config-assert.py` succeeds
    - `python3 scripts/phase03-config-assert.py` exits non-zero on current host config (pre-dual-write)
  </acceptance_criteria>
  <verify>
    <automated>test -f scripts/phase03-config-assert.py &amp;&amp; python3 -m py_compile scripts/phase03-config-assert.py &amp;&amp; (python3 scripts/phase03-config-assert.py; echo EXIT:$?)</automated>
  </verify>
  <done>Wave 0 assert script exists, compiles, encodes all Phase 3 live-config predicates, and is red until dual-write.</done>
</task>

<task type="auto" id="03-01-02">
  <name>Task 2: Wire Wave 0 into VALIDATION.md task map</name>
  <files>.planning/phases/03-system-audio-modules/03-VALIDATION.md</files>
  <read_first>
    - .planning/phases/03-system-audio-modules/03-VALIDATION.md
    - scripts/phase03-config-assert.py
  </read_first>
  <action>
    Update `03-VALIDATION.md` only:

    1. Mark Wave 0 requirement for `scripts/phase03-config-assert.py` checkbox complete and document command `python3 scripts/phase03-config-assert.py`.
    2. Replace TBD task IDs in Per-Task Verification Map with concrete IDs:
       - 03-01-01 / 03-01-02 → Wave 0 harness
       - 03-02-* → dual-write Config + live JSON
       - 03-03-* → Resource.qml dual thresholds / labels
       - 03-04-* → Audio 130% + auto-unmute + clamp paths
       - 03-05-* → ResourceUsage multi-rate + disk
       - 03-06-* → Resources.qml strip
       - 03-07-* → BarContent mute/mic
       - 03-08-* → static gates + nyquist sign-off
    3. Set frontmatter `wave_0_complete: true` when script path is documented. Leave `nyquist_compliant: false` and `status: draft` until plan 03-08.
    4. Keep Manual-Only Verifications and Threat Model tables; do not invent unit tests.
  </action>
  <acceptance_criteria>
    - VALIDATION.md Wave 0 checkbox for phase03-config-assert.py is marked done
    - No `TBD` remains in Task ID column of Per-Task Verification Map
    - Frontmatter has `wave_0_complete: true`
    - Frontmatter still has `nyquist_compliant: false`
    - Documented command includes `python3 scripts/phase03-config-assert.py`
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'phase03-config-assert|wave_0_complete|nyquist_compliant|03-0[1-8]' .planning/phases/03-system-audio-modules/03-VALIDATION.md | head -40</automated>
  </verify>
  <done>VALIDATION.md Wave 0 is wired to concrete plan/task IDs; assert path documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live config JSON | Untrusted-if-tampered thresholds/intervals/maxAllowed/volumeMixer; assert establishes known-good baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | live config thresholds / maxAllowed | high | mitigate | Assert script encodes dual-write targets; plan 03-02 dual-writes trusted defaults |
| T-03-02 | Injection | volumeMixer string | medium | accept | Not modified this plan; residual handled in 03-07 via existing Config path only |
| T-03-03 | DoS | df spam | medium | mitigate | Assert encodes diskUpdateInterval 10000 (D-14) |
| T-03-04 | Tampering | volume blast | high | mitigate | Assert maxAllowed &gt;= 130 floor for protection when later enabled |
| T-03-05 | Info disclosure | disk tooltips | low | accept | No product change this plan; D-16 enforced in 03-06 |
| T-03-SC | Tampering | package installs | high | accept | No npm/pip/cargo installs in this phase |
</threat_model>

<verification>
- `python3 -m py_compile scripts/phase03-config-assert.py`
- Assert exits non-zero on pre-dual-write host config
- VALIDATION.md references phase03-config-assert and concrete task IDs
</verification>

<success_criteria>
- Wave 0 harness exists and is runnable offline
- All Phase 3 dual-write predicates are encoded
- Later plans can depend on assert going green after dual-write
</success_criteria>

<output>
Create `.planning/phases/03-system-audio-modules/03-01-SUMMARY.md` when done
</output>
