---
id: 28-02
phase: 28-terminal-fuzzel-launcher-dynamic-palette
name: Fuzzel launcher theme integrity & Terminal theme configuration verification
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - scripts/phase28-terminal-fuzzel-assert.sh
autonomous: true
requirements: [TERM-01, TERM-02]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Live ~/.config/fuzzel/fuzzel_theme.ini exists, is non-empty, and contains a valid [colors] section (D-12, TERM-01)"
    - "All 7 required Fuzzel color tokens (background, text, selection, selection-text, border, match, selection-match) strictly match 8-digit hex format ^[0-9a-fA-F]{8}$ (D-12, D-13, TERM-01)"
    - "Fuzzel background alpha is ff (solid contrast) and border alpha is dd (D-13)"
    - "Headless dry-run of fuzzel --config ~/.config/fuzzel/fuzzel.ini -d -R < /dev/null exits cleanly without stderr error output (D-14, TERM-01)"
    - "Live ~/.local/state/quickshell/user/generated/terminal/kitty-theme.conf exists, is non-empty, and contains valid #RRGGBB hex tokens for ANSI color0-color15 and Starship prompt greys color232-color255 (D-11, TERM-02)"
    - "Native Kitty configuration parser probe (kitty +runpy) validates background_opacity 0.85, shell zsh, and window_margin_width 21.75 (D-01, D-02, D-09, TERM-02)"
    - "~/.config/illogical-impulse/config.json retains forceDarkMode: true under appearance.wallpaperTheming.terminalGenerationProps to guarantee dark backgrounds (D-10)"
    - "~/.config/illogical-impulse/config.json retains tuned harmony parameters harmony: 0.6 and termFgBoost: 0.35 (D-11)"
    - "~/.local/state/quickshell/user/generated/terminal/sequences.txt exists and contains OSC color escape sequences (D-06)"
    - "Sections 2 and 3 of scripts/phase28-terminal-fuzzel-assert.sh pass with FAIL=0 (TERM-01, TERM-02)"
  artifacts:
    - scripts/phase28-terminal-fuzzel-assert.sh
  key_links:
    - "fuzzel_theme.ini provides the dynamic palette tokens read on each Fuzzel invocation"
    - "kitty-theme.conf provides the dynamic color definitions included by kitty.conf"
    - "config.json enforces dark-mode policy and contrast harmony across dynamic theme compilations"
  prohibitions:
    - "Must not edit files under vendor/dots-hyprland/"
    - "Must not touch closed-phase assert scripts under scripts/"
    - "Must not hardcode color values in stow/kitty/.config/kitty/kitty.conf"
    - "Must not disable forceDarkMode in config.json"
    - "Must not spawn interactive GUI windows during test assertions"
---

<objective>
Implement and verify Section 2 (Fuzzel theme syntax, M3 color tokens, solid `ff` background alpha, `dd` border alpha, and headless dry-run execution covering TERM-01, D-13, D-14) and Section 3 (Kitty theme syntax, ANSI `color0`–`color15`, Starship grey overrides `color232`–`color255`, native Kitty option parser probe for opacity 0.85 and shell zsh, `config.json` `forceDarkMode: true` and harmony tuning, and `sequences.txt` presence covering TERM-02, D-01, D-02, D-08..D-11) in `scripts/phase28-terminal-fuzzel-assert.sh`.

Purpose: Guarantee that the dynamic theme outputs for Fuzzel and Kitty conform to expected syntax specifications, contrast policies, and application configuration constraints.
Output: Validated Section 2 and Section 3 in `scripts/phase28-terminal-fuzzel-assert.sh` passing with FAIL=0.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-CONTEXT.md
@.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-RESEARCH.md
@.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-PATTERNS.md
@.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-VALIDATION.md
@scripts/phase28-terminal-fuzzel-assert.sh
@stow/kitty/.config/kitty/kitty.conf
@stow/fuzzel/.config/fuzzel/fuzzel.ini
</context>

<assumptions>
- If fuzzel_theme.ini or kitty-theme.conf has not yet been generated on disk, executing switchwall.sh --noswitch populates them.
- Fuzzel parser dry-run (--config <path> -d -R < /dev/null) exercises syntax and theme parsing without launching a Wayland surface.
- Kitty option loading via python load_config evaluates config directives identically to the live C application.
</assumptions>

<tasks>

<task id="28-02-01" type="execute">
  <title>Task 1: Implement and verify Section 2 (Fuzzel Theme Syntax & Configuration Integrity)</title>
  <read_first>
    ~/.config/fuzzel/fuzzel_theme.ini
    stow/fuzzel/.config/fuzzel/fuzzel.ini
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-PATTERNS.md
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-RESEARCH.md
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-CONTEXT.md
  </read_first>
  <action>
    Implement Section 2 assertions in scripts/phase28-terminal-fuzzel-assert.sh and verify execution per D-13, D-14, and TERM-01:
    1. Check existence and non-empty size of $XDG_CONFIG_HOME/fuzzel/fuzzel_theme.ini.
    2. Implement programmatic INI token validation via python3 using configparser:
       - Verify [colors] section exists.
       - Assert all 7 required color tokens exist: background, text, selection, selection-text, border, match, selection-match.
       - Assert each token matches 8-digit hex pattern ^[0-9a-fA-F]{8}$.
       - Assert background alpha is ff (solid contrast) per D-13.
       - Assert border alpha is dd per D-13.
    3. Dry-run parser probe: invoke fuzzel --config "$LIVE_FUZZEL_INI" -d -R < /dev/null, capturing stderr and asserting zero configuration error output.
    4. Run bash scripts/phase28-terminal-fuzzel-assert.sh --section 2 and confirm FAIL=0.
  </action>
  <acceptance_criteria>
    bash scripts/phase28-terminal-fuzzel-assert.sh --section 2 exits 0 with FAIL=0.
    Python INI validation passes confirming all 7 color tokens match ^[0-9a-fA-F]{8}$ with ff background alpha and dd border alpha.
    fuzzel dry-run parser probe exits without emitting error strings to stderr.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase28-terminal-fuzzel-assert.sh --section 2</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
</task>

<task id="28-02-02" type="execute">
  <title>Task 2: Implement and verify Section 3 (Terminal Theme Syntax & Configuration Integrity)</title>
  <read_first>
    ~/.local/state/quickshell/user/generated/terminal/kitty-theme.conf
    stow/kitty/.config/kitty/kitty.conf
    ~/.config/illogical-impulse/config.json
    ~/.local/state/quickshell/user/generated/terminal/sequences.txt
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-PATTERNS.md
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-RESEARCH.md
    .planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-CONTEXT.md
  </read_first>
  <action>
    Implement Section 3 assertions in scripts/phase28-terminal-fuzzel-assert.sh and verify execution per D-01, D-02, D-08..D-11, and TERM-02:
    1. Assert $XDG_STATE_HOME/quickshell/user/generated/terminal/kitty-theme.conf exists and is non-empty.
    2. Implement programmatic color token validation via python3 for kitty-theme.conf:
       - Verify presence of background, foreground, cursor, selection_background, selection_foreground matching ^#[0-9a-fA-F]{6}$.
       - Verify presence of ANSI color0 through color15 matching ^#[0-9a-fA-F]{6}$.
       - Verify presence of Starship prompt greys color232 through color240 and color248 through color255 matching ^#[0-9a-fA-F]{6}$ per D-11.
    3. Native Kitty configuration parser probe: execute kitty +runpy loading $HOME/.config/kitty/kitty.conf with kitty.config.load_config, asserting background_opacity equals 0.85 (D-09), shell equals 'zsh' (D-02), window_margin_width equals 21.75 (D-01), and confirm_os_window_close equals 0 (D-01).
    4. In $XDG_CONFIG_HOME/illogical-impulse/config.json, assert .appearance.wallpaperTheming.terminalGenerationProps.forceDarkMode equals true (D-10), harmony equals 0.6 (D-11), and termFgBoost equals 0.35 (D-11).
    5. Assert $XDG_STATE_HOME/quickshell/user/generated/terminal/sequences.txt exists and is non-empty per D-06.
    6. Run bash scripts/phase28-terminal-fuzzel-assert.sh --section 3 and confirm FAIL=0.
    7. Run both Section 2 and Section 3 together and assert FAIL=0.
  </action>
  <acceptance_criteria>
    bash scripts/phase28-terminal-fuzzel-assert.sh --section 3 exits 0 with FAIL=0.
    bash scripts/phase28-terminal-fuzzel-assert.sh --section 2 && bash scripts/phase28-terminal-fuzzel-assert.sh --section 3 exits 0 with FAIL=0.
    kitty +runpy configuration loader verifies background_opacity 0.85, shell zsh, window_margin_width 21.75.
    jq inspection confirms forceDarkMode=true, harmony=0.6, termFgBoost=0.35 in config.json.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase28-terminal-fuzzel-assert.sh --section 2 && bash scripts/phase28-terminal-fuzzel-assert.sh --section 3</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python Script Execution (`kitty +runpy`) → Terminal Process Engine | Evaluating configuration options in a headless Python interpreter |
| Local Theme Files (`kitty-theme.conf`, `fuzzel_theme.ini`) → UI Launchers | Ingesting dynamically generated hex color strings into terminal and app launcher configs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-03 | Denial of Service / UI Render Failure via Corrupted Hex Tokens | Fuzzel & Kitty theme parser | high | mitigate | Validate all color tokens strictly against hex regex patterns (8-digit hex for Fuzzel, 6-digit hex for Kitty) before runtime acceptance (D-11, D-13). |
| T-28-04 | Information Disclosure / Shell Hijacking via Config Drift | Kitty shell configuration | medium | mitigate | Enforce opts.shell == 'zsh' assertion to prevent accidental adoption of upstream fish shell or unintended binary execution (D-02). |
</threat_model>

<verification>
Per-task automated checks:
1. `bash scripts/phase28-terminal-fuzzel-assert.sh --section 2`
2. `bash scripts/phase28-terminal-fuzzel-assert.sh --section 2 && bash scripts/phase28-terminal-fuzzel-assert.sh --section 3`
</verification>

<success_criteria>
1. `fuzzel_theme.ini` validated with 7 required M3 color tokens, solid `ff` background alpha, and `dd` border alpha.
2. `fuzzel --config ~/.config/fuzzel/fuzzel.ini -d -R < /dev/null` parses cleanly.
3. `kitty-theme.conf` validated with ANSI colors and Starship prompt greys.
4. Kitty configuration validated with opacity 0.85, shell zsh, and margin 21.75.
5. `config.json` confirms `forceDarkMode: true` and tuned harmony parameters.
6. `bash scripts/phase28-terminal-fuzzel-assert.sh --section 2 && bash scripts/phase28-terminal-fuzzel-assert.sh --section 3` passes with FAIL=0.
</success_criteria>

<output>
Create `.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-02-SUMMARY.md` when done
</output>

## Multi-Source Coverage Audit

| Requirement | REQUIREMENTS.md | ROADMAP.md | VALIDATION.md | Plan & Task | Status |
|---|---|---|---|---|---|
| **TERM-01** | Line 29 | Phase 28 Criterion 1 | Task 28-02-01 | Plan 28-02 Task 1 | Planned (Wave 2) |
| **TERM-02** | Line 30 | Phase 28 Criterion 2 | Task 28-02-02 | Plan 28-02 Task 2 | Planned (Wave 2) |

## Artifacts this phase produces
- Test assert harness: `scripts/phase28-terminal-fuzzel-assert.sh` (Sections 2 & 3 implemented)
- Plan summary: `.planning/phases/28-terminal-fuzzel-launcher-dynamic-palette/28-02-SUMMARY.md`
