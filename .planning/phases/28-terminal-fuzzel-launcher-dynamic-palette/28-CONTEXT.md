# Phase 28: Terminal & Fuzzel Launcher Dynamic Palette - Context

**Gathered:** 2026-09-17
**Status:** Ready for planning

<domain>
## Phase Boundary

Extend dynamic Material You styling to Fuzzel app launcher and terminal emulators:

1. **Fuzzel Launcher Theme Integration (TERM-01):** Fuzzel launcher sources `~/.config/fuzzel/fuzzel_theme.ini` generated by Matugen upon wallpaper change. Track `fuzzel.ini` in `stow/fuzzel/.config/fuzzel/fuzzel.ini` with upstream defaults (`radius=17`, `font=Google Sans Flex:weight=medium`, `prompt=">>  "`, and `terminal=kitty -1`), while `fuzzel_theme.ini` is dynamically rendered and guarded in `guard-paths.tsv`.
2. **Primary Terminal Dynamic Theming (TERM-02):** Primary terminal emulator (Kitty) adopts upstream `dots-hyprland` configuration baseline including `include ~/.local/state/quickshell/user/generated/terminal/kitty-theme.conf`, upstream margin (`window_margin_width 21.75`), upstream font (`JetBrains Mono Nerd Font` at `11.0`), and keybindings (smart `copy_or_interrupt` on `Ctrl+C`, interactive search kitten on `Ctrl+F` via claimed `search.py`/`scroll_mark.py`). Preserves personal cursor trail preferences and explicitly retains user's login shell `zsh` (omitting upstream `shell fish`).
3. **Terminal Opacity & Mode Policy (TERM-02):** Configure subtle 85% background opacity (`background_opacity 0.85` in `kitty.conf` and `term_alpha=85` for PTY sequences) for balanced readability and desktop blur. Enforce `forceDarkMode: true` in `~/.config/illogical-impulse/config.json` so terminals always maintain a dark background for eye comfort regardless of wallpaper mode, and retain tuned harmony/contrast boost parameters (`harmony: 0.6`, `termFgBoost: 0.35`, Starship prompt grey overrides `color232`–`color255`).
4. **Signaling & Secondary Terminals:** Upstream fail-soft reload mechanism: `switchwall.sh` triggers `applycolor.sh` in background to update `kitty-theme.conf` and dispatch non-blocking `kill -SIGUSR1 $(pidof kitty)` with `pgrep` guards. Broadcast universal OSC escape sequences (`apply_anyterm` to `/dev/pts/*`) for secondary terminals (Foot, Alacritty) without rewriting static config files.
5. **Test Harness & Strict Verification (INTG-01, INTG-02):** Author `scripts/phase28-terminal-fuzzel-assert.sh` exercising 5 automated fail-closed sections, verifying template readiness, Fuzzel/Kitty theme syntax, live mtime advancement during `switchwall.sh --noswitch`, dual-mode SIGUSR1 / syntax validation, and strict `arch/dots-hyprland.sh verify --strict` zero findings with 100% clean working tree.

Out of scope:
- Reconciling `guard-paths.tsv` and `collision-map.tsv` system-wide contracts (Phase 29 owns this).
- Bootstrap integration testing and fresh-system smoke test (Phase 29 owns this).
- Custom Waybar widget ports (v2 requirements).
- Rewriting static configuration files for secondary terminals (Alacritty/Foot).

</domain>

<decisions>
## Implementation Decisions

### Primary Terminal & Reload Strategy (TERM-02)

- **D-01:** Kitty configuration baseline: Adopt upstream `dots-hyprland` `kitty.conf` layout in `stow/kitty/.config/kitty/kitty.conf`. Include `~/.local/state/quickshell/user/generated/terminal/kitty-theme.conf`, set `window_margin_width 21.75`, set `confirm_os_window_close 0`, adopt `font_family JetBrains Mono Nerd Font` at `11.0`, and preserve personal cursor trail settings (`cursor_trail 3`, `cursor_trail_decay 0.1 0.4`, `cursor_trail_start_threshold 2`). — **Reversibility:** costly — changes core terminal configuration and affects all terminal windows.
- **D-02:** Shell selection: Explicitly keep `zsh` as the terminal shell in `stow/kitty/.config/kitty/kitty.conf` (omit upstream's `shell fish`), honoring user's existing login shell environment.
- **D-03:** Upstream helper kittens: Copy upstream `search.py` and `scroll_mark.py` into `stow/kitty/.config/kitty/` so `Ctrl+F` search kitten works out-of-the-box and `arch/dots-hyprland.sh verify --strict` claims them as managed files rather than unclaimed stubs.
- **D-04:** Single-instance mode: Kitty is invoked in single-instance mode (`kitty -1`) across Hyprland binds, Quickshell app launchers, and Fuzzel configuration for faster launches and shared daemon memory.
- **D-05:** Process signaling: Retain upstream fail-soft signaling in `applycolor.sh`: `kill -SIGUSR1 $(pidof kitty)` guarded by `pgrep -f kitty`. Wallpaper switching remains non-blocking whether Kitty is running or closed, and fresh instances read the updated theme from disk on startup.
- **D-06:** Secondary terminals: Use universal OSC escape sequences (`apply_anyterm` broadcasting to `/dev/pts/*`) for secondary terminals (Foot, Alacritty) without rewriting their static configuration files.
- **D-07:** Cold-start seed: Ensure `kitty-theme.conf` is seeded or generated via `switchwall.sh --noswitch` if missing, so Kitty never logs a missing include error on clean setups.
- **D-08:** Upstream shortcuts: Retain all upstream shortcuts in `kitty.conf` including smart `copy_or_interrupt` on `Ctrl+C`, interactive search kitten on `Ctrl+F` / `kitty_mod+F`, and zoom/scroll navigation bindings.

### Terminal Opacity & Color Mode (TERM-02)

- **D-09:** Background opacity: Configure subtle 85% opacity (`background_opacity 0.85`) directly in `stow/kitty/.config/kitty/kitty.conf` and `term_alpha=85` for PTY escape sequences, providing balanced desktop wallpaper blur while preserving high text contrast and legibility.
- **D-10:** Dark mode enforcement: Keep `forceDarkMode: true` in `~/.config/illogical-impulse/config.json` under `appearance.wallpaperTheming.terminalGenerationProps`, ensuring terminals always generate dark backgrounds for eye comfort even if a light desktop theme is selected.
- **D-11:** Contrast and harmony tuning: Retain upstream tuned parameters (`harmony: 0.6`, `harmonizeThreshold: 100`, `termFgBoost: 0.35`) in `config.json` and Starship prompt grey color overrides (`color232`–`color255`) in `kitty-theme.conf` to guarantee sharp text contrast and harmonious prompt styling.

### Fuzzel Launcher Theming & Integration (TERM-01)

- **D-12:** Dynamic palette generation: Fuzzel sources `~/.config/fuzzel/fuzzel_theme.ini` generated by Matugen from wallpaper colors via `~/.config/matugen/templates/fuzzel/fuzzel_theme.ini`.
- **D-13:** Fuzzel opacity & contrast: Retain upstream solid background (`ff` alpha) and `dd` alpha border in `fuzzel_theme.ini` for maximum legibility and crisp search contrast.
- **D-14:** Fuzzel geometry: Retain upstream defaults in `fuzzel.ini` (`radius=17` squircle matching Hyprland's 18px rounding, `width=1`, `prompt=">>  "`, font `Google Sans Flex:weight=medium`, `layer=overlay`, and `exit-immediately-if-empty=yes` under `[dmenu]`).
- **D-15:** Terminal invocation: Configure `terminal=kitty -1` in `fuzzel.ini`, matching the Area 1 single-instance Kitty choice.
- **D-16:** Repository tracking: Track `fuzzel.ini` in `stow/fuzzel/.config/fuzzel/fuzzel.ini` symlinked to `~/.config/fuzzel/fuzzel.ini`. `fuzzel_theme.ini` remains dynamically generated and guarded against git churn by `guard-paths.tsv` line 18.

### Test Suite & Verification Criteria (INTG-01, INTG-02)

- **D-17:** 5-section automated test suite: Author `scripts/phase28-terminal-fuzzel-assert.sh` exercising 5 fail-closed sections:
  1. Template & config readiness: verify Matugen fuzzel template, `stow/kitty/kitty.conf` include statement, `fuzzel.ini` include statement, and `guard-paths.tsv` protection.
  2. Fuzzel theme syntax: assert `fuzzel_theme.ini` contains valid INI syntax and non-empty `[colors]` section with required 8-digit hex tokens (`background`, `text`, `match`, `selection`, `border`).
  3. Terminal theme syntax: assert `kitty-theme.conf` contains valid ANSI tokens (`color0`–`color15`, foreground, background) and Starship overrides (`color232`–`color255`), and verify `background_opacity 0.85` in `kitty.conf`.
  4. Live reload drill: execute `switchwall.sh --noswitch`, assert mtime advancement on `fuzzel_theme.ini` and `kitty-theme.conf`, test dual-mode Kitty SIGUSR1 dispatch (if running) or config dry-run syntax (if headless), and assert PTY sequence generation.
  5. Strict verification & zero git drift: assert `arch/dots-hyprland.sh verify --strict` reports `FAIL=0 FINDINGS=0` and `git status --porcelain` is 100% clean.
- **D-18:** Dual-mode process probe: Live `SIGUSR1` signal dispatched to running Kitty instances, with graceful fallback to syntax and configuration validation in headless/closed environments.
- **D-19:** Strict zero git drift gate: Running the test harness or changing wallpapers must produce zero untracked files, zero modified files, and zero findings in `verify --strict`.

### the agent's Discretion

- Minor formatting of comments and section headers in `stow/kitty/.config/kitty/kitty.conf` and `stow/fuzzel/.config/fuzzel/fuzzel.ini`.
- Internal assertion organization and progress output in `scripts/phase28-terminal-fuzzel-assert.sh`.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Roadmap and Requirements
- `.planning/ROADMAP.md` §Phase 28 — goal statement, requirements, success criteria
- `.planning/REQUIREMENTS.md` §TERM — TERM-01, TERM-02 specifications
- `.planning/STATE.md` §Milestone v0.5 — current state and accumulated decisions

### Data Contracts and Guard Status
- `guard-paths.tsv` line 18 — `$XDG_CONFIG_HOME/fuzzel/fuzzel_theme.ini` guarded as `generated_theme`
- `collision-map.tsv` lines 5, 8 — `$XDG_CONFIG_HOME/fuzzel` and `$XDG_CONFIG_HOME/kitty` tracked as restow
- `~/.config/illogical-impulse/config.json` lines 42–52 — `appearance.wallpaperTheming.enableTerminal` and `terminalGenerationProps`

### Upstream and Repository Terminal Configs
- `vendor/dots-hyprland/dots/.config/kitty/kitty.conf` — upstream Kitty configuration baseline
- `vendor/dots-hyprland/dots/.config/kitty/search.py` — upstream interactive search kitten
- `vendor/dots-hyprland/dots/.config/kitty/scroll_mark.py` — upstream scroll mark helper
- `stow/kitty/.config/kitty/kitty.conf` — repository Kitty configuration
- `vendor/dots-hyprland/dots/.config/foot/foot.ini` — upstream Foot configuration
- `stow/alacritty/.config/alacritty/alacritty.toml` — repository Alacritty configuration

### Upstream and Repository Fuzzel Configs
- `vendor/dots-hyprland/dots/.config/fuzzel/fuzzel.ini` — upstream Fuzzel configuration
- `vendor/dots-hyprland/dots/.config/fuzzel/fuzzel_theme.ini` — upstream default Fuzzel palette
- `vendor/dots-hyprland/dots/.config/matugen/templates/fuzzel/fuzzel_theme.ini` — Matugen Fuzzel template
- `~/.config/matugen/config.toml` lines 16–18 — `[templates.fuzzel]` mapping
- `~/.config/fuzzel/fuzzel.ini` — live Fuzzel configuration
- `~/.config/fuzzel/fuzzel_theme.ini` — live generated Fuzzel palette
- `~/.config/hypr/hyprland/scripts/fuzzel-emoji.sh` — emoji selector script invoked with Fuzzel

### Upstream Dynamic Reload Pipeline
- `vendor/dots-hyprland/dots/.config/quickshell/ii/scripts/colors/switchwall.sh` lines 295–315 — wallpaper pipeline invoking Matugen, `generate_colors_material.py`, and `applycolor.sh`
- `vendor/dots-hyprland/dots/.config/quickshell/ii/scripts/colors/applycolor.sh` lines 30–80 — `apply_kitty`, `apply_anyterm`, and `apply_term`
- `vendor/dots-hyprland/dots/.config/quickshell/ii/scripts/colors/terminal/kitty-theme.conf` — Kitty theme template
- `vendor/dots-hyprland/dots/.config/quickshell/ii/scripts/colors/terminal/sequences.txt` — universal terminal escape sequence template
- `~/.local/state/quickshell/user/generated/terminal/kitty-theme.conf` — live generated Kitty theme
- `~/.local/state/quickshell/user/generated/terminal/sequences.txt` — live generated terminal escape sequences

### Verification Engine & Prior Test Harnesses
- `arch/dots-hyprland.sh` lines 1120–1385 — `run_verify`, guard path checks, and live sweep
- `scripts/phase25-gtk-material-you-assert.sh` — Phase 25 model test harness
- `scripts/phase26-qt-kde-material-you-assert.sh` — Phase 26 model test harness
- `scripts/phase27-accent-coordination-assert.sh` — Phase 27 model test harness

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `~/.config/quickshell/ii/scripts/colors/switchwall.sh`: Established orchestrator for wallpaper changes and palette generation.
- `~/.config/quickshell/ii/scripts/colors/applycolor.sh`: Sub-pipeline that populates `kitty-theme.conf` and dispatches `SIGUSR1` to Kitty and OSC sequences to `/dev/pts/*`.
- `~/.config/matugen/config.toml`: Already contains configured mapping for `[templates.fuzzel]` writing to `~/.config/fuzzel/fuzzel_theme.ini`.
- `arch/dots-hyprland.sh verify --strict`: Verification engine reporting 0 findings on clean state.

### Established Patterns
- **Purity of Upstream Defaults:** Follow upstream `dots-hyprland` templates and config files as baseline authority.
- **Fail-Soft Reloading:** Component-level reloader failures (e.g. Kitty signal when not running) fail soft without aborting the wallpaper transition or desktop shell.
- **Zero Drift Invariant:** Repository working tree must remain 100% clean (`git status --porcelain` empty); all dynamically generated files are guarded in `guard-paths.tsv` or stored in `$XDG_STATE_HOME`.
- **Restow / Stow Parity:** All files under `stow/` are symlinked into `$HOME` and verified by `arch/dots-hyprland.sh verify --strict`.

### Integration Points
- `~/.config/matugen/config.toml` -> Generates `fuzzel_theme.ini` (consumed by Fuzzel).
- `switchwall.sh` -> Invokes `matugen` and `applycolor.sh`.
- `applycolor.sh` -> Writes `kitty-theme.conf` (consumed by Kitty) and broadcasts `sequences.txt` to `/dev/pts/*`.
- `stow/kitty/.config/kitty/kitty.conf` -> Includes `kitty-theme.conf` and applies `background_opacity 0.85`.
- `stow/fuzzel/.config/fuzzel/fuzzel.ini` -> Includes `fuzzel_theme.ini` and sets `terminal=kitty -1`.
- `guard-paths.tsv` -> Guards `fuzzel_theme.ini` from repository churn.

</code_context>

<specifics>
## Specific Ideas

- **Shell Preference:** Keep user's default login shell `zsh` in `kitty.conf`, explicitly omitting upstream's `shell fish` directive.
- **Subtle Opacity:** Set `background_opacity 0.85` natively in `kitty.conf` to provide subtle transparency without modifying upstream scripts.
- **Claim Upstream Kitty Kittens:** Stowing `search.py` and `scroll_mark.py` in `stow/kitty/.config/kitty/` claims them cleanly in `arch/dots-hyprland.sh verify --strict` instead of leaving them as unclaimed stubs.
- **Single Instance Kitty:** Ensure `terminal=kitty -1` in `fuzzel.ini` and Hyprland launch scripts for fast launches and memory reuse.

</specifics>

<deferred>
## Deferred Ideas

- Phase 29: Theme data contracts reconciliation (`guard-paths.tsv`, `collision-map.tsv`) and `./bootstrap.sh` full integration run.
- v2 Milestone: Waybar custom widget ports (ping, weather, earthquake).

</deferred>

---

*Phase: 28-terminal-fuzzel-launcher-dynamic-palette*
*Context gathered: 2026-09-17*
