# Phase 7: Validate Keymap Requirements

**Goal:** Verify KEY-01, KEY-02, KEY-03 are satisfied; fix gaps found during fresh scan

**Requirements:** KEY-01, KEY-02, KEY-03

**Gap Closure:** Closes partial requirement status for Phase 2 keymaps

---

## Context

Phase 2 plans (02-01, 02-02, 02-03) established the central keymap registry. Fresh code scan reveals:
- 02-VERIFICATION.md exists with PASS for KEY-01/02/03
- **Gap**: `snacks.lua` lacks `keys={}` — ~16 search/codelsp picker keymaps dead
- **Gap**: `lsp.lua:121` has duplicate `<leader>th>` direct call vs registry

## Tasks

### 7.1: Wire Snacks Picker Keys

**File:** `.config/nvim/lua/plugins/snacks.lua`

Add `keys = {}` to register all search/codelsp picker keys via lazy.nvim:

```lua
return {
  "folke/snacks.nvim",
  priority = 1000,
  lazy = false,
  -- Existing opts...
  keys = function()
    return require("core.keymaps.lazy").get_all_keys()
  end,
}
```

This wires: `<leader>ff/fg/fc/fh/fk/fb/fw/fW/fd/fr/fo/<leader><leader>`, `<leader>/`, `<leader>gg`, `<leader>gp/gt`, `<leader>lw`.

### 7.2: Remove Duplicate `<leader>th` from LSP

**File:** `.config/nvim/lua/plugins/lsp.lua`

Remove lines 120-124 (the direct `vim.keymap.set("n", "<leader>th", ...)` call inside the `LspAttach` autocmd callback).

The registry's `lsp.toggle_inlay` (`id: "lsp.toggle_inlay"`, `scope: "buffer"`, `attach: "LspAttach"`) is applied via `attach.apply_lsp(event.buf)` at line 95 — this is the correct source.

### 7.3: Update 02-VERIFICATION.md

**File:** `.planning/milestones/v1.0-phases/02-central-command-and-keymap-architecture/02-VERIFICATION.md`

Update evidence with current state:
- KEY-01: Add evidence about snacks.lua `keys = {}` wiring
- KEY-03: Add evidence about `<leader>th` duplicate removal

### 7.4: Run Health Check

Run `nvim-validate.sh all` and confirm PASS.

---

## Deliverables

- `snacks.lua` updated with `keys = {}` from registry
- `lsp.lua` duplicate `<leader>th>` removed
- `02-VERIFICATION.md` updated with fresh evidence
- Health check PASS
