---
id: 01-03
phase: 1
name: Integration and Service Singleton Verification
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: [".config/quickshell/services/*"]
autonomous: true
requirements: ["FWK-05"]
---

<objective>
Verify that the service-singleton pattern works by running Quickshell, ensuring the bar renders on all monitors, and fixing any hard errors that prevent startup.
</objective>

## Tasks

<task id="01-03-01">
<title>Execute and Fix Hard Startup Errors</title>
<read_first>
- .config/quickshell/shell.qml
- .config/quickshell/panelFamilies/IllogicalImpulseFamily.qml
- ../dots-hyprland/dots/.config/quickshell/ii/shell.qml
- ../dots-hyprland/dots/.config/quickshell/ii/panelFamilies/IllogicalImpulseFamily.qml
</read_first>
<action>
1. Run `arch/quickshell.sh` to provision the system (installs dependencies, sets up symlinks, generates colors).
2. Launch quickshell directly in a terminal using `quickshell`.
3. Observe the output and fix ONLY hard errors that cause Quickshell to crash or fail to launch (e.g., missing dependencies, invalid imports).
4. Do NOT modify or remove dead services (like AI, Booru, SongRec) unless they emit fatal errors that prevent `shell.qml` from loading.
</action>
<acceptance_criteria>
- `quickshell` runs without crashing.
- A visible bar (top panel) renders on every connected monitor.
- At least one service singleton (e.g. `MaterialThemeLoader` or `Updates`) initializes without a hard crash.
</acceptance_criteria>
<verify>
# Run manually to verify visible bars on all monitors without immediate crash
timeout 2 quickshell || true
</verify>
</task>

## must_haves

truths:
  - Service singletons are ported verbatim and preserved unless they cause hard startup crashes.
  - Quickshell successfully launches and renders the `IllogicalImpulseFamily` panel on all monitors.
prohibitions:
  - Do NOT poll ddcutil (causes iGPU crashes).
  - Do NOT modify dots-hyprland source code (the `../dots-hyprland/` directory).
  - Do NOT remove dead services just because they have no backend yet.

## Artifacts this phase produces

- Verifiable running instance of Quickshell rendering top bars on DP-1 and HDMI-A-2.
- Minor fixes in `.config/quickshell/services/` if any hard crashes are found (e.g., removing missing component instantiations).
