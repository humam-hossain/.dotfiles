---
id: 01-04
phase: 1
name: Gap closure — bar visual fonts and QML defects
wave: 1
depends_on: ["01-03"]
gap_closure: true
gap_ids: [G-01-1]
files_modified:
  - arch/fonts.sh
  - arch/quickshell.sh
  - .config/quickshell/modules/common/Config.qml
  - .config/quickshell/modules/common/Appearance.qml
  - .config/quickshell/modules/ii/bar/BarContent.qml
  - .config/quickshell/modules/ii/bar/Workspaces.qml
  - .config/quickshell/modules/ii/notificationPopup/NotificationPopup.qml
  - .config/quickshell/modules/settings/InterfaceConfig.qml
autonomous: true
requirements: ["FWK-01", "THM-02"]
---

<objective>
Close UAT gap G-01-1: bar is visible but unusable visually (missing icons, overlapping text, inconsistent design) and logs QML/Hyprland errors. Provision required fonts, fix layout/config key bugs, and restore stock workspace dispatch so the bar is visually coherent and interactive.
</objective>

## Context

Diagnosed in `.planning/debug/bar-visual-icons-layout.md` after `/gsd-verify-work 1`.

User confirmed Material colors apply (Test 2 pass). Gap is **usability/visual quality of the bar chrome**, not theme pipeline.

## Tasks

<task id="01-04-01">
<title>Provision Material Symbols and font fallbacks</title>
<read_first>
- arch/fonts.sh
- arch/quickshell.sh
- .config/quickshell/modules/common/Config.qml (appearance.fonts)
- .config/quickshell/modules/common/Appearance.qml (font.family.iconMaterial)
- .planning/debug/bar-visual-icons-layout.md
</read_first>
<action>
1. Add `ttf-material-symbols-variable` to package install path used for this machine (`arch/fonts.sh` and/or `arch/quickshell.sh` PACKAGES). Prefer official `extra/ttf-material-symbols-variable` (provides Material Symbols Rounded).
2. After install, verify with `fc-list | rg -i 'Material Symbols'`.
3. Update `Config.options.appearance.fonts` defaults to families that exist on this host when Google Sans Flex / Readex Pro / Space Grotesk are unavailable:
   - main / numbers / title → `Noto Sans` (or another installed sans if preferred)
   - iconNerd / monospace → actual JetBrains family name from `fc-list` (e.g. `JetBrainsMono Nerd Font`)
   - reading / expressive → same sans fallback unless packages are added
4. Keep `Appearance.font.family.iconMaterial` as `"Material Symbols Rounded"` once the package is installed.
5. Document in script comments that ii shell requires Material Symbols for chrome icons.
</action>
<acceptance_criteria>
- `fc-list` reports a Material Symbols Rounded (or variable Material Symbols) family.
- Config default font families resolve via `fc-list` (count ≥ 1 each).
- Restart quickshell: MaterialSymbol icons in the bar render as glyphs (not empty boxes); body text uses a real font with stable metrics.
</acceptance_criteria>
<verify>
fc-list : family | rg -i 'Material Symbols' | head -5
fc-list : family | rg -F "$(rg -o 'property string main: \"[^\"]+\"' .config/quickshell/modules/common/Config.qml | head -1 | sed 's/.*"\(.*\)"/\1/')" | head -3
</verify>
</task>

<task id="01-04-02">
<title>Fix BarContent padding and Workspaces dispatch</title>
<read_first>
- .config/quickshell/modules/ii/bar/BarContent.qml (around middleCenterGroup / padding)
- .config/quickshell/modules/ii/bar/Workspaces.qml (properties + switchWorkspaceToHovered / onWheel)
- .config/quickshell/modules/ii/bar/BarGroup.qml
</read_first>
<action>
1. Add `property real widgetPadding: 0` (or a sensible default matching BarGroup’s visual intent, e.g. 5) on the Workspaces root so `BarContent`’s `padding: workspacesWidget.widgetPadding` is a real number.
2. Replace invalid Hyprland dispatches:
   - `hl.dsp.focus({workspace = N})` → stock `workspace ${N}` (or `workspace r±1` for wheel)
   - Apply to click handler and wheel handler in Workspaces.qml
3. Do not introduce a Hyprland plugin dependency for Phase 1 gap closure.
</action>
<acceptance_criteria>
- Quickshell log no longer shows `Unable to assign [undefined] to double` for BarContent padding.
- Clicking a workspace indicator switches workspace without `Invalid dispatcher` for `hl.dsp.focus`.
- Wheel over workspaces cycles workspaces with stock dispatcher.
</acceptance_criteria>
<verify>
# After restart: logs must not contain hl.dsp.focus Invalid dispatcher on workspace click
rg -n "widgetPadding|hl\.dsp\.focus|workspace " .config/quickshell/modules/ii/bar/Workspaces.qml | head -20
</verify>
</task>

<task id="01-04-03">
<title>Align notifications monitor Config key and quiet theme token warnings</title>
<read_first>
- .config/quickshell/modules/common/Config.qml (notifications block)
- .config/quickshell/modules/ii/notificationPopup/NotificationPopup.qml
- .config/quickshell/modules/settings/InterfaceConfig.qml (forceMonitor UI)
- .config/quickshell/modules/common/Appearance.qml (m3colors properties)
- ~/.local/state/quickshell/user/generated/colors.json (keys with _dim)
</read_first>
<action>
1. Pick one API and make Config + consumers consistent:
   - Preferred: rename consumer references from `forceMonitor` → `monitor` to match Config.qml (`notifications.monitor.enable` / `.name`), **or** add `forceMonitor` alias object in Config that mirrors `monitor`.
   - Update NotificationPopup.qml and InterfaceConfig.qml accordingly.
2. On Appearance.m3colors, add missing color properties emitted by generate_theme / colors.json that currently warn:
   - at minimum `m3primaryDim`, `m3secondaryDim`, `m3tertiaryDim`, `m3errorDim`
   - optional palette key colors if assigned at runtime
   Use reasonable greyscale/default placeholders matching neighboring tokens.
3. Do not change MaterialThemeLoader’s assign loop unless needed; properties should exist so assignment succeeds.
</action>
<acceptance_criteria>
- No `Cannot read property 'enable' of undefined` from NotificationPopup on load.
- No `Cannot assign to non-existent property "m3primaryDim"` (and sibling *Dim) after theme reapply.
- Material colors still apply (regression: Test 2 still true).
</acceptance_criteria>
<verify>
rg -n "forceMonitor|notifications\.monitor" .config/quickshell/modules/ii/notificationPopup/NotificationPopup.qml .config/quickshell/modules/settings/InterfaceConfig.qml .config/quickshell/modules/common/Config.qml
rg -n "m3primaryDim|m3secondaryDim|m3tertiaryDim|m3errorDim" .config/quickshell/modules/common/Appearance.qml
</verify>
</task>

## must_haves

truths:
  - Material Symbols font is installed and bar Material icons render (no mass missing glyph boxes).
  - Config UI fonts resolve to installed families (no silent fallback to broken metrics).
  - BarContent workspace group padding is a defined number.
  - Workspace click/wheel uses stock Hyprland `workspace` dispatch successfully.
  - NotificationPopup does not TypeError on forceMonitor/monitor.
  - Theme reapply does not warn on m3primaryDim (and other added *Dim keys).

## Artifacts this phase produces

- Updated `arch/fonts.sh` / `arch/quickshell.sh` font deps
- Config font fallbacks
- Fixed Workspaces + BarContent padding/dispatch
- Aligned notification monitor config
- Appearance m3*Dim properties

## Out of scope

- Phase 2 module feature work (tray polish, network redesign)
- Installing Hyprland plugins for `hl.dsp.*`
- Perfect Google Sans Flex licensing/install if AUR unavailable — fallbacks are enough
- Fixing every ToolbarTabBar empty-tab warning when no tabs exist (secondary; only if trivial)
