---
phase: 21-ii-bar-config-capture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - arch/dots-hyprland.sh
  - scripts/phase21-ii-bar-config-capture-assert.sh
autonomous: true
requirements: [BAR-01]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A dedicated assert harness scripts/phase21-ii-bar-config-capture-assert.sh exists, is executable, and adheres to the dotfiles assert harness contract (set -euo pipefail, four prefixes [PASS]/[FAIL]/[FINDING]/[INFO], counters FAIL/FINDINGS, trap cleanup, --section parsing)"
    - "arch/dots-hyprland.sh capture accepts --quiet and --notify, preserving raw file formatting via cp -p without synthetic diff churn"
    - "run_capture change detection (cmp -s) skips unchanged files before validation as a zero-cost no-op"
    - "run_capture validates JSON syntax via jq empty and non-zero size test, failing closed on 0-byte or corrupted JSON while keeping repository history untouched"
    - "run_capture writes files to the repository mirror atomically using temporary file rename (cp -p to temp file then mv -f)"
    - "Section 1 of the assert script verifies all 6 ingest invariants (valid JSON copy, unchanged skip, 0-byte refusal, syntax error refusal, live symlink refusal, and dirty repo mirror skip) in an isolated scratch fixture"
    - "Section 2 of the assert script proves in an isolated scratch XDG fixture that switchwall.sh:147 mv destroys symlinks and that capture synchronizes the resulting plain file into the repo mirror"
  artifacts:
    - scripts/phase21-ii-bar-config-capture-assert.sh
    - arch/dots-hyprland.sh
  key_links:
    - "Section 1 and 2 tests run against isolated `mktemp -d` scratch trees and prove ingest validation and symlink-destruction recovery before live configuration adoption in Plan 21-03"
---

<objective>
Enhance the core capture engine in `arch/dots-hyprland.sh` (`run_capture`) with format-generic JSON validation, `cmp -s` change-detection optimization, atomic temp-file replace, and desktop notification flags (`--quiet`, `--notify`), while scaffolding the Phase 21 assertion harness (`scripts/phase21-ii-bar-config-capture-assert.sh`) with Sections 1 and 2 to verify all ingest invariants and prove why the `capture/` model is required.

Purpose: Guarantee that Quickshell configuration capture is fail-closed, atomic, and capable of ingesting plain files left by upstream scripts that sever symlinks.
Output: Enhanced `arch/dots-hyprland.sh` and `scripts/phase21-ii-bar-config-capture-assert.sh` with passing Sections 1 and 2.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-ii-bar-config-capture/21-CONTEXT.md
@.planning/phases/21-ii-bar-config-capture/21-RESEARCH.md
@.planning/phases/21-ii-bar-config-capture/21-PATTERNS.md
@.planning/phases/21-ii-bar-config-capture/21-VALIDATION.md
@arch/dots-hyprland.sh
@scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end tracer — CLI ingest validation, atomic replace, and assert harness Section 1</name>
  <files>arch/dots-hyprland.sh, scripts/phase21-ii-bar-config-capture-assert.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (lines 1390-1520: run_capture function and mirror_is_capturable)
    - scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh (lines 1-63: boilerplate, trap cleanup, CLI argument parsing)
    - .planning/phases/21-ii-bar-config-capture/21-CONTEXT.md (D-01 through D-07, D-12, D-13, D-21)
    - .planning/phases/21-ii-bar-config-capture/21-RESEARCH.md (lines 182-212, 278-283: ingest validation, atomic replace, and 0-byte jq pitfall)
    - .planning/phases/21-ii-bar-config-capture/21-PATTERNS.md (lines 22-170, 353-433: run_capture pattern, assert script pattern)
  </read_first>
  <action>
    Scaffold `scripts/phase21-ii-bar-config-capture-assert.sh` with permissions 0755 adhering to the dotfiles assert harness contract (per D-21):
    1. Shebang `#!/usr/bin/env bash`, purpose header, `set -euo pipefail`.
    2. Resolve `REPO_ROOT` as `$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)` and `cd "$REPO_ROOT"`.
    3. Initialize counters `FAIL=0` and `FINDINGS=0`, and define standard helpers: `pass()`, `fail()`, `finding()`, `info()`.
    4. Set up dynamic cleanup arrays `TMP_FILES=()` and `SCRATCH_ROOTS=()`. Implement `cleanup()` function that iterates over `TMP_FILES` removing them, and iterates over `SCRATCH_ROOTS` applying `chmod -R u+rwX` before `rm -rf`, registered via `trap cleanup EXIT`.
    5. Implement CLI argument parsing supporting `--section <N>` (where N is 1 to 7) to run a specific section, or running all sections if omitted. Add `-h|--help` usage display.
    6. Implement Section 1: "BAR-01: Ingest validation, atomic copy, symlink refusal, and dirty repo mirror skip" (per D-01, D-02, D-03, D-05, D-06):
       - Create isolated scratch directory via `mktemp -d /tmp/p21-assert-s1-XXXXXX` and append to `SCRATCH_ROOTS`.
       - Construct a mock repository structure with git init, tracking `capture/testpkg/.config/testpkg/config.json`.
       - Construct matching mock `$HOME` directory `home/.config/testpkg/`.
       - Test Case A (valid update): modify live JSON, run `capture`, assert exit code 0, `[PASS]`, and repo mirror file matches live file.
       - Test Case B (unchanged skip): re-run `capture` without changes, assert `cmp -s` skips copy as unchanged no-op (per D-02).
       - Test Case C (0-byte file): truncate live file to 0 bytes, run `capture`, assert non-zero exit, `[FAIL] invalid or empty JSON`, and repo mirror remains intact (per D-03).
       - Test Case D (corrupt syntax): write malformed JSON (unclosed brace) to live file, run `capture`, assert non-zero exit, `[FAIL] invalid or empty JSON`, and repo mirror remains intact (per D-01, D-03).
       - Test Case E (live symlink refusal): set up live path as a symlink pointing into the repo, run `capture`, assert non-zero exit and `[FAIL] refusing live path that is a symlink resolving into repo` (per D-06, D-39).
       - Test Case F (dirty repo mirror): make an uncommitted edit in the repo mirror, run `capture`, assert `[FINDING] repo mirror is dirty against HEAD` and repo mirror is not overwritten (per D-06).
    7. Add terminal summary block printing `=== done: FAIL=${FAIL} FINDINGS=${FINDINGS} ===` and exiting with code 1 if `FAIL > 0`, else code 0.

    Enhance `arch/dots-hyprland.sh` `run_capture` (per D-01, D-02, D-03, D-04, D-05, D-06, D-12, D-13):
    1. Widen the flag parser at lines 1395-1417: add `local quiet=0 notify=0 captured_count=0`. Recognize `--quiet` setting `quiet=1`, and `--notify` setting `notify=1`. Keep unknown flag check exiting 1.
    2. Update `usage()` in `arch/dots-hyprland.sh` at line 33 and line 95 to document `capture [--dry-run] [--quiet] [--notify]`.
    3. Update logging helpers in `run_capture`: implement `pass()` and `info()` as `if ((quiet == 0)); then printf ...; fi` blocks (per D-13) to ensure quiet mode suppresses passing/noop output without short-circuit `&&` exit issues under `set -e`.
    4. Implement `cmp -s` change detection immediately after live path existence check (per D-02, D-06): if `cmp -s -- "$live" "$repo_file"`, log `info "unchanged: $live"` and `continue`.
    5. Implement format-generic JSON validation (per D-01, D-03, D-06): if `[[ "$live" == *.json ]]`, check `if [[ ! -s "$live" ]] || ! jq empty "$live" >/dev/null 2>&1; then fail "invalid or empty JSON: $live"; continue; fi`.
    6. Implement atomic write into git working tree via temporary file rename (per D-04, D-05, D-06): use `local tmp_repo="${repo_file}.tmp.$$"`, copy with attributes `cp -p -- "$live" "$tmp_repo"` then `mv -f -- "$tmp_repo" "$repo_file"`. If successful, log `pass "captured: $live -> $repo_file"` and increment `captured_count`. On failure, clean up `tmp_repo` with `rm -f` and log `fail`.
    7. Notification dispatch (per D-12, D-13): after package loops finish, if `((notify == 1 && captured_count > 0))`, run `notify-send "Dotfiles Capture" "Captured updates to repository" -a "Shell" -u low || true`.
  </action>
  <acceptance_criteria>
    - `scripts/phase21-ii-bar-config-capture-assert.sh` exists and is executable (`test -x scripts/phase21-ii-bar-config-capture-assert.sh`).
    - `bash -n scripts/phase21-ii-bar-config-capture-assert.sh` and `bash -n arch/dots-hyprland.sh` pass with 0 syntax errors.
    - Executing `./scripts/phase21-ii-bar-config-capture-assert.sh --section 1` exits 0.
    - Section 1 output prints `[PASS]` for valid update, unchanged skip, 0-byte refusal, syntax error refusal, live symlink refusal, and dirty repo mirror skip.
    - `arch/dots-hyprland.sh help` displays `--quiet` and `--notify` under capture usage.
    - Scratch directories from Section 1 are cleaned up on exit (`ls /tmp/p21-assert-s1-* 2>/dev/null` returns empty).
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase21-ii-bar-config-capture-assert.sh --section 1</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0</fails_when>
  </verify>
  <done>End-to-end tracer slice complete: run_capture validates JSON, preserves raw format, writes atomically, and Section 1 verifies all 6 ingest invariants in an isolated fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Section 2 (isolated scratch XDG drill proving switchwall.sh symlink destruction and capture recovery)</name>
  <files>scripts/phase21-ii-bar-config-capture-assert.sh</files>
  <read_first>
    - scripts/phase21-ii-bar-config-capture-assert.sh (from Task 1)
    - vendor/dots-hyprland/dots/.config/quickshell/ii/scripts/colors/switchwall.sh (lines 144-156: set_wallpaper_path and set_thumbnail_path)
    - .planning/phases/21-ii-bar-config-capture/21-CONTEXT.md (D-19)
    - .planning/phases/21-ii-bar-config-capture/21-RESEARCH.md (lines 70-74, 284-289, 308-317)
  </read_first>
  <action>
    Add Section 2 to `scripts/phase21-ii-bar-config-capture-assert.sh`: "BAR-01: isolated fixture wallpaper switch symlink destruction and capture recovery drill" (per D-19):
    1. Create an isolated scratch fixture `DRILL_ROOT="$(mktemp -d /tmp/p21-assert-s2-XXXXXX)"` and register with `SCRATCH_ROOTS`.
    2. Set up mock repository with git init tracking `capture/ii/.config/illogical-impulse/config.json` containing valid initial JSON: `{"background": {"wallpaperPath": "/old/path.jpg"}}`.
    3. Set up mock home `home/.config/illogical-impulse/` where `config.json` is initially created as a symbolic link (`ln -s`) pointing to the repository file.
    4. Assert `test -L "$DRILL_ROOT/home/.config/illogical-impulse/config.json"` is true before the wallpaper change.
    5. Replicate the exact `switchwall.sh:147` rename logic in the fixture:
       `SHELL_CONFIG_FILE="$DRILL_ROOT/home/.config/illogical-impulse/config.json"`
       `jq --arg path "/new/wallpaper.jpg" '.background.wallpaperPath = $path' "$SHELL_CONFIG_FILE" > "$SHELL_CONFIG_FILE.tmp" && mv "$SHELL_CONFIG_FILE.tmp" "$SHELL_CONFIG_FILE"`
    6. Assert that `test -L "$SHELL_CONFIG_FILE"` is now FALSE and `test -f "$SHELL_CONFIG_FILE"` is TRUE, empirically demonstrating that `rename()` (`mv`) severs symbolic links and replaces them with a plain file.
    7. Execute `arch/dots-hyprland.sh capture` targeting the fixture repository and mock `$HOME`.
    8. Assert that `capture` exits 0, validates the plain JSON file, and copies the updated configuration containing `"/new/wallpaper.jpg"` into the repository mirror.
    9. Assert that `git -C "$DRILL_ROOT/repo" diff` shows the updated wallpaper path unstaged in the repository mirror.
    10. Emit `[PASS]` for initial symlink state, `[PASS]` for symlink severed to plain file after `mv`, and `[PASS]` for capture synchronizing the plain file into the repo mirror.
  </action>
  <acceptance_criteria>
    - Executing `./scripts/phase21-ii-bar-config-capture-assert.sh --section 2` exits 0.
    - Section 2 output prints `[PASS]` for symlink presence before switchwall, `[PASS]` for symlink severed to plain file after `mv`, and `[PASS]` for capture synchronizing the plain file into the repo mirror.
    - Real `$HOME` is completely untouched by the drill test.
    - Scratch directories are wiped on exit.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase21-ii-bar-config-capture-assert.sh --section 2</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0</fails_when>
  </verify>
  <done>Section 2 automated drill proves why the capture/ tree is necessary: switchwall.sh's mv replaces symlinks with plain files, and capture cleanly synchronizes them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live filesystem → Git working tree (`capture/` mirror) | Live configuration files read from `$HOME` are copied into the repository working tree |
| Test fixture → Host filesystem | Assert test fixtures must remain completely quarantined within `/tmp/` and must never delete or modify real user files in `$HOME` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | `arch/dots-hyprland.sh` (`run_capture`) | high | mitigate | Ingest-time JSON validation (`jq empty "$live"` paired with `[[ -s "$live" ]]`) prevents 0-byte or corrupted JSON from overwriting clean repository history (per D-01, D-03) |
| T-21-02 | Tampering | `arch/dots-hyprland.sh` (`run_capture`) | high | mitigate | Atomic replacement via `cp -p -- "$live" "$tmp" && mv -f -- "$tmp" "$repo_file"` ensures git working tree never observes a truncated or partial write if interrupted (per D-05) |
| T-21-03 | Elevation of Privilege | `arch/dots-hyprland.sh` (`run_capture`) | high | mitigate | Path containment guard asserts `realpath -m "$repo_file"` remains under `$REPO_ROOT/capture/` and live symlinks resolving into repo are refused (per D-06, D-39) |
| T-21-04 | Tampering | Assert harness (`phase21-ii-bar-config-capture-assert.sh`) | high | mitigate | Fixture operations strictly confined to `mktemp -d /tmp/p21-...` and cleaned up by EXIT trap with `chmod -R u+rwX` before removal |
| T-21-SC | Tampering | Script dependencies | low | accept | Relies only on existing system tools (`jq`, `cmp`, `git`, `bash`, `mv`, `cp`) |
</threat_model>

<verification>
Automated test harness verification across Sections 1 and 2:
`./scripts/phase21-ii-bar-config-capture-assert.sh --section 1`
`./scripts/phase21-ii-bar-config-capture-assert.sh --section 2`
</verification>

<success_criteria>
1. `scripts/phase21-ii-bar-config-capture-assert.sh` exists with trap cleanup and CLI section parsing.
2. `arch/dots-hyprland.sh capture` supports `--quiet` and `--notify`, validates JSON before writing, skips unchanged files via `cmp -s`, and replaces files atomically.
3. Sections 1 and 2 of the assert script pass with 0 failures in isolated fixtures.
</success_criteria>

<output>
Create `.planning/phases/21-ii-bar-config-capture/21-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces
- CLI flags: `--quiet` and `--notify` added to `./arch/dots-hyprland.sh capture`
- Functions: Enhanced `run_capture()` with check pipeline evaluation order (D-06)
- New file paths: `scripts/phase21-ii-bar-config-capture-assert.sh` (Sections 1 and 2)
