---
phase: 21-ii-bar-config-capture
plan: 03
type: execute
wave: 3
depends_on: [21-01, 21-02]
files_modified:
  - capture/ii/.config/illogical-impulse/config.json
  - scripts/phase21-ii-bar-config-capture-assert.sh
autonomous: true
requirements: [BAR-01, BAR-02, CAP-06]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "capture/ii/.config/illogical-impulse/config.json exists as the repository mirror of deliberate personal bar preferences"
    - "Personal bar settings in config.json lock top horizontal bar, spark icon, Dhaka weather, 5 workspaces, and utility toggles"
    - "Section 6 verifies the baseline settings and executes the defaults-reset recovery drill with timestamped backup and Quickshell process reload"
    - "Section 3 confirms that live wallpaper change via switchwall.sh leaves config.json a plain file, capture synchronizes it, and generated theme changes are cleanly reverted"
    - "Section 7 runs the full assertion suite (Sections 1-6) and passes with FAIL=0 FINDINGS=0"
    - "arch/dots-hyprland.sh verify --strict exits 0 with FAIL=0 FINDINGS=0 confirming inverted capture expectations and link integrity"
  artifacts:
    - capture/ii/.config/illogical-impulse/config.json
    - scripts/phase21-ii-bar-config-capture-assert.sh
  key_links:
    - "capture/ii/.config/illogical-impulse/config.json mirrors live ~/.config/illogical-impulse/config.json and survives wallpaper changes"
    - "arch/dots-hyprland.sh verify --strict checks that live config.json is a plain file, never a symlink into repo"
---

<objective>
Adopt the deliberate personal bar baseline into `capture/ii/.config/illogical-impulse/config.json`, implement Section 6 (defaults-reset recovery drill with timestamped backup) and Section 3 (live wallpaper confirmation with clean theme revert) in `scripts/phase21-ii-bar-config-capture-assert.sh`, wire Section 7 for the full suite gate, and pass strict link-aware verification across the entire repository.

Purpose: Solidify the personal bar configuration in the repository mirror and prove through live drills that settings survive both defaults-resets and wallpaper changes.
Output: Tracked `capture/ii/.config/illogical-impulse/config.json`, completed `scripts/phase21-ii-bar-config-capture-assert.sh`, and a passing strict verification gate.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-ii-bar-config-capture/21-CONTEXT.md
@.planning/phases/21-ii-bar-config-capture/21-RESEARCH.md
@.planning/phases/21-ii-bar-config-capture/21-PATTERNS.md
@.planning/phases/21-ii-bar-config-capture/21-VALIDATION.md
@~/.config/illogical-impulse/config.json
@capture/README.md
@scripts/phase21-ii-bar-config-capture-assert.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt deliberate personal bar baseline in capture/ii/ and implement Section 6 (defaults-reset recovery drill)</name>
  <files>capture/ii/.config/illogical-impulse/config.json, scripts/phase21-ii-bar-config-capture-assert.sh</files>
  <read_first>
    - ~/.config/illogical-impulse/config.json (lines 1-580: live configuration schema)
    - capture/README.md (lines 1-54: package layout and capture taxonomy)
    - .planning/phases/21-ii-bar-config-capture/21-CONTEXT.md (D-04, D-15, D-16, D-17, D-18)
    - .planning/phases/21-ii-bar-config-capture/21-RESEARCH.md (lines 388-433: live bar settings and defaults-reset recovery)
    - .planning/phases/21-ii-bar-config-capture/21-PATTERNS.md (lines 203-293: JSON configuration layout)
  </read_first>
  <action>
    Adopt live configuration into repository (per D-04, D-15, D-16, D-17, BAR-02):
    1. Create directory `capture/ii/.config/illogical-impulse/` adhering strictly to the stow-relative layout `capture/<pkg>/<rel_to_home>` (per D-15).
    2. Copy live `~/.config/illogical-impulse/config.json` to `capture/ii/.config/illogical-impulse/config.json` using `cp -p` to preserve raw file bytes, mtime, and 4-space indentation without synthetic formatting diffs (per D-04, D-16).
    3. Validate syntax using `jq empty capture/ii/.config/illogical-impulse/config.json`.
    4. Assert deliberate personal bar preferences are set (per BAR-02, D-16):
       - `"bar.bottom": false` (top bar)
       - `"bar.topLeftIcon": "spark"` (spark icon)
       - `"bar.weather.city": "Dhaka"` (Dhaka weather)
       - `"bar.workspaces.shown": 5` (5 workspaces)
       - `"bar.utilButtons.showMicToggle": true` (mic utility button)
       - `"bar.utilButtons.showScreenSnip": true` (screen snip button)
    5. Assert scope exclusions (per D-17): confirm upstream installer state files (`installed_listfile`, `installed_true`) and runtime directories (`actions/`) are NOT copied or tracked in `capture/ii/`.
    6. Stage `capture/ii/.config/illogical-impulse/config.json` in git (`git add capture/ii/.config/illogical-impulse/config.json`).

    Implement Section 6 in `scripts/phase21-ii-bar-config-capture-assert.sh`:
    1. Section header: "BAR-02: Personal bar settings baseline check and defaults-reset recovery drill" (per D-18, BAR-02).
    2. Verify repo mirror file exists and is tracked: assert `test -f capture/ii/.config/illogical-impulse/config.json` and `git ls-files --error-unmatch capture/ii/.config/illogical-impulse/config.json`.
    3. Verify personal bar settings values via `jq -e`:
       - `jq -e '.bar.bottom == false' capture/ii/.config/illogical-impulse/config.json`
       - `jq -e '.bar.topLeftIcon == "spark"' capture/ii/.config/illogical-impulse/config.json`
       - `jq -e '.bar.weather.city == "Dhaka"' capture/ii/.config/illogical-impulse/config.json`
       - `jq -e '.bar.workspaces.shown == 5' capture/ii/.config/illogical-impulse/config.json`
       - `jq -e '.bar.utilButtons.showMicToggle == true' capture/ii/.config/illogical-impulse/config.json`
       - `jq -e '.bar.utilButtons.showScreenSnip == true' capture/ii/.config/illogical-impulse/config.json`
    4. Execute defaults-reset recovery drill (per D-18):
       - Backup live file: `BACKUP_FILE="$HOME/.config/illogical-impulse/config.json.bak.$(date +%s)"` via `cp -p "$HOME/.config/illogical-impulse/config.json" "$BACKUP_FILE"`. Register `$BACKUP_FILE` with `TMP_FILES`.
       - Simulate defaults reset: write empty JSON `{}` to `$HOME/.config/illogical-impulse/config.json`.
       - Restore from repository mirror: copy `capture/ii/.config/illogical-impulse/config.json` to `$HOME/.config/illogical-impulse/config.json` via `cp -p`.
       - Assert restored file is byte-identical to the backup: `cmp -s "$HOME/.config/illogical-impulse/config.json" "$BACKUP_FILE"`.
       - Remove the backup file: `rm -f "$BACKUP_FILE"`.
       - Reload Quickshell ii process: execute `qs kill -c ii 2>/dev/null || true` followed by `qs -d -c ii >/dev/null 2>&1 & sleep 1`.
       - Assert Quickshell process is running: `pgrep -f "quickshell.*ii" >/dev/null`.
    5. Emit `[PASS]` for baseline existence, `[PASS]` for personal bar preferences, `[PASS]` for backup creation, `[PASS]` for repo restore match, and `[PASS]` for Quickshell reload.
  </action>
  <acceptance_criteria>
    - `capture/ii/.config/illogical-impulse/config.json` exists, is tracked in git, and matches live settings.
    - Upstream state files (`installed_listfile`, `installed_true`) are absent from `capture/ii/`.
    - Executing `./scripts/phase21-ii-bar-config-capture-assert.sh --section 6` exits 0.
    - Section 6 outputs `[PASS]` for baseline settings presence, `[PASS]` for defaults-reset backup, `[PASS]` for repo restoration, and `[PASS]` for Quickshell process reload.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase21-ii-bar-config-capture-assert.sh --section 6</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0</fails_when>
  </verify>
  <done>Personal bar settings baseline is captured in capture/ii/ and defaults-reset recovery drill passes cleanly, verified by Section 6 asserts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Section 3 (live wallpaper confirmation & theme revert), Section 7 (full assert suite), and strict verify gate</name>
  <files>scripts/phase21-ii-bar-config-capture-assert.sh</files>
  <read_first>
    - scripts/phase21-ii-bar-config-capture-assert.sh (from Task 1)
    - vendor/dots-hyprland/dots/.config/quickshell/ii/scripts/colors/switchwall.sh (lines 144-156)
    - arch/dots-hyprland.sh (lines 744-770, 1090-1135: run_verify capture checks)
    - .planning/phases/21-ii-bar-config-capture/21-CONTEXT.md (D-07, D-20, D-21)
    - .planning/phases/21-ii-bar-config-capture/21-RESEARCH.md (lines 40-43, 485-490)
  </read_first>
  <action>
    Implement Section 3 in `scripts/phase21-ii-bar-config-capture-assert.sh` (per D-20, BAR-01):
    1. Section header: "BAR-01: Live wallpaper switch confirmation and theme revert drill".
    2. Confirm live wallpaper path exists: `/home/pera/Pictures/55192173787_b8322b1190_o.jpg`.
    3. Execute upstream wallpaper switcher using current wallpaper (non-disruptive per D-20):
       `bash /home/pera/.config/quickshell/ii/scripts/colors/switchwall.sh "/home/pera/Pictures/55192173787_b8322b1190_o.jpg"`
    4. Assert that `~/.config/illogical-impulse/config.json` is a plain regular file (`test -f`) and NOT a symlink (`test ! -L`).
    5. Run `./arch/dots-hyprland.sh capture --quiet` and assert exit code is 0.
    6. Inspect generated theme files (`kdeglobals`, `colors.lua`, `hyprlock/colors.conf`) and record observation notes for Phase 22 (Q7/Q8).
    7. Cleanly revert generated theme modifications and working tree: run `git checkout -- .` to restore clean repository state.
    8. Emit `[PASS]` for live wallpaper plain file confirmation, `[PASS]` for capture run, and `[PASS]` for clean working tree revert.

    Implement Section 7 in `scripts/phase21-ii-bar-config-capture-assert.sh`:
    1. Section header: "Full suite integration gate".
    2. When `--section 7` is passed or when running without `--section`, execute all sections 1 through 6 sequentially.
    3. Ensure `=== done: FAIL=${FAIL} FINDINGS=${FINDINGS} ===` reflects all tests.

    Execute the complete Phase 21 verification gate:
    1. Run `./scripts/phase21-ii-bar-config-capture-assert.sh` and assert exit code 0 with `FAIL=0 FINDINGS=0`.
    2. Run `./arch/dots-hyprland.sh verify --strict` and assert exit code 0 with `FAIL=0 FINDINGS=0`, confirming that `verify` validates link identity for `stow/` and `restow/` packages while enforcing inverted plain-file expectation for `capture/ii/` without JSON syntax errors.
  </action>
  <acceptance_criteria>
    - Executing `./scripts/phase21-ii-bar-config-capture-assert.sh --section 3` exits 0.
    - Section 3 confirms live wallpaper switch leaves `config.json` a plain file, runs `capture`, and reverts generated theme modifications cleanly.
    - Executing `./scripts/phase21-ii-bar-config-capture-assert.sh` runs all sections and exits 0 with `FAIL=0 FINDINGS=0`.
    - Executing `./arch/dots-hyprland.sh verify --strict` exits 0 with `FAIL=0 FINDINGS=0`.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase21-ii-bar-config-capture-assert.sh && ./arch/dots-hyprland.sh verify --strict</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0, or findings > 0</fails_when>
  </verify>
  <done>All Phase 21 assertions pass: live wallpaper test leaves plain file captured, full assert suite passes 7/7 sections, and arch/dots-hyprland.sh verify --strict confirms zero defects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream script execution → Live desktop session | switchwall.sh executes external tools (jq, matugen, hyprctl) that touch live theme files |
| Defaults-reset recovery drill → Live desktop configuration | Live config.json is temporarily reset and restored from repo mirror |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-08 | Tampering | Upstream `switchwall.sh` execution | high | mitigate | Live confirmation test uses the currently active wallpaper (`55192173787_b8322b1190_o.jpg`) so desktop visuals do not change; git working tree is cleaned via `git checkout` immediately after testing (per D-20) |
| T-21-09 | Denial of Service | Defaults-reset recovery drill | high | mitigate | Timestamped backup `config.json.bak.<epoch>` is created before resetting `config.json` to `{}`; restored from repo mirror and verified byte-identical before resuming normal session (per D-18) |
| T-21-10 | Information Disclosure | `capture/ii/` repository mirror | high | mitigate | Scrutinize `config.json` before committing to verify no API secrets or private tokens are tracked; state files (`installed_listfile`, `installed_true`) are strictly excluded (per D-16, D-17) |
| T-21-SC | Tampering | Quickshell bar process restart | low | accept | Quickshell process restarted using standard `qs kill -c ii && qs -d -c ii` without logging out of Hyprland |
</threat_model>

<verification>
Automated test harness verification across Sections 3, 6, and 7, followed by strict link-aware verification:
`./scripts/phase21-ii-bar-config-capture-assert.sh --section 3`
`./scripts/phase21-ii-bar-config-capture-assert.sh --section 6`
`./scripts/phase21-ii-bar-config-capture-assert.sh && ./arch/dots-hyprland.sh verify --strict`
</verification>

<success_criteria>
1. `capture/ii/.config/illogical-impulse/config.json` is tracked in git and contains deliberate personal bar settings.
2. Section 6 defaults-reset recovery drill and Quickshell process reload pass cleanly.
3. Section 3 live wallpaper test proves `config.json` remains a plain file and capture synchronizes it.
4. `./scripts/phase21-ii-bar-config-capture-assert.sh` exits 0 with `FAIL=0 FINDINGS=0`.
5. `./arch/dots-hyprland.sh verify --strict` exits 0 with `FAIL=0 FINDINGS=0`.
</success_criteria>

<output>
Create `.planning/phases/21-ii-bar-config-capture/21-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces
- New file paths: `capture/ii/.config/illogical-impulse/config.json`
- Assert sections: Sections 3, 6, and 7 in `scripts/phase21-ii-bar-config-capture-assert.sh`
- Verifier gate: `./arch/dots-hyprland.sh verify --strict` pass
