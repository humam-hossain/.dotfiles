---
phase: 07-keymap-reliability-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - .planning/phases/06-runtime-failure-inventory/FAILURES.md
  - .planning/phases/06-runtime-failure-inventory/CHECKLIST.md
  - .config/nvim/README.md
autonomous: false
requirements:
  - BUG-01

must_haves:
  truths:
    - "Every confirmed Phase 6 BUG-01 keymap is re-checked after registry normalization and lazy-dispatch cleanup with no Lua/E488 runtime error"
    - "FAILURES.md marks Phase 7-fixed items as Fixed instead of Confirmed"
    - "CHECKLIST.md becomes a usable post-fix regression checklist, not only a pre-fix repro log"
    - "README is updated only if keymap wording or helper terminology changed in a user-visible way"
  artifacts:
    - path: ".planning/phases/06-runtime-failure-inventory/FAILURES.md"
      provides: "Updated living bug ledger with fixed status for shared keymaps"
      contains: "Fixed"
    - path: ".planning/phases/06-runtime-failure-inventory/CHECKLIST.md"
      provides: "Manual regression checklist for shared keymaps after Phase 7"
      contains: "Expected:"
  key_links:
    - from: ".planning/phases/06-runtime-failure-inventory/CHECKLIST.md"
      to: ".planning/phases/06-runtime-failure-inventory/FAILURES.md"
      via: "BUG IDs and status language remain aligned"
      pattern: "BUG-005|BUG-012|BUG-015"
    - from: ".config/nvim/README.md"
      to: ".config/nvim/lua/core/keymaps/registry.lua"
      via: "shared keymap wording remains consistent with actual behavior"
      pattern: "keymap|registry"
---

<objective>
Re-verify the shared keymap surface after Plan 7-01 lands, then refresh the living failure/docs artifacts so later phases inherit accurate status. This plan owns the evidence-gathering pass: manual keypress confirmation for each confirmed BUG-01 entry, automated startup/smoke/full-suite validation, and minimal doc edits if shared keymap wording changed.

Purpose: BUG-01 is not closed by code edits alone. Phase 7 must leave behind trustworthy inventory/docs that say the keymaps are fixed and how to re-check them.

Output: updated failure inventory, updated checklist, and README wording only where behavior/terminology truly changed.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-keymap-reliability-fixes/07-CONTEXT.md
@.planning/phases/07-keymap-reliability-fixes/07-RESEARCH.md
@.planning/phases/07-keymap-reliability-fixes/07-VALIDATION.md
@.planning/phases/07-keymap-reliability-fixes/07-01-PLAN.md
@.planning/phases/06-runtime-failure-inventory/FAILURES.md
@.planning/phases/06-runtime-failure-inventory/CHECKLIST.md
@.config/nvim/README.md
@scripts/nvim-validate.sh
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manually re-run every confirmed BUG-01 keymap</name>
  <files>N/A (interactive Neovim verification)</files>
  <read_first>
    - .planning/phases/06-runtime-failure-inventory/CHECKLIST.md
    - .planning/phases/06-runtime-failure-inventory/FAILURES.md
    - .config/nvim/lua/core/keymaps/registry.lua
  </read_first>
  <action>
    Open Neovim and manually re-run the full shared-keymap regression set from Phase 6 after Plan 7-01 code changes:

    - `<leader>b` → opens new empty buffer
    - `<leader>lw` → toggles `vim.wo.wrap` without error
    - `<leader>sn` → writes current file without format/autocmd side effects
    - `<leader>xs` → closes current split only
    - `<leader>v` → opens vertical split
    - `<leader>h` → opens horizontal split
    - `<leader>se` → equalizes splits
    - `<leader>gp` → previews current hunk in a modified tracked file
    - `<leader>gt` → toggles current line blame in a tracked file

    Record for each key:
    - whether behavior matched expectation after the mapping moved to its intended execution path
    - whether any Lua/E488/runtime error appeared
    - any preconditions needed (for example "tracked file with git diff")

    If any key still fails, stop and feed exact error text back into the summary instead of marking docs fixed.
  </action>
  <verify>
    <automated>./scripts/nvim-validate.sh startup && ./scripts/nvim-validate.sh smoke</automated>
  </verify>
  <acceptance_criteria>
    - Human verification notes exist for all 9 target mappings
    - No target mapping emits E488, Lua error, or "not a valid function or action"
    - `./scripts/nvim-validate.sh startup` exits 0
    - `./scripts/nvim-validate.sh smoke` exits 0
    - Manual verification does not reveal any regression caused by moving shared mappings out of `M.lazy`
  </acceptance_criteria>
  <done>Shared keymap regression set is re-run interactively and backed by green automated harness output.</done>
</task>

<task type="auto">
  <name>Task 2: Update living failure and checklist artifacts to reflect fixed shared keymaps</name>
  <files>.planning/phases/06-runtime-failure-inventory/FAILURES.md, .planning/phases/06-runtime-failure-inventory/CHECKLIST.md</files>
  <read_first>
    - .planning/phases/06-runtime-failure-inventory/FAILURES.md
    - .planning/phases/06-runtime-failure-inventory/CHECKLIST.md
    - .planning/phases/07-keymap-reliability-fixes/07-RESEARCH.md
  </read_first>
  <action>
    Update `FAILURES.md` and `CHECKLIST.md` in place using exact BUG IDs from Phase 6.

    In `FAILURES.md`:
    - Change BUG-005 through BUG-012 and BUG-015 status from `**Confirmed**` to `**Fixed**` after manual verification passes.
    - Add a short Phase 7 note near each fixed entry or in the summary section stating the action is now callback-based in `registry.lua`.
    - Keep BUG-017 deferred and leave unrelated By Design / Not a Bug items untouched.

    In `CHECKLIST.md`:
    - Preserve each BUG section and numbered steps.
    - Replace pure pre-fix error expectations with post-fix regression wording, for example `Expected: key executes with no error` plus the concrete behavior.
    - Keep git-context prerequisites on BUG-012 and BUG-015.
    - Keep the file usable as a future regression checklist rather than deleting historical context.
  </action>
  <verify>
    <automated>rg -n 'BUG-005|BUG-006|BUG-007|BUG-008|BUG-009|BUG-010|BUG-011|BUG-012|BUG-015' .planning/phases/06-runtime-failure-inventory/FAILURES.md .planning/phases/06-runtime-failure-inventory/CHECKLIST.md && rg -n 'Fixed|Expected:' .planning/phases/06-runtime-failure-inventory/FAILURES.md .planning/phases/06-runtime-failure-inventory/CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `FAILURES.md` contains `**Fixed**` for BUG-005 through BUG-012 and BUG-015
    - `CHECKLIST.md` retains BUG sections and numbered steps for those IDs
    - `CHECKLIST.md` contains `Expected:` or equivalent post-fix expectation text for each fixed keymap
    - No unrelated bug disposition is changed
  </acceptance_criteria>
  <done>Phase 6 artifacts become accurate post-fix sources of truth for later verification phases.</done>
</task>

<task type="auto">
  <name>Task 3: Patch README only if shared-keymap wording or helper terminology changed</name>
  <files>.config/nvim/README.md</files>
  <read_first>
    - .config/nvim/README.md
    - .config/nvim/lua/core/keymaps/registry.lua
    - .config/nvim/lua/core/keymaps/attach.lua
    - .planning/phases/07-keymap-reliability-fixes/07-RESEARCH.md
  </read_first>
  <action>
    Inspect README sections that describe:
    - central keymap rule
    - mapping scopes (`global`, `lazy`, `buffer`, `plugin-local`)
    - manual keymap smoke guidance

    Only edit README if repo state after Plan 7-01 makes current wording inaccurate. Concrete examples of acceptable edits:
    - normalize `plugin-local` terminology if docs still use another token
    - add or tighten wording that shared keymaps are callback-based through the registry rather than brittle string notation

    Do not do broad README cleanup. Do not rewrite unrelated plugin history or rollout sections.
  </action>
  <verify>
    <automated>rg -n 'plugin-local|global|lazy|buffer|registry' .config/nvim/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README remains unchanged if no user-visible wording drift exists
    - If README is changed, edits are limited to shared-keymap scope/behavior wording
    - README does not introduce new inaccurate plugin/runtime claims while touching those sections
  </acceptance_criteria>
  <done>User-facing docs match the fixed shared-keymap architecture without accidental scope creep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| interactive verification ↔ planning artifact | BUG-01 closure depends on real keypress evidence, not only static inspection |
| living bug docs ↔ future phases | incorrect status/docs would mislead later validation and milestone verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Repudiation | manual keymap verification | mitigate | Require explicit human rerun of every affected keymap and carry findings into summary/doc updates |
| T-07-04 | Tampering | `FAILURES.md` / `CHECKLIST.md` status edits | mitigate | Restrict edits to exact BUG IDs fixed in Phase 7 and keep unrelated dispositions untouched |
| T-07-05 | Information Integrity | README patching | mitigate | Limit README edits to precise keymap wording changes; no unrelated cleanup during verification plan |
</threat_model>

<verification>
After all tasks:

1. `./scripts/nvim-validate.sh all` exits 0.
2. Manual verification confirms all 9 target mappings execute without config-caused runtime error.
3. `FAILURES.md` and `CHECKLIST.md` reflect fixed status and future regression expectations for the same BUG IDs.
</verification>

<success_criteria>
- Every confirmed Phase 6 BUG-01 keymap is re-checked after code changes
- Fixed shared-keymap statuses are recorded in `FAILURES.md`
- `CHECKLIST.md` is usable as future regression guidance
- README is only touched if keymap wording really changed
</success_criteria>

<output>
After completion, create `.planning/phases/07-keymap-reliability-fixes/07-02-SUMMARY.md`.
</output>
