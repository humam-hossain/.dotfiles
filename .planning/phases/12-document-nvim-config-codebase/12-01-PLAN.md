---
phase: 12-document-nvim-config-codebase
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .config/nvim/lua/core/options.lua
  - .config/nvim/lua/core/keymaps.lua
  - .config/nvim/lua/core/keymaps/registry.lua
  - .config/nvim/lua/core/keymaps/whichkey.lua
  - .config/nvim/lua/core/keymaps/apply.lua
  - .config/nvim/lua/core/keymaps/attach.lua
  - .config/nvim/lua/core/keymaps/lazy.lua
  - .config/nvim/lua/core/health.lua
  - .config/nvim/lua/core/open.lua
autonomous: true

must_haves:
  truths:
    - "Core Lua files documented with TODO:/NOTE:/FIXME: comments"
    - "Each module has file-level TODO comment explaining purpose"
    - "Uncommented options commented briefly"
  artifacts:
    - path: .config/nvim/lua/core/options.lua
      contains: TODO comment at top
    - path: .config/nvim/lua/core/keymaps.lua
      contains: TODO comment at top
    - path: .config/nvim/lua/core/keymaps/registry.lua
      contains: TODO comment at top
  key_links:
    - from: init.lua
      to: lua/core/options.lua
      via: require
---

<objective>
Document core Lua modules with todo-comments.nvim format.

Purpose: Make codebase self-documenting for future maintainers
Output: Annotated core Lua files
</objective>

<tasks>

<task type="auto">
  <name>Task: Document core/options.lua and core/keymaps.lua</name>
  <files>.config/nvim/lua/core/options.lua,.config/nvim/lua/core/keymaps.lua</files>
  <action>
    Add file-level TODO comment at top of each file using todo-comments format:
    - options.lua: `--- TODO: Editor defaults - cursor, fold, search, clipboard, etc. ---`
    - keymaps.lua: `--- TODO: Global keymaps - smart quit, save/format, tmux navigation ---`

    For each vim.opt/vim.cmd section, add brief NOTE comments explaining what each group does.
    Remove verbose "=====" separators if present.
  </action>
  <verify>
    <automated>head -1 .config/nvim/lua/core/options.lua | grep -q "TODO:" && head -1 .config/nvim/lua/core/keymaps.lua | grep -q "TODO:" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All core keymap sections have NOTE comments, no banner separators</done>
</task>

<task type="auto">
  <name>Task: Document core/keymaps/*.lua modules</name>
  <files>.config/nvim/lua/core/keymaps/registry.lua,.config/nvim/lua/core/keymaps/whichkey.lua,.config/nvim/lua/core/keymaps/apply.lua,.config/nvim/lua/core/keymaps/attach.lua,.config/nvim/lua/core/keymaps/lazy.lua</files>
  <action>
    Add TODO comment at top of each:
    - registry.lua: `--- TODO: Declarative keymap registry - id, lhs, mode, desc, domain, scope ---`
    - whichkey.lua: `--- TODO: Which-key group registration ---`
    - apply.lua: `--- TODO: Global mapping application at startup ---`
    - attach.lua: `--- TODO: Buffer-local mappings on LSP attach ---`
    - lazy.lua: `--- TODO: Lazy keymap compilation for plugin specs ---`

    For option tables, add one-line NOTE comments for groups of settings.
  </action>
  <verify>
    <automated>grep -l "TODO:" .config/nvim/lua/core/keymaps/*.lua | wc -l | grep -q "^5$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 5 keymaps modules documented</done>
</task>

<task type="auto">
  <name>Task: Document core/health.lua and core/open.lua</name>
  <files>.config/nvim/lua/core/health.lua,.config/nvim/lua/core/open.lua</files>
  <action>
    Add TODO comment at top:
    - health.lua: `--- TODO: Health snapshot for validation harness ---`
    - open.lua: `--- TODO: External file open via vim.ui.open() - cross-platform ---`

    Add NOTE comments for key functions.
  </action>
  <verify>
    <automated>head -1 .config/nvim/lua/core/health.lua | grep -q "TODO:" && head -1 .config/nvim/lua/core/open.lua | grep -q "TODO:" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>health.lua and open.lua documented</done>
</task>

</tasks>

<verification>
Check each file has TODO comment at line 1
</verification>

<success_criteria>
- [ ] All core/*.lua files start with TODO comment
- [ ] Option groups have NOTE comments
- [ ] No "===" banner separators remain
</success_criteria>

<output>
After completion, create `.planning/phases/12-document-nvim-config-codebase/12-01-SUMMARY.md`
</output>