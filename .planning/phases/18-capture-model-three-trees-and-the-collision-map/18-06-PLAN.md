---
phase: 18-capture-model-three-trees-and-the-collision-map
plan: 06
type: execute
wave: 4
depends_on: ["18-04"]
files_modified:
  - stow/hypr/.config/hypr/custom/env.lua
  - stow/hypr/.config/hypr/custom/execs.lua
  - stow/hypr/.config/hypr/custom/general.lua
  - stow/hypr/.config/hypr/hyprland-gui.conf
  - stow/hypr/.config/hypr/hyprpaper.conf
  - restow/hypr/.config/hypr/hypridle.conf
  - restow/hypr/.config/hypr/hyprlock.conf
  - restow/hypr/.config/hypr/hyprland/scripts/launch_first_available.sh
  - .config/hypr/custom/env.lua
  - .config/hypr/custom/execs.lua
  - .config/hypr/custom/general.lua
  - .config/hypr/hyprland-gui.conf
  - .config/hypr/hyprpaper.conf
  - .config/hypr/hypridle.conf
  - .config/hypr/hyprlock.conf
  - .config/hypr/hyprland/scripts/launch_first_available.sh
autonomous: true
requirements: [FIX-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`hypr` exists as a package in both `stow/` and `restow/`, split exactly where the map splits it — the custom overlay directory is preserved by the installer, the hyprland directory is destroyed by it, and the tree decides the meaning of the name (D-25)"
    - "`custom/execs.lua` is in `stow/hypr/` — the row Phase 17's D-18 mandated must not go missing"
    - "`launch_first_available.sh` is in `restow/hypr/` tagged as the re-stow recovery class, carrying the repo copy's content, because its live bytes are byte-identical to upstream's after the installer destroyed the customised copy (D-22, D-29)"
    - "Repo-root `.config/` no longer exists — not present-and-empty — and `git ls-files -- .config/` returns nothing"
    - "A live counterpart that is already a symlink into the repo is left alone; only a real file is moved aside before stowing, and GNU Stow is never asked to overwrite a non-link (RESEARCH F-2)"
    - "The aside copy of each live file is retained until the `test -L` confirmation passes, so an interrupted migration leaves a recoverable copy rather than a missing config"
    - "If a migration is interrupted between the aside move and the stow, the live path has no link and the next stow run either completes or exits 1 naming the conflict — it never silently half-links"
    - "`./arch/dots-hyprland.sh verify` runs green over the moved packages after the re-stow, so no repo file in `stow/` or `restow/` is left without a live counterpart (D-51, D-54)"
  artifacts:
    - path: "stow/hypr/.config/hypr/custom/execs.lua"
      provides: "The Phase 17 D-18 mandated overlay file under the non-colliding tree"
    - path: "restow/hypr/.config/hypr/hypridle.conf"
      provides: "An auto-backup destination under the overwriting tree, where the map puts it"
    - path: "restow/hypr/.config/hypr/hyprland/scripts/launch_first_available.sh"
      provides: "The customised launcher, repo content, three levels deep inside a path the installer deletes on every run"
  key_links:
    - from: "stow/hypr/.config/hypr/custom/"
      to: "$HOME/.config/hypr/custom/"
      via: "GNU Stow symlinks created after each live real file is moved aside"
      pattern: "no-folding"
    - from: "restow/hypr/"
      to: "collision-map.tsv"
      via: "membership derived by prefix matching against the destroying rows"
      pattern: "collision-map\\.tsv"
---

<objective>
Split the hypr configuration across the two trees exactly where the collision map splits it, link
both halves live, and remove repo-root `.config/` as a second authoring tree.

`hypr` becomes two packages with the same name. The custom overlay directory is reached by a
primitive that short-circuits on the whole destination and leaves the link alone; the hyprland
directory is reached by a sync-with-delete that replaces the link with a regular file on every
install. The tree decides the meaning of the name (D-25), and the map decides the split (D-26).

Two files in this plan are counter-intuitive and both were decided on measured evidence.
`hypridle.conf` and `hyprlock.conf` look like quiet personal config and belong in `restow/`, because
the auto-backup primitive renames the link away on firstrun. `launch_first_available.sh` is one file
three directories deep inside a path the installer fully owns and deletes on every run — path depth
is not a reason to break the rule, and the tag communicates precisely the fact that it is destroyed
on every install until re-stowed.

RESEARCH F-2 governs every move here: none of these live counterparts is a symlink, all are plain
files, and GNU Stow 2.4.1 exits 1 rather than overwrite a non-link. The live file is moved aside
first and the aside copy is kept until `test -L` confirms.

Purpose: finish placing what the repo-root tree held, then delete the tree.
Output: `stow/hypr/`, `restow/hypr/`, both linked live, and repo-root `.config/` gone.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-CONTEXT.md
@.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-RESEARCH.md
@.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-04-SUMMARY.md
@docs/config-redistribution.md
@collision-map.tsv
@stow/README.md
@restow/README.md
</context>

<tracked_source_paths>
| Path | Status |
|------|--------|
| `.config/hypr/**` (8 remaining files) | tracked — move sources |
| `stow/hypr/**`, `restow/hypr/**` | new tracked paths created by `git mv` |
| `docs/config-redistribution.md` | tracked (created in 18-04; the destination authority) |
| `collision-map.tsv` | tracked (created in 18-01; read, not edited) |
| `$HOME/.config/hypr/...` | live filesystem, not repo paths — named only as stow targets |
</tracked_source_paths>

<tasks>

<task type="auto">
  <name>Task 1: The non-colliding half — stow/hypr/ and its live links</name>
  <precondition>Every live counterpart of the files this task moves is a plain file, not a symlink into the repo — assert with `test -L` on each before moving anything, and halt if any is already a link, because that path is already managed and the aside step would destroy it (RESEARCH F-2).</precondition>
  <files>stow/hypr/.config/hypr/custom/env.lua, stow/hypr/.config/hypr/custom/execs.lua, stow/hypr/.config/hypr/custom/general.lua, stow/hypr/.config/hypr/hyprland-gui.conf, stow/hypr/.config/hypr/hyprpaper.conf, .config/hypr/custom/env.lua, .config/hypr/custom/execs.lua, .config/hypr/custom/general.lua, .config/hypr/hyprland-gui.conf, .config/hypr/hyprpaper.conf</files>
  <read_first>
    - `docs/config-redistribution.md` — the reconciled destination for each file, written in 18-04
    - `collision-map.tsv` — the rows those destinations were derived from
    - `stow/README.md` § the stow-over-a-real-file procedure
    - `18-CONTEXT.md` D-13, D-22, D-25, D-51
    - `18-RESEARCH.md` F-2, F-4, P-1
  </read_first>
  <action>
    Create `stow/hypr/` and move into it every file `docs/config-redistribution.md` assigns to the
    non-colliding tree, preserving the `.config/hypr/...` relative layout that GNU Stow reproduces
    under `$HOME`. At the reconciliation written in 18-04 that is the three custom overlay files plus
    whichever of `hyprland-gui.conf` and `hyprpaper.conf` the map showed uncovered by any destroying
    row. Read the destinations out of the redistribution document; do not re-derive them here.

    `custom/execs.lua` must be among them. Phase 17's D-18 mandates that row explicitly, and it is the
    one row in the whole redistribution that another phase depends on by name.

    Use `git mv` for every move so git records renames (D-28). All of these files are byte-identical to
    their live counterparts, so they move unchanged with no content decision.

    Then link the package live, in this order:
    1. Simulate first: run the stow simulate form from `stow/` with the verbose, no-folding and `-n`
       flags, target `~`, package `hypr`. Read the conflict list. It is non-mutating and names every
       collision exactly; it also exits 1, so capture its status rather than letting `set -e` abort.
    2. Take a timestamped `cp -a` backup of the live paths this package will claim, before anything
       moves. These are files a running Hyprland session reads.
    3. Move each live real file aside to a timestamped neighbour rather than deleting it.
    4. Run the real stow with the fixed idiom from `arch/kitty.sh:10` — `cd` into `stow/`, then `stow`
       with the verbose and no-folding flags in that order, target `~`, package name last. Do not add
       a stow call site in `arch/` for this package: D-19 authorises exactly three new call sites and
       none of them is hypr, because Phase 20 owns Hyprland config placement. This re-stow is performed
       as part of the migration (D-51), the same way D-17 performs the folded-directory unfold by hand.
    5. Confirm each live path with `test -L` and `readlink -f` equality against its repo path.
    6. Only after every confirmation passes, remove the aside copies. Keeping them until then is what
       makes an interrupted migration recoverable rather than a missing config.

    Record in the commit message that `install_dir__ignore_existing` short-circuits on the whole
    destination directory (RESEARCH F-4), so once the live custom directory exists a new file added
    upstream under it is never installed — which is why this half is safe in `stow/` under both folding
    states, and why Phase 17's mandated row is safe either way.

    Do not reach for the flag GNU Stow's conflict message advertises.
    <!-- planner-discipline-allow: --adopt -->

    Commit this disposition on its own, per D-28.
  </action>
  <verify>
    <automated>test -f stow/hypr/.config/hypr/custom/execs.lua && test -L "$HOME/.config/hypr/custom/execs.lua" && [ "$(readlink -f -- "$HOME/.config/hypr/custom/execs.lua")" = "$(readlink -f -- stow/hypr/.config/hypr/custom/execs.lua)" ] && test ! -d .config/hypr/custom && find stow/hypr -type f | sort && bash -c 'set -euo pipefail; for f in $(find stow/hypr -type f -printf "%P\n"); do t="$HOME/${f#.config/}"; t="$HOME/.config/${f#.config/}"; test -L "$t" || { echo "NOT_A_LINK: $t"; exit 1; }; done; echo ALL_LINKED'</automated>
    <fails_when>the mandated overlay file is missing from `stow/hypr/`; its live path is not a symlink or resolves somewhere other than the repo copy; the repo-root custom directory still exists; or the final loop prints `NOT_A_LINK` for any file in the package, meaning the stow was refused by a conflict or only partly applied.</fails_when>
    <human-check>Open a new terminal in the running Hyprland session and confirm the session still behaves normally — the custom overlay files are now symlinks and are read at session start, so confirm the monitor layout and workspace pins are unchanged and no Hyprland error notification appeared.</human-check>
  </verify>
  <done>
    `stow/hypr/` holds every file the redistribution document assigns to the non-colliding tree,
    including the Phase 17 mandated overlay file, each moved with `git mv`. Every corresponding live
    path is a symlink resolving to its repo copy. No aside copy remains. The timestamped backup was
    taken before anything moved. The disposition is committed on its own.
  </done>
</task>

<task type="auto">
  <name>Task 2: The overwriting half — restow/hypr/ and its live links</name>
  <precondition>Every live counterpart of the files this task moves is a plain file, not a symlink into the repo — assert with `test -L` on each before moving anything and halt if any is already a link (RESEARCH F-2).</precondition>
  <files>restow/hypr/.config/hypr/hypridle.conf, restow/hypr/.config/hypr/hyprlock.conf, restow/hypr/.config/hypr/hyprland/scripts/launch_first_available.sh, .config/hypr/hypridle.conf, .config/hypr/hyprlock.conf, .config/hypr/hyprland/scripts/launch_first_available.sh</files>
  <read_first>
    - `docs/config-redistribution.md` — the reconciled destinations and the content decision for the launcher script
    - `18-CONTEXT.md` D-22, D-26, D-29
    - `18-RESEARCH.md` F-5, F-12
    - `restow/README.md` — the two tags and their recovery commands
  </read_first>
  <action>
    Create `restow/hypr/` and move into it every file `docs/config-redistribution.md` assigns to the
    overwriting tree, preserving the `.config/hypr/...` relative layout. At the reconciliation that is
    `hypridle.conf`, `hyprlock.conf` and `hypr/hyprland/scripts/launch_first_available.sh`.

    Use `git mv` for every move. Then apply the one content decision in this set: the launcher script
    takes **repo** content, not live (D-22's vendor-match exception). Its live bytes are byte-identical
    to upstream's, because the sync-with-delete over the hyprland directory has already destroyed the
    customised copy at least once. Taking live there would repeat exactly the loss the repo recorded
    for `starship.toml`. `hypridle.conf` and `hyprlock.conf` are byte-identical to live and move unchanged.

    Link the package live using the same six-step sequence as Task 1 — simulate, timestamped `cp -a`
    backup, move each live real file aside, real stow from `restow/` with the fixed flag idiom, confirm
    with `test -L` and `readlink -f` equality, then remove the aside copies. Do not add a stow call
    site in `arch/` for this package; D-19 authorises exactly three new call sites and none is hypr.

    Two things to record in the commit message, because both will look wrong to a later reader:
    - `hypridle.conf` and `hyprlock.conf` are here because the auto-backup primitive renames the link
      away on firstrun, which is a destroyed outcome. On this host that branch is currently disarmed —
      the firstrun marker exists, so an install today writes a `.new` sidecar and leaves the target
      alone, and both sidecars are present live right now. The map records the firstrun outcome
      deliberately; the observation is the disarmed branch, not a falsification (RESEARCH F-5).
    - The launcher script is one file three directories deep inside a path the installer fully owns and
      deletes on every run (D-29). Path depth is not a reason to break the rule, and the recovery tag
      communicates precisely the fact that it is destroyed on every install until re-stowed. The
      recovery command is the one in `restow/README.md`'s generated table, not a restatement here.

    Do not reach for the flag GNU Stow's conflict message advertises.
    <!-- planner-discipline-allow: --adopt -->

    Commit this disposition on its own, per D-28.
  </action>
  <verify>
    <automated>test -f restow/hypr/.config/hypr/hypridle.conf && test -f restow/hypr/.config/hypr/hyprland/scripts/launch_first_available.sh && test -L "$HOME/.config/hypr/hypridle.conf" && test -L "$HOME/.config/hypr/hyprland/scripts/launch_first_available.sh" && [ "$(readlink -f -- "$HOME/.config/hypr/hyprland/scripts/launch_first_available.sh")" = "$(readlink -f -- restow/hypr/.config/hypr/hyprland/scripts/launch_first_available.sh)" ] && test ! -e .config/hypr/hypridle.conf && test ! -e .config/hypr/hyprland</automated>
    <fails_when>any file is missing from `restow/hypr/`; any live path is not a symlink, meaning the stow was refused by the F-2 conflict; the launcher's live link does not resolve to the repo copy, meaning the content decision landed in the wrong direction; or any source path still exists under repo-root `.config/`.</fails_when>
    <human-check>Confirm the running session's lock and idle behaviour is unchanged — trigger the lock screen once and confirm it renders, since its config path is now a symlink.</human-check>
  </verify>
  <done>
    `restow/hypr/` holds every file the redistribution document assigns to the overwriting tree, each
    moved with `git mv`, with the launcher script carrying repo content. Every corresponding live path
    is a symlink resolving to its repo copy. No aside copy remains. The commit message records the
    disarmed auto-backup branch and the deliberate depth of the launcher row. The disposition is
    committed on its own.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove repo-root .config/ and prove the trees are whole</name>
  <files>.config</files>
  <read_first>
    - `docs/config-redistribution.md` — the complete accounting that must be satisfied before removal
    - `18-CONTEXT.md` D-24, D-28, D-51
  </read_first>
  <action>
    Before removing anything, prove the accounting is complete: every source path listed in
    `docs/config-redistribution.md` must now exist at its recorded destination, and repo-root `.config/`
    must hold no tracked file that the table does not list. If either check fails, stop and fix the
    table or the move rather than removing the tree — nothing here may be disposed of as generated or
    silently deleted.

    Then remove repo-root `.config/` entirely, so it is absent rather than present-and-empty. Use
    `git rm -r` for anything tracked that remains and remove any empty directories left behind, so
    `git ls-files -- .config/` returns nothing and `test -e .config` is false.

    Then run `./arch/dots-hyprland.sh verify` and require it green over the packages this phase has
    moved so far. D-51's sequence ends here: map, move, re-stow, verify. D-54 makes a repo file in
    `stow/` or `restow/` with no live counterpart a `[FAIL]`, so a green run is the proof that every
    moved file is linked and no half-migration is left behind. Report any failure against a package
    this phase has not yet touched as expected rather than treating it as a regression — the starship
    split and the two folded directories land in plan 18-08.

    Commit the removal on its own, as the last of D-28's per-disposition commits in this plan.
  </action>
  <verify>
    <automated>test ! -e .config && [ -z "$(git ls-files -- .config/)" ] && ./arch/dots-hyprland.sh verify; echo "verify_exit=$?"</automated>
    <fails_when>`.config` still exists at the repo root in any form; `git ls-files` still tracks a path under it; or `verify` exits non-zero because a repo file in `stow/` or `restow/` has no live counterpart, meaning a move was not followed by its stow.</fails_when>
  </verify>
  <done>
    Repo-root `.config/` does not exist and no path under it is tracked. Every source row in
    `docs/config-redistribution.md` resolves to an existing destination. `./arch/dots-hyprland.sh verify`
    runs green over the packages moved so far. The removal is its own commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → live `$HOME/.config/hypr` | GNU Stow crosses here into the directory a running desktop session reads on every reload |
| live `$HOME/.config/hypr` → repo working tree | the launcher script's content decision crosses in the opposite direction, and the wrong direction adopts upstream's bytes over the customised ones |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-16 | Denial of Service | GNU Stow over a real live file | high | mitigate | Simulate run first for the exact conflict list; each live real file moved aside rather than deleted; `test -L` plus `readlink -f` equality before any aside copy is removed. Without the aside step stow exits 1 and aborts the caller (RESEARCH F-2, P-1). |
| T-18-22 | Denial of Service | live Hyprland session during the swap | high | mitigate | Timestamped `cp -a` backup of every live path the package will claim, taken before anything moves; aside copies retained until confirmation; a `<human-check>` on the running session after each half. |
| T-18-15 | Tampering | the launcher script's content direction | high | mitigate | Direction decided in `docs/config-redistribution.md` from measured hashes before any bytes move: live is byte-identical to upstream, so repo wins. Committed as its own reviewable diff. |
| T-18-23 | Tampering | removing the tree before the accounting is complete | high | mitigate | Every source row must resolve to an existing destination before `git rm` runs; repo-root `.config/` must hold no tracked file the table does not list; a `verify` run after removal proves no moved file was left unlinked. |
| T-18-17 | Tampering | reaching for the banned adopt flag under the F-2 conflict | high | mitigate | The replacement procedure is named inline in both migration tasks and documented in `stow/README.md` beside the ban. |
| T-18-04 | Tampering | path handling in the move and stow loops | medium | mitigate | Quote every expansion; `IFS=` on every `read`; `--` before every `git` and `readlink` pathspec. |
| T-18-SC | Tampering | package-manager installs | low | accept | This plan adds no install task and contacts no registry. |
</threat_model>

<verification>
- `stow/hypr/` and `restow/hypr/` both exist, split where `collision-map.tsv` splits them
- `custom/execs.lua` is in `stow/hypr/` and its live path is a symlink into it
- `launch_first_available.sh` is in `restow/hypr/` carrying repo content, and its live path resolves there
- Every file in both packages has a live symlink resolving to its repo copy
- `test -e .config` is false and `git ls-files -- .config/` is empty
- `./arch/dots-hyprland.sh verify` runs green over the moved packages
</verification>

<success_criteria>
- Repo-root `.config/` no longer exists and every file it held is accounted for in the redistribution table (ROADMAP criterion 6 / FIX-03, partial — the no-reader scan lands in 18-09)
- Every package boundary is a mechanical consequence of `collision-map.tsv` (D-26)
- No migration step asks GNU Stow to overwrite a non-link, and no aside copy is removed before its link is confirmed
</success_criteria>

<output>
Create `.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-06-SUMMARY.md` when done
</output>
