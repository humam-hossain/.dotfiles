---
phase: 18-capture-model-three-trees-and-the-collision-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/gen-collision-map.sh
  - collision-map.tsv
  - scripts/phase18-capture-model-assert.sh
autonomous: false
requirements: [CAP-02, CAP-03]

assumption_delta_decision:
  primary_noun: custom
  decision: no-change
  rationale: "The detector matched the directory name `hypr/custom/` in the ROADMAP Q5 text, not a derived-to-chosen transition; `custom/` is upstream's own path segment and no assumption changed."

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`collision-map.tsv` exists at the repo root, carries the pin SHA of `vendor/dots-hyprland` in its comment header, and holds one row per destination `3.files-legacy.sh` writes (D-02, D-04)"
    - "`scripts/gen-collision-map.sh <source-root>` emits the complete TSV to stdout and writes nothing in place (D-59)"
    - "Regenerating the map against the real submodule produces a byte-identical file to the committed one, so `diff` is empty on a clean tree"
    - "Two consecutive regenerations against the same source root are byte-identical to each other (F-3 determinism)"
    - "A source root whose `3.files-legacy.sh` names a primitive the lookup does not know makes the generator exit non-zero with a message naming the primitive, rather than emitting a benign-looking row (D-03)"
    - "Running the generator against a fake source tree with `install_dir__ignore_existing` swapped for `install_dir__sync` produces a map that differs from the committed one (D-55)"
    - "The `tree` column holds only `stow` or `restow`, and is computed from `symlink_outcome` and `repo_outcome`, never authored (D-05)"
    - "Two `3.files-legacy.sh` destinations that share a path prefix — `$XDG_CONFIG_HOME/hypr/hyprland` and `$XDG_CONFIG_HOME/hypr/hyprland.conf` — emit two separate rows and neither is merged into the other"
    - "The generator refuses a source root with no `dots/.config/` directory, exiting non-zero and naming the missing path, rather than emitting a header-only map that would diff clean against nothing"
    - "Map rows are emitted through `LC_ALL=C sort`, so a regeneration on a different filesystem or after directory-entry churn produces the same row order"
    - "The generator emits to stdout only, so an interrupted regeneration cannot leave a half-written `collision-map.tsv`"
  artifacts:
    - path: "scripts/gen-collision-map.sh"
      provides: "Deterministic TSV generator over an arbitrary dots-hyprland source root"
      min_lines: 90
    - path: "collision-map.tsv"
      provides: "The checked-in collision map with its reasoning header"
      contains: "pin="
    - path: "scripts/phase18-capture-model-assert.sh"
      provides: "Phase assert, sections 2 and 3"
      min_lines: 80
  key_links:
    - from: "scripts/gen-collision-map.sh"
      to: "vendor/dots-hyprland/sdata/subcmd-install/3.files-legacy.sh"
      via: "reads the MISC find expression and the named install_* call sites at the pinned SHA"
      pattern: "3\\.files-legacy\\.sh"
    - from: "scripts/phase18-capture-model-assert.sh"
      to: "collision-map.tsv"
      via: "regenerate-and-diff — the diff IS the pin-rot signal"
      pattern: "gen-collision-map\\.sh"
  prohibitions:
    - "collision-map.tsv must never be hand-edited to match observed host behaviour; a row that disagrees with reality is corrected at the generator or at the source, never in the artifact"
---

<objective>
Ship the spine of the capture model: a generator that derives the installer collision map
mechanically from the pinned submodule, the committed map itself, and the assert sections that
make the map unable to rot silently.

This is the phase's tracer slice. It wires one real path end to end — vendored installer source →
`scripts/gen-collision-map.sh` → `collision-map.tsv` on disk → `scripts/phase18-capture-model-assert.sh`
verdict — with no stubs. Every later plan in this phase reads the artifact this plan produces:
tree membership (D-05), the `rsync-replace`/`cp-through` tags (D-09), and every FIX-03 destination
decision (D-26) are derived from it.

D-26 makes this plan a hard ordering gate: the map is generated and committed **before any file
moves**.

Purpose: make "how is this file captured" a mechanical question with a checked-in answer.
Output: `scripts/gen-collision-map.sh`, `collision-map.tsv`, and sections 2 and 3 of the phase assert.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-CONTEXT.md
@.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-RESEARCH.md
@.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-PATTERNS.md
@vendor/dots-hyprland/sdata/subcmd-install/3.files-legacy.sh
@scripts/phase17-unblock-assert.sh
@scripts/phase14-verify.sh
</context>

<tracked_source_paths>
Every path this plan writes or reads as a pattern source is git-tracked, or is created by this plan
as new tracked source. No gitignored install mirror is referenced. `vendor/dots-hyprland/**` appears
only as read-only input data to the generator and is never an edit target.

| Path | Status |
|------|--------|
| `scripts/gen-collision-map.sh` | new — tracked on first commit |
| `collision-map.tsv` | new — tracked on first commit |
| `scripts/phase18-capture-model-assert.sh` | new — tracked on first commit |
| `scripts/phase17-unblock-assert.sh` | tracked (pattern source, not edited here) |
| `scripts/phase14-verify.sh` | tracked (pattern source, not edited here) |
| `vendor/dots-hyprland/sdata/subcmd-install/3.files-legacy.sh` | submodule content — read-only input |
</tracked_source_paths>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm the collision map's column set and the derived-tree rule before the generator is written</decision>
  <context>
    D-05 is rated **one-way** in `18-CONTEXT.md` and D-03 is rated **costly**. The column set
    (`dest`, `primitive`, `symlink_outcome`, `repo_outcome`, `tree`, `source`) becomes the checked-in
    artifact that the assert's parser, both tree READMEs, the `rsync-replace`/`cp-through` tag
    derivation and every FIX-03 destination decision read. Changing it after this plan means
    regenerating the map, rewriting the assert's parser, and re-deriving every package's placement.

    `18-CONTEXT.md` already records the developer choosing option A below. This gate exists to
    confirm the door before the agent walks through it, not to reopen the decision.
  </context>
  <options>
    <option id="derived-two-value">
      <name>Derive `tree` from the two outcome columns; only `stow` and `restow` ever appear (D-05, as decided)</name>
      <pros>One source of truth. A new primitive needs no second lookup table. "How is this file captured" has exactly one answer, which is the property CAP-01 rests on.</pros>
      <cons>`capture/` membership cannot be derived from the map at all and must be hand-assigned prose in `capture/README.md`. No per-file override exists if a destination turns out to need one.</cons>
    </option>
    <option id="authored-override">
      <name>Allow an authored `tree` override column</name>
      <pros>A destination that the outcome columns misclassify can be corrected in place without touching the generator.</pros>
      <cons>Every reader must consult two sources to answer "how is this file captured" — exactly the ambiguity this phase exists to remove. Rejected in `18-CONTEXT.md`.</cons>
    </option>
  </options>
  <resume-signal>Type "derived-two-value" to confirm the recorded decision, or "authored-override" to change course.</resume-signal>
</task>

<task type="tracer" tdd="false">
  <name>Task 1: End-to-end collision map — vendored source to committed TSV to assert verdict</name>
  <reversibility rating="costly">D-03 — the generator's column set becomes the checked-in artifact that the assert parser, both tree READMEs and every tree placement read; changing it later means regenerating, reparsing and re-deriving every package.</reversibility>
  <files>scripts/gen-collision-map.sh, collision-map.tsv, scripts/phase18-capture-model-assert.sh</files>
  <read_first>
    - `vendor/dots-hyprland/sdata/subcmd-install/3.files-legacy.sh` — all 79 lines; the map's sole source
    - `18-RESEARCH.md` § The Verified Primitive Matrix — the seven-entry lookup, authored from probe evidence
    - `18-RESEARCH.md` § Code Examples → Deterministic MISC-loop expansion — the `find` expression and `LC_ALL=C sort`
    - `18-PATTERNS.md` § `scripts/gen-collision-map.sh` — the prologue, the loud-failure-inside-a-subshell caveat, the refusal-message shape
    - `scripts/phase14-verify.sh:24-41` — the four-prefix, two-counter contract (D-49)
    - `scripts/phase17-unblock-assert.sh:31-41, 54-71, 925-933` — prologue, vacuity guard, process-substitution read loop
  </read_first>
  <action>
    Write `scripts/gen-collision-map.sh`, generate `collision-map.tsv` from it, and create
    `scripts/phase18-capture-model-assert.sh` carrying the sections that verify the map. One
    executable path from the pinned submodule to a green assert verdict.

    GENERATOR (`scripts/gen-collision-map.sh`, per D-58, D-59):
    - Prologue `set -euo pipefail` plus `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"`,
      matching `scripts/phase17-unblock-assert.sh:31-34`. Source root is `"${1:-vendor/dots-hyprland}"`
      per D-55 so the pin-bump simulation can point it at a temporary tree.
    - Emit everything to stdout. Never open `collision-map.tsv` for writing from inside the generator
      (D-59) — real regeneration is the operator running `scripts/gen-collision-map.sh > collision-map.tsv`,
      and that command string appears in the assert's failure message and in `restow/README.md`.
    - Refuse a source root with no `dots/.config/` directory: print a `[FAIL]`-prefixed message to
      stderr naming the missing path and exit non-zero. A header-only map would diff clean against
      nothing and silently destroy the regenerate-and-diff signal.
    - Hand-author the `primitive -> (symlink_outcome, repo_outcome)` lookup as a `case` over the seven
      primitive names, taking the values from `18-RESEARCH.md` § The Verified Primitive Matrix. Do NOT
      transcribe the table at `.planning/research/PITFALLS.md:285-290`: per RESEARCH F-1 it merges
      `install_dir` with `install_dir__ignore_existing` and describes only the benign one, which would
      produce a wrong row for the konsole destination at `3.files-legacy.sh:18`. The seven entries are
      `install_file` (preserved / OVERWRITTEN), `install_file__auto_backup` (DESTROYED / untouched —
      the firstrun branch, see the header note below), `install_dir` (DESTROYED / untouched),
      `install_dir__sync` (DESTROYED / untouched), `install_dir__sync_exclude` (DESTROYED / untouched),
      `install_dir__ignore_existing` (preserved / untouched), `inline_rename` (DESTROYED / untouched).
    - Unrecognised primitive: print the primitive name and its source line to stderr and `exit 1`
      (D-03). The emitter must therefore NOT run inside a pipeline or a command substitution — per
      `18-PATTERNS.md` and the comment at `scripts/phase14-verify.sh:50-57`, a subshell `exit` leaves
      only the subshell and lets the generator sail on. Feed the loop with process substitution
      (`done < <( ... )`), the technique at `scripts/phase17-unblock-assert.sh:933`.
    - Derive the `tree` column from the two outcome columns, never from `primitive` (D-05): symlink
      preserved AND repo untouched derives `stow`; symlink destroyed OR repo overwritten derives
      `restow`. Only those two values are ever emitted.
    - MISC loop (`3.files-legacy.sh:11`): reproduce the installer's `find` expression verbatim in its
      exclusions (`! -name 'quickshell' ! -name 'fish' ! -name 'hypr' ! -name 'fontconfig'`) and its
      `[ -d ]` / `[ -f ]` discrimination at lines 14-15, then pass the result through `LC_ALL=C sort`
      before the loop. GNU `find` returns readdir order (RESEARCH F-3), which would make the assert's
      regenerate-and-diff fire spuriously on a clean tree. `LC_ALL=C` pins collation so `Kvantum`
      sorts consistently.
    - Named rows: emit one row per named destination at `3.files-legacy.sh:18, 26, 33, 41, 50, 51-54,
      56, 61, 68, 75, 79`, using the literal XDG variable in the `dest` column (`$XDG_CONFIG_HOME/...`
      or `$XDG_DATA_HOME/...` per D-06) and `3.files-legacy.sh:<line>` in the `source` column.
    - Special-case the inline rename at `3.files-legacy.sh:51-54` by name (D-03): it is a bare `mv`
      with no source file and no primitive call, so primitive parsing would silently omit it and the
      loud-failure rule would never fire. Emit it with primitive `inline_rename`, outcomes
      DESTROYED / untouched, source `3.files-legacy.sh:51-54`.
    - Sort the whole emitted body by `dest` under `LC_ALL=C` after the header, so the ordering rule is
      one guarantee rather than two.
    - Do NOT hard-code a row count anywhere, in the generator or in the assert. Per RESEARCH F-13 the
      pin yields 18 MISC rows plus 11 named rows; `$XDG_CONFIG_HOME/hypr/hyprland.lua` at
      `3.files-legacy.sh:61` is a real destination the CONTEXT never enumerates. A count constant is
      the `-eq 15` problem again: one more thing a pin bump falsifies.

    MAP HEADER (comment lines, per D-01, D-60) — the header is where this artifact's reasoning lives,
    so a future reader does not re-derive it or "fix" the map to match a host observation:
    - `# collision-map v1  pin=<output of git -C vendor/dots-hyprland rev-parse HEAD>` (D-60). The pin
      is read at generation time, so a pin bump dirties the map even when no row moves — that is the
      intended re-review signal.
    - The column header line: `# dest	primitive	symlink_outcome	repo_outcome	tree	source`.
    - The regeneration command, spelled out.
    - D-04's coverage reasoning: every destination `3.files-legacy.sh` writes is in, whichever XDG base
      it lands in; `2.setups.sh` is out because its only link-touching line is a `sudo ln -s` for a
      systemd unit outside `$HOME`; font *package* installation and installer state are out, but the
      fontconfig *destination* is in.
    - D-06's reasoning for having no `SKIP_*` column: Phase 16 retired the safe profile, so every
      `SKIP_*` is false on the one install path.
    - The F-3 ordering guarantee: rows are sorted by `dest` under `LC_ALL=C` by the generator; the
      order is a guarantee, not an accident of readdir.
    - The RESEARCH F-5 note, stated plainly: the `install_file__auto_backup` rows record the
      **firstrun** outcome, which is DESTROYED. On this host the destructive branch is currently
      disarmed because `~/.config/illogical-impulse/installed_true` exists, so an install run today
      writes a `.new` sidecar and leaves `hypridle.conf` and `hyprlock.conf` intact. Deleting that
      marker, or passing upstream's `--firstrun`, re-arms it. An empirical spot-check will therefore
      contradict these rows; the rows are right and the observation is the disarmed branch. Do not
      "correct" the map to match the host.
    - The two known coverage gaps (RESEARCH F-13): `3.files-legacy.sh:42`, the alternate fontconfig
      source reached by upstream's font-set flag, and `3.files-legacy.sh:72`, the fedora-only
      `execs.conf` append that is unreachable on Arch.
    - The D-32 accepted risk, stated so the gap is auditable rather than silent: the CAP-08 gate is a
      deny-list of exactly one flag. Upstream's nix, core and skip-family flags still forward, and so
      does the font-set flag, which reaches `3.files-legacy.sh:42` — a fontconfig source this map does
      not model. That is a deliberate, recorded coverage gap, not an oversight.

    ASSERT (`scripts/phase18-capture-model-assert.sh`, per D-57) — create the file with its header,
    contract block and sections 2 and 3a:
    - `#!/usr/bin/env bash`, mode 755, `set -euo pipefail`, the `REPO_ROOT` prologue and `cd "$REPO_ROOT"`.
    - A header comment naming which contract this script follows, as both predecessors do: four
      prefixes `[PASS]` `[FAIL]` `[FINDING]` `[INFO]` and two counters, copied from
      `scripts/phase14-verify.sh:24-41` per D-49, with everything else taking its shape from
      `scripts/phase17-unblock-assert.sh`. State that divergence explicitly in the header.
    - Section banner `echo "=== Phase 18 capture model (non-mutating) ==="` and the closing
      `echo "=== done: FAIL=${FAIL} FINDINGS=${FINDINGS} ==="` followed by `exit 1` when `FAIL > 0`.
    - Section 2 / CAP-02: `collision-map.tsv` exists and is non-empty; its header pin equals
      `git -C vendor/dots-hyprland rev-parse HEAD`; every `3.files-legacy.sh` destination line number
      cited in the map's `source` column resolves to a line that exists in the vendored file; the
      `tree` column holds only `stow` or `restow`. Count data rows with comments filtered out —
      `grep -v '^#' collision-map.tsv | grep -c .` — never a bare `grep -c` over the whole file, since
      the header prose would inflate the count.
    - Section 3a / CAP-03: regenerate and diff. `./scripts/gen-collision-map.sh | diff -u collision-map.tsv -`
      must be empty. On failure print the diff and name the regeneration command in the failure message.
    - Vacuity guard before each of the above, in the shape of `scripts/phase17-unblock-assert.sh:54-71`:
      assert `collision-map.tsv` exists and holds data rows, and that the vendored `3.files-legacy.sh`
      exists and is non-empty, before any check that would otherwise pass over nothing.

    Quoting discipline throughout both scripts: quote every expansion, use `IFS=` on every `read`, and
    put `--` before pathspecs in every `git` invocation.
  </action>
  <verify>
    <automated>bash -n scripts/gen-collision-map.sh && bash -n scripts/phase18-capture-model-assert.sh && ./scripts/gen-collision-map.sh | diff -u collision-map.tsv - && ./scripts/phase18-capture-model-assert.sh</automated>
    <fails_when>`bash -n` reports a syntax error; or `diff -u` prints a hunk and exits 1 because the regenerated TSV differs from the committed `collision-map.tsv`; or the assert prints a `[FAIL]` line and exits 1.</fails_when>
  </verify>
  <done>
    `collision-map.tsv` is committed with a header carrying the current pin SHA, D-04's exclusion
    reasoning, D-06's no-`SKIP_*` reasoning, the F-3 ordering guarantee, the F-5 disarmed-branch note
    and the D-32 accepted-risk note. `scripts/gen-collision-map.sh` reproduces it byte-for-byte from
    the pinned submodule. `scripts/phase18-capture-model-assert.sh` exits 0 with sections 2 and 3a green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove the map cannot rot — determinism, pin-bump simulation, and the two disarmed-state notices</name>
  <files>scripts/phase18-capture-model-assert.sh</files>
  <read_first>
    - `18-CONTEXT.md` D-55 — the fake-tree pin-bump simulation
    - `18-RESEARCH.md` F-3, F-5, F-11 — determinism, the disarmed `auto_backup` branch, the stale Phase 17 message
    - `scripts/phase16-retire-assert.sh:32-41` — the repo's multi-fixture `trap ... EXIT` idiom
  </read_first>
  <action>
    Extend `scripts/phase18-capture-model-assert.sh` with the checks that make CAP-03's "the map
    cannot silently rot against a submodule bump" a demonstrated property rather than a claim.

    Section 3b — determinism (RESEARCH F-3): run the generator twice against the real source root,
    capture both outputs to `mktemp` files, and require them byte-identical via `cmp -s`. A row order
    that depends on readdir order would make section 3a flaky, and a flaky gate gets ignored — which
    discards the real pin-bump signal along with the noise.

    Section 3c — simulated pin bump (D-55): build a minimal fake dots-hyprland tree under
    `mktemp -d` outside the repo. It needs `dots/.config/` with two or three entries so the MISC loop
    has something to expand, a `dots/.local/share/konsole` directory, and an `sdata/subcmd-install/3.files-legacy.sh`
    whose `hypr/custom` call site uses `install_dir__sync` where the real one uses
    `install_dir__ignore_existing`. Run `./scripts/gen-collision-map.sh "$FAKE_ROOT"` against it and
    require the output to DIFFER from the committed `collision-map.tsv`. That proves the generator
    reacts to upstream rather than merely that a diff can be produced. Requiring a non-zero `diff`
    exit is the check; do not compare row counts.

    Cleanup for both fixtures uses `trap 'rm -rf "$F1" "$F2" ...' EXIT` set immediately after each
    `mktemp`, per `scripts/phase16-retire-assert.sh:32-41` and CONTEXT D-56's rule. Under
    `set -euo pipefail` a `fail()` that only increments a counter does not abort, but any other
    non-zero command between creation and cleanup does — and STATE.md records the Phase 17 incident
    where a fixture-handling assumption destroyed tracked files. The assert must leave `git status`
    unchanged; add a closing self-check that `git status --porcelain` is empty of any path this script
    created.

    Two `[INFO]` lines, neither of which moves the exit code:
    - Name the disarmed `install_file__auto_backup` branch (RESEARCH F-5): state that
      `~/.config/illogical-impulse/installed_true` exists on this host, so an install run today writes
      a `.new` sidecar rather than renaming the target away, and that the map deliberately records the
      firstrun outcome. Without this line a green run reads as evidence that the benign branch is the
      modelled one, and the next reader "corrects" the map back into the loss this phase exists to prevent.
    - Name the now-stale Phase 17 message (RESEARCH F-11): `scripts/phase17-unblock-assert.sh`'s
      fourteen-element syntax-file list still says "all 14 files holding a stow call site are present",
      and that sentence becomes factually stale once this phase adds call sites. It counts its own list,
      not the filesystem, so it stays green and is deliberately not edited — a closed assert must keep
      describing what its phase verified.

    Do not add a row-count constant to any section.
  </action>
  <verify>
    <automated>bash -n scripts/phase18-capture-model-assert.sh && ./scripts/phase18-capture-model-assert.sh && ./scripts/phase18-capture-model-assert.sh | grep -c '^\[INFO\]' && git status --porcelain</automated>
    <fails_when>`bash -n` reports a syntax error; the assert prints a `[FAIL]` line and exits 1; `grep -c` reports zero `[INFO]` lines, meaning the F-5 and F-11 notices were not emitted; or `git status --porcelain` prints a fixture path the assert left behind.</fails_when>
  </verify>
  <done>
    Sections 3b and 3c are green. Two consecutive generator runs are byte-identical. A fake source
    tree with one primitive changed produces a map that differs from the committed one. The assert
    emits the disarmed-branch and stale-message `[INFO]` lines, cleans both fixtures through an `EXIT`
    trap, and leaves the working tree unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored submodule → generator | `3.files-legacy.sh` is upstream-controlled text parsed by a shell script; a pin bump changes it without review |
| generator → committed repo artifact | the only write this plan performs lands in the git working tree |
| assert fixture → tracked tree | temporary fake source trees are created and removed inside an assert run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Tampering | `scripts/gen-collision-map.sh`, `scripts/phase18-capture-model-assert.sh` | medium | mitigate | Quote every expansion; `IFS=` on every `read`; `--` before every `git` pathspec. Upstream paths reach the generator as loop variables and must never word-split or glob-expand. |
| T-18-05 | Tampering | assert fixtures (D-55 fake tree, determinism temp files) | medium | mitigate | `trap 'rm -rf ...' EXIT` set immediately after each `mktemp`; `mktemp -d` outside the repo; a closing self-check that `git status --porcelain` names no path the assert created. |
| T-18-08 | Spoofing | `collision-map.tsv` pin header | medium | mitigate | The pin is read from `git -C vendor/dots-hyprland rev-parse HEAD` at generation time, never typed. The regenerate-and-diff in section 3a makes a stale or hand-edited pin a failing diff. |
| T-18-09 | Repudiation | unrecognised installer primitive | high | mitigate | D-03's loud failure: exit non-zero naming the primitive and its source line. The emitter runs under process substitution, not a pipeline, so the `exit` is reachable (`scripts/phase14-verify.sh:50-57`). A defaulted benign row is silent misinformation about a destroying primitive. |
| T-18-SC | Tampering | package-manager installs | low | accept | This phase adds no `npm`/`pip`/`cargo` install task and contacts no registry. Every tool used is already installed and already invoked by committed scripts. No `postinstall` surface exists. |
</threat_model>

<flagged_assumptions>
The edge-coverage probe returned one `unclassified` row for CAP-03 that could not be auto-resolved and
is surfaced here rather than dropped:

- **CAP-03 / unclassified** — "An assert script fails when a path's declared mechanism contradicts the
  collision map, so the map cannot silently rot against a submodule bump" produced no edge category the
  probe could classify. The plan covers the two demonstrations the ROADMAP names (regenerate-and-diff
  plus the D-55 pin-bump simulation here; the D-56 mis-filed fixture in plan 18-10), but no edge
  predicate was derivable for the requirement as a whole. Review manually at verification time.
</flagged_assumptions>

<verification>
- `./scripts/gen-collision-map.sh | diff -u collision-map.tsv -` is empty on a clean tree
- `./scripts/gen-collision-map.sh` run twice produces byte-identical output
- `./scripts/gen-collision-map.sh "$fake_root"` against a tree with one primitive swapped differs from the committed map
- `./scripts/phase18-capture-model-assert.sh` exits 0
- `git status --porcelain` is empty after an assert run
- `grep -v '^#' collision-map.tsv | grep -c .` reports a non-zero data-row count, and no script hard-codes that number
</verification>

<success_criteria>
- `collision-map.tsv` is checked in, covers every destination `3.files-legacy.sh` writes, and records the submodule SHA it was derived from (ROADMAP criterion 2 / CAP-02)
- The map cannot rot silently: a pin bump produces a failing diff, and a changed primitive produces a changed map (ROADMAP criterion 3 / CAP-03, partial — the mis-filed fixture lands in 18-10)
- The `tree` column is derived from the two outcome columns and holds only `stow` or `restow`
- No row count is hard-coded anywhere
</success_criteria>

<output>
Create `.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-01-SUMMARY.md` when done
</output>
