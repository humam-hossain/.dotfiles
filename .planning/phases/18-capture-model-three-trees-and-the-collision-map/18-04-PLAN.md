---
phase: 18-capture-model-three-trees-and-the-collision-map
plan: 04
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - docs/config-redistribution.md
  - docs/archive/README.md
  - docs/archive/hyprland.conf
  - docs/archive/hyprland.conf.bak
  - restow/dolphinrc/.config/dolphinrc
  - restow/kdeglobals/.config/kdeglobals
  - .config/dolphinrc
  - .config/kdeglobals
  - .config/hypr/hyprland.conf
  - .config/hypr/hyprland.conf.bak
autonomous: true
requirements: [FIX-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`docs/config-redistribution.md` records one row per file that left repo-root `.config/`, naming its destination, which copy won the content decision, and why (D-22, D-24)"
    - "Every `hypr/` destination in that table is reconciled against `collision-map.tsv` by prefix matching before any file moves, so each package boundary is a mechanical consequence of the map rather than of intuition (D-07, D-13, D-26)"
    - "`hypridle.conf` and `hyprlock.conf` resolve to `restow/` because `install_file__auto_backup` renames the link away on firstrun — the placement intuition would get wrong — and the table records that reasoning (D-26)"
    - "The `$XDG_CONFIG_HOME/hypr/hyprland.lua` row that the CONTEXT never enumerated is reconciled too: it is a real destination with no repo-root counterpart, so it creates a map row and no move"
    - "`hyprland.conf` and `hyprland.conf.bak` are in `docs/archive/` with a note citing `3.files-legacy.sh:51-54` and Phase 14, and neither is in a capture tree (D-23)"
    - "`dolphinrc` and `kdeglobals` live in `restow/` carrying their live content, and no GNU Stow invocation runs in this plan — every live link in this phase is created by plan 18-11 in the main worktree, after all repo-side moves have merged (D-22, RESEARCH F-2)"
    - "Moves are committed per disposition — into `stow/`, into `restow/`, into `docs/archive/`, script retargeting, then removal — so each is independently revertible and git records the unchanged moves as renames (D-28)"
  artifacts:
    - path: "docs/config-redistribution.md"
      provides: "The FIX-03 redistribution table: source path, disposition, destination, content winner, reason"
      min_lines: 30
    - path: "restow/dolphinrc/.config/dolphinrc"
      provides: "dolphinrc under the restow tree with live content, repo-side only"
    - path: "restow/kdeglobals/.config/kdeglobals"
      provides: "kdeglobals under the restow tree with live content"
    - path: "docs/archive/hyprland.conf"
      provides: "The Phase 14 renamed-away session config, retired"
  key_links:
    - from: "docs/config-redistribution.md"
      to: "collision-map.tsv"
      via: "every destination is justified by the map row its path prefix-matches"
      pattern: "collision-map\\.tsv"
    - from: "restow/dolphinrc/.config/dolphinrc"
      to: "docs/config-redistribution.md"
      via: "the destination row that justifies this package, derived from the map"
      pattern: "dolphinrc"
  prohibitions:
    - "No file leaving repo-root .config/ may be disposed of as generated or deleted without its content existing elsewhere in git history, in docs/archive/, or in the live tree at the moment of removal"
---

<objective>
Reconcile the redistribution table against the generated collision map, archive the two files that
have no capture future, and move the two non-hypr files into `restow/` with their content decision
resolved.

D-26 is the ordering constraint this plan sits inside: the map is generated and committed first
(plan 18-01), and only then does anything move. Every package boundary must be a mechanical
consequence of the map. Intuition gets `hypridle.conf` and `hyprlock.conf` wrong — they look like
quiet personal config, and `install_file__auto_backup` renames the link away on firstrun, which puts
them in `restow/`.

This plan is repo-side only. It moves files between tracked directories and writes a record; it runs
no GNU Stow invocation and creates no symlink under `$HOME`. Live linking for every package this phase
migrates is concentrated in plan 18-11, which runs in the main worktree after all repo-side moves have
merged. RESEARCH F-2's aside-then-stow procedure — all twelve live counterparts are plain files, and
GNU Stow 2.4.1 exits 1 rather than overwrite a non-link — is carried out there, not here.

Purpose: place every file the repo-root tree held, with a recorded reason, before the tree is removed.
Output: the redistribution table in `docs/`, two archived files, and two `restow/` packages on disk.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-CONTEXT.md
@.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-RESEARCH.md
@.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-01-SUMMARY.md
@.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-02-SUMMARY.md
@collision-map.tsv
@restow/README.md
@docs/archive/README.md
</context>

<tracked_source_paths>
| Path | Status |
|------|--------|
| `.config/dolphinrc`, `.config/kdeglobals`, `.config/hypr/hyprland.conf`, `.config/hypr/hyprland.conf.bak` | tracked — move sources |
| `docs/config-redistribution.md` | new — tracked on first commit |
| `docs/archive/README.md` | tracked (created in 18-02) |
| `docs/archive/hyprland.conf`, `docs/archive/hyprland.conf.bak` | new tracked paths created by `git mv` |
| `restow/dolphinrc/.config/dolphinrc`, `restow/kdeglobals/.config/kdeglobals` | new tracked paths created by `git mv` |
| `collision-map.tsv` | tracked (created in 18-01; read, not edited) |
| `$HOME/.config/...` | live filesystem, not repo paths — read only, for the content comparison; this plan writes nothing under `$HOME` |
</tracked_source_paths>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the redistribution table against the generated map</name>
  <precondition>`collision-map.tsv` exists at the repo root and `./scripts/gen-collision-map.sh | diff -u collision-map.tsv -` is empty — the map must be committed and current before any destination is decided (D-26).</precondition>
  <files>docs/config-redistribution.md</files>
  <read_first>
    - `collision-map.tsv` — the full generated map, all rows
    - `18-CONTEXT.md` § Specific Ideas → the redistribution table — the twelve rows and their recorded dispositions
    - `18-CONTEXT.md` D-07, D-13, D-22, D-24, D-26
    - `18-RESEARCH.md` F-12 — the three drifted files reproduced byte for byte; F-13 — the `hyprland.lua` row
  </read_first>
  <action>
    Write `docs/config-redistribution.md`, the operator-facing half of D-24's record. The other half
    stays in `18-CONTEXT.md`; this file is the one `docs/` reader-facing copy, and it is a record of a
    one-time move, not a machine-checked artifact.

    Before writing a destination, resolve each `hypr/` row against `collision-map.tsv` by prefix
    matching (D-13): a file belongs in `restow/` when any map row at or above its live destination has
    `symlink_outcome` DESTROYED or `repo_outcome` OVERWRITTEN. D-07 constrains the whole migration —
    only what the map says must move is moved, so the diff stays small and every move is justified by
    a row. Read the derived values out of the map; do not restate them from memory.

    The reconciliation must resolve, at minimum:
    - the three `custom/*.lua` files against the `$XDG_CONFIG_HOME/hypr/custom` row
    - `hypridle.conf` and `hyprlock.conf` against their `install_file__auto_backup` rows, and record
      in the table *why* the answer is counter-intuitive: the firstrun branch renames the link away,
      which is a destroyed outcome (D-26). Also record the RESEARCH F-5 fact that the destructive
      branch is currently disarmed on this host, so an operator who runs an install and sees the file
      survive is watching the benign branch, not falsifying the row.
    - `hyprland-gui.conf` and `hyprpaper.conf`, which the CONTEXT marks "not a legacy-script
      destination; confirm during generation". Confirm against the generated map and record the result
      and the evidence — if no row covers their path prefix, they do not collide and belong in `stow/`.
    - `hypr/hyprland/scripts/launch_first_available.sh` against the `$XDG_CONFIG_HOME/hypr/hyprland`
      row, which is a sync-with-delete destination.
    - the `$XDG_CONFIG_HOME/hypr/hyprland.lua` row (RESEARCH F-13, source `3.files-legacy.sh:61`). It
      is a real destination that the CONTEXT's twelve-row table never enumerates, it has no repo-root
      counterpart, and it therefore creates a map row and **no move**. Record it in the table as a
      reconciliation note with that explanation, so the next reader does not treat it as a missing row.

    Columns: source path, disposition, destination, content winner, reason. Populate from
    `18-CONTEXT.md`'s recorded table, with the destinations replaced by the reconciled values.

    Record the three content decisions verbatim with their reasoning (D-22): `dolphinrc` and
    `kdeglobals` take **live** content because all three copies differ and live is genuine
    KDE-written state; `launch_first_available.sh` takes **repo** content because its live bytes are
    byte-identical to upstream's — the installer's sync-with-delete over `hypr/hyprland/` has already
    destroyed the customised copy at least once, so taking live there would repeat exactly the loss
    the repo recorded for `starship.toml`. The remaining seven files match their live counterpart byte
    for byte and move unchanged.

    Record the two archive rows (D-23): `hyprland.conf` has no live counterpart because the installer
    renamed it to `.old` in Phase 14 at `3.files-legacy.sh:51-54`, and `hyprland.conf.bak` is
    byte-identical to a backup that already exists live. Neither belongs in a capture tree.

    Record D-28's commit granularity at the top of the file: the removal is split into separate commits
    per disposition — moves into `stow/`, moves into `restow/`, moves into `docs/archive/`, script
    retargeting, then the final removal of the now-empty tree. Each is independently revertible, git
    records the unchanged moves as renames, and reverting a bad live-content adoption is the likely
    failure mode.

    State explicitly that no file in this table is disposed of as generated or simply deleted: every
    row ends in `stow/`, `restow/` or `docs/archive/`.
  </action>
  <verify>
    <automated>test -f docs/config-redistribution.md && grep -q -- 'hyprland.lua' docs/config-redistribution.md && bash -c 'set -u; n=0; miss=0; while IFS= read -r f; do n=$((n+1)); grep -q -F -- "$f" docs/config-redistribution.md || { echo "UNACCOUNTED: $f"; miss=1; }; done < <(git ls-files -- .config/); [ "$n" -gt 0 ] || { echo "NO_SOURCE_FILES"; exit 1; }; [ "$miss" -eq 0 ] || exit 1; echo "ALL_ACCOUNTED n=$n"'</automated>
    <fails_when>the file is missing; the `hyprland.lua` reconciliation note is absent, meaning the unenumerated map row was not reconciled; `NO_SOURCE_FILES` is printed, meaning the accounting loop iterated over nothing; or `UNACCOUNTED` is printed for any path, meaning a file still tracked under repo-root `.config/` has no row in the table. The expected set is derived from `git ls-files -- .config/` at run time rather than compared against a hard-coded row count, so it stays correct if the tree's contents differ from the planning snapshot.</fails_when>
  </verify>
  <done>
    `docs/config-redistribution.md` exists with one row per file under repo-root `.config/`, every
    `hypr/` destination derived from `collision-map.tsv` by prefix matching rather than asserted, the
    three content decisions recorded with their reasoning, the two archive rows recorded with their
    provenance, the `hyprland.lua` reconciliation note present, and D-28's per-disposition commit order
    stated. No row is disposed of as generated or deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Archive the two files with no capture future</name>
  <files>docs/archive/hyprland.conf, docs/archive/hyprland.conf.bak, docs/archive/README.md, .config/hypr/hyprland.conf, .config/hypr/hyprland.conf.bak</files>
  <read_first>
    - `18-CONTEXT.md` D-23, D-27, D-28
    - `docs/archive/README.md` — the entry table created in 18-02
  </read_first>
  <action>
    Move `.config/hypr/hyprland.conf` and `.config/hypr/hyprland.conf.bak` into `docs/archive/` under
    their original basenames, using `git mv` so git records the moves as renames and the content stays
    linked to its history (D-28). Do not use copy-then-delete: it loses the link to history and makes
    the per-disposition revert much harder.

    Add one row per file to the entry table in `docs/archive/README.md`, each naming why it was retired
    and — per D-27 — the commit that retired it. The commit hash is not known until the commit exists,
    so write the rows with the retiring-commit cell filled from `git rev-parse --short HEAD` after the
    move is committed, in a follow-up amend or a second commit within this task. State the retirement
    reasons as D-23 records them: `hyprland.conf` has no live counterpart because the installer renamed
    it to `.old` back in Phase 14 at `3.files-legacy.sh:51-54`; `hyprland.conf.bak` is byte-identical to
    a backup that already exists live. Neither belongs in a capture tree.

    Note in the archive README that `~/.config/hypr/hyprland.conf.old` is the live artefact of that
    Phase 14 rename and is deliberately untracked, so a reader connecting the two does not go looking
    for it in the repo.

    Commit this disposition on its own, per D-28.
  </action>
  <verify>
    <automated>test -f docs/archive/hyprland.conf && test -f docs/archive/hyprland.conf.bak && test ! -e .config/hypr/hyprland.conf && test ! -e .config/hypr/hyprland.conf.bak && git log -1 --format= --name-status -- docs/archive/hyprland.conf | grep -q '^R' && grep -q -F -- 'hyprland.conf.bak' docs/archive/README.md && grep -q -F -- 'hyprland.conf' docs/archive/README.md</automated>
    <fails_when>either archived file is missing from `docs/archive/`; either source path still exists under repo-root `.config/`; the name-status line does not begin with a rename marker, meaning copy-then-delete was used instead of `git mv` — anchored to line start so an unrelated filename containing the letter cannot satisfy it; or the archive README's entry table has no row for either archived file.</fails_when>
  </verify>
  <done>
    Both files are in `docs/archive/` under their original basenames, recorded as renames by git, and
    absent from repo-root `.config/`. `docs/archive/README.md` has one entry row per file naming the
    retirement reason and the retiring commit, plus the note about the untracked live `.old` artefact.
    The disposition is committed on its own.
  </done>
</task>

<task type="auto">
  <name>Task 3: Move dolphinrc and kdeglobals into restow/</name>
  <precondition>This task performs no GNU Stow invocation and creates no symlink under `$HOME`. Every live link in this phase is created by plan 18-11, which runs in the main worktree after all repo-side moves have merged. If a future revision reintroduces a stow call here, it will run against whichever working tree this plan was dispatched into — and when that is a per-agent worktree, every link it creates points into a directory that is deleted at wave teardown, leaving the operator's live configuration resolving to nothing.</precondition>
  <files>restow/dolphinrc/.config/dolphinrc, restow/kdeglobals/.config/kdeglobals, .config/dolphinrc, .config/kdeglobals</files>
  <read_first>
    - `docs/config-redistribution.md` — the content decisions written in Task 1
    - `18-RESEARCH.md` F-2 — the measured stow conflict and its exit codes in both simulate and real mode
    - `stow/README.md` § the stow-over-a-real-file procedure — the operator-facing form of the same steps
    - `arch/kitty.sh:10` — the fixed stow call-site idiom
  </read_first>
  <action>
    Create two `restow/` packages, one per file, following the naming precedent D-52 sets with
    `restow/starship/` — a package named for the single file it carries. `restow/dolphinrc/.config/dolphinrc`
    and `restow/kdeglobals/.config/kdeglobals`. Both are MISC-loop destinations reached by
    `install_file`, which writes through the link with a forced copy: the symlink survives and the repo
    copy is overwritten, which is the `cp-through` tag and a `restow/` placement.

    Move the repo files with `git mv` so the rename is recorded, then apply the D-22 content decision:
    live content wins for both, because all three copies differ and live is genuine KDE-written state.
    Overwrite the moved repo copy with the live bytes and commit that as a visible content change, so
    the adoption is a reviewable diff rather than an invisible side effect of a move.

    Stop there. This task is repo-side only: it does not run GNU Stow, does not move any live file
    aside, and does not create any symlink under `$HOME`. The live counterparts stay exactly as they
    are — plain files that the running desktop keeps reading — until plan 18-11 links every migrated
    package in one deliberate pass.

    The reason is structural rather than stylistic. A plan with more than two tasks and no decision
    checkpoint is dispatched to a subagent, and with worktree isolation enabled that subagent runs in
    a per-agent worktree that is torn down when the wave completes. A stow run from inside it would
    create links resolving into that worktree, and every one would dangle the moment it is removed.
    The obvious-looking repair — resolve the main worktree root and stow from there — cannot work
    here either: the package this task creates with `git mv` exists only in the dispatched working
    tree until the wave merges, so the main root has no such package to stow. Separating the repo-side
    move from the live-side link is what makes both halves correct under either execution mode.

    Record in the commit message that the live counterparts are still plain files and that plan 18-11
    owns the link, so an operator reading the history mid-phase is not left wondering.

    Commit this disposition on its own, per D-28.
  </action>
  <verify>
    <automated>test -f restow/dolphinrc/.config/dolphinrc && test -f restow/kdeglobals/.config/kdeglobals && test ! -e .config/dolphinrc && test ! -e .config/kdeglobals && cmp -s "$HOME/.config/dolphinrc" restow/dolphinrc/.config/dolphinrc && cmp -s "$HOME/.config/kdeglobals" restow/kdeglobals/.config/kdeglobals && [ -z "$(git -C . diff --name-only HEAD -- restow/dolphinrc restow/kdeglobals)" ] && ! git log -1 --format=%B | grep -q -- 'stow --verbose'</automated>
    <fails_when>either repo file is missing from its `restow/` package or still present under repo-root `.config/`; either `cmp -s` reports a difference, meaning the live-content adoption did not land; the moved packages still carry uncommitted changes; or the commit message records a stow invocation, meaning a live-linking step was reintroduced into a plan that may be executing in a disposable worktree. Live symlink state is deliberately NOT asserted here — no link exists yet, and plan 18-11 owns both the linking and the assertion that each link resolves under the main worktree.</fails_when>
  </verify>
  <done>
    `restow/dolphinrc/` and `restow/kdeglobals/` exist, each holding one file at its `.config/` path
    with the live content adopted as a reviewable diff. Neither file remains under repo-root `.config/`.
    No GNU Stow invocation ran and no symlink was created under `$HOME`; both live counterparts are
    still the plain files the desktop is reading, and plan 18-11 links them. The disposition is
    committed on its own.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live `$HOME/.config` → repo working tree | the content adoption crosses here; the wrong direction silently destroys the authoring copy |
| dispatched working tree → live `$HOME/.config` | NOT crossed by this plan. GNU Stow would cross it, and under worktree isolation the links would resolve into a directory deleted at wave teardown — which is why every stow invocation lives in plan 18-11 instead |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-15 | Tampering | the D-22 content decision | high | mitigate | Direction is decided per file in `docs/config-redistribution.md` before any bytes move, with the measured evidence recorded. The adoption is committed as its own visible diff so a wrong direction is reviewable and revertible, which D-28's per-disposition commits exist to enable. |
| T-18-34 | Denial of Service | live symlinks created from a disposable working tree | critical | mitigate | This plan performs no stow. A plan with more than two tasks and no decision checkpoint dispatches to a subagent, and with worktree isolation on that subagent runs in a per-agent worktree removed at wave teardown; links created there dangle, taking the running desktop with them. The `<precondition>` names the failure mode and the verify asserts the commit records no stow invocation, so a future revision cannot silently reintroduce one. |
| T-18-17 | Tampering | reaching for the banned adopt flag under the F-2 conflict | high | transfer | No stow runs here, so the conflict cannot arise in this plan. The mitigation lives with the linking work in plan 18-11, beside `stow/README.md`'s documented replacement procedure. |
| T-18-18 | Repudiation | loss of file history across the move | medium | mitigate | `git mv` for every move so git records renames; copy-then-delete is explicitly rejected because it breaks the per-disposition revert D-28 relies on. |
| T-18-04 | Tampering | path handling in the move and stow steps | medium | mitigate | Quote every expansion; `--` before every pathspec in `git` and `readlink` invocations. |
| T-18-SC | Tampering | package-manager installs | low | accept | This plan adds no install task and contacts no registry. |
</threat_model>

<verification>
- `docs/config-redistribution.md` accounts for every file that was under repo-root `.config/`, with each `hypr/` destination derived from `collision-map.tsv`
- `docs/archive/` holds `hyprland.conf` and `hyprland.conf.bak`, recorded as renames, with entry rows naming the reason and the retiring commit
- `restow/dolphinrc/` and `restow/kdeglobals/` exist and carry live content
- No symlink was created under `$HOME` and no stow invocation ran; both live counterparts are still plain files
- Each disposition is its own commit
</verification>

<success_criteria>
- Every file the repo-root tree held is accounted for in a redistribution table, moved to `stow/`, `restow/` or `docs/archive/` (ROADMAP criterion 6 / FIX-03, partial — the hypr split lands in 18-06 and the removal plus reader scan in 18-09)
- Every destination is a mechanical consequence of `collision-map.tsv`, not of intuition (D-26)
- No file is disposed of as generated or silently deleted
- No live symlink is created from a working tree that may be discarded; linking is plan 18-11's job
</success_criteria>

<output>
Create `.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-04-SUMMARY.md` when done
</output>
