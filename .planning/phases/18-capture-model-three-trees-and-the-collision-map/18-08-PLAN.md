---
phase: 18-capture-model-three-trees-and-the-collision-map
plan: 08
type: execute
wave: 6
depends_on: ["18-06"]
files_modified:
  - restow/starship/.config/starship.toml
  - stow/zsh/.config/starship.toml
  - arch/zsh.sh
  - arch/qbittorrent.sh
  - arch/scrutiny.sh
  - scripts/phase17-unblock-assert.sh
autonomous: true
requirements: [CAP-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`starship.toml` lives in its own `restow/starship/` package and `stow/zsh/` keeps the three shell files, so every package lives wholly in one tree — which is what makes a file's capture mechanism knowable from its location alone (D-08)"
    - "`arch/zsh.sh` gains exactly one new stow invocation, for the starship package; `arch/zsh_powerlevel.sh` gains none, because it is a prompt-theme installer that does not own starship (D-08, Q-2)"
    - "`arch/qbittorrent.sh` exists and carries the fixed stow call-site idiom, and `arch/scrutiny.sh` gains one, so neither package depends on having been stowed by hand on this machine (D-19)"
    - "All 18 stow call sites under `arch/` carry the literal verbose-then-no-folding flag pair in that fixed order, so a single counted grep remains a complete audit"
    - "`scripts/phase17-unblock-assert.sh` changes by exactly one line — its call-site count constant — with a comment naming the two decisions that moved it; every other line, including the folded-directory probe patterns and the fourteen-element syntax-file list, is untouched (D-20)"
    - "`~/.config/qBittorrent` and `~/.config/smartmontools` are real directories whose contents are symlinks into `stow/`, so no package remains folded and the assert needs no exception list (D-15, D-17)"
    - "After the unfold only the tracked qBittorrent files stay linked; runtime output stops landing inside the repo, which retires the condition the Phase 17 probe patterns exist to police (D-16)"
  artifacts:
    - path: "restow/starship/.config/starship.toml"
      provides: "The starship prompt config as its own package under the overwriting tree"
    - path: "arch/qbittorrent.sh"
      provides: "First stow call site for the qbittorrent package"
      min_lines: 5
  key_links:
    - from: "arch/zsh.sh"
      to: "restow/starship/"
      via: "a second stow invocation targeting the restow tree"
      pattern: "restow"
    - from: "scripts/phase17-unblock-assert.sh"
      to: "arch/*.sh"
      via: "counted grep of the literal flag pair across all call sites"
      pattern: "verbose=5"
  prohibitions:
    - "A gate is never turned green by editing a closed-phase record or by weakening what it asserts; the only permitted change to a closed assert is one that keeps it asserting the same property"
---

<objective>
Finish the tree split for the one package that straddles two trees, give the two hand-stowed packages
real call sites, and unfold the last two folded directories.

`starship.toml` is the clearest instance of the problem this phase exists to solve: the installer
writes through its symlink with a forced copy, so the repo copy is overwritten while the link
survives — and that loss is not hypothetical, it already happened and the repo has carried upstream's
bytes ever since. Splitting it into `restow/starship/` is what makes every package live wholly in one
tree, which is what makes CAP-01 true.

`qBittorrent` and `smartmontools` have no stow call site at all today — both were stowed by hand, so
a fresh machine never links either one. They also hold the last two folded directory symlinks that
Phase 17's audit found and handed here. Unfolding them means every package obeys the no-folding
default and the assert needs no exception list.

Adding call sites moves the count a closed Phase 17 assert hard-codes. That constant is the one
authorised edit to that file: the property under test — every call site carrying the literal flag pair
in fixed order — is unchanged, and only the number of sites moved.

Purpose: no package straddles two trees, no package is folded, and no package depends on having been
stowed by hand.
Output: `restow/starship/`, three new call sites, and two unfolded directories.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-CONTEXT.md
@.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-RESEARCH.md
@.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-06-SUMMARY.md
@collision-map.tsv
@restow/README.md
@arch/kitty.sh
@scripts/phase17-unblock-assert.sh
</context>

<tracked_source_paths>
| Path | Status |
|------|--------|
| `stow/zsh/.config/starship.toml` | tracked — move source |
| `restow/starship/.config/starship.toml` | new tracked path created by `git mv` |
| `arch/zsh.sh`, `arch/scrutiny.sh`, `scripts/phase17-unblock-assert.sh` | tracked |
| `arch/qbittorrent.sh` | new — tracked on first commit |
| `arch/kitty.sh` | tracked (call-site idiom source, not edited) |
| `stow/qbittorrent/`, `stow/smartmontools/` | tracked packages (re-stowed, not edited) |
</tracked_source_paths>

<tasks>

<task type="auto">
  <name>Task 1: Split starship into its own restow package and give it a call site</name>
  <files>restow/starship/.config/starship.toml, stow/zsh/.config/starship.toml, arch/zsh.sh</files>
  <read_first>
    - `collision-map.tsv` — the row for the starship destination and its derived tree
    - `18-CONTEXT.md` D-08, D-10, D-51, D-52 and the Q-2 scope decision
    - `restow/README.md` — the tag definitions and the note this package needs
    - `arch/zsh.sh:43` and `arch/kitty.sh:10` — the existing call site and the canonical idiom
  </read_first>
  <action>
    Split `starship.toml` out of the `zsh` package. `stow/zsh/` keeps its three shell files and stays
    in the non-colliding tree; the prompt config moves to `restow/starship/.config/starship.toml`. Every
    package then lives wholly in one tree, which is the property CAP-01 rests on. The accepted cost is
    one more package and a second stow invocation in `arch/zsh.sh`.

    Confirm the destination against `collision-map.tsv` before moving: the starship destination is
    reached by the forced-copy primitive, so the link survives and the repo copy is overwritten — the
    write-through tag, recovered by checking the repo path out of git and then re-stowing.

    Sequence, per D-51:
    1. Confirm the live path is currently a symlink into `stow/zsh/`. If it is a real file, apply the
       aside procedure from `stow/README.md` instead; do not let stow refuse.
    2. Remove the `zsh` package's links from `stow/` with stow's delete mode. This briefly unlinks the
       three shell files as well — note it in the commit message; the window is short and no shell is
       re-sourced during it.
    3. `git mv` the file into `restow/starship/.config/starship.toml`, content as-is (D-52).
    4. Re-stow `zsh` from `stow/` and stow `starship` from `restow/`, both with the fixed flag idiom.
    5. Confirm both live paths with `test -L` and `readlink -f` equality.

    Add exactly one new line to `arch/zsh.sh`, beside the existing invocation at line 43, using the
    `arch/kitty.sh:10` form with the tree component changed to the overwriting tree and the package
    name `starship`. The flag order is fixed and load-bearing — it is what lets one counted grep audit
    every site — so reproduce it exactly.

    Do **not** add a starship line to `arch/zsh_powerlevel.sh`. It independently stows the `zsh`
    package but is a prompt-theme installer and does not own starship (Q-2). That boundary is already
    recorded in `restow/README.md`; leave it there rather than duplicating the note.

    Confirm `restow/README.md`'s existing note about this package is still accurate after the move: the
    repo copy is byte-identical to upstream's and has been since the commit that accepted the overwrite
    as a known loss, so a reader does not find the package puzzling. Restoring a custom prompt config
    is a preference decision deferred out of this phase; the package is the correct slot for it.
  </action>
  <verify>
    <automated>test -f restow/starship/.config/starship.toml && test ! -e stow/zsh/.config/starship.toml && test -L "$HOME/.config/starship.toml" && [ "$(readlink -f -- "$HOME/.config/starship.toml")" = "$(readlink -f -- restow/starship/.config/starship.toml)" ] && test -L "$HOME/.zshrc" && bash -n arch/zsh.sh && [ "$(grep -c -- '--verbose=5 --no-folding' arch/zsh.sh)" -eq 2 ] && [ "$(grep -c -- '--verbose=5 --no-folding' arch/zsh_powerlevel.sh)" -eq 1 ]</automated>
    <fails_when>the file is missing from the new package or still present in the old one; the live prompt-config path is not a symlink or resolves to the old package; the shell rc link was not restored by the re-stow; `bash -n` reports a syntax error; the zsh installer does not carry exactly 2 flag-pair occurrences; or the powerlevel installer does not carry exactly 1, meaning a starship line was wrongly added there. Both counts are captured and compared to an explicit number — a `grep -c` left bare would exit 0 for any count of 1 or more and certify nothing.</fails_when>
  </verify>
  <done>
    `restow/starship/` holds the prompt config, `stow/zsh/` no longer does, both live paths are symlinks
    resolving to their repo copies, and the shell rc links are intact. `arch/zsh.sh` carries exactly two
    stow invocations and `arch/zsh_powerlevel.sh` still carries exactly one.
  </done>
</task>

<task type="auto">
  <name>Task 2: Call sites for the two hand-stowed packages, and the one authorised constant</name>
  <files>arch/qbittorrent.sh, arch/scrutiny.sh, scripts/phase17-unblock-assert.sh</files>
  <read_first>
    - `arch/kitty.sh` — the whole file; the canonical nine-line package-script shape
    - `arch/scrutiny.sh` — its current tail, which ends in docker metric collection with no stow line
    - `scripts/phase17-unblock-assert.sh:85-96` — the counted grep, its rationale comment, and the constant
    - `18-CONTEXT.md` D-19, D-20
    - `18-RESEARCH.md` F-10, F-11
  </read_first>
  <action>
    Create `arch/qbittorrent.sh` on the `arch/kitty.sh` shape — the shebang, the strict-mode prologue and
    trace flag, an install echo and the package install, then a config echo and the single stow line for
    the `qbittorrent` package from the non-colliding tree. Neither this package nor smartmontools
    collides with the installer, so both stay in `stow/` (D-15).

    Append the same single stow line to `arch/scrutiny.sh` for the `smartmontools` package, using the
    `arch/kitty.sh:10` form with the tree component left as the non-colliding tree. Place it after the
    existing content, keeping the file's current structure.

    Both packages have no call site today — both were stowed by hand, so a fresh machine never links
    either one (D-19). Reproduce the flag order exactly: verbose flag then no-folding flag, target `~`,
    package name last.

    Then change `scripts/phase17-unblock-assert.sh` by exactly one line: the call-site count constant
    moves from 15 to 18. Add a short comment on or above that line naming the two decisions that moved
    it — one new call site each for the two packages above, and one for the starship split.

    The arithmetic: 15 existing sites, plus this task's two, plus Task 1's one, is 18. `arch/hyprland.sh`
    holds two of the existing fifteen. Neither hypr package added in plan 18-06 gets a call site, because
    Phase 20 owns Hyprland config placement.

    Nothing else in that file changes. The folded-directory probe patterns stay exactly as they are —
    that script is a closed phase's verification record, and editing it retroactively would make the
    Phase 17 record describe something Phase 17 did not verify. Phase 18's own assert simply does not
    carry those patterns. The fourteen-element syntax-file list also stays: it counts its own list rather
    than the filesystem, so adding two `arch/*.sh` files does not change the count and it stays green
    with no edit (RESEARCH F-11).

    The narrow exception for the constant is authorised because the property under test is unchanged —
    every call site carrying the literal flag pair in fixed order — and only the number of sites moved.
    Leaving it at 15 would make the script fail forever, which destroys its value as a regression check
    far more thoroughly than moving the constant does. A self-deriving count was rejected: rewriting a
    closed script's logic is a larger departure than changing its constant, and the reasoning comment
    above the grep would have to be rewritten with it.

    After the edit, run the Phase 17 assert and require it green. Do not adjust any other constant or
    message in it to make it pass; if something else is red, that is a finding for this plan, not an
    invitation to edit the record.
  </action>
  <verify>
    <automated>bash -n arch/qbittorrent.sh && bash -n arch/scrutiny.sh && bash -n scripts/phase17-unblock-assert.sh && [ "$(grep -ho -- '--verbose=5 --no-folding' arch/*.sh | wc -l)" -eq 18 ] && ./scripts/phase17-unblock-assert.sh && [ "$(git log -1 --format= --numstat -- scripts/phase17-unblock-assert.sh | awk '{print $1, $2}')" = "1 1" ]</automated>
    <fails_when>`bash -n` reports a syntax error in any of the three scripts; the counted flag pairs across `arch/*.sh` is not exactly 18 — the count is captured and compared, not printed; `./scripts/phase17-unblock-assert.sh` prints a `[FAIL]` line and exits 1; or the numstat of the most recent commit touching the closed assert is not exactly one line added and one removed, meaning something beyond the authorised constant was edited. `git log -1 --numstat` on the path is used rather than a diff against `HEAD~1`, so the check does not depend on the commit ordering around it.</fails_when>
  </verify>
  <done>
    `arch/qbittorrent.sh` exists on the canonical package-script shape with one stow line, and
    `arch/scrutiny.sh` has gained one. The literal flag pair appears at 18 sites across `arch/*.sh`.
    `scripts/phase17-unblock-assert.sh` differs by exactly one line — the constant — carrying a comment
    naming the two decisions that moved it, and the Phase 17 assert runs green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Unfold the last two folded directories</name>
  <precondition>qBittorrent is not running — it rewrites its config on exit, and a write landing in the window between the unstow and the re-stow creates a real file the re-stow then refuses to overwrite, aborting mid-operation. Confirm with a process check before touching anything, and halt if it is running (D-18).</precondition>
  <files>stow/qbittorrent, stow/smartmontools</files>
  <read_first>
    - `18-CONTEXT.md` D-15, D-16, D-17, D-18, D-20
    - `scripts/phase17-unblock-assert.sh:425-436` — the folded-directory audit that found these two and handed them here
    - `stow/README.md` § the stow-over-a-real-file procedure
  </read_first>
  <action>
    Unfold both folded directory symlinks that Phase 17's audit found: the qBittorrent and smartmontools
    configuration directories under the live config root. Each is currently a single symlink pointing at
    a package directory inside `stow/`; after the unfold each is a real directory whose contents are
    individual symlinks into `stow/`.

    No one-shot migration script ships (D-17). Perform the unfold directly, per package:
    1. Take a timestamped `cp -a` backup of the live directory's resolved contents.
    2. Remove the package's links with stow's delete mode from `stow/`, which removes the folded
       directory symlink itself.
    3. Confirm nothing real was left at the live path. If a real file or directory is there, apply the
       aside procedure from `stow/README.md` before continuing — do not let the re-stow be refused.
    4. Re-stow the package from `stow/` with the fixed flag idiom, including the no-folding flag, which
       is what produces individual file links inside a real directory.
    5. Confirm the live path is a real directory, that it is not itself a symlink, and that each of its
       entries is a symlink resolving into the package.

    Do the smartmontools package first: it has no running-process precondition, because the scrutiny
    installer copies its files into the system config tree rather than reading the linked path. Do
    qBittorrent second, under the precondition above.

    Record the accepted consequence for qBittorrent in the commit message (D-16): once unfolded, only
    the tracked files stay linked, and runtime output — feed additions, lock files, sockets, and
    categories created through the GUI — stops landing inside the repo. This is the correct behaviour
    and it retires the condition the Phase 17 probe patterns exist to police. A genuinely new category
    or feed now needs an explicit capture, which Phase 21 owns.

    Do not edit those Phase 17 probe patterns (D-20). They stay as the closed record of what that phase
    verified; Phase 18's own assert simply does not carry them.
  </action>
  <verify>
    <automated>test -d "$HOME/.config/qBittorrent" && test ! -L "$HOME/.config/qBittorrent" && test -d "$HOME/.config/smartmontools" && test ! -L "$HOME/.config/smartmontools" && bash -c 'set -euo pipefail; n=0; for d in "$HOME/.config/qBittorrent" "$HOME/.config/smartmontools"; do while IFS= read -r e; do test -L "$e" || { echo "NOT_A_LINK: $e"; exit 1; }; n=$((n+1)); done < <(find "$d" -maxdepth 1 -mindepth 1 | sort); done; test "$n" -gt 0 || { echo "NO_ENTRIES"; exit 1; }; echo "ALL_LINKED n=$n"' && [ -z "$(find "$HOME/.config" -maxdepth 2 -type l -lname '*.dotfiles*' -exec test -d {} \; -print)" ]</automated>
    <fails_when>either live path is missing, or is still a symlink rather than a real directory; the entry loop prints `NOT_A_LINK`, meaning the re-stow left a real file behind; `NO_ENTRIES` is printed, meaning the directory is empty and the check observed nothing; or the folded-directory scan prints any path, meaning a folded directory symlink still exists under the live config root. That last scan's output is tested for emptiness rather than left to `find`'s exit status, which is 0 whether or not it matched.</fails_when>
    <human-check>Start qBittorrent and confirm it opens with its existing settings, feeds and categories intact — its config path is now a real directory of links rather than one folded link, and the application rewrites that config on exit.</human-check>
  </verify>
  <done>
    Both live paths are real directories whose every entry is a symlink resolving into its `stow/`
    package. No folded directory symlink remains under the live config root. Timestamped backups were
    taken before each unfold. The qBittorrent consequence is recorded in the commit message. The Phase
    17 probe patterns are untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → live `$HOME/.config` | stow's delete and re-stow modes both cross here, and the window between them has no link at all |
| running application → live config path | qBittorrent writes its config on exit, straight into the unlink window |
| Phase 18 edits → a closed Phase 17 record | one authorised constant change to a file that is otherwise evidence, not code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-25 | Denial of Service | the unlink window during an unfold | high | mitigate | A `<precondition>` that qBittorrent is not running, checked before anything is touched and halting if it is; a timestamped `cp -a` backup of each live directory's resolved contents before the unstow; a real-file check between the unstow and the re-stow so a refused re-stow is caught rather than aborting mid-operation (D-18). |
| T-18-26 | Repudiation | editing a closed-phase assert | high | mitigate | Exactly one line changes — the call-site count — with a comment naming the two decisions that moved it. The diff is asserted to be one line. The property under test is unchanged; only the number of sites moved. Every other line, including the folded-directory probe patterns and the syntax-file list, stays. |
| T-18-16 | Denial of Service | GNU Stow over a real live file | high | mitigate | Each step checks whether the live path is a link or a real file before acting, and routes a real file through the documented aside procedure rather than letting stow refuse and abort a `set -euo pipefail` caller. |
| T-18-27 | Denial of Service | briefly unlinked shell rc files during the starship split | medium | accept | The window between the unstow and the re-stow of the zsh package is short, no shell is re-sourced during it, and the re-stow is asserted to have restored the link. Recorded in the commit message rather than engineered around. |
| T-18-04 | Tampering | path handling in the unfold loops | medium | mitigate | Quote every expansion; `IFS=` on every `read`; feed the entry loop with process substitution so a failure inside it can end the check rather than only its subshell. |
| T-18-SC | Tampering | package-manager installs | low | accept | `arch/qbittorrent.sh` installs the qbittorrent package from the distribution's own repositories using the same pattern every other `arch/*.sh` uses. No language-registry install is added, and no `postinstall` surface exists. |
</threat_model>

<verification>
- `restow/starship/` holds the prompt config; `stow/zsh/` holds only the three shell files
- `arch/zsh.sh` carries two stow invocations; `arch/zsh_powerlevel.sh` still carries one
- `arch/qbittorrent.sh` exists and `arch/scrutiny.sh` has a stow line; the literal flag pair counts 18 across `arch/*.sh`
- `./scripts/phase17-unblock-assert.sh` runs green and differs by exactly one line from its previous state
- Both previously folded live directories are real directories whose entries are all symlinks into `stow/`
- No folded directory symlink remains under the live config root
</verification>

<success_criteria>
- Every package lives wholly in one tree, so a file's capture mechanism is knowable from its location alone (ROADMAP criterion 1 / CAP-01)
- Every package has a stow call site, so a fresh machine links all of them
- Every package obeys the no-folding default and the assert needs no exception list
</success_criteria>

<output>
Create `.planning/phases/18-capture-model-three-trees-and-the-collision-map/18-08-SUMMARY.md` when done
</output>
