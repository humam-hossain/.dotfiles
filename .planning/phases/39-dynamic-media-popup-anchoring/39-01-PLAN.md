---
phase: 39-dynamic-media-popup-anchoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - restow/quickshell/.config/quickshell/ii/GlobalStates.qml
  - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
  - restow/quickshell/.config/quickshell/ii/modules/ii/verticalBar/VerticalBarContent.qml
  - restow/quickshell/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml
  - scripts/phase39-media-popup-assert.sh
autonomous: true
requirements:
  - MEDIA-01
  - MEDIA-02

estimate:
  tokens: 45000
  raw_tokens: 22000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "MediaControls.qml dynamically aligns horizontally under the Media pill center when activated from the status bar (MEDIA-01, D-01)."
    - "MediaControls.qml enforces horizontal boundary clamping ensuring popup edges maintain at least Appearance.sizes.hyprlandGapsOut (5px) margin from display boundaries on standard, ultrawide, and narrow screens (MEDIA-02, D-09, D-10)."
    - "When opened via shortcut or IPC without an active pill click (mediaPillCenterX <= 0), MediaControls.qml cleanly falls back to upstream center margin math (D-08)."
    - "Multi-monitor target display assignment (GlobalStates.mediaPillScreen) opens the popup on whichever monitor's bar pill was clicked (D-06, D-07)."
    - "Bar layout shifts (e.g. track title change expanding pill) reactively update popup coordinates while open without layer-shell jitter (D-04)."
    - "Vertical bar mode anchors popup vertically relative to GlobalStates.mediaPillCenterY with boundary clamping (D-03)."
    - "All QML changes are deployed via restow/quickshell/ leaf symlinks without modifying vendor/dots-hyprland (INTG-01, D-13)."
    - "Test harness scripts/phase39-media-popup-assert.sh validates all 5 sections and ./arch/dots-hyprland.sh verify --strict exits 0 with FAIL=0 FINDINGS=0 (INTG-02, INTG-03)."
  artifacts:
    - path: "restow/quickshell/.config/quickshell/ii/GlobalStates.qml"
      provides: "Desktop shell singleton overlay exposing mediaPillCenterX, mediaPillCenterY, and mediaPillScreen"
      contains: "mediaPillCenterX"
    - path: "restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml"
      provides: "Bar content layout capturing pill center coordinates via mapToItem(null, ...) with HoverHandler multi-monitor isolation and reactive geometry tracking"
      contains: "updateMediaPillCoords"
    - path: "restow/quickshell/.config/quickshell/ii/modules/ii/verticalBar/VerticalBarContent.qml"
      provides: "Vertical bar layout capturing vertical pill center coordinates via verticalMedia.mapToItem(null, ...)"
      contains: "updateVerticalMediaPillCoords"
    - path: "restow/quickshell/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml"
      provides: "Popup panel overlay binding screen: GlobalStates.mediaPillScreen ?? null and computing dynamically clamped margins with upstream fallback"
      contains: "GlobalStates.mediaPillScreen"
    - path: "scripts/phase39-media-popup-assert.sh"
      provides: "5-section automated test harness validating symlink packaging, static AST properties, headless coordinate clamping math across resolutions, multi-monitor lifecycle, and strict dots verification"
      contains: "phase39-media-popup-assert"
  key_links:
    - from: "restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml"
      to: "restow/quickshell/.config/quickshell/ii/GlobalStates.qml"
      via: "mediaLoader.item.mapToItem(null, ...) setting mediaPillCenterX, mediaPillCenterY, mediaPillScreen"
      pattern: "GlobalStates\\.mediaPillCenterX\\s*="
    - from: "restow/quickshell/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml"
      to: "restow/quickshell/.config/quickshell/ii/GlobalStates.qml"
      via: "PanelWindow.screen and margins.left binding"
      pattern: "GlobalStates\\.mediaPillScreen"
    - from: "scripts/phase39-media-popup-assert.sh"
      to: "arch/dots-hyprland.sh"
      via: "strict repository verification gate invocation"
      pattern: "\\./arch/dots-hyprland\\.sh verify --strict"
---

<objective>
Dynamically position `MediaControls.qml` directly beneath the top status bar's `Media` pill across active monitors, implement horizontal and vertical boundary clamping enforcing `Appearance.sizes.hyprlandGapsOut` margins, preserve upstream center positioning fallback when opened via shortcut or IPC, deploy modifications strictly via `restow/quickshell/` leaf symlinks without modifying `vendor/dots-hyprland`, and establish an automated 5-section assertion test harness (`scripts/phase39-media-popup-assert.sh`) passing `./arch/dots-hyprland.sh verify --strict` with zero git churn.

Purpose: Eliminate awkward static screen-center media popup positioning by contextually anchoring the popup directly underneath the clicked media pill on any monitor, with smooth edge clamping preventing off-screen clipping on ultrawide, standard, or compact displays.
Output: Restow overlays `GlobalStates.qml`, `BarContent.qml`, `VerticalBarContent.qml`, `MediaControls.qml`, and executable test harness `scripts/phase39-media-popup-assert.sh`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-dynamic-media-popup-anchoring/39-CONTEXT.md
@.planning/phases/39-dynamic-media-popup-anchoring/39-RESEARCH.md
@.planning/phases/39-dynamic-media-popup-anchoring/39-PATTERNS.md
@.planning/phases/39-dynamic-media-popup-anchoring/39-VALIDATION.md
@restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
@restow/quickshell/.config/quickshell/ii/modules/ii/verticalBar/VerticalBarContent.qml
@vendor/dots-hyprland/dots/.config/quickshell/ii/GlobalStates.qml
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/Appearance.qml
@vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/bar/StyledPopup.qml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — Test harness foundation (scripts/phase39-media-popup-assert.sh), GlobalStates.qml bridge overlay, and headless state verification</name>
  <read_first>
    - .planning/phases/39-dynamic-media-popup-anchoring/39-CONTEXT.md
    - .planning/phases/39-dynamic-media-popup-anchoring/39-RESEARCH.md
    - .planning/phases/39-dynamic-media-popup-anchoring/39-PATTERNS.md
    - vendor/dots-hyprland/dots/.config/quickshell/ii/GlobalStates.qml
    - scripts/phase38-power-profiles-assert.sh
  </read_first>
  <files>restow/quickshell/.config/quickshell/ii/GlobalStates.qml, scripts/phase39-media-popup-assert.sh</files>
  <action>
    Establish the phase test harness scaffold at `scripts/phase39-media-popup-assert.sh` and execute the end-to-end tracer slice deploying the `GlobalStates.qml` state bridge overlay to prove cross-process coordinate sharing (per D-05, MEDIA-01, INTG-01).

    1. Construct `scripts/phase39-media-popup-assert.sh`:
       - Follow the proven 5-section architecture from `scripts/phase38-power-profiles-assert.sh` and `scripts/phase37-voice-pill-assert.sh`.
       - Enforce bash strict mode (`set -euo pipefail`), non-root guard (`EUID != 0`), CLI argument parsing for `--section <1-5>` (`-s <1-5>`) and `--syntax` (`-c`), cleanup trap for temporary files, and git porcelain snapshot tracking (`PORCELAIN_BEFORE` and `PORCELAIN_AFTER`).
       - Implement `run_qs_test()` helper running headless Quickshell (`quickshell -p`) on a temporary QML test file with timeout.
       - Implement Section 1 (Symlink & Packaging Integrity):
         * Assert that parent directories in `~/.config/quickshell/ii/` are real directories (no folding).
         * Assert that `~/.config/quickshell/ii/GlobalStates.qml` is a symlink resolving into `restow/quickshell/.config/quickshell/ii/GlobalStates.qml`.
         * Assert that git submodule `vendor/dots-hyprland` has zero git diff or untracked files.
       - Implement initial Section 2 (QML Property & Clamping Math Static AST) checks:
         * Assert `restow/quickshell/.config/quickshell/ii/GlobalStates.qml` declares `property real mediaPillCenterX: -1`, `property real mediaPillCenterY: -1`, and `property var mediaPillScreen: null`.
       - Implement initial Section 4 (Multi-Monitor Screen Binding & State Reset Lifecycle) headless check:
         * Execute headless Quickshell test confirming `GlobalStates` singleton instantiates and exposes `mediaPillCenterX == -1`, `mediaPillCenterY == -1`, and `mediaPillScreen == null`.
       - Make `scripts/phase39-media-popup-assert.sh` executable (`chmod +x scripts/phase39-media-popup-assert.sh`).

    2. Author `restow/quickshell/.config/quickshell/ii/GlobalStates.qml`:
       - Copy `vendor/dots-hyprland/dots/.config/quickshell/ii/GlobalStates.qml` into `restow/quickshell/.config/quickshell/ii/GlobalStates.qml`.
       - Add the three coordinate bridge properties directly under `property bool mediaControlsOpen: false` (per D-05):
         ```qml
         property real mediaPillCenterX: -1
         property real mediaPillCenterY: -1
         property var mediaPillScreen: null
         ```
       - Maintain all upstream singletons, imports, shortcuts, and handlers byte-identical.

    3. Deploy `GlobalStates.qml` via GNU Stow:
       - Note reversibility: check if `~/.config/quickshell/ii/GlobalStates.qml` exists as a regular file; if so, preserve it by moving to `~/.config/quickshell/ii/GlobalStates.qml.bak` before stowing.
       - Run `stow --verbose=5 --no-folding -d "$REPO_ROOT/restow" -t "$HOME" quickshell`.
       - Verify that `~/.config/quickshell/ii/GlobalStates.qml` is now a valid symlink pointing to the restow repository file.
  </action>
  <verify>
    <automated>test -x scripts/phase39-media-popup-assert.sh &amp;&amp; ./scripts/phase39-media-popup-assert.sh --section 1</automated>
    <fails_when>non-zero exit code or output containing [FAIL]</fails_when>
    <automated>grep -E 'property real mediaPillCenterX: -1' restow/quickshell/.config/quickshell/ii/GlobalStates.qml &amp;&amp; grep -E 'property var mediaPillScreen: null' restow/quickshell/.config/quickshell/ii/GlobalStates.qml</automated>
    <fails_when>exit code 1 or missing bridge property definitions</fails_when>
    <automated>./scripts/phase39-media-popup-assert.sh --syntax</automated>
    <fails_when>non-zero exit code or static AST failure</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/phase39-media-popup-assert.sh` exists, is executable, and passes Section 1 and `--syntax` checks.
    - `restow/quickshell/.config/quickshell/ii/GlobalStates.qml` contains upstream properties plus `mediaPillCenterX`, `mediaPillCenterY`, and `mediaPillScreen`.
    - `~/.config/quickshell/ii/GlobalStates.qml` is a valid leaf symlink into `restow/quickshell/`.
    - Headless Quickshell instantiates `GlobalStates` with initial sentinel values `-1` and `null`.
    - `vendor/dots-hyprland` remains 100% clean with zero git diff.
  </acceptance_criteria>
  <done>Tracer slice is proven: GlobalStates bridge properties are authored, stowed as leaf symlinks, and verified via automated test harness.</done>
</task>

<task type="auto">
  <name>Task 2: Dynamic Pill coordinate capture, multi-monitor hover gating, and reactive tracking in BarContent.qml and VerticalBarContent.qml</name>
  <read_first>
    - restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml
    - restow/quickshell/.config/quickshell/ii/modules/ii/verticalBar/VerticalBarContent.qml
    - vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/bar/StyledPopup.qml
    - .planning/phases/39-dynamic-media-popup-anchoring/39-CONTEXT.md
    - .planning/phases/39-dynamic-media-popup-anchoring/39-RESEARCH.md
  </read_first>
  <files>restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml, restow/quickshell/.config/quickshell/ii/modules/ii/verticalBar/VerticalBarContent.qml, scripts/phase39-media-popup-assert.sh</files>
  <action>
    Integrate pill center coordinate mapping (`mapToItem(null, ...)`), pointer hover disambiguation, and reactive layout tracking into `BarContent.qml` and `VerticalBarContent.qml` (per D-03, D-04, D-06, MEDIA-01).

    1. In `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml`:
       - Define coordinate conversion function on `root`:
         ```qml
         function updateMediaPillCoords() {
             if (!mediaLoader.item) return;
             const pt = mediaLoader.item.mapToItem(null, mediaLoader.item.width / 2, mediaLoader.item.height / 2);
             GlobalStates.mediaPillCenterX = pt.x;
             GlobalStates.mediaPillCenterY = pt.y;
             GlobalStates.mediaPillScreen = root.screen;
         }
         ```
       - Inside `mediaLoader` (line ~192), add a `HoverHandler`:
         ```qml
         HoverHandler {
             id: mediaHoverHandler
         }
         ```
         (Note: `HoverHandler` is non-intrusive and does not intercept inner `MouseArea` clicks in `Media.qml`).
       - Add lifecycle `Connections` targeting `GlobalStates`:
         ```qml
         Connections {
             target: GlobalStates
             function onMediaControlsOpenChanged() {
                 if (GlobalStates.mediaControlsOpen) {
                     if (mediaHoverHandler.hovered && mediaLoader.item) {
                         root.updateMediaPillCoords();
                     }
                 } else {
                     if (GlobalStates.mediaPillScreen === root.screen) {
                         GlobalStates.mediaPillCenterX = -1;
                         GlobalStates.mediaPillCenterY = -1;
                         GlobalStates.mediaPillScreen = null;
                     }
                 }
             }
         }
         ```
       - Add dynamic layout tracking `Connections` (per D-04):
         ```qml
         Connections {
             target: (GlobalStates.mediaControlsOpen && GlobalStates.mediaPillScreen === root.screen) ? mediaLoader.item : null
             function onWidthChanged() { root.updateMediaPillCoords(); }
             function onXChanged() { root.updateMediaPillCoords(); }
         }
         ```

    2. In `restow/quickshell/.config/quickshell/ii/modules/ii/verticalBar/VerticalBarContent.qml`:
       - Locate `VerticalMedia` in `middleSection` (line ~99). Add `id: verticalMedia` if not already assigned, and attach `HoverHandler { id: verticalMediaHoverHandler }`.
       - Define vertical coordinate update function on `root`:
         ```qml
         function updateVerticalMediaPillCoords() {
             if (!verticalMedia) return;
             const pt = verticalMedia.mapToItem(null, verticalMedia.width / 2, verticalMedia.height / 2);
             GlobalStates.mediaPillCenterX = pt.x;
             GlobalStates.mediaPillCenterY = pt.y;
             GlobalStates.mediaPillScreen = root.screen;
         }
         ```
       - Add lifecycle `Connections` on `GlobalStates` for open/close coordinate update and dismissal reset (`mediaPillCenterX = -1`, `mediaPillCenterY = -1`, `mediaPillScreen = null`) (per D-03).
       - Add dynamic layout tracking `Connections` for geometry shifts (`onYChanged`, `onHeightChanged`).

    3. In `scripts/phase39-media-popup-assert.sh`:
       - Expand Section 2 to assert presence of `mediaHoverHandler`, `updateMediaPillCoords`, `verticalMediaHoverHandler`, and `updateVerticalMediaPillCoords`.
       - Expand Section 4 to test multi-monitor hover isolation and coordinate lifecycle via headless runner snippet.
  </action>
  <verify>
    <automated>./scripts/phase39-media-popup-assert.sh --section 2</automated>
    <fails_when>non-zero exit code or output containing [FAIL]</fails_when>
    <automated>grep -q "mapToItem(null" restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml &amp;&amp; grep -q "mediaHoverHandler" restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml</automated>
    <fails_when>exit code 1 or missing mapToItem/mediaHoverHandler in BarContent.qml</fails_when>
    <automated>grep -q "verticalMediaHoverHandler" restow/quickshell/.config/quickshell/ii/modules/ii/verticalBar/VerticalBarContent.qml</automated>
    <fails_when>exit code 1 or missing verticalMediaHoverHandler in VerticalBarContent.qml</fails_when>
  </verify>
  <acceptance_criteria>
    - `BarContent.qml` uses `mapToItem(null, ...)` to calculate pill center coordinates and updates `GlobalStates`.
    - `BarContent.qml` filters click coordinates via `mediaHoverHandler.hovered`, preventing crosstalk from secondary monitors.
    - Reactive `Connections` update coordinates dynamically on pill width or position changes while the popup is open.
    - Popup dismissal cleanly resets `GlobalStates` coordinates to `-1` and screen to `null`.
    - `VerticalBarContent.qml` maps vertical coordinates and binds `GlobalStates.mediaPillCenterY`.
    - Section 2 static AST assertions pass with zero failures.
  </acceptance_criteria>
  <done>Coordinate capture and multi-monitor disambiguation are implemented in horizontal and vertical bars and verified via static and headless checks.</done>
</task>

<task type="auto">
  <name>Task 3: Clamped MediaControls.qml overlay, multi-monitor screen binding, and full 5-section verification suite</name>
  <read_first>
    - vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml
    - vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/Appearance.qml
    - .planning/phases/39-dynamic-media-popup-anchoring/39-CONTEXT.md
    - .planning/phases/39-dynamic-media-popup-anchoring/39-RESEARCH.md
    - .planning/phases/39-dynamic-media-popup-anchoring/39-VALIDATION.md
  </read_first>
  <files>restow/quickshell/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml, scripts/phase39-media-popup-assert.sh</files>
  <action>
    Author the minimal overlay `restow/quickshell/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml` with dynamic edge clamping and screen binding (per D-01, D-02, D-07, D-08, D-09, D-10, D-11, D-12, D-13), deploy via restow leaf symlinks, and complete the full 5-section assertion test harness (`scripts/phase39-media-popup-assert.sh`).

    1. Seed `restow/quickshell/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml`:
       - Ensure target directory exists: `mkdir -p restow/quickshell/.config/quickshell/ii/modules/ii/mediaControls`.
       - Copy `vendor/dots-hyprland/dots/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml` into `restow/quickshell/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml`.
       - Modify strictly `screen` and `margins` (per D-13):
         * On `PanelWindow` (lines ~84-86), add:
           ```qml
           screen: GlobalStates.mediaPillScreen ?? null
           ```
         * Replace `margins` block (lines ~100-105) with dynamic clamping:
           ```qml
           margins {
               top: {
                   if (Config.options.bar.vertical) {
                       if (GlobalStates.mediaPillCenterY <= 0 || !panelWindow.screen) {
                           return ((panelWindow.screen ? panelWindow.screen.height : 1080) / 2) - (root.widgetHeight * 1.5);
                       }
                       const gap = Appearance.sizes.hyprlandGapsOut;
                       const targetY = GlobalStates.mediaPillCenterY - (root.widgetHeight / 2);
                       const minY = gap;
                       const sHeight = panelWindow.screen ? panelWindow.screen.height : 1080;
                       const maxY = sHeight - root.widgetHeight - gap;
                       const clampedY = (maxY < minY) ? minY : Math.max(minY, Math.min(targetY, maxY));
                       return Math.round(clampedY);
                   }
                   return Appearance.sizes.barHeight;
               }
               bottom: Appearance.sizes.barHeight
               left: {
                   if (Config.options.bar.vertical) {
                       return Appearance.sizes.barHeight;
                   }
                   if (GlobalStates.mediaPillCenterX <= 0 || !panelWindow.screen) {
                       const sWidth = panelWindow.screen ? panelWindow.screen.width : 1920;
                       return (sWidth / 2) - (root.osdWidth / 2) - root.widgetWidth;
                   }
                   const gap = Appearance.sizes.hyprlandGapsOut;
                   const targetX = GlobalStates.mediaPillCenterX - (root.widgetWidth / 2);
                   const minX = gap;
                   const sWidth = panelWindow.screen ? panelWindow.screen.width : 1920;
                   const maxX = sWidth - root.widgetWidth - gap;
                   const clampedX = (maxX < minX) ? minX : Math.max(minX, Math.min(targetX, maxX));
                   return Math.round(clampedX);
               }
               right: Appearance.sizes.barHeight
           }
           ```
       - Preserve all other controls, MPRIS logic, cava visualizer, and shortcuts byte-identical to upstream.

    2. Deploy `MediaControls.qml` via GNU Stow:
       - Note reversibility: check if `~/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml` exists as a regular file. If so, preserve it by moving to `MediaControls.qml.bak` before stowing.
       - Ensure `~/.config/quickshell/ii/modules/ii/mediaControls` is a real directory (no folding).
       - Run `stow --verbose=5 --no-folding -d "$REPO_ROOT/restow" -t "$HOME" quickshell`.
       - Verify that `~/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml` is now a valid leaf symlink pointing into `restow/quickshell/`.

    3. Complete `scripts/phase39-media-popup-assert.sh` across all 5 sections:
       - Section 1: Symlink & Packaging Integrity:
         * Verify all 4 restow files (`GlobalStates.qml`, `BarContent.qml`, `VerticalBarContent.qml`, `MediaControls.qml`) are symlinked into `~/.config/quickshell/ii/`.
         * Verify parent directories are real directories (no folding).
         * Verify submodule `vendor/dots-hyprland` has zero git working tree churn.
       - Section 2: QML Property & Clamping Math Static AST:
         * Verify bridge properties in `GlobalStates.qml`.
         * Verify `updateMediaPillCoords` and `mediaHoverHandler` in `BarContent.qml`.
         * Verify `updateVerticalMediaPillCoords` in `VerticalBarContent.qml`.
         * Verify `screen: GlobalStates.mediaPillScreen ?? null` and `margins.left` clamping logic in `MediaControls.qml`.
       - Section 3: Headless Quickshell Coordinate Mapping & Clamping Math Execution:
         * Assert normal center alignment: on 1920x1080 display with `mediaPillCenterX = 1500`, target = `1500 - 220 = 1280`, clamped = `1280`.
         * Assert left edge clamp: with `mediaPillCenterX = 100`, target = `100 - 220 = -120`, clamped = `minX = 5` (`Appearance.sizes.hyprlandGapsOut`).
         * Assert right edge clamp: with `mediaPillCenterX = 1800`, target = `1800 - 220 = 1580`, clamped = `maxX = 1920 - 440 - 5 = 1475`.
         * Assert ultrawide clamp: on 3440x1440 display with `mediaPillCenterX = 3200`, target = `2980`, clamped = `2980`; with `mediaPillCenterX = 3400`, clamped = `3440 - 440 - 5 = 2995`.
         * Assert narrow display guard: on 400px screen (`maxX < minX`), clamps to `minX = 5` without boundary inversion.
         * Assert fallback calculation: when `mediaPillCenterX = -1`, left margin matches upstream center formula `(sWidth / 2) - (osdWidth / 2) - widgetWidth`.
         * Assert subpixel rounding: non-integer center values produce integer results via `Math.round`.
       - Section 4: Multi-Monitor Screen Binding & State Reset Lifecycle:
         * Execute headless Quickshell test confirming `PanelWindow.screen` reflects assigned screen.
         * Assert state reset on dismissal sets coordinates to `-1` and screen to `null`.
       - Section 5: Repository Integrity & Strict Verification:
         * Run `./arch/dots-hyprland.sh verify --strict` and assert output has `FAIL=0 FINDINGS=0`.
         * Compare `PORCELAIN_BEFORE` and `PORCELAIN_AFTER` to assert zero unexpected git changes.

    4. Execute full assertion suite and strict verification.
  </action>
  <verify>
    <automated>./scripts/phase39-media-popup-assert.sh</automated>
    <fails_when>non-zero exit code, output containing [FAIL], or findings count greater than 0</fails_when>
    <automated>./arch/dots-hyprland.sh verify --strict</automated>
    <fails_when>non-zero exit code or output containing [FAIL] or non-zero findings</fails_when>
  </verify>
  <acceptance_criteria>
    - `restow/quickshell/.../MediaControls.qml` is deployed as a leaf symlink to `~/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml`.
    - `MediaControls.qml` binds `screen: GlobalStates.mediaPillScreen ?? null`.
    - Margins formula correctly aligns popup center with `mediaPillCenterX` and clamps against `Appearance.sizes.hyprlandGapsOut` margins.
    - Fallback math exactly reproduces upstream center offset when `mediaPillCenterX <= 0`.
    - All 5 sections of `scripts/phase39-media-popup-assert.sh` exit 0 with `FAIL=0 FINDINGS=0`.
    - `./arch/dots-hyprland.sh verify --strict` passes with 0 findings and zero git working tree churn.
  </acceptance_criteria>
  <done>MediaControls.qml dynamic anchoring and clamping are deployed, verified across multiple resolutions, and validated by the full 5-section assertion test harness and strict dots verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Layer-Shell Surface Positioning within Wayland / Quickshell | Positioning math operates entirely within Quickshell UI layout logic without external IPC command injection or privilege escalation. |
| Shared Singleton State (`GlobalStates.qml`) | Desktop singleton properties `mediaPillCenterX`, `mediaPillCenterY`, `mediaPillScreen` are accessed only by internal Quickshell bar and popup components within the unprivileged user session. |
| Filesystem & Stow Deployment (`restow/quickshell/` to `~/.config/quickshell/`) | Leaf symlinks deployed by GNU Stow must never fold parent directories or overwrite files outside the user's config directory; upstream git submodule remains read-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Denial of Service | `MediaControls.qml` coordinate clamping | medium | mitigate | Enforce `Math.max(minX, Math.min(targetX, maxX))` with `(maxX < minX) ? minX : ...` guard to prevent negative or NaN margins on extreme aspect ratios / narrow screens (D-10, MEDIA-02). |
| T-39-02 | Information Disclosure | `GlobalStates.qml` state bridge | low | mitigate | Bridge properties store purely layout integers/screen references; no user data, MPRIS metadata, or credentials are exposed in singleton state (D-05). |
| T-39-03 | Denial of Service | `BarContent.qml` multi-monitor coordinate race | low | mitigate | Gate coordinate updates with `mediaHoverHandler.hovered` so only the display receiving the pointer click updates global state; reset coordinates cleanly on dismissal (D-06, D-08). |
| T-39-04 | Tampering | Submodule drift in `vendor/dots-hyprland` | high | mitigate | Keep `vendor/dots-hyprland` submodule 100% pristine; author all modifications exclusively in `restow/quickshell/` and verify with `dots-hyprland.sh verify --strict` (INTG-01, INTG-03). |
</threat_model>

## Artifacts this phase produces

- `restow/quickshell/.config/quickshell/ii/GlobalStates.qml`: Desktop shell singleton overlay exposing `mediaPillCenterX`, `mediaPillCenterY`, and `mediaPillScreen`.
- `restow/quickshell/.config/quickshell/ii/modules/ii/bar/BarContent.qml`: Bar content layout capturing pill center coordinates via `mapToItem(null, ...)` with `HoverHandler` multi-monitor isolation and reactive geometry tracking.
- `restow/quickshell/.config/quickshell/ii/modules/ii/verticalBar/VerticalBarContent.qml`: Vertical bar layout capturing vertical pill center coordinates via `verticalMedia.mapToItem(null, ...)`.
- `restow/quickshell/.config/quickshell/ii/modules/ii/mediaControls/MediaControls.qml`: Popup panel overlay binding `screen: GlobalStates.mediaPillScreen ?? null` and computing dynamically clamped margins with upstream fallback.
- `scripts/phase39-media-popup-assert.sh`: 5-section test harness validating symlink integrity, QML AST properties, headless coordinate mapping & clamping math across resolutions, multi-monitor lifecycle, and strict dots-hyprland verification.

## Multi-Source Coverage Audit

| Source | Item ID / Reference | Description | Status | Covered By | Notes |
|---|---|---|---|---|---|
| GOAL | ROADMAP §Phase 39 | Dynamically position `MediaControls.qml` directly beneath top status bar `Media` pill across active displays with boundary clamping. | COVERED | Task 1, Task 2, Task 3 | Complete stack established from coordinate capture in bar to clamped popup panel. |
| REQ | MEDIA-01 | `MediaControls.qml` popup dynamically anchors directly beneath top status bar `Media` pill across active monitors. | COVERED | Task 1, Task 2, Task 3 | Coords captured via `mapToItem(null, ...)` in `BarContent.qml`, bridged via `GlobalStates`, bound to `PanelWindow.screen` and `margins.left`. |
| REQ | MEDIA-02 | `MediaControls.qml` popup enforces horizontal boundary clamping (`Math.min` / `Math.max`) to prevent off-screen clipping. | COVERED | Task 3 | Symmetrical clamping using `Appearance.sizes.hyprlandGapsOut` across standard, ultrawide, and narrow screens; fallback to center when `mediaPillCenterX <= 0`. |
| REQ | INTG-01 | All QML modifications deployed via `restow/quickshell/` leaf symlinks without modifying `vendor/dots-hyprland`. | COVERED | Task 1, Task 2, Task 3 | Deployed via GNU Stow leaf symlinks; verified in Section 1. |
| REQ | INTG-02 | Automated assertion test harness validates media popup positioning. | COVERED | Task 1, Task 2, Task 3 | Implemented in `scripts/phase39-media-popup-assert.sh` across 5 sections. |
| REQ | INTG-03 | `arch/dots-hyprland.sh verify --strict` passes with 0 findings and zero git working-tree churn. | COVERED | Task 3 | Verified in Section 5 of assert harness and as final phase gate. |
| CONTEXT | D-01 | Horizontally align center of `MediaControls.qml` with center of `Media` pill (`targetLeft = mediaPillCenterX - widgetWidth / 2`). | COVERED | Task 3 | Clamping formula centers popup under pill center. |
| CONTEXT | D-02 | Keep upstream popup dimensions (`widgetWidth: 440`, `widgetHeight: 160` from `Appearance.sizes`). | COVERED | Task 3 | Widget width and height constants preserved verbatim. |
| CONTEXT | D-03 | Vertical bar parity: anchor vertically relative to `GlobalStates.mediaPillCenterY` with top/bottom boundary clamping. | COVERED | Task 2, Task 3 | `VerticalBarContent.qml` captures Y; `MediaControls.qml` clamps Y in vertical mode. |
| CONTEXT | D-04 | Reactive position tracking: update popup position on pill layout shifts without animation jitter. | COVERED | Task 2 | Reactive `Connections` on `mediaLoader.item` `onWidthChanged` and `onXChanged`. |
| CONTEXT | D-05 | `GlobalStates` state bridge: `mediaPillCenterX`, `mediaPillCenterY`, `mediaPillScreen`. | COVERED | Task 1 | Declared in `GlobalStates.qml` restow overlay. |
| CONTEXT | D-06 | `BarContent` capture point: observe `GlobalStates.mediaControlsOpen` and calculate pill coordinates using `mapToItem(null, ...)`. | COVERED | Task 2 | Implemented with `HoverHandler` isolation. |
| CONTEXT | D-07 | Multi-monitor screen binding: `screen: GlobalStates.mediaPillScreen ?? null` on `PanelWindow`. | COVERED | Task 3 | PanelWindow binds to clicked monitor's screen reference. |
| CONTEXT | D-08 | Shortcut & IPC fallback: when `mediaPillCenterX <= 0`, fallback to upstream default center margin. | COVERED | Task 3 | Fallback branch exactly reproduces upstream center formula. |
| CONTEXT | D-09 | Symmetrical Hyprland gap margins: clamp boundaries using `Appearance.sizes.hyprlandGapsOut` (5px). | COVERED | Task 3 | Clamping bounds `minX = gap`, `maxX = sWidth - widgetWidth - gap`. |
| CONTEXT | D-10 | Clamping formula with subpixel rounding: `Math.round(clampedX)`. | COVERED | Task 3 | Subpixel rounding applied to prevent Wayland blur. |
| CONTEXT | D-11 | Vertical distance from bar: match upstream `Appearance.sizes.barHeight`. | COVERED | Task 3 | Applied to `margins.top` and `margins.bottom`. |
| CONTEXT | D-12 | Silent clamping: do not render pointer arrows or offset indicators. | COVERED | Task 3 | Silent boundary pinning without UI callouts. |
| CONTEXT | D-13 | Minimal 4-line `MediaControls.qml` override: modify strictly `screen` and `margins`. | COVERED | Task 3 | Keeps internal controls, MPRIS logic, and shortcuts byte-identical. |
| RESEARCH | PATTERNS §1 | `GlobalStates.qml` Singleton Property Pattern. | COVERED | Task 1 | Seeded and bridge properties added. |
| RESEARCH | PATTERNS §2 | `BarContent.qml` HoverHandler & `mapToItem` Pattern. | COVERED | Task 2 | Implemented in horizontal bar. |
| RESEARCH | PATTERNS §3 | `VerticalBarContent.qml` Vertical Hover & `mapToItem` Pattern. | COVERED | Task 2 | Implemented in vertical bar. |
| RESEARCH | PATTERNS §4 | `MediaControls.qml` PanelWindow Screen Binding & Clamped Margins Pattern. | COVERED | Task 3 | Implemented with fallback. |
| RESEARCH | PATTERNS §5 | `scripts/phase39-media-popup-assert.sh` 5-Section Test Harness Pattern. | COVERED | Task 1, Task 2, Task 3 | Implemented across all 5 sections. |

<verification>
Execute the full test harness:
`./scripts/phase39-media-popup-assert.sh`
Assert strict repository cleanliness:
`./arch/dots-hyprland.sh verify --strict`
</verification>

<success_criteria>
1. `MediaControls.qml` computes horizontal position dynamically centered beneath the `Media` pill.
2. Boundary clamping maintains `Appearance.sizes.hyprlandGapsOut` margins on left and right monitor edges across 1920x1080 standard, 3440x1440 ultrawide, and narrow screens.
3. Fallback calculation exactly reproduces upstream center math when `mediaPillCenterX <= 0`.
4. Multi-monitor screen binding opens popup on the display where the pill was activated.
5. All 4 QML modifications are deployed as leaf symlinks via `restow/quickshell/` without modifying `vendor/dots-hyprland`.
6. `./scripts/phase39-media-popup-assert.sh` passes all 5 sections with FAIL=0 FINDINGS=0.
7. `./arch/dots-hyprland.sh verify --strict` passes with 0 findings and zero git working tree churn.
</success_criteria>

<output>
Create `.planning/phases/39-dynamic-media-popup-anchoring/39-01-SUMMARY.md` when done
</output>
