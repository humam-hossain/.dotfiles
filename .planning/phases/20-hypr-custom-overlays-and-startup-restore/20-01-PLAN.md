---
phase: 20-hypr-custom-overlays-and-startup-restore
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh
autonomous: true
requirements: [SAFE-01]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "A dedicated assert harness `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` exists, is executable, and follows the repo standard (`set -euo pipefail`, four prefixes `[PASS]`/`[FAIL]`/`[FINDING]`/`[INFO]`, two counters `FAIL`/`FINDINGS`)"
    - "Section 1 verifies SAFE-01 isolated scratch fixture backup creation, `stow -n --no-folding` dry-run, and unmanaged stub pruning non-destructively without modifying `$HOME`"
    - "Section 2 verifies SAFE-01 isolated scratch fixture one-line undo drill (`stow -D` and backup restoration) and re-stow, confirming exact content restoration"
    - "The assert script safely handles section filtering (`--section N`) and cleans up all temporary files and scratch directories via an EXIT trap"
  artifacts:
    - scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh
  key_links:
    - "Section 1 and 2 tests run against isolated `mktemp -d` scratch trees and prove the exact migration and escape sequence before live execution in Plan 20-04"
---

<objective>
Build the assertion test harness foundation (`scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh`) for Phase 20, establishing the test framework, trap cleanup, CLI section filtering, and Sections 1 and 2 that prove the SAFE-01 backup, dry-run, and one-line undo escape route logic non-destructively in isolated fixtures.

Purpose: Guarantee that no live configuration files are touched until the migration drill procedure and its undo mechanism are proven safe and reversible in an isolated scratch environment.
Output: `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` with passing Sections 1 and 2.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-CONTEXT.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-RESEARCH.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-PATTERNS.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-VALIDATION.md
@scripts/phase19-link-aware-verify-assert.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold assert test harness with Section 1 (SAFE-01 scratch backup and dry-run)</name>
  <files>scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh</files>
  <read_first>
    - scripts/phase19-link-aware-verify-assert.sh (lines 64-105, 1720-1732)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-CONTEXT.md (D-20, D-21, D-22)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-RESEARCH.md (lines 263-289, 533-559)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-PATTERNS.md (lines 289-332, 501-516)
  </read_first>
  <action>
    Create `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` with permissions 0755 adhering strictly to the dotfiles assert harness contract (D-22):
    1. Shebang `#!/usr/bin/env bash`, header comment noting Phase 20 purpose (D-22), `set -euo pipefail`.
    2. Resolve `REPO_ROOT` as `$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)` and `cd "$REPO_ROOT"`.
    3. Initialize counters `FAIL=0` and `FINDINGS=0`, and define standard helpers: `pass()`, `fail()`, `finding()`, `info()`.
    4. Set up dynamic cleanup array `TMP_FILES=()` and `SCRATCH_ROOTS=()`. Implement `cleanup()` function that iterates over `TMP_FILES` removing them, and iterates over `SCRATCH_ROOTS` applying `chmod -R u+rwX` before `rm -rf`, registered via `trap cleanup EXIT`.
    5. Implement CLI argument parsing supporting `--section <N>` (where N is 1 to 7) to run a specific section, or running all sections if omitted.
    6. Implement Section 1: "SAFE-01: isolated fixture backup, stub pruning, and stow dry-run" (D-20, D-21):
       - Create isolated scratch directory via `mktemp -d /tmp/p20-assert-s1-XXXXXX` and append to `SCRATCH_ROOTS`.
       - Construct a mock repository structure with a package directory `repo/stow/testpkg/.config/testpkg/target.lua` containing `return "managed"`.
       - Construct a mock `$HOME` directory `home/.config/testpkg/` containing an unmanaged plain file `target.lua` with `return "legacy stub"`.
       - Verify timestamped backup creation: copy `home/.config/testpkg` to `home/.config/testpkg.backup.$(date +%s)` and assert the backup directory exists and contains `target.lua` with content `return "legacy stub"`.
       - Verify stub pruning: remove `home/.config/testpkg/target.lua` and assert `test ! -e`.
       - Verify dry-run stow: run `stow -n -v --no-folding -d "$DRILL_ROOT/repo/stow" -t "$DRILL_ROOT/home" testpkg`. Assert exit code is 0 and output confirms dry-run would link without conflict.
       - Run actual stow in fixture: run `stow -v --no-folding -d "$DRILL_ROOT/repo/stow" -t "$DRILL_ROOT/home" testpkg`. Assert exit code is 0, `test -L "$DRILL_ROOT/home/.config/testpkg/target.lua"` is true, and `test "$DRILL_ROOT/repo/stow/testpkg/.config/testpkg/target.lua" -ef "$DRILL_ROOT/home/.config/testpkg/target.lua"` passes.
    7. Add terminal summary block printing `=== done: FAIL=${FAIL} FINDINGS=${FINDINGS} ===` and exiting with code 1 if `FAIL > 0`, else code 0.
  </action>
  <acceptance_criteria>
    - `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` exists and is executable (`test -x`).
    - `bash -n scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` passes with 0 syntax errors.
    - Executing `./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 1` exits 0.
    - Output of Section 1 prints `[PASS]` lines for backup directory creation, stub pruning, dry-run stow, and symlink inode identity.
    - Temporary directories are cleaned up on exit (`ls /tmp/p20-assert-s1-*` returns nothing).
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 1</automated>
  </verify>
  <done>Section 1 runs cleanly in isolated fixture proving SAFE-01 backup and dry-run invariants.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Section 2 (SAFE-01 non-destructive one-line undo drill in scratch fixture)</name>
  <files>scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh</files>
  <read_first>
    - scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh (from Task 1)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-CONTEXT.md (D-21)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-PATTERNS.md (lines 398-430)
  </read_first>
  <action>
    Add Section 2 to `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh`: "SAFE-01: isolated fixture live undo drill and re-stow rehearsal" (D-21):
    1. Create an isolated scratch fixture `DRILL_ROOT="$(mktemp -d /tmp/p20-assert-s2-XXXXXX)"` and register with `SCRATCH_ROOTS`.
    2. Set up mock repo `repo/stow/hypr_drill/.config/hypr/custom/{variables.lua,keybinds.lua,rules.lua}` with contents `return "repo"`, and mock live home `home/.config/hypr/custom/` with regular files containing `return "legacy stub"`.
    3. Take timestamped backup `BACKUP_DIR="$DRILL_ROOT/home/.config/hypr/custom.backup.$(date +%s)"` using `cp -a`.
    4. Remove stubs in `home/.config/hypr/custom/` and execute `stow --no-folding -d "$DRILL_ROOT/repo/stow" -t "$DRILL_ROOT/home" hypr_drill`.
    5. Assert all 3 target files are symlinks (`test -L`).
    6. Execute the documented one-line undo drill:
       Run `stow -D --no-folding -d "$DRILL_ROOT/repo/stow" -t "$DRILL_ROOT/home" hypr_drill`.
       Assert symlinks are removed (`test ! -L` and `test ! -e`).
       Restore live files from backup: `cp -a "$BACKUP_DIR/." "$DRILL_ROOT/home/.config/hypr/custom/"`.
       Assert target files are restored as regular files containing `return "legacy stub"` (`test ! -L` and `grep -q "legacy stub"`).
    7. Execute re-stow following undo:
       Prune stubs again (`rm "$DRILL_ROOT/home/.config/hypr/custom/"*.lua`).
       Run `stow --no-folding -d "$DRILL_ROOT/repo/stow" -t "$DRILL_ROOT/home" hypr_drill`.
       Assert symlinks are cleanly restored and point to repo files (`test "$DRILL_ROOT/repo/stow/hypr_drill/.config/hypr/custom/variables.lua" -ef "$DRILL_ROOT/home/.config/hypr/custom/variables.lua"`).
    8. Emit `[PASS]` for each stage of the drill.
  </action>
  <acceptance_criteria>
    - Executing `./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 2` exits 0.
    - Output of Section 2 logs `[PASS]` for initial stow, `[PASS]` for unstow (`stow -D`), `[PASS]` for backup restore, and `[PASS]` for re-stow.
    - Real `$HOME/.config/hypr` is completely untouched by the drill test.
    - Scratch directories are wiped by the EXIT trap.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 2</automated>
  </verify>
  <done>Section 2 automated drill fixture proves that the SAFE-01 escape route and re-stow sequence works predictably and safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixture → Live filesystem | Assert test fixtures must remain completely quarantined within `/tmp/` and must never delete or modify real user files in `$HOME` |
| GNU Stow → Live filesystem | Stow invocations must use explicit target directories and universal `--no-folding` to prevent accidental symlink folding of parent directories |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering | Assert test fixture | high | mitigate | Confine all fixture operations to `mktemp -d /tmp/p20-...`, install EXIT trap immediately after directory creation, and use `chmod -R u+rwX` before deletion |
| T-20-02 | Denial of Service | Backup/Undo drill logic | high | mitigate | Assert harness tests the undo drill non-destructively in scratch space before any real stow operation touches `~/.config/hypr/custom` in Plan 20-04 |
| T-20-SC | Tampering | Script dependencies | low | accept | Harness relies only on standard system binaries already installed from Arch core (`bash`, `stow`, `mktemp`, `rm`, `cp`, `chmod`) |
</threat_model>

<verification>
Automated test command:
`./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 1 && ./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 2`
Expected outcome: Both sections report `[PASS]` and exit 0 with `FAIL=0 FINDINGS=0`.
</verification>

<success_criteria>
1. `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` exists and is executable.
2. Sections 1 and 2 pass in isolated scratch fixtures without touching real user files.
3. SAFE-01 backup, stub removal, dry-run stow, `stow -D` undo, and re-stow mechanics are validated by automated tests.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` — Test harness script with framework helpers, trap cleanup, CLI section filter, and Sections 1 and 2 assert checks.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-01-SUMMARY.md` when done
</output>
