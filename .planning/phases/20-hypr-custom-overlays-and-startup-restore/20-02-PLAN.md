---
phase: 20-hypr-custom-overlays-and-startup-restore
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - stow/hypr/.config/hypr/custom/variables.lua
  - stow/hypr/.config/hypr/custom/rules.lua
  - stow/hypr/.config/hypr/custom/env.lua
  - stow/hypr/.config/hypr/custom/execs.lua
  - /home/pera/.config/gtk-3.0/settings.ini
  - /home/pera/.config/xsettingsd/xsettingsd.conf
  - scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh
autonomous: true
requirements: [HYPR-03, START-01]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "`stow/hypr/.config/hypr/custom/variables.lua` explicitly defines primary application variables (`terminal`, `browser`, `fileManager`, `textEditor`, `taskManager`, `officeSoftware`, `workspaceGroupSize`, `qsConfig`) per HYPR-03 and D-16 without modifying vendor files"
    - "`stow/hypr/.config/hypr/custom/rules.lua` defines floating window rules for Python graphical tools per D-17"
    - "`stow/hypr/.config/hypr/custom/env.lua` has authoring header and remains an empty require slot per D-18"
    - "`stow/hypr/.config/hypr/custom/execs.lua` restores Polkit KDE agent, workspace 1 Chrome and kitty+tmux, and special workspace btop and discord/vesktop autostarts inside `hl.on('hyprland.start', ...)` per START-01, D-01, D-02, while preserving the START-02 session target start (D-05) and dropping `wl-clip-persist` (D-03)"
    - "`~/.config/gtk-3.0/settings.ini` and `~/.config/xsettingsd/xsettingsd.conf` are aligned to `Bibata-Modern-Classic 24` eliminating legacy Catppuccin cursor overrides per D-04"
    - "Submodule directory `vendor/dots-hyprland` has zero modifications (`git diff --exit-code vendor/dots-hyprland` passes)"
    - "Assert script sections 3, 4, and 5 pass (`FAIL=0`)"
  artifacts:
    - stow/hypr/.config/hypr/custom/variables.lua
    - stow/hypr/.config/hypr/custom/rules.lua
    - stow/hypr/.config/hypr/custom/env.lua
    - stow/hypr/.config/hypr/custom/execs.lua
    - /home/pera/.config/gtk-3.0/settings.ini
    - /home/pera/.config/xsettingsd/xsettingsd.conf
  key_links:
    - "Application preferences in `custom/variables.lua` are required by upstream `hyprland/keybinds.lua:3-5` before any keybindings are registered"
    - "Autostarts in `custom/execs.lua` are registered strictly inside `hl.on('hyprland.start')` to prevent re-execution on config reload"
---

<objective>
Author the repository-side custom overlay configurations (`stow/hypr/.config/hypr/custom/{variables,rules,env,execs}.lua`) and align cursor theme settings across GTK-3.0 and xsettingsd.

Purpose: Deliver personal application launcher defaults (HYPR-03) bypassing upstream discovery loops without vendor drift, restore the lost autostart programs (START-01) inside the single-fire startup hook, and eliminate legacy Catppuccin cursor overrides across desktop toolkits.
Output: Authored custom overlay Lua configs in `stow/hypr/`, aligned cursor configs in `~/.config/`, and Sections 3, 4, 5 active in the assert script.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-CONTEXT.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-RESEARCH.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-PATTERNS.md
@.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-01-SUMMARY.md
@vendor/dots-hyprland/dots/.config/hypr/hyprland/variables.lua
@vendor/dots-hyprland/dots/.config/hypr/hyprland/rules.lua
@vendor/dots-hyprland/dots/.config/hypr/hyprland/execs.lua
@docs/archive/hyprland.conf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author repo-side variables, rules, and env overlays, and wire Section 3 asserts</name>
  <files>stow/hypr/.config/hypr/custom/variables.lua, stow/hypr/.config/hypr/custom/rules.lua, stow/hypr/.config/hypr/custom/env.lua, scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh</files>
  <read_first>
    - vendor/dots-hyprland/dots/.config/hypr/hyprland/variables.lua
    - vendor/dots-hyprland/dots/.config/hypr/hyprland/keybinds.lua (lines 1-6)
    - vendor/dots-hyprland/dots/.config/hypr/hyprland/rules.lua (lines 28-35)
    - docs/archive/hyprland.conf (lines 458-475)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-CONTEXT.md (D-16, D-17, D-18)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-PATTERNS.md (lines 98-152, 195-216)
  </read_first>
  <action>
    Author repository-side files under `stow/hypr/.config/hypr/custom/` with the standard authoring single-source-of-truth comment header (D-16, D-17, D-18):
    1. In `stow/hypr/.config/hypr/custom/variables.lua`:
       - Add authoring header comment: `-- Authoring SoT: parent-repo .config/hypr/custom/ (see 13-SOT-APPLY.md). Do not commit into vendor/dots-hyprland.`
       - Lock primary application preferences per D-16:
         `terminal = "kitty"`
         `browser = "google-chrome-stable"`
         `fileManager = "dolphin"`
         `textEditor = "kitty -e nvim"`
         `taskManager = "kitty --class btop -e btop"`
         `officeSoftware = "libreoffice"`
         `workspaceGroupSize = 10`
         `hl.env("qsConfig", "ii")`
       - Add explanatory comment noting that secondary variables (`codeEditor`, `volumeMixer`, `settingsApp`) fall back to upstream `launch_first_available.sh`.
    2. In `stow/hypr/.config/hypr/custom/rules.lua`:
       - Add authoring header comment.
       - Define floating rules for python graphical tools from pre-adopt config (D-17):
         `hl.window_rule({ match = { class = "^(main.py)$" }, float = true })`
         `hl.window_rule({ match = { class = "^(python3)$" }, float = true })`
    3. In `stow/hypr/.config/hypr/custom/env.lua`:
       - Add authoring header comment.
       - Include comment block per D-18 documenting that upstream `hyprland/env.lua` already sets Wayland, Qt platform themes, and virtualenv paths, leaving this file as an empty require slot for personal environment additions.
    4. In `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh`:
       - Implement Section 3: "HYPR-03: application defaults, rules, and vendor isolation" (D-16, D-17, D-18):
         - Assert all primary variables in `stow/hypr/.config/hypr/custom/variables.lua` match expected strings.
         - Assert `hl.window_rule` calls for `main.py` and `python3` exist in `stow/hypr/.config/hypr/custom/rules.lua`.
         - Assert `vendor/dots-hyprland` has zero changes using `git diff --exit-code vendor/dots-hyprland`.
  </action>
  <acceptance_criteria>
    - `stow/hypr/.config/hypr/custom/variables.lua` contains all 8 locked settings per D-16.
    - `stow/hypr/.config/hypr/custom/rules.lua` contains both python float rules per D-17.
    - `stow/hypr/.config/hypr/custom/env.lua` contains the authoring header per D-18.
    - `git diff --exit-code vendor/dots-hyprland` exits 0 (clean submodule).
    - Executing `./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 3` exits 0 and reports `[PASS]`.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 3</automated>
  </verify>
  <done>Application defaults, window rules, and env require slot authored cleanly with zero submodule drift.</done>
</task>

<task type="auto">
  <name>Task 2: Author autostart entries in execs.lua and wire Section 4 asserts</name>
  <files>stow/hypr/.config/hypr/custom/execs.lua, scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh</files>
  <read_first>
    - stow/hypr/.config/hypr/custom/execs.lua
    - vendor/dots-hyprland/dots/.config/hypr/hyprland/execs.lua
    - docs/archive/hyprland.conf (lines 55-106)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-CONTEXT.md (D-01, D-02, D-03, D-05)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-PATTERNS.md (lines 153-193, 465-474)
  </read_first>
  <action>
    Update `stow/hypr/.config/hypr/custom/execs.lua` to restore the pre-adopt autostart applications inside `hl.on("hyprland.start", function () ... end)` (START-01, D-01, D-02, D-03, D-05):
    1. Retain the existing START-02 screen-share service start call: `hl.exec_cmd("systemctl --user start hyprland-session.service")` along with its full architectural explanatory comments.
    2. Add Polkit KDE authentication agent execution (D-02):
       `hl.exec_cmd("/usr/lib/polkit-kde-authentication-agent-1")`
    3. Add workspace-pinned autostart applications using direct string syntax per D-01:
       `hl.exec_cmd("[workspace 1] google-chrome-stable --profile-directory='Default' --ozone-platform-hint=auto")`
       `hl.exec_cmd("[workspace 1] kitty -e tmux")`
       `hl.exec_cmd("[workspace special:btop silent] kitty --class btop -e btop")`
       `hl.exec_cmd("[workspace special:social silent] sh -c 'command -v vesktop >/dev/null 2>&1 && exec vesktop || exec discord'")`
    4. Confirm that all autostart commands sit strictly INSIDE the `hl.on("hyprland.start", ...)` callback so they run only at session startup and do not re-spawn on compositor config reload.
    5. Confirm that `wl-clip-persist` is omitted per D-03 (Quickshell cliphist clipboard daemon active).
    6. In `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh`:
       - Implement Section 4: "START-01: startup restore and lifecycle containment" (D-01, D-02, D-03, D-05):
         - Assert `stow/hypr/.config/hypr/custom/execs.lua` contains `hl.on("hyprland.start"`.
         - Assert all 6 commands (`hyprland-session.service`, `polkit-kde-authentication-agent-1`, `google-chrome-stable`, `kitty -e tmux`, `special:btop`, `special:social`) exist inside the callback.
         - Assert `wl-clip-persist` is absent from `stow/hypr/.config/hypr/custom/execs.lua`.
  </action>
  <acceptance_criteria>
    - `stow/hypr/.config/hypr/custom/execs.lua` contains the 6 autostart calls inside `hl.on("hyprland.start", ...)`.
    - `wl-clip-persist` does not appear in `stow/hypr/.config/hypr/custom/execs.lua`.
    - Executing `./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 4` exits 0 and reports `[PASS]`.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 4</automated>
  </verify>
  <done>Startup applications restored inside the single-fire lifecycle hook without duplicate daemons.</done>
</task>

<task type="auto">
  <name>Task 3: Align cursor theme settings in GTK-3.0 and xsettingsd, and wire Section 5 asserts</name>
  <files>/home/pera/.config/gtk-3.0/settings.ini, /home/pera/.config/xsettingsd/xsettingsd.conf, scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh</files>
  <read_first>
    - /home/pera/.config/gtk-3.0/settings.ini
    - /home/pera/.config/xsettingsd/xsettingsd.conf
    - vendor/dots-hyprland/dots/.config/hypr/hyprland/execs.lua (line 24)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-CONTEXT.md (D-04)
    - .planning/phases/20-hypr-custom-overlays-and-startup-restore/20-PATTERNS.md (lines 257-286, 491-500)
  </read_first>
  <action>
    Align cursor theme settings in `~/.config/gtk-3.0/settings.ini` and `~/.config/xsettingsd/xsettingsd.conf` to `Bibata-Modern-Classic` with size 24 (D-04):
    1. In `/home/pera/.config/gtk-3.0/settings.ini`:
       - Replace `gtk-cursor-theme-name=catppuccin-mocha-blue-cursors` with `gtk-cursor-theme-name=Bibata-Modern-Classic`.
       - Replace `gtk-cursor-theme-size=30` with `gtk-cursor-theme-size=24`.
    2. In `/home/pera/.config/xsettingsd/xsettingsd.conf`:
       - Replace `Gtk/CursorThemeName "catppuccin-mocha-blue-cursors"` with `Gtk/CursorThemeName "Bibata-Modern-Classic"`.
       - Ensure `Gtk/CursorThemeSize 24` is present immediately following the theme name.
    3. In `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh`:
       - Implement Section 5: "D-04: cursor theme consistency across toolkits":
         - Assert `grep -q '^gtk-cursor-theme-name=Bibata-Modern-Classic$' "$HOME/.config/gtk-3.0/settings.ini"`.
         - Assert `grep -q '^gtk-cursor-theme-size=24$' "$HOME/.config/gtk-3.0/settings.ini"`.
         - Assert `grep -q '^Gtk/CursorThemeName "Bibata-Modern-Classic"$' "$HOME/.config/xsettingsd/xsettingsd.conf"`.
         - Assert `grep -q '^Gtk/CursorThemeSize 24$' "$HOME/.config/xsettingsd/xsettingsd.conf"`.
         - Assert absence of legacy string `catppuccin-mocha-blue-cursors` in both files.
  </action>
  <acceptance_criteria>
    - `/home/pera/.config/gtk-3.0/settings.ini` contains `gtk-cursor-theme-name=Bibata-Modern-Classic` and `gtk-cursor-theme-size=24`.
    - `/home/pera/.config/xsettingsd/xsettingsd.conf` contains `Gtk/CursorThemeName "Bibata-Modern-Classic"` and `Gtk/CursorThemeSize 24`.
    - Neither file contains `catppuccin-mocha-blue-cursors`.
    - Executing `./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 5` exits 0 and reports `[PASS]`.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 5</automated>
  </verify>
  <done>Cursor theme unified to Bibata-Modern-Classic 24 across GTK and XWayland configurations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo config → Submodule isolation | Edits to personal configuration overlays must reside strictly within `stow/hypr/` and must never pollute `vendor/dots-hyprland` |
| Autostart hook → Process table | Autostarts must be registered in the single-fire lifecycle handler to avoid process multiplication and memory exhaustion on config reload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-03 | Tampering | Git submodule tracking | medium | mitigate | All personal configurations authored in `stow/hypr/.config/hypr/custom/`; assert Section 3 enforces `git diff --exit-code vendor/dots-hyprland` |
| T-20-04 | Denial of Service | Hyprland Lua autostart | high | mitigate | Encapsulate all autostart commands inside `hl.on("hyprland.start", ...)` callback, preventing re-spawning on `hyprctl reload` |
| T-20-05 | Elevation of Privilege | Polkit KDE agent | high | mitigate | Restore explicit autostart of `/usr/lib/polkit-kde-authentication-agent-1` to ensure GUI root authentication prompts succeed |
</threat_model>

<verification>
Automated test command:
`./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 3 && ./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 4 && ./scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh --section 5`
Expected outcome: All three sections report `[PASS]` and exit 0 with `FAIL=0 FINDINGS=0`.
</verification>

<success_criteria>
1. `stow/hypr/.config/hypr/custom/{variables,rules,env,execs}.lua` exist with complete personal configurations.
2. `vendor/dots-hyprland` submodule is completely clean.
3. GTK-3.0 and xsettingsd cursor theme settings are aligned to Bibata-Modern-Classic 24.
4. Assert sections 3, 4, and 5 pass.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- `stow/hypr/.config/hypr/custom/variables.lua` — Personal app launcher variables (HYPR-03, D-16).
- `stow/hypr/.config/hypr/custom/rules.lua` — Window rules for python tools (D-17).
- `stow/hypr/.config/hypr/custom/env.lua` — Require slot header (D-18).
- `stow/hypr/.config/hypr/custom/execs.lua` — Single-fire autostarts for polkit agent, Chrome, kitty, btop, discord (START-01, D-01, D-02).
- `/home/pera/.config/gtk-3.0/settings.ini` — Aligned cursor theme setting (D-04).
- `/home/pera/.config/xsettingsd/xsettingsd.conf` — Aligned cursor theme setting (D-04).
- `scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh` — Extended with Sections 3, 4, and 5.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/20-hypr-custom-overlays-and-startup-restore/20-02-SUMMARY.md` when done
</output>
