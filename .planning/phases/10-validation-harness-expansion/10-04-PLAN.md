---
phase: 10-validation-harness-expansion
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - .config/nvim/lua/core/keymaps/whichkey.lua
  - .config/nvim/lua/config/health.lua
  - .planning/tmp/nvim-validate/config-warning-families.tsv
  - .planning/phases/06-runtime-failure-inventory/FAILURES.md
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "A fresh `checkhealth` run distinguishes config-caused warnings from by-design or environment-only warnings."
    - "Every config-caused warning family found by the fresh audit is captured with exact warning text and owner file, then removed in this phase."
    - "FAILURES.md records the disposition of current warning findings as a living document."
  artifacts:
    - path: ".config/nvim/lua/core/keymaps/whichkey.lua"
      provides: "Targeted which-key duplicate-prefix warning fix if the audit confirms repo ownership"
    - path: ".config/nvim/lua/config/health.lua"
      provides: "Targeted repo-owned health warning fix if the audit confirms repo ownership"
    - path: ".planning/tmp/nvim-validate/config-warning-families.tsv"
      provides: "Exact warning-text manifest for every repo-owned config-caused warning discovered by Task 1"
    - path: ".planning/phases/06-runtime-failure-inventory/FAILURES.md"
      provides: "Updated warning audit results and dispositions"
  key_links:
    - from: ".config/nvim/lua/core/keymaps/whichkey.lua|.config/nvim/lua/config/health.lua"
      to: ".planning/tmp/nvim-validate/checkhealth.txt"
      via: "fresh warning audit"
      pattern: "Duplicates for <<leader>e>|Duplicates for <<leader>b>|vim.lsp.buf_get_clients() is deprecated"
    - from: ".planning/tmp/nvim-validate/config-warning-families.tsv"
      to: ".planning/tmp/nvim-validate/checkhealth.txt"
      via: "exact warning-text manifest"
      pattern: "every repo-owned config-caused warning string must disappear after remediation"
    - from: ".planning/phases/06-runtime-failure-inventory/FAILURES.md"
      to: ".planning/tmp/nvim-validate/checkhealth.txt"
      via: "warning classification entries"
      pattern: "WARNING|By Design|Won't Fix|Fixed"
---

<objective>
Run a fresh warning audit and remove config-caused warning noise without pretending environment warnings are repo bugs.

Purpose: Implement D-13 through D-16 by re-running `checkhealth`, fixing every config-caused warning family where the repo owns the root cause, and recording the rest as By Design or Won't Fix in the living failure inventory.
Output: Cleaned repo-owned warning surface for all findings warranted by the audit, plus updated `FAILURES.md` warning dispositions.
</objective>

<execution_context>
@/home/pera/.codex/get-shit-done/workflows/execute-plan.md
@/home/pera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-validation-harness-expansion/10-CONTEXT.md
@.planning/phases/10-validation-harness-expansion/10-RESEARCH.md
@.planning/phases/06-runtime-failure-inventory/06-CONTEXT.md
@.planning/phases/09-health-signal-cleanup/09-01-SUMMARY.md
@.planning/tmp/nvim-validate/checkhealth.txt
@.config/nvim/lua/core/keymaps/whichkey.lua
@.planning/phases/06-runtime-failure-inventory/FAILURES.md

<interfaces>
From .config/nvim/lua/core/keymaps/whichkey.lua:
```lua
for _, g in ipairs(groups) do
  which_key.add({
    { "<leader>" .. g.prefix, group = g.label },
  })
end
...
table.insert(global_specs, {
  map.lhs,
  desc = map.desc,
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run checkhealth and classify every current WARNING finding</name>
  <read_first>
    .planning/phases/10-validation-harness-expansion/10-CONTEXT.md
    .planning/phases/09-health-signal-cleanup/09-01-SUMMARY.md
    .planning/tmp/nvim-validate/checkhealth.txt
    .planning/phases/06-runtime-failure-inventory/FAILURES.md
  </read_first>
  <files>.planning/phases/06-runtime-failure-inventory/FAILURES.md and `.planning/tmp/nvim-validate/config-warning-families.tsv`</files>
  <acceptance_criteria>
    - `./scripts/nvim-validate.sh checkhealth` completes and refreshes `.planning/tmp/nvim-validate/checkhealth.txt`.
    - `.planning/tmp/nvim-validate/config-warning-families.tsv` exists and lists one repo-owned config-caused warning per line in the format `exact warning text<TAB>owner file path`.
    - `rg -n "which-key|lazy\\.nvim|mason|vim\\.provider|vim\\.deprecated|By Design|Won't Fix|config-caused" .planning/phases/06-runtime-failure-inventory/FAILURES.md` returns matches in the new warning audit notes.
    - `FAILURES.md` explicitly records whether the which-key duplicate-prefix warnings are config-caused or informational-by-design.
  </acceptance_criteria>
  <action>
    Start exactly as D-14 requires: run `./scripts/nvim-validate.sh checkhealth`, read the refreshed `.planning/tmp/nvim-validate/checkhealth.txt`, and enumerate every WARNING family currently present. Record the classification in `.planning/phases/06-runtime-failure-inventory/FAILURES.md` using the living-doc pattern from Phase 6 and Phase 9: config-caused, environment-only, or optional-tool gap. At minimum classify the currently visible families from the existing artifact: lazy luarocks lua-5.1 warnings, mason missing Ruby/PHP/Julia helpers, render-markdown latex tool warning, vim.provider host-package warnings, vim.deprecated third-party warning, and which-key overlap/duplicate-prefix warnings. Mark environment-only and optional-tool findings as `By Design` or `Won't Fix` with the reason written inline. For every warning family classified as config-caused and owned by repo code, append one line to `.planning/tmp/nvim-validate/config-warning-families.tsv` in the format `exact warning text<TAB>owner file path`; use the exact warning text emitted in `checkhealth.txt`, not a summary label.
  </action>
  <verify>
    <automated>./scripts/nvim-validate.sh checkhealth && test -f .planning/tmp/nvim-validate/config-warning-families.tsv && rg -n "which-key|lazy\\.nvim|mason|vim\\.provider|vim\\.deprecated|By Design|Won't Fix|config-caused" .planning/phases/06-runtime-failure-inventory/FAILURES.md</automated>
  </verify>
  <done>`FAILURES.md` carries a fresh Phase 10 warning audit with dispositions for every current warning family instead of a stale copied list.</done>
</task>

<task type="auto">
  <name>Task 2: Fix every repo-owned config-caused warning captured by Task 1</name>
  <read_first>
    .planning/phases/10-validation-harness-expansion/10-CONTEXT.md
    .planning/tmp/nvim-validate/checkhealth.txt
    .planning/tmp/nvim-validate/config-warning-families.tsv
    .config/nvim/lua/core/keymaps/whichkey.lua
    .config/nvim/lua/core/keymaps/registry.lua
    .config/nvim/lua/config/health.lua
  </read_first>
  <files>repo-owned config files named in `.planning/tmp/nvim-validate/config-warning-families.tsv`, plus `.planning/phases/06-runtime-failure-inventory/FAILURES.md`</files>
  <acceptance_criteria>
    - For every line in `.planning/tmp/nvim-validate/config-warning-families.tsv`, `./scripts/nvim-validate.sh checkhealth` produces no matching warning text in `.planning/tmp/nvim-validate/checkhealth.txt` after remediation.
    - If `.planning/tmp/nvim-validate/config-warning-families.tsv` contains `Duplicates for <<leader>e> in mode \`n\`:` or `Duplicates for <<leader>b> in mode \`n\`:` entries, `rg -n "Duplicates for <<leader>e>|Duplicates for <<leader>b>" .planning/tmp/nvim-validate/checkhealth.txt` returns no matches after the fix.
    - If `.planning/tmp/nvim-validate/config-warning-families.tsv` contains `vim.lsp.buf_get_clients() is deprecated. Feature was removed in Nvim 0.12`, `rg -n "vim\\.lsp\\.buf_get_clients\\(\\) is deprecated\\. Feature was removed in Nvim 0\\.12" .planning/tmp/nvim-validate/checkhealth.txt` returns no matches after the fix.
    - Any modified repo config file still contains the interface anchors required by its area, such as `registry\\.groups|registry\\.get_by_scope\\(\"global\"\\)|registry\\.get_by_scope\\(\"lazy\"\\)` in `.config/nvim/lua/core/keymaps/whichkey.lua` when which-key code changes are part of the remediation.
  </acceptance_criteria>
  <action>
    Use `.planning/tmp/nvim-validate/config-warning-families.tsv` as the source of truth and remediate every warning family listed there in the repo-owned file named on that line. If the manifest lists the `which-key` duplicate-prefix warnings for `<leader>e` and `<leader>b`, modify `core/keymaps/whichkey.lua` so group registration skips any `<leader><prefix>` group whose exact lhs is already claimed by a real mapping in the registry while preserving existing global and lazy mapping description registration. If the manifest lists `vim.lsp.buf_get_clients() is deprecated. Feature was removed in Nvim 0.12`, update `.config/nvim/lua/config/health.lua` to use the non-deprecated API path and remove the warning. If Task 1 captures any additional repo-owned config-caused warning, fix that warning in the exact owner file recorded in the manifest during this task; do not leave any manifest entry unresolved when Task 2 completes.
  </action>
  <verify>
    <automated>./scripts/nvim-validate.sh checkhealth && while IFS=$'\t' read -r warning owner; do [ -z "$warning" ] && continue; if rg -F "$warning" .planning/tmp/nvim-validate/checkhealth.txt; then exit 1; fi; done < .planning/tmp/nvim-validate/config-warning-families.tsv</automated>
  </verify>
  <done>Every repo-owned config-caused warning captured in `.planning/tmp/nvim-validate/config-warning-families.tsv` is removed from refreshed `checkhealth.txt` by the end of Task 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkhealth output -> warning classification | Misclassifying warnings could hide a real config defect or waste work on environment-only noise |
| which-key registration -> maintainer diagnostics | Duplicate-prefix warnings can obscure real issues if repo-generated noise is left untriaged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | I | `.planning/phases/06-runtime-failure-inventory/FAILURES.md` | mitigate | Re-run `./scripts/nvim-validate.sh checkhealth` at the start of the plan and record dispositions from the fresh artifact, not from stale notes. |
| T-10-08 | T | `repo-owned config files named in .planning/tmp/nvim-validate/config-warning-families.tsv` | mitigate | Task 1 writes the exact warning-text manifest with owner files, and Task 2 must remove every listed warning from refreshed `checkhealth.txt`. |
</threat_model>

<verification>
Run `./scripts/nvim-validate.sh checkhealth` before and after any which-key adjustment, then confirm `FAILURES.md` reflects the final warning dispositions.
</verification>

<success_criteria>
- Phase 10 warning work starts from a fresh `checkhealth.txt` artifact.
- `FAILURES.md` records the disposition of current warning families.
- Repo-owned duplicate-prefix warning noise is fixed if confirmed; environment-only and optional-tool warnings are documented rather than hidden.
</success_criteria>

<output>
After completion, create `.planning/phases/10-validation-harness-expansion/10-04-SUMMARY.md`
</output>
