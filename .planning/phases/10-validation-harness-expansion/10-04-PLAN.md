---
phase: 10-validation-harness-expansion
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - .config/nvim/lua/core/keymaps/whichkey.lua
  - .planning/phases/06-runtime-failure-inventory/FAILURES.md
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "A fresh `checkhealth` run distinguishes config-caused warnings from by-design or environment-only warnings."
    - "Config-caused which-key warning noise is removed or explicitly proven to be non-actionable."
    - "FAILURES.md records the disposition of current warning findings as a living document."
  artifacts:
    - path: ".config/nvim/lua/core/keymaps/whichkey.lua"
      provides: "Adjusted which-key registration behavior for duplicate-prefix warnings if audit proves they are config-caused"
    - path: ".planning/phases/06-runtime-failure-inventory/FAILURES.md"
      provides: "Updated warning audit results and dispositions"
  key_links:
    - from: ".config/nvim/lua/core/keymaps/whichkey.lua"
      to: ".planning/tmp/nvim-validate/checkhealth.txt"
      via: "fresh which-key warning audit"
      pattern: "Duplicates for <<leader>e>|Duplicates for <<leader>b>"
    - from: ".planning/phases/06-runtime-failure-inventory/FAILURES.md"
      to: ".planning/tmp/nvim-validate/checkhealth.txt"
      via: "warning classification entries"
      pattern: "WARNING|By Design|Won't Fix|Fixed"
---

<objective>
Run a fresh warning audit and remove config-caused warning noise without pretending environment warnings are repo bugs.

Purpose: Implement D-13 through D-16 by re-running `checkhealth`, fixing config-caused warning entries where the repo owns the root cause, and recording the rest as By Design or Won't Fix in the living failure inventory.
Output: Cleaned which-key warning surface if warranted by the audit, plus updated `FAILURES.md` warning dispositions.
</objective>

<execution_context>
@/home/pera/.codex/get-shit-done/workflows/execute-plan.md
@/home/pera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-validation-harness-expansion/10-CONTEXT.md
@.planning/phases/10-validation-harness-expansion/10-RESEARCH.md
@.planning/phases/06-runtime-failure-inventory/06-CONTEXT.md
@.planning/phases/09-health-signal-cleanup/09-01-SUMMARY.md
@.planning/tmp/nvim-validate/checkhealth.txt
@.config/nvim/lua/core/keymaps/whichkey.lua
@.planning/phases/06-runtime-failure-inventory/FAILURES.md

<interfaces>
From .config/nvim/lua/core/keymaps/whichkey.lua:
```lua
for _, g in ipairs(groups) do
  which_key.add({
    { "<leader>" .. g.prefix, group = g.label },
  })
end
...
table.insert(global_specs, {
  map.lhs,
  desc = map.desc,
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run checkhealth and classify every current WARNING finding</name>
  <read_first>
    .planning/phases/10-validation-harness-expansion/10-CONTEXT.md
    .planning/phases/09-health-signal-cleanup/09-01-SUMMARY.md
    .planning/tmp/nvim-validate/checkhealth.txt
    .planning/phases/06-runtime-failure-inventory/FAILURES.md
  </read_first>
  <files>.planning/phases/06-runtime-failure-inventory/FAILURES.md</files>
  <acceptance_criteria>
    - `./scripts/nvim-validate.sh checkhealth` completes and refreshes `.planning/tmp/nvim-validate/checkhealth.txt`.
    - `rg -n "which-key|lazy\\.nvim|mason|vim\\.provider|vim\\.deprecated|By Design|Won't Fix|config-caused" .planning/phases/06-runtime-failure-inventory/FAILURES.md` returns matches in the new warning audit notes.
    - `FAILURES.md` explicitly records whether the which-key duplicate-prefix warnings are config-caused or informational-by-design.
  </acceptance_criteria>
  <action>
    Start exactly as D-14 requires: run `./scripts/nvim-validate.sh checkhealth`, read the refreshed `.planning/tmp/nvim-validate/checkhealth.txt`, and enumerate every WARNING family currently present. Record the classification in `.planning/phases/06-runtime-failure-inventory/FAILURES.md` using the living-doc pattern from Phase 6 and Phase 9: config-caused, environment-only, or optional-tool gap. At minimum classify the currently visible families from the existing artifact: lazy luarocks lua-5.1 warnings, mason missing Ruby/PHP/Julia helpers, render-markdown latex tool warning, vim.provider host-package warnings, vim.deprecated third-party warning, and which-key overlap/duplicate-prefix warnings. Mark environment-only and optional-tool findings as `By Design` or `Won't Fix` with the reason written inline.
  </action>
  <verify>
    <automated>./scripts/nvim-validate.sh checkhealth && rg -n "which-key|lazy\\.nvim|mason|vim\\.provider|vim\\.deprecated|By Design|Won't Fix|config-caused" .planning/phases/06-runtime-failure-inventory/FAILURES.md</automated>
  </verify>
  <done>`FAILURES.md` carries a fresh Phase 10 warning audit with dispositions for every current warning family instead of a stale copied list.</done>
</task>

<task type="auto">
  <name>Task 2: Remove config-caused which-key duplicate-prefix warnings if the fresh audit confirms repo ownership</name>
  <read_first>
    .planning/phases/10-validation-harness-expansion/10-CONTEXT.md
    .planning/tmp/nvim-validate/checkhealth.txt
    .config/nvim/lua/core/keymaps/whichkey.lua
    .config/nvim/lua/core/keymaps/registry.lua
  </read_first>
  <files>.config/nvim/lua/core/keymaps/whichkey.lua</files>
  <acceptance_criteria>
    - If the fresh audit still reports `Duplicates for <<leader>e>` and `Duplicates for <<leader>b>` as config-caused, `rg -n "Duplicates for <<leader>e>|Duplicates for <<leader>b>" .planning/tmp/nvim-validate/checkhealth.txt` returns no matches after the fix.
    - `rg -n "registry\\.groups|registry\\.get_by_scope\\(\"global\"\\)|registry\\.get_by_scope\\(\"lazy\"\\)" .config/nvim/lua/core/keymaps/whichkey.lua` still returns matches after the change.
    - `whichkey.lua` no longer registers a group spec for a `<leader>` prefix that is also owned by a real mapping if that exact duplication is what the audit proved.
  </acceptance_criteria>
  <action>
    If Task 1 proves the `which-key` duplicate-prefix warnings for `<leader>e` and `<leader>b` are repo-owned noise from `core/keymaps/whichkey.lua`, modify `whichkey.lua` so group registration skips any `<leader><prefix>` group whose exact lhs is already claimed by a real mapping in the registry. Keep the existing global and lazy mapping description registration intact, and do not attempt to remove the informational overlap warnings for `<leader>x` vs `<leader>xs` or `gc` vs `gcc` unless the fresh audit proves they are emitted as hard config issues rather than which-key informational overlap notices. If Task 1 reclassifies the duplicate-prefix warnings as harmless-by-design, leave code unchanged and record that disposition in `FAILURES.md` instead of forcing a speculative fix.
  </action>
  <verify>
    <automated>./scripts/nvim-validate.sh checkhealth && rg -n "registry\\.groups|registry\\.get_by_scope\\(\"global\"\\)|registry\\.get_by_scope\\(\"lazy\"\\)" .config/nvim/lua/core/keymaps/whichkey.lua</automated>
  </verify>
  <done>The plan either removes repo-owned duplicate-prefix warning noise from which-key or explicitly documents why no code change was warranted after the fresh audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkhealth output -> warning classification | Misclassifying warnings could hide a real config defect or waste work on environment-only noise |
| which-key registration -> maintainer diagnostics | Duplicate-prefix warnings can obscure real issues if repo-generated noise is left untriaged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | I | `.planning/phases/06-runtime-failure-inventory/FAILURES.md` | mitigate | Re-run `./scripts/nvim-validate.sh checkhealth` at the start of the plan and record dispositions from the fresh artifact, not from stale notes. |
| T-10-08 | T | `.config/nvim/lua/core/keymaps/whichkey.lua` | mitigate | Only change group registration if the audit proves the duplicate-prefix warnings are repo-caused; otherwise preserve behavior and document a by-design disposition. |
</threat_model>

<verification>
Run `./scripts/nvim-validate.sh checkhealth` before and after any which-key adjustment, then confirm `FAILURES.md` reflects the final warning dispositions.
</verification>

<success_criteria>
- Phase 10 warning work starts from a fresh `checkhealth.txt` artifact.
- `FAILURES.md` records the disposition of current warning families.
- Repo-owned duplicate-prefix warning noise is fixed if confirmed; environment-only and optional-tool warnings are documented rather than hidden.
</success_criteria>

<output>
After completion, create `.planning/phases/10-validation-harness-expansion/10-04-SUMMARY.md`
</output>
