---
phase: 10-validation-harness-expansion
plan: "03"
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - .config/nvim/README.md
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "Maintainer can read any validation artifact and tell whether it points to a config regression, an environment gap, or an optional-tool warning."
    - "Maintainer can map each artifact file in `.planning/tmp/nvim-validate/` to the command that produced it."
    - "README triage guidance stays next to the validation harness docs instead of splitting into a second doc surface."
  artifacts:
    - path: ".config/nvim/README.md"
      provides: "Reading validation output guidance tied to actual artifacts"
  key_links:
    - from: ".config/nvim/README.md"
      to: ".planning/tmp/nvim-validate/"
      via: "artifact interpretation table"
      pattern: "startup\\.log|sync\\.log|smoke\\.log|health\\.json|checkhealth\\.txt|keymap-regression\\.log|format-regression\\.log"
    - from: ".config/nvim/README.md"
      to: ".config/nvim/lua/config/health.lua"
      via: "Phase 9 classification reuse"
      pattern: "config regression|environment gap|optional tool"
---

<objective>
Document how to interpret Phase 10 validation output without inventing a second taxonomy.

Purpose: Deliver D-12 by teaching maintainers how to read the final artifact set and separate config regressions from environment or optional-tool gaps using the Phase 9 classification approach.
Output: A new `Reading validation output` section in `.config/nvim/README.md`.
</objective>

<execution_context>
@/home/pera/.codex/get-shit-done/workflows/execute-plan.md
@/home/pera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-validation-harness-expansion/10-CONTEXT.md
@.planning/phases/10-validation-harness-expansion/10-RESEARCH.md
@.planning/phases/09-health-signal-cleanup/09-CONTEXT.md
@.planning/phases/09-health-signal-cleanup/09-01-SUMMARY.md
@.config/nvim/README.md
@scripts/nvim-validate.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add artifact-by-artifact interpretation guidance to the README</name>
  <read_first>
    .planning/phases/10-validation-harness-expansion/10-CONTEXT.md
    .planning/phases/09-health-signal-cleanup/09-CONTEXT.md
    .config/nvim/README.md
    scripts/nvim-validate.sh
  </read_first>
  <files>.config/nvim/README.md</files>
  <acceptance_criteria>
    - `rg -n "^### Reading validation output|startup\\.log|sync\\.log|smoke\\.log|health\\.json|checkhealth\\.txt|keymap-regression\\.log|format-regression\\.log" .config/nvim/README.md` returns matches.
    - The new section states the producing command for each artifact file and the first action to take when that artifact fails.
    - `rg -n "config regression|environment gap|optional tool gap" .config/nvim/README.md` returns matches in the new section.
  </acceptance_criteria>
  <action>
    Add a `### Reading validation output` subsection inside the existing validation harness documentation in `.config/nvim/README.md`. Build one table or bullet list that covers every committed artifact name exactly: `startup.log`, `sync.log`, `smoke.log`, `health.json`, `checkhealth.txt`, `keymap-regression.log`, and `format-regression.log`. For each artifact, name the harness subcommand that produces it and give one concrete first-response rule, for example: startup/sync/smoke failures indicate config or plugin-load regressions to fix in repo code; `health.json` missing required tools means rollout-blocking machine setup gap; `checkhealth.txt` WARN-only output must be classified into config regression vs environment gap vs optional tool gap; `keymap-regression.log` and `format-regression.log` failures point directly at Phase 7 lazy dispatcher behavior or `conform.lua` guard behavior.
  </action>
  <verify>
    <automated>rg -n "^### Reading validation output|startup\\.log|sync\\.log|smoke\\.log|health\\.json|checkhealth\\.txt|keymap-regression\\.log|format-regression\\.log|config regression|environment gap|optional tool gap" .config/nvim/README.md</automated>
  </verify>
  <done>The README contains one artifact-by-artifact interpretation guide tied to the actual Phase 10 output files.</done>
</task>

<task type="auto">
  <name>Task 2: Add a triage decision path that reuses Phase 9 classification</name>
  <read_first>
    .planning/phases/10-validation-harness-expansion/10-CONTEXT.md
    .planning/phases/09-health-signal-cleanup/09-CONTEXT.md
    .config/nvim/README.md
  </read_first>
  <files>.config/nvim/README.md</files>
  <acceptance_criteria>
    - `rg -n "Fix now|By Design|Won't Fix|optional tool|machine-local|repo code" .config/nvim/README.md` returns matches in the new triage subsection.
    - The README explicitly tells maintainers not to create a separate TRIAGE.md and keeps the guidance in README.
    - The new text mentions `:checkhealth config` or Phase 9 health classification as the source of truth for warning interpretation.
  </acceptance_criteria>
  <action>
    In the same new section, add a short triage decision path that reuses the Phase 9 taxonomy instead of inventing new labels. State the concrete categories and actions: `config regression -> fix repo code before rollout`, `environment gap -> document or install on the target machine`, `optional tool gap -> keep as warning or mark By Design/Won't Fix when the feature is intentionally absent`. Mention `:checkhealth config` and the validator artifacts as the paired sources of truth. Do not create `TRIAGE.md`; keep the guidance in `.config/nvim/README.md` per D-12.
  </action>
  <verify>
    <automated>rg -n "Fix now|By Design|Won't Fix|optional tool|machine-local|repo code|:checkhealth config|TRIAGE\\.md" .config/nvim/README.md</automated>
  </verify>
  <done>The README now teaches maintainers how to route failures and warnings into repo fixes, machine setup work, or documented-by-design outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer -> rollout decision | The README guidance shapes whether a failing signal blocks rollout or is documented as environmental |
| validation artifacts -> human interpretation | Poor documentation can cause operators to misclassify config regressions as harmless warnings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | I | `.config/nvim/README.md` | mitigate | Enumerate every artifact file by exact filename and pair it with the producing subcommand and first-response rule. |
| T-10-06 | R | README triage path | mitigate | Reuse the exact Phase 9 categories (`config regression`, `environment gap`, `optional tool gap`) so maintainers do not create a second competing taxonomy. |
</threat_model>

<verification>
Run `./scripts/nvim-validate.sh all` once Plans 10-01 and 10-02 are complete, then confirm the README section covers every artifact that the command writes.
</verification>

<success_criteria>
- README has a `Reading validation output` section that lists all seven artifact files.
- The section maps each artifact to its subcommand and triage action.
- The guidance explicitly reuses Phase 9 classification and does not spin off a second triage document.
</success_criteria>

<output>
After completion, create `.planning/phases/10-validation-harness-expansion/10-03-SUMMARY.md`
</output>
