---
phase: 10-validation-harness-expansion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .config/nvim/README.md
  - .config/nvim/lua/plugins/conform.lua
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "Maintainer can see one documented validation contract for the v1.1 harness."
    - "The documented Phase 10 artifact set names the new regression logs before script implementation begins."
    - "The format-on-save target file no longer carries stale Phase 10 TODO text."
  artifacts:
    - path: ".config/nvim/README.md"
      provides: "Authoritative Phase 10 command and artifact contract"
    - path: ".config/nvim/lua/plugins/conform.lua"
      provides: "Clean format_on_save guard source without stale TODO banner"
  key_links:
    - from: ".config/nvim/README.md"
      to: "scripts/nvim-validate.sh"
      via: "documented command table and artifact list"
      pattern: "nvim-validate\\.sh (checkhealth|keymaps|formats|all)"
    - from: ".config/nvim/lua/plugins/conform.lua"
      to: "scripts/nvim-validate.sh"
      via: "Plan 10-02 direct guard probe target"
      pattern: "format_on_save = function"
---

<objective>
Lock the Phase 10 validation contract before adding new regression probes.

Purpose: Implement D-01 through D-04 by auditing the existing harness against v1.1 changes, defining the artifact contract for the new regression logs, and removing stale cleanup noise that would mislead the next plan.
Output: Updated README validation contract and cleaned `conform.lua` header.
</objective>

<execution_context>
@/home/pera/.codex/get-shit-done/workflows/execute-plan.md
@/home/pera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-validation-harness-expansion/10-CONTEXT.md
@.planning/phases/10-validation-harness-expansion/10-RESEARCH.md
@.planning/phases/10-validation-harness-expansion/10-PATTERNS.md
@scripts/nvim-validate.sh
@.config/nvim/README.md
@.config/nvim/lua/plugins/conform.lua

<interfaces>
From scripts/nvim-validate.sh:
```bash
REPORT_DIR="$REPO_ROOT/.planning/tmp/nvim-validate"
PLUGIN_LIST="{'snacks','lualine','lspconfig','conform','nvim-treesitter.configs','blink.cmp','gitsigns','ufo','bufferline','which-key','render-markdown'}"
TOOL_LIST="{'stylua','black','isort','prettierd','prettier','clang-format','shfmt','rg','git','node','go','clangd','gopls','lua-language-server'}"
```

From .config/nvim/lua/plugins/conform.lua:
```lua
format_on_save = function(bufnr)
  ...
  return { timeout_ms = 500, lsp_format = "fallback" }
end,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the existing harness contract against v1.1 decisions</name>
  <read_first>
    .planning/phases/10-validation-harness-expansion/10-CONTEXT.md
    scripts/nvim-validate.sh
    .config/nvim/README.md
  </read_first>
  <files>.config/nvim/README.md</files>
  <acceptance_criteria>
    - `rg -n "keymap-regression\\.log|format-regression\\.log" .config/nvim/README.md` returns matches.
    - `rg -n "checkhealth.*keymaps.*formats|startup.*sync.*smoke.*health.*checkhealth.*keymaps.*formats" .config/nvim/README.md` returns a Phase 10 sequence match.
    - The README validation section names `./scripts/nvim-validate.sh checkhealth`, `./scripts/nvim-validate.sh keymaps`, and `./scripts/nvim-validate.sh formats` exactly once each in the command table added for Phase 10.
  </acceptance_criteria>
  <action>
    Audit `scripts/nvim-validate.sh` against D-01 and explicitly confirm in the README contract that `PLUGIN_LIST` already excludes neo-tree and `TOOL_LIST` already reflects `pyright`-era Phase 8 behavior, so no harness-list correction is needed in this plan. Then update the stale Phase 3 validation table in `.config/nvim/README.md` so the documented target contract matches D-02 and D-04 exactly: add rows for `checkhealth`, `keymaps`, and `formats`; describe `all` as `startup -> sync -> smoke -> health -> checkhealth -> keymaps -> formats`; and extend the report-output list with `keymap-regression.log` and `format-regression.log` under `.planning/tmp/nvim-validate/`. Keep this documentation adjacent to the existing validation harness section instead of creating a new file.
  </action>
  <verify>
    <automated>rg -n "keymap-regression\\.log|format-regression\\.log|nvim-validate\\.sh keymaps|nvim-validate\\.sh formats|startup.*sync.*smoke.*health.*checkhealth.*keymaps.*formats" .config/nvim/README.md</automated>
  </verify>
  <done>The README exposes one authoritative Phase 10 validation contract that matches D-02 and D-04 and records the target artifact names for Plan 10-02.</done>
</task>

<task type="auto">
  <name>Task 2: Remove stale Phase 10 TODO noise from the format-on-save source</name>
  <read_first>
    .planning/phases/10-validation-harness-expansion/10-CONTEXT.md
    .config/nvim/lua/plugins/conform.lua
  </read_first>
  <files>.config/nvim/lua/plugins/conform.lua</files>
  <acceptance_criteria>
    - `rg -n "^--- TODO: Format-on-save dispatcher" .config/nvim/lua/plugins/conform.lua` returns no matches.
    - `rg -n "format_on_save = function\\(bufnr\\)" .config/nvim/lua/plugins/conform.lua` still returns a match.
    - `rg -n "timeout_ms = 500, lsp_format = \"fallback\"" .config/nvim/lua/plugins/conform.lua` still returns a match.
  </acceptance_criteria>
  <action>
    Implement D-03 literally: remove the line `--- TODO: Format-on-save dispatcher - conform.nvim ---` from `.config/nvim/lua/plugins/conform.lua` and leave the `format_on_save` guard logic unchanged. Do not alter the excluded filetype list, guard ordering, or return table in this plan; Plan 10-02 depends on the current guard behavior as-is.
  </action>
  <verify>
    <automated>! rg -n "^--- TODO: Format-on-save dispatcher" .config/nvim/lua/plugins/conform.lua && rg -n "format_on_save = function\\(bufnr\\)|timeout_ms = 500, lsp_format = \"fallback\"" .config/nvim/lua/plugins/conform.lua</automated>
  </verify>
  <done>`conform.lua` is cleanup-only for this plan: the stale TODO banner is gone and the guard implementation remains intact for the upcoming regression probe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer -> README contract | Incorrect docs would cause maintainers to run the wrong rollout gate |
| shell harness -> Lua guard target | Misstating the guard contract would make the later regression probe validate the wrong behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | T | `.config/nvim/README.md` | mitigate | Copy the exact Phase 10 command names and artifact filenames from D-02/D-04 so the contract cannot drift into invented names. |
| T-10-02 | I | `.config/nvim/lua/plugins/conform.lua` | mitigate | Limit the plan to banner removal only and verify the `format_on_save` signature and return table still exist after edit. |
</threat_model>

<verification>
Run the README grep checks plus the `conform.lua` signature checks from the task-level `<automated>` commands.
</verification>

<success_criteria>
- README names the full Phase 10 artifact contract including `keymap-regression.log` and `format-regression.log`.
- The stale Phase 3 validation table no longer documents the pre-checkhealth `all` sequence.
- `conform.lua` contains no stale Phase 10 TODO banner and still exports the same `format_on_save` guard.
</success_criteria>

<output>
After completion, create `.planning/phases/10-validation-harness-expansion/10-01-SUMMARY.md`
</output>
