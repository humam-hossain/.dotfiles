---
phase: 10-validation-harness-expansion
plan: "02"
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - scripts/nvim-validate.sh
  - .planning/phases/06-runtime-failure-inventory/CHECKLIST.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "Maintainer can run one command to probe keymap dispatcher regressions headlessly."
    - "Maintainer can run one command to probe format-on-save guard regressions headlessly."
    - "The full `all` gate executes the new probes after `checkhealth`."
    - "Manual-only blind spots remain documented in the failure checklist instead of being silently dropped."
  artifacts:
    - path: "scripts/nvim-validate.sh"
      provides: "`keymaps` and `formats` subcommands plus updated `all` sequence"
    - path: ".planning/phases/06-runtime-failure-inventory/CHECKLIST.md"
      provides: "Phase 10 manual regression section for LSP attach safety and non-scripted follow-up"
  key_links:
    - from: "scripts/nvim-validate.sh"
      to: ".config/nvim/lua/core/keymaps/lazy.lua"
      via: "headless dispatcher probe"
      pattern: "require\\(\"core\\.keymaps\\.lazy\"\\)"
    - from: "scripts/nvim-validate.sh"
      to: ".config/nvim/lua/plugins/conform.lua"
      via: "direct format_on_save guard probe"
      pattern: "require\\(\"plugins\\.conform\"\\)|format_on_save"
    - from: "scripts/nvim-validate.sh"
      to: ".planning/tmp/nvim-validate/"
      via: "log artifact writes"
      pattern: "keymap-regression\\.log|format-regression\\.log"
---

<objective>
Add the two Phase 10 scripted blind-spot checks to the single validation harness.

Purpose: Deliver D-05 through D-11 without creating a second script surface, and capture the remaining manual-only checks in the living checklist per D-07 and D-08.
Output: Extended `nvim-validate.sh`, new regression logs in `.planning/tmp/nvim-validate/`, and a new Phase 10 manual regression section in `CHECKLIST.md`.
</objective>

<execution_context>
@/home/pera/.codex/get-shit-done/workflows/execute-plan.md
@/home/pera/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-validation-harness-expansion/10-CONTEXT.md
@.planning/phases/10-validation-harness-expansion/10-RESEARCH.md
@.planning/phases/10-validation-harness-expansion/10-PATTERNS.md
@.planning/phases/07-keymap-reliability-fixes/07-01-SUMMARY.md
@.planning/phases/09-health-signal-cleanup/09-01-SUMMARY.md
@scripts/nvim-validate.sh
@.config/nvim/lua/core/keymaps/lazy.lua
@.config/nvim/lua/plugins/conform.lua
@.planning/phases/06-runtime-failure-inventory/CHECKLIST.md

<interfaces>
From .config/nvim/lua/core/keymaps/lazy.lua:
```lua
elseif type(map.action) == "string" then
  if map.action:match("<[^>]+>") then
    vim.api.nvim_feedkeys(
      vim.api.nvim_replace_termcodes(map.action, true, false, true),
      "n",
      false
    )
  else
    vim.cmd(map.action)
  end
end
```

From .config/nvim/lua/plugins/conform.lua:
```lua
if buftype ~= "" and buftype ~= "acwrite" then
  return false
end
if not vim.bo[bufnr].modifiable then
  return false
end
if bufname == "" then
  return false
end
return { timeout_ms = 500, lsp_format = "fallback" }
```

From scripts/nvim-validate.sh:
```bash
case "$SUBCMD" in
  startup)     cmd_startup ;;
  sync)        cmd_sync ;;
  health)      cmd_health ;;
  smoke)       cmd_smoke ;;
  checkhealth) cmd_checkhealth ;;
  all)         cmd_all ;;
esac
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the lazy key dispatcher regression subcommand</name>
  <read_first>
    .planning/phases/10-validation-harness-expansion/10-CONTEXT.md
    scripts/nvim-validate.sh
    .config/nvim/lua/core/keymaps/lazy.lua
    .planning/phases/07-keymap-reliability-fixes/07-01-SUMMARY.md
  </read_first>
  <files>scripts/nvim-validate.sh</files>
  <acceptance_criteria>
    - `rg -n "cmd_keymaps\\(\\)|keymap-regression\\.log|keymaps\\)\\s+cmd_keymaps" scripts/nvim-validate.sh` returns matches.
    - `rg -n "<cmd>enew<CR>|<C-w>s|:close<CR>" scripts/nvim-validate.sh` returns the three concrete probe values from D-05 and Specific Ideas.
    - `rg -n "all ABORTED at keymaps|checkhealth.*cmd_keymaps|cmd_keymaps.*cmd_formats" scripts/nvim-validate.sh` shows `keymaps` inserted after `checkhealth` in `cmd_all`.
  </acceptance_criteria>
  <action>
    Extend `scripts/nvim-validate.sh` with a new `cmd_keymaps()` subcommand using the existing `cmd_smoke()` pattern and D-05/D-09 exactly. Write the artifact to `$REPORT_DIR/keymap-regression.log`. In the headless Lua probe, load `core.keymaps.lazy`, construct a minimal dispatcher exercise that proves all three previously broken string-action families complete without throwing: `"<cmd>enew<CR>"`, `"<C-w>s"`, and `":close<CR>"`. Use `pcall` around each action and write one PASS/FAIL line per probe into the log, then exit non-zero if any probe failed. Wire `keymaps` into `usage()`, the main `case` dispatch, and `cmd_all` immediately after `checkhealth`.
  </action>
  <verify>
    <automated>./scripts/nvim-validate.sh keymaps</automated>
  </verify>
  <done>The harness exposes a working `keymaps` subcommand, writes `keymap-regression.log`, and the `all` gate now includes the keymap regression check after `checkhealth`.</done>
</task>

<task type="auto">
  <name>Task 2: Add the direct format-on-save guard regression subcommand</name>
  <read_first>
    .planning/phases/10-validation-harness-expansion/10-CONTEXT.md
    scripts/nvim-validate.sh
    .config/nvim/lua/plugins/conform.lua
  </read_first>
  <files>scripts/nvim-validate.sh</files>
  <acceptance_criteria>
    - `rg -n "cmd_formats\\(\\)|format-regression\\.log|formats\\)\\s+cmd_formats" scripts/nvim-validate.sh` returns matches.
    - `rg -n "nofile|acwrite|timeout_ms = 500|lsp_format = \"fallback\"" scripts/nvim-validate.sh` shows the exact guard cases asserted by the probe.
    - `rg -n "all ABORTED at formats|cmd_keymaps.*cmd_formats|keymaps.*formats all succeeded" scripts/nvim-validate.sh` shows `formats` is the last step in `cmd_all`.
  </acceptance_criteria>
  <action>
    Add `cmd_formats()` to `scripts/nvim-validate.sh` following the same temp-Lua-file pattern as `cmd_smoke()` and the direct-function-call rule from D-06. Write the artifact to `$REPORT_DIR/format-regression.log`. In the Lua probe, load the plugin spec from `plugins.conform`, extract `opts.format_on_save`, and call it directly for three concrete cases: `buftype="nofile", modifiable=true, name=""` must return `false`; `buftype="", modifiable=true, name=""` must return `false`; `buftype="acwrite", modifiable=true, filetype="lua", name="/tmp/phase10.lua"` must return a table containing `timeout_ms = 500` and `lsp_format = "fallback"`. Do not simulate `BufWritePre`. Wire `formats` into `usage()`, the main `case` dispatch, and `cmd_all` immediately after `keymaps`.
  </action>
  <verify>
    <automated>./scripts/nvim-validate.sh formats</automated>
  </verify>
  <done>The harness exposes a working `formats` subcommand, writes `format-regression.log`, and fails if any of the three direct guard cases regress.</done>
</task>

<task type="auto">
  <name>Task 3: Add the manual-only Phase 10 regression checklist section</name>
  <read_first>
    .planning/phases/10-validation-harness-expansion/10-CONTEXT.md
    .planning/phases/06-runtime-failure-inventory/CHECKLIST.md
    scripts/nvim-validate.sh
  </read_first>
  <files>.planning/phases/06-runtime-failure-inventory/CHECKLIST.md</files>
  <acceptance_criteria>
    - `rg -n "^## Phase 10 Regression Checks" .planning/phases/06-runtime-failure-inventory/CHECKLIST.md` returns a match.
    - `rg -n "LSP attach safety|./scripts/nvim-validate.sh keymaps|./scripts/nvim-validate.sh formats|Regression signal:" .planning/phases/06-runtime-failure-inventory/CHECKLIST.md` returns matches.
    - The new section contains at least one numbered checklist for an LSP attach scenario and one numbered checklist that tells maintainers to pair manual checks with the new scripted subcommands.
  </acceptance_criteria>
  <action>
    Append a new `## Phase 10 Regression Checks` section to `.planning/phases/06-runtime-failure-inventory/CHECKLIST.md` per D-07 and D-08. Use the established checklist idiom already in the file: numbered steps, an `Expected:` line, and a `Regression signal:` line. Include one subsection for LSP attach safety that tells the maintainer to open representative Lua and Go files, wait for attach, and confirm no attach-time Lua errors, and one subsection that explicitly pairs `./scripts/nvim-validate.sh keymaps` and `./scripts/nvim-validate.sh formats` with any interactive follow-up needed if the logs fail.
  </action>
  <verify>
    <automated>rg -n "^## Phase 10 Regression Checks|LSP attach safety|./scripts/nvim-validate.sh keymaps|./scripts/nvim-validate.sh formats|Regression signal:" .planning/phases/06-runtime-failure-inventory/CHECKLIST.md</automated>
  </verify>
  <done>`CHECKLIST.md` now covers the remaining non-scripted regression surface instead of pretending full automation exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell harness -> headless Neovim probe | The shell script launches Lua probes that must validate the real config code, not a fake path |
| maintainer -> regression logs | The log artifacts must distinguish pass/fail cleanly enough for rollout decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | T | `scripts/nvim-validate.sh` | mitigate | Reuse the existing temp-Lua and PASS/FAIL logging pattern so new subcommands write deterministic artifacts and non-zero exits on regression. |
| T-10-04 | R | `.planning/tmp/nvim-validate/*.log` | mitigate | Write one PASS/FAIL line per concrete probe case and fail the subcommand if any expected false/table result is wrong. |
</threat_model>

<verification>
Run `./scripts/nvim-validate.sh keymaps`, `./scripts/nvim-validate.sh formats`, and then `./scripts/nvim-validate.sh all` after both subcommands are wired.
</verification>

<success_criteria>
- `scripts/nvim-validate.sh keymaps` and `scripts/nvim-validate.sh formats` both exist and produce log artifacts.
- `cmd_all` runs `startup -> sync -> smoke -> health -> checkhealth -> keymaps -> formats` in that exact order.
- `CHECKLIST.md` carries a new Phase 10 section for LSP attach safety and scripted-regression follow-up.
</success_criteria>

<output>
After completion, create `.planning/phases/10-validation-harness-expansion/10-02-SUMMARY.md`
</output>
