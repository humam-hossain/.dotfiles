---
phase: 16-retire-the-safe-profile-full-only-wrapper-and-playbook
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - arch/dots-hyprland.sh
  - scripts/phase16-retire-assert.sh
autonomous: false
requirements: [FULL-01, FULL-02, FULL-04, D-02, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-35]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A bare `./arch/dots-hyprland.sh install-files --dry-run` prints a would-exec line carrying none of the three retired residual flags — full is what an operator gets without asking for it (D-04, FULL-01, FULL-02)."
    - "`--full` is accepted on every install-family subcommand, prints a `[CONFIG]` note saying it is ignored, and never reaches the would-exec line (D-05)."
    - "`install` and `install-files` forward the upstream skip-backup flag; `install-deps` and `install-setups` do not, because upstream reads it only in `3.files.sh` (D-06, research Pitfall 4 / assumption A1)."
    - "No wrapper-owned prompt stands between an `install` invocation and upstream `./setup` — the exact-token gate line is gone from every install path (D-09)."
    - "`uninstall --dry-run` still exits 0, and the real path still runs its own type-yes confirmation (D-10)."
    - "`protect` is refused by the allowlist as an unknown subcommand (D-07)."
    - "`bash -n arch/dots-hyprland.sh` is clean and the surviving uninstall machinery still resolves every helper it calls, so nothing aborts under `set -euo pipefail` (D-11, research §Signature changes)."
    - "A new executable contract exists at `scripts/phase16-retire-assert.sh` and exits 0 against the rewritten wrapper (D-35)."
  artifacts:
    - path: "arch/dots-hyprland.sh"
      provides: "Full-only install path, stripped uninstall, array-only exec preserved"
      contains: "would exec"
    - path: "scripts/phase16-retire-assert.sh"
      provides: "The retirement contract: argv asserts over dry-run output, non-mutating"
      contains: "=== done: FAIL="
  key_links:
    - from: "arch/dots-hyprland.sh"
      to: "vendor/dots-hyprland/setup"
      via: "run_install_family builds a cmd array and execs it inside a cd subshell — never eval"
      pattern: "cmd\\[@\\]"
    - from: "scripts/phase16-retire-assert.sh"
      to: "arch/dots-hyprland.sh"
      via: "dry-run argv capture greped through the literal would-exec contract line"
      pattern: "would exec"
    - from: "arch/dots-hyprland.sh"
      to: "docs/dots-hyprland-workflow.md"
      via: "usage heredoc playbook pointer, asserted by scripts/phase12-full-smoke.sh"
      pattern: "dots-hyprland-workflow"
  prohibitions:
    - statement: "No `--safe` flag, and no safe/full profile switch of any spelling, is added to the wrapper (D-05)."
      status: active
      verification: backstop
    - statement: "`print_lines` is not deleted — it sits inside the apparent range of the removed package machinery but has three live callers in the surviving uninstall helpers (D-07 keep-list)."
      status: active
      verification: backstop
    - statement: "`run_upstream_uninstall_dangerous` and its `--upstream-dangerous` flag are not modified; the type-token guard in front of them stays (recorded assumption A3)."
      status: active
      verification: backstop
    - statement: "No task in this plan mutates the live machine — no real install, no real uninstall, no package operation. Dry-run and syntax checks only (D-35)."
      status: active
      verification: backstop
    - statement: "WR-02 stays open and unowned: the repo copy `.config/hypr/hyprland.conf` is not deleted and its two hook lines are left in place as dead archive (CONTEXT.md 'Not in this phase', D-08)."
      status: active
      verification: backstop
    - statement: "The D-38 `graphical-session.target` autostart bootstrap is not restored, worked around, or given an owner in this phase (CONTEXT.md 'Not in this phase')."
      status: active
      verification: backstop
    - statement: "No tombstone comment naming a deleted symbol is left behind in `arch/dots-hyprland.sh`; comments cite decision IDs, not the identifiers they replaced."
      status: active
      verification: backstop
---

<objective>
Flip the wrapper's install contract from safe-by-default to full-only, and prove it with a new executable contract in the same commit sequence.

The single seam is the argv build inside `run_install_family`: it is the only place in the repo where a profile decision reaches upstream `./setup`. Everything else this plan deletes — the residual array, the profile predicate, the backup gate and its two helper predicates, the package re-marking machinery, the session-hook injection machinery — is downstream of that one seam and falls out once it goes.

Purpose: `docs/dots-hyprland-workflow.md` quotes the wrapper's literal stdout, so the wrapper must be final before any documentation is written (D-42). This plan is the wrapper half. It also lifts Phase 15's D-19 no-scripts fence for this phase (D-02).
Output: a full-only `arch/dots-hyprland.sh` that still preflights, still allowlists, still execs an array and still uninstalls; plus `scripts/phase16-retire-assert.sh`, which is the contract of record for the retirement.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-PATTERNS.md
</context>

<tasks>

<!-- planner-discipline-allow: SAFE_DEFAULTS -->
<!-- planner-discipline-allow: II_BACKUP_DIR -->
<!-- planner-discipline-allow: backup_gate -->
<!-- planner-discipline-allow: needs_safe_defaults -->
<!-- planner-discipline-allow: is_help_only_user_flags -->
<!-- planner-discipline-allow: user_flags_contain -->
<!-- planner-discipline-allow: PROTECT_EXPLICIT -->
<!-- planner-discipline-allow: protect_explicit_packages -->
<!-- planner-discipline-allow: enable_hypr_ii_hooks -->
<!-- planner-discipline-allow: disable_hypr_ii_hooks -->
<!-- planner-discipline-allow: run_protect -->
<!-- planner-discipline-allow: --allow-skip-backup -->
<!-- planner-discipline-allow: --skip-protect -->
<!-- planner-discipline-allow: --keep-hypr-hooks -->
<!-- planner-discipline-allow: --skip-hyprland -->
<!-- planner-discipline-allow: --skip-sysupdate -->
<!-- planner-discipline-allow: --core -->
<!-- planner-discipline-allow: Type 'yes' -->

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Confirm the two one-way removals before any code is deleted</name>
  <decision>Proceed with removing the install backup and the install confirmation prompt permanently, or keep one of them?</decision>
  <context>
`REVERSIBILITY_GATES` is on, and two locked decisions in `16-CONTEXT.md` are rated **one-way**:

- **D-06** — backups are removed entirely. `backup_gate` and `II_BACKUP_DIR` go, and the upstream skip-backup flag is passed by default. Upstream reads that flag in exactly one place, `vendor/dots-hyprland/sdata/subcmd-install/3.files.sh:219`, where it is the only thing standing between an install and `auto_backup_configs`.
- **D-09** — there is no interactive install gate at all afterwards. `backup_gate` was the wrapper's sole install confirmation (Phase 11 D-11 / D-13).

Together these mean: **a future `install` replaces the live session configuration with no prompt, no snapshot and no undo.** Once one such install has run, the pre-install configuration state is gone; re-adding the gate later cannot recover it. That is what makes the rating one-way rather than costly.

Two facts that bound the risk, both verified in `16-RESEARCH.md`:
- The existing snapshots `~/ii-original-dots-backup` and `~/ii-original-dots-backup.20260904T171128Z` are **left untouched on disk**. This plan changes future installs only.
- The install does **not** become unattended. Upstream `./setup` still runs `0.greeting.sh` and its own `pause()` unless `-f/--force` is passed, and the wrapper never injects that. So an operator still gets a greeting and at least one `Enter to proceed` — just no wrapper-owned backup gate.

`16-CONTEXT.md` records that the user was told exactly this ("no prompt, no backup, no undo") and chose to proceed. This checkpoint exists because the reversibility gate requires a one-way decision to be confirmed immediately before the task that implements it, not because the planner is reopening the decision.

A third decision, **D-07** (deleting the package re-marking machinery), is rated *costly* rather than one-way and is therefore flagged here rather than gated: after it lands, a later `yay -Yc` or `pacman -Rns $(pacman -Qtdq)` can remove `hyprland`, `kitty`, `starship`, `cliphist`, `bc` and `jq`, because the Phase 14 adopt already left them marked as dependencies. Restoring the capability means rebuilding a 60-entry package list and four helpers from git history.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed as decided — remove both (recommended, matches D-06 and D-09)</name>
      <pros>Delivers the phase as locked. The wrapper stops carrying two mechanisms whose only purpose was the retired profile. The install path becomes one code path with one seam, which is what makes the documentation rewrite in wave 3 possible at all.</pros>
      <cons>Future installs are unrecoverable from a wrapper-made snapshot. The `[CONFIG] Type 'yes' to continue` guard disappears from the install path, so a mistyped `install` starts the upstream flow immediately (upstream's own greeting and pause remain).</cons>
    </option>
    <option id="keep-gate">
      <name>Remove the backup but keep a bare confirmation prompt</name>
      <pros>Preserves one human stop before an irreversible install.</pros>
      <cons>Contradicts D-09 as written. It also leaves a prompt whose original text is entirely about a backup that no longer happens, so it would need re-authoring, and every dry-run capture in the assert scripts would have to keep feeding it stdin.</cons>
    </option>
    <option id="keep-backup">
      <name>Keep the upstream backup — do not pass the skip flag</name>
      <pros>Retains a restore path for future installs.</pros>
      <cons>Contradicts D-06 as written, and leaves `II_BACKUP_DIR` plus its two echo sites in the surviving uninstall machinery, so the deletion set stops being self-consistent.</cons>
    </option>
  </options>
  <resume-signal>Reply `proceed`, `keep-gate`, or `keep-backup`. Reply `proceed` to execute the plan as written.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: End-to-end full-only install path — one bare invocation from argv to upstream, with its assert</name>
  <reversibility rating="one-way">Implements D-06 and D-09: after this task an install carries no wrapper-made snapshot and no wrapper prompt, and a completed install cannot be undone from anything this wrapper produced. Confirmed at Task 1.</reversibility>
  <read_first>
    - `arch/dots-hyprland.sh` — read in full before editing. The regions this task changes are the header constants block, `needs_safe_defaults`, `backup_gate`, `is_help_only_user_flags`, `user_flags_contain`, and all of `run_install_family`. Re-derive every line number with `grep -n` immediately before each edit (D-12): the numbers in the planning documents are navigation aids that go stale after the first deletion.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md` — §"Wrapper Surgery Map" (the symbol tables and the three call-site tables, including the four sites CONTEXT.md does not enumerate), §"Architecture Patterns" (the before/after diagrams and Patterns 1 and 2), §"Code Examples" (the replacement `run_install_family` skeleton), and Pitfalls 3, 4, 5 and 10.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-PATTERNS.md` — the `scripts/phase16-*-assert.sh` section in full: the preamble, the tempfile/trap block, the syntax gate, the dry-run argv capture with its two load-bearing grep subtleties, and the summary footer.
    - `scripts/phase12-full-smoke.sh` — read in full. It is the structural analog the new assert copies, and it is the suite this plan deliberately breaks (rewritten in plan `16-02`).
    - `vendor/dots-hyprland/sdata/subcmd-install/options.sh` — the upstream `getopt` long-option list. `full` is not on it; anything not on it that reaches `./setup` aborts the install.
    - `vendor/dots-hyprland/sdata/subcmd-install/3.files.sh` — around line 219, the single place upstream reads the skip-backup flag. This is what scopes the injection.
  </read_first>
  <files>arch/dots-hyprland.sh, scripts/phase16-retire-assert.sh</files>
  <action>
Take one bare `install-files` invocation all the way from argv to the upstream exec with no profile decision anywhere on the path, then write the assert that proves it. Wrapper first, assert second, both verified before the task is done.

**Work bottom-up through the file** (research Pattern 2): every deletion invalidates the line numbers below it but not above it, so edit from the highest line number to the lowest and re-run `grep -n` after each step.

**A. `run_install_family` — the seam.** Reduce it to the shape in `16-RESEARCH.md` §"Code Examples":

- In the argv `case` loop, delete the `--allow-skip-backup)` arm entirely. Keep the `--dry-run)` arm as it is. **Keep the `--full)` arm** — do not delete it — and replace its body with a single `[CONFIG]`-prefixed echo stating that the flag is accepted but ignored because full is now the only install behavior. Do not set any variable from it. The arm must stay because the `*)` catch-all below it appends unrecognized args to `user_flags`, which are forwarded verbatim to upstream; deleting the arm would forward a long option upstream's `getopt` does not know, and upstream's `*)` handler exits 1 (D-05).
- Delete the `--full` scope-validation block that currently refuses the flag on anything but `install` / `install-files`. Per recorded assumption A2, `--full` becomes accepted on all four install-family subcommands. This deliberately flips `install-deps --full` from exit 1 to exit 0.
- Delete the block that refuses a bare upstream skip-backup flag, and delete the `backup_gate` invocation.
- Replace the whole argv-injection branch — the `needs_safe_defaults` conditional and both of its echo arms — with a plain array initialisation `local -a cmd=(./setup "$subcmd")` followed by a conditional append of the upstream skip-backup long option.
- Add a one-line predicate function immediately above `run_install_family`, named `touches_files`, that returns 0 for `install` and `install-files` and 1 otherwise. Scope the skip-backup append with it. Rationale to record in a short comment citing D-06 and the upstream file: upstream reads that flag only in `3.files.sh`, which `setup` sources for `install` and `install-files` only, so appending it for `install-deps` / `install-setups` would put a flag in the dry-run preview that cannot affect anything (research Pitfall 4, assumption A1).
- Keep the `user_flags` append and the `[INSTALL] ${cmd[*]}  (cwd=$II_ROOT)` echo unchanged.
- Delete the entire `case "$subcmd" in ... esac` mirror block inside the `--dry-run` branch, including its two `[CONFIG] dry-run: after setup, would ...` echoes and the two calls they narrate. Do not leave an empty `case` behind (research Pitfall 5). What survives from that branch is exactly two lines: the `[CONFIG] dry-run: would exec from $II_ROOT: ${cmd[*]}` echo and the `exit 0`.
- **The `[CONFIG] dry-run: would exec from ...` line is the contract string every assert script in this repo greps as `would exec`. Do not reword it, re-prefix it, or move the variable expansion.**
- Delete the post-setup `case "$subcmd" in ... esac` that follows the real array exec, including its two calls. Keep the `( cd "$II_ROOT"; "${cmd[@]}" )` subshell exactly as it is — array-only exec, never `eval` (D-11).
- Keep the `preflight` call (D-11).

**B. Header constants and the now-dead helpers.** Delete the `SAFE_DEFAULTS` array assignment and the `II_BACKUP_DIR` assignment from the constants block. Delete the function definitions `needs_safe_defaults`, `backup_gate`, `is_help_only_user_flags` and `user_flags_contain` — the last two die because their only call sites were inside the two blocks deleted in step A. Confirm before deleting with `grep -n` that no other caller exists.

**C. The two `II_BACKUP_DIR` readers inside machinery this phase KEEPS.** `16-CONTEXT.md` D-06 does not enumerate these and they are aborts, not no-ops, under `set -euo pipefail`. Both are `if [[ -d "$II_BACKUP_DIR" ]]` guards wrapping one echo each: one in `uninstall_gate` printing where an install-time backup would still be, and one in the tail of `run_safe_uninstall` printing the same fact after removal. Delete both guards with their echoes. Find them with `grep -n 'II_BACKUP_DIR' arch/dots-hyprland.sh` after step B and confirm the grep returns nothing when you are done.

**D. `run_uninstall`'s stale meta arm.** Delete the `--allow-skip-backup)` case arm in `run_uninstall`, the one whose comment calls it harmless if mixed. Its key no longer exists anywhere.

**E. Create `scripts/phase16-retire-assert.sh`** and `chmod +x` it. Copy the structure of `scripts/phase12-full-smoke.sh` exactly — this is not a new idiom, it is the repo's existing evidence shape (`16-PATTERNS.md`):

- Shebang, then a header comment block carrying: what the script asserts, `Usage (from REPO_ROOT):`, the exit contract sentence "Exit 0 if all hard asserts pass; non-zero if any hard FAIL.", and an explicit `Constraints (Phase 16):` list stating it is non-mutating, dry-run argv only, never runs a live install, and that its documentation ban-grep half is scoped to `docs/dots-hyprland-workflow.md` only.
- `set -euo pipefail`, the `REPO_ROOT`/`cd` preamble, then the `FAIL=0` / `pass()` / `fail()` triple byte-identical to the four existing assert scripts. Do not invent a variant and do not add a findings tier.
- `WRAP="./arch/dots-hyprland.sh"`, one `mktemp` per capture, and a single `trap 'rm -f ...' EXIT` carrying the `# shellcheck disable=SC2064` comment above it, as the analog does. `shellcheck` is not installed; the comment is repo convention.
- An opening `echo "=== Phase 16 retirement contract (non-mutating) ==="`.
- Asserts, each an `if`/`else` with both branches present and every failure branch dumping context with `sed -n '1,40p' "$OUT" || true`:
  1. Syntax gate: `bash -n arch/dots-hyprland.sh`, first in the file.
  2. Bare `install --dry-run`, stdin fed from `printf ''`: the capture contains `would exec`; it does **not** contain `--skip-hyprland`; it does **not** contain `--skip-sysupdate`; it does **not** match the word-boundary ERE `(^|[[:space:]])--core([[:space:]]|$)` — use that ERE, not a plain fixed-string grep; the would-exec line **does** contain the upstream skip-backup long option.
  3. Bare `install-files --dry-run`: same four omission asserts, and the skip-backup presence assert.
  4. `install-setups --dry-run`: the would-exec line does **not** carry the skip-backup long option — this is the crisp negative that proves the predicate in step A is scoped rather than unconditional.
  5. No install-path prompt: the `install --dry-run` capture does not contain the exact-token gate string `Type 'yes'` (D-09).
  6. `install --full --dry-run` exits 0; the capture contains a `[CONFIG]` line mentioning the flag is ignored; and — using the **two-stage grep** `grep 'would exec' "$OUT" | grep -q -- '--full'` — the would-exec line does not carry the flag. The two-stage form is load-bearing: the wrapper legitimately prints the flag in its own note line, so grepping the whole capture would make the ignored-note fail its own assert.
  7. `install-deps --full --dry-run` exits 0, recording assumption A2's flip from refusal to acceptance.
- Footer copied verbatim from the analog: `echo "=== done: FAIL=${FAIL} ==="`, then `if [[ "$FAIL" -gt 0 ]]; then exit 1; fi`, then `exit 0`.

Do **not** add the documentation ban-grep half yet. It bans strings that are still all over `docs/dots-hyprland-workflow.md` until wave 3; plan `16-04` adds it in the same task that removes them (D-36).

**Comment hygiene.** Any comment you leave behind in `arch/dots-hyprland.sh` must cite decision IDs (D-04, D-05, D-06, D-09, D-11), not the names of the symbols you deleted. A tombstone comment naming a removed identifier re-introduces the very token the asserts grep for absence, and the grep cannot tell a comment from code.

**Known-red, do not "fix".** From this task until plan `16-06` lands, `./scripts/phase13-d19-assert.sh` fails on `arch/dots-hyprland.sh changed since 14c6828`. That is D-38(1) working as designed. Do not loosen it, do not re-pin it here, and do not run it as a gate. `./scripts/phase12-full-smoke.sh` also goes red from this task; plan `16-02` rewrites it.
  </action>
  <verify>
    <automated>bash -n arch/dots-hyprland.sh && test -x scripts/phase16-retire-assert.sh && ./scripts/phase16-retire-assert.sh >/tmp/p16-01-assert.txt 2>&1; tail -3 /tmp/p16-01-assert.txt; grep -q '^=== done: FAIL=0 ===$' /tmp/p16-01-assert.txt</automated>
    <fails_when>`bash -n` exits non-zero, `scripts/phase16-retire-assert.sh` is missing or not executable, or the tail of its output is any summary line other than the exact `=== done: FAIL=0 ===` — every `[FAIL]` line printed above that summary names the assert that broke</fails_when>
    <automated>test "$(grep -vc '^\s*#' /dev/null)" -eq 0; for s in '^SAFE_DEFAULTS=' 'II_BACKUP_DIR' 'backup_gate' 'needs_safe_defaults' 'is_help_only_user_flags' 'user_flags_contain' 'allow-skip-backup'; do if grep -q -- "$s" arch/dots-hyprland.sh; then echo "SURVIVOR: $s"; grep -n -- "$s" arch/dots-hyprland.sh || true; exit 1; fi; done</automated>
    <fails_when>a `SURVIVOR: <pattern>` line is printed followed by the offending `path:line` and the loop exits 1 — a deleted symbol, one of its call sites, or a tombstone comment naming it is still in the wrapper</fails_when>
    <automated>printf '' | ./arch/dots-hyprland.sh install-setups --dry-run >/tmp/p16-01-setups.txt 2>&1 && ! grep 'would exec' /tmp/p16-01-setups.txt | grep -q -- '--skip-backup' && printf '' | ./arch/dots-hyprland.sh install --dry-run >/tmp/p16-01-install.txt 2>&1 && grep 'would exec' /tmp/p16-01-install.txt | grep -q -- '--skip-backup' && ! grep -q "Type 'yes'" /tmp/p16-01-install.txt</automated>
    <fails_when>non-zero exit — either dry-run exited non-zero, the `install-setups` would-exec line carries the skip-backup long option (the predicate is unconditional rather than scoped), the `install` would-exec line does not carry it (the predicate never fires), or the exact-token gate string still appears on the install path</fails_when>
    <automated>test "$(grep -c 'print_lines' arch/dots-hyprland.sh)" -ge 4 && printf '' | ./arch/dots-hyprland.sh uninstall --dry-run >/tmp/p16-01-uninst.txt 2>&1; echo "uninstall exit=$?"; grep -q 'print_lines' arch/dots-hyprland.sh</automated>
    <fails_when>the `print_lines` occurrence count is below 4 (one definition plus three callers in the surviving uninstall helpers), or the echoed `uninstall exit=` value is anything but 0 — a non-zero value here is the `set -u` unbound-variable abort that a shrunk signature with an un-shrunk positional read produces</fails_when>
  </verify>
  <acceptance_criteria>
    - `bash -n arch/dots-hyprland.sh` exits 0.
    - `scripts/phase16-retire-assert.sh` exists, is executable, and its final line is exactly `=== done: FAIL=0 ===`.
    - `arch/dots-hyprland.sh` contains no line matching `^SAFE_DEFAULTS=`, and no occurrence of `II_BACKUP_DIR`, `backup_gate`, `needs_safe_defaults`, `is_help_only_user_flags`, `user_flags_contain`, or `allow-skip-backup` — in code or in comments.
    - `arch/dots-hyprland.sh` still contains the literal `would exec`, the literal `cmd[@]`, and a `preflight` call inside `run_install_family`.
    - `printf '' | ./arch/dots-hyprland.sh install-files --dry-run` prints a `would exec` line containing none of `--skip-hyprland`, `--skip-sysupdate`, and no standalone `--core` under the ERE `(^|[[:space:]])--core([[:space:]]|$)`.
    - The `would exec` line of `install --dry-run` and of `install-files --dry-run` contains `--skip-backup`; the `would exec` line of `install-setups --dry-run` does not.
    - `install --dry-run` output contains no occurrence of `Type 'yes'`.
    - `install --full --dry-run` exits 0, prints a `[CONFIG]` line stating the flag is ignored, and its `would exec` line does not contain `--full`.
    - `install-deps --full --dry-run` exits 0.
    - `grep -c 'print_lines' arch/dots-hyprland.sh` is at least 4.
    - `uninstall --dry-run` exits 0.
  </acceptance_criteria>
  <done>A bare install invocation runs the full behavior end to end — parsed, preflighted, argv-built with no profile decision, and handed to upstream through the array exec — and `scripts/phase16-retire-assert.sh` proves it at `FAIL=0`. The package-marking and session-hook machinery is still present and still called from the uninstall path; task 3 removes it. `scripts/phase12-full-smoke.sh` and `scripts/phase13-d19-assert.sh` are expected-red from this commit and are repaired in plans `16-02` and `16-06` respectively.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the package-marking and session-hook machinery, and shrink the two uninstall signatures</name>
  <reversibility rating="costly">Implements D-07 and D-08. Restoring the package-marking capability means rebuilding a 60-entry array and four collect helpers from git history; in the meantime an orphan sweep can remove the personal stack. Flagged at Task 1, not separately gated.</reversibility>
  <read_first>
    - `arch/dots-hyprland.sh` — as left by task 2. Re-derive every symbol range with `grep -n '^[A-Za-z_][A-Za-z0-9_]*()' arch/dots-hyprland.sh` and read the definition immediately above and below each deletion target; a symbol occupies from its own line to the line before the next definition, at that instant only.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md` — §"Wrapper Surgery Map", specifically the two call-site tables and the "Signature changes forced by the deletions" subsection with its `set -u` hazard note; and §"Anti-Patterns to Avoid".
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md` — D-07, D-08, D-10, D-11, D-13, and the `<code_context>` integration-points note about the collision inside `uninstall_gate`.
  </read_first>
  <files>arch/dots-hyprland.sh</files>
  <action>
Remove the two machinery clusters whose only purpose was the retired model, keep the uninstall path working, and shrink the two function signatures the removals force. Work bottom-up again and re-grep after every deletion.

**A. Package-marking cluster (D-07).** Delete these definitions in full: the `PROTECT_EXPLICIT` array, `resolve_real_package_name`, `collect_installed_protect_packages`, `collect_missing_protect_packages`, `protect_explicit_packages`, `install_missing_protect_packages`, `run_protect`.

**`print_lines` must survive.** It sits between `user_flags_contain` and `resolve_real_package_name` in the file, inside the range earlier notes described as dead, but it has three live callers in the surviving uninstall helpers `collect_ii_meta_packages`, `collect_ii_config_targets` and `collect_ii_state_targets`. Confirm with `grep -n 'print_lines' arch/dots-hyprland.sh` before and after — the count must not drop.

Call sites to remove with the cluster: the `protect` element of the `ALLOWLIST` array (removing it from the array is what actually retires the subcommand — deleting the function alone would leave a dispatch hole); the `protect)` arm in `main`'s dispatch `case`; the two post-setup call sites left over in `run_install_family` if task 2 did not already take them; the preview branch inside `uninstall_gate` and the `collect_installed_protect_packages` call above it; the two branches in `run_safe_uninstall` (one on the dry-run path, one on the real path); the `--skip-protect)` case arm in `run_uninstall`; and the trailing `[DONE]` echo near the end of `run_safe_uninstall` that reports personal-stack packages re-marked as explicit — that last one is a site `16-CONTEXT.md` does not enumerate.

**B. Session-hook cluster (D-08).** Delete these definitions in full: `list_hypr_ii_hook_target_files`, `list_active_hypr_ii_hook_files`, `list_any_hypr_ii_hook_files`, `file_has_active_ii_hooks`, `file_has_commented_ii_hooks`, `warn_hypr_ii_hooks`, `disable_hypr_ii_hooks`, `enable_hypr_ii_hooks`.

Call sites: the `list_active_hypr_ii_hook_files` call and the preview branch inside `uninstall_gate`; the disable-plus-warn pairs on both the dry-run and real paths of `run_safe_uninstall`; the leftover-hook re-scan block near the end of `run_safe_uninstall` guarded on the keep-hooks flag — another site `16-CONTEXT.md` does not enumerate; the `--keep-hypr-hooks)` case arm in `run_uninstall`; and any remaining post-setup enable calls in `run_install_family`.

The justification is verified live state, and it is what the playbook will say in wave 3: `~/.config/hypr/hyprland.conf` no longer exists, the session loads through `hyprland.lua`, and ii supplies both hooks itself in its own Lua tree. **Do not delete the repo copy `.config/hypr/hyprland.conf` and do not strip its two hook lines** — WR-02 is still open on what that file is for.

**C. Signature shrink.** `uninstall_gate` currently takes five positional parameters and `run_safe_uninstall` six; removing the keep-hooks and skip-protect parameters reduces them to three and four. Delete the corresponding `local ...="$N"` reads **in the same edit** as the two call sites in `run_uninstall`, and renumber the remaining positional reads so they are contiguous. Under `set -euo pipefail` a `local x="$5"` left in a function now called with four arguments is an unbound-variable abort, not a silent default. Also delete the two local variables in `run_uninstall` that fed those parameters, and confirm no other reader remains.

**D. Header comment.** The file's own header comment block describes uninstall and the package-marking subcommand as the two wrapper-owned paths. Reword it so it names only the surviving wrapper-owned path — the uninstall — and keeps the standing warning that upstream's own removal subcommand is never called from the normal path. Do not name the removed subcommand.

**E. Leave `run_upstream_uninstall_dangerous` and its `--upstream-dangerous` flag completely alone**, including the type-token guard in front of it and its dispatch from `run_uninstall`. `16-CONTEXT.md` D-10 is silent on this path; it is orthogonal to the retired profile, and after this phase it is the only remaining route to a cascading removal. Recorded as assumption A3 below — considered, and deliberately kept unchanged.

**Comment hygiene** as in task 2: cite D-07, D-08, D-10, D-13, never the names of the identifiers you removed.

**On the retired wording (D-13).** The word "safe" in "safe uninstall" means "does not run a cascading removal" and is unrelated to the retired profile. It stays. What goes is the stale *dual-run* qualifier attached to it in the usage text and in the `[UNINSTALL]` banner echo — that wording described the machinery being deleted here. Plan `16-02` rewrites the usage heredoc; in this task, fix only the `[UNINSTALL]` banner echo inside `uninstall_gate` so it names the wrapper-owned uninstall without the retired qualifier.
  </action>
  <verify>
    <automated>bash -n arch/dots-hyprland.sh && ./scripts/phase16-retire-assert.sh >/tmp/p16-01b-assert.txt 2>&1; tail -2 /tmp/p16-01b-assert.txt; grep -q '^=== done: FAIL=0 ===$' /tmp/p16-01b-assert.txt</automated>
    <fails_when>`bash -n` exits non-zero, or the assert summary line is anything but the exact `=== done: FAIL=0 ===` — the `[FAIL]` lines above it name which retirement assert regressed while the machinery was being removed</fails_when>
    <automated>for s in 'PROTECT_EXPLICIT' 'resolve_real_package_name' 'collect_installed_protect_packages' 'collect_missing_protect_packages' 'protect_explicit_packages' 'install_missing_protect_packages' 'run_protect' 'list_hypr_ii_hook_target_files' 'list_active_hypr_ii_hook_files' 'list_any_hypr_ii_hook_files' 'file_has_active_ii_hooks' 'file_has_commented_ii_hooks' 'warn_hypr_ii_hooks' 'disable_hypr_ii_hooks' 'enable_hypr_ii_hooks' 'skip-protect' 'keep-hypr-hooks'; do if grep -q -- "$s" arch/dots-hyprland.sh; then echo "SURVIVOR: $s"; grep -n -- "$s" arch/dots-hyprland.sh || true; exit 1; fi; done; test "$(grep -c 'print_lines' arch/dots-hyprland.sh)" -ge 4</automated>
    <fails_when>a `SURVIVOR: <pattern>` line is printed with its `path:line` and the loop exits 1 — a deleted definition, a call site, a retired flag arm or a tombstone comment naming one is still present; or the final `test` exits non-zero because `print_lines` lost callers, which means the keep-list symbol or one of the three surviving uninstall helpers was deleted with the cluster</fails_when>
    <automated>printf '' | ./arch/dots-hyprland.sh uninstall --dry-run >/tmp/p16b-u.txt 2>&1; echo "uninstall_dry_exit=$?"; ./arch/dots-hyprland.sh protect --dry-run >/tmp/p16b-p.txt 2>&1 || true; grep -q 'non-allowlisted subcommand: protect' /tmp/p16b-p.txt</automated>
    <fails_when>the echoed `uninstall_dry_exit=` value is anything but 0 — that is the `set -u` unbound-variable abort a shrunk signature with an un-shrunk positional read produces — or the final grep exits non-zero because the retired subcommand was not refused by the allowlist</fails_when>
  </verify>
  <acceptance_criteria>
    - `bash -n arch/dots-hyprland.sh` exits 0 and `./scripts/phase16-retire-assert.sh` still ends in exactly `=== done: FAIL=0 ===`.
    - `arch/dots-hyprland.sh` contains no occurrence of any of: `PROTECT_EXPLICIT`, `resolve_real_package_name`, `collect_installed_protect_packages`, `collect_missing_protect_packages`, `protect_explicit_packages`, `install_missing_protect_packages`, `run_protect`, `list_hypr_ii_hook_target_files`, `list_active_hypr_ii_hook_files`, `list_any_hypr_ii_hook_files`, `file_has_active_ii_hooks`, `file_has_commented_ii_hooks`, `warn_hypr_ii_hooks`, `disable_hypr_ii_hooks`, `enable_hypr_ii_hooks`, `skip-protect`, `keep-hypr-hooks` — in code or in comments.
    - `grep -c 'print_lines' arch/dots-hyprland.sh` is at least 4, and `collect_ii_meta_packages`, `collect_ii_config_targets`, `collect_ii_state_targets` all still exist.
    - The `ALLOWLIST` array assignment in `arch/dots-hyprland.sh` lists exactly `install`, `install-deps`, `install-setups`, `install-files`, `uninstall`.
    - `./arch/dots-hyprland.sh protect --dry-run` exits non-zero and prints a line containing `non-allowlisted subcommand: protect`.
    - `printf '' | ./arch/dots-hyprland.sh uninstall --dry-run` exits 0.
    - `arch/dots-hyprland.sh` still contains `run_upstream_uninstall_dangerous` and `upstream-dangerous`, byte-unchanged from HEAD~ for that function: `git diff -U0 HEAD~ -- arch/dots-hyprland.sh | grep -c 'upstream_uninstall_dangerous'` is 0.
    - `uninstall_gate` and `run_safe_uninstall` each read a contiguous run of positional parameters with no gap and no read beyond their new arity, and their call sites in `run_uninstall` pass exactly that many arguments.
  </acceptance_criteria>
  <done>The wrapper contains only machinery the full-only model needs. Uninstall still gates, still removes ii packages without a cascade, still removes ii configs and state, and still honours its four surviving flags. The retired subcommand is refused by the allowlist rather than by a missing function. `usage()` is now stale — it still documents removed flags — and is rewritten in plan `16-02`, which also repairs `scripts/phase12-full-smoke.sh`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator argv → upstream `./setup` | Every token the wrapper does not explicitly parse falls through `user_flags` and is handed to a privileged installer. This is the boundary D-05 exists to hold. |
| wrapper → live filesystem | `uninstall` deletes paths under `$HOME`; `install` hands the filesystem to upstream. After D-06/D-09 nothing wrapper-owned stands in front of the install side. |
| wrapper stdout → assert scripts and published docs | Five scripts and one playbook grep this program's literal output. A reworded echo silently breaks a contract that has no compiler. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Tampering | `run_install_family` argv `case`, the `*)` catch-all | high | mitigate | Keep an explicit `--full)` arm that swallows the flag and never appends it to `cmd`; the assert proves it with a two-stage `grep 'would exec' \| grep -- '--full'` so upstream's `getopt` never sees an unknown long option |
| T-16-02 | Elevation of Privilege | `ALLOWLIST` + `is_allowlisted` before dispatch | high | mitigate | The retired subcommand is removed from the array, not just from dispatch; task 3's verify asserts the refusal message rather than merely the absence of the function |
| T-16-03 | Tampering | `cmd` array construction and the `cd` subshell exec | high | mitigate | Array-only exec preserved verbatim (D-11); acceptance criteria assert the literal `cmd[@]` still present and forbid any `eval` |
| T-16-04 | Denial of Service | `set -euo pipefail` + shrunk `uninstall_gate` / `run_safe_uninstall` signatures | high | mitigate | Positional reads and call sites changed in one edit; verified by an actual `uninstall --dry-run` exit-code check, which is where an unbound-variable abort surfaces |
| T-16-05 | Destruction of data | install path after D-06 / D-09 | high | accept | Accepted risk with named user acceptance: `16-CONTEXT.md` D-06 and D-09 record that the user was told an install then has no restore path and chose to proceed. Re-confirmed at Task 1's blocking checkpoint. Existing snapshots on disk are left untouched. |
| T-16-06 | Denial of Service | package marks after D-07 | medium | accept | Accepted risk with named user acceptance: `16-CONTEXT.md` D-07 records that the user was told an orphan sweep can then remove `hyprland`, `kitty`, `starship`, `cliphist`, `bc` and `jq`, and chose to proceed. Flagged, not gated (rated costly, not one-way). |
| T-16-07 | Destruction of data | `run_upstream_uninstall_dangerous` — the only remaining cascade route | medium | mitigate | Left byte-unchanged including its type-token guard; acceptance criteria assert the function is not in this plan's diff (assumption A3) |
| T-16-08 | Information Disclosure | wrapper stdout | low | accept | The wrapper prints paths and flags, no credentials. The one env var in play, `ILLOGICAL_IMPULSE_VIRTUAL_ENV`, is a path. |
| T-16-SC | Tampering | package-manager installs | low | accept | This phase adds no `npm` / `pip` / `cargo` / `pacman` install step — it deletes one. `16-RESEARCH.md` § Package Legitimacy Audit records the ecosystem gate as not applicable, with no `[ASSUMED]`, `[SUS]` or `[SLOP]` entries. |
</threat_model>

## Artifacts this phase produces

**Created by this plan:**

| Artifact | Kind | Note |
|----------|------|------|
| `scripts/phase16-retire-assert.sh` | new file | the retirement contract; extended with the documentation ban-grep half by plan `16-04` (D-35, D-36) |
| `touches_files()` | new function in `arch/dots-hyprland.sh` | one-line subcommand predicate scoping the upstream skip-backup append (research Pitfall 4 / A1) |
| `[CONFIG]` ignored-note echo for `--full` | new output line in `arch/dots-hyprland.sh` | quoted by the playbook in wave 3 (D-05, D-23); the exact wording is set here and must not drift afterwards |

**Deleted by this plan — intentional removals, not omissions:**

`SAFE_DEFAULTS`, `II_BACKUP_DIR`, `needs_safe_defaults()`, `backup_gate()`, `is_help_only_user_flags()`, `user_flags_contain()`, `PROTECT_EXPLICIT`, `resolve_real_package_name()`, `collect_installed_protect_packages()`, `collect_missing_protect_packages()`, `protect_explicit_packages()`, `install_missing_protect_packages()`, `run_protect()`, `list_hypr_ii_hook_target_files()`, `list_active_hypr_ii_hook_files()`, `list_any_hypr_ii_hook_files()`, `file_has_active_ii_hooks()`, `file_has_commented_ii_hooks()`, `warn_hypr_ii_hooks()`, `disable_hypr_ii_hooks()`, `enable_hypr_ii_hooks()`; the `protect` subcommand and its `ALLOWLIST` entry; the flags `--allow-skip-backup`, `--skip-protect`, `--keep-hypr-hooks`; the `[CONFIG] Type 'yes' to continue` install gate; the `[CONFIG] safe defaults:` and `[CONFIG] full profile:` argv-branch echoes; the `[CONFIG] dry-run: after setup, would ...` mirror echoes.

**Explicitly NOT deleted:** `print_lines()`, `preflight()`, `is_allowlisted()`, `safe_rm_path()`, `uninstall_gate()`, `run_safe_uninstall()`, `run_upstream_uninstall_dangerous()`, the `[CONFIG] dry-run: would exec from ...` line, the `[INSTALL] ...` line, the repo copy `.config/hypr/hyprland.conf` and its two hook lines.

## Flagged Assumptions

Surfaced, not silently resolved. The spec-less edge probe was **skipped this run** — the phase has no requirement IDs in `ROADMAP.md` (`**Requirements**: TBD`) and no `16-SPEC.md` exists, so there was nothing to probe. That skip is recorded here rather than papered over with a fabricated coverage report; `must_haves` above is derived directly from `16-CONTEXT.md`'s decisions and `16-RESEARCH.md`'s verified findings.

| # | Item | Status | Why it is flagged |
|---|------|--------|-------------------|
| A1 | The upstream skip-backup flag is appended for `install` / `install-files` only, via a renamed one-line predicate, rather than unconditionally | resolved by the planner | Upstream reads the flag only in `3.files.sh`, which `setup` sources for those two subcommands. Appending it everywhere is harmless but puts a flag in the dry-run preview that cannot affect anything. `16-RESEARCH.md` calls this a planner decision, not a user question. Gives the assert a crisp negative on `install-setups`. |
| A2 | `--full` becomes accepted on all four install-family subcommands; `install-deps --full` flips from exit 1 to exit 0 | resolved by the planner | The scope check used the same predicate D-04 deletes. A no-op alias with a scope restriction is self-contradictory, and the printed note makes the behavior self-explaining. User-visible, so it is asserted explicitly rather than left implicit. |
| A3 | `run_upstream_uninstall_dangerous` and `--upstream-dangerous` survive unchanged | resolved by the planner, conservative reading | `16-CONTEXT.md` D-10 enumerates what uninstall keeps and loses and is silent on this path. It is orthogonal to the retired profile, so keeping it is the conservative reading — but it becomes the only remaining cascade route in a phase that is otherwise removing safety machinery. Recorded as a diff-level acceptance criterion so a later reader can see it was considered. |
| A4 | The re-pin in plan `16-06` cites the last commit that touched `arch/dots-hyprland.sh`, not this plan's commit | resolved by the planner | The wrapper is touched again in plan `16-02` (usage rewrite). `git diff <BASE> -- <path>` compares BASE against the **working tree**, so the baseline must be the final wrapper state. `16-06` resolves it at run time rather than hard-coding a SHA from this plan. |
| DIV-1 | `16-RESEARCH.md` recommends a five-wave decomposition with the wrapper surgery and the smoke rewrite in one wave-1 commit | divergence, deliberate | The smoke rewrite and the `usage()` heredoc rewrite are moved to plan `16-02` so no plan exceeds three tasks at `granularity: fine`. Ordering is unchanged: the wrapper is still final before any documentation is written (D-42), and `16-02` is still ahead of the wave-3 documentation plans. |

<verification>
- `scripts/phase16-retire-assert.sh` is the regression floor from task 2 onward and must be at `FAIL=0` at the end of every task in this plan.
- `scripts/phase12-full-smoke.sh` and `scripts/phase13-d19-assert.sh` are **expected-red** from task 2 and must not be run as gates here, nor "repaired" by loosening them. `16-02` rewrites the first; `16-06` re-pins the second.
- `scripts/phase14-verify.sh` is not run in this plan at all: it asserts a clean working tree outside a hard-coded Phase 14 prefix, so it cannot pass mid-wave (research Pitfall 1). It is edited in `16-03` and run at the phase gate in `16-10`.
- Every line number quoted in `16-CONTEXT.md` and `16-RESEARCH.md` is a navigation aid only. Re-derive with `grep -n` before each edit and again after each deletion (D-12). Research verified that the deletion-target numbers are exact at baseline but that the keep-list numbers drift by up to 24 lines, so validate the keep list by symbol name, never by quoted line.
- The `--core` omission check must use the word-boundary ERE `(^|[[:space:]])--core([[:space:]]|$)`, not a fixed-string grep.
- The "flag not forwarded" check must grep the `would exec` line only, never the whole capture, or the ignored-note fails its own assert.
</verification>

<success_criteria>
- A bare `install` or `install-files` runs what the opt-in full path ran before, with no residual flags and no wrapper prompt.
- The retired opt-in flag is still accepted and still never reaches upstream.
- `uninstall` works, gates, and does not cascade.
- `scripts/phase16-retire-assert.sh` exists and exits 0.
- `bash -n` is clean and no deleted symbol survives anywhere in the wrapper, including in comments.
</success_criteria>

<output>
Create `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-01-SUMMARY.md` when done.
</output>