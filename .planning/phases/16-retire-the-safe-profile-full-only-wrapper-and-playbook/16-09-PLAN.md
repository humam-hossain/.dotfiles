---
phase: 16-retire-the-safe-profile-full-only-wrapper-and-playbook
plan: 09
type: execute
wave: 6
depends_on: ["16-07", "16-08"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [IN-11, D-01, D-03, D-28, D-30]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The roadmap's milestone description states the outcome that shipped — one install path — rather than the two-profile plan the milestone started with (D-28)."
    - "The roadmap's coverage line reads nineteen requirements mapped, matching the arithmetic plan `16-07` wrote into the requirement set (D-27, D-28)."
    - "The Phase 10 success criterion whose second half mirrors the rewritten inventory requirement is corrected the same way, so the two do not disagree (D-28)."
    - "The per-phase roadmap lines for Phases 11, 12 and 14 are left as history; they record what those phases delivered (D-28)."
    - "`.planning/STATE.md` carries no current-state claim that this phase falsified, and its accumulated-context entries that record past decisions are annotated rather than rewritten (D-30)."
    - "The open-review entry for the deleted preflight script is closed in the state file, matching the closure recorded in the milestone audit (D-30, D-33)."
    - "No Phase 17 exists anywhere in the roadmap; this phase is the merged one (D-01)."
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Milestone description, Phase 10 criterion and coverage line consistent with what shipped"
      contains: "19/19"
    - path: ".planning/STATE.md"
      provides: "Project state whose current position and carry-forward decisions are true"
      contains: "superseded"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the coverage line must equal the arithmetic in the requirement set"
      pattern: "19"
    - from: ".planning/STATE.md"
      to: ".planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md"
      via: "the state file's carry-forward entries point at the phase record for what changed"
      pattern: "16-DOC-SWEEP\\.md"
  prohibitions:
    - statement: "Neither file is rewritten whole. Both are edited with scoped replacements only; a full-file write over either would destroy entries outside the edit window and both are also written by tooling during execution."
      status: active
      verification: backstop
    - statement: "The per-phase roadmap lines for Phases 11, 12 and 14, and the Phase 12 line in the milestone body, are left as history and not corrected (D-28)."
      status: active
      verification: backstop
    - statement: "No Phase 17 is added to the roadmap. The wrapper change and the documentation retirement are one phase (D-01)."
      status: active
      verification: backstop
    - statement: "The deferred-items and concerns entries for the three-roles question and the compositor-target autostart keep their open, unowned status. This phase does not close them and does not assign them an owner (CONTEXT.md 'Not in this phase')."
      status: active
      verification: backstop
    - statement: "The state file's progress counters, milestone fields and front-matter are not hand-edited; those are written by tooling and a manual edit will be overwritten or will conflict."
      status: active
      verification: backstop
    - statement: "`arch/dots-hyprland.sh` is not modified. The drift baseline pinned in plan `16-06` compares against the working tree."
      status: active
      verification: backstop
---

<objective>
Bring the two tooling-managed planning documents into agreement with the phase, using scoped edits only.

The roadmap describes this milestone as one where the wrapper gains an opt-in profile while the safe defaults remain the default, and the playbook documents the two side by side. Neither half is what shipped. Its coverage line still counts twenty-two requirements after plan `16-07` reduced the set to nineteen. And one Phase 10 success criterion carries the same promise the inventory requirement just lost, so leaving it would make the two documents disagree with each other.

The state file has the same two classes as the project file: current-state claims that are now false, and accumulated-context entries recording past decisions that stay true about the past.

Purpose: both files are also written by the workflow tooling during execution. That is why this plan runs alone in its own wave and why every edit is scoped.
Output: a roadmap whose milestone prose and arithmetic match what shipped, and a state file whose present tense is true.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md
@.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md
</context>

<tasks>

<!-- planner-discipline-allow: SAFE_DEFAULTS -->
<!-- planner-discipline-allow: dual-run -->
<!-- planner-discipline-allow: --skip-hyprland -->
<!-- planner-discipline-allow: --core -->
<!-- planner-discipline-allow: --skip-sysupdate -->
<!-- planner-discipline-allow: --full -->
<!-- planner-discipline-allow: safe vs full -->
<!-- planner-discipline-allow: phase14-preflight.sh -->

<task type="auto">
  <name>Task 1: Amend the roadmap's milestone prose, Phase 10 criterion and coverage line</name>
  <read_first>
    - `.planning/ROADMAP.md` — read the milestone list, the overview paragraph, the not-this-milestone line, the Phase 10 entry with its success criteria, the Phase 16 entry, the progress table and the coverage line at the end. Locate each with `grep -n` rather than by quoted line number.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md` — D-28's list of amend sites and, equally important, its explicit leave-as-history list; plus D-03, which is why this file may be edited directly at all, and D-01, which forbids a Phase 17.
    - `.planning/REQUIREMENTS.md` — as amended by plan `16-07`, for the arithmetic the coverage line must match.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md` — the roadmap sweep section confirming each site and giving the coverage line's location.
  </read_first>
  <files>.planning/ROADMAP.md</files>
  <action>
Four scoped edits. Use `Edit` with a unique surrounding anchor for each; never write the file whole. The workflow tooling also commits to this file during execution, and a full-file write would drop phase entries outside your diff window.

**The milestone overview paragraph.** It describes the milestone as moving from the retired session model to a full install path, with the wrapper gaining an opt-in profile while the residual defaults remain the default, and the playbook documenting the two side by side. Rewrite the second half: the wrapper's only install path is full, the residual defaults are retired, and the playbook documents one install path. Keep the first half — the inventory-then-disposition gate — because that is exactly how the milestone ran and it is still the process that governs.

**The not-this-milestone line.** It excludes a default removal of the session chrome. That removal was accepted in Phase 11 and executed in the Phase 14 adopt. Remove that exclusion. Leave the other exclusions on the line intact.

**The Phase 10 success criterion.** Its second half promises that the retired install profile remains available after this milestone — the same clause the inventory requirement just lost. Drop that clause and keep the first half, which records that the inventory captured the residual behavior. Correcting both the same way is the point: leaving one would put the roadmap and the requirement set in direct contradiction.

**The coverage line.** It reads twenty-two requirements mapped with zero unmapped. Plan `16-07` reduced the set to nineteen. Update both numbers to nineteen and keep the unmapped count at zero and the v0.1 and v0.2 clauses unchanged.

**Leave as history:** the per-phase line for the wrapper-profile phase in the milestone body, the Phase 11 success criterion about the residual default, the Phase 12 success criterion about the meta flag, and the Phase 14 success criterion about the session-chrome policy. These record what those phases delivered and D-28 names each one as out of scope for this sweep.

**Do not add a Phase 17.** The wrapper change and the documentation retirement are one phase by decision. If the phase list or the progress table implies a successor phase, leave it alone; adding one here would contradict the phase's own shape.

**Do not hand-edit the progress table's counts for this phase.** Those are maintained by the workflow tooling when the phase completes.
  </action>
  <verify>
    <automated>R=.planning/ROADMAP.md; grep -q '19/19 requirements mapped' "$R" && grep -q '0 unmapped' "$R" && ! grep -q '22/22' "$R"</automated>
    <fails_when>non-zero exit — the coverage line still reads the pre-amendment count, or the unmapped clause was lost while editing the numbers</fails_when>
    <automated>R=.planning/ROADMAP.md; ! grep -qiE 'wrapper gains an explicit full profile|safe defaults remain the default|documents safe vs full|remains available after this milestone|default removal of Waybar/rofi/swaync' "$R"</automated>
    <fails_when>non-zero exit — one of the five retired claims survives verbatim in the milestone overview, the not-this-milestone line, or the Phase 10 criterion</fails_when>
    <automated>R=.planning/ROADMAP.md; grep -qE '^### Phase 16:' "$R" && ! grep -qE '^### Phase 17:' "$R" && test "$(grep -c '^### Phase ' "$R")" -ge 7 && grep -q 'Depends on:\*\* Phase 15' "$R"</automated>
    <fails_when>non-zero exit — the Phase 16 entry is missing, a Phase 17 entry was added against D-01, the phase-entry count dropped (a scoped edit removed an entry outside its window), or the dependency line was lost</fails_when>
    <automated>R=.planning/ROADMAP.md; C="$(grep -c 'SAFE_DEFAULTS' "$R")"; echo "history SAFE_DEFAULTS occurrences=$C"; test "$C" -ge 1 && grep -qE 'Phase 1[124]' "$R"</automated>
    <fails_when>the echoed count is 0, or the phase references are gone — the sweep over-reached into the per-phase history lines D-28 explicitly leaves alone, which is as much an error as leaving a current-state claim stale</fails_when>
    <automated>git diff --quiet HEAD -- arch/ docs/ scripts/ .config/ stow/ vendor/ .planning/REQUIREMENTS.md .planning/PROJECT.md</automated>
    <fails_when>non-zero exit — this task left an uncommitted change in a file it does not own; the requirement set and the project file were finalised in wave 5 and must not be re-touched here</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains `19/19 requirements mapped` and `0 unmapped`, and contains no `22/22`.
    - The file matches, case-insensitively, none of: the opt-in-profile milestone clause, the residual-defaults-remain-default clause, the profile-comparison playbook clause, the remains-available-after-this-milestone clause, or the chrome-removal exclusion.
    - The file still contains a `### Phase 16:` entry with its `Depends on: Phase 15` line, contains no `### Phase 17:` entry, and has at least seven `### Phase ` entries.
    - At least one occurrence of the residual-array identifier survives in the per-phase history lines D-28 leaves alone.
    - `git diff --quiet HEAD -- arch/ docs/ scripts/ .config/ stow/ vendor/ .planning/REQUIREMENTS.md .planning/PROJECT.md` exits 0.
  </acceptance_criteria>
  <done>The roadmap's milestone prose describes the outcome that shipped, its Phase 10 criterion agrees with the rewritten inventory requirement, and its coverage line matches the requirement set's arithmetic. The per-phase history lines are untouched and no successor phase was invented.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the state file — rewrite the present, annotate the carry-forward</name>
  <read_first>
    - `.planning/STATE.md` — read in full. Distinguish four regions: the front matter and progress block (tooling-owned, do not hand-edit), the current-position and core-value prose, the accumulated-context decision lists, and the operator next steps.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-CONTEXT.md` — D-30's two lists: what is rewritten as current state and what is marked superseded.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-RESEARCH.md` — the state-file sweep section, which names additional sites D-30 does not, including the core-value line that says the remaining work is documentation and the roadmap-evolution entry that still describes this phase by its retired scope.
    - `16-07-SUMMARY.md` and `16-08-SUMMARY.md` — for the superseded-annotation wording chosen earlier in the phase. Reuse it verbatim.
    - `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-DOC-SWEEP.md` — the phase record this file should point at for what changed.
  </read_first>
  <files>.planning/STATE.md</files>
  <action>
Scoped edits only. This file is written by the workflow tooling on every plan completion; re-read it immediately before each edit and use a unique surrounding anchor. Do not touch the front matter, the progress counters, the milestone fields or the performance table.

**Rewrite as current state:**
- The core-value line saying the remaining work is documentation. That stopped being true the moment this phase's scope reached the wrapper and the scripts.
- The current-focus line, which still describes this phase by the two audit items it was originally scoped to. Restate it as the retirement it became.
- The carry-forward decision line describing the wrapper as a thin one carrying the residual defaults and a backup gate with array exec. Only the array exec survives; restate the line to say so.
- The carry-forward decision line about personal session hooks for the environment variable and the shell launch. ii owns both now, in its own Lua tree.
- The operator-next-step asserting that a default install still injects the residual flags. It does not.
- The first operator next step, which still describes this phase by its retired scope. Replace the operator next steps with what is actually next after this phase, keeping any step that is still genuinely outstanding — in particular the two retroactive checks for an earlier phase, if they are still listed and still undone.
- The roadmap-evolution entry describing this phase by its original two-item scope.

**Mark superseded, do not rewrite:**
- The milestone-framing line about not blindly dropping the residual defaults.
- The Phase 11 entry recording the residual as still default.
- The Phase 12 entry recording the opt-in meta flag.
- The Phase 14 entry recording the three-tier rollback.
- The Phase 6 and Phase 10 decision-log rows.
- The deferred-items row whose reason cites the retired session-chrome policy.

Use exactly the annotation wording plans `16-07` and `16-08` used.

**Close one open item.** The concerns list carries an open review entry about the deleted preflight script printing backup rotation as mandatory. It closes with the deletion in plan `16-03`. Move it out of the open concerns into a closed or resolved position with a one-line note naming the plan, matching however the file already distinguishes resolved from open.

**Leave open, and do not assign an owner:** the concern about the three roles of the repository copy of the pre-adopt compositor config, and the deferred item about the compositor-target autostart bootstrap. Both stay exactly as they are, open and unowned.

**Add one pointer.** In the accumulated-context section, add a single line naming this phase's sweep record as where the detail of what changed lives. That is the file a future session should read rather than re-deriving the change set from the plan bodies.

Write `Waybar`, `rofi` and `swaync` literally in every line you touch. The file carries two collective-noun occurrences in entries this plan annotates rather than rewrites; leave those as they are and do not add a third.
  </action>
  <verify>
    <automated>S=.planning/STATE.md; test "$(grep -ci 'supersede' "$S")" -ge 6 && ! grep -qiE 'remaining work is documentation|SAFE_DEFAULTS \+ backup gate|still injects SAFE_DEFAULTS|dual-run restore route' "$S"</automated>
    <fails_when>non-zero exit — fewer than six superseded annotations were applied, or a current-state claim this phase falsified survives verbatim in the core-value line, a carry-forward decision, or an operator next step</fails_when>
    <automated>S=.planning/STATE.md; grep -q '16-DOC-SWEEP.md' "$S" && grep -qiE 'IN-11[^.]{0,120}(closed|resolved|deleted)' "$S" && grep -q 'WR-02' "$S" && grep -q 'graphical-session.target' "$S"</automated>
    <fails_when>non-zero exit — the pointer to the phase sweep record is missing, the review entry for the deleted script is not recorded as closed, or one of the two items that must stay open was removed from the file entirely</fails_when>
    <automated>S=.planning/STATE.md; ! grep -qiE '(owner|owned by|assigned)[^.]{0,50}(WR-02|graphical-session)' "$S" && grep -qiE '(open|unowned)[^.]{0,80}(WR-02|graphical-session|no owning phase)' "$S"</automated>
    <fails_when>non-zero exit — one of the two deferred items was given an owner this phase has no authority to assign, or neither is any longer marked open and unowned</fails_when>
    <automated>S=.planning/STATE.md; test "$(grep -oE '[A-Za-z0-9_-]*[Cc]hrome[A-Za-z0-9_-]*' "$S" | grep -vx 'google-chrome-stable' | wc -l)" -eq 2 && A="$(grep -oiE 'superseded[^.|)]{0,60}' "$S" | sed 's/[[:space:]]*$//' | sort -u | wc -l)"; echo "annotation variants=$A"; test "$A" -le 2</automated>
    <fails_when>the collective-noun count is not exactly 2 — above means a rewritten line introduced one, below means an annotated historical entry was edited away; or the echoed annotation-variant count exceeds 2, meaning the wording drifted from the one plans `16-07` and `16-08` used</fails_when>
    <automated>git diff --quiet HEAD -- arch/ docs/ scripts/ .config/ stow/ vendor/ .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md && ./scripts/phase13-d19-assert.sh >/tmp/p16-09-d.txt 2>&1; test "$(grep -c '^\[FAIL\]' /tmp/p16-09-d.txt)" -eq 0</automated>
    <fails_when>non-zero exit — this task left an uncommitted change in a file it does not own, or the overlay assert regressed, which at this point in the phase can only mean the wrapper or the playbook fence was touched after the baseline was pinned</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` contains at least six superseded annotations, using at most two distinct spellings, matching the wording used in `.planning/REQUIREMENTS.md` and `.planning/PROJECT.md`.
    - The file contains none of: the remaining-work-is-documentation claim, the wrapper-carries-residual-defaults-and-a-backup-gate decision line, the default-still-injects operator step, or the retired restore-route framing of this phase.
    - The file contains a pointer to `16-DOC-SWEEP.md`, and records the preflight-script review entry as closed.
    - Both the three-roles concern and the compositor-target autostart item are still present, still marked open or unowned, and neither has an owner.
    - Non-allowlisted `chrome` tokens in the file: exactly 2.
    - The front matter, progress counters, milestone fields and performance table are byte-identical to their previous state.
    - `git diff --quiet HEAD -- arch/ docs/ scripts/ .config/ stow/ vendor/ .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md` exits 0, and `./scripts/phase13-d19-assert.sh` prints zero `[FAIL]` lines.
  </acceptance_criteria>
  <done>The state file's present tense describes the project after the retirement, its carry-forward history is annotated with the same wording used in the other two files, the review item closed by deletion is recorded as closed, and the two items that stay open are still open and still unowned. A future session reading this file is pointed at the phase sweep record for the detail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state file → the next session's first read | This is the entry point for resuming work. A stale current-focus line sends the next session at a scope that no longer exists. |
| roadmap coverage line → milestone audit arithmetic | A coverage line that disagrees with the requirement set produces a milestone audit finding that is an artifact of bookkeeping rather than of the work. |
| hand edit → tooling-managed file | Both files are written by the workflow tooling during execution. A full-file write races with that and drops entries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-56 | Destruction of data | full-file write over a tooling-managed document | high | mitigate | Scoped `Edit` only, with a re-read immediately before each edit; the phase-entry count and the byte-identical front-matter criterion catch a write that dropped content outside the window |
| T-16-57 | Repudiation | coverage arithmetic disagreeing across two files | high | mitigate | The roadmap line is asserted at nineteen and the pre-amendment count is banned, so a partial edit fails rather than passing |
| T-16-58 | Repudiation | per-phase history over-swept | medium | mitigate | At least one occurrence of the residual-array identifier must survive in the roadmap's history lines; over-sweeping fails the task exactly as under-sweeping does |
| T-16-59 | Repudiation | an open item silently closed or given an owner | high | mitigate | Both deferred items must remain present and marked open, and an owner-assignment pattern is banned outright |
| T-16-60 | Tampering | scope creep into files finalised in earlier waves | medium | mitigate | `git diff --quiet HEAD` covers the code and documentation directories plus the three planning files finalised in waves 5 and 6 |
| T-16-61 | Repudiation | annotation vocabulary drift across three files | medium | mitigate | Variant ceiling of two per file, with the wording sourced from the earlier plans' summaries |
| T-16-62 | Information Disclosure | planning artifacts | low | accept | These files record positions, decisions and paths. No credential appears in either. |
| T-16-SC | Tampering | package-manager installs | low | accept | This plan adds no `npm` / `pip` / `cargo` / `pacman` install step. `16-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable, with no `[ASSUMED]`, `[SUS]` or `[SLOP]` entries. |
</threat_model>

## Artifacts this phase produces

**Created by this plan:** none — no new phase entry, no new requirement, no new concern.

**Modified surfaces:**

| Artifact | Kind | Note |
|----------|------|------|
| the roadmap milestone overview | rewritten | one install path replaces the two-profile plan (D-28) |
| the roadmap not-this-milestone line | amended | the chrome-removal exclusion is dropped as delivered (D-28) |
| the Phase 10 success criterion | amended | its second half mirrors the rewritten inventory requirement (D-28) |
| the roadmap coverage line | recomputed | 22/22 → 19/19 (D-27, D-28) |
| state-file current-state prose | rewritten | core value, current focus, two carry-forward decisions, operator next steps, roadmap evolution (D-30) |
| state-file historical entries | annotated | same wording as the requirement set and the project file (D-30) |
| the preflight-script review entry | closed | closed by the deletion in plan `16-03` (D-33) |

**Deleted by this plan:** nothing — no phase entry, no concern, no deferred item is removed. Two items change status; none disappears.

**Explicitly NOT touched:** the roadmap's per-phase history lines for Phases 11, 12 and 14, the state file's front matter, progress counters, milestone fields and performance table, the two collective-noun occurrences in annotated entries, and every file finalised in an earlier wave.

## Flagged Assumptions

| # | Item | Status | Why it is flagged |
|---|------|--------|-------------------|
| A26 | This plan runs alone in its own wave rather than alongside the other planning-artifact plans | divergence, deliberate | Research puts all planning-artifact edits in one wave. Both files here are written by the workflow tooling on plan completion, so editing them while sibling plans complete in the same wave invites a lost update. Running alone costs one wave and removes the race. |
| A27 | The Phase 16 goal line and plan list in the roadmap are filled by the planner at plan time, not by this plan | resolved by the planner | D-28 assigns the goal line to the planner. It is written during planning so the roadmap is coherent before execution starts; this plan owns only the milestone prose, the Phase 10 criterion and the coverage line. |
| A28 | The operator next steps are replaced rather than annotated | resolved by the planner | They are instructions, not records. An annotated instruction is still an instruction. The two retroactive checks for an earlier phase are carried forward if still outstanding, so replacing the list does not silently drop live work. |

<verification>
- Both files are tooling-managed. Every edit is a scoped `Edit` preceded by a fresh read; a full-file write is forbidden by the prohibition block and is what the phase-entry count and the byte-identical front-matter criterion are there to catch.
- The scope fence widens as the phase progresses: this plan may not touch the code and documentation directories, nor the three planning files finalised in wave 5 and in this plan's own task 1.
- `scripts/phase13-d19-assert.sh` is re-run as the wrapper-untouched check. After plan `16-06` pinned the baseline against the working tree, any wrapper edit turns it red.
- Over-sweeping is checked as explicitly as under-sweeping. The roadmap must retain at least one residual-array occurrence in its history lines, and the state file's collective-noun count is pinned at exactly its existing value.
- The annotation wording is sourced from the earlier plans' summaries rather than re-chosen, and the variant ceiling makes drift detectable.
</verification>

<success_criteria>
- The roadmap describes the milestone that shipped and counts nineteen requirements.
- The state file's present tense is true and its history is annotated with the phase's shared wording.
- The review item closed by deletion is recorded as closed; the two genuinely open items are still open and unowned.
- Nothing outside these two files changed.
</success_criteria>

<output>
Create `.planning/phases/16-retire-the-safe-profile-full-only-wrapper-and-playbook/16-09-SUMMARY.md` when done.
</output>
