---
phase: 08-retire-local-quickshell-product
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - RET-01
user_setup:
  - service: local-host-reinstall
    why: "Only if live health fails — interactive wrapper backup gate + optional yay/sudo during reinstall"
    env_vars: []
    dashboard_config:
      - task: "If reinstall branch taken: type exact yes (case-sensitive) at wrapper backup gate"
        location: "Terminal running ./arch/dots-hyprland.sh install (or install-files)"
      - task: "If upstream auto_backup prompts: prefer y to create/update ~/ii-original-dots-backup"
        location: "Same reinstall session"

must_haves:
  truths:
    - "Live ~/.config/quickshell is a real directory (not a symlink) with ii/shell.qml present before any in-repo product deletion (D-01, D-14, D-15)"
    - "readlink -f of live QS path does not resolve under REPO_ROOT/.config/quickshell"
    - "ii Python venv directory exists at ~/.local/state/quickshell/.venv"
    - "Repo hypr SoT still has env = ILLOGICAL_IMPULSE_VIRTUAL_ENV and exec-once = qs -c ii (LIVE-02 hold)"
    - "If live install was unhealthy: reinstall used only arch/dots-hyprland.sh (D-02/D-13) with dry-run first and backup gate honored (no bare skip-backup)"
    - "No new phase08 smoke scripts were created (D-03); phase07-live-smoke.sh was not used as this plan's success gate"
  artifacts:
    - path: "~/.config/quickshell/ii/shell.qml"
      provides: "Healthy live ii product independent of in-repo tree"
      contains: "Real file under home XDG config (not under .dotfiles product)"
    - path: "~/.local/state/quickshell/.venv"
      provides: "ii Python venv for qs -c ii session tools"
      contains: "Directory present (pyvenv.cfg expected)"
    - path: "arch/dots-hyprland.sh"
      provides: "Sole reinstall entry if health branch fails (D-13)"
      contains: "SAFE_DEFAULTS + backup_gate + allowlist install"
  key_links:
    - from: "inline LIVE-01/02 asserts"
      to: "go/no-go for 08-02 tree delete"
      via: "D-14 step 1 health before any git rm of product"
    - from: "arch/dots-hyprland.sh install|install-files"
      to: "~/.config/quickshell real dir"
      via: "reinstall branch only; dry-run then interactive gate"
  prohibitions:
    - "Must NOT delete live $HOME/.config/quickshell as retirement (D-14)"
    - "Must NOT re-run or resurrect the retired local-product installer under arch/ (D-02)"
    - "Must NOT re-symlink live QS path into the repo product tree (D-15)"
    - "Must NOT create scripts/phase08-*-smoke.sh or rewrite phase07-live-smoke.sh (D-03/D-11)"
    - "Must NOT invert Phase 7 D-04 into a Phase 8 retirement gate (D-03)"
    - "Must NOT pass bare --skip-backup / --allow-skip-backup on routine reinstall (T-8-05)"
    - "Must NOT uninstall packages (D-05)"
    - "Must NOT delete in-repo .config/quickshell in this plan (08-02 owns RET-01 delete)"
---

<objective>
Confirm practical live dots-hyprland install health (or reinstall via the Phase 6 wrapper only) so Phase 8 can safely retire the in-repo product without destroying the running shell — per D-01, D-02, D-03, D-13, D-14, D-15.

Purpose: Gate RET-01 on a live path that is already independent of the repo tree (LIVE-01 hold). Never treat home-tree deletion as retirement.

Output: Documented green live-health evidence (or reinstall + re-assert); machine ready for 08-02 `git rm -r` of the in-repo tree only.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-retire-local-quickshell-product/08-CONTEXT.md
@.planning/phases/08-retire-local-quickshell-product/08-RESEARCH.md
@.planning/phases/08-retire-local-quickshell-product/08-PATTERNS.md
@.planning/phases/08-retire-local-quickshell-product/08-VALIDATION.md
@.planning/phases/07-install-session-hooks-dual-run-verify/07-CONTEXT.md
@arch/dots-hyprland.sh
@scripts/phase07-live-smoke.sh
</context>

## Phase Goal

**Single product path — remove the v0.1 in-repo Quickshell tree and old installer.** This plan only ensures the live installed ii path is healthy (or restored via wrapper) before any repo product deletion.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Live health green evidence (or reinstall repair) | `~/.config/quickshell/ii/shell.qml` + venv + hypr hooks | **08-01** |
| In-repo product tree deleted (RET-01) | `.config/quickshell/` gone from REPO | 08-02 |
| Old installer hard-deleted (RET-02) | `arch/quickshell.sh` gone | 08-03 |
| Minimal ref cleanup (optional comment) | `arch/dots-hyprland.sh` header only | 08-03 |

**Deletions are not performed in this plan.** Protect live home tree and wrapper.

## Non-gate notice (D-03 / D-11)

Do **not** run `./scripts/phase07-live-smoke.sh` as this phase's success gate. After 08-02 deletes the in-repo tree, that script's Phase 7 D-04 assert ("in-repo `.config/quickshell` still present") is **expected red**. Leave the script frozen. Lift only LIVE-01/02-style asserts **inline** below (no new smoke files).

## Tasks

<task type="auto">
  <name>Task 1: Inline live install health asserts (D-01, D-14, D-15)</name>
  <files></files>
  <read_first>
    - .planning/phases/08-retire-local-quickshell-product/08-CONTEXT.md (D-01, D-02, D-03, D-14, D-15)
    - .planning/phases/08-retire-local-quickshell-product/08-RESEARCH.md (Pattern 1 health gate; Code Examples live install health; Pitfalls 1–4)
    - .planning/phases/08-retire-local-quickshell-product/08-PATTERNS.md (inline LIVE-01/02 lift; D-04 non-gate)
    - .planning/phases/08-retire-local-quickshell-product/08-VALIDATION.md (08-01 rows; non-gates)
    - scripts/phase07-live-smoke.sh (LIVE-01/02 blocks only — lift asserts; do not run full script as gate)
    - arch/dots-hyprland.sh (reinstall entry awareness only — do not edit)
  </read_first>
  <action>
    From REPO_ROOT, prove live dots-hyprland install health with **inline** bash only (per D-01, D-03, D-14, D-15). Do not create any new script under scripts/.

    1. Resolve REPO_ROOT via `git rev-parse --show-toplevel` and cd there.
    2. LIVE-01 path shape (hard fail if any fail):
       - `test ! -L "${HOME}/.config/quickshell"`
       - `test -d "${HOME}/.config/quickshell"`
       - `test -f "${HOME}/.config/quickshell/ii/shell.qml"`
       - `case "$(readlink -f "${HOME}/.config/quickshell")" in */.dotfiles/.config/quickshell*) echo "[FAIL] live under repo product"; exit 1 ;; esac`
    3. Venv: `test -d "${HOME}/.local/state/quickshell/.venv"`.
    4. Personal hypr not renamed away: `test -f "${HOME}/.config/hypr/hyprland.conf"` and `test ! -e "${HOME}/.config/hypr/hyprland.conf.old"`.
    5. LIVE-02 hooks in **repo** SoT:
       - `grep -E 'env = ILLOGICAL_IMPULSE_VIRTUAL_ENV,' "${REPO_ROOT}/.config/hypr/hyprland.conf"`
       - `grep -E 'exec-once = qs -c ii' "${REPO_ROOT}/.config/hypr/hyprland.conf"`
    6. Soft (non-blocking): optional `pgrep -a qs | grep -E -- '-c ii|\bii\b'` and `pgrep -x waybar` — record in SUMMARY; do not formal LIVE-04 chrome re-ceremony (D-03).
    7. Explicitly do **not**: delete live home QS tree; re-symlink live→repo; run the retired local-product installer under arch/; start in-repo tree deletion; run full phase07-live-smoke as the gate.
    8. Record pass/fail of each hard assert in 08-01-SUMMARY. If any hard assert fails, Task 2 reinstall branch is mandatory. If all hard asserts pass, Task 2 documents skip.
  </action>
  <verify>
    <automated>set -euo pipefail; REPO_ROOT="$(git rev-parse --show-toplevel)"; test ! -L "${HOME}/.config/quickshell"; test -d "${HOME}/.config/quickshell"; test -f "${HOME}/.config/quickshell/ii/shell.qml"; case "$(readlink -f "${HOME}/.config/quickshell")" in */.dotfiles/.config/quickshell*) echo "[FAIL] under repo"; exit 1 ;; esac; test -d "${HOME}/.local/state/quickshell/.venv"; test -f "${HOME}/.config/hypr/hyprland.conf"; test ! -e "${HOME}/.config/hypr/hyprland.conf.old"; grep -E 'env = ILLOGICAL_IMPULSE_VIRTUAL_ENV,' "${REPO_ROOT}/.config/hypr/hyprland.conf" >/dev/null; grep -E 'exec-once = qs -c ii' "${REPO_ROOT}/.config/hypr/hyprland.conf" >/dev/null; echo "[VERIFY] OK live health hard asserts"</automated>
  </verify>
  <acceptance_criteria>
    - Live QS path is not a symlink and is a directory.
    - `${HOME}/.config/quickshell/ii/shell.qml` exists as a regular file.
    - `readlink -f` of live QS path does not match `*/.dotfiles/.config/quickshell*`.
    - `${HOME}/.local/state/quickshell/.venv` exists as a directory.
    - Repo `.config/hypr/hyprland.conf` contains both `env = ILLOGICAL_IMPULSE_VIRTUAL_ENV,` and `exec-once = qs -c ii`.
    - No new file matching `scripts/phase08*` was created.
    - Full `./scripts/phase07-live-smoke.sh` was not required to exit 0 for this task.
  </acceptance_criteria>
  <done>Hard live-health asserts evaluated; pass → skip reinstall, fail → Task 2 reinstall required.</done>
</task>

<task type="auto">
  <name>Task 2: Reinstall via wrapper only if health failed (D-02, D-13, T-8-05)</name>
  <files></files>
  <read_first>
    - arch/dots-hyprland.sh (SAFE_DEFAULTS, backup_gate, dry-run, allowlist — invoke only, do not edit)
    - .planning/phases/08-retire-local-quickshell-product/08-CONTEXT.md (D-02, D-05, D-13)
    - .planning/phases/08-retire-local-quickshell-product/08-RESEARCH.md (Pattern 2 reinstall; Pitfall 6 skip-backup)
    - .planning/phases/08-retire-local-quickshell-product/08-PATTERNS.md (reinstall sequence; preflight)
    - .planning/phases/07-install-session-hooks-dual-run-verify/07-CONTEXT.md (dry-run first habit; backup gate)
  </read_first>
  <action>
    Branch on Task 1 result (per D-02, D-13; mitigate T-8-01/T-8-03/T-8-05):

    **If Task 1 hard asserts all passed:** echo `[SKIP] live health green — no reinstall`; do not invoke install. Proceed to Task 3.

    **If any Task 1 hard assert failed:** repair live install via wrapper only:

    1. From REPO_ROOT: optional `pkill -x qs 2>/dev/null || true` so files reinstall is not fighting a live reader.
    2. Pre-reinstall symlink guard: `test ! -L "${HOME}/.config/quickshell"` — if it is a symlink, stop; do not rsync through a link. Operator/executor must break the link first (plain unlink of the link only — never recursive remove of home tree as "retirement").
    3. Dry-run first: `printf 'yes\n' | ./arch/dots-hyprland.sh install --dry-run` (or `install-files --dry-run` if only files/tree missing and deps already known good). Tee output to `/tmp/p8-reinstall-dry.txt`. Assert output shows `--core`, `--skip-hyprland`, `--skip-sysupdate`, and dry-run would-exec wording.
    4. Live reinstall **interactive** (do not pipe yes so operator sees the gate): `./arch/dots-hyprland.sh install` (or `install-files` when deps/setups already healthy). Operator types exact `yes` at wrapper backup gate. Prefer upstream backup `y` if prompted.
    5. Never pass bare `--skip-backup` or `--allow-skip-backup` for routine repair (T-8-05). Never call raw vendor `./setup` for this repair. Never run the retired local-product installer under arch/. Never re-symlink live config into the repo. Never uninstall packages (D-05).
    6. On mid-install failure: fix cause and re-run wrapper only (same as Phase 7 D-08 spirit). No automated rollback script.
    7. After success, re-run the Task 1 hard asserts before Task 3 closes the plan.
  </action>
  <verify>
    <automated>set -euo pipefail; test ! -L "${HOME}/.config/quickshell"; test -d "${HOME}/.config/quickshell"; test -f "${HOME}/.config/quickshell/ii/shell.qml"; test -d "${HOME}/.local/state/quickshell/.venv"; test -x "$(git rev-parse --show-toplevel)/arch/dots-hyprland.sh"; test ! -e "$(git rev-parse --show-toplevel)/scripts/phase08-live-smoke.sh"; echo "[VERIFY] OK health-or-reinstall end state (live real; no phase08 smoke file)"</automated>
  </verify>
  <acceptance_criteria>
    - Either SUMMARY records `[SKIP] live health green — no reinstall`, or reinstall completed via `./arch/dots-hyprland.sh` only with dry-run evidence in `/tmp/p8-reinstall-dry.txt` containing the three SAFE_DEFAULTS flags.
    - Live QS path is still not a symlink and has `ii/shell.qml`.
    - No bare skip-backup flags were used on the reinstall command line.
    - No `scripts/phase08*` smoke harness file exists.
    - The retired local-product installer under arch/ was not executed.
  </acceptance_criteria>
  <done>Live install healthy without reinstall, or restored via wrapper only.</done>
</task>

<task type="auto">
  <name>Task 3: Final health green + document non-gates before tree delete</name>
  <files></files>
  <read_first>
    - .planning/phases/08-retire-local-quickshell-product/08-VALIDATION.md (Wave 0; non-gates; 08-01 map)
    - .planning/phases/08-retire-local-quickshell-product/08-RESEARCH.md (Pitfall 4 phase07 smoke; Validation Architecture)
    - .planning/REQUIREMENTS.md (RET-01 text — deletion still pending in 08-02)
  </read_first>
  <action>
    Close the health wave so 08-02 may delete the in-repo product only (D-14 step 1 complete):

    1. Re-run the full Task 1 hard assert set; must all pass (exit non-zero if not).
    2. Confirm in-repo product still exists for now (expected pre-RET-01): `test -d "$(git rev-parse --show-toplevel)/.config/quickshell"` — retirement delete is **08-02**, not this plan.
    3. Confirm wrapper remains executable: `test -x arch/dots-hyprland.sh`.
    4. Document explicitly in 08-01-SUMMARY:
       - Live health green (or reinstall performed + green).
       - `./scripts/phase07-live-smoke.sh` is **not** the Phase 8 gate; after 08-02 its D-04 line is expected red — do not thrash or "fix" it (D-03/D-11/D-12).
       - No phase08 smoke script created (Wave 0: none).
       - 08-02 may proceed with REPO-scoped `git rm -r .config/quickshell` only.
    5. Do not start tree delete, installer delete, or ref cleanup here.
  </action>
  <verify>
    <automated>set -euo pipefail; REPO_ROOT="$(git rev-parse --show-toplevel)"; test ! -L "${HOME}/.config/quickshell"; test -f "${HOME}/.config/quickshell/ii/shell.qml"; case "$(readlink -f "${HOME}/.config/quickshell")" in */.dotfiles/.config/quickshell*) exit 1 ;; esac; test -d "${HOME}/.local/state/quickshell/.venv"; test -d "${REPO_ROOT}/.config/quickshell"; test -x "${REPO_ROOT}/arch/dots-hyprland.sh"; test ! -e "${REPO_ROOT}/scripts/phase08-live-smoke.sh"; grep -E 'exec-once = qs -c ii' "${REPO_ROOT}/.config/hypr/hyprland.conf" >/dev/null; echo "[VERIFY] OK 08-01 ready for tree delete"</automated>
  </verify>
  <acceptance_criteria>
    - All Task 1 hard live-health asserts pass.
    - In-repo `.config/quickshell` still present (delete deferred to 08-02).
    - `arch/dots-hyprland.sh` is executable.
    - SUMMARY states phase07-live-smoke D-04 will be expected red after RET-01 and must not be "fixed".
    - No `scripts/phase08*` file exists.
  </acceptance_criteria>
  <done>Live install health green; 08-02 authorized for in-repo tree delete only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator/executor shell → live XDG QS tree | Health checks read host state; reinstall may overwrite live QS files |
| Wrapper yes-token → vendor `./setup` files step | Human must acknowledge backup before rsync |
| Live home path vs REPO_ROOT product path | Name collision — health must prove independence before delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-01 | Tampering / DoS | Live `~/.config/quickshell` deleted as "retirement" | high | mitigate | This plan only asserts/repairs live path; forbids home delete; 08-02 scoped to REPO `git rm` only |
| T-8-02 | Tampering | Retired installer re-run while still present | high | mitigate | Explicit forbid execution; reinstall only via `arch/dots-hyprland.sh` |
| T-8-03 | Tampering | Symlink regression (live → repo) | high | mitigate | Hard `test ! -L` + readlink-not-under-repo before and after reinstall |
| T-8-04 | Tampering | Rewriting historical tests hides RET failures | medium | accept | D-03/D-11: leave phase07 smoke frozen; document expected D-04 red post-RET-01 |
| T-8-05 | Tampering / info loss | Reinstall without backup / bare skip-backup | high | mitigate | Dry-run first; interactive wrapper gate; never pass bare skip-backup on routine repair |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | No registry package installs this phase (D-05) |
</threat_model>

<verification>
Inline LIVE-01/02 hard asserts green; optional reinstall only via wrapper with dry-run + backup gate; no phase08 smoke file; phase07 full smoke not used as gate; in-repo tree still present pending 08-02.
</verification>

<success_criteria>
Live ii install is healthy and independent of the repo product path (or was restored via `arch/dots-hyprland.sh` only). RET-01 deletion is safe to start in wave 2. No home-tree retirement, no re-symlink, no new smoke suite.
</success_criteria>

<output>
Create `.planning/phases/08-retire-local-quickshell-product/08-01-SUMMARY.md` when done
</output>
