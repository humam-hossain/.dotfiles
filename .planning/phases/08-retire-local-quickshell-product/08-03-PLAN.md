---
phase: 08-retire-local-quickshell-product
plan: 03
type: execute
wave: 3
depends_on:
  - "08-02"
files_modified:
  - arch/quickshell.sh
  - arch/dots-hyprland.sh
autonomous: true
requirements:
  - RET-02
  - RET-01
user_setup: []

must_haves:
  truths:
    - "arch/quickshell.sh is hard-deleted via git rm — no deprecation stub file remains (D-04 / RET-02)"
    - "Installer deletion is its own atomic commit separate from the tree-delete commit (D-09)"
    - "arch/dots-hyprland.sh remains the only Arch install entry for the shell product (D-13) and is executable"
    - "Minimal ref cleanup only: no non-planning code path still calls or re-teaches the deleted installer as the current step (D-11); historical phase0x scripts left alone (D-12)"
    - "Optional header Pattern reword on arch/dots-hyprland.sh is a separate small cleanup commit if done (D-09)"
    - "After RET-02: live ~/.config/quickshell still real with ii/shell.qml; in-repo product still absent (RET-01 hold)"
    - "No new smoke suite; phase07-live-smoke left frozen expected red on D-04 (D-03)"
  artifacts:
    - path: "arch/quickshell.sh"
      provides: "ABSENT — hard deleted installer (RET-02)"
      contains: "Path must not exist; no stub replacement"
    - path: "arch/dots-hyprland.sh"
      provides: "Sole remaining Arch shell-product install entry"
      contains: "SAFE_DEFAULTS + backup_gate + allowlist; optional Pattern comment reword"
    - path: ".config/quickshell"
      provides: "Still ABSENT under REPO (RET-01 hold from 08-02)"
      contains: "Must remain missing"
  key_links:
    - from: "git rm arch/quickshell.sh"
      to: "no second installer surface"
      via: "D-04 hard delete (Pitfall 10 zombie installer)"
    - from: "post-RET grep on arch/ scripts/ .config/"
      to: "no active caller of deleted installer"
      via: "D-11 classification; historical scripts excluded from rewrite"
    - from: "live ii path"
      to: "session continuity"
      via: "final LIVE-01 hold asserts after installer gone"
  prohibitions:
    - "Must NOT leave a deprecation stub at the old installer path (D-04)"
    - "Must NOT delete live $HOME/.config/quickshell (D-14)"
    - "Must NOT re-symlink live config into the repo (D-15)"
    - "Must NOT rewrite scripts/phase07-live-smoke.sh, scripts/phase04-*.py, or other historical asserts (D-11/D-12)"
    - "Must NOT create scripts/phase08-*-smoke.sh (D-03)"
    - "Must NOT uninstall packages (D-05)"
    - "Must NOT mount a docs/playbook campaign (D-06 / Phase 9)"
    - "Must NOT combine tree delete + installer delete into one commit (D-09 — tree already committed in 08-02)"
---

<objective>
Hard-delete the old local-product installer `arch/quickshell.sh` (no stub), optionally reword the sole style Pattern comment in `arch/dots-hyprland.sh`, run minimal stale-ref grep cleanup classification, and re-assert full post-retirement state — per RET-02 and D-04, D-09, D-11, D-12, D-13, D-14.

Purpose: Eliminate the second installer surface (zombie installer / Pitfall 10) so only the dots-hyprland wrapper remains.

Output: Installer path absent; wrapper sole entry; optional tiny cleanup commit; live hold + RET-01 hold green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-retire-local-quickshell-product/08-CONTEXT.md
@.planning/phases/08-retire-local-quickshell-product/08-RESEARCH.md
@.planning/phases/08-retire-local-quickshell-product/08-PATTERNS.md
@.planning/phases/08-retire-local-quickshell-product/08-VALIDATION.md
@.planning/phases/08-retire-local-quickshell-product/08-02-SUMMARY.md
@arch/dots-hyprland.sh
@arch/quickshell.sh
</context>

## Phase Goal

**Single product path — remove the v0.1 in-repo Quickshell tree and old installer.** This plan finishes RET-02 and minimal ref cleanup after 08-02 RET-01.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| In-repo product tree deleted | `REPO/.config/quickshell` ABSENT | 08-02 |
| **Old installer hard-deleted (RET-02)** | `arch/quickshell.sh` **ABSENT** (no stub) | **08-03** |
| Optional Pattern comment reword | `arch/dots-hyprland.sh` L5 | **08-03** (discretion) |
| Sole install entry preserved | `arch/dots-hyprland.sh` executable | kept |

## Non-gate notice (D-03 / D-11)

`./scripts/phase07-live-smoke.sh` remains historical: D-04 red after RET-01 is expected. Do not rewrite it to pass. Do not add phase08 smoke scripts. Final verifies are inline only.

## Tasks

<task type="auto">
  <name>Task 1: Hard-delete arch/quickshell.sh — own commit (D-04, D-09, RET-02)</name>
  <files>arch/quickshell.sh</files>
  <read_first>
    - arch/quickshell.sh (anti-pattern reference only — symlink_config destroys live real dir; do not execute)
    - .planning/phases/08-retire-local-quickshell-product/08-CONTEXT.md (D-04, D-05, D-06, D-09)
    - .planning/phases/08-retire-local-quickshell-product/08-RESEARCH.md (RET-02; Pitfall 3 re-run; Pitfall 10 zombie)
    - .planning/phases/08-retire-local-quickshell-product/08-02-SUMMARY.md (RET-01 done; tree commit separate)
    - .planning/REQUIREMENTS.md (RET-02)
  </read_first>
  <action>
    Retire the second installer by hard delete only (D-04 / RET-02 / D-09):

    1. Confirm 08-02 completed: `test ! -e "$(git rev-parse --show-toplevel)/.config/quickshell"`. If tree still present, stop — finish 08-02 first.
    2. Confirm target still exists before delete: `test -f arch/quickshell.sh`.
    3. **Never execute** that script (T-8-02 / T-8-03). Read only if needed for SUMMARY anti-pattern note.
    4. `git rm arch/quickshell.sh` from REPO_ROOT.
    5. Do **not** create any replacement file at that path (no deprecation stub, no README pointer file).
    6. Own atomic commit (D-09), separate from tree delete and from optional comment cleanup. Suggested message: `chore(08): remove arch/quickshell.sh old product installer (RET-02)`.
    7. Assert: `test ! -e arch/quickshell.sh`.
    8. Do not uninstall packages (D-05). Do not rewrite historical scripts. Do not touch live home QS tree.
  </action>
  <verify>
    <automated>set -euo pipefail; REPO_ROOT="$(git rev-parse --show-toplevel)"; test ! -e "${REPO_ROOT}/arch/quickshell.sh"; test ! -e "${REPO_ROOT}/.config/quickshell"; test -x "${REPO_ROOT}/arch/dots-hyprland.sh"; git -C "$REPO_ROOT" log -1 --oneline | grep -qiE 'quickshell\.sh|RET-02|installer|08'; echo "[VERIFY] OK RET-02 installer absent; wrapper remains"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e "$(git rev-parse --show-toplevel)/arch/quickshell.sh"` succeeds.
    - No new file was created at that path as a stub or redirect.
    - Latest commit documents installer removal / RET-02 and is not the same commit as the tree delete (tree delete already in history from 08-02).
    - `arch/dots-hyprland.sh` still exists and is executable.
    - In-repo `.config/quickshell` remains absent (RET-01 hold).
  </acceptance_criteria>
  <done>RET-02 hard delete committed; no stub; wrapper sole installer entry.</done>
</task>

<task type="auto">
  <name>Task 2: Minimal stale-ref cleanup + optional Pattern reword (D-11, D-12, D-13)</name>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (header lines 1–12; L5 Pattern comment only discretionary)
    - .planning/phases/08-retire-local-quickshell-product/08-CONTEXT.md (D-11, D-12, D-13; Agent's Discretion)
    - .planning/phases/08-retire-local-quickshell-product/08-RESEARCH.md (Pattern 3 classification table; post-RET grep)
    - .planning/phases/08-retire-local-quickshell-product/08-PATTERNS.md (stale ref cleanup; discretionary reword)
  </read_first>
  <action>
    Clean only refs that would re-teach the deleted installer as the **current** install step (D-11). Leave historical scripts broken if re-run (D-12).

    1. From REPO_ROOT, inventory non-planning hits:
       `git grep -n 'arch/quickshell\.sh' -- arch/ scripts/ .config/ 2>/dev/null || true`
       Expected pre-reword: at most the Pattern style comment on `arch/dots-hyprland.sh` line 5 (and no callers under scripts/ or .config/).
    2. **Discretionary reword (preferred):** Edit only the Pattern comment line in `arch/dots-hyprland.sh` so it cites `arch/waybar.sh` (or generic `arch/*.sh` REPO_ROOT + main dispatcher + labeled echos) as style ancestry — **not** the deleted installer filename. Do not change SAFE_DEFAULTS, backup_gate, allowlist, or exec logic.
    3. If reword applied: own tiny cleanup commit (D-09), e.g. `chore(08): reword dots-hyprland wrapper pattern comment after installer retirement`. If left unchanged, document why in SUMMARY (harmless ancestry comment only).
    4. **Must leave alone:** `scripts/phase07-live-smoke.sh`, `scripts/phase04-*.py`, other phase0x asserts, `.planning/**` historical text, `vendor/dots-hyprland/**`, wrapper backup-gate text about live `~/.config/quickshell` (that is the correct live target).
    5. Post-cleanup gate: under `arch/`, `scripts/`, `.config/`, there must be **no** line that instructs running the deleted installer as a required current step. After preferred reword, `git grep 'arch/quickshell\.sh' -- arch/ scripts/ .config/` should print nothing.
    6. Do not create phase08 smoke files. Do not package uninstall. Do not docs playbook (Phase 9).
  </action>
  <verify>
    <automated>set -euo pipefail; REPO_ROOT="$(git rev-parse --show-toplevel)"; test ! -e "${REPO_ROOT}/arch/quickshell.sh"; test -x "${REPO_ROOT}/arch/dots-hyprland.sh"; bash -n "${REPO_ROOT}/arch/dots-hyprland.sh"; if git -C "$REPO_ROOT" grep -n 'arch/quickshell\.sh' -- arch/ scripts/ .config/ 2>/dev/null | grep -v 'Pattern:' ; then echo "[FAIL] active non-Pattern ref remains" >&2; exit 1; fi; echo "[VERIFY] OK no active installer caller refs"</automated>
  </verify>
  <acceptance_criteria>
    - `arch/dots-hyprland.sh` still passes `bash -n` and remains executable.
    - `git grep 'arch/quickshell\.sh' -- arch/ scripts/ .config/` either has zero hits, or only a residual style Pattern comment that does not instruct running the file (preferred: zero hits after reword).
    - `scripts/phase07-live-smoke.sh` and `scripts/phase04-ipc-reload-assert.py` were not modified.
    - No `scripts/phase08*` file exists.
    - If comment reword committed, it is a separate commit from the installer `git rm` commit.
  </acceptance_criteria>
  <done>Minimal refs clean; wrapper sole entry; historical scripts untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Final post-retirement asserts (RET-01 + RET-02 + live hold)</name>
  <files>~/.config/quickshell, ~/.local/state/quickshell/.venv, .config/quickshell, arch/quickshell.sh, arch/dots-hyprland.sh, .config/hypr/hyprland.conf</files>
  <read_first>
    - .planning/phases/08-retire-local-quickshell-product/08-VALIDATION.md (full post-RET set; threat refs)
    - .planning/phases/08-retire-local-quickshell-product/08-RESEARCH.md (Post-RET asserts Code Example; success criteria)
    - .planning/ROADMAP.md (Phase 8 success criteria 1–3)
    - .planning/REQUIREMENTS.md (RET-01, RET-02)
  </read_first>
  <action>
    Goal-backward final proof for Phase 8 (ROADMAP success criteria + RET-01/02):

    1. Product no longer shipped: `test ! -e "${REPO_ROOT}/.config/quickshell"`.
    2. Old installer gone (no stub): `test ! -e "${REPO_ROOT}/arch/quickshell.sh"`.
    3. Only install entry: `test -x "${REPO_ROOT}/arch/dots-hyprland.sh"`.
    4. Live still independent:
       - `test ! -L "${HOME}/.config/quickshell"`
       - `test -f "${HOME}/.config/quickshell/ii/shell.qml"`
       - readlink-f case not under repo product path
       - `test -d "${HOME}/.local/state/quickshell/.venv"`
    5. LIVE-02 hooks still in repo hypr SoT (env + `qs -c ii`).
    6. Soft: waybar exec-once still present in repo hypr conf.
    7. Grep gate (same as Task 2): no active non-Pattern caller of the deleted installer under arch/scripts/.config.
    8. Confirm no phase08 smoke harness file.
    9. Record in 08-03-SUMMARY: RET-01, RET-02, live hold, commit SHAs for tree delete / installer delete / optional cleanup; note phase07 D-04 expected red.
  </action>
  <verify>
    <automated>set -euo pipefail; REPO_ROOT="$(git rev-parse --show-toplevel)"; test ! -e "${REPO_ROOT}/.config/quickshell"; test ! -e "${REPO_ROOT}/arch/quickshell.sh"; test -x "${REPO_ROOT}/arch/dots-hyprland.sh"; test ! -L "${HOME}/.config/quickshell"; test -f "${HOME}/.config/quickshell/ii/shell.qml"; case "$(readlink -f "${HOME}/.config/quickshell")" in */.dotfiles/.config/quickshell*) exit 1 ;; esac; test -d "${HOME}/.local/state/quickshell/.venv"; grep -E 'env = ILLOGICAL_IMPULSE_VIRTUAL_ENV,' "${REPO_ROOT}/.config/hypr/hyprland.conf" >/dev/null; grep -E 'exec-once = qs -c ii' "${REPO_ROOT}/.config/hypr/hyprland.conf" >/dev/null; test ! -e "${REPO_ROOT}/scripts/phase08-live-smoke.sh"; if git -C "$REPO_ROOT" grep -n 'arch/quickshell\.sh' -- arch/ scripts/ .config/ 2>/dev/null | grep -v 'Pattern:' ; then exit 1; fi; echo "[VERIFY] OK Phase 8 post-retirement full set"</automated>
  </verify>
  <acceptance_criteria>
    - In-repo `.config/quickshell` absent (RET-01).
    - `arch/quickshell.sh` absent with no stub (RET-02).
    - `arch/dots-hyprland.sh` executable (sole entry).
    - Live QS path real, has `ii/shell.qml`, not symlink, not under repo product.
    - Repo hypr still has ii env + `exec-once = qs -c ii`.
    - No phase08 smoke script; historical phase07 smoke not rewritten.
    - SUMMARY lists separate commits per D-09.
  </acceptance_criteria>
  <done>Phase 8 retirement complete: single product path; live ii intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Installer path presence → operator muscle memory | File must be gone so it cannot re-symlink live into missing repo tree |
| Wrapper comment/docs text → operator procedure | Only clean re-teaching refs; do not break wrapper behavior |
| Final session state | Live XDG tree must remain the product after all git ops |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-01 | Tampering / DoS | Live path deleted during installer cleanup | high | mitigate | No home-path deletes in any task; final assert live `ii/shell.qml` exists |
| T-8-02 | Tampering | Old installer left callable or re-run | high | mitigate | `git rm` hard delete; `test ! -e`; never execute before delete; grep for active callers |
| T-8-03 | Tampering | Symlink regression after installer removal | high | mitigate | Final `test ! -L` + readlink-not-under-repo; forbid re-symlink |
| T-8-04 | Tampering | Historical test rewrite hides failures | medium | accept | D-03/D-11 leave phase07/phase04 scripts; inline final asserts only |
| T-8-05 | Tampering / info loss | Accidental reinstall without gate in this plan | high | mitigate | No reinstall tasks here; wrapper untouched functionally except optional comment |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | No registry packages (D-05) |
</threat_model>

<verification>
`test ! -e arch/quickshell.sh`; `test ! -e .config/quickshell`; `test -x arch/dots-hyprland.sh`; live LIVE-01 hold; hypr hooks present; no active non-Pattern installer refs under arch/scripts/.config; no phase08 smoke.
</verification>

<success_criteria>
RET-02 true: old installer hard-deleted (no stub). RET-01 still true. `arch/dots-hyprland.sh` is the only Arch shell-product install entry. Live session still runs from installed `~/.config/quickshell` (no symlink-at-repo). Phase 8 roadmap success criteria 1–3 satisfied.
</success_criteria>

<output>
Create `.planning/phases/08-retire-local-quickshell-product/08-03-SUMMARY.md` when done
</output>
