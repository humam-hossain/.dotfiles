---
phase: 22-kde-and-gtk-capture
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - stow/gtk/.config/gtk-3.0/settings.ini
  - stow/gtk/.config/gtk-3.0/bookmarks
  - stow/gtk/.config/gtk-4.0/settings.ini
  - .gitignore
  - scripts/phase22-kde-and-gtk-capture-assert.sh
autonomous: true
requirements: [KDE-02]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "stow/gtk/.config/ manages gtk-3.0/settings.ini, gtk-3.0/bookmarks, and gtk-4.0/settings.ini per file with parent directories strictly unfolded"
    - "Live ~/.config/gtk-3.0 and ~/.config/gtk-4.0 are real directories on the host, never symlinks into the repository"
    - "Live ~/.config/gtk-3.0/settings.ini, ~/.config/gtk-3.0/bookmarks, and ~/.config/gtk-4.0/settings.ini are symlinks resolving into stow/gtk/ with identical inodes"
    - "Root .gitignore contains slash-free gtk-dark.css under the generated theme outputs section beside gtk.css"
    - "GTK 2.0 legacy files (~/.gtkrc-2.0, ~/.config/gtkrc) are untracked and excluded from stow/gtk/"
    - "Section 3 of the assert script verifies GTK package layout, unfolded parent directories, gitignore entries, and GLib link severance behavior in a scratch fixture"
  artifacts:
    - stow/gtk/.config/gtk-3.0/settings.ini
    - stow/gtk/.config/gtk-3.0/bookmarks
    - stow/gtk/.config/gtk-4.0/settings.ini
    - .gitignore
    - scripts/phase22-kde-and-gtk-capture-assert.sh
  key_links:
    - "stow/gtk/ uses --no-folding to link individual files inside ~/.config/gtk-3.0/ and ~/.config/gtk-4.0/ without replacing the directories"
    - "arch/dots-hyprland.sh verify folded ancestor check permanently prevents ~/.config/gtk-3.0 or ~/.config/gtk-4.0 from becoming directory symlinks"
---

<objective>
Adopt GTK-3.0 and GTK-4.0 configuration files (`settings.ini`, `bookmarks`) into `stow/gtk/` per file following the SAFE-01 adoption protocol, enforce parent directory unfolding (`--no-folding`) so generated theme siblings (`gtk.css`) are not stowed into the repository, add `gtk-dark.css` to `.gitignore`, and implement Section 3 in `scripts/phase22-kde-and-gtk-capture-assert.sh` including scratch GLib link severance simulation.

Purpose: Capture personal GTK theme, font, and bookmark preferences declaratively while keeping host directories unfolded and theme css files gitignored.
Output: New package `stow/gtk/` deployed live, updated `.gitignore`, and passing Section 3 in `scripts/phase22-kde-and-gtk-capture-assert.sh`.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-kde-and-gtk-capture/22-CONTEXT.md
@.planning/phases/22-kde-and-gtk-capture/22-RESEARCH.md
@.planning/phases/22-kde-and-gtk-capture/22-PATTERNS.md
@.planning/phases/22-kde-and-gtk-capture/22-VALIDATION.md
@scripts/phase22-kde-and-gtk-capture-assert.sh
@stow/qbittorrent/.config/qBittorrent/qBittorrent.conf
@stow/kitty/.config/kitty/kitty.conf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt GTK-3.0 and GTK-4.0 per-file into stow/gtk/ via SAFE-01 protocol and gitignore gtk-dark.css</name>
  <files>stow/gtk/.config/gtk-3.0/settings.ini, stow/gtk/.config/gtk-3.0/bookmarks, stow/gtk/.config/gtk-4.0/settings.ini, .gitignore</files>
  <read_first>
    - stow/qbittorrent/.config/qBittorrent/qBittorrent.conf (INI format reference)
    - stow/kitty/.config/kitty/kitty.conf (plain text bookmarks reference)
    - .planning/phases/22-kde-and-gtk-capture/22-CONTEXT.md (D-08, D-09, D-11, D-12, D-13)
    - .planning/phases/22-kde-and-gtk-capture/22-RESEARCH.md (lines 98-99, 309-322: GTK per-file layout and folding pitfall)
    - .planning/phases/22-kde-and-gtk-capture/22-PATTERNS.md (lines 234-262, 309-322, 395-406: GTK patterns, per-file unfolding, .gitignore)
  </read_first>
  <action>
    Adopt GTK-3.0 and GTK-4.0 configuration files into `stow/gtk/` per file following SAFE-01 (per D-08, D-09, D-11, D-12, D-13):
    1. Preflight parent directory check: ensure `$HOME/.config/gtk-3.0` and `$HOME/.config/gtk-4.0` are regular directories (create if missing; assert not symlinks per D-09).
    2. Take timestamped backup of live files:
       `EPOCH="$(date +%s)"`
       `cp -a "$HOME/.config/gtk-3.0/settings.ini" "$HOME/.config/gtk-3.0/settings.ini.bak.$EPOCH"`
       `cp -a "$HOME/.config/gtk-3.0/bookmarks" "$HOME/.config/gtk-3.0/bookmarks.bak.$EPOCH"`
       `cp -a "$HOME/.config/gtk-4.0/settings.ini" "$HOME/.config/gtk-4.0/settings.ini.bak.$EPOCH"`
    3. Create repo package directories:
       `mkdir -p "$REPO_ROOT/stow/gtk/.config/gtk-3.0" "$REPO_ROOT/stow/gtk/.config/gtk-4.0"`
    4. Copy live content into repo:
       `cp -a "$HOME/.config/gtk-3.0/settings.ini" "$REPO_ROOT/stow/gtk/.config/gtk-3.0/settings.ini"`
       `cp -a "$HOME/.config/gtk-3.0/bookmarks" "$REPO_ROOT/stow/gtk/.config/gtk-3.0/bookmarks"`
       `cp -a "$HOME/.config/gtk-4.0/settings.ini" "$REPO_ROOT/stow/gtk/.config/gtk-4.0/settings.ini"`
    5. Perform dry-run stow to verify no directory folding occurs:
       `stow -n --no-folding -d "$REPO_ROOT/stow" -t "$HOME" gtk`
    6. Remove live regular files and link with Stow:
       `rm -f "$HOME/.config/gtk-3.0/settings.ini" "$HOME/.config/gtk-3.0/bookmarks" "$HOME/.config/gtk-4.0/settings.ini"`
       `stow --verbose=5 --no-folding -d "$REPO_ROOT/stow" -t "$HOME" gtk`
    7. Assert live symlinks and inode identity:
       `test -L "$HOME/.config/gtk-3.0/settings.ini"`
       `test -L "$HOME/.config/gtk-3.0/bookmarks"`
       `test -L "$HOME/.config/gtk-4.0/settings.ini"`
       `test "$(stat -c %i "$HOME/.config/gtk-3.0/settings.ini")" -eq "$(stat -c %i "$REPO_ROOT/stow/gtk/.config/gtk-3.0/settings.ini")"`
       `test "$(stat -c %i "$HOME/.config/gtk-3.0/bookmarks")" -eq "$(stat -c %i "$REPO_ROOT/stow/gtk/.config/gtk-3.0/bookmarks")"`
       `test "$(stat -c %i "$HOME/.config/gtk-4.0/settings.ini")" -eq "$(stat -c %i "$REPO_ROOT/stow/gtk/.config/gtk-4.0/settings.ini")"`
    8. Assert host parent directories remained unfolded regular directories:
       `test -d "$HOME/.config/gtk-3.0" && test ! -L "$HOME/.config/gtk-3.0"`
       `test -d "$HOME/.config/gtk-4.0" && test ! -L "$HOME/.config/gtk-4.0"`
    9. Assert bookmarks retain literal path portability (`file:///home/pera/...` paths preserved per D-11).
    10. Assert legacy GTK 2.0 files (`~/.gtkrc-2.0`, `~/.config/gtkrc`) remain unmanaged (per D-12).
    11. Update root `.gitignore`: add `gtk-dark.css` directly below `gtk.css` under the generated theme outputs block (per D-13).
  </action>
  <acceptance_criteria>
    - `stow/gtk/.config/gtk-3.0/settings.ini`, `stow/gtk/.config/gtk-3.0/bookmarks`, and `stow/gtk/.config/gtk-4.0/settings.ini` exist in repository.
    - Live `~/.config/gtk-3.0/settings.ini`, `~/.config/gtk-3.0/bookmarks`, and `~/.config/gtk-4.0/settings.ini` are symlinks resolving into `stow/gtk/`.
    - Host directories `~/.config/gtk-3.0` and `~/.config/gtk-4.0` are directories, NOT symlinks.
    - `.gitignore` contains `gtk-dark.css` (`grep -q '^gtk-dark\.css$' .gitignore`).
    - `git check-ignore ~/.config/gtk-3.0/gtk.css` and `git check-ignore ~/.config/gtk-4.0/gtk-dark.css` both return exit code 0.
  </acceptance_criteria>
  <verify>
    <automated>test -L "$HOME/.config/gtk-3.0/settings.ini" && test -L "$HOME/.config/gtk-3.0/bookmarks" && test -L "$HOME/.config/gtk-4.0/settings.ini" && test -d "$HOME/.config/gtk-3.0" && test ! -L "$HOME/.config/gtk-3.0" && test -d "$HOME/.config/gtk-4.0" && test ! -L "$HOME/.config/gtk-4.0" && grep -q '^gtk-dark\.css$' .gitignore</automated>
    <fails_when>non-zero exit code or missing symlinks or folded parent directories or missing gitignore pattern</fails_when>
  </verify>
  <done>GTK-3.0 and GTK-4.0 configurations stowed per-file with parents unfolded, bookmarks preserved, and gtk-dark.css gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Section 3 in scripts/phase22-kde-and-gtk-capture-assert.sh (unfolded parent assertion and scratch link severance test)</name>
  <files>scripts/phase22-kde-and-gtk-capture-assert.sh</files>
  <read_first>
    - scripts/phase22-kde-and-gtk-capture-assert.sh (from Plan 22-01)
    - .planning/phases/22-kde-and-gtk-capture/22-CONTEXT.md (D-08, D-09, D-10, D-11, D-12, D-13)
    - .planning/phases/22-kde-and-gtk-capture/22-RESEARCH.md (lines 98-99, 593-594: python GLib file_set_contents)
    - .planning/phases/22-kde-and-gtk-capture/22-PATTERNS.md (lines 106-116: scratch fixture isolation)
  </read_first>
  <action>
    Add Section 3 to `scripts/phase22-kde-and-gtk-capture-assert.sh`: "KDE-02: GTK package layout, unfolded parent directory assertion, and scratch link severance test" (per D-08, D-09, D-10, D-11, D-12, D-13):
    1. Verify repository files exist:
       - `stow/gtk/.config/gtk-3.0/settings.ini`
       - `stow/gtk/.config/gtk-3.0/bookmarks`
       - `stow/gtk/.config/gtk-4.0/settings.ini`
    2. Verify live counterparts are symlinks with identical inodes resolving into `stow/gtk/`.
    3. Assert parent directory unfolding invariant (D-09):
       - `test -d "$HOME/.config/gtk-3.0" && test ! -L "$HOME/.config/gtk-3.0"`
       - `test -d "$HOME/.config/gtk-4.0" && test ! -L "$HOME/.config/gtk-4.0"`
    4. Assert `.gitignore` contains `gtk.css` and `gtk-dark.css` (D-13).
    5. Assert bookmarks path portability (D-11): verify `file:///home/pera/` URI format in `bookmarks`.
    6. Assert legacy GTK 2.0 exclusion (D-12): verify no `gtkrc` or `.gtkrc-2.0` in `stow/gtk/`.
    7. Implement scratch GLib link severance simulation drill (Q6, D-10):
       - Create scratch fixture root `S3_ROOT="$(mktemp -d /tmp/p22-assert-s3-XXXXXX)"` and append to `SCRATCH_ROOTS`.
       - Create mock repo file `$S3_ROOT/repo/bookmarks` with test bookmark data.
       - Create mock live target `$S3_ROOT/home/.config/gtk-3.0/bookmarks` as a symlink pointing to `$S3_ROOT/repo/bookmarks`.
       - Assert initial state is a symlink: `test -L "$S3_ROOT/home/.config/gtk-3.0/bookmarks"`.
       - Execute python3 using `gi.repository.GLib.file_set_contents` to simulate atomic overwrite:
         `python3 -c "import gi; gi.require_version('GLib', '2.0'); from gi.repository import GLib; GLib.file_set_contents('$S3_ROOT/home/.config/gtk-3.0/bookmarks', b'file:///test Test\n')"`
       - Assert that `test -L "$S3_ROOT/home/.config/gtk-3.0/bookmarks"` is now FALSE and `test -f "$S3_ROOT/home/.config/gtk-3.0/bookmarks"` is TRUE, empirically demonstrating that `g_file_set_contents` severs symlinks by renaming over them.
       - Document that `arch/dots-hyprland.sh verify` detects this condition as `[FAIL] not a symlink`, recoverable via `stow --verbose=5 --no-folding -d stow -t ~ gtk`.
    8. Emit `[PASS]` for repo layout, `[PASS]` for live symlinks and inodes, `[PASS]` for unfolded parent directories, and `[PASS]` for scratch GLib link severance demonstration.
  </action>
  <acceptance_criteria>
    - Executing `./scripts/phase22-kde-and-gtk-capture-assert.sh --section 3` exits 0 with `FAIL=0 FINDINGS=0`.
    - Section 3 output prints `[PASS]` for GTK package files, live symlinks, unfolded parent directories, `.gitignore` entries, and scratch link severance test.
    - Scratch directories are wiped on exit.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase22-kde-and-gtk-capture-assert.sh --section 3</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0</fails_when>
  </verify>
  <done>Section 3 of assert harness implemented and passing, verifying GTK per-file layout, host parent unfolding, and GLib link severance behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live GTK configs → Git working tree (`stow/gtk/`) | User settings read from `$HOME/.config/gtk-{3,4}.0/` are committed into the repository |
| Host directories → Stow package | Host directories `~/.config/gtk-3.0/` and `~/.config/gtk-4.0/` must remain real directories, never folded into repo symlinks |
| Test fixture → Host filesystem | Scratch GLib link severance drill must remain quarantined within `/tmp/` and never alter real bookmarks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-05 | Tampering | `stow/gtk/` | high | mitigate | Enforce `--no-folding` on GNU Stow calls and verify parent directories `~/.config/gtk-3.0` and `~/.config/gtk-4.0` are real folders to prevent matugen's `gtk.css` from writing into repository (per D-08, D-09) |
| T-22-06 | Denial of Service | Live symlink integrity | medium | mitigate | Manage GTK files as declarative dotfiles and rely on `arch/dots-hyprland.sh verify` link checks to detect any link severance by `g_file_set_contents`, recoverable via re-stowing (per D-10) |
| T-22-07 | Information Disclosure | `stow/gtk/.config/` | low | mitigate | Audit `settings.ini` and `bookmarks` ensuring only desktop themes, fonts, cursor sizes, and directory bookmarks are tracked (no sensitive tokens) |
| T-22-08 | Tampering | Assert harness (`phase22-kde-and-gtk-capture-assert.sh`) | high | mitigate | Confine Python GLib link severance drill to `mktemp -d /tmp/p22-assert-s3-XXXXXX` registered with EXIT trap |
| T-22-SC | Tampering | Script dependencies | low | accept | Relies on system `stow`, `python3-gobject`, `bash`, `git`, and coreutils |
</threat_model>

<verification>
Automated test harness verification for Section 3:
`./scripts/phase22-kde-and-gtk-capture-assert.sh --section 3`
</verification>

<success_criteria>
1. `stow/gtk/.config/` manages `gtk-3.0/settings.ini`, `gtk-3.0/bookmarks`, and `gtk-4.0/settings.ini` per file.
2. Live parent directories `~/.config/gtk-3.0` and `~/.config/gtk-4.0` are real folders, not symlinks.
3. Root `.gitignore` contains `gtk-dark.css`.
4. Section 3 of `scripts/phase22-kde-and-gtk-capture-assert.sh` passes with 0 failures and 0 findings.
</success_criteria>

<output>
Create `.planning/phases/22-kde-and-gtk-capture/22-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces
- Stow package: `stow/gtk/.config/gtk-3.0/settings.ini`
- Stow package: `stow/gtk/.config/gtk-3.0/bookmarks`
- Stow package: `stow/gtk/.config/gtk-4.0/settings.ini`
- Git configuration: Updated `.gitignore` with `gtk-dark.css`
- Assert harness: Section 3 added to `scripts/phase22-kde-and-gtk-capture-assert.sh`
