---
phase: 12-wrapper-full-profile
plan: 04
type: execute
wave: 4
depends_on:
  - 12-03
files_modified:
  - arch/dots-hyprland.sh
  - scripts/phase12-full-smoke.sh
  - .planning/phases/12-wrapper-full-profile/12-VALIDATION.md
autonomous: true
requirements:
  - FULL-05
  - FULL-01
  - FULL-02
  - FULL-03
  - FULL-04
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - "FULL-05: printf 'yes\\n' | ./arch/dots-hyprland.sh install --full --dry-run plans protect re-mark (protect-list / re-mark explicit language) after would-exec"
    - "D-16: same full dry-run plans enable ii hooks (ii hooks / enable ii hooks language) after would-exec"
    - "D-14: post-setup protect path is not branched off when full==1 — same PROTECT_EXPLICIT list and call sites as safe path for install|install-files (and install deps portion of unified install)"
    - "D-15: enable_hypr_ii_hooks still invoked under full (dry-run plan and real-path case arm)"
    - "PROTECT_EXPLICIT array contents unchanged this phase (no expand)"
    - "scripts/phase12-full-smoke.sh exists, is executable, passes bash -n, and exits 0 only when FULL-01..05 dry-run/refuse/help matrix is green (all non-mutating)"
    - "bash -n arch/dots-hyprland.sh exits 0"
  artifacts:
    - path: "arch/dots-hyprland.sh"
      provides: "Confirm protect + ii hooks under --full (no skip branch)"
      contains: "protect_explicit_packages + enable_hypr_ii_hooks on full dry-run and real path"
    - path: "scripts/phase12-full-smoke.sh"
      provides: "Nyquist FULL-01..05 non-mutating smoke harness"
      contains: "help --full; safe residual; full residual absence; skip-backup refuse; protect/hooks plan greps"
  key_links:
    - from: "run_install_family dry-run case install|install-deps|install-files"
      to: "protect_explicit_packages 1 + enable_hypr_ii_hooks 1"
      via: "unchanged under full==1 (D-14/D-16)"
      pattern: "protect-list"
    - from: "scripts/phase12-full-smoke.sh"
      to: "12-VALIDATION.md matrix"
      via: "automated FULL-01..05 commands"
      pattern: "FULL-"
  prohibitions:
    - statement: "no live full install"
      status: active
      verification: "Smoke and tasks use --dry-run / help / refuse only; harness must not invoke non-dry-run install --full"
    - statement: "no PROTECT expand"
      status: active
      verification: "PROTECT_EXPLICIT members unchanged vs pre-phase list"
    - statement: "no chrome teardown / pre-flight sync / overlay writes"
      status: active
      verification: "No dual-run chrome stop, no live→repo sync, no hypr/custom writes (D-17)"
    - statement: "no INV/DISP wrapper gate"
      status: active
      verification: "Still no runtime INV/DISP presence checks"
---

<objective>
Wave 4 closer for Phase 12: lock **FULL-05** post-setup behavior under `--full` (PROTECT re-mark + ii hooks, including dry-run mirror per D-14..D-16) and land a single non-mutating smoke harness that proves FULL-01..05 end-to-end for Nyquist / phase gate.

Purpose: Ensure full profile never skips personal-stack protect or ii hooks; give execute/verify a one-command suite.

Output: Confirmed post-setup path in `arch/dots-hyprland.sh` + `scripts/phase12-full-smoke.sh` + VALIDATION status notes.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-wrapper-full-profile/12-CONTEXT.md
@.planning/phases/12-wrapper-full-profile/12-RESEARCH.md
@.planning/phases/12-wrapper-full-profile/12-PATTERNS.md
@.planning/phases/12-wrapper-full-profile/12-VALIDATION.md
@.planning/phases/12-wrapper-full-profile/12-01-PLAN.md
@.planning/phases/12-wrapper-full-profile/12-02-PLAN.md
@.planning/phases/12-wrapper-full-profile/12-03-PLAN.md
@arch/dots-hyprland.sh
@scripts/phase07-live-smoke.sh
</context>

## Phase Goal

**Operator can dry-run and invoke an explicit full-install path without making full the accidental default.**

This plan delivers **FULL-05 post-setup under full** + consolidated smoke covering all phase requirements.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Wrapper full-profile CLI | `arch/dots-hyprland.sh` | 12-01..03 |
| Full gate messaging | backup_gate / full branch | 12-02 |
| Help / usage primary full path | `usage()` | 12-03 |
| FULL-01..05 smoke harness | `scripts/phase12-full-smoke.sh` | **12-04** |
| Protect + hooks under full | same wrapper post-setup case | **12-04** (confirm/lock) |

## Decisions locked in this plan

- D-14: PROTECT_EXPLICIT re-mark always runs after `--full` on install/install-files (incl. deps portion of unified install)
- D-15: Keep `enable_hypr_ii_hooks` on `--full` (same targets as safe path)
- D-16: `--full --dry-run` post-setup plan mirrors real path (protect plan + would enable ii hooks)
- D-17: No additional post-full side effects (chrome stop, pre-flight sync, overlays, session verify, live adopt)

## Assumptions (specless probe)

- Existing dry-run case already calls `protect_explicit_packages 1` + `enable_hypr_ii_hooks 1` for install|install-deps|install-files — Task 1 confirms this is **not** gated on `full==0` and fixes if 12-01 accidentally skipped it
- Smoke harness style follows `scripts/phase07-live-smoke.sh` (pass/fail counters, exit non-zero on hard fail)
- Exact protect/hooks log strings may vary slightly; greps use stable tokens (`protect-list`, `ii hooks`)

<tasks>

<task type="auto" id="12-04-01">
  <name>Task 1: FULL-05 protect re-mark + ii hooks under --full (incl. dry-run mirror)</name>
  <reversibility rating="one-way">D-14: if protect is skipped on full, personal stack can remain asdeps after full deps — hard to recover after operators run full once.</reversibility>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (run_install_family dry-run block ~L1411-1423; real post-setup ~L1433-1442; protect_explicit_packages ~L318; enable_hypr_ii_hooks ~L758; PROTECT_EXPLICIT array ~L197)
    - .planning/phases/12-wrapper-full-profile/12-CONTEXT.md (D-14, D-15, D-16, D-17)
    - .planning/phases/12-wrapper-full-profile/12-RESEARCH.md (Pattern 4 post-setup; Pitfall 6 protect skipped on full; Validation FULL-05)
    - .planning/phases/12-wrapper-full-profile/12-VALIDATION.md (12-FULL-05 row)
    - .planning/phases/12-wrapper-full-profile/12-PATTERNS.md (post-setup protect + ii hooks)
  </read_first>
  <action>
    Lock FULL-05 / D-14..D-16 on the full path. Prefer **confirm existing call sites still run when full==1**; only edit if a prior plan introduced a full skip.

    1. **Dry-run mirror (D-16):** In `run_install_family`, when `dry_run==1` and `subcmd` is `install|install-deps|install-files`, keep (or restore) calls to `protect_explicit_packages 1 …` and `enable_hypr_ii_hooks 1` **regardless of `full`**. Do not wrap these in `if ((full == 0))`.

    2. **Real path (D-14/D-15):** Same case arm for non-dry-run must still call `protect_explicit_packages 0` and `enable_hypr_ii_hooks 0` after successful setup for install|install-deps|install-files, including when `full==1`.

    3. **PROTECT list (D-14):** Do **not** expand `PROTECT_EXPLICIT` array members this phase.

    4. **No extra side effects (D-17):** Do not add chrome process teardown, pre-flight live→repo sync, hypr/custom overlay writes, session verify, or live adopt.

    5. **Messaging:** Dry-run may keep existing `[CONFIG] dry-run: after setup, would re-mark protect-list…` and `would enable ii hooks…` lines; ensure they still appear on `--full --dry-run`.
  </action>
  <verify>
    <automated>bash -n arch/dots-hyprland.sh &amp;&amp; printf 'yes\n' | ./arch/dots-hyprland.sh install --full --dry-run &gt;/tmp/p12-04-full.txt 2&gt;&amp;1 &amp;&amp; grep -q 'would exec' /tmp/p12-04-full.txt &amp;&amp; grep -q 'protect-list' /tmp/p12-04-full.txt &amp;&amp; grep -qiE 'ii hooks|enable ii hooks' /tmp/p12-04-full.txt &amp;&amp; printf 'yes\n' | ./arch/dots-hyprland.sh install-files --full --dry-run &gt;/tmp/p12-04-files.txt 2&gt;&amp;1 &amp;&amp; grep -q 'protect-list' /tmp/p12-04-files.txt</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n arch/dots-hyprland.sh` exits 0
    - `printf 'yes\n' | ./arch/dots-hyprland.sh install --full --dry-run` exits 0
    - Output contains `would exec`
    - Output contains `protect-list` (or equivalent protect re-mark plan token already used by wrapper)
    - Output matches `ii hooks` or `enable ii hooks` (case-insensitive)
    - `install-files --full --dry-run` also shows protect-list plan
    - No new members added to `PROTECT_EXPLICIT=(` block vs pre-task list
    - No chrome kill / overlay / sync functions added for full-only path
  </acceptance_criteria>
  <done>Full dry-run still plans protect re-mark + ii hooks; real-path arms not skipped when full==1.</done>
</task>

<task type="auto" id="12-04-02">
  <name>Task 2: phase12-full-smoke.sh FULL-01..05 matrix + VALIDATION wave notes</name>
  <reversibility rating="reversible">Harness only; can delete without affecting wrapper.</reversibility>
  <files>scripts/phase12-full-smoke.sh, .planning/phases/12-wrapper-full-profile/12-VALIDATION.md</files>
  <read_first>
    - scripts/phase07-live-smoke.sh (pass/fail counter pattern)
    - .planning/phases/12-wrapper-full-profile/12-VALIDATION.md (Requirement → Automated Gate Map + cheatsheet)
    - .planning/phases/12-wrapper-full-profile/12-RESEARCH.md (Concrete command cheatsheet)
    - arch/dots-hyprland.sh (final behavior after 12-01..04 Task 1)
  </read_first>
  <action>
    Create `scripts/phase12-full-smoke.sh` as the Nyquist full-suite command for Phase 12.

    1. **Shape:** `#!/usr/bin/env bash`, `set -euo pipefail`, REPO_ROOT cd, FAIL counter, `pass`/`fail` helpers modeled on `scripts/phase07-live-smoke.sh`.

    2. **Hard asserts (all non-mutating)** — implement from 12-VALIDATION.md:
       - syntax: `bash -n arch/dots-hyprland.sh`
       - FULL-01 help: `help` contains `--full`
       - FULL-01/04 full dry-run: `printf 'yes\n' | install --full --dry-run` — would-exec present; no `--skip-hyprland`; no `--skip-sysupdate`; no standalone `--core`; no meta `--full` on would-exec line
       - FULL-02: bare `install --dry-run` still shows all three residuals
       - FULL-02b: bare `install-files --dry-run` still shows residuals
       - FULL-03: `install --full --skip-backup --dry-run` exits non-zero
       - FULL-03b: dual-key allow dry-run greps (optional but preferred)
       - FULL-05: full dry-run contains protect-list + ii hooks plan
       - D-02: `install-deps --full --dry-run` exits non-zero
       - D-04: help does not contain vendor-outside full note
       - D-13: help contains `dots-hyprland-workflow`

    3. **Safety:** Harness must never call `install --full` without `--dry-run`. Prefer capturing outputs under `/tmp/p12-smoke-*`.

    4. **Executable:** `chmod +x scripts/phase12-full-smoke.sh`.

    5. **VALIDATION.md:** Mark Wave 0 harness item complete / note full suite command `./scripts/phase12-full-smoke.sh` in Test Infrastructure if not already; do not set `nyquist_compliant: true` (that is validate-phase after execute).
  </action>
  <verify>
    <automated>test -x scripts/phase12-full-smoke.sh &amp;&amp; bash -n scripts/phase12-full-smoke.sh &amp;&amp; ./scripts/phase12-full-smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/phase12-full-smoke.sh` exists and is executable
    - `bash -n scripts/phase12-full-smoke.sh` exits 0
    - `./scripts/phase12-full-smoke.sh` exits 0 after 12-01..04 implementation is complete (during execute; if run mid-phase before impl, expect non-zero — executor runs after wrapper changes)
    - Script source contains `--dry-run` on all install invocations and does not contain a bare `install --full` without dry-run
    - Script covers FULL-01..05 by name or comment labels
    - `12-VALIDATION.md` references `./scripts/phase12-full-smoke.sh` as full suite command
  </acceptance_criteria>
  <done>One-command non-mutating FULL-01..05 smoke harness ready for phase gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dry-run post-setup plan → operator trust | Must mirror real protect/hooks so full dry-run is honest (D-16) |
| Smoke harness → host | Must not mutate packages, hypr, or run non-dry-run setup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-12 | Tampering | Skip protect on full | critical | mitigate | D-14: protect always runs; FULL-05 greps on full dry-run |
| T-12-13 | Tampering | Skip ii hooks on full | high | mitigate | D-15/D-16: enable_hypr_ii_hooks still planned/run |
| T-12-14 | Tampering | Smoke harness live install | high | mitigate | Harness ban non-dry-run install --full; code review grep |
| T-12-01 | Elevation of privilege | Accidental full default | critical | mitigate | FULL-02 asserts in harness |
| T-12-03 | Tampering | Bare skip-backup | high | mitigate | FULL-03 assert in harness |
| T-12-04 | Tampering | Dry-run mutates | high | mitigate | dry-run only; protect/hooks called with dry_run=1 |
| T-12-05 | Injection | cmd construction | high | mitigate | No eval; harness only greps wrapper output |
| T-12-SC | Tampering | package installs in harness | low | mitigate | No pacman/yay install in smoke script |
</threat_model>

<verification>
bash -n wrapper; full dry-run protect+hooks greps; ./scripts/phase12-full-smoke.sh (after impl).
</verification>

<success_criteria>
`install --full --dry-run` still plans PROTECT re-mark and ii hooks; `./scripts/phase12-full-smoke.sh` encodes FULL-01..05 non-mutating proofs for the phase gate.
</success_criteria>

<output>
Create `.planning/phases/12-wrapper-full-profile/12-04-SUMMARY.md` when done
</output>
