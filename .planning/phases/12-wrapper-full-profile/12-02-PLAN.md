---
phase: 12-wrapper-full-profile
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - arch/dots-hyprland.sh
autonomous: true
requirements:
  - FULL-03
  - FULL-04
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-06/D-08: printf 'yes\\n' | ./arch/dots-hyprland.sh install --full --dry-run hits the same type-yes interactive gate (prompt/output contains Type and yes) then prints would-exec"
    - "D-07: full-path gate messaging covers: no SAFE_DEFAULTS residuals on this path; hypr conf may become .old; misc may overwrite without --core; sysupdate/pacman -Syu may run on deps path; ~/ii-original-dots-backup; still refuse bare --skip-backup"
    - "FULL-03: ./arch/dots-hyprland.sh install --full --skip-backup --dry-run exits non-zero (bare skip refused without --allow-skip-backup)"
    - "FULL-03b: printf 'yes\\n' | ./arch/dots-hyprland.sh install --full --skip-backup --allow-skip-backup --dry-run exits 0; would-exec contains --skip-backup; strips --allow-skip-backup and --full"
    - "D-06: gate still accepts exact token yes only (not FULL / two-step); printf 'no\\n' | install --full --dry-run exits non-zero"
    - "bash -n arch/dots-hyprland.sh exits 0; safe-path backup_gate messaging for non-full install unchanged"
  artifacts:
    - path: "arch/dots-hyprland.sh"
      provides: "Full-specific backup gate messaging + FULL-03 dual-key skip-backup policy on --full"
      contains: "full-aware gate (backup_gate branch or full_backup_gate); bare --skip-backup refuse before gate; type-yes prompt"
  key_links:
    - from: "run_install_family full flag"
      to: "backup_gate / full_backup_gate"
      via: "needs_safe_defaults path still calls gate; messaging branches when full==1"
      pattern: "backup_gate"
    - from: "user_flags --skip-backup"
      to: "allow_skip_backup dual-key refuse"
      via: "refuse before gate when allow_skip_backup==0 (FULL-03)"
      pattern: "allow-skip-backup"
    - from: "install --full --dry-run"
      to: "gate then would-exec"
      via: "read Type yes then dry-run print (D-08)"
      pattern: "would exec"
  prohibitions:
    - statement: "no live full install"
      status: active
      verification: "All verifies use --dry-run only; no non-dry-run install --full"
    - statement: "no stronger FULL phrase / two-step gate"
      status: active
      verification: "Prompt still Type yes; no type FULL or second confirmation step (D-06)"
    - statement: "no never-allow-skip on full harder rule"
      status: active
      verification: "FULL-03b dual-key allow still succeeds (D-09)"
    - statement: "no INV/DISP wrapper gate"
      status: active
      verification: "No filesystem checks for 10-INVENTORY.md or 11-DISPOSITIONS.md"
    - statement: "no chrome teardown / live adopt"
      status: active
      verification: "No process kill or non-dry-run side effects added"
---

<objective>
Wave 2 expansion for Phase 12: full-path intentionality — same type-yes backup gate with full-specific D-07 blast-radius messaging, gate still on `--full --dry-run` (D-08), and FULL-03 bare `--skip-backup` refuse with dual-key allow intact.

Purpose: Prove operators cannot accidentally skip backup or miss full-profile risks while keeping the familiar gate UX (D-06/D-09).

Output: `arch/dots-hyprland.sh` with full-aware gate messaging and verified FULL-03 / gate-on-dry-run behavior.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-wrapper-full-profile/12-CONTEXT.md
@.planning/phases/12-wrapper-full-profile/12-RESEARCH.md
@.planning/phases/12-wrapper-full-profile/12-PATTERNS.md
@.planning/phases/12-wrapper-full-profile/12-VALIDATION.md
@.planning/phases/12-wrapper-full-profile/12-01-PLAN.md
@arch/dots-hyprland.sh
</context>

## Phase Goal

**Operator can dry-run and invoke an explicit full-install path without making full the accidental default.**

This plan delivers **full-path gate messaging + FULL-03 skip-backup policy** on top of the 12-01 `--full` argv strip/injection tracer.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Wrapper full-profile CLI | `arch/dots-hyprland.sh` (`--full` strip + injection branch) | 12-01 (core); expanded 12-02..04 |
| Full gate messaging | `backup_gate` / full branch in same file | **12-02** |
| Help / usage primary full path | `usage()` in same file | 12-03 |
| FULL-01..05 smoke proof | plan-task verifies (+ optional harness) | 12-04 |

## Decisions locked in this plan

- D-06: Full path uses the **same type-yes** interactive backup gate (not stronger phrase / two-step); messaging is full-specific
- D-07: Full-path gate messaging covers no SAFE_DEFAULTS residuals; hypr conf may become `.old`; misc may overwrite without `--core`; sysupdate may run on deps path; `~/ii-original-dots-backup`; still refuse bare `--skip-backup`
- D-08: `--dry-run` with `--full` **still hits** the interactive gate, then prints would-exec
- D-09: Bare `--skip-backup` on full still refused without `--allow-skip-backup` (FULL-03); dual-key allow still works

## Assumptions (specless probe)

- **EDGE_ABSENT:** FULL-03 / FULL-03b / gate-on-dry-run lifted into must_haves.truths from VALIDATION rows 12-FULL-03, 12-FULL-03b, 12-FULL-04
- Exact D-07 sentence wording is Claude discretion (CONTEXT); greps key themes not full paragraphs
- Gate structure: branch inside `backup_gate` **or** separate `full_backup_gate` — both acceptable per discretion

<tasks>

<task type="auto" id="12-02-01">
  <name>Task 1: Full-path type-yes gate messaging (D-06/D-07/D-08)</name>
  <reversibility rating="reversible">D-06/D-07: full-specific gate copy; safe path messaging must remain for non-full installs.</reversibility>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (backup_gate ~L149-160; run_install_family gate call ~L1394-1397; meta strip + full flag from 12-01)
    - .planning/phases/12-wrapper-full-profile/12-CONTEXT.md (D-06, D-07, D-08)
    - .planning/phases/12-wrapper-full-profile/12-RESEARCH.md (Pattern 3 backup gate; Pitfall 4 skip gate on dry-run; Pitfall 5 safe messaging on full; Validation D-07 / FULL-04)
    - .planning/phases/12-wrapper-full-profile/12-VALIDATION.md (12-FULL-04 gate + would-exec; D-07 themes; cheatsheet gate abort)
  </read_first>
  <action>
    Implement full-path gate messaging in `arch/dots-hyprland.sh` (per D-06, D-07, D-08). Depends on 12-01 having `full` available in `run_install_family`.

    1. **Same type-yes UX (D-06):** Keep the interactive gate pattern: `read -r -p` requiring exact answer `yes` (not a stronger phrase, not a two-step gate). Abort path still prints `[FAIL]` and exits 1 without invoking `./setup`. Safe-path (non-full) `backup_gate` text that mentions residual protection of personal hypr must **remain** for default install.

    2. **Full-specific messaging (D-07):** When `full==1`, gate output must cover these themes (wording discretion; greppable themes):
       - This path does **not** inject SAFE_DEFAULTS residuals (no residual safe flags on this path)
       - hypr conf may become `.old` (personal hyprland.conf is at risk of rename)
       - misc may overwrite when `--core` is absent
       - sysupdate / `pacman -Syu` may run on the deps portion of the path
       - upstream backup dir `~/ii-original-dots-backup` (or `ii-original-dots-backup`)
       - bare `--skip-backup` is still refused / do not pass bare skip on first adoption
       Do **not** print the safe-path claim that defaults include skip-hyprland so personal conf is not renamed — that misleads on full (RESEARCH Pitfall 5).

    3. **Structure (discretion):** Either (a) branch inside `backup_gate` on a full argument / global, or (b) add `full_backup_gate` and call it from `run_install_family` when `full==1`. Call site must still run for `needs_safe_defaults` paths that are not help-only — including when `dry_run==1` (D-08).

    4. **Wire full into gate call:** Ensure `run_install_family` passes `full` into the gate (or selects the full branch) so `--full --dry-run` hits full messaging then continues to would-exec print. Do not skip the gate when dry-run is set.

    5. **Out of scope this task:** usage() rewrite (12-03); protect/hooks dry-run plan changes (already present from prior phases); live install; INV/DISP checks (D-10).
  </action>
  <verify>
    <automated>bash -n arch/dots-hyprland.sh &amp;&amp; printf 'yes\n' | ./arch/dots-hyprland.sh install --full --dry-run &gt;/tmp/p12-02-gate.txt 2&gt;&amp;1 &amp;&amp; (grep -q 'Type' /tmp/p12-02-gate.txt || grep -q "yes" /tmp/p12-02-gate.txt) &amp;&amp; grep -q 'would exec' /tmp/p12-02-gate.txt &amp;&amp; grep -q 'ii-original-dots-backup' /tmp/p12-02-gate.txt &amp;&amp; grep -qiE '\.old|sysupdate|SAFE_DEFAULTS|residual|skip-backup' /tmp/p12-02-gate.txt &amp;&amp; printf 'no\n' | ./arch/dots-hyprland.sh install --full --dry-run &gt;/tmp/p12-02-abort.txt 2&gt;&amp;1; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n arch/dots-hyprland.sh` exits 0
    - `printf 'yes\n' | ./arch/dots-hyprland.sh install --full --dry-run` exits 0
    - Output shows type-yes gate evidence (`Type` and/or `yes` prompt text per existing wrapper style)
    - Output contains `would exec` (or existing dry-run would-exec marker) after the gate (D-08)
    - Output contains `ii-original-dots-backup`
    - Output covers D-07 full-risk themes (at least one of: `.old`, sysupdate, no SAFE_DEFAULTS residual / residual wording, bare skip-backup refuse note)
    - Full path does **not** claim that defaults keep personal hyprland.conf un-renamed via skip-hyprland
    - `printf 'no\n' | ./arch/dots-hyprland.sh install --full --dry-run` exits non-zero
    - Default (non-full) `install --dry-run` still uses safe-path gate messaging (residual protection note intact)
  </acceptance_criteria>
  <done>Full --dry-run hits type-yes gate with D-07 messaging then prints would-exec; abort still works; safe path gate text unchanged.</done>
</task>

<task type="auto" id="12-02-02">
  <name>Task 2: FULL-03 bare skip-backup refuse + dual-key allow on full</name>
  <reversibility rating="reversible">D-09: shared dual-key policy on full; no harder never-allow-skip rule.</reversibility>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (skip-backup refuse ~L1387-1392; meta strip --allow-skip-backup / --full from 12-01; backup_gate call site)
    - .planning/phases/12-wrapper-full-profile/12-CONTEXT.md (D-09 FULL-03)
    - .planning/phases/12-wrapper-full-profile/12-RESEARCH.md (Validation FULL-03 / FULL-03b; Architecture diagram dual-key before gate)
    - .planning/phases/12-wrapper-full-profile/12-VALIDATION.md (12-FULL-03 / 12-FULL-03b rows + cheatsheet)
  </read_first>
  <action>
    Lock FULL-03 on the full path without inventing a harder never-allow-skip rule (per D-09).

    1. **Bare refuse (FULL-03):** Keep the existing pre-gate check in `run_install_family`: if `user_flags` contain `--skip-backup` and `allow_skip_backup==0`, print `[FAIL]` and exit 1. This must fire for `install --full --skip-backup --dry-run` **before** the interactive gate (operator never reaches type-yes on bare skip). Do not special-case full to permanently ban skip even with allow.

    2. **Dual-key allow (FULL-03b / D-09):** When both `--skip-backup` (in user flags) and `--allow-skip-backup` (meta, stripped) are present with `--full --dry-run`, path proceeds through gate (feed `yes`) to would-exec. Would-exec / install log must include `--skip-backup` (forwarded upstream flag) and must **not** include wrapper meta tokens `--allow-skip-backup` or `--full`.

    3. **Strip contract:** Confirm `--full` and `--allow-skip-backup` remain strip-only in the meta case arm (never appended to `user_flags` / `cmd`). Do not forward them to `./setup`.

    4. **Do not** change SAFE_DEFAULTS injection logic from 12-01. Do not rewrite `usage()` (12-03). Do not add live install paths.
  </action>
  <verify>
    <automated>bash -n arch/dots-hyprland.sh &amp;&amp; ./arch/dots-hyprland.sh install --full --skip-backup --dry-run &gt;/tmp/p12-02-sb-refuse.txt 2&gt;&amp;1; test $? -ne 0 &amp;&amp; grep -qi 'skip-backup' /tmp/p12-02-sb-refuse.txt &amp;&amp; printf 'yes\n' | ./arch/dots-hyprland.sh install --full --skip-backup --allow-skip-backup --dry-run &gt;/tmp/p12-02-sb-allow.txt 2&gt;&amp;1 &amp;&amp; grep -q 'would exec' /tmp/p12-02-sb-allow.txt &amp;&amp; grep -q -- '--skip-backup' /tmp/p12-02-sb-allow.txt &amp;&amp; ! grep -q -- '--allow-skip-backup' /tmp/p12-02-sb-allow.txt &amp;&amp; ! grep -q -- '--full' /tmp/p12-02-sb-allow.txt</automated>
  </verify>
  <acceptance_criteria>
    - `./arch/dots-hyprland.sh install --full --skip-backup --dry-run` exits non-zero
    - Refuse output mentions skip-backup / allow-skip-backup policy (existing `[FAIL]` style)
    - `printf 'yes\n' | ./arch/dots-hyprland.sh install --full --skip-backup --allow-skip-backup --dry-run` exits 0
    - Dual-key dry-run output contains `would exec`
    - Dual-key dry-run output contains `--skip-backup`
    - Dual-key dry-run output does not contain `--allow-skip-backup` (meta stripped)
    - Dual-key dry-run output does not contain `--full` (meta stripped)
    - Gate still ran on dual-key path (type-yes evidence present when stdin fed `yes`)
  </acceptance_criteria>
  <done>Bare --skip-backup on full refused; dual-key allow works; --full and --allow-skip-backup stripped from would-exec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell → wrapper | Untrusted argv (`--full`, `--skip-backup`, `--allow-skip-backup`, `--dry-run`) crosses into `run_install_family` |
| Interactive TTY → gate | Operator confirmation (`yes`) required before files-touching / dry-run would-exec |
| Wrapper → `./setup` | Only stripped, allowlisted subcommands + built `cmd` array (meta never forwarded) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03 | Tampering | Bare `--skip-backup` on full | high | mitigate | Pre-gate refuse without `--allow-skip-backup` (FULL-03); dual-key allow only (D-09) |
| T-12-06 | Elevation of privilege | Full gate messaging | high | mitigate | D-07 blast-radius themes before type-yes; no safe-path “conf protected” claim on full |
| T-12-07 | Tampering | Skip gate on `--full --dry-run` | medium | mitigate | D-08: gate still runs before would-exec; abort with non-yes exits non-zero |
| T-12-08 | Spoofing | Stronger phrase / two-step confusion | low | mitigate | D-06: same type-yes token only; no type FULL |
| T-12-02 | Tampering | Meta flag forward to setup | high | mitigate | Strip `--full` and `--allow-skip-backup`; FULL-03b greps absence |
| T-12-04 | Tampering | Dry-run mutates host | high | mitigate | Verifies only `--dry-run`; exit before `"${cmd[@]}"` |
| T-12-05 | Injection | cmd construction | high | mitigate | Array exec only; never eval |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase |
</threat_model>

<verification>
bash -n; install --full --dry-run gate + would-exec + D-07 themes; bare --skip-backup refuse; dual-key allow greps; gate abort with no.
</verification>

<success_criteria>
Operator feeding `yes` on `install --full --dry-run` sees full-risk gate messaging then would-exec; bare `--skip-backup` on full fails; dual-key allow still works with meta stripped.
</success_criteria>

<output>
Create `.planning/phases/12-wrapper-full-profile/12-02-SUMMARY.md` when done
</output>
