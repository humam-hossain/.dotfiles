---
phase: 06-thin-setup-wrapper-safe-defaults
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - arch/dots-hyprland.sh
autonomous: true
requirements:
  - WRAP-02
  - WRAP-03
  - WRAP-04
user_setup: []

must_haves:
  truths:
    - "For install and install-files, dry-run argv includes --core --skip-hyprland --skip-sysupdate before any user flags"
    - "install-deps and install-setups do not inject those file-protection defaults"
    - "Hard interactive gate requires typing yes before install/install-files (skipped for pure subcommand help -h/--help)"
    - "Gate messaging names ~/ii-original-dots-backup (or ii-original-dots-backup), Quickshell overwrite risk, and skip-hyprland protection"
    - "--skip-backup alone is refused non-zero; with --allow-skip-backup, --skip-backup is forwarded and the meta flag is not"
    - "User extra flags appear after injected defaults in logged argv (WRAP-04); wrapper never auto-injects --force or --skip-allgreeting"
    - "Protection default is full --skip-hyprland, not entry-only skip"
  artifacts:
    - path: "arch/dots-hyprland.sh"
      provides: "Safe dual-run defaults, backup gate, skip-backup policy, passthrough merge"
      contains: "needs_safe_defaults, backup_gate, SAFE_DEFAULTS injection, meta-flag strip, D-10 logging"
  key_links:
    - from: "SAFE_DEFAULTS"
      to: "./setup install|install-files argv"
      via: "cmd=(./setup sub) + defaults + user_flags"
    - from: "backup_gate"
      to: "install|install-files exec"
      via: "read yes-token before dry-run print or real exec"
    - from: "--allow-skip-backup"
      to: "wrapper only"
      via: "strip before setup; unlock forwarding of --skip-backup"
  prohibitions:
    - "Do not default-inject --skip-hyprland-entry as hypr protection"
    - "Do not default-inject --force or --skip-allgreeting"
    - "Do not silently strip --skip-backup; refuse unless --allow-skip-backup"
    - "Do not forward --allow-skip-backup or --dry-run to setup"
    - "Do not gate install-deps or install-setups"
    - "Do not live-mutate machine in plan verifies (always --dry-run)"
    - "Do not reimplement package lists"
---

<objective>
Encode WRAP-02 safe dual-run defaults, WRAP-03 backup gate / skip-backup policy, and WRAP-04 passthrough merge order on top of the 06-01 scaffold (per D-05..D-13).

Purpose: Make the default operator path non-destructive to personal hyprland.conf and force an explicit backup acknowledgment before files-touching setup calls.

Output: Same `arch/dots-hyprland.sh` with policy-complete control flow; dry-run asserts prove defaults, gate text, refuse/allow skip-backup, and flag order.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-CONTEXT.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-RESEARCH.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-PATTERNS.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-VALIDATION.md
@.planning/phases/06-thin-setup-wrapper-safe-defaults/06-01-SUMMARY.md
@arch/dots-hyprland.sh
@vendor/dots-hyprland/sdata/subcmd-install/options.sh
</context>

## Phase Goal

Provide a `.dotfiles`-native entrypoint that drives upstream setup without destroying personal Hyprland config. This plan encodes the safe default profile and backup messaging.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Thin setup wrapper (policy complete) | `arch/dots-hyprland.sh` | 06-01 scaffold + **06-02** policy |
| Full WRAP smoke proof | SUMMARY + asserts | 06-03 |

## Tasks

<task type="auto">
  <name>Task 1: Inject SAFE_DEFAULTS for install and install-files only</name>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (06-01 output)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-CONTEXT.md (D-05, D-06, D-07, D-08, D-09, D-10)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-RESEARCH.md (Defaults matrix; Pattern 2 argv; Recommended control flow steps 5,7–9; known limitation no --no-skip-hyprland)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-VALIDATION.md (06-02-T1, T2, T3, T7 commands)
  </read_first>
  <action>
    Wire safe-default injection into the existing exec path (per D-05..D-10):

    1. Add `needs_safe_defaults()` true only for subcommands `install` and `install-files` (false for install-deps and install-setups).
    2. When building `cmd`: start `cmd=(./setup "$subcmd")`; if needs_safe_defaults, append `"${SAFE_DEFAULTS[@]}"` then append user flag array. Final order must be: `./setup` → subcommand → `--core --skip-hyprland --skip-sysupdate` → user flags (D-09). Duplicates OK if user also passed the same long flag; do not auto-dedupe or require a raw escape hatch.
    3. Before exec/dry-run print: `echo "[CONFIG] safe defaults: ${SAFE_DEFAULTS[*]}"` when defaults were injected; always log full argv with a labeled `[INSTALL]` (or equivalent) line including cwd II_ROOT (D-10).
    4. Never auto-inject `--force`, `-f`, or `--skip-allgreeting` (D-07). Never use entry-only hypr skip as the protection default — only full `--skip-hyprland` inside SAFE_DEFAULTS (D-08).
    5. Keep `--dry-run` and `--allow-skip-backup` stripped from forwarded flags. User extras such as `--exp-files` must appear after defaults in the logged line (WRAP-04).
    6. Help text (if not already complete) must state which subcommands get defaults and list the three default flags. Note known limitation: once injected, there is no upstream positive flag to undo `--skip-hyprland`; full hypr install is via direct vendor setup (document; do not add escape hatch).
    7. Do not implement backup_gate in this task (Task 2). Temporary note: install dry-run may still run without yes until Task 2 lands in the same plan wave order — complete Task 2 before finishing the plan.
  </action>
  <verify>
    <automated>bash -n arch/dots-hyprland.sh && out=$(./arch/dots-hyprland.sh install-deps --dry-run); echo "$out" | grep -q setup; ! echo "$out" | grep -q -- '--skip-hyprland'</automated>
  </verify>
  <acceptance_criteria>
    - `needs_safe_defaults` (or equivalent) is true only for install and install-files.
    - After Task 2 enables the gate, `printf 'yes\n' | ./arch/dots-hyprland.sh install --dry-run` output contains `--core`, `--skip-hyprland`, and `--skip-sysupdate`.
    - `./arch/dots-hyprland.sh install-deps --dry-run` output mentions setup/install-deps and does not contain `--skip-hyprland` as an injected default.
    - SAFE_DEFAULTS does not include force flags or entry-only hypr skip.
    - `bash -n` still clean.
  </acceptance_criteria>
  <done>Safe defaults inject only on install/install-files and log in argv order defaults-then-user.</done>
</task>

<task type="auto">
  <name>Task 2: Hard backup gate + --skip-backup refuse/allow policy</name>
  <files>arch/dots-hyprland.sh</files>
  <read_first>
    - arch/dots-hyprland.sh (after Task 1)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-CONTEXT.md (D-11, D-12, D-13)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-RESEARCH.md (Pattern 3 backup_gate; control flow steps 3 and 6; Pitfall 1 and 4)
    - .planning/phases/06-thin-setup-wrapper-safe-defaults/06-VALIDATION.md (06-02-T4, T5, T6)
  </read_first>
  <action>
    Implement WRAP-03 policy on files-touching subcommands:

    1. **Skip-backup policy (D-12)** — after scanning args, if user flags contain `--skip-backup` and wrapper did not see `--allow-skip-backup`, print `[FAIL]` explaining that bare skip-backup is refused and that first adoption must not skip backup; mention the override flag name; exit non-zero without calling setup. If both present: proceed, forward `--skip-backup` only (meta override already stripped). Never silently drop `--skip-backup`. Never encourage skip on first adoption in help or gate text.
    2. **Hard gate (D-11)** — for `install` and `install-files` only, before dry-run print or real exec, call `backup_gate` unless the remaining user args are solely subcommand help (`-h` or `--help` present as the help path — skip gate so `install -h` stays non-interactive). No gate on install-deps / install-setups.
    3. **Gate messaging (D-13)** must include all three facts (wording discretionary, content locked):
       - Upstream backup location using `ii-original-dots-backup` (home-relative form OK)
       - Quickshell config will be overwritten
       - Defaults keep personal hyprland.conf via `--skip-hyprland`
       - Optional extra line discouraging skip-backup on first adoption
    4. **Yes-token (discretion):** require exact case-sensitive `yes` via `read -r` so tests can `printf 'yes\n' |`. On any other answer: `[FAIL] Aborted (backup gate). No ./setup invoked.` exit non-zero.
    5. Order relative to dry-run: policy checks and gate run even for `--dry-run` on install/install-files so smoke covers gate text; after yes, dry-run still only prints would-exec and exits 0.
    6. Ensure refuse path for bare `--skip-backup` does not require the yes gate (fail closed earlier is fine).
    7. Do not live-run real install in verification.
  </action>
  <verify>
    <automated>bash -n arch/dots-hyprland.sh && ./arch/dots-hyprland.sh install --skip-backup --dry-run; test $? -ne 0 && printf 'yes\n' | ./arch/dots-hyprland.sh install --dry-run 2>&1 | tee /tmp/w6-02-gate.txt | grep -q 'ii-original-dots-backup' && grep -qi 'quickshell' /tmp/w6-02-gate.txt && grep -q 'skip-hyprland' /tmp/w6-02-gate.txt && printf 'yes\n' | ./arch/dots-hyprland.sh install --skip-backup --allow-skip-backup --dry-run | tee /tmp/w6-02-sb.txt | grep -q -- '--skip-backup' && ! grep -q -- '--allow-skip-backup' /tmp/w6-02-sb.txt && printf 'yes\n' | ./arch/dots-hyprland.sh install --dry-run | grep -q -- '--core' && printf 'yes\n' | ./arch/dots-hyprland.sh install --dry-run | grep -q -- '--skip-hyprland' && printf 'yes\n' | ./arch/dots-hyprland.sh install --dry-run | grep -q -- '--skip-sysupdate' && printf 'yes\n' | ./arch/dots-hyprland.sh install --exp-files --dry-run | grep -E -- '--core.*--exp-files|--skip-hyprland.*--exp-files'</automated>
  </verify>
  <acceptance_criteria>
    - `./arch/dots-hyprland.sh install --skip-backup --dry-run` exits non-zero.
    - `printf 'yes\n' | ./arch/dots-hyprland.sh install --dry-run` exits 0; output includes ii-original-dots-backup, quickshell (case-insensitive), skip-hyprland, --core, --skip-sysupdate.
    - `printf 'yes\n' | ./arch/dots-hyprland.sh install --skip-backup --allow-skip-backup --dry-run` shows `--skip-backup` in would-exec and does not forward `--allow-skip-backup`.
    - `printf 'no\n' | ./arch/dots-hyprland.sh install --dry-run` exits non-zero.
    - `printf 'yes\n' | ./arch/dots-hyprland.sh install --exp-files --dry-run` shows user flag after defaults in the logged argv.
    - install-deps --dry-run still has no backup gate prompt requirement (exits 0 without piping yes).
    - No real install-files mutation occurred during verifies.
  </acceptance_criteria>
  <done>Backup gate + skip-backup policy + safe defaults form the complete WRAP-02/03/04 operator path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator confirmation → files-touching setup | Human must acknowledge backup/overwrite before install/install-files |
| Operator flags → setup getopt | Meta flags stripped; dangerous skip-backup dual-keyed |
| Defaults injection → hypr tree | Full --skip-hyprland protects personal conf |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02 | Tampering / data loss | --skip-backup path | high | mitigate | Refuse bare --skip-backup unless --allow-skip-backup; never default skip-backup; strip meta before setup |
| T-06-03 | Tampering | hypr conf install | high | mitigate | Default inject full --skip-hyprland (not entry-only) plus --core --skip-sysupdate on install/install-files |
| T-06-01 | Elevation of Privilege | subcommand surface | high | mitigate | Unchanged allowlist from 06-01; policy does not widen subcommands |
| T-06-04 | Tampering | argv merge | high | mitigate | Array append defaults then user flags; no shell-eval; strip --dry-run and --allow-skip-backup |
| T-06-06 | Information Disclosure | gate/logs | low | mitigate | Gate text is operational paths only; no secrets |
| T-06-SC | Tampering | package installs | low | accept | No package-manager installs in this plan |
</threat_model>

<verification>
Dry-run matrix from 06-VALIDATION 06-02-T1..T7: defaults on install, absent on install-deps, gate messaging, skip-backup refuse/allow, passthrough order.
</verification>

<success_criteria>
WRAP-02, WRAP-03, and WRAP-04 hold on dry-run paths: safe defaults, hard backup gate, skip-backup dual-key, user flags after defaults.
</success_criteria>

<output>
Create `.planning/phases/06-thin-setup-wrapper-safe-defaults/06-02-SUMMARY.md` when done
</output>
