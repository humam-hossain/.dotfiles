---
phase: 04-ipc-keybinds-integration
plan: 04
type: execute
wave: 4
depends_on:
  - "04-01"
files_modified:
  - .planning/phases/04-ipc-keybinds-integration/04-DEFERRED.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - FWK-02
  - IPC-02
must_haves:
  truths:
    - FWK-02 (exec-once auto-start) is explicitly deferred with finishing-touch notes and zero hyprland.conf product edits this pass (D-01, D-02, D-03)
    - IPC-02 (Hyprland bar-toggle keybind) is explicitly deferred with chord undecided and GlobalShortcut names left unbound (D-09)
    - Roadmap SC-2 and SC-4 are documented as out of this pass acceptance; SC-1 and SC-3 remain in-pass (D-03)
    - Waybar cutover and hard-restart keybind are backlog-only with no implementation tasks
  artifacts:
    - path: .planning/phases/04-ipc-keybinds-integration/04-DEFERRED.md
      provides: "Finishing-touch backlog for FWK-02, IPC-02, Waybar cutover, hard-restart"
    - path: .planning/ROADMAP.md
      provides: "Phase 4 plan list + acceptance note that SC-2/SC-4 are out of this pass"
  key_links:
    - from: 04-DEFERRED.md
      to: .config/hypr/hyprland.conf
      via: "reference-only; no edits this pass"
      pattern: "exec-once"
    - from: 04-DEFERRED.md
      to: Bar.qml GlobalShortcut barToggle
      via: "future Hyprland global bind target"
      pattern: "barToggle"
  prohibitions:
    - Do not edit hyprland.conf this pass (no keybind, no exec-once, no SUPER+w changes)
    - Do not remove Waybar from session
    - Do not add hard-restart keybind
    - Do not add soft Hyprland stubs unless finishing pass requests them (user default: no)
    - Do not implement FWK-02 or IPC-02 product behavior in this plan
    - Do not invent reload CLI or new IPC targets
---

<objective>
Package deferred Phase 4 finishing-touch work (FWK-02 exec-once, IPC-02 Hyprland keybind, Waybar cutover, hard-restart keybind) as an explicit backlog document and roadmap/state notes so this pass cannot accidentally implement them, while still covering those requirement IDs as deferred.

Purpose: CONTEXT D-01–D-03 narrow Phase 4 acceptance to IPC-01 + IPC-03 verify. Roadmap still lists full integration goals — planner must leave zero Hyprland conf changes and document SC-2/SC-4 as out of this pass.
Output: 04-DEFERRED.md + ROADMAP/STATE notes. No product QML or hyprland.conf edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ipc-keybinds-integration/04-CONTEXT.md
@.planning/phases/04-ipc-keybinds-integration/04-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.config/hypr/hyprland.conf
</context>

## Artifacts this phase produces (this plan)

| Symbol / artifact | Kind | Path |
|-------------------|------|------|
| Deferred backlog doc | docs | `04-DEFERRED.md` |
| Phase 4 acceptance note | docs | `.planning/ROADMAP.md` |
| Position / next-step note | docs | `.planning/STATE.md` |

## Decisions locked in this plan

- D-01 / D-02 / D-03: IPC+reload only this pass; SC-2 and SC-4 out of acceptance
- D-09: GlobalShortcut names may remain; do not bind in Hyprland
- D-12: hard-restart keybind not this pass

## Assumptions (specless probe)

- **FWK-02 edges:** deferred — login auto-start not verified this pass
- **IPC-02 edges (idempotency/concurrency):** deferred — keybind not wired; when finished, prefer idempotent toggle via existing bar IPC or global shortcut
- This repo uses default config (`qs` / `quickshell`, not `qs -c ii`) for any future exec-once line

<tasks>

<task type="auto" id="04-04-01">
  <name>Task 1: Write 04-DEFERRED.md finishing-touch backlog</name>
  <files>.planning/phases/04-ipc-keybinds-integration/04-DEFERRED.md</files>
  <read_first>
    - .planning/phases/04-ipc-keybinds-integration/04-CONTEXT.md (Deferred Ideas, D-01..D-03, D-09, D-12)
    - .planning/phases/04-ipc-keybinds-integration/04-RESEARCH.md (Deferred Backlog table, Pattern 4 launch facts)
    - .planning/REQUIREMENTS.md (FWK-02, IPC-02)
    - .config/hypr/hyprland.conf (current exec-once and SUPER+w — read only)
  </read_first>
  <action>
    Create `.planning/phases/04-ipc-keybinds-integration/04-DEFERRED.md` documenting finishing-touch backlog only (no implementation):

    1. **Header:** Phase 4 deferred items; status = not in this pass acceptance (D-01..D-03).

    2. **IPC-02 — Hyprland bar toggle keybind:**
       - Requirement text from REQUIREMENTS.md
       - Options for later: Hyprland `global` bind to stock GlobalShortcut name `barToggle` (dots-style) **or** `exec, qs ipc call bar toggle`
       - Chord undecided (roadmap example SUPER+B; SUPER+w currently restarts Waybar — do not change now)
       - Note GlobalShortcut blocks may already exist in Bar.qml; leave unbound this pass (D-09)

    3. **FWK-02 — exec-once auto-start:**
       - Requirement text
       - Future line should use default config (`qs -d` or `qs &amp;` / `quickshell`), **not** `qs -c ii`
       - Coordinate with dual-run Waybar until cutover
       - Current hyprland.conf keeps existing waybar/swaync/hyprpaper exec-once — do not edit this pass

    4. **Waybar removal / cutover:** after milestone parity SC-5; leave dual-run now

    5. **Hard-restart keybind:** dots-style killall + relaunch — only if desired later; not graceful reload; D-12 manual recovery remains default

    6. **Explicit non-actions this pass:** zero hyprland.conf edits; no soft stubs by default; no ReloadPopup enable; no auto-relaunch daemon

    7. **In-pass requirements reminder:** IPC-01 and IPC-03 handled by plans 04-01..04-03

    Do not modify `.config/hypr/hyprland.conf` or any QML.
  </action>
  <acceptance_criteria>
    - File `04-DEFERRED.md` exists
    - Mentions FWK-02 and IPC-02 as deferred
    - Mentions default config launch (no requirement to use named `-c ii`)
    - Mentions Waybar cutover and hard-restart as backlog
    - States zero hyprland.conf changes for this pass
    - Does not contain implementation steps that edit hyprland.conf now
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/04-ipc-keybinds-integration/04-DEFERRED.md &amp;&amp; rg -n 'FWK-02|IPC-02|exec-once|barToggle|Waybar|hard-restart|hyprland.conf' .planning/phases/04-ipc-keybinds-integration/04-DEFERRED.md | head -40</automated>
  </verify>
  <done>Deferred backlog packaged with finishing-touch notes; no product/Hyprland edits.</done>
</task>

<task type="auto" id="04-04-02">
  <name>Task 2: Annotate ROADMAP and STATE for narrowed pass acceptance</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 section)
    - .planning/STATE.md
    - .planning/phases/04-ipc-keybinds-integration/04-DEFERRED.md
    - .planning/phases/04-ipc-keybinds-integration/04-CONTEXT.md (D-03)
  </read_first>
  <action>
    Scoped edits only (do not rewrite entire roadmap):

    1. **ROADMAP.md Phase 4 section:**
       - Ensure **Plans:** lists 4 plans with checkboxes for 04-01..04-04 and brief objectives
       - Add a short **This pass acceptance** note under Phase 4: SC-1 (IPC show/hide) and SC-3 (graceful soft reload) in scope; SC-2 (keybind) and SC-4 (exec-once) deferred per D-03 / 04-DEFERRED.md; SC-5 remains milestone gate
       - Do not mark Phase 4 complete
       - Do not delete deferred requirements from the requirements line (FWK-02, IPC-01, IPC-02, IPC-03 stay listed)

    2. **STATE.md:**
       - Note Phase 4 plans created; in-pass focus IPC-01 + IPC-03 verify; FWK-02/IPC-02 deferred to finishing touch
       - Point next execute toward `/gsd-execute-phase 4` (or current project command style)
       - Keep accumulated decisions; add a one-line Phase 4 scope-narrowing decision if missing

    3. Verify no accidental hyprland.conf changes in the working tree from this plan (`git status` / `git diff -- .config/hypr/hyprland.conf` empty for this work).
  </action>
  <acceptance_criteria>
    - ROADMAP Phase 4 lists plans 04-01 through 04-04
    - ROADMAP notes SC-2 and SC-4 out of this pass acceptance (or equivalent D-03 wording)
    - STATE.md mentions Phase 4 plan focus IPC-01/IPC-03 and deferred FWK-02/IPC-02
    - `git diff -- .config/hypr/hyprland.conf` is empty for this plan's changes
  </acceptance_criteria>
  <verify>
    <automated>rg -n '04-0[1-4]|SC-2|SC-4|deferred|IPC-01|IPC-03|FWK-02|IPC-02' .planning/ROADMAP.md .planning/STATE.md | head -50; git diff -- .config/hypr/hyprland.conf | wc -l</automated>
  </verify>
  <done>Roadmap and state reflect narrowed pass acceptance; deferred requirements covered as backlog only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs only | This plan does not touch runtime IPC or Hyprland session config |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | Future keybind IPC misuse | low | accept | Deferred; when implemented prefer global shortcut or stock bar IPC without new targets |
| T-04-03 | Availability | Future auto-start crash loops | medium | mitigate | Deferred FWK-02 notes dual-run caution; D-12 still forbids auto-relaunch daemon |
| T-04-SC | Tampering | package installs | high | accept | No installs |
</threat_model>

<verification>
- 04-DEFERRED.md documents FWK-02 and IPC-02
- ROADMAP/STATE note SC-2/SC-4 out of pass
- hyprland.conf untouched
</verification>

<success_criteria>
- Deferred requirement IDs covered as explicit backlog (not silent omission)
- Zero Hyprland product changes this pass
- Finishing-touch notes sufficient for a later plan without re-discussing stock IPC surface
</success_criteria>

<output>
Create `.planning/phases/04-ipc-keybinds-integration/04-04-SUMMARY.md` when done
</output>
