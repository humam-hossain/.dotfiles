---
phase: 04-ipc-keybinds-integration
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - .planning/phases/04-ipc-keybinds-integration/04-UAT.md
  - .planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md
autonomous: false
requirements:
  - IPC-03
must_haves:
  truths:
    - "Soft reload produces no ReloadPopup / reload banner (QS_NO_RELOAD_POPUP=1 retained) (D-10)"
    - "After successful soft reload, bar modules remain usable and tray remains interactive on the Quickshell bar (D-11)"
    - Soft reload keeps the same process PID (file-watch content change path); IPC bar open still works after
    - Hide-state survival across reload is not required; post-reload usability is required (research A1)
    - "No new visual components or layout/token changes ship unless verification proves stock broken (D-13)"
    - statement: "After soft reload, tray may briefly rebind SNI under dual Waybar+QS; pass when QS tray icons are present and clickable within normal settle time; dual-host log noise alone is not failure"
      verification: backstop
    - statement: "UAT exercises 0, 1, and many tray icons after reload when feasible; layout remains usable without new empty-state copy"
      verification: backstop
  artifacts:
    - path: .planning/phases/04-ipc-keybinds-integration/04-UAT.md
      provides: "IPC-03 soft-reload + tray human UAT section"
    - path: .planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md
      provides: "IPC-03 automated row statuses + optional nyquist note"
  key_links:
    - from: shell.qml QS_NO_RELOAD_POPUP pragma
      to: ReloadPopup suppression
      via: "env pragma + ReloadPopup instance remains silent"
      pattern: "QS_NO_RELOAD_POPUP=1"
    - from: file-watch content change
      to: same PID soft reload
      via: "Quickshell.watchFiles default true"
      pattern: "Configuration Loaded"
    - from: post-reload SysTray
      to: TrayService / SystemTray items
      via: "StatusNotifier rebind after QML rebuild"
      pattern: "SysTray"
  prohibitions:
    - Do not invent a reload CLI subcommand missing on Quickshell 0.3.0
    - Do not add custom reload IpcHandler (D-07)
    - Do not enable ReloadPopup / remove silence pragma (D-10)
    - Do not add auto-relaunch daemon or hard-restart keybind (D-12)
    - Do not edit hyprland.conf
    - Do not require GlobalStates.barOpen=false to survive reload
    - Do not pre-harden tray without red UAT evidence (D-13)
---

<objective>
Prove IPC-03 graceful soft reload: content-change file-watch reload keeps the same PID, stays silent (no popup), leaves bar modules usable and system tray interactive on the Quickshell bar, and keeps stock bar IPC working afterward.

Purpose: Research corrected D-07/D-11 — there is no reload CLI on 0.3.0; stock soft reload is content-change / Quickshell.reload API. This plan verifies that path and D-10/D-11 UX without inventing wrappers.
Output: Automated soft-reload evidence, 04-UAT.md IPC-03 section, human tray/bar post-reload approval; minimal QML fix only if red (D-13).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ipc-keybinds-integration/04-CONTEXT.md
@.planning/phases/04-ipc-keybinds-integration/04-RESEARCH.md
@.planning/phases/04-ipc-keybinds-integration/04-UI-SPEC.md
@.planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md
@.planning/phases/04-ipc-keybinds-integration/04-UAT.md
@scripts/phase04-ipc-reload-assert.py
@.config/quickshell/shell.qml
@.config/quickshell/ReloadPopup.qml
@.config/quickshell/modules/ii/bar/SysTray.qml
@.config/quickshell/services/TrayService.qml
</context>

## Artifacts this phase produces (this plan)

| Symbol / artifact | Kind | Path |
|-------------------|------|------|
| IPC-03 UAT section | docs | `04-UAT.md` |
| IPC-03 gate statuses | docs | `04-VALIDATION.md` |
| Conditional tray/shell fix | QML (only if red) | SysTray / TrayService / shell.qml |

## Decisions locked in this plan

- D-07: no custom reload IPC; stock soft reload only (research: file-watch content change)
- D-10: keep silent reload pragma
- D-11: bar + tray usable after reload
- D-12: failed reload → manual relaunch only
- D-13: code only if verification red

## Assumptions (specless probe)

- **IPC-03 unclassified edge:** Soft reload same-PID + bar/tray usable; GlobalStates survival not required
- Dual Waybar+QS SNI noise acceptable until cutover; failure = QS tray unusable only
- Hard process kill+relaunch is recovery only, not the graceful path

<tasks>

<task type="auto" id="04-03-01">
  <name>Task 1: Automated soft-reload same-PID + post-reload IPC gates</name>
  <files>.planning/phases/04-ipc-keybinds-integration/04-UAT.md, .planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md</files>
  <read_first>
    - scripts/phase04-ipc-reload-assert.py
    - .planning/phases/04-ipc-keybinds-integration/04-RESEARCH.md (Pattern 5, Pitfalls 1/3/4/5)
    - .planning/phases/04-ipc-keybinds-integration/04-UI-SPEC.md (Reload UX Contract)
    - .planning/phases/04-ipc-keybinds-integration/04-UAT.md
    - .planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md
    - .config/quickshell/shell.qml
  </read_first>
  <action>
    1. Static gates (must pass):
       - shell.qml retains silent-reload pragma QS_NO_RELOAD_POPUP=1 (D-10)
       - ReloadPopup instance may remain in shell.qml but must stay suppressed (do not enable feedback)
       - Bar.qml still has no reload function on IpcHandler (D-07)

    2. Run `python3 scripts/phase04-ipc-reload-assert.py` (includes soft-reload probe + same-PID + post-reload `qs ipc call bar open`). Expect exit 0.

    3. Manually corroborate soft path once for SUMMARY evidence (scripted is primary):
       - Capture PID from `qs list`
       - Content-change append a unique comment line to the watched shell.qml path used by the instance; wait for reload; assert same PID; restore file; `qs ipc call bar open` exit 0
       - Never call a non-existent reload CLI subcommand; never kill/relaunch as the happy path

    4. Extend `04-UAT.md` with section **IPC-03 — Soft reload**:
       - Trigger: content-change on watched QML (document exact steps); note 0.3.0 has no reload CLI
       - Expected: no reload popup/banner; same PID; bar modules update (clock/resources); tray interactive on **Quickshell** bar; IPC still works
       - Note: hide state need not survive reload; may run `qs ipc call bar open` after reload
       - Failed reload recovery (D-12): fix QML + manual `qs` relaunch; no auto-relaunch
       - Pass/fail rows for silent reload, bar usable, tray usable, IPC after reload (pending human)

    5. Update VALIDATION.md IPC-03 automated rows (04-RLD-*) to green when assert passed. Leave manual tray/bar rows pending. Do **not** set `nyquist_compliant: true` until human UAT for D-11 also done (or set true only for automated subset and note manual remaining — prefer leave false until task 04-03-02 completes if both required for phase gate).

    6. **If automated red:** investigate logs; minimal fix only (e.g. restore accidental pragma removal). Do not invent reload IPC or CLI wrappers. Re-run assert.
  </action>
  <acceptance_criteria>
    - `rg -n 'QS_NO_RELOAD_POPUP=1' .config/quickshell/shell.qml` matches at least once
    - `python3 scripts/phase04-ipc-reload-assert.py` exits 0 (same-PID soft-reload section included)
    - 04-UAT.md contains IPC-03 section documenting content-change soft reload and D-12 manual recovery
    - 04-UAT.md does not instruct operators to use a non-existent reload CLI as the primary path
    - VALIDATION.md IPC-03 automated soft-reload / post-reload IPC rows updated from this run
    - No new IpcHandler target for reload appears in Bar.qml
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'QS_NO_RELOAD_POPUP=1' .config/quickshell/shell.qml &amp;&amp; python3 scripts/phase04-ipc-reload-assert.py &amp;&amp; rg -n 'IPC-03|soft reload|same PID|tray' .planning/phases/04-ipc-keybinds-integration/04-UAT.md | head -40</automated>
  </verify>
  <done>Automated soft-reload + silent pragma + post-reload IPC green; UAT IPC-03 section written for human tray/bar checks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" id="04-03-02">
  <name>Task 2: Human UAT post-reload bar modules + tray + silent UX</name>
  <files>.planning/phases/04-ipc-keybinds-integration/04-UAT.md, .planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md</files>
  <read_first>
    - .planning/phases/04-ipc-keybinds-integration/04-UAT.md
    - .planning/phases/04-ipc-keybinds-integration/04-UI-SPEC.md (Reload UX checklist)
  </read_first>
  <action>
    Pause for human UAT of silent soft reload, post-reload bar modules, and QS tray interactivity (per D-10, D-11, D-12, D-13). Do not enable ReloadPopup or invent reload IPC. After resume, record IPC-03 rows in 04-UAT.md and update VALIDATION nyquist frontmatter only if automated gates are green and human D-11 criteria pass.
  </action>
  <what-built>
    Stock Quickshell soft reload (file-watch content change) with silent pragma. Automated same-PID and post-reload IPC proof completed. No ReloadPopup enablement. No auto-relaunch.
  </what-built>
  <how-to-verify>
    1. Trigger soft reload per 04-UAT.md (content-change probe on watched QML; restore after). Confirm **no** top-of-screen reload popup/banner.
    2. Confirm process still the same long-lived Quickshell PID (not a full kill+relaunch).
    3. Confirm bar modules usable: clock ticks; CPU/RAM/resources update when load changes.
    4. Confirm system tray on the **Quickshell** bar: icons present and clickable (open a tray menu). Dual Waybar tray noise is OK; failure only if QS tray unusable after settle.
    5. When feasible, note tray with zero / one / many icons remains layout-usable (backstop; not a hard fail if apps unavailable).
    6. Run `qs ipc call bar close` then `open` after reload — still works.
    7. Mark IPC-03 rows in 04-UAT.md. If tray/bar red, report for D-13 minimal fix (TrayService/SysTray only as needed) — do not enable ReloadPopup.

    After human approval, executor may set VALIDATION frontmatter `nyquist_compliant: true` only if all automated gates are green and manual IPC-03 D-11 rows are recorded pass (or leave false with explicit remaining manual note if user rejects).
  </how-to-verify>
  <resume-signal>Type "approved" if silent soft reload + bar + tray pass, or describe failures</resume-signal>
  <acceptance_criteria>
    - Human confirms no reload popup on soft reload
    - Human confirms bar modules usable after reload
    - Human confirms QS tray interactive after reload (or documents no tray apps + empty tray acceptable)
    - Human confirms IPC still works after reload
    - 04-UAT.md IPC-03 rows updated with pass/fail
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'IPC-03|popup|tray|pass|fail' .planning/phases/04-ipc-keybinds-integration/04-UAT.md | head -40</automated>
    <human-check>Silent soft reload; bar+tray usable; IPC after reload</human-check>
  </verify>
  <done>Human-approved IPC-03 graceful reload criteria; UAT recorded; nyquist updated only if fully earned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Watched QML files → soft reload | User-owned config tree executes as shell code on content change |
| Post-reload SNI / tray | D-Bus StatusNotifier rebind in user session |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | Unexpected IPC after reload | high | mitigate | No new targets; re-assert stock bar IPC post-reload |
| T-04-02 | Elevation of privilege | Cross-user IPC | medium | mitigate | Socket remains under `$XDG_RUNTIME_DIR` |
| T-04-03 | Availability | Auto-relaunch after failed reload | medium | mitigate | D-12 manual only; plan documents no daemon |
| T-04-06 | Tampering | Malicious QML on reload | medium | accept | Config is trusted user code; no remote QML load |
| T-04-SC | Tampering | package installs | high | accept | No installs |
</threat_model>

<verification>
- phase04 assert green including soft-reload section
- silent pragma retained
- human UAT bar+tray post-reload
- no reload IPC / no invented reload CLI
</verification>

<success_criteria>
- IPC-03: graceful soft reload proven (same PID, silent, bar+tray usable, IPC survives)
- D-12 recovery documented
- No Hyprland or exec-once scope creep
</success_criteria>

<output>
Create `.planning/phases/04-ipc-keybinds-integration/04-03-SUMMARY.md` when done
</output>
