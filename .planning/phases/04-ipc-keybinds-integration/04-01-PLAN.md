---
phase: 04-ipc-keybinds-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase04-ipc-reload-assert.py
  - .planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md
autonomous: true
requirements:
  - IPC-01
  - IPC-03
must_haves:
  truths:
    - Wave 0 assert script exists and runs with Python 3 stdlib only (subprocess + pathlib + sys)
    - Static gates cover IpcHandler target bar (toggle/open/close), QS_NO_RELOAD_POPUP=1, and GlobalStates.barOpen
    - Live gates require a running qs instance (default config shell.qml), assert target bar via qs ipc show, and qs ipc call bar open exit 0
    - Soft-reload probe uses content-change (not mtime-only), restores the probed file, and asserts same PID plus post-reload bar open
    - VALIDATION.md Wave 0 checkbox and per-task map use concrete 04-0N task IDs (not TBD / 04-0x)
  artifacts:
    - path: scripts/phase04-ipc-reload-assert.py
      provides: "Static + live IPC-01 and soft-reload IPC-03 assert harness"
      min_lines: 80
    - path: .planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md
      provides: "Wave 0 wired with concrete plan/task IDs"
  key_links:
    - from: scripts/phase04-ipc-reload-assert.py
      to: .config/quickshell/modules/ii/bar/Bar.qml
      via: "static rg/read of IpcHandler target bar"
      pattern: "target: \"bar\""
    - from: scripts/phase04-ipc-reload-assert.py
      to: qs CLI (running instance)
      via: "subprocess qs list / qs ipc show / qs ipc call bar open"
      pattern: "qs ipc call bar"
  prohibitions:
    - Do not add new IpcHandler targets
    - Do not edit hyprland.conf (no keybind, no exec-once)
    - Do not invent a reload CLI subcommand that does not exist on Quickshell 0.3.0
    - Do not enable ReloadPopup / remove QS_NO_RELOAD_POPUP silence
    - Do not add auto-relaunch daemon or hard-restart keybind
    - Do not modify product QML in this plan (assert-only; expected green if stock works)
    - Do not install npm/pip/cargo packages or new test frameworks
---

<objective>
Create Wave 0 Nyquist harness for Phase 4: `scripts/phase04-ipc-reload-assert.py` plus VALIDATION.md wiring so later IPC-01/IPC-03 plans prove stock bar IPC and soft reload without ad-hoc commands.

Purpose: Phase 4 is verify/UAT of stock surfaces (D-13). A single fail-loud script encodes static source gates + live `qs ipc` + optional same-PID soft-reload probe (per D-04..D-07, D-10, D-11 research correction: no inventing a missing reload CLI).
Output: phase04-ipc-reload-assert.py + updated 04-VALIDATION.md task map / Wave 0 checkboxes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ipc-keybinds-integration/04-CONTEXT.md
@.planning/phases/04-ipc-keybinds-integration/04-RESEARCH.md
@.planning/phases/04-ipc-keybinds-integration/04-PATTERNS.md
@.planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md
@scripts/phase02-config-assert.py
@scripts/phase03-config-assert.py
@.config/quickshell/modules/ii/bar/Bar.qml
@.config/quickshell/shell.qml
@.config/quickshell/GlobalStates.qml
</context>

## Artifacts this phase produces (this plan)

| Symbol / artifact | Kind | Path |
|-------------------|------|------|
| `phase04-ipc-reload-assert.py` | script | `scripts/phase04-ipc-reload-assert.py` |
| `main()` exit codes 0/1 | API | assert script |
| Wave 0 task map rows | docs | `04-VALIDATION.md` |

## Decisions locked in this plan

- D-04 / D-05 / D-06: stock `bar` IPC only (`toggle`/`open`/`close`); bare `qs ipc` (default config, no `-c ii`)
- D-07: no custom reload IPC; soft reload = file-watch content change (research correction — do not invent missing CLI)
- D-10: assert `QS_NO_RELOAD_POPUP=1` present in shell.qml
- D-13: this plan is assert-only; no product QML edits

## Assumptions (specless probe)

- **IPC-01 concurrency:** Stock `qs ipc call` is sequential CLI — one call completes before the next; no parallel IPC guarantee required beyond exit-0 sequential open/close/toggle in later plans
- **IPC-03:** Soft reload same-PID + bar usable after; GlobalStates.barOpen survival across reload is **not** required (research A1)
- Soft-reload probe section may be skipped with clear stderr if instance log path cannot be resolved, but static + live IPC sections are mandatory
- EDGE_ABSENT=1 / PROHIB_ABSENT=1 — no SPEC.md; edges handled via assumptions above

<tasks>

<task type="auto" id="04-01-01">
  <name>Task 1: Create phase04 IPC + soft-reload assert script</name>
  <files>scripts/phase04-ipc-reload-assert.py</files>
  <read_first>
    - scripts/phase02-config-assert.py
    - scripts/phase03-config-assert.py
    - .planning/phases/04-ipc-keybinds-integration/04-RESEARCH.md (Pattern 4 CLI, Pattern 5 soft reload, Pitfalls 1–6)
    - .planning/phases/04-ipc-keybinds-integration/04-PATTERNS.md (Wave 0 harness section)
    - .planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md (prescriptive suite)
    - .config/quickshell/modules/ii/bar/Bar.qml
    - .config/quickshell/shell.qml
    - .config/quickshell/GlobalStates.qml
  </read_first>
  <action>
    Create `scripts/phase04-ipc-reload-assert.py` as standalone Python 3 (stdlib only: pathlib, sys, subprocess, re, time, shutil, tempfile as needed). Clone control-flow from phase02/phase03 asserts: shebang, phase docstring, main() -&gt; int, preflight errors to stderr exit 1, AssertionError path prints `ipc/reload assert FAIL: …` exit 1, success prints exactly `ipc/reload asserts OK` exit 0, `if __name__ == "__main__": sys.exit(main())`.

    Resolve repo root as parent of `scripts/` (script lives at repo `scripts/phase04-ipc-reload-assert.py`). Use repo-relative paths for static files under `.config/quickshell/`.

    **Section A — Static (always run):**
    1. Bar.qml contains IpcHandler target bar and typed void functions toggle, open, close (per D-04/D-05). Fail if any missing.
    2. shell.qml contains the silent-reload env pragma value QS_NO_RELOAD_POPUP=1 (per D-10).
    3. GlobalStates.qml declares property bool barOpen (per D-08).
    4. Prefer reading file text in Python (or subprocess rg). Do not require hyprland.conf changes; do not assert presence of qs exec-once.

    **Section B — Live (requires running shell):**
    1. Preflight: `qs` on PATH; else stderr + exit 1.
    2. Run `qs list`. Require at least one instance whose config path contains `quickshell/shell.qml` (default config — not named `-c ii`). Capture Process ID for later.
    3. Run `qs ipc show`. Require output exposes target bar with functions toggle, open, close (per D-05/D-06).
    4. Run `qs ipc call bar open` (idempotent). Require exit code 0.
    5. Instance selection: use bare `qs ipc …` when a single default instance is present; if multiple, pin with `-i`/`--pid` from `qs list`. Never pick raw first socket under `$XDG_RUNTIME_DIR/quickshell/by-id` without `qs list` (stale dirs).

    **Section C — Soft-reload probe (include; fail-loud on broken restore):**
    1. Capture PID from Section B.
    2. Content-change only: append a unique one-line comment probe to the live watched file that backs the running config (typically `Path.home() / ".config/quickshell/shell.qml"` if that is the instance path from `qs list`; if symlink, still append via that path). mtime-only touch is forbidden (research pitfall).
    3. Wait briefly (poll up to ~5s) for soft reload success signals if an instance log is discoverable under `$XDG_RUNTIME_DIR/quickshell/`; if log not found, still proceed to PID + IPC checks after a short sleep.
    4. Assert same PID still running (graceful soft reload, not kill+relaunch).
    5. Restore the file exactly (remove the probe line / restore prior content). Use try/finally so restore always runs.
    6. After restore settle, `qs ipc call bar open` exit 0 again.
    7. Do **not** invoke a non-existent reload CLI subcommand. Do **not** add a reload IpcHandler (D-07).

    chmod +x. Do not edit QML product files, hyprland.conf, or install packages. Expected **green** if stock already works (unlike phase 02/03 intentional red).
  </action>
  <acceptance_criteria>
    - File `scripts/phase04-ipc-reload-assert.py` exists and starts with `#!/usr/bin/env python3`
    - Script imports only Python stdlib (no third-party packages)
    - Source mentions `target` bar surface and functions toggle/open/close in assert logic
    - Source checks for silent-reload pragma QS_NO_RELOAD_POPUP
    - Source checks GlobalStates barOpen property
    - Source invokes live `qs list`, `qs ipc show`, and `qs ipc call bar open` via subprocess
    - Source implements content-append probe + restore (try/finally or equivalent) and same-PID check
    - Source success line is exactly `ipc/reload asserts OK`
    - `python3 -m py_compile scripts/phase04-ipc-reload-assert.py` succeeds
    - With a running default qs instance, `python3 scripts/phase04-ipc-reload-assert.py` exits 0; without qs/instance, exits non-zero with stderr
  </acceptance_criteria>
  <verify>
    <automated>test -f scripts/phase04-ipc-reload-assert.py &amp;&amp; python3 -m py_compile scripts/phase04-ipc-reload-assert.py &amp;&amp; (python3 scripts/phase04-ipc-reload-assert.py; echo EXIT:$?)</automated>
  </verify>
  <done>Wave 0 assert script exists, compiles, encodes static IPC/reload gates + live qs ipc + soft-reload same-PID probe, fail-loud exit codes.</done>
</task>

<task type="auto" id="04-01-02">
  <name>Task 2: Wire Wave 0 into VALIDATION.md task map</name>
  <files>.planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md</files>
  <read_first>
    - .planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md
    - scripts/phase04-ipc-reload-assert.py
  </read_first>
  <action>
    Update `04-VALIDATION.md` only:

    1. Mark Wave 0 requirement for the phase04 assert script complete; document command `python3 scripts/phase04-ipc-reload-assert.py` (and note bash equivalent only if a .sh wrapper is not used — this plan ships .py).
    2. Replace placeholder task IDs (`04-W0-01`, `04-0x`, TBD) in Per-Task Verification Map with concrete IDs:
       - 04-01-01 / 04-01-02 → Wave 0 harness + VALIDATION wire
       - 04-02-* → IPC-01 live verify + UAT
       - 04-03-* → IPC-03 soft-reload verify + tray UAT
       - 04-04-* → deferred FWK-02 / IPC-02 packaging (no product tests)
    3. Set frontmatter `wave_0_complete: true` when script path is documented. Leave `nyquist_compliant: false` and `status: draft` until later plans green + UAT.
    4. Keep Manual-Only Verifications and deferred rows; do not invent unit-test framework; do not claim manual UAT green.
  </action>
  <acceptance_criteria>
    - VALIDATION.md Wave 0 checkbox for phase04-ipc-reload-assert is marked done
    - Documented command includes `python3 scripts/phase04-ipc-reload-assert.py`
    - No `04-0x` or `TBD` remains in Task ID / Plan columns of Per-Task Verification Map for in-scope rows
    - Frontmatter has `wave_0_complete: true`
    - Frontmatter still has `nyquist_compliant: false`
    - Deferred IPC-02 / FWK-02 row remains marked deferred (not claimed green)
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'phase04-ipc-reload-assert|wave_0_complete|nyquist_compliant|04-0[1-4]' .planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md | head -50</automated>
  </verify>
  <done>VALIDATION.md Wave 0 is wired to concrete plan/task IDs; assert path documented; nyquist still false until later gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External CLI → QS IPC socket | Untrusted local processes in the same user session can call stock IPC if they can reach `$XDG_RUNTIME_DIR` socket |
| Assert script → filesystem | Script reads QML sources and may briefly append/restore a probe comment on shell.qml |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | Unexpected IPC target abuse | high | mitigate | Assert only stock target `bar`; D-04 — no new targets added this plan |
| T-04-02 | Elevation of privilege | Cross-user IPC | medium | mitigate | Socket remains under `$XDG_RUNTIME_DIR`; script never relocates socket to `/tmp` |
| T-04-03 | Availability | Auto-relaunch after crash | medium | accept | Explicitly out of scope (D-12); harness does not install relaunch daemon |
| T-04-04 | Tampering | Soft-reload probe leaves dirty shell.qml | medium | mitigate | try/finally restore of probe content before exit |
| T-04-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this phase |
</threat_model>

<verification>
- `python3 -m py_compile scripts/phase04-ipc-reload-assert.py`
- With running qs: assert exits 0 and prints `ipc/reload asserts OK`
- VALIDATION.md references phase04 assert and concrete 04-0N task IDs
</verification>

<success_criteria>
- Wave 0 harness exists and is runnable
- Static + live IPC-01 and soft-reload IPC-03 predicates encoded
- Later plans depend on this script for automated green checks
</success_criteria>

<output>
Create `.planning/phases/04-ipc-keybinds-integration/04-01-SUMMARY.md` when done
</output>
