---
phase: 04-ipc-keybinds-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - .planning/phases/04-ipc-keybinds-integration/04-UAT.md
  - .planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md
autonomous: false
requirements:
  - IPC-01
must_haves:
  truths:
    - "qs ipc call bar close hides the Quickshell bar on all active bar monitors; open restores it on all (D-08)"
    - Stock IpcHandler target bar exposes toggle, open, close; live qs ipc call open/close/toggle each exit 0 (D-04, D-05, D-06)
    - No new IPC targets, toasts, or on-screen IPC chrome ship in this pass
    - Exact operator CLI for bar control is documented for UAT/dev (default config, bare qs)
    - statement: "After soft reload, tray may briefly rebind under dual Waybar+QS; not scored in this plan (IPC-03 owns tray)"
      verification: backstop
  artifacts:
    - path: .planning/phases/04-ipc-keybinds-integration/04-UAT.md
      provides: "IPC-01 human UAT checklist (multi-monitor hide/show/toggle)"
    - path: .planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md
      provides: "IPC-01 automated row statuses updated from live runs"
  key_links:
    - from: qs ipc call bar close|open|toggle
      to: GlobalStates.barOpen
      via: "IpcHandler in Bar.qml"
      pattern: "GlobalStates.barOpen"
    - from: GlobalStates.barOpen
      to: Bar LazyLoader active
      via: "active: GlobalStates.barOpen && !GlobalStates.screenLocked"
      pattern: "barOpen"
  prohibitions:
    - Do not add new IpcHandler targets or custom CLI wrappers (D-04)
    - Do not edit hyprland.conf (no keybind IPC-02, no exec-once FWK-02)
    - Do not invent a reload CLI or reload IPC method (D-07)
    - Do not enable ReloadPopup
    - Do not add auto-relaunch daemon
    - Do not pre-build QML fixes without red evidence (D-13)
---

<objective>
Prove stock IPC-01 bar show/hide: automated `qs ipc call bar {open,close,toggle}` exit 0 against the running default-config shell, document the exact CLI contract, and capture multi-monitor human UAT for all-monitor visibility (D-08).

Purpose: Locked decisions require verify-only productization of existing `Bar.qml` IpcHandler (D-13). Roadmap SC-1 (IPC show/hide) is in this pass; SC-2 keybind is not.
Output: 04-UAT.md IPC-01 section, VALIDATION IPC-01 rows updated, human multi-monitor confirmation; QML fix only if automated or UAT red.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ipc-keybinds-integration/04-CONTEXT.md
@.planning/phases/04-ipc-keybinds-integration/04-RESEARCH.md
@.planning/phases/04-ipc-keybinds-integration/04-PATTERNS.md
@.planning/phases/04-ipc-keybinds-integration/04-UI-SPEC.md
@.planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md
@scripts/phase04-ipc-reload-assert.py
@.config/quickshell/modules/ii/bar/Bar.qml
@.config/quickshell/GlobalStates.qml
</context>

## Artifacts this phase produces (this plan)

| Symbol / artifact | Kind | Path |
|-------------------|------|------|
| IPC-01 UAT checklist | docs | `04-UAT.md` |
| IPC-01 gate statuses | docs | `04-VALIDATION.md` |
| Conditional Bar.qml fix | QML (only if red) | `.config/quickshell/modules/ii/bar/Bar.qml` |

## Decisions locked in this plan

- D-04 / D-05 / D-06: stock bar IPC only; CLI `qs ipc call bar toggle|open|close`
- D-08: all monitors together via GlobalStates.barOpen
- D-09: leave GlobalShortcut names unbound in Hyprland
- D-13: fix QML only if verification finds show/hide broken

## Assumptions (specless probe)

- **IPC-01 concurrency:** Sequential CLI calls only; no parallel multi-client contract
- Dual Waybar may remain visible; UAT judges **Quickshell** bar only
- REQUIREMENTS text mentioning reload on the socket is satisfied by show/hide IPC here + separate IPC-03 soft-reload path (D-07), not a reload IPC method

<tasks>

<task type="auto" id="04-02-01">
  <name>Task 1: Automated IPC-01 proof + CLI contract in 04-UAT.md</name>
  <files>.planning/phases/04-ipc-keybinds-integration/04-UAT.md, .planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md</files>
  <read_first>
    - scripts/phase04-ipc-reload-assert.py
    - .planning/phases/04-ipc-keybinds-integration/04-VALIDATION.md
    - .planning/phases/04-ipc-keybinds-integration/04-UI-SPEC.md (Visibility Contract)
    - .planning/phases/04-ipc-keybinds-integration/04-RESEARCH.md (Pattern 4 prescriptive CLI)
    - .config/quickshell/modules/ii/bar/Bar.qml (IpcHandler block)
  </read_first>
  <action>
    1. Ensure a Quickshell instance is running with default config (`qs list` shows path containing `quickshell/shell.qml`). If missing, start with `qs -d` (or `qs &amp;`) and re-check — do not use `-c ii`.

    2. Run automated suite:
       - `python3 scripts/phase04-ipc-reload-assert.py` → expect exit 0 and `ipc/reload asserts OK`
       - Static: confirm Bar.qml still has only stock target bar handlers (no extra targets added this pass)
       - Live sequence (each exit 0): `qs ipc call bar close`; `qs ipc call bar open`; `qs ipc call bar toggle` (twice is fine for flip proof later in UAT)

    3. Create `.planning/phases/04-ipc-keybinds-integration/04-UAT.md` with:
       - Title Phase 4 UAT
       - Section **IPC-01 — Bar show/hide** documenting exact CLI (prescriptive):
         - Preflight: `qs list`, `qs ipc show`
         - `qs ipc call bar close` — bars should disappear on all monitors
         - `qs ipc call bar open` — bars should reappear on all monitors with modules updating
         - `qs ipc call bar toggle` twice — flips then restores
         - Note: default config, bare `qs` (no `-c ii`)
         - Note: judge Quickshell bar only if Waybar also visible
       - Pass/fail table rows for open/close/toggle (status pending until human checkpoint)
       - Explicit non-goals: no Hyprland keybind, no exec-once, no new IPC targets

    4. Update 04-VALIDATION.md IPC-01 automated rows (04-IPC-* / 04-02-*) to green if commands passed; leave manual multi-monitor rows pending.

    5. **If automated red only:** minimal fix in Bar.qml IpcHandler / LazyLoader visibility per D-13 (restore stock toggle/open/close setting GlobalStates.barOpen). Re-run suite. Do not add targets, wrappers, or Hyprland edits. If green, do not edit QML.
  </action>
  <acceptance_criteria>
    - `python3 scripts/phase04-ipc-reload-assert.py` exits 0
    - `qs ipc call bar open`, `qs ipc call bar close`, and `qs ipc call bar toggle` each exit 0 on the running instance
    - File `04-UAT.md` exists and documents the three bar IPC call forms and multi-monitor expectation
    - 04-UAT.md states default config / bare qs (not named config `-c ii`)
    - VALIDATION.md IPC-01 live smoke rows reflect automated results (not still blank placeholders for 04-02)
    - `rg -n 'IpcHandler' .config/quickshell/modules/ii/bar/Bar.qml` still shows a single bar target block (no new targets introduced by this plan)
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/phase04-ipc-reload-assert.py &amp;&amp; qs ipc call bar close &amp;&amp; qs ipc call bar open &amp;&amp; qs ipc call bar toggle &amp;&amp; test -f .planning/phases/04-ipc-keybinds-integration/04-UAT.md &amp;&amp; rg -n 'ipc call bar (open|close|toggle)|IPC-01' .planning/phases/04-ipc-keybinds-integration/04-UAT.md | head -30</automated>
  </verify>
  <done>Automated IPC-01 green; CLI contract documented in 04-UAT.md; product QML untouched unless red evidence forced a minimal fix.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" id="04-02-02">
  <name>Task 2: Human UAT multi-monitor bar hide/show/toggle</name>
  <files>.planning/phases/04-ipc-keybinds-integration/04-UAT.md</files>
  <read_first>
    - .planning/phases/04-ipc-keybinds-integration/04-UAT.md
    - .planning/phases/04-ipc-keybinds-integration/04-UI-SPEC.md (Visibility Contract)
  </read_first>
  <action>
    Pause for human multi-monitor UAT of stock bar IPC (per D-06, D-08, D-13). Do not change QML unless the human reports visual failure after automated green. After resume, update 04-UAT.md IPC-01 pass/fail rows from the human signal.
  </action>
  <what-built>
    Stock Quickshell IPC for bar visibility (IpcHandler target bar → GlobalStates.barOpen → LazyLoader on every monitor). Automated CLI exit-0 proof completed in task 04-02-01. No new UI chrome.
  </what-built>
  <how-to-verify>
    On the live Hyprland session with Quickshell running (dual monitors DP-1 + HDMI-A-2 if available):

    1. Run `qs ipc call bar close`. Confirm the **Quickshell** bar disappears on **all** monitors (not per-monitor). Waybar may remain — ignore Waybar.
    2. Run `qs ipc call bar open`. Confirm QS bars reappear on all monitors with prior module layout (workspaces, clock, tray, network, resources, volume).
    3. Run `qs ipc call bar toggle` twice. Confirm one hide and one show flip sequence.
    4. Confirm no new toast/badge/status line appeared for IPC calls.
    5. Mark IPC-01 rows in 04-UAT.md pass or fail with brief notes.

    If visual hide/show fails while CLI exits 0, report failure (executor may apply D-13 minimal QML fix and re-request verify).
  </how-to-verify>
  <resume-signal>Type "approved" if multi-monitor hide/show/toggle pass, or describe which monitor/action failed</resume-signal>
  <acceptance_criteria>
    - Human confirms bars hide on all active QS bar monitors on close
    - Human confirms bars show on all monitors on open
    - Human confirms toggle flips visibility
    - 04-UAT.md IPC-01 rows updated with pass/fail from this checkpoint
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'IPC-01|pass|fail|close|open|toggle' .planning/phases/04-ipc-keybinds-integration/04-UAT.md | head -40</automated>
    <human-check>Multi-monitor QS bar hide/show/toggle matches UI-SPEC Visibility Contract</human-check>
  </verify>
  <done>Human-approved IPC-01 multi-monitor visibility; UAT rows recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator CLI → IPC socket | Local user invokes `qs ipc call bar …` against session socket |
| barOpen → all monitors | Single boolean affects every bar window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | Unexpected IPC target abuse | high | mitigate | Keep stock bar target only (D-04); no new handlers |
| T-04-02 | Elevation of privilege | Cross-user IPC | medium | mitigate | Socket stays in `$XDG_RUNTIME_DIR`; no `/tmp` socket |
| T-04-05 | Information disclosure | IPC surface enumeration | low | accept | `qs ipc show` is local-user only; bar handlers take no args |
| T-04-SC | Tampering | package installs | high | accept | No installs |
</threat_model>

<verification>
- phase04 assert green
- live open/close/toggle exit 0
- human multi-monitor UAT approved
- no hyprland.conf diff for this plan
</verification>

<success_criteria>
- IPC-01 observable: external CLI show/hide works on all monitors
- CLI contract documented
- No scope creep into IPC-02/FWK-02
</success_criteria>

<output>
Create `.planning/phases/04-ipc-keybinds-integration/04-02-SUMMARY.md` when done
</output>
