---
phase: 23-one-command-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/phase23-bootstrap-assert.sh
  - bootstrap.sh
  - arch/dots-hyprland.sh
autonomous: true
requirements: [BOOT-01]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "./bootstrap.sh exists at repository root with executable permissions (0755), asserts non-root execution (EUID != 0 per D-06, supporting DOTFILES_MOCK_EUID override for testing), and asserts Arch Linux platform (/etc/arch-release)"
    - "./bootstrap.sh implements closed CLI flag parser (--dry-run, --from <step>, --only <step>, --reset, --snapshot, --no-pause, -h/--help), rejecting any unknown option with exit code 2 (D-04)"
    - "arch/dots-hyprland.sh registers bootstrap in ALLOWLIST and dispatches via exec \"$REPO_ROOT/bootstrap.sh\" \"$@\" (D-01), preserving PAIR_COUNT == 18 in arch/*.sh"
    - "Dual-stream logging streams live output to console while tee-ing to $XDG_STATE_HOME/dotfiles/logs/bootstrap-<timestamp>.log using duplicated file descriptors (exec 3>&1 4>&2) and retains the 5 most recent logs (D-07)"
    - "$XDG_STATE_HOME/dotfiles/bootstrap-state persists state machine progress in JSON format (D-02), tracking schema_version, timestamps, stage, current_step, and per-step statuses (pending, running, complete, failed)"
    - "Resumability after simulated failure and idempotence on re-run: failed step records status failed with last_error and prints exact resume command (./bootstrap.sh --from <step>) (D-05); --from resumes accurately; re-running on completed state is idempotent (BOOT-01)"
    - "scripts/phase23-bootstrap-assert.sh scaffolds Section 1 (CLI flags, root check, wrapper delegation) and Section 3 (state engine resumability, failure traps, and idempotence) and passes cleanly"
  artifacts:
    - scripts/phase23-bootstrap-assert.sh
    - bootstrap.sh
    - arch/dots-hyprland.sh
  key_links:
    - "arch/dots-hyprland.sh bootstrap executes ./bootstrap.sh via exec without spawning child wrappers or adding stow call sites under arch/"
    - "$XDG_STATE_HOME/dotfiles/bootstrap-state persists execution progress across shell restarts and relogins without polluting git tracking"
  prohibitions:
    - "Must not allow running as root EUID == 0 (D-06)"
    - "Must not create new scripts under arch/ (must preserve PAIR_COUNT == 18)"
    - "Must not use eval or unquoted string concatenation for command execution"
    - "Must not delete conflicting regular files without backing them up to ~/.dotfiles-backup.<epoch>/ with MANIFEST.txt (D-16)"
    - "Must not modify or delete any path listed in guard-paths.tsv (D-17)"
    - "Must not modify package snapshots during standard bootstrap (must require --snapshot flag) (D-21)"
---

<objective>
Scaffold the Phase 23 assertion harness (`scripts/phase23-bootstrap-assert.sh`) with Section 1 and Section 3, create the root orchestrator entry point (`./bootstrap.sh`), wire the wrapper delegation in `arch/dots-hyprland.sh bootstrap` (preserving `PAIR_COUNT == 18`), implement the closed CLI parser and dual-stream logging engine, and build the resumable JSON state machine (`$XDG_STATE_HOME/dotfiles/bootstrap-state`) supporting `--from`, `--only`, `--reset`, and failure recovery.

Purpose: Deliver the foundational CLI contract, logging, and state persistence engine guaranteeing that dotfiles bootstrap can be interrupted at any step, cleanly resumed, and run idempotently without working-tree corruption.
Output: Scaffolding script `scripts/phase23-bootstrap-assert.sh` with passing Sections 1 and 3, root orchestrator `./bootstrap.sh`, and updated `arch/dots-hyprland.sh`.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-one-command-bootstrap/23-CONTEXT.md
@.planning/phases/23-one-command-bootstrap/23-RESEARCH.md
@.planning/phases/23-one-command-bootstrap/23-PATTERNS.md
@.planning/phases/23-one-command-bootstrap/23-VALIDATION.md
@scripts/phase22-kde-and-gtk-capture-assert.sh
@scripts/phase19-link-aware-verify-assert.sh
@scripts/phase17-unblock-assert.sh
@arch/dots-hyprland.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end tracer — assert harness scaffolding (Section 1), root orchestrator CLI parser, non-root guard, dual logging, and wrapper delegation</name>
  <files>scripts/phase23-bootstrap-assert.sh, bootstrap.sh, arch/dots-hyprland.sh</files>
  <read_first>
    - .planning/phases/23-one-command-bootstrap/23-CONTEXT.md (D-01, D-04, D-06, D-07, D-23)
    - .planning/phases/23-one-command-bootstrap/23-RESEARCH.md (lines 8-71, 301-346, 528-530, 548-552)
    - .planning/phases/23-one-command-bootstrap/23-PATTERNS.md (lines 32-177, 550-643, 800-843)
    - scripts/phase22-kde-and-gtk-capture-assert.sh (lines 1-85: trap cleanup, CLI parsing, test section structure)
    - scripts/phase17-unblock-assert.sh (lines 85-96: PAIR_COUNT invariant assertion)
    - arch/dots-hyprland.sh (lines 10-55, 1720-1745: allowlist and case dispatch routing)
  </read_first>
  <action>
    1. Scaffold `scripts/phase23-bootstrap-assert.sh` with permissions `0755` adhering to the repository assert harness contract (per D-23 and Pattern 2.1):
       - Shebang `#!/usr/bin/env bash`, `set -euo pipefail`.
       - Resolve `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"` and `cd "$REPO_ROOT"`.
       - Define status helpers: `pass()`, `fail()`, `finding()`, `info()`.
       - Maintain `FAIL=0` and `FINDINGS=0` counters.
       - Implement dynamic cleanup arrays `TMP_FILES=()` and `SCRATCH_ROOTS=()`.
       - Implement `cleanup()` trap registered on `EXIT`: wipes `TMP_FILES`, runs `chmod -R u+rwX` then `rm -rf` on `SCRATCH_ROOTS`.
       - Implement git working-tree porcelain bracket (`porcelain_snapshot`) before and after execution to assert zero repository mutation (Pattern 2.2).
       - Implement CLI argument parsing supporting `--section <N>` (where N is 1 to 5) or all sections if omitted, plus `-h|--help`.
       - Implement Section 1 ("CLI flags, unknown option rejection, root check, wrapper delegation" per Pattern 2.3):
         a. Test `./bootstrap.sh --help`: assert exit 0 and presence of `"Usage:"`.
         b. Test `./bootstrap.sh --bogus-flag`: assert exit code 2 and presence of `"Unknown bootstrap flag"`.
         c. Test root user rejection: invoke `./bootstrap.sh` in a subshell with `DOTFILES_MOCK_EUID=0 ./bootstrap.sh`: assert non-zero exit code 1 and error message naming non-root requirement (per D-06). Note: GNU Bash EUID is a readonly variable, so testing via `DOTFILES_MOCK_EUID=0` safely simulates root without triggering shell readonly errors.
         d. Test `arch/dots-hyprland.sh bootstrap --help`: assert exit 0 and output matching `./bootstrap.sh --help` (per D-01).
         e. Test `PAIR_COUNT` invariant in `arch/*.sh`: verify that `grep -ho -- '--verbose=5 --no-folding' arch/*.sh | wc -l` remains strictly 18 (scripts/phase17-unblock-assert.sh).
       - Add terminal summary line `=== done: FAIL=${FAIL} FINDINGS=${FINDINGS} ===` exiting 1 if `FAIL > 0`, else 0.

    2. Create `./bootstrap.sh` at repository root with permissions `0755`:
       - Shebang `#!/usr/bin/env bash`, `set -euo pipefail`.
       - Resolve physical repository root via `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"` and `cd "$REPO_ROOT"`.
       - Enforce non-root execution check (per D-06 and Pattern 1.1): support overridable UID checking via `CURRENT_EUID="${DOTFILES_MOCK_EUID:-${EUID:-$(id -u)}}"`. If `[[ "$CURRENT_EUID" -eq 0 ]]`, emit `"[FAIL] ./bootstrap.sh must NOT be run as root. Run as regular operator."` to stderr and exit 1.
       - Enforce Arch Linux platform gate (per Pattern 1.1): check `[[ ! -f /etc/arch-release ]]`; if true, emit `"[FAIL] /etc/arch-release not found. Arch Linux is required for bootstrap."` to stderr and exit 1.
       - Set up dual-stream transcript logging (per D-07 and Pattern 1.2):
         Define `XDG_STATE_HOME="${XDG_STATE_HOME:-$HOME/.local/state}"`, `LOG_DIR="$XDG_STATE_HOME/dotfiles/logs"`.
         Create `mkdir -p "$LOG_DIR"`. Clean up older logs keeping the 5 most recent `bootstrap-*.log` files.
         Duplicate file descriptors: `exec 3>&1 4>&2` and redirect to `tee -a "$LOG_FILE"`.
         Trap EXIT to restore descriptors (`exec 1>&3 2>&4 3>&- 4>&-`) and `wait 2>/dev/null || true` to prevent dropped log lines (Pitfall 3).
       - Implement closed CLI flag parsing (per D-04 and Pattern 1.3):
         Parse `--dry-run`, `--from <step>`, `--only <step>`, `--reset`, `--snapshot`, `--no-pause`, `-h|--help`.
         Collect any unknown arguments into `UNKNOWN_FLAGS=()`. If `UNKNOWN_FLAGS` is non-empty, print error and exit 2.
         Support `--help` displaying usage, options, and the 7 pipeline steps (`submodules`, `packages`, `installer`, `destub`, `stow`, `capture_seed`, `verify`).

    3. Update `arch/dots-hyprland.sh` to route the `bootstrap` subcommand (per D-01 and Pattern 1.4):
       - In line 17, add `bootstrap` to `ALLOWLIST`:
         `ALLOWLIST=(install install-deps install-setups install-files uninstall verify capture bootstrap)`
       - In `usage()` (lines 30-55), add `arch/dots-hyprland.sh bootstrap [flags…]` and document subcommand.
       - In `main()` case statement (lines 1726-1740), add case `bootstrap)` routing to `exec "$REPO_ROOT/bootstrap.sh" "$@"`.
       - Verify no new stow call sites or scripts are added under `arch/`, keeping `PAIR_COUNT == 18`.
  </action>
  <acceptance_criteria>
    - `test -x scripts/phase23-bootstrap-assert.sh` evaluates true.
    - `test -x bootstrap.sh` evaluates true.
    - `bash -n scripts/phase23-bootstrap-assert.sh` and `bash -n bootstrap.sh` and `bash -n arch/dots-hyprland.sh` all pass with 0 syntax errors.
    - `./bootstrap.sh --help` exits 0 with complete usage message.
    - `./bootstrap.sh --invalid-option` exits 2 with error on stderr.
    - `./arch/dots-hyprland.sh bootstrap --help` exits 0 with identical output.
    - `grep -ho -- '--verbose=5 --no-folding' arch/*.sh | wc -l` prints `18`.
    - Executing `./scripts/phase23-bootstrap-assert.sh --section 1` exits 0 with `FAIL=0 FINDINGS=0`.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase23-bootstrap-assert.sh --section 1</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0</fails_when>
  </verify>
  <done>Scaffolded assert harness with Section 1 passing, created root orchestrator ./bootstrap.sh with strict CLI flag parser and non-root gate, wired arch/dots-hyprland.sh delegation preserving PAIR_COUNT == 18.</done>
</task>

<task type="auto">
  <name>Task 2: Implement persistent JSON state engine, resumability traps, failure recovery, and Section 3 test suite</name>
  <files>bootstrap.sh, scripts/phase23-bootstrap-assert.sh</files>
  <read_first>
    - .planning/phases/23-one-command-bootstrap/23-CONTEXT.md (D-02, D-03, D-05)
    - .planning/phases/23-one-command-bootstrap/23-RESEARCH.md (lines 208-243, 301-346, 531)
    - .planning/phases/23-one-command-bootstrap/23-PATTERNS.md (lines 179-253, 678-695)
    - bootstrap.sh (from Task 1)
  </read_first>
  <action>
    1. Implement persistent JSON state machine engine in `./bootstrap.sh` (per D-02, D-03, D-05 and Pattern 1.4):
       - Path: `STATE_FILE="$XDG_STATE_HOME/dotfiles/bootstrap-state"`.
       - Implement `init_state()`:
         If `--reset` was passed and `STATE_FILE` exists, remove it.
         If `STATE_FILE` does not exist, create parent directory and initialize via `jq -n` writing schema version 1, ISO timestamps for `started_at` and `updated_at`, stage integer 1, `current_step: "submodules"`, a `steps` dictionary mapping each of the 7 steps (`submodules`, `packages`, `installer`, `destub`, `stow`, `capture_seed`, `verify`) to `{ "status": "pending", "timestamp": null }`, and `last_error: null` (per D-02 and Pattern 1.4).
         Write atomically via `$STATE_FILE.tmp.$$` and `mv`.
       - Implement `set_step_status(step, status, err)`:
         Atomically update `.updated_at`, `.current_step`, `.steps[$step].status`, `.steps[$step].timestamp`, and `.last_error` using `jq`.
       - Implement `get_step_status(step)`:
         Query `.steps[$step].status // "pending"` using `jq -r`.
       - Implement `get_stage()` and `set_stage(stage)` using `jq`.
       - Implement step execution wrapper `execute_step(step_name, step_command_func)`:
         Check `--from` and `--only` filters. If a step is skipped, log info.
         If step is already marked `complete` and not targeted by `--only`, log `"[SKIP] Step '$step_name' already completed."` and return 0 (idempotence per BOOT-01).
         Set status to `running`.
         Execute `step_command_func`.
         On success: set status to `complete`.
         On failure: capture exit code `$rc`, set status to `failed` and record error message in state file.
         Output prominent fail-closed instructions (per D-05): log `"[FAIL] Bootstrap failed during step '$step_name' (exit code $rc)."` followed by `"[INFO] To resume bootstrap from this step after addressing the issue, run: ./bootstrap.sh --from $step_name"`.
         Exit with the step's exact exit code `$rc`.

    2. Implement Section 3 in `scripts/phase23-bootstrap-assert.sh` ("State machine resumability, --from, --only, --reset, idempotence" per Pattern 2.5):
       - Create isolated scratch state environment:
         `S3_ROOT="$(mktemp -d /tmp/p23-assert-s3-XXXXXX)"`, append to `SCRATCH_ROOTS`.
         Export `XDG_STATE_HOME="$S3_ROOT/state"`.
       - Test 1: Verify state initialization creates valid JSON schema adhering to D-02.
       - Test 2: Simulate step failure (mock failing function). Verify state JSON records `status: "failed"` and `last_error` populated. Verify exit code equals failed step exit code.
       - Test 3: Test `--from <step>`: verify execution begins at specified step, skipping prior steps.
       - Test 4: Test `--only <step>`: verify execution runs strictly the named step and terminates.
       - Test 5: Test `--reset`: verify existing state file is removed and reinitialized.
       - Test 6: Test idempotence: execute pipeline in completed state without flags; assert exit 0 and zero redundant operations.
  </action>
  <acceptance_criteria>
    - `jq empty "$STATE_FILE"` succeeds on newly initialized state file.
    - Simulated step failure sets `.steps[$step].status == "failed"` and exits with non-zero step code.
    - Failure output explicitly prints `./bootstrap.sh --from <step>`.
    - `--from` and `--only` accurately gate execution sequence.
    - Executing `./scripts/phase23-bootstrap-assert.sh --section 3` exits 0 with `FAIL=0 FINDINGS=0`.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase23-bootstrap-assert.sh --section 3</automated>
    <fails_when>non-zero exit code, or output contains "[FAIL]" or "FAIL=N" where N > 0</fails_when>
  </verify>
  <done>JSON state machine engine implemented in ./bootstrap.sh with atomic updates, error trapping, resumability via --from/--only, and Section 3 tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator / Environment → `./bootstrap.sh` | Untrusted command-line arguments and environment variables passed to the orchestrator |
| `./bootstrap.sh` → File system (`$XDG_STATE_HOME`) | State persistence and log file writes on the local file system |
| Wrapper (`arch/dots-hyprland.sh`) → Root Orchestrator | Process replacement via `exec` preserving arguments and environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Elevation of Privilege | `./bootstrap.sh` | high | mitigate | Enforce `EUID != 0` check immediately at script startup; refuse root execution with exit 1; delegate privileged operations to on-demand `sudo` (per D-06) |
| T-23-02 | Tampering | `$XDG_STATE_HOME/dotfiles/bootstrap-state` | medium | mitigate | Perform all JSON updates atomically using temporary files (`$STATE_FILE.tmp.$$`) on the same filesystem and rename via `mv` (per D-02) |
| T-23-03 | Denial of Service | CLI Flag Parser | medium | mitigate | Reject any unrecognized or ambiguous CLI flags with exit 2 and usage instructions, preventing unexpected execution (per D-04) |
| T-23-04 | Information Disclosure | `$LOG_DIR/bootstrap-*.log` | low | mitigate | Store transcripts in `$XDG_STATE_HOME/dotfiles/logs` with user-only permissions, avoiding world-readable `/tmp` storage |
</threat_model>

<verification>
Automated test harness verification across Sections 1 and 3:
`./scripts/phase23-bootstrap-assert.sh --section 1`
`./scripts/phase23-bootstrap-assert.sh --section 3`
</verification>

<success_criteria>
1. `scripts/phase23-bootstrap-assert.sh` exists with trap cleanup, porcelain brackets, and passing Sections 1 and 3.
2. `./bootstrap.sh` exists at repo root with non-root guard, closed CLI parsing, logging, and JSON state persistence.
3. `arch/dots-hyprland.sh bootstrap` dispatches to `./bootstrap.sh` without altering `PAIR_COUNT == 18`.
4. State resumability and re-run idempotence proven against simulated failure.
</success_criteria>

<output>
Create `.planning/phases/23-one-command-bootstrap/23-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces
- Root orchestrator: `bootstrap.sh`
- Assert test harness: `scripts/phase23-bootstrap-assert.sh` (Sections 1 and 3)
- Modified wrapper: `arch/dots-hyprland.sh` (ALLOWLIST + case dispatch)
- Persistent state file: `$XDG_STATE_HOME/dotfiles/bootstrap-state`
- Transcript logs: `$XDG_STATE_HOME/dotfiles/logs/bootstrap-<timestamp>.log`
- CLI functions in `bootstrap.sh`: `usage()`, `init_state()`, `set_step_status()`, `get_step_status()`, `execute_step()`
