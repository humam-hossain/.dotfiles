---
phase: "29"
plan: "02"
type: "execute"
wave: 2
depends_on:
  - "29-01"
files_modified:
  - bootstrap.sh
  - scripts/phase29-theme-data-contracts-assert.sh
autonomous: true
requirements: [INTG-01, INTG-02, INTG-03]

must_haves:
  truths:
    - "Section 2 of scripts/phase29-theme-data-contracts-assert.sh executes switchwall.sh --noswitch live, polls kdeglobals until background updates complete, verifies strictly monotonic mtime advancement across all 5 dynamic themed outputs, and confirms byte-identical porcelain snapshot with zero git churn (D-14 Section 2, D-15, INTG-01)"
    - "bootstrap.sh Step 4 (destub) implements hierarchical prefix matching in is_guarded_path() and explicitly prunes legacy Catppuccin symlinks pointing to /usr/share/themes/Catppuccin* in GTK directories (D-02, D-11, INTG-03)"
    - "bootstrap.sh Step 5 (stow) pre-creates sensitive parent directories (.config/fuzzel, .config/kitty, .config/gtk-3.0, .config/gtk-4.0, .config/hypr/custom, .config/systemd/user) prior to running GNU Stow with --verbose=5 --no-folding (D-10, INTG-03)"
    - "bootstrap.sh Step 6 (capture_seed) sanitizes GTK 4 template pseudo-classes (:insensitive -> :disabled) and triggers initial theme generation via switchwall.sh --noswitch with fail-soft color fallback (switchwall.sh --color '#3f51b5') when wallpaper image is absent (D-12, D-13, INTG-03)"
    - "bootstrap.sh Step 7 (verify) enables dotfiles-capture.timer and executes arch/dots-hyprland.sh verify --strict bound 1:1 to exit status (INTG-03)"
    - "Section 4 of scripts/phase29-theme-data-contracts-assert.sh tests bootstrap de-stubbing, Catppuccin pruning, guard preservation, parent pre-creation, and fallback theming in an isolated scratch environment with FAIL=0 (D-14 Section 4, D-16, INTG-03)"
    - "Section 5 of scripts/phase29-theme-data-contracts-assert.sh runs phase25, phase26, phase27, and phase28 assert suites sequentially with FAIL=0 across all four milestones (D-14 Section 5, INTG-01..03)"
    - "Full assertion suite bash scripts/phase29-theme-data-contracts-assert.sh and arch/dots-hyprland.sh verify --strict pass with FAIL=0 FINDINGS=0 (INTG-01, INTG-02, INTG-03)"
  artifacts:
    - bootstrap.sh
    - scripts/phase29-theme-data-contracts-assert.sh
  key_links:
    - "bootstrap.sh is the root one-command installer orchestrator ensuring reproducible fresh-machine deployment without manual intervention"
    - "switchwall.sh coordinates theme propagation across Hyprland, Quickshell ii, GTK, Qt, and terminals without repository churn"
    - "scripts/phase29-theme-data-contracts-assert.sh gates milestone v0.5 completion with 5-section validation and multi-phase regression sweeps"
  prohibitions:
    - "Must not edit files under vendor/dots-hyprland/"
    - "Must not alter closed-phase assert scripts under scripts/"
    - "Must not allow bootstrap de-stubbing to delete guarded dynamic theme files"
    - "Must not leave temporary files in /tmp or scratch directories after test completion"
    - "Must not permit git status --porcelain drift across switchwall.sh or test execution"
---

<objective>
Implement Section 2 live zero git churn drill asserting monotonic mtime advancement across all 5 themed components with asynchronous polling for background KDE theming without working tree drift (D-14 Section 2, D-15, INTG-01), harden `bootstrap.sh` orchestrator with hierarchical prefix guard matching (D-02), explicit legacy Catppuccin symlink pruning in de-stubbing (D-11), sensitive parent directory pre-creation in stow (D-10), and initial theme generation with GTK 4 template sanitization and fail-soft color seed fallback (D-12, D-13, INTG-03), implement Section 4 scratch isolation drill (D-14 Section 4, D-16), implement Section 5 full v0.5 multi-phase regression sweep (D-14 Section 5), and execute the complete verification suite with `arch/dots-hyprland.sh verify --strict` achieving `FAIL=0 FINDINGS=0` (INTG-01, INTG-02, INTG-03).

Purpose: Complete the bootstrap integration and automated regression verification for Milestone v0.5, ensuring fresh-machine reproducibility and zero-churn theme operations.
Output: Hardened `bootstrap.sh`, complete 5-section `scripts/phase29-theme-data-contracts-assert.sh`, and passing full regression suite.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-CONTEXT.md
@.planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-RESEARCH.md
@.planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-PATTERNS.md
@.planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-VALIDATION.md
@bootstrap.sh
@arch/dots-hyprland.sh
@guard-paths.tsv
@scripts/phase25-gtk-material-you-assert.sh
@scripts/phase26-qt-kde-material-you-assert.sh
@scripts/phase27-accent-coordination-assert.sh
@scripts/phase28-terminal-fuzzel-assert.sh
@scripts/phase29-theme-data-contracts-assert.sh
</context>

<assumptions>
- In switchwall.sh, handle_kde_material_you_colors & executes in the background; polling kdeglobals mtime with a brief timeout (up to 3-5 seconds) prevents race conditions during Section 2 mtime and porcelain checks.
- On a fresh machine clone, the wallpaper image configured in config.json may not be downloaded or present; falling back to color seed #3f51b5 guarantees complete theme generation without failing bootstrap.
- GTK 4 CSS provider rejects :insensitive pseudo-class; sanitizing the template to :disabled prevents parser warnings and assert failures.
- Pre-creating .config/fuzzel and .config/kitty as physical directories before running GNU Stow prevents stow from creating directory symlinks (directory folding).
- Pruning legacy Catppuccin symlinks pointing to /usr/share/themes/Catppuccin* avoids permission errors when regular user Matugen writes to ~/.config/gtk-4.0/gtk.css.
- Running regression assert scripts sequentially in Section 5 produces zero working tree churn because all outputs are properly guarded in guard-paths.tsv and .gitignore.
</assumptions>

<tasks>

<task id="29-02-01" type="execute">
  <name>Implement Section 2 live zero git churn drill with switchwall.sh monotonic mtime verification and background polling</name>
  <read_first>
    scripts/phase28-terminal-fuzzel-assert.sh
    scripts/phase27-accent-coordination-assert.sh
    ~/.config/quickshell/ii/scripts/colors/switchwall.sh
    .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-PATTERNS.md
    .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-CONTEXT.md
  </read_first>
  <files>
    scripts/phase29-theme-data-contracts-assert.sh
  </files>
  <action>
    Implement Section 2 in `scripts/phase29-theme-data-contracts-assert.sh` per D-14 Section 2, D-15, and INTG-01:
    1. Locate live theme generation script `switchwall.sh` at `$XDG_CONFIG_HOME/quickshell/ii/scripts/colors/switchwall.sh` and assert executable permissions.
    2. Inspect mtime across all 5 dynamic themed components prior to reload:
       - Fuzzel INI: `$XDG_CONFIG_HOME/fuzzel/fuzzel_theme.ini`
       - Kitty theme: `$XDG_STATE_HOME/quickshell/user/generated/terminal/kitty-theme.conf`
       - GTK 3 CSS: `$XDG_CONFIG_HOME/gtk-3.0/gtk.css`
       - Hyprland colors: `$XDG_CONFIG_HOME/hypr/hyprland/colors.lua`
       - KDE globals: `$XDG_CONFIG_HOME/kdeglobals`
    3. Capture baseline git status porcelain snapshot into temporary file `$DRILL_BEFORE` using `porcelain_snapshot`.
    4. Sleep 1 second to guarantee timestamp resolution, then invoke `switchwall.sh --noswitch`.
    5. Assert exit code 0 from `switchwall.sh --noswitch`.
    6. Because `switchwall.sh` dispatches `handle_kde_material_you_colors &` as an asynchronous background job, implement polling for `kdeglobals` mtime: loop up to 15 iterations (sleeping 0.2s each, max 3 seconds) until `$(stat -c %Y "$F_KDE") -gt "$B_KDE"`.
    7. Query post-execution mtimes for all 5 components, asserting strictly monotonic advancement (`post_mtime > pre_mtime`) for each file.
    8. Capture post-execution git status porcelain snapshot into `$DRILL_AFTER`.
    9. Assert `cmp -s "$DRILL_BEFORE" "$DRILL_AFTER"` succeeds with zero git working tree churn (no modified, untracked, or deleted files).
    10. Run `bash scripts/phase29-theme-data-contracts-assert.sh --section 2` and assert FAIL=0.
  </action>
  <acceptance_criteria>
    bash scripts/phase29-theme-data-contracts-assert.sh --section 2 exits 0 with FAIL=0.
    porcelain diff between pre- and post-switchwall is empty.
    all 5 themed outputs advance mtime monotonically with race-free kdeglobals polling.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase29-theme-data-contracts-assert.sh --section 2</automated>
    <fails_when>non-zero exit code, "[FAIL]" in output, or git working tree churn detected</fails_when>
  </verify>
  <done>Section 2 implemented in scripts/phase29-theme-data-contracts-assert.sh executing switchwall.sh --noswitch, polling kdeglobals for asynchronous background completion, proving strictly monotonic mtime advancement across all 5 themed outputs, and verifying byte-identical porcelain snapshot with zero git churn.</done>
</task>

<task id="29-02-02" type="execute">
  <name>Harden bootstrap.sh orchestrator and implement Section 4 scratch drill</name>
  <read_first>
    bootstrap.sh
    scripts/phase23-bootstrap-assert.sh
    .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-PATTERNS.md
    .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-CONTEXT.md
  </read_first>
  <files>
    bootstrap.sh
    scripts/phase29-theme-data-contracts-assert.sh
  </files>
  <action>
    Harden `bootstrap.sh` orchestrator and implement Section 4 test suite per D-02, D-10, D-11, D-12, D-13, D-14 Section 4, D-16, and INTG-03:
    1. In `bootstrap.sh`, update `is_guarded_path()` (~line 322) to implement hierarchical prefix matching: traverse path ancestors up to root, returning 0 if `${GUARDED_PATHS["$cur"]:-}` is set for any ancestor component per D-02.
    2. In Step 4 `run_destub()` (~line 441), add explicit legacy Catppuccin symlink pruning: iterate through `$target/.config/gtk-3.0` and `$target/.config/gtk-4.0`; if any symlink targets `/usr/share/themes/Catppuccin*` or contains `*/Catppuccin*`, unlink it and log `[PRUNE]` per D-11.
    3. In Step 5 `run_stow_step()` (~line 485), add pre-creation of sensitive parent directories: `mkdir -p` for `.config/fuzzel`, `.config/gtk-3.0`, `.config/gtk-4.0`, `.config/hypr/custom`, `.config/kitty`, and `.config/systemd/user` before stowing with `--verbose=5 --no-folding` per D-10.
    4. In Step 6 `step_capture_seed()` (~line 565), author `generate_initial_theme()`:
       - Sanitize `~/.config/matugen/templates/gtk-4.0/gtk.css` by replacing `.boxed-list row:insensitive` with `.boxed-list row:disabled` to prevent `Gtk.CssProvider` syntax parse errors (Pitfall 4).
       - Extract `background.wallpaperPath` from `~/.config/illogical-impulse/config.json` via `jq`.
       - If wallpaper image exists, invoke `switchwall.sh --noswitch`; if execution fails or wallpaper is missing, fail soft with color seed fallback: `switchwall.sh --color "#3f51b5"` per D-12, D-13.
    5. In Step 7 `step_verify()` (~line 638), enable `dotfiles-capture.timer` and execute `arch/dots-hyprland.sh verify --strict` with exit status bound 1:1.
    6. Implement Section 4 in `scripts/phase29-theme-data-contracts-assert.sh` using an isolated scratch directory (`$S4_ROOT` with mock `$MOCK_HOME` and `$MOCK_REPO`) per D-14 Section 4 and D-16:
       - Assert `is_guarded_path` recognizes nested files inside guarded directories (e.g. `Kvantum/theme.kvconfig`, `kde-material-you-colors/config.conf`).
       - Assert `run_destub` safely prunes root-owned Catppuccin symlinks in `gtk-4.0` while preserving guarded `gtk.css` in `gtk-3.0` untouched.
       - Assert `run_stow_step` pre-creates `.config/fuzzel`, `.config/kitty`, and sensitive parent directories.
       - Assert `generate_initial_theme` sanitizes GTK 4 template pseudo-classes and triggers color seed fallback when wallpaper is absent.
    7. Run `bash scripts/phase29-theme-data-contracts-assert.sh --section 4` and assert FAIL=0.
  </action>
  <acceptance_criteria>
    grep -q 'while.*cur.*dirname' bootstrap.sh exits 0.
    grep -q 'Catppuccin' bootstrap.sh exits 0.
    grep -q '\.config/fuzzel' bootstrap.sh exits 0.
    grep -q 'switchwall.*--color' bootstrap.sh exits 0.
    bash scripts/phase29-theme-data-contracts-assert.sh --section 4 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase29-theme-data-contracts-assert.sh --section 4</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>bootstrap.sh hardened with hierarchical prefix guard matching, explicit legacy Catppuccin symlink pruning in destub, parent directory pre-creation (.config/fuzzel, .config/kitty, etc.) in stow, initial theme generation with GTK 4 template sanitization and color fallback in capture_seed, and Section 4 scratch drill passes with FAIL=0.</done>
</task>

<task id="29-02-03" type="execute">
  <name>Implement Section 5 full v0.5 regression sweep and execute full phase verification suite</name>
  <read_first>
    scripts/phase25-gtk-material-you-assert.sh
    scripts/phase26-qt-kde-material-you-assert.sh
    scripts/phase27-accent-coordination-assert.sh
    scripts/phase28-terminal-fuzzel-assert.sh
    arch/dots-hyprland.sh
    .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-PATTERNS.md
    .planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-CONTEXT.md
  </read_first>
  <files>
    scripts/phase29-theme-data-contracts-assert.sh
  </files>
  <action>
    Implement Section 5 multi-phase regression sweep and execute complete phase sign-off per D-14 Section 5, D-15, INTG-01, INTG-02, and INTG-03:
    1. In `scripts/phase29-theme-data-contracts-assert.sh`, implement Section 5 executing all preceding v0.5 assertion suites sequentially:
     - `scripts/phase25-gtk-material-you-assert.sh` (GTK theming & Catppuccin de-linking)
     - `scripts/phase26-qt-kde-material-you-assert.sh` (Qt & KDE harmonization)
     - `scripts/phase27-accent-coordination-assert.sh` (Hyprland & Quickshell accent coordination)
     - `scripts/phase28-terminal-fuzzel-assert.sh` (Terminal & Fuzzel launcher dynamic palette)
    2. For each suite, assert exit code 0 and zero failure reports.
    3. Ensure final closing self-check compares `$PORCELAIN_BEFORE` and `$PORCELAIN_AFTER` via `cmp -s`, failing if any test suite caused uncommitted working-tree modifications.
    4. Run `bash scripts/phase29-theme-data-contracts-assert.sh --section 5` and assert FAIL=0.
    5. Run the complete full-suite verification command:
       `bash scripts/phase29-theme-data-contracts-assert.sh && ./arch/dots-hyprland.sh verify --strict`
       Assert exit code 0 with `FAIL=0 FINDINGS=0` across all 5 sections and the strict repository verifier.
  </action>
  <acceptance_criteria>
    bash scripts/phase29-theme-data-contracts-assert.sh --section 5 exits 0 with FAIL=0.
    bash scripts/phase29-theme-data-contracts-assert.sh exits 0 with FAIL=0 FINDINGS=0.
    ./arch/dots-hyprland.sh verify --strict exits 0 with FAIL=0 FINDINGS=0.
    git status --porcelain is byte-identical clean before and after suite execution.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase29-theme-data-contracts-assert.sh && ./arch/dots-hyprland.sh verify --strict</automated>
    <fails_when>non-zero exit code, "[FAIL]" in assert output, or findings reported by verify --strict</fails_when>
  </verify>
  <done>Section 5 implemented running phase25, phase26, phase27, and phase28 regression sweeps, closing porcelain invariant validated, and the full suite passes with FAIL=0 FINDINGS=0 under arch/dots-hyprland.sh verify --strict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bootstrap Destubbing → User Filesystem | Removing conflicts and unlinking legacy stubs without destroying personal unmanaged configuration |
| Dynamic Wallpaper Generation → System Stability | Executing switchwall.sh live in headless or offline environments without crashing the bootstrap process |
| Multi-Phase Regression Suite → Repository Hygiene | Running multiple legacy assert scripts without introducing working tree pollution or leftover state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-29-04 | Tampering / Accidental Data Loss | bootstrap.sh run_destub | high | mitigate | Scope Catppuccin pruning specifically to symlinks targeting /usr/share/themes/Catppuccin* in gtk-3.0 and gtk-4.0, preserving regular files and guarded theme outputs via hierarchical prefix checks (D-02, D-11, INTG-03). |
| T-29-05 | Denial of Service / Bootstrap Crash | bootstrap.sh initial theming | medium | mitigate | Implement fail-soft fallback in generate_initial_theme(): sanitize GTK 4 template :insensitive pseudo-class, verify wallpaper file existence before switchwall.sh --noswitch, and fall back to switchwall.sh --color "#3f51b5" on error (D-12, D-13, INTG-03). |
| T-29-06 | Tampering / Silent Regression | Full v0.5 test execution | high | mitigate | Implement Section 5 unified regression sweep executing Phases 25 through 28 harnesses, checking porcelain diff before and after the entire run to prevent silent breakage or repository drift (D-14, D-15, INTG-01..03). |
</threat_model>

<verification>
Automated verification commands:
1. `bash scripts/phase29-theme-data-contracts-assert.sh --section 2`
2. `bash scripts/phase29-theme-data-contracts-assert.sh --section 4`
3. `bash scripts/phase29-theme-data-contracts-assert.sh --section 5`
4. `bash scripts/phase29-theme-data-contracts-assert.sh && ./arch/dots-hyprland.sh verify --strict`
</verification>

<success_criteria>
1. Section 2 executes `switchwall.sh --noswitch`, polling `kdeglobals` for asynchronous background job completion, proving monotonic mtime advancement across all 5 themed outputs and zero git churn.
2. `bootstrap.sh` implements hierarchical prefix guard matching, legacy Catppuccin pruning, parent directory pre-creation, and fail-soft initial theme generation.
3. Section 4 executes an isolated scratch drill verifying de-stubbing, pruning, and directory creation with FAIL=0.
4. Section 5 executes all 4 prior phase test suites (`phase25`, `phase26`, `phase27`, `phase28`), asserting all pass with 0 failures.
5. `bash scripts/phase29-theme-data-contracts-assert.sh && ./arch/dots-hyprland.sh verify --strict` exits 0 with FAIL=0 FINDINGS=0.
</success_criteria>

<output>
Create `.planning/phases/29-theme-data-contracts-verification-bootstrap-integration/29-02-SUMMARY.md` when done
</output>
