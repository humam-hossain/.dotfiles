---
phase: 05-fork-submodule-pin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - OWN-01
user_setup: []

must_haves:
  truths:
    - "GitHub hosts a public personal fork humam-hossain/dots-hyprland of end-4/dots-hyprland"
    - "gh repo view humam-hossain/dots-hyprland reports isFork=true and parent end-4/dots-hyprland"
    - "Fork creation used gh repo fork end-4/dots-hyprland --clone=false (not browser-only, not bare gh repo fork inside .dotfiles)"
    - "Re-running the fork step when the fork already exists is treated as success (idempotent)"
    - "~/github_repo/dots-hyprland is not deleted, moved, retargeted, or used as seed"
  artifacts:
    - "Remote GitHub repository: humam-hossain/dots-hyprland (public fork)"
  key_links:
    - "Fork parent relationship: humam-hossain/dots-hyprland → end-4/dots-hyprland"
  prohibitions:
    - "Do not run vendor/dots-hyprland/setup or any ./setup"
    - "Do not create arch/dots-hyprland.sh or any install wrapper"
    - "Do not mv or submodule-add from ~/github_repo/dots-hyprland"
    - "Do not git submodule add end-4/dots-hyprland URL"
    - "Do not pass -b/--branch on submodule add (no auto-track line in .gitmodules)"
    - "Do not embed tokens, SSH private keys, or gh credentials in files or commit messages"
    - "Do not set -x around gh/auth commands"
    - "Do not install packages (npm/pip/cargo) this phase"
---

<objective>
Create the public personal GitHub fork of end-4/dots-hyprland owned by humam-hossain, with no local submodule materialization yet.

Purpose: Establish OWN-01 remote ownership on GitHub before any parent-repo pin (D-01, D-03). Everything is fresh — sibling clone is irrelevant (D-02, D-14).

Output: Verified public fork `humam-hossain/dots-hyprland` ready for `git submodule add` in plan 05-02.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-fork-submodule-pin/05-CONTEXT.md
@.planning/phases/05-fork-submodule-pin/05-RESEARCH.md
@.planning/phases/05-fork-submodule-pin/05-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

## Phase Goal

Own and pin dots-hyprland inside `.dotfiles` before any install mutates the machine. This plan only creates the personal fork on GitHub.

## Artifacts this phase produces

| Artifact | Path / identity | Created in |
|----------|-----------------|------------|
| Personal fork (GitHub) | `humam-hossain/dots-hyprland` | **05-01** |
| Parent `.gitmodules` | `.gitmodules` | 05-02 |
| Outer submodule checkout | `vendor/dots-hyprland/` | 05-02 |
| Nested shapes tree | `vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/widgets/shapes/` | 05-02 |
| Dual remotes (local) | origin SSH fork + upstream HTTPS end-4 inside vendor | 05-02 |
| Parent pin commit | gitlink mode 160000 + `.gitmodules` same commit | 05-03 |

## Tasks

<task type="auto">
  <name>Task 1: Preflight tools and create personal fork</name>
  <files>GitHub:humam-hossain/dots-hyprland (remote-only; no local path yet)</files>
  <read_first>
    - .planning/phases/05-fork-submodule-pin/05-CONTEXT.md (D-01..D-04, D-14, D-16)
    - .planning/phases/05-fork-submodule-pin/05-RESEARCH.md (Pattern 1 non-interactive fork; Pitfall 6 fork race)
    - .planning/research/PITFALLS.md (Pitfall 4 — do not convert sibling)
  </read_first>
  <action>
    From any directory (not limited to REPO_ROOT):

    1. Confirm tooling: `gh --version`, `git --version`, and that `gh auth status` shows authenticated user humam-hossain. Do not print tokens. Do not enable shell xtrace around auth.
    2. Confirm sibling path is out of band per D-02/D-14: do not read remotes of `~/github_repo/dots-hyprland` for seeding; do not delete, move, or rewrite that path.
    3. Create the fork with the exact locked command per D-01: `gh repo fork end-4/dots-hyprland --clone=false`. Always pass owner/repo — never bare `gh repo fork` inside `.dotfiles` (would fork the wrong repo).
    4. Idempotency: if the fork already exists, treat gh success-or-already-exists as OK and continue (do not fail the plan for re-run).
    5. Do not clone the fork into a temp path. Do not `git submodule add` yet (plan 05-02). Do not run setup or create wrapper files (D-16).
  </action>
  <verify>
    <automated>gh repo view humam-hossain/dots-hyprland --json name,isFork,visibility,url,parent --jq 'select(.isFork==true and .name=="dots-hyprland") | .name'</automated>
  </verify>
  <acceptance_criteria>
    - Command `gh repo view humam-hossain/dots-hyprland` exits 0 (fork is resolvable on GitHub).
    - JSON field `isFork` is true.
    - Fork was created via `gh repo fork end-4/dots-hyprland --clone=false` (or already existed from a prior identical run).
    - No new local path `vendor/dots-hyprland` was created in this task.
    - Path `~/github_repo/dots-hyprland` still exists if it existed before (left alone per D-14).
    - No file under `.dotfiles` contains a GitHub token or private key introduced by this task.
  </acceptance_criteria>
  <done>Public personal fork humam-hossain/dots-hyprland exists and is viewable via gh.</done>
</task>

<task type="auto">
  <name>Task 2: Verify fork parent, visibility, and readiness for submodule</name>
  <files>GitHub:humam-hossain/dots-hyprland (remote-only; no local path yet)</files>
  <read_first>
    - .planning/phases/05-fork-submodule-pin/05-CONTEXT.md (D-03 public, D-04 URL style for next plan)
    - .planning/phases/05-fork-submodule-pin/05-RESEARCH.md (Environment Availability; Pitfall 6 retry readiness)
  </read_first>
  <action>
    1. Run `gh repo view humam-hossain/dots-hyprland --json name,isFork,visibility,url,parent,defaultBranchRef` and assert:
       - `isFork` is true (D-01 ownership)
       - `visibility` is PUBLIC (D-03)
       - parent owner/name is `end-4/dots-hyprland`
       - default branch name is recorded for awareness only (do not force-reset later — D-09/D-10 trust fork tip at add time)
    2. Optionally probe SSH reachability of the future origin URL without cloning: `git ls-remote git@github.com:humam-hossain/dots-hyprland.git HEAD` should print a 40-hex SHA. If this fails immediately after fork create, wait briefly and retry once (fork propagation — RESEARCH Pitfall 6). Do not fall back to the sibling clone.
    3. Confirm parent REPO_ROOT still has no `vendor/dots-hyprland` and no `.gitmodules` yet (greenfield for 05-02).
    4. Do not add remotes on any local clone yet — dual remotes are configured inside the submodule checkout in 05-02 (D-04, D-07).
  </action>
  <verify>
    <automated>gh repo view humam-hossain/dots-hyprland --json isFork,visibility,parent --jq 'select(.isFork==true and (.visibility|ascii_upcase)=="PUBLIC" and .parent.nameWithOwner=="end-4/dots-hyprland") | "OWN-01-fork-ok"'</automated>
  </verify>
  <acceptance_criteria>
    - `isFork` is true and parent is `end-4/dots-hyprland`.
    - Visibility is public (PUBLIC).
    - `git ls-remote git@github.com:humam-hossain/dots-hyprland.git HEAD` returns a commit SHA (SSH fork URL ready for submodule add).
    - `test ! -e vendor/dots-hyprland` is true at REPO_ROOT.
    - `test ! -f .gitmodules` is true at REPO_ROOT.
    - No `./setup` was executed; no wrapper script created.
  </acceptance_criteria>
  <done>Fork metadata matches OWN-01 GitHub side; machine is ready for submodule add of the fork SSH URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell → GitHub API (`gh`) | Authenticated API creates/views fork; tokens must stay in keyring |
| Operator shell → GitHub SSH | Future origin transport; keys not logged |
| Local sibling path → phase work | Untrusted as seed; must not become SoT |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Spoofing | Fork target / future submodule URL | high | mitigate | Only create/use `humam-hossain/dots-hyprland` forked from `end-4/dots-hyprland`; never submodule-add end-4 URL as origin |
| T-05-02 | Information Disclosure | gh auth / logs | medium | mitigate | No `set -x` around auth; never write tokens into repo files or SUMMARY |
| T-05-03 | Elevation of Privilege | `./setup` | high | mitigate | D-16: do not execute setup or install wrappers this plan |
| T-05-04 | Tampering | Sibling conversion | high | mitigate | D-02/D-14: leave `~/github_repo/dots-hyprland` alone; fresh fork only |
| T-05-05 | Tampering | Nested shapes host | medium | accept | Nested content not materialized until 05-02; mitigated there |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in Phase 5; host git+gh only |
</threat_model>

<verification>
Full plan gate:
1. `gh repo view humam-hossain/dots-hyprland --json isFork,visibility,parent` shows public fork of end-4/dots-hyprland
2. `git ls-remote git@github.com:humam-hossain/dots-hyprland.git HEAD` succeeds
3. REPO_ROOT still lacks `vendor/dots-hyprland` and `.gitmodules`
4. Sibling path untouched; no setup run
</verification>

<success_criteria>
- OWN-01 GitHub ownership half complete: personal public fork exists
- D-01, D-02, D-03, D-14, D-16 honored
- Ready for 05-02 `git submodule add git@github.com:humam-hossain/dots-hyprland.git vendor/dots-hyprland`
</success_criteria>

<output>
Create `.planning/phases/05-fork-submodule-pin/05-01-SUMMARY.md` when done
</output>
