---
phase: 19-link-aware-verify
plan: 04
type: execute
wave: 4
depends_on: ["19-03"]
files_modified:
  - scripts/phase19-link-aware-verify-assert.sh
autonomous: true
requirements: [VER-02, VER-03]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`capture/` content drift is `[FINDING]`, a class of its own, distinct from the `[FAIL]` a link failure produces — it compares two real artifacts and observes a genuine disagreement, which is a higher-confidence observation than the repo-vs-`HEAD` guess that D-21 reports as `[INFO]` (D-22, VER-02)."
    - "A `capture/` path whose live file is byte-identical to its repo mirror emits `[PASS]` and moves neither counter; only a real byte disagreement, or an absent live counterpart, produces `[FINDING]`. [edge: VER-02/adjacency]"
    - "A `capture/` tree holding only `README.md` and no package directories produces zero capture-tree lines and cannot change the verdict. The block is vacuous on today's tree by design, so the staged fixture is the only thing that exercises it at all. [edge: VER-02/empty]"
    - "The `capture/` walk enumerates with the same `find … -print0 | LC_ALL=C sort -z` idiom as the repo-side walk, so two runs over an unchanged tree list capture paths in the same order. [edge: VER-02/ordering]"
    - statement: "An interrupted or concurrently-running `verify` leaves no partial state behind: `git status --porcelain --ignored` is byte-identical before and after a full run, and a transcript with no `=== done:` line is never a verdict."
      verification: backstop
    - "`--strict` exits 1 on a fixture that has findings and no failures, and the identical fixture exits 0 without the flag — `[FINDING]` never moves the exit code on its own, and `[INFO]` never moves it under any flag (D-13, D-35, VER-03)."
    - "`capture/` content drift and `capture/` missing-live-counterpart are the only two `[FINDING]` sources this phase has, after D-06 moved non-repo dangling links to `[INFO]` and D-21 made the content observation `[INFO]` — so the findings-only fixture must stage a `capture/` package or there is nothing for `--strict` to promote (D-35)."
    - "Both branches of the installer's auto-backup primitive are covered: the first-run branch renames the target aside and copies, destroying the link and leaving a `.old` sibling; the non-first-run branch copies alongside, leaving the link intact and a `.new` sibling (D-32)."
    - "ROADMAP criterion 5 is CHECKED, not asserted: the final section runs `verify` read-only against the real `$HOME` after every fixture is torn down and fails only if it does not exit 0 — no property of today's tree is encoded as an expectation that would rot (D-36)."
  artifacts:
    - "scripts/phase19-link-aware-verify-assert.sh — Section 6 (the findings-only `capture/` fixture and the `--strict` promotion), Section 7 (both auto-backup branches), Section 8 (the read-only real-tree run)"
  key_links:
    - "the staged `capture/` package -> the `capture/` block at `arch/dots-hyprland.sh:806-838` -> the only `[FINDING]` this phase can produce -> the only thing `--strict` has to promote"
    - "fixture teardown -> Section 8's real-tree run (it must run after every scratch `$HOME` is gone, or a leaked fixture becomes part of the criterion-5 answer)"
---

<objective>
Prove the two things a clean tree cannot prove: that `capture/` drift is its own finding class, and that `--strict` promotes it.

Purpose: `capture/` holds only `README.md` today, so the block that implements VER-02 is vacuous on the live tree and every assertion about it has to come from a staged fixture. The same fixture is the only thing `--strict` has to promote — after D-06 moved the non-repo dangling links to `[INFO]` and D-21 made the content observation `[INFO]`, `capture/` drift and `capture/` missing-live-counterpart are the phase's only two `[FINDING]` sources. This plan also covers the installer primitive whose two branches straddle the link class and the content class, and closes with the criterion-5 check that deliberately asserts nothing about today's tree beyond its exit code.

Output: three new sections in `scripts/phase19-link-aware-verify-assert.sh`. No production code changes — the wrapper reached its final state in plan `19-03`.
</objective>

<execution_context>
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/workflows/execute-plan.md
@/home/pera/github_repo/.dotfiles/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-link-aware-verify/19-CONTEXT.md
@.planning/phases/19-link-aware-verify/19-RESEARCH.md
@.planning/phases/19-link-aware-verify/19-PATTERNS.md
@.planning/phases/19-link-aware-verify/19-03-SUMMARY.md
@arch/dots-hyprland.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section 6 — the findings-only `capture/` fixture and the `--strict` promotion</name>
  <files>scripts/phase19-link-aware-verify-assert.sh</files>
  <read_first>
    - `scripts/phase19-link-aware-verify-assert.sh` as left by plan `19-03` — the fixture builder, the runner, Sections 1 through 5, the criteria summary block
    - `arch/dots-hyprland.sh` lines 806-838 — the `capture/` block with its inverted expectation, its `[FAIL]` for a live path that is a symlink into the repo, its `[FINDING]` for an absent live counterpart, and its `cmp -s` drift `[FINDING]`
    - `arch/dots-hyprland.sh` lines 840-845 as edited by plan `19-01` — the `--strict` exit condition being asserted here
    - `capture/README.md` — the tree contract, and the fact that the tree holds nothing else
    - `.planning/phases/19-link-aware-verify/19-CONTEXT.md` — D-13, D-22, D-35
    - `.planning/phases/19-link-aware-verify/19-RESEARCH.md` — §Phase Requirements, VER-02 row
  </read_first>
  <action>
Append Section 6 to `scripts/phase19-link-aware-verify-assert.sh`, before the closing block, reusing the existing fixture builder and runner.

Extend the fixture builder — or add a thin variant beside it, whichever keeps the factoring cleaner — so it can stage a `capture/` package in the scratch repo in addition to the `stow/fixture` package. A `capture/` package's contract is inverted: the live file is a REAL file, not a link, and the repo holds a mirror copy of it. Build one whose live file and repo mirror have different bytes.

Assert, from one run of the runner with no flags, that this fixture produces:

- a `[FINDING]` naming the live path as content drift between live and repo, and not a `[FAIL]` — this is the distinct finding class VER-02 requires, and the labels differ from the link class deliberately rather than inconsistently. The `capture/` comparison holds two real artifacts side by side and observes a genuine disagreement; D-21's repo-vs-`HEAD` observation cannot distinguish an installer write-through from an operator's edit and is therefore `[INFO]`. Record that contrast in a comment.
- a summary line whose `FAIL=` is 0 and whose `FINDINGS=` is the exact number the staged set implies.
- `rc` equal to 0. A finding on its own does not move the exit code.

Then run the SAME fixture through the runner with `--strict` and assert `rc` equals 1 while `FAIL=` is still 0. That pair — identical fixture, identical labels, different exit code — is the whole of D-13 and the reason `--strict` exists. Assert additionally that the `--strict` capture is byte-identical to the no-flag capture above the summary line, reusing the comparison helper Section 3 already wrote rather than writing a second one.

Add two further cases in the same section, each from its own fixture so they do not interact:

- A `capture/` package whose live counterpart does not exist: assert a `[FINDING]` naming it, `rc` 0 without `--strict`, `rc` 1 with it. This is the second and last `[FINDING]` source in the phase.
- A `capture/` package whose live path is a symlink into the repo: assert a `[FAIL]` naming it as wrongly stowed, and `rc` 1 without any flag. The inverted expectation is a hard condition, not an observation.

Add a third case asserting the empty shape: build a scratch repo whose `capture/` directory holds only a `README.md` and no package directories, and assert the run emits no capture-tree line at all and that `rc`, `FAIL=` and `FINDINGS=` are unchanged from the same fixture built without a `capture/` directory. That is the shape of the real tree today, and asserting it explicitly is what stops the block's vacuity from being mistaken for coverage.

Add a fourth, one-line case asserting the ordering property: a `capture/` package holding several files produces them in the same order on two consecutive runs.

Extend the criteria summary block so ROADMAP criteria 2 and 3 point at Section 6.
  </action>
  <acceptance_criteria>
    - Section 6 stages a `capture/` package with a live file that differs from its repo mirror and asserts a `[FINDING]`, not a `[FAIL]`, for it
    - the same fixture asserts `rc` 0 without `--strict` and `rc` 1 with it, with `FAIL=0` in both
    - the `--strict` and no-flag captures are asserted byte-identical above the summary line
    - separate cases assert the missing-live-counterpart `[FINDING]` and the wrongly-stowed `[FAIL]`
    - a case asserts a `capture/` tree holding only `README.md` produces no capture-tree line and changes neither counter
    - a case asserts capture-path listing order is identical across two consecutive runs
    - `./scripts/phase19-link-aware-verify-assert.sh` exits 0 with `FAIL=0` and now emits at least 48 `[PASS]` lines
    - `git status --porcelain --ignored` is byte-identical before and after the assert run
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -u; bash -n scripts/phase19-link-aware-verify-assert.sh && B="$(git status --porcelain --ignored)" && OUT="$(./scripts/phase19-link-aware-verify-assert.sh)" && A="$(git status --porcelain --ignored)" && [ "$B" = "$A" ] || { echo "assert_mutated_tree"; exit 1; }; printf "%s\n" "$OUT" | grep -q -- "=== done: FAIL=0" || { echo "assert_red"; exit 1; }; P="$(printf "%s\n" "$OUT" | grep -c "^\[PASS\]")"; [ "$P" -ge 48 ] || { echo "pass_count=$P too low"; exit 1; }; echo "SECTION6_OK pass=$P"'</automated>
    <fails_when>non-zero exit, or any of `assert_mutated_tree`, `assert_red`, `pass_count=` appears, or `SECTION6_OK` is absent. A `[PASS]` count below 48 means Section 6's `capture/` and `--strict` cases did not all run — most likely the findings-only fixture produced zero findings, leaving `--strict` nothing to promote and the promotion assertion vacuously satisfied</fails_when>
    <automated>bash -c 'set -u; grep -q -- "capture" scripts/phase19-link-aware-verify-assert.sh && grep -q -- "--strict" scripts/phase19-link-aware-verify-assert.sh && ./arch/dots-hyprland.sh verify --strict >/dev/null && ./arch/dots-hyprland.sh verify >/dev/null'</automated>
    <fails_when>non-zero exit — the real tree has `FAIL=0 FINDINGS=0` today, so `--strict` must still exit 0 there; a non-zero exit here means `--strict` was wired as a blanket demotion of green rather than as a promotion of findings</fails_when>
  </verify>
  <done>A staged `capture/` package with drifted content produces a `[FINDING]` that does not move the exit code, the same fixture exits 1 under `--strict` with identical output above the summary line, and a `capture/` tree holding only its README produces no capture-tree line at all.</done>
</task>

<task type="auto">
  <name>Task 2: Section 7 — both branches of the installer's auto-backup primitive</name>
  <files>scripts/phase19-link-aware-verify-assert.sh</files>
  <read_first>
    - `scripts/phase19-link-aware-verify-assert.sh` as left by Task 1
    - `vendor/dots-hyprland/sdata/subcmd-install/3.files.sh` lines 100-120 — `install_file__auto_backup` and its branch on the first-run flag: one arm renames the target aside then copies, the other copies alongside with a different suffix
    - `arch/dots-hyprland.sh` — the repo-side walk's not-a-symlink `[FAIL]` arm and the sweep's installer-artifact `[INFO]` arm, so the assertions match the wording each actually emits
    - `.planning/phases/19-link-aware-verify/19-CONTEXT.md` — D-32, D-33, D-47
    - `.planning/research/PITFALLS.md` §30 — the write-primitive catalogue and why a content-only check reports no drift in exactly the destroying case
  </read_first>
  <action>
Append Section 7 to `scripts/phase19-link-aware-verify-assert.sh`, before the closing block.

Reproduce both branches of the installer's auto-backup primitive against a scratch fixture and assert what `verify` says about each. Reproduce the primitive's EFFECT with plain shell commands inside the fixture; do not source, execute, or otherwise depend on anything under `vendor/dots-hyprland`. D-47 requires `verify` to work with the submodule de-initialised and the assert must hold the same line — read the vendor file to confirm the two effects, then stage them by hand.

First-run branch. The target is renamed aside with the first backup suffix and a fresh regular file is copied into its place. Effect on a stowed path: the symlink is destroyed and a sibling carrying that suffix appears beside it. Assert from one run: the live path appears on a `[FAIL]` line stating it is not a symlink, that `[FAIL]` line carries a `stow -t` recovery invocation naming the right package (D-33), the sibling appears on an `[INFO]` line naming it an installer backup artifact, and `rc` equals 1.

Non-first-run branch. The target is left alone and the new content is copied beside it under the second suffix. Effect on a stowed path: the symlink is INTACT and a differently-suffixed sibling appears beside it. Assert from one run: the live path appears on a `[PASS]` line, the sibling appears on an `[INFO]` line naming it an installer backup artifact, and `rc` equals 0.

That contrast is the section's point: the same primitive, branching on one flag, lands on opposite sides of the link/content boundary. One branch is a loud failure; the other is a silent survivor that only the sweep's artifact arm reports at all.

Stage each branch in its own fixture so neither masks the other, and put the artifact sibling in a package-owned directory at depth two or more below the scratch `$HOME` for one case and in a shared root for the other — the artifact-shape arm is tested before the shared-root exemption, so both must produce the `[INFO]`. That second placement is the regression guard for the arm-ordering property research measured: five of the nineteen artifacts on the real tree live in the two shared roots and would vanish if the order were inverted.

Extend the criteria summary block so ROADMAP criterion 4's installer-primitive coverage points at Section 7.
  </action>
  <acceptance_criteria>
    - Section 7 stages both branches in separate fixtures and contains no reference to `vendor/dots-hyprland`
    - the first-run branch asserts a not-a-symlink `[FAIL]` carrying a `stow -t` recovery invocation, an installer-artifact `[INFO]` for the sibling, and `rc` 1
    - the non-first-run branch asserts a `[PASS]` on the link, an installer-artifact `[INFO]` for the sibling, and `rc` 0
    - one of the two artifact siblings is staged in a shared root and still produces its `[INFO]`
    - no non-comment line of the assert names the vendored submodule path: filtering comment lines out of the script and grepping the remainder for that path finds nothing
    - `./scripts/phase19-link-aware-verify-assert.sh` exits 0 with `FAIL=0` and now emits at least 56 `[PASS]` lines
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -u; bash -n scripts/phase19-link-aware-verify-assert.sh || { echo "syntax_error"; exit 1; }; CODE="$(grep -v "^[[:space:]]*#" scripts/phase19-link-aware-verify-assert.sh)"; case "$CODE" in *vendor/dots-hyprland*) echo "assert_depends_on_vendor"; exit 1 ;; esac; B="$(git status --porcelain --ignored)"; OUT="$(./scripts/phase19-link-aware-verify-assert.sh)" || { echo "assert_red"; exit 1; }; A="$(git status --porcelain --ignored)"; [ "$B" = "$A" ] || { echo "assert_mutated_tree"; exit 1; }; printf "%s\n" "$OUT" | grep -q -- "=== done: FAIL=0" || { echo "assert_red"; exit 1; }; P="$(printf "%s\n" "$OUT" | grep -c "^\[PASS\]")"; [ "$P" -ge 56 ] || { echo "pass_count=$P too low"; exit 1; }; echo "SECTION7_OK pass=$P"'</automated>
    <fails_when>non-zero exit, or any of `syntax_error`, `assert_depends_on_vendor`, `assert_mutated_tree`, `assert_red`, `pass_count=` appears, or `SECTION7_OK` is absent. `assert_depends_on_vendor` means a non-comment line of the section reached into the submodule, breaking the D-47 property that this tooling runs with it de-initialised</fails_when>
    <automated>bash -c 'set -u; git submodule deinit -f vendor/dots-hyprland >/dev/null 2>&1 || true; rc=0; ./arch/dots-hyprland.sh verify >/dev/null 2>&1 || rc=$?; git submodule update --init vendor/dots-hyprland >/dev/null 2>&1 || true; [ "$rc" -eq 0 ] || { echo "verify_needs_submodule_rc=$rc"; exit 1; }; echo D47_HOLDS'</automated>
    <fails_when>non-zero exit, or `verify_needs_submodule_rc=` appears, or `D47_HOLDS` is absent — `verify` must run to a real exit code with the vendored submodule de-initialised</fails_when>
  </verify>
  <done>The first-run branch of the installer's auto-backup primitive is a loud `[FAIL]` with a named recovery; the non-first-run branch is a `[PASS]` on the link plus an `[INFO]` on the sibling; and neither the assert nor `verify` touches the vendored submodule.</done>
</task>

<task type="auto">
  <name>Task 3: Section 8 — the read-only run against the real tree</name>
  <files>scripts/phase19-link-aware-verify-assert.sh</files>
  <read_first>
    - `scripts/phase19-link-aware-verify-assert.sh` as left by Task 2 — every section, every fixture, the cleanup function, the closing self-check
    - `scripts/phase18-capture-model-assert.sh` lines 687-741 — the Section 7a real-tree invocation and exit-code-capture idiom, which is the closest analog for this section
    - `.planning/phases/19-link-aware-verify/19-CONTEXT.md` — D-36, D-38
    - `.planning/phases/19-link-aware-verify/19-RESEARCH.md` — Pitfall 4 (why the porcelain bracket needs the ignored form), Pitfall 6 (why no line-count total is asserted), §Anti-Patterns to Avoid
    - `.planning/ROADMAP.md` §Phase 19 success criterion 5
  </read_first>
  <action>
Append Section 8 to `scripts/phase19-link-aware-verify-assert.sh` as the LAST section before the closing self-check, and make sure every fixture from Sections 1 through 7 has been torn down before it runs. If any section holds a scratch root open past its own body, close it — a leaked fixture becomes part of this section's answer and the answer would then be about the harness rather than about the tree.

Run `./arch/dots-hyprland.sh verify` against the real `$HOME` and the real repo, read-only, with no flags. Assert exactly one thing: that it exits 0. Then run it once more with `--strict` and assert the same. Emit the run's `=== done:` summary line into the transcript as an `[INFO]` so a reader can see what the tree looked like, and emit the counts of each label as a second `[INFO]`.

Assert nothing else about the real tree. This section CHECKS criterion 5, it does not assert it (D-36). Encoding today's `[INFO]` volume, or today's `[PASS]` count, or today's total line count as an expectation is exactly the rot research warns about: the volume moves with the operator's uncommitted edits and with whatever installer backup files the last install left. Write that reasoning as a comment above the section so a later reader does not "strengthen" it into a brittle assertion.

Two things this section must NOT do. It must not run `verify` with any argument that would make it write, because there is no such argument and adding one would be out of scope. And it must not be moved above the fixture sections for tidiness — its position after teardown is the property being relied on.

Then update the closing self-check block that plan `19-01` wrote:

- Confirm the porcelain bracket compares the ignored form at both ends, and that its filter still covers only the prefixes known to be ignored on a clean tree rather than swallowing new ones.
- Confirm the `FIXTURE_LEAK` sweep covers every scratch root any section created, including the ones added in plans `19-02` through `19-04`. A sweep that was written when there was one fixture and never extended is a self-check that silently stopped checking.
- Complete the `=== Phase 19 ROADMAP Criteria Summary ===` block so all five criteria are named and each points at the section or sections that cover it, with no criterion left marked pending.

Finally, add a short header comment at the top of the script listing the sections in order with a one-line purpose each, so the next person reading it can find the section that owns a given claim without reading the whole file.
  </action>
  <acceptance_criteria>
    - Section 8 is the last section before the closing self-check and runs after every fixture is torn down
    - it asserts `rc` 0 for both a bare `verify` and a `verify --strict` against the real tree, and asserts nothing else about the tree
    - it emits the real run's summary line and per-label counts as `[INFO]`
    - the `FIXTURE_LEAK` sweep names every scratch root created by any section
    - the criteria summary block names all five ROADMAP criteria with no entry left pending
    - `grep -c 'pending' scripts/phase19-link-aware-verify-assert.sh` reports 0 within the criteria summary block
    - a header comment lists the sections in order
    - `./scripts/phase19-link-aware-verify-assert.sh` exits 0 with `FAIL=0` and emits at least 60 `[PASS]` lines and at least 3 `[INFO]` lines
    - `git status --porcelain --ignored` is byte-identical before and after the assert run and names no fixture path
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'set -u; bash -n scripts/phase19-link-aware-verify-assert.sh && B="$(git status --porcelain --ignored)" && OUT="$(./scripts/phase19-link-aware-verify-assert.sh)" && A="$(git status --porcelain --ignored)" && [ "$B" = "$A" ] || { echo "assert_mutated_tree"; exit 1; }; printf "%s\n" "$OUT" | grep -q -- "=== done: FAIL=0" || { echo "assert_red"; exit 1; }; P="$(printf "%s\n" "$OUT" | grep -c "^\[PASS\]")"; I="$(printf "%s\n" "$OUT" | grep -c "^\[INFO\]")"; [ "$P" -ge 60 ] || { echo "pass_count=$P too low"; exit 1; }; [ "$I" -ge 3 ] || { echo "info_count=$I too low"; exit 1; }; printf "%s\n" "$OUT" | grep -q -- "Criteria Summary" || { echo "no_criteria_summary"; exit 1; }; echo "SECTION8_OK pass=$P info=$I"'</automated>
    <fails_when>non-zero exit, or any of `assert_mutated_tree`, `assert_red`, `pass_count=`, `info_count=`, `no_criteria_summary` appears, or `SECTION8_OK` is absent. `info_count=` below 3 means Section 8 asserted the real-tree exit code without recording what the tree actually looked like, which is the transcript a post-mortem needs</fails_when>
    <automated>bash -c 'set -u; A="$(./scripts/phase19-link-aware-verify-assert.sh)" && B="$(./scripts/phase19-link-aware-verify-assert.sh)" && printf "%s\n" "$A" | grep -c "^\[PASS\]" > /tmp/p19a.$$ && printf "%s\n" "$B" | grep -c "^\[PASS\]" > /tmp/p19b.$$ && cmp -s /tmp/p19a.$$ /tmp/p19b.$$ || { echo "assert_not_idempotent"; rm -f /tmp/p19a.$$ /tmp/p19b.$$; exit 1; }; rm -f /tmp/p19a.$$ /tmp/p19b.$$; echo IDEMPOTENT_OK'</automated>
    <fails_when>non-zero exit, or `assert_not_idempotent` appears, or `IDEMPOTENT_OK` is absent — two back-to-back runs must produce the same number of proven assertions, which is what proves no fixture survived the first run to change the second</fails_when>
    <human-check>Read the `[INFO]` lines Section 8 emits for the real tree — the summary line and the per-label counts — and read the full `./arch/dots-hyprland.sh verify` output once. Confirm that the `[INFO]` lines describe things you recognise as true about your machine (installer backup files you know about, stubs you have not claimed yet, the Steam links that dangle when Steam is not running) and that nothing on the list surprises you. This is the one judgement the assert deliberately does not make: it checks that the tree is green, not that green is the right answer.</human-check>
  </verify>
  <done>The assert ends with a read-only run against the real tree that asserts only its exit code and records what it saw; the fixture-leak sweep covers every scratch root the script creates; and the criteria summary names all five ROADMAP criteria with none left pending.</done>
</task>

</tasks>

## Artifacts this phase produces

Symbols created by this plan (new — not pre-existing, and not subject to drift verification):

- `scripts/phase19-link-aware-verify-assert.sh`: Section 6 and its `capture/`-package fixture variant; Section 7 and its two auto-backup-branch fixtures; Section 8 and the real-tree read-only run; the completed `=== Phase 19 ROADMAP Criteria Summary ===` block; the script's section-index header comment
- No production symbols — `arch/dots-hyprland.sh` reached its final Phase 19 state in plan `19-03`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scratch fixtures → the real working tree | every section creates a scratch root; a leaked one becomes part of Section 8's answer |
| `vendor/dots-hyprland` → this phase's tooling | the submodule may be de-initialised; neither `verify` nor the assert may depend on it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-07 | Tampering | fixture leakage into the real working tree | high | mitigate | every scratch root removed by an `EXIT` trap set before its first write; the `FIXTURE_LEAK` sweep extended to name every root any section creates; the `git status --porcelain --ignored` bracket compared at both ends |
| T-19-10 | Denial of verification | Section 8 answering a question about the harness instead of about the tree | high | mitigate | Section 8 positioned after every teardown, and the back-to-back idempotency check proves no fixture survived the first run |
| T-19-11 | Denial of verification | a vacuous `--strict` promotion assertion (nothing staged to promote) | medium | mitigate | the findings-only fixture's `FINDINGS=` value is asserted by exact number, so a fixture that produced zero findings fails rather than trivially satisfying the promotion |
| T-19-12 | Tampering | assert reaching into `vendor/dots-hyprland` | medium | mitigate | the installer primitive's effects are reproduced with plain shell commands inside the fixture; a source-level gate asserts zero submodule references, and a de-initialised-submodule run asserts `verify` still exits 0 |
| T-19-06 | Information disclosure | Section 8's `[INFO]` lines naming real `$HOME` paths | low | accept | paths and counts only, never content; the operator is the only reader |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | this phase installs no external packages; `19-RESEARCH.md` §Package Legitimacy Audit records the gate as vacuous |
</threat_model>

<verification>
- `bash -n` clean
- `./scripts/phase19-link-aware-verify-assert.sh` exits 0 with `FAIL=0`, at least 60 `[PASS]` and at least 3 `[INFO]` lines
- two back-to-back assert runs produce the same `[PASS]` count
- `git status --porcelain --ignored` byte-identical before and after, naming no fixture path
- `./arch/dots-hyprland.sh verify` and `verify --strict` both exit 0 on the real tree
- `./arch/dots-hyprland.sh verify` exits 0 with `vendor/dots-hyprland` de-initialised
- the criteria summary block names all five ROADMAP criteria with none pending
</verification>

<success_criteria>
ROADMAP criterion 2 is met: `capture/` drift is reported as its own finding class, distinct from a link failure, proven against a staged package because the live tree cannot exercise it. ROADMAP criterion 3 is met in full: `--strict` promotes findings to a failing exit code on a fixture that exits 0 without it. ROADMAP criterion 5 is checked rather than asserted: `verify` is green on today's tree and the assert records what it saw without encoding it.
</success_criteria>

<output>
Create `.planning/phases/19-link-aware-verify/19-04-SUMMARY.md` when done
</output>
