---
phase: 24-address-tech-debt-bookkeeping-and-validation-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["24-01", "24-02"]
files_modified:
  - scripts/phase24-tech-debt-assert.sh
  - .planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-VALIDATION.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [DEBT-01, DEBT-02, DEBT-03, DEBT-04]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "scripts/phase24-tech-debt-assert.sh exists at 0755 permissions and exercises all Phase 24 deliverables across Sections 1 to 5 (D-15)"
    - "Section 1 verifies DEBT-01: zero stale Pending markers in REQUIREMENTS.md, DEBT-01..04 defined, and summary-extract outputs non-empty requirements_completed for summaries 18-02, 18-03, 23-01, 23-02, 23-03 (D-15)"
    - "Section 2 verifies DEBT-02: all six v0.4 VALIDATION.md files have status: validated, nyquist_compliant: true, and wave_0_complete: true, with manual items and scratch boundary documented (D-15)"
    - "Section 3 verifies DEBT-03: .gitignore:61 scopes *.socket with !stow/systemd/**, git check-ignore validates unit preservation, STATE.md triages ping/.env and 12 gitleaks entries, and non-interactive capture --notify executes cleanly with mocked notify-send (D-11, D-15)"
    - "Section 4 verifies DEBT-04: keybinds.lua passes luac -p, contains hl.unbind(\"SUPER + SHIFT + L\"), adheres 100% to 'Category: Label' taxonomy with 0 duplicate chords, and live hyprctl binds -j confirms live registration of Sleep, Logout, and Lock screen (D-13, D-14, D-15)"
    - "Section 5 runs arch/dots-hyprland.sh verify --strict and asserts FAIL=0 FINDINGS=0 with zero working tree drift across porcelain snapshots (D-15, D-17)"
    - "24-VALIDATION.md is updated to status: validated, wave_0_complete: true, and nyquist_compliant: true"
    - "Milestone audit criteria are satisfied to transition milestone v0.4 to clean status (D-16)"
  artifacts:
    - scripts/phase24-tech-debt-assert.sh
    - .planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-VALIDATION.md
    - .planning/STATE.md
    - .planning/ROADMAP.md
  key_links:
    - "scripts/phase24-tech-debt-assert.sh is the gating test suite proving closure of all v0.4 audit findings"
    - "arch/dots-hyprland.sh verify --strict is the inviolable repository verification invariant"
  prohibitions:
    - "Must not edit closed-phase assert scripts under scripts/ (D-20 frozen history precedent)"
    - "Must not add stow sites under arch/ (PAIR_COUNT == 18 invariant must hold)"
    - "Must not leave working tree dirty or uncommitted changes after test completion"
---

<objective>
Author the comprehensive Phase 24 assertion harness (`scripts/phase24-tech-debt-assert.sh`) covering Sections 1 through 5, validate all Phase 24 requirements (`DEBT-01` through `DEBT-04`), close `24-VALIDATION.md` to `status: validated` and `nyquist_compliant: true`, pass strict verification (`arch/dots-hyprland.sh verify --strict`), and execute the milestone audit verification to achieve clean exit gate status for milestone v0.4.

Purpose: Provide an automated, deterministic test suite locking in all tech debt remediations and proving milestone v0.4 is 100% ready for formal archival.
Output: Executable `scripts/phase24-tech-debt-assert.sh`, updated `24-VALIDATION.md`, updated `STATE.md`, and updated `ROADMAP.md`.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v0.4-MILESTONE-AUDIT.md
@.planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-CONTEXT.md
@.planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-RESEARCH.md
@.planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-PATTERNS.md
@.planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-VALIDATION.md
@scripts/phase23-bootstrap-assert.sh
@scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh
@scripts/phase21-ii-bar-config-capture-assert.sh
@arch/dots-hyprland.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Author dedicated assert harness scripts/phase24-tech-debt-assert.sh covering Sections 1–5</name>
  <files>scripts/phase24-tech-debt-assert.sh</files>
  <read_first>
    - scripts/phase23-bootstrap-assert.sh (lines 1-85, 465-495: harness structure, trap cleanup, porcelain bracket)
    - scripts/phase20-hypr-custom-overlays-and-startup-restore-assert.sh (lines 436-471: Python taxonomy & chord validator)
    - scripts/phase21-ii-bar-config-capture-assert.sh (lines 68-96: scratch capture & mock notify-send)
    - .planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-CONTEXT.md (D-04, D-11, D-15)
    - .planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-PATTERNS.md (Pattern 1.1 through 1.7)
  </read_first>
  <action>
    1. Author `scripts/phase24-tech-debt-assert.sh` with permissions `0755` adhering strictly to the repository test harness contract:
       - Shebang `#!/usr/bin/env bash`, `set -euo pipefail`.
       - Resolve `REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"` and `cd "$REPO_ROOT"`.
       - Implement helpers `pass()`, `fail()`, `info()`, `finding()`, and counters `FAIL=0`, `FINDINGS=0`.
       - Implement `cleanup()` trap registered on `EXIT` for dynamic `TMP_FILES` and `SCRATCH_ROOTS` arrays.
       - Implement CLI flag parsing supporting `--section <1-5>` (default 0 = all sections), `-h|--help`.
       - Implement git porcelain snapshots (`PORCELAIN_BEFORE` and `PORCELAIN_AFTER`) ignoring standard cache paths.
       - Implement **Section 1: Bookkeeping & Traceability (`DEBT-01`)** (Pattern 1.3):
         a. Assert 0 stale `Pending` status markers in `.planning/REQUIREMENTS.md`.
         b. Assert `DEBT-01`, `DEBT-02`, `DEBT-03`, `DEBT-04` are registered in `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`.
         c. Use `node /home/pera/.gemini/gsd-core/bin/gsd-tools.cjs query summary-extract` (with fallback to hermes path) to verify that `requirements_completed` contains:
            - `BOOT-01`, `BOOT-02` in `23-01-SUMMARY.md`
            - `BOOT-03`, `BOOT-05` in `23-02-SUMMARY.md`
            - `BOOT-04` in `23-03-SUMMARY.md`
            - `CAP-05` in `18-02-SUMMARY.md`
            - `CAP-08` in `18-03-SUMMARY.md`
       - Implement **Section 2: Nyquist Validation Compliance (`DEBT-02`)** (Pattern 1.4):
         a. Check `VALIDATION.md` for Phases 17, 18, 20, 21, 22, 23.
         b. Assert `status: validated`, `nyquist_compliant: true`, and `wave_0_complete: true` across all 6 files.
         c. Assert Phase 20 startup apps and Phase 21 desktop notifications are documented as manual inspection items.
         d. Assert Phase 23 documents the scratch-XDG testing boundary.
       - Implement **Section 3: Repository Hygiene & Debt Disposition (`DEBT-03`)** (Pattern 1.5):
         a. Assert `.gitignore` contains `*.socket` AND `!stow/systemd/**`.
         b. Verify via `git check-ignore` that a socket file under `stow/systemd/` is NOT ignored, and `foo.socket` IS ignored.
         c. Assert `STATE.md` documents `ping/.env` as tracked local non-credential configuration and records the 12 gitleaks allowlist entries as accepted historical risk.
         d. Codify non-interactive test of `arch/dots-hyprland.sh capture --notify` in an isolated scratch git repo with mocked `notify-send` logging arguments to a file, asserting that `notify-send` is called with `"Dotfiles Capture"` and `-a Shell -u low` arguments (D-11).
       - Implement **Section 4: Session Keybindings & Cheatsheet Realignment (`DEBT-04`)** (Pattern 1.6):
         a. Check syntax of `stow/hypr/.config/hypr/custom/keybinds.lua` using `luac -p`.
         b. Assert `hl.unbind("SUPER + SHIFT + L")` exists.
         c. Execute embedded Python validator checking that all personal keybindings follow `"Category: Label"` taxonomy and contain zero duplicate key chords.
         d. If `hyprctl` is available, query `hyprctl binds -j` to verify:
            - `SUPER + SHIFT + L` (modmask 65, key L) is purged from sleep.
            - `Scroll_Lock` (modmask 0) is registered as `Session: Lock screen`.
            - `SUPER + Scroll_Lock` (modmask 64) is registered as `Session: Sleep`.
            - `SUPER + SHIFT + Scroll_Lock` (modmask 65) is registered as `Session: Logout`.
       - Implement **Section 5: Strict System Verification & Zero Drift Gate** (Pattern 1.7):
         a. Execute `./arch/dots-hyprland.sh verify --strict` asserting exit 0 (`FAIL=0 FINDINGS=0`).
         b. Execute closing porcelain comparison asserting `PORCELAIN_BEFORE` equals `PORCELAIN_AFTER`.
         c. Output `=== done: FAIL=${FAIL} FINDINGS=${FINDINGS} ===` exiting 1 if `FAIL > 0`, else 0.
    2. Make script executable: `chmod +x scripts/phase24-tech-debt-assert.sh`.
    3. Run `./scripts/phase24-tech-debt-assert.sh` and verify that all sections pass cleanly with `FAIL=0 FINDINGS=0`.
    4. Commit change atomically with message:
       `test(24): add tech debt and validation cleanup assert script` (D-18).
  </action>
  <acceptance_criteria>
    - `scripts/phase24-tech-debt-assert.sh` exists, has permissions 0755, and runs with `bash -n` cleanly.
    - Executing `./scripts/phase24-tech-debt-assert.sh` exits 0 with `FAIL=0 FINDINGS=0`.
    - CLI `--section <1-5>` works for individual sections.
    - Sections 1, 2, 3, 4, and 5 each report `[PASS]` for their respective criteria.
    - Pre- and post-porcelain snapshots match with zero repository changes across the run.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase24-tech-debt-assert.sh</automated>
    <fails_when>non-zero exit code, output containing "[FAIL]", or FAIL > 0</fails_when>
  </verify>
  <done>Dedicated assert harness scripts/phase24-tech-debt-assert.sh created, all sections passing cleanly with FAIL=0 FINDINGS=0.</done>
</task>

<task type="execute">
  <name>Task 2: Update 24-VALIDATION.md, verify strict system integrity, and confirm milestone audit readiness</name>
  <files>.planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-VALIDATION.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-VALIDATION.md (lines 1-82)
    - .planning/v0.4-MILESTONE-AUDIT.md (lines 1-68)
    - .planning/STATE.md (lines 1-38, 168-240)
    - .planning/ROADMAP.md (lines 174-218)
  </read_first>
  <action>
    1. In `24-VALIDATION.md`:
       - Update frontmatter:
         ```yaml
         status: validated
         nyquist_compliant: true
         wave_0_complete: true
         validated: "2026-09-16"
         ```
       - In `## Per-Task Verification Map`, flip all rows from `❌ W0 | ⬜ pending` to `✅ exists | ✅ green`.
       - In `## Wave 0 Requirements`, check off `scripts/phase24-tech-debt-assert.sh`.
       - In `## Validation Sign-Off`, check all boxes and set `Approval: verified`.
    2. Run full system strict verification:
       `./arch/dots-hyprland.sh verify --strict`
       Assert exit code 0 (`FAIL=0 FINDINGS=0`).
    3. Run `git status --porcelain` to confirm zero uncommitted or untracked file drift.
    4. Update `.planning/STATE.md`:
       - Under `## Current Position`: update status to reflect Phase 24 complete, milestone v0.4 ready for closeout.
       - In progress block: update completed plans and percentage.
    5. In `.planning/ROADMAP.md`:
       - In `### Phase 24: Address tech debt: bookkeeping and validation cleanup`:
         Mark all 3 plans `[x]` complete.
       - In `## Progress` table: update Phase 24 to `3/3 plans complete`, status `Complete`.
    6. Run `./scripts/phase24-tech-debt-assert.sh` once more to guarantee full end-to-end green status.
  </action>
  <acceptance_criteria>
    - `24-VALIDATION.md` has `status: validated` and `nyquist_compliant: true`.
    - `./arch/dots-hyprland.sh verify --strict` exits 0 with `FAIL=0 FINDINGS=0`.
    - `git status --porcelain` is empty (except for tracked documentation updates).
    - `./scripts/phase24-tech-debt-assert.sh` exits 0 with `FAIL=0 FINDINGS=0`.
  </acceptance_criteria>
  <verify>
    <automated>./scripts/phase24-tech-debt-assert.sh && ./arch/dots-hyprland.sh verify --strict && grep -q '^nyquist_compliant:\s*true' .planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-VALIDATION.md</automated>
    <fails_when>non-zero exit code, verify --strict failure, or 24-VALIDATION.md missing nyquist_compliant: true</fails_when>
  </verify>
  <done>Phase 24 validation closed, strict verification passed with zero findings, and milestone audit exit gate satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Assert Script (`scripts/phase24-tech-debt-assert.sh`) → Host File System & Processes | Execution of commands, subshells, temporary files in `/tmp`, and IPC queries (`hyprctl`) |
| Strict Verification (`arch/dots-hyprland.sh verify --strict`) → Dotfiles Tree | Recursive inspection of symlink targets and repository content integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-05 | Tampering / File Corruption | Scratch test runner fixtures | high | mitigate | Subshell and mock environments in `scripts/phase24-tech-debt-assert.sh` must operate strictly in isolated temporary directories created via `mktemp -d /tmp/...` and registered in `SCRATCH_ROOTS` for automatic cleanup via `EXIT` trap; never mutate `$HOME` or live repo files during mock assertions. |
| T-24-06 | Denial of Service / Hanging Execution | Mock notification test & IPC calls | medium | mitigate | Shadow `notify-send` via an isolated `PATH` override that writes arguments to a log file and returns 0 immediately, preventing any interactive GUI hang or popup spam during test runs (D-11). Guard `hyprctl` calls with presence checks and fallback defaults. |
| T-24-07 | Repudiation / False Green Audit | Milestone verification gate | high | mitigate | Enforce that `scripts/phase24-tech-debt-assert.sh` checks hard assertions (exit code 1 on any failure) and couples directly with `arch/dots-hyprland.sh verify --strict` and `porcelain_snapshot`, ensuring zero false green states (D-15, D-17). |
</threat_model>

<verification>
Automated test suite and strict verification gate:
1. `./scripts/phase24-tech-debt-assert.sh --section 1`
2. `./scripts/phase24-tech-debt-assert.sh --section 2`
3. `./scripts/phase24-tech-debt-assert.sh --section 3`
4. `./scripts/phase24-tech-debt-assert.sh --section 4`
5. `./scripts/phase24-tech-debt-assert.sh --section 5`
6. `./scripts/phase24-tech-debt-assert.sh && ./arch/dots-hyprland.sh verify --strict`
</verification>

<success_criteria>
1. `scripts/phase24-tech-debt-assert.sh` authored, executable (0755), and passes all 5 sections with `FAIL=0 FINDINGS=0`.
2. `arch/dots-hyprland.sh verify --strict` exits 0 with zero findings and zero drift.
3. `24-VALIDATION.md` closed with `status: validated` and `nyquist_compliant: true`.
4. Working tree is clean and ready for milestone v0.4 archival.
</success_criteria>

<output>
Create `.planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-03-SUMMARY.md` when done
</output>

## Multi-Source Coverage Audit

| Requirement | REQUIREMENTS.md | ROADMAP.md | VALIDATION.md | Plan & Task | Status |
|---|---|---|---|---|---|
| **DEBT-01** | Registered / Mapped | Phase 24 Criterion 1 | Task 24-03-01 (Section 1) | Plan 24-03 Task 1 | Planned (Wave 3) |
| **DEBT-02** | Registered / Mapped | Phase 24 Criterion 2 | Task 24-03-01 (Section 2) | Plan 24-03 Task 1 | Planned (Wave 3) |
| **DEBT-03** | Registered / Mapped | Phase 24 Criterion 3 | Task 24-03-01 (Section 3) | Plan 24-03 Task 1 | Planned (Wave 3) |
| **DEBT-04** | Registered / Mapped | Phase 24 Criterion 4 | Task 24-03-01 (Section 4) | Plan 24-03 Task 1 | Planned (Wave 3) |

## Artifacts this phase produces
- New test harness: `scripts/phase24-tech-debt-assert.sh`
- Closed phase validation contract: `.planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-VALIDATION.md`
- Updated architectural state: `.planning/STATE.md`
- Updated roadmap: `.planning/ROADMAP.md`
- Plan summary: `.planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-03-SUMMARY.md`
