---
phase: 24-address-tech-debt-bookkeeping-and-validation-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stow/hypr/.config/hypr/custom/keybinds.lua
  - .gitignore
  - .planning/STATE.md
autonomous: true
requirements: [DEBT-03, DEBT-04]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "stow/hypr/.config/hypr/custom/keybinds.lua unbinds upstream SUPER + SHIFT + L (upstream sleep) before binding custom keys (D-12)"
    - "SUPER + Scroll_Lock is bound to 'Session: Sleep' via systemctl suspend || loginctl suspend with locked = true (D-12)"
    - "SUPER + SHIFT + Scroll_Lock is bound to 'Session: Logout' via hl.dsp.exit() (D-12)"
    - "Scroll_Lock is retained for 'Session: Lock screen' via hyprlock (D-12)"
    - "All personal keybindings in keybinds.lua pass luac -p syntax checking, strictly adhere to the 'Category: Label' taxonomy, and contain zero duplicate key chords (D-13)"
    - "Live compositor reload (hyprctl reload) registers the new session bindings cleanly in hyprctl binds -j, purging the obsolete SUPER + SHIFT + L chord (D-14)"
    - ".gitignore scopes the *.socket pattern with the un-ignore exception !stow/systemd/** to prevent silent exclusion of systemd socket activation units (D-10)"
    - ".planning/STATE.md formally documents operator triage affirming stow/system_monitor/.config/system_monitor/ping/.env is tracked non-credential configuration and records the 12 gitleaks allowlist entries as accepted historical risk (D-08, D-09)"
  artifacts:
    - stow/hypr/.config/hypr/custom/keybinds.lua
    - .gitignore
    - .planning/STATE.md
  key_links:
    - "stow/hypr/.config/hypr/custom/keybinds.lua is symlinked directly to ~/.config/hypr/custom/keybinds.lua and feeds Quickshell cheatsheet via hyprctl binds -j"
    - ".gitignore un-ignore exception ensures systemd units under stow/systemd/ are tracked while temporary sockets are excluded"
  prohibitions:
    - "Must not edit files under vendor/dots-hyprland/ (permanent invariant)"
    - "Must not touch closed-phase test scripts under scripts/ (D-20 frozen history rule)"
    - "Must not leave duplicate chords in keybinds.lua (violates cheatsheet taxonomy)"
    - "Must not omit locked = true on sleep and lockscreen bindings (threat T-24-01)"
---

<objective>
Realign desktop session keybindings in `stow/hypr/.config/hypr/custom/keybinds.lua` to retire obsolete upstream sleep chords, bind `SUPER + Scroll_Lock` to sleep and `SUPER + SHIFT + Scroll_Lock` to logout, ensure 100% Quickshell cheatsheet (`SUPER + /`) accuracy with strict `"Category: Label"` taxonomy, scope `.gitignore` against accidental systemd socket unit exclusion, and formally record non-credential triage and accepted gitleaks risk in `.planning/STATE.md`.

Purpose: Eliminate user-facing keybinding ambiguity on the desktop, guarantee reliable sleep/logout shortcuts, and resolve repository hygiene debt items identified in the v0.4 milestone audit.
Output: Modified `stow/hypr/.config/hypr/custom/keybinds.lua`, `.gitignore`, and `.planning/STATE.md`.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-CONTEXT.md
@.planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-RESEARCH.md
@.planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-PATTERNS.md
@.planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-VALIDATION.md
@stow/hypr/.config/hypr/custom/keybinds.lua
@vendor/dots-hyprland/dots/.config/hypr/hyprland/keybinds.lua
@.gitignore
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Reallocate session keybindings in keybinds.lua, enforce cheatsheet taxonomy, and verify live compositor binds</name>
  <files>stow/hypr/.config/hypr/custom/keybinds.lua</files>
  <read_first>
    - stow/hypr/.config/hypr/custom/keybinds.lua (lines 1-80: upstream unbinds and session controls)
    - vendor/dots-hyprland/dots/.config/hypr/hyprland/keybinds.lua (lines 334-342: upstream sleep binding)
    - .planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-CONTEXT.md (D-12, D-13, D-14)
    - .planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-PATTERNS.md (Pattern 2.1, 2.2, 2.3)
  </read_first>
  <action>
    1. In `stow/hypr/.config/hypr/custom/keybinds.lua`:
       - In the "Upstream unbinds" block (around line 24), append:
         ```lua
         hl.unbind("SUPER + SHIFT + L") -- upstream sleep (reassigned to SUPER + Scroll_Lock)
         ```
       - In the "Session controls" block (around lines 62-65), replace existing session bindings with:
         ```lua
         -- Session controls (D-12, D-13)
         hl.bind("Scroll_Lock", hl.dsp.exec_cmd("hyprlock"), { description = "Session: Lock screen" })
         hl.bind("SUPER + Scroll_Lock", hl.dsp.exec_cmd("systemctl suspend || loginctl suspend"), { locked = true, description = "Session: Sleep" })
         hl.bind("SUPER + SHIFT + Scroll_Lock", hl.dsp.exit(), { description = "Session: Logout" })
         ```
    2. Syntax validation:
       - Run `luac -p stow/hypr/.config/hypr/custom/keybinds.lua`.
    3. Taxonomy and duplicate chord validation:
       - Run the Python validator (Pattern 1.6 in `24-PATTERNS.md`) to assert that all `description` values strictly adhere to `"Category: Label"` format and that zero duplicate key chords exist across `keybinds.lua`.
    4. Live compositor reload and IPC inspection (D-14):
       - If running inside a live Hyprland session, trigger `hyprctl reload`.
       - Query `hyprctl binds -j` to verify:
         - `SUPER + SHIFT + L` (modmask 65, key L) is no longer bound to sleep.
         - `Scroll_Lock` (modmask 0) is bound to `Session: Lock screen`.
         - `SUPER + Scroll_Lock` (modmask 64) is bound to `Session: Sleep`.
         - `SUPER + SHIFT + Scroll_Lock` (modmask 65) is bound to `Session: Logout`.
    5. Commit change atomically with message:
       `fix(hypr): realign sleep and logout keybinds for cheatsheet accuracy` (D-18).
  </action>
  <acceptance_criteria>
    - `luac -p stow/hypr/.config/hypr/custom/keybinds.lua` exits 0.
    - `grep -qF 'hl.unbind("SUPER + SHIFT + L")' stow/hypr/.config/hypr/custom/keybinds.lua` returns 0.
    - `grep -qF 'hl.bind("SUPER + Scroll_Lock", hl.dsp.exec_cmd("systemctl suspend || loginctl suspend"), { locked = true, description = "Session: Sleep" })' stow/hypr/.config/hypr/custom/keybinds.lua` returns 0.
    - `grep -qF 'hl.bind("SUPER + SHIFT + Scroll_Lock", hl.dsp.exit(), { description = "Session: Logout" })' stow/hypr/.config/hypr/custom/keybinds.lua` returns 0.
    - Python taxonomy inspection confirms 100% adherence to `"Category: Label"` and 0 duplicate chords.
    - `hyprctl binds -j` (if available) confirms `Scroll_Lock` binds for Sleep (modmask 64) and Logout (modmask 65), and absence of `SUPER + SHIFT + L` sleep.
  </acceptance_criteria>
  <verify>
    <automated>luac -p stow/hypr/.config/hypr/custom/keybinds.lua && grep -qF 'hl.unbind("SUPER + SHIFT + L")' stow/hypr/.config/hypr/custom/keybinds.lua && grep -qF 'SUPER + Scroll_Lock' stow/hypr/.config/hypr/custom/keybinds.lua && grep -qF 'SUPER + SHIFT + Scroll_Lock' stow/hypr/.config/hypr/custom/keybinds.lua</automated>
    <fails_when>non-zero exit code or missing required keybinding definitions</fails_when>
  </verify>
  <done>Keybindings in keybinds.lua realigned for lock, sleep, and logout, upstream sleep unbound, taxonomy verified, and live compositor updated.</done>
</task>

<task type="execute">
  <name>Task 2: Scope .gitignore socket pattern and formally document repository hygiene triage in STATE.md</name>
  <files>.gitignore, .planning/STATE.md</files>
  <read_first>
    - .gitignore (lines 50-63: machine-written state rules)
    - .planning/STATE.md (lines 47-65, 168-240: decisions and risk log)
    - .planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-CONTEXT.md (D-08, D-09, D-10)
    - .planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-PATTERNS.md (Pattern 3.1, 6.1, 6.2, 6.3)
  </read_first>
  <action>
    1. In `.gitignore`:
       - Scope the `*.socket` pattern around line 61 by adding the un-ignore exception `!stow/systemd/**`:
         ```gitignore
         *.sock
         *.socket
         !stow/systemd/**
         *.lock
         ```
       - Verify via `git check-ignore -v stow/systemd/.config/systemd/user/dotfiles-capture.socket` that it is NOT ignored (exit non-zero).
       - Verify via `git check-ignore -v foo.socket` that it IS ignored (exit 0).
    2. Commit `.gitignore` change atomically with message:
       `fix(git): scope socket ignore pattern in .gitignore` (D-18).
    3. In `.planning/STATE.md`:
       - Under the `## Decisions` section (or append as Phase 24 decisions per Pattern 6.1, 6.2, 6.3):
         - Record non-credential triage for `ping/.env` (D-08):
           `- [Phase 24]: Triaged tracked stow/system_monitor/.config/system_monitor/ping/.env configuration: verified to contain strictly non-credential local loopback daemon parameters (BIND_HOST=127.0.0.1, PORT=8765, COLLECTION_INTERVAL=5, STALE_AFTER_SECONDS=15) with zero secrets; affirmed as intentionally tracked configuration.`
         - Record gitleaks accepted risk (D-09):
           `- [Phase 24]: Formally recorded the 12 allowlist entries in .gitleaks.toml as accepted historical risk for dead credentials in published pre-v0.3 commits.`
         - Record .gitignore scoping decision (D-10):
           `- [Phase 24]: Scoped .gitignore *.socket pattern with !stow/systemd/** to prevent silent exclusion of systemd socket activation units (D-10).`
         - Record keybindings realignment decision (D-12, D-13, D-14):
           `- [Phase 24]: Realigned desktop session keybindings in custom/keybinds.lua: unbound upstream SUPER + SHIFT + L, bound SUPER + Scroll_Lock to sleep (locked=true), SUPER + SHIFT + Scroll_Lock to logout, retaining Scroll_Lock for lock screen with 100% Quickshell cheatsheet accuracy (D-12, D-13, D-14).`
  </action>
  <acceptance_criteria>
    - `grep -q '^\!stow/systemd/\*\*' .gitignore` returns 0.
    - `git check-ignore stow/systemd/.config/systemd/user/dotfiles-capture.socket` returns non-zero (file is NOT ignored).
    - `git check-ignore foo.socket` returns 0 (generic socket IS ignored).
    - `grep -q 'stow/system_monitor/.config/system_monitor/ping/.env' .planning/STATE.md` returns 0.
    - `grep -q 'gitleaks.*accepted historical risk' .planning/STATE.md` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>! git check-ignore -q stow/systemd/.config/systemd/user/dotfiles-capture.socket && git check-ignore -q foo.socket && grep -q 'stow/system_monitor/.config/system_monitor/ping/.env' .planning/STATE.md && grep -q 'accepted historical risk' .planning/STATE.md</automated>
    <fails_when>non-zero exit code, socket under stow/systemd is ignored, generic socket is not ignored, or STATE.md is missing required triage documentation</fails_when>
  </verify>
  <done>.gitignore scoped with !stow/systemd/** and STATE.md updated with non-credential and gitleaks accepted risk records.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compositor Keybinds → OS Session Controls | Hyprland Lua configuration triggering privileged OS state changes (`systemctl suspend`, `hyprlock`, compositor termination) |
| Git Ignore Rules → VCS Tracked Files | `.gitignore` rules defining which files are tracked or ignored by git commands |
| Repository Files → Secret Exposure | Tracked configuration files containing operational parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Elevation of Privilege / Security Bypass | `keybinds.lua` session bindings | high | mitigate | Ensure `locked = true` is strictly applied ONLY to non-destructive actions (`hyprlock` and `systemctl suspend`) so the lockscreen cannot be bypassed. The logout chord (`hl.dsp.exit()`) must NOT carry `locked = true`, preventing unauthenticated session termination while locked (D-12). |
| T-24-02 | Tampering / Information Loss | `.gitignore` socket rule | medium | mitigate | Add explicit exception `!stow/systemd/**` right after `*.socket` to prevent unintentional omission of systemd socket activation units from version control (D-10). |
| T-24-03 | Information Disclosure | `ping/.env` | low | mitigate | Formally affirm and document in `STATE.md` that `stow/system_monitor/.config/system_monitor/ping/.env` contains strictly non-sensitive localhost loopback networking variables (`127.0.0.1:8765`), not API tokens or passwords (D-08). |
</threat_model>

<verification>
Per-task automated checks:
1. `luac -p stow/hypr/.config/hypr/custom/keybinds.lua`
2. `! git check-ignore -q stow/systemd/.config/systemd/user/dotfiles-capture.socket && git check-ignore -q foo.socket`
3. `grep -q 'stow/system_monitor/.config/system_monitor/ping/.env' .planning/STATE.md`
4. `grep -q 'accepted historical risk' .planning/STATE.md`
</verification>

<success_criteria>
1. `stow/hypr/.config/hypr/custom/keybinds.lua` passes `luac -p`, contains no duplicate chords, unbinds `SUPER + SHIFT + L`, and maps `SUPER + Scroll_Lock` to sleep and `SUPER + SHIFT + Scroll_Lock` to logout.
2. `hyprctl reload` live query `hyprctl binds -j` confirms live compositor registration.
3. `.gitignore` line 61 scoped with `!stow/systemd/**`.
4. `STATE.md` updated with non-credential and gitleaks accepted risk triage.
</success_criteria>

<output>
Create `.planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-01-SUMMARY.md` when done
</output>

## Multi-Source Coverage Audit

| Requirement | REQUIREMENTS.md | ROADMAP.md | VALIDATION.md | Plan & Task | Status |
|---|---|---|---|---|---|
| **DEBT-03** | Registered / Mapped | Phase 24 Criterion 3 | Task 24-01-03 | Plan 24-01 Task 2 | Planned (Wave 1) |
| **DEBT-04** | Registered / Mapped | Phase 24 Criterion 4 | Tasks 24-01-01, 24-01-02 | Plan 24-01 Task 1 | Planned (Wave 1) |

## Artifacts this phase produces
- Modified keybinding configuration: `stow/hypr/.config/hypr/custom/keybinds.lua`
- Modified VCS ignore rules: `.gitignore`
- Updated architectural state register: `.planning/STATE.md`
- Plan summary: `.planning/phases/24-address-tech-debt-bookkeeping-and-validation-cleanup/24-01-SUMMARY.md`
