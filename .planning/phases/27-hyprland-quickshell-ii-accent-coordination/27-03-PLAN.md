---
phase: 27-hyprland-quickshell-ii-accent-coordination
plan: 03
type: execute
wave: 3
depends_on: ["27-01", "27-02"]
files_modified:
  - scripts/phase27-accent-coordination-assert.sh
  - .planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-VALIDATION.md
autonomous: true
requirements: [SHELL-03, INTG-02]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "switchwall.sh exists and is executable at ~/.config/quickshell/ii/scripts/colors/switchwall.sh (D-26)"
    - "Executing switchwall.sh --noswitch runs Matugen synchronously, followed by asynchronous non-blocking background dispatch of terminal and Qt reloads, completing with exit 0 (D-22, D-23, D-24, SHELL-03)"
    - "Quickshell ii renders static wallpapers directly via Config.options.background.wallpaperPath, with mpvpaper automatically spawned for video wallpapers (D-25)"
    - "Session startup loads persisted theme files with zero lag, running switchwall.sh --noswitch only if generated files are missing (D-27)"
    - "Executing switchwall.sh --noswitch advances file modification timestamps (mtime) on both ~/.config/hypr/hyprland/colors.lua and ~/.local/state/quickshell/user/generated/colors.json (AFTER_MTIME > BEFORE_MTIME), resolving filesystem second-granularity race conditions via 1-second clock tick delay (D-32 edge predicate)"
    - "Non-critical subsystem reload errors (Kitty signal when not running, KDE kwin DBus notices) are handled fail-soft without breaking core shell or wallpaper switching (D-23)"
    - "When running under an active Hyprland compositor, Hyprland 0.56 inotify automatically re-evaluates col.active_border matching the updated colors.lua token live without compositor restart or visual stutter (D-21)"
    - "Packaging directories (stow/, restow/, capture/) remain 100% clean in git status --porcelain across wallpaper switching and test runs (D-28, INTG-02)"
    - "arch/dots-hyprland.sh verify --strict passes with FAIL=0 FINDINGS=0 (INTG-02)"
    - "Full suite scripts/phase27-accent-coordination-assert.sh passes all Sections 1 to 5 with FAIL=0 FINDINGS=0 and closing porcelain immutability confirmed (D-28, D-29)"
    - "27-VALIDATION.md is updated with status: validated and nyquist_compliant: true"
  artifacts:
    - scripts/phase27-accent-coordination-assert.sh
    - .planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-VALIDATION.md
  key_links:
    - "switchwall.sh is the master wallpaper switcher coordinating Hyprland, Quickshell, GTK, and Qt theming"
    - "arch/dots-hyprland.sh verify --strict ensures Phase 27 maintains strict data contracts and zero git drift"
  prohibitions:
    - "Must not edit files under vendor/dots-hyprland/ (permanent repository invariant)"
    - "Must not touch closed-phase assert scripts under scripts/ (D-20 frozen history rule)"
    - "Must not invoke hyprctl reload in wallpaper scripts or tests (D-21 inotify reload purity)"
    - "Must not restart Quickshell (pkill qs) on theme changes (D-16 in-process hot reload)"
---

<objective>
Implement and verify Section 4 (Live coordinated reload probe via `switchwall.sh --noswitch` covering D-16, D-21..D-28, D-32, and SHELL-03) and Section 5 (Strict verification engine & zero git drift covering D-28, D-29, and INTG-02) in `scripts/phase27-accent-coordination-assert.sh`, execute the complete 5-section assertion suite alongside `arch/dots-hyprland.sh verify --strict`, and complete validation sign-off in `27-VALIDATION.md`.

Purpose: Guarantee that the dynamic wallpaper reload pipeline executes reliably with high-precision mtime updates, seamless compositor inotify sync, fail-soft error handling, and zero git drift, fulfilling all validation and data contract requirements.
Output: Validated Section 4 and Section 5 in `scripts/phase27-accent-coordination-assert.sh`, passing `verify --strict` watchdog, and signed-off `27-VALIDATION.md`.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-CONTEXT.md
@.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-RESEARCH.md
@.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-PATTERNS.md
@.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-VALIDATION.md
@scripts/phase27-accent-coordination-assert.sh
@arch/dots-hyprland.sh
</context>

<tasks>

<task type="execute">
  <name>Task 1: Implement and verify Section 4 (Live coordinated reload probe via switchwall.sh --noswitch)</name>
  <files>scripts/phase27-accent-coordination-assert.sh</files>
  <read_first>
    - vendor/dots-hyprland/dots/.config/quickshell/ii/scripts/colors/switchwall.sh (lines 306-375: synchronous Matugen generation and background post-processing)
    - .planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-PATTERNS.md (Pattern 4: Coordinated wallpaper reload probe)
    - .planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-CONTEXT.md (D-21..D-28, D-32)
  </read_first>
  <action>
    Implement Section 4 assertions in `scripts/phase27-accent-coordination-assert.sh` and verify execution per D-21..D-28, D-32, and SHELL-03:
    1. Switchwall executable assertion (D-26):
       - Assert `$HOME/.config/quickshell/ii/scripts/colors/switchwall.sh` exists and is executable.
    2. Coordinated reload drill with clock-tick mtime validation (D-32):
       - Record pre-run mtimes: `BEFORE_LUA_MTIME` (`$XDG_CONFIG_HOME/hypr/hyprland/colors.lua`) and `BEFORE_JSON_MTIME` (`$XDG_STATE_HOME/quickshell/user/generated/colors.json`).
       - Execute `sleep 1` to guarantee a clock tick on filesystems with 1-second timestamp resolution.
       - Execute `"$SWITCHWALL" --noswitch` and assert it exits 0 (fail-soft error handling per D-23).
       - Record post-run mtimes: `AFTER_LUA_MTIME` and `AFTER_JSON_MTIME`.
       - Assert strictly monotonic advancement: `AFTER_LUA_MTIME > BEFORE_LUA_MTIME` and `AFTER_JSON_MTIME > BEFORE_JSON_MTIME`.
    3. Inotify live border sync probe (D-21):
       - When `HYPRLAND_INSTANCE_SIGNATURE` is set: insert a brief pause (`sleep 0.2`) to allow compositor inotify processing, extract updated `active_border` hex from `colors.lua`, transpose to `AARRGGBB` format, query `hyprctl -j getoption general:col.active_border`, and assert live compositor border gradient matches `colors.lua` without restart.
    4. Run `bash scripts/phase27-accent-coordination-assert.sh --section 4` and confirm it passes with `FAIL=0`.
  </action>
  <acceptance_criteria>
    - bash scripts/phase27-accent-coordination-assert.sh --section 4 exits 0 with FAIL=0.
    - Both colors.lua and colors.json show advanced mtimes after --noswitch execution.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase27-accent-coordination-assert.sh --section 4</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>Section 4 live coordinated reload probe, mtime advancement checks, and compositor inotify sync verified passing with zero failures.</done>
</task>

<task type="execute">
  <name>Task 2: Implement and verify Section 5 (Strict verification engine & zero git drift) and complete validation sign-off</name>
  <files>scripts/phase27-accent-coordination-assert.sh, .planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-VALIDATION.md</files>
  <read_first>
    - arch/dots-hyprland.sh (lines 1120-1385: run_verify, guard path checks, and live sweep)
    - .planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-PATTERNS.md (Pattern 5: Section 5 implementation and closing porcelain check)
    - .planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-VALIDATION.md (validation sign-off checklist and per-task verification map)
    - .planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-CONTEXT.md (D-28, D-29, INTG-02)
  </read_first>
  <action>
    Implement Section 5 in `scripts/phase27-accent-coordination-assert.sh`, execute full verification suite, and sign off `27-VALIDATION.md` per D-28, D-29, INTG-01, and INTG-02:
    1. Section 5 implementation:
       - Assert packaging directories (`stow/`, `restow/`, `capture/`) have clean git status (`git status --porcelain stow/ restow/ capture/` is empty).
       - Execute `$REPO_ROOT/arch/dots-hyprland.sh verify --strict` and assert it exits 0 with zero findings per INTG-02.
    2. Closing porcelain snapshot assertion (D-28):
       - Compare `$PORCELAIN_BEFORE` against `$PORCELAIN_AFTER` using `cmp -s`, guaranteeing that running the test harness (including the `--noswitch` drill) causes zero git repository drift.
    3. Run individual Section 5: `bash scripts/phase27-accent-coordination-assert.sh --section 5`.
    4. Run full test suite: `bash scripts/phase27-accent-coordination-assert.sh && ./arch/dots-hyprland.sh verify --strict`.
       - Confirm all 5 sections report `[PASS]` with `FAIL=0 FINDINGS=0`.
    5. Update `.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-VALIDATION.md`:
       - Update task statuses in per-task verification map to completed.
       - Check Wave 0 requirements.
       - Set `status: validated` and `nyquist_compliant: true` in frontmatter and sign-off block.
  </action>
  <acceptance_criteria>
    - bash scripts/phase27-accent-coordination-assert.sh --section 5 exits 0 with FAIL=0.
    - bash scripts/phase27-accent-coordination-assert.sh && ./arch/dots-hyprland.sh verify --strict exits 0 with FAIL=0 FINDINGS=0.
    - 27-VALIDATION.md frontmatter contains status: validated and nyquist_compliant: true.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase27-accent-coordination-assert.sh && ./arch/dots-hyprland.sh verify --strict</automated>
    <fails_when>non-zero exit code or "[FAIL]" in assert output or non-zero findings in verify output</fails_when>
  </verify>
  <done>Section 5 strict verification and zero git drift checks verified passing, complete 5-section test harness green, and validation sign-off completed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process Execution (`switchwall.sh`) → Downstream Subsystems | Invoking desktop services, terminal sequences, and theme wrappers |
| Test Harness → Git Repository Working Tree | Executing theme generation drills without contaminating tracked files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-05 | Elevation of Privilege / Command Injection via Wallpaper Path | `switchwall.sh` CLI invocation | high | mitigate | `switchwall.sh` strictly quotes `$imgpath` and validates file existence; test harness verifies wallpaper fixture path before drill execution (D-26). |
| T-27-06 | Repudiation / Git Repository Churn | Test drill & verification engine | medium | mitigate | Porcelain snapshot comparison before and after test execution ensures zero untracked or modified repository files across runs (D-28, INTG-02). |
</threat_model>

<verification>
Per-task automated checks:
1. `bash scripts/phase27-accent-coordination-assert.sh --section 4`
2. `bash scripts/phase27-accent-coordination-assert.sh && ./arch/dots-hyprland.sh verify --strict`
</verification>

<success_criteria>
1. `switchwall.sh --noswitch` runs successfully with exit 0.
2. `colors.lua` and `colors.json` mtimes advance monotonically across reload drill.
3. Compositor inotify updates active border live without restart.
4. `arch/dots-hyprland.sh verify --strict` exits 0 with 0 findings.
5. `scripts/phase27-accent-coordination-assert.sh` passes all 5 sections with FAIL=0.
6. `27-VALIDATION.md` signed off with `nyquist_compliant: true`.
</success_criteria>

<output>
Create `.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-03-SUMMARY.md` when done
</output>

## Multi-Source Coverage Audit

| Requirement | REQUIREMENTS.md | ROADMAP.md | VALIDATION.md | Plan & Task | Status |
|---|---|---|---|---|---|
| **SHELL-03** | Line 25 | Phase 27 Criterion 3 | Task 27-03-01 | Plan 27-03 Task 1 | Planned (Wave 3) |
| **INTG-02** | Line 35 | Phase 29 Criterion 2 | Task 27-03-02 | Plan 27-03 Task 2 | Planned (Wave 3) |

## Artifacts this phase produces
- Test assert harness: `scripts/phase27-accent-coordination-assert.sh` (complete 5-section implementation)
- Signed validation artifact: `.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-VALIDATION.md`
- Plan summary: `.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-03-SUMMARY.md`
