---
phase: 27-hyprland-quickshell-ii-accent-coordination
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - scripts/phase27-accent-coordination-assert.sh
autonomous: true
requirements: [SHELL-01, SHELL-02]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Live ~/.config/hypr/hyprland/colors.lua declares active_border (outline_variant 77%), inactive_border (surface_container_low 33%), background_color (surface.dark FF%), and pinned border rule (primary accent gradient) (D-01..D-03, D-08, SHELL-01)"
    - "Endianness gradient translation converts rgba(RRGGBBAA) from colors.lua to AARRGGBB hex format matching Hyprland IPC gradient representation (SHELL-01)"
    - "When HYPRLAND_INSTANCE_SIGNATURE is set, hyprctl -j getoption general:col.active_border matches expected AARRGGBB gradient from colors.lua; in headless CI environments, harness falls back gracefully to static token validation without failing (D-30, SHELL-01)"
    - "Upstream Hyprland decoration geometry, gaps, dimming, and snapping defaults are affirmed: 18px rounding with 2.5 power squircle, 1px border, 5% inactive dimming, 20% special dimming, gaps_in = 4, gaps_out = 5, gaps_workspaces = 50, resize_on_border = true with 4/5px snapping (D-05..D-07, D-09..D-12)"
    - "Adjacent or touching windows maintain explicit 4px inner gap (gaps_in = 4) and separate cleanly with edge snapping rather than colliding or overlapping (D-10, D-12 edge predicate)"
    - "~/.local/state/quickshell/user/generated/colors.json exists, is non-empty, contains valid JSON syntax, and contains all 8 required M3 palette tokens (primary, secondary, tertiary, surface, error, outline_variant, surface_container_low, background) strictly matching #RRGGBB hex format (D-31, SHELL-02)"
    - "capture/ii/.config/illogical-impulse/config.json configures appearance palette type 'auto', background transparency enable: false (opaque), and resource warning thresholds cpuWarningThreshold: 90, memoryWarningThreshold: 95, swapWarningThreshold: 85 (D-13..D-15, SHELL-02)"
    - "Resource metric rings and top bar indicators switch fill from m3primary to m3error when usage equals or exceeds warning thresholds (CPU >= 90%, RAM >= 95%, Swap >= 85%) (D-14 edge predicate)"
    - "Sections 2 and 3 of scripts/phase27-accent-coordination-assert.sh pass with FAIL=0 (SHELL-01, SHELL-02)"
  artifacts:
    - scripts/phase27-accent-coordination-assert.sh
  key_links:
    - "colors.lua provides the dynamic color bindings re-evaluated by Hyprland 0.56 inotify"
    - "colors.json provides the M3 tokens ingested in-process by Quickshell's MaterialThemeLoader.qml"
  prohibitions:
    - "Must not edit files under vendor/dots-hyprland/ (permanent repository invariant)"
    - "Must not touch closed-phase assert scripts under scripts/ (D-20 frozen history rule)"
    - "Must not compare rgba(RRGGBBAA) directly to hyprctl gradient output without transposing alpha to front (endianness conversion rule)"
    - "Must not fail test execution on headless environments when compositor socket is absent (D-30 headless fallback)"
---

<objective>
Implement and verify Section 2 (Hyprland window borders & compositor token match covering D-01..D-03, D-05..D-12, D-30, and SHELL-01) and Section 3 (Quickshell ii token schema & appearance integrity covering D-13..D-15, D-18..D-20, D-31, and SHELL-02) in `scripts/phase27-accent-coordination-assert.sh`.

Purpose: Ensure Hyprland window decorations, active/inactive borders, and pinned rules dynamically align with Matugen colors and upstream geometry, and verify that Quickshell ii consumes valid Material You tokens and maintains upstream appearance baseline and threshold contracts.
Output: Validated Section 2 and Section 3 in `scripts/phase27-accent-coordination-assert.sh` passing with FAIL=0.
</objective>

<execution_context>
@/home/pera/.gemini/gsd-core/workflows/execute-plan.md
@/home/pera/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-CONTEXT.md
@.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-RESEARCH.md
@.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-PATTERNS.md
@.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-VALIDATION.md
@scripts/phase27-accent-coordination-assert.sh
@capture/ii/.config/illogical-impulse/config.json
</context>

<tasks>

<task type="execute">
  <name>Task 1: Implement and verify Section 2 (Hyprland window borders & compositor token match)</name>
  <files>scripts/phase27-accent-coordination-assert.sh</files>
  <read_first>
    - vendor/dots-hyprland/dots/.config/hypr/hyprland/general.lua (lines 48-106: window decorations, borders, rounding, gaps, dimming, snapping)
    - ~/.config/hypr/hyprland/colors.lua (lines 1-17: active_border, inactive_border, background_color, pinned border rule)
    - .planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-PATTERNS.md (Pattern 1 & Pattern 2: Compositor color translation and Section 2 implementation)
    - .planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-CONTEXT.md (D-01..D-03, D-05..D-12, D-30)
  </read_first>
  <action>
    Implement Section 2 assertions in `scripts/phase27-accent-coordination-assert.sh` and verify execution per D-01..D-03, D-05..D-12, D-30, and SHELL-01:
    1. Parse color tokens from `$XDG_CONFIG_HOME/hypr/hyprland/colors.lua`:
       - Extract raw `active_border` hex (`rgba(RRGGBBAA)`): separate RGB (first 6 chars) and Alpha (last 2 chars) and transpose to expected gradient format `${lua_active_alpha}${lua_active_rgb}` per D-01.
       - Extract raw `inactive_border` hex (`rgba(RRGGBBAA)`): separate RGB and Alpha and transpose to `${lua_inactive_alpha}${lua_inactive_rgb}` per D-02.
       - Assert pinned window rule binds primary accent gradient (`match = { pin = 1 }` and `border_color = "rgba(...) rgba(...)"`) per D-03.
       - Assert canvas `background_color` is declared as `rgba(...)` with FF alpha per D-08.
    2. Upstream decoration geometry, dimming, and snapping assertions:
       - Verify upstream defaults in `vendor/dots-hyprland/dots/.config/hypr/hyprland/general.lua`: 18px rounding with 2.5 power squircle (`rounding = 18`, `rounding_power = 2.5`), 1px border (`border_size = 1`), 5% inactive dimming (`dim_strength = 0.05`), 20% special dimming (`dim_special = 0.2`), 3-pass x-ray blur (`passes = 3`, `xray = true`), `gaps_in = 4`, `gaps_out = 5`, `gaps_workspaces = 50`, `resize_on_border = true` with edge snapping (`window_gap = 4`, `monitor_gap = 5`) per D-05..D-07, D-09..D-12.
       - Verify edge predicate: adjacent or touching windows maintain explicit 4px inner gap and separate cleanly with edge snapping rather than colliding or overlapping.
    3. Live compositor query with headless CI fallback (D-30):
       - Check if `HYPRLAND_INSTANCE_SIGNATURE` is set and `hyprctl` is executable.
       - If active compositor detected: query `hyprctl -j getoption general:col.active_border` and `general:col.inactive_border`, parse the gradient value, and assert case-insensitive equality against expected gradients from `colors.lua` (`${expected_active_gradient,,}` equals `${live_active_gradient,,}`).
       - If headless CI (signature unset or `hyprctl` missing): log `[INFO]` notice and affirm static `colors.lua` structure and token validity without returning failure (D-30).
    4. Run `bash scripts/phase27-accent-coordination-assert.sh --section 2` and confirm it passes with `FAIL=0`.
  </action>
  <acceptance_criteria>
    - bash scripts/phase27-accent-coordination-assert.sh --section 2 exits 0 with FAIL=0.
    - Token translation correctly transposes alpha: e.g. rgba(46474777) evaluates to expected compositor gradient 77464747.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase27-accent-coordination-assert.sh --section 2</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>Section 2 Hyprland border token parsing, gradient translation, upstream decoration parameter verification, and live compositor probe verified passing with zero failures.</done>
</task>

<task type="execute">
  <name>Task 2: Implement and verify Section 3 (Quickshell ii token schema & appearance integrity)</name>
  <files>scripts/phase27-accent-coordination-assert.sh</files>
  <read_first>
    - vendor/dots-hyprland/dots/.config/quickshell/ii/modules/common/Appearance.qml (lines 37-100, 150-199: M3 color properties and bindings)
    - ~/.local/state/quickshell/user/generated/colors.json (lines 1-52: live generated M3 tokens)
    - capture/ii/.config/illogical-impulse/config.json (lines 32-53, 152-158: appearance palette type, transparency, and resource warning thresholds)
    - .planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-CONTEXT.md (D-13..D-15, D-18..D-20, D-31)
  </read_first>
  <action>
    Implement Section 3 assertions in `scripts/phase27-accent-coordination-assert.sh` and verify execution per D-13..D-15, D-18..D-20, D-31, and SHELL-02:
    1. Quickshell generated `colors.json` verification:
       - Assert `$XDG_STATE_HOME/quickshell/user/generated/colors.json` exists and is non-empty.
       - Assert `colors.json` is syntactically valid JSON using `jq empty`.
    2. Material You token schema assertion (D-31):
       - Loop through required M3 tokens: `primary`, `secondary`, `tertiary`, `surface`, `error`, `outline_variant`, `surface_container_low`, `background`.
       - Assert each token exists in `colors.json` and strictly matches the hex color regex `^#[0-9a-fA-F]{6}$`.
    3. Appearance baseline and threshold validation in `config.json` (D-13, D-14, D-15):
       - Check `capture/ii/.config/illogical-impulse/config.json` (with fallback `$XDG_CONFIG_HOME/illogical-impulse/config.json`).
       - Assert `.appearance.palette.type` equals `"auto"` per D-13.
       - Assert `.appearance.transparency.enable` equals `false` (opaque container backgrounds for contrast per D-15).
       - Assert resource warning thresholds: `.resources.cpuWarningThreshold == 90`, `.resources.memoryWarningThreshold == 95`, `.resources.swapWarningThreshold == 85` per D-14.
       - Assert warning threshold edge predicate: resource metric rings and top bar indicators switch fill from `m3primary` to `m3error` when usage equals or exceeds warning thresholds (CPU >= 90%, RAM >= 95%, Swap >= 85%).
       - Validate that wallpaper fixture path `.background.wallpaperPath` points to an existing, readable image file.
    4. Run `bash scripts/phase27-accent-coordination-assert.sh --section 3` and confirm it passes with `FAIL=0`.
    5. Run both Section 2 and Section 3 together.
  </action>
  <acceptance_criteria>
    - bash scripts/phase27-accent-coordination-assert.sh --section 3 exits 0 with FAIL=0.
    - bash scripts/phase27-accent-coordination-assert.sh --section 2 && bash scripts/phase27-accent-coordination-assert.sh --section 3 exits 0 with FAIL=0.
  </acceptance_criteria>
  <verify>
    <automated>bash scripts/phase27-accent-coordination-assert.sh --section 2 && bash scripts/phase27-accent-coordination-assert.sh --section 3</automated>
    <fails_when>non-zero exit code or "[FAIL]" in output</fails_when>
  </verify>
  <done>Section 3 Quickshell token schema validation, hex pattern checking, appearance baseline assertions, and threshold checks verified passing with zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compositor IPC Socket (`$HYPRLAND_INSTANCE_SIGNATURE`) | Communicating with the Wayland compositor via hyprctl to query runtime options |
| Local Filesystem State (`colors.json`) | Parsing generated theme tokens consumed by desktop shell widgets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-03 | Denial of Service / UI Breakage via Corrupted Palette Tokens | `colors.json` & Appearance Singleton | high | mitigate | Validate syntactic validity via `jq empty` and enforce strict regex validation (`^#[0-9a-fA-F]{6}$`) on all required M3 tokens before propagation (D-31). |
| T-27-04 | False Failure in Non-Interactive / Headless Environments | Compositor IPC Query | low | accept | Condition live compositor query on active `$HYPRLAND_INSTANCE_SIGNATURE` presence with clean static validation fallback in headless CI (D-30). |
</threat_model>

<verification>
Per-task automated checks:
1. `bash scripts/phase27-accent-coordination-assert.sh --section 2`
2. `bash scripts/phase27-accent-coordination-assert.sh --section 2 && bash scripts/phase27-accent-coordination-assert.sh --section 3`
</verification>

<success_criteria>
1. `colors.lua` token declarations match live compositor border gradients when compositor is active.
2. `colors.json` contains valid hex colors for all 8 required M3 tokens.
3. `capture/ii/.config/illogical-impulse/config.json` maintains palette type `auto`, opaque background, and warning thresholds 90/95/85.
4. `bash scripts/phase27-accent-coordination-assert.sh --section 2` passes with FAIL=0.
5. `bash scripts/phase27-accent-coordination-assert.sh --section 3` passes with FAIL=0.
</success_criteria>

<output>
Create `.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-02-SUMMARY.md` when done
</output>

## Multi-Source Coverage Audit

| Requirement | REQUIREMENTS.md | ROADMAP.md | VALIDATION.md | Plan & Task | Status |
|---|---|---|---|---|---|
| **SHELL-01** | Line 23 | Phase 27 Criterion 1 | Task 27-02-01 | Plan 27-02 Task 1 | Planned (Wave 2) |
| **SHELL-02** | Line 24 | Phase 27 Criterion 2 | Task 27-02-02 | Plan 27-02 Task 2 | Planned (Wave 2) |

## Artifacts this phase produces
- Test assert harness: `scripts/phase27-accent-coordination-assert.sh` (Sections 2 & 3 implemented)
- Plan summary: `.planning/phases/27-hyprland-quickshell-ii-accent-coordination/27-02-SUMMARY.md`
