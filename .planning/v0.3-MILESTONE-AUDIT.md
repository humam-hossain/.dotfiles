---
milestone: v0.3
audited: 2026-09-06T16:52:08+06:00
status: gaps_found
dispositioned: 2026-09-08
dispositioned_by: "16-retire-the-safe-profile-full-only-wrapper-and-playbook"
disposition_note: "status and scores are the 2026-09-06 measurement and are left as measured. Findings are dispositioned in place, not deleted: each carries a resolution recording what actually closed it."
scores:
  requirements: 20/22
  phases: 6/6
  integration: 18/21
  flows: 1/3
gaps:
  requirements:
    - id: "DOC-03"
      status: "partial"
      phase: "15-playbook-safe-vs-full"
      claimed_by_plans: ["15-01-PLAN.md", "15-02-PLAN.md", "15-03-PLAN.md", "15-04-PLAN.md", "15-05-PLAN.md"]
      completed_by_plans: ["15-01-SUMMARY.md", "15-02-SUMMARY.md", "15-03-SUMMARY.md", "15-04-SUMMARY.md", "15-05-SUMMARY.md"]
      verification_status: "passed"
      disposition: "closed"
      resolution: "Closed by Phase 16, which retired the safe profile rather than branching for it. B-1: arch/dots-hyprland.sh no longer injects --core / --skip-hyprland / --skip-sysupdate on any subcommand (plan 16-01), so the steady-state apply is one flat command, ./arch/dots-hyprland.sh install-files, and it updates the ii hypr/ tree the adopted session depends on (playbook section 10.3 as rewritten by plan 16-04). B-2: closed by retiring the destination, not by documenting a path to it; the session-hook machinery was removed in Phase 16 and no path back to the retired session model exists in any operator document, by design. DOC-03 itself was rewritten to the full-only playbook contract and re-mapped to Phase 16 (plan 16-07)."
      evidence: "Phase 15 VERIFICATION.md marks DOC-03 verified, but cross-phase integration found two blockers in the shipped playbook. B-1: docs/dots-hyprland-workflow.md:476-492 (section 10.3) prescribes plain `install-files` / `install` for every pin-bump and asserts safe defaults still apply; on a machine that took the documented full adopt this injects --skip-hyprland, so the ii hypr/ tree, hyprland.lua and custom/ seeds the live session now depends on are never updated. `--full` appears nowhere in section 10. B-2: the playbook documents no route back from a full adopt to the safe dual-run profile, so the safe re-install flow is only completable on a machine that never adopted — which the playbook does not state."
    - id: "DISP-03"
      status: "partial"
      phase: "11-disposition-matrix"
      claimed_by_plans: ["11-01-PLAN.md", "11-02-PLAN.md", "11-03-PLAN.md", "11-04-PLAN.md"]
      completed_by_plans: ["11-02-SUMMARY.md", "11-03-SUMMARY.md"]
      verification_status: "passed"
      disposition: "closed"
      resolution: "The archive path recorded here is corrected to the location that exists, and is being fixed at its source by plan 16-08, which rewrites 11-DISPOSITIONS.md:201 in place (W-2). The second half of the original finding — that no document explains how to bring the archive back after a full adopt — is closed by retirement rather than by writing that document: Waybar, rofi and swaync were accept-removed at Phase 11 D-11, Phase 16 deleted the machinery that would have made them a supported path (plan 16-01), and the trees stay in the repository as archive only."
      evidence: "Archive intent is satisfied — the Waybar, rofi and swaync trees are committed and recoverable — but 11-DISPOSITIONS.md:201 records the archive path under per-application directories in .config/, which do not exist in the repo. The trees are at stow/waybar, stow/rofi and stow/swaync, which is what the playbook cites."
  integration:
    - id: "B-1"
      severity: "blocker"
      from: "15-playbook-safe-vs-full"
      to: "12-full-profile-flag"
      issue: "Playbook section 10.3 update contract contradicts the Phase 14 full-adopted session model. The documented steady-state pin-bump path injects SAFE_DEFAULTS, which skips exactly the hypr/ tree the adopted session depends on. Affects DOC-03; indirectly FULL-01 (the --full path exists but the playbook's steady-state loop never reaches it)."
      location: "docs/dots-hyprland-workflow.md section 10.3 — the line numbers moved when plan 16-04 rewrote the playbook; the section is now at :402-416"
      disposition: "closed"
      resolution: "Closed by the wrapper flip, not by adding the opt-in flag to section 10.3. Plan 16-01 deleted the SAFE_DEFAULTS array and its injection branch, so no subcommand injects the residual flags and a bare install-files is the full apply. Plan 16-04 rewrote section 10.3 to that one flat command, with no profile probe and no conditional. The flag the recorded remedy would have prescribed survives only as an announced no-op alias (plan 16-02). Evidence: scripts/phase16-retire-assert.sh and scripts/phase12-full-smoke.sh."
    - id: "B-2"
      severity: "blocker"
      from: "14-live-adopt"
      to: "15-playbook-safe-vs-full"
      issue: "No dual-run restore path. After a full adopt, list_hypr_ii_hook_target_files (arch/dots-hyprland.sh:651-665) finds only the repo copy because upstream renamed the live ~/.config/hypr/hyprland.conf to .old, so the safe profile's hook injection has no live target. The waybar/rofi/swaync directories still exist on disk but nothing starts them, and no phase owns instructions for restoring the stow/ chrome trees. Logged unowned at 15-DOC-SWEEP.md:77."
      location: "15-DOC-SWEEP.md:77"
      disposition: "closed"
      resolution: "Closed by retiring the destination — the opposite of the recorded remedy, and deliberately so: the instruction for Phase 16 was to remove the destination rather than to document a path to it. The machinery this issue statement names was removed in Phase 16: list_hypr_ii_hook_target_files and the whole session-hook cluster were deleted by plan 16-01 (D-08), and ii owns the session hooks now through hyprland/env.lua and hyprland/execs.lua. No path back to the retired session model is documented in any operator document, by design. The stow/waybar, stow/rofi and stow/swaync trees stay in the repository untouched as archive, not as a supported path."
    - id: "W-1"
      severity: "warning"
      from: "11-disposition-matrix"
      to: "13-overlay-slots"
      issue: "Stale source of truth. 11-DISPOSITIONS.md:110 still records the env must-keeps (cursor and ILLOGICAL_IMPULSE_VIRTUAL_ENV) as migrate-to-hypr-custom, and section 3 closes with a migrate-to-hypr-custom discipline naming those rows. Phase 13 reversed that in D-08/D-09/D-21 (upstream hyprland/env.lua already sets the venv path; upstream cursor is fine), and 13-SOT-APPLY.md:80 now forbids those tokens in custom/*.lua. Repo env.lua is a 1-byte empty slot. The decision is sound and documented, but Phase 11 is the file Phases 12-14 are told to consume, and the two artifacts read as contradictory. Affects DISP-01, OVL-01."
      location: "11-DISPOSITIONS.md:110"
      disposition: "closed"
      resolution: "Resolved in Phase 16 by plan 16-08, which annotates the stale migrate-to-hypr-custom rows in 11-DISPOSITIONS.md in place with the Phase 13 reversal. Not pre-empted here."
    - id: "W-2"
      severity: "warning"
      from: "11-disposition-matrix"
      to: "15-playbook-safe-vs-full"
      issue: "D-12 archive path drift. 11-DISPOSITIONS.md:201 promises the archive lives under per-application directories in .config/; the Waybar, rofi and swaync trees are actually at stow/waybar, stow/rofi and stow/swaync. Affects DISP-03."
      location: "11-DISPOSITIONS.md:201"
      disposition: "closed"
      resolution: "Resolved in Phase 16 by plan 16-08, which corrects the path in 11-DISPOSITIONS.md in place. Not pre-empted here."
    - id: "W-3"
      severity: "warning"
      from: "13-overlay-slots"
      to: "15-playbook-safe-vs-full"
      issue: "Playbook section 6 duplicates the D-18 apply fence from 13-SOT-APPLY.md. The two copies are currently byte-identical and the playbook labels itself subordinate, but no assert script compares them. This is the exact rot risk phase14-adopt-runbook.md:220 avoided by referencing the source of truth instead of copying it. Affects OVL-03, DOC-04."
      location: "docs/dots-hyprland-workflow.md section 6"
      disposition: "closed"
      resolution: "Resolved in Phase 16 by plan 16-06, which extended scripts/phase13-d19-assert.sh with a fence-drift assert comparing the playbook operator copy against the 13-SOT-APPLY.md D-18 source of truth. The playbook block stays operator-runnable; the assert is what makes the copy checked instead of trusted."
  flows:
    - name: "Flow A — safe re-install"
      status: "retired"
      breaks_at: "n/a — the flow no longer exists"
      detail: "At audit time: broken. Hops 1-3 were verified with real artifacts (playbook -> default install -> residual flags injected, confirmed by install --dry-run printing ./setup install --core --skip-hyprland --skip-sysupdate); hop 4 had no artifact on an adopted machine. See B-2."
      disposition: "retired"
      resolution: "Retired in Phase 16 rather than repaired. The residual-flag injection hops 2 and 3 describe was deleted (plan 16-01), and the machinery hop 4 depended on was removed in Phase 16. The flow leaves the completion count rather than staying in it as broken."
    - name: "Flow C — steady-state pin-bump after full adopt"
      status: "complete"
      breaks_at: null
      detail: "At audit time: broken at the apply hop. See B-1. Operator followed the playbook and silently pinned the live session model to the adopt-day vendor state."
      disposition: "closed"
      resolution: "Closed in Phase 16 by the same change that closed B-1. The apply hop is now ./arch/dots-hyprland.sh install-files, which carries no profile decision and therefore updates the ii hypr/ tree the live session depends on; playbook section 10.3 prescribes that one command and nothing else (plans 16-01, 16-04)."
tech_debt:
  - phase: 14-live-adopt
    items:
      - "W3 open (operator decision): D-38 graphical-session.target autostart was deferred to Phase 15, but Phase 15's ROADMAP success criteria (ROADMAP.md:195-196) are documentation-only. The item has no owning phase. Live state: `systemctl --user is-active graphical-session.target` returns inactive; the unit file survives at stow/systemd/.config/systemd/user/hyprland-session.service (659 B); only its exec-once starter is gone. Screen share still works (`busctl` ScreenCast AvailableSourceTypes = u 7, unchanged from pre-adopt)."
      - "IN-11 — closed by deletion in Phase 16 (plan 16-03). Original entry, kept as the record: scripts/phase14-preflight.sh:263-264 printed `--rotate-backup` as unconditional mandatory remediation; post-adopt that renames away ~/ii-original-dots-backup, which was ADOPT-04 rollback tier-1 source 3. Bounded (rename, not delete). Prose mitigations shipped in docs/dots-hyprland-workflow.md and docs/phase14-adopt-runbook.md, both naming IN-11, but only the docs were fixed and the executable still contradicted them. Resolution: D-33 superseded the plan to fix the message. scripts/phase14-preflight.sh was deleted whole, 327 lines, rather than edited — the adopt it gated ran on 2026-09-04, nothing re-runs it, and running it today would have renamed away a rollback source. ADOPT-04 itself was retired from the requirement set by plan 16-07."
      - "Cosmetic (INFO 2) — moot as of Phase 16 (plan 16-03). Original entry: check_tier1_source was applied to rollback sources 1-2 at scripts/phase14-verify.sh:372-373 while source 3 got presence and non-empty checks at :374-378, with its hash asserted separately in the D-36 block; the asymmetry was stylistic. D-37 deleted both the source-3 probe and the D-36 hash block, so the asymmetry the entry described no longer exists."
      - "Bookkeeping ran ahead of verification (INFO 3): REQUIREMENTS.md marked ADOPT-01..04 complete while ROADMAP.md still showed 1/2 plans done."
      - "Accepted loss: stow/zsh/.config/starship.toml was overwritten through its stow symlink (D-17). Committed in 2539238; recovery ref 96ea7e8:stow/zsh/.config/starship.toml."
      - "Deliberately left open in 14-REVIEW.md: WR-02, WR-04, WR-05, WR-07, WR-10, WR-11, WR-12, WR-13, plus 12 Info findings."
      - "Known D-38 losses accepted: four autostarts, wl-clip-persist, and hyprpaper stopped."
  - phase: 15-playbook-safe-vs-full
    items:
      - "Three deferred fixes were recorded as unowned in 15-DOC-SWEEP.md; two are settled by Phase 16. IN-11, the preflight remediation text, is closed by deletion of the script that carried it (plan 16-03). The missing path back to the retired session model is settled the same way — by retiring the destination (plans 16-01, 16-04), not by writing the path. D-38 (one systemctl --user line or one custom/execs.lua line) is still open and still unowned. WR-02, the three roles of the repo .config/hypr/hyprland.conf, is also still open and unowned: Phase 16 killed only its pre-adopt hook-injection role, leaving two live roles and no statement of which governs."
      - "15-VERIFICATION.md is 30 lines — the thinnest verification report in the milestone. It confirms DOC-03 and DOC-04 verified and states no human verification was required, but the two blockers above were not caught at phase level."
  - phase: 11-disposition-matrix
    items:
      - "No SECURITY.md. Phases 10, 12, 13, 14 each have one."
      - "No UAT.md. Phases 10, 12, 13, 14 each have one, all status complete with pass results."
  - phase: cross-cutting
    items:
      - "ROADMAP.md footer still reads 'Last updated: 2026-08-07 — Phase 10 complete' although Phase 15 completed 2026-09-06. Phase 16 plan 16-09 updates ROADMAP.md, including this footer and the coverage line."
      - "STATE.md records the v0.2 formal milestone audit as skipped at close (per-phase verification passed)."
      - "STATE.md:62 still lists D-38 as open with no owning phase."
nyquist:
  compliant_phases: [10, 11, 12, 13]
  partial_phases: [14]
  not_validated_phases: [15]
  missing_phases: []
  overall: partial
---

# Milestone v0.3 — "Full ii install" Audit

**Status:** gaps_found
**Audited:** 2026-09-06
**Phases in scope:** 10, 11, 12, 13, 14, 15
**Requirements in scope:** 22 at audit time (19 after Phase 16 retired three of them — see Requirements Coverage)

## Summary

Every phase in v0.3 verified `passed` at phase level, and all 22 requirements
clear the three-source cross-reference (REQUIREMENTS.md traceability table,
per-phase VERIFICATION.md requirements tables, and SUMMARY.md
`requirements-completed` frontmatter). The wrapper itself is in good shape: all
four phase assert scripts exit 0 from the repo root, `scripts/phase14-verify.sh`
reports `FAIL=0 FINDINGS=1`, and the one finding is D-38 exactly as the playbook
predicts.

The gaps are not in the code. They are in the documentation contract that
Phase 15 shipped, and they only appear when the phases are traced against each
other rather than individually. Two blockers survive:

1. The playbook's steady-state update procedure (section 10.3) prescribes the
   safe profile for every pin-bump. On a machine that took the documented full
   adopt, the safe profile injects `--skip-hyprland` and therefore never updates
   the ii `hypr/` tree that the live session now depends on.
2. There is no documented route back from a full adopt to the safe dual-run
   profile, so the safe re-install flow cannot complete on an adopted machine.

Both trace to DOC-03. Neither is a code defect — the wrapper does exactly what
Phase 12 designed it to do. The playbook simply does not cover the state the
machine is actually in after Phase 14.

**Disposition, 2026-09-08.** Phase 16 closed both blockers, and neither closed
the way this audit proposed. B-1 was to be closed by adding the opt-in flag to
the playbook's update contract; it closed instead because the flag became
unnecessary — the wrapper stopped injecting the residual flags altogether, so
the plain command the playbook already prescribed became the full apply. B-2 was
to be closed by documenting a path back to the retired session model; it closed
instead because the destination was retired, on the user's explicit instruction.
The findings below are dispositioned in place rather than removed: each keeps its
identifier, severity and original issue statement, and gains a resolution naming
the plan that closed it. Two items stay genuinely open and Phase 16 assigns no
owner to either — see Recommended Closure.

## Requirements Coverage

Three-source cross-reference: 22/22 requirements are `[x]` in REQUIREMENTS.md,
appear in a phase VERIFICATION.md requirements table with status verified, and
are listed in at least one SUMMARY.md `requirements-completed` block. On the
step 5d status matrix alone the milestone scores 22/22 satisfied with zero
orphans.

Integration evidence downgrades two of those to `partial`, giving the reported
score of 20/22.

| Requirement | Phase | 3-source | Integration | Final |
|---|---|---|---|---|
| INV-01 | 10 | satisfied | wired | satisfied |
| INV-02 | 10 | satisfied | wired | satisfied |
| INV-03 | 10 | satisfied | wired | satisfied |
| INV-04 | 10 | satisfied | wired | satisfied |
| DISP-01 | 11 | satisfied | wired (W-1) | satisfied |
| DISP-02 | 11 | satisfied | wired | satisfied |
| DISP-03 | 11 | satisfied | partial (W-2, B-2) | **partial** |
| DISP-04 | 11 | satisfied | wired | satisfied |
| FULL-01 | 12 | satisfied | wired | satisfied |
| FULL-02 | 12 | satisfied | wired | satisfied |
| FULL-03 | 12 | satisfied | wired | satisfied — row retired from the requirement set by Phase 16 (plan 16-07) |
| FULL-04 | 12 | satisfied | wired | satisfied |
| FULL-05 | 12 | satisfied | wired | satisfied — row retired from the requirement set by Phase 16 (plan 16-07) |
| OVL-01 | 13 | satisfied | wired (W-1) | satisfied |
| OVL-02 | 13 | satisfied | wired | satisfied |
| OVL-03 | 13 | satisfied | wired (W-3) | satisfied |
| ADOPT-01 | 14 | satisfied | wired | satisfied |
| ADOPT-02 | 14 | satisfied | wired | satisfied |
| ADOPT-03 | 14 | satisfied | wired (D-38 finding) | satisfied |
| ADOPT-04 | 14 | satisfied | wired (IN-11, closed by deletion in plan 16-03) | satisfied — row retired from the requirement set by Phase 16 (plan 16-07) |
| DOC-03 | 15 | satisfied | **partial (B-1, B-2)** | **partial** |
| DOC-04 | 15 | satisfied | wired (W-3) | satisfied |

No orphans: every REQ-ID in the traceability table has at least one consumer in
a later phase. INV-01..04 are consumed only as documents (by Phase 11), which is
the intended contract for this repo type rather than a gap.

**Amended 2026-09-08 (plan `16-07`).** Phase 16 deleted three of the rows above
from `.planning/REQUIREMENTS.md` — the install-time snapshot prompt, the
post-install package re-marking pass, and the rollback guidance that depended on
both — together with the machinery each described, and rewrote six more
(`INV-04`, `FULL-01`, `FULL-02`, `FULL-04`, `ADOPT-03`, `DOC-03`). The live v0.3
count is **19 mapped, 0 unmapped**, not the 22 measured above. The table is left
as the 2026-09-06 measurement; `.planning/REQUIREMENTS.md` is the current set.

## Phase Verification

| Phase | Name | Status | Blockers | Notes |
|---|---|---|---|---|
| 10 | inventory | passed | 0 | 12/12 checks, `gaps: []`, 4/4 requirements. Non-blocking notes carried to Phase 11. |
| 11 | disposition matrix | passed | 0 | 14/14 checks, `gaps: []`, 4/4 requirements. No SECURITY.md, no UAT.md. |
| 12 | full profile flag | passed | 0 | 15/15 checks, decision coverage 17/17, 5/5 requirements. `verify.key-links` reported 0 because PLAN `from:` fields are component names, not paths; wiring confirmed manually. |
| 13 | overlay slots | passed | 0 | 15/15 checks, decision coverage 15/15, 3/3 requirements. Same prose-path `verify.key-links` artifact. |
| 14 | live adopt | passed | 0 | 4/4, adversarial goal-backward stance. 3 warnings (2 remediated, 1 open) and 3 info findings. Remediated 2026-09-05 in `859e434`. |
| 15 | playbook | passed | 0 | 30-line report, both requirements verified, no human verification required. The two blockers above escaped this pass. |

Phase 14 warning W1 was **closed by retraction**: the record named a runbook
heredoc defect present in no committed revision of
`docs/phase14-adopt-runbook.md` (`grep -cE "<<-?'?SH'?"` returns 0 across all
three revisions). Retractions are written into `14-LIVE-VERIFY.md` section
Findings 4 and `14-02-SUMMARY.md`. Warning W2 was fixed: the runbook now prints
`cd stow && stow -R -v=5 -t ~ kitty`, matching the `-t ~` target form of
`arch/kitty.sh:10`.

## Cross-Phase Integration — 18/21 links wired at audit time; 21/21 after the Phase 16 dispositions

### Wired

- **Phase 10 inventory → Phase 11 dispositions.** All 13 unique HIGH-effect
  paths from `10-INVENTORY.md` appear in `11-DISPOSITIONS.md` with an enum value
  and an inventory-source cell naming a real row. The only non-inventory rows
  (waybar/rofi/swaync, section 6) are explicitly labelled *emerged surface* with
  a stated D-15 provenance.
- **Phase 11 DISP-02 → Phase 12 `--full`, superseded in Phase 16.** At audit time
  the link was an opt-in flag that dropped exactly the three residuals and was
  rejected outside `install` / `install-files`. That selector and its scope check
  were removed in Phase 16 (plans 16-01, 16-02): DISP-02's drop-all-three is now
  unconditional, and `--full` survives only as an announced no-op alias accepted
  on all four install-family subcommands. Evidence re-captured 2026-09-08 from
  `printf '' | ./arch/dots-hyprland.sh install --full --dry-run`, replacing the
  quoted strings the wrapper stopped printing:

  ```
  [CONFIG] --full is accepted but ignored: full is now the only install behavior.
  [INSTALL] ./setup install --skip-backup  (cwd=REPO_ROOT/vendor/dots-hyprland)
  [CONFIG] dry-run: would exec from REPO_ROOT/vendor/dots-hyprland: ./setup install --skip-backup
  ```
- **Phase 12 residual injection survived Phases 13-15 — and was removed in
  Phase 16.** At audit time `install --dry-run` printed `./setup install --core
  --skip-hyprland --skip-sysupdate`; the residual array was still in the wrapper,
  its injection branch was gated on the flag being absent, the install-time
  snapshot prompt fired on both profiles with a bare `--skip-backup` refused
  before it, and a package-marking pass plus a session-hook pass ran after both
  profiles. Plan 16-01 deleted all of it (D-04, D-06, D-07, D-08); the symbol and
  line citations that stood here are dropped rather than carried forward, because
  none of them resolves any more. Evidence re-captured 2026-09-08 from
  `printf '' | ./arch/dots-hyprland.sh install --dry-run`:

  ```
  [INSTALL] ./setup install --skip-backup  (cwd=REPO_ROOT/vendor/dots-hyprland)
  [CONFIG] dry-run: would exec from REPO_ROOT/vendor/dots-hyprland: ./setup install --skip-backup
  ```
- **Phase 13 overlays → vendor Lua require contract → live state.**
  `vendor/dots-hyprland/dots/.config/hypr/hyprland.lua` guards all three
  `require("custom.*")` calls behind `is_file_exists`; all three repo files
  exist. `general.lua` carries the DP-1/HDMI-A-2 `hl.monitor` entries and 11
  `hl.workspace_rule` calls. `diff -rq .config/hypr/custom ~/.config/hypr/custom`
  shows the three named files identical. Live extras (`keybinds.lua`,
  `rules.lua`, `variables.lua`, `scripts/`) are the ii `ignore_existing` seed the
  apply deliberately never touches, documented as expected in playbook section 6.
- **Phase 14 runbook → Phase 13 apply command.** The runbook defers to the
  `13-SOT-APPLY.md` D-18 fence by reference; the playbook's operator copy is
  byte-identical with an explicit "if the two ever disagree, the source of truth
  wins" note. As of Phase 16 that copy is checked rather than trusted:
  `scripts/phase13-d19-assert.sh` compares the two on every run (plan 16-06,
  closing W-3). Line citations dropped — both documents were rewritten in
  Phase 16.
- **Phase 14 rollback guidance — superseded in Phase 16.** The runbook's ban on
  the upstream removal subcommand survives, consistent with the REQUIREMENTS.md
  out-of-scope entry ("Cascade risk; wrapper safe paths only"), and it is the
  only part of this link that does. At audit time the guidance was three
  escalating tiers built on `cp -a` copies plus two wrapper-owned subcommands,
  with `phase14-verify.sh` proving the tier-1 sources sha256-matched a pre-adopt
  fixture. Plan 16-05 replaced the tier list with three plain statements —
  recovery is a clean reinstall from the pinned submodule, the wrapper never
  calls upstream's own removal subcommand, and nothing is preserved on install.
  The package-marking subcommand and the tier-1 hash assertions were removed in
  Phase 16 (plans 16-01, 16-03), and the requirement that promised rollback
  guidance was retired with them (plan 16-07). The pre-adopt snapshot directories
  are still on disk, untouched and unreferenced.

### Broken or stale

| ID | Severity | Link | Issue |
|---|---|---|---|
| B-1 | blocker | 15 → 12 | Playbook section 10.3 update contract skipped the ii `hypr/` tree on an adopted machine. **Closed by Phase 16** — the wrapper injects no residual flags and section 10.3 is one flat `install-files` (plans 16-01, 16-04). |
| B-2 | blocker | 14 → 15 | No path back to the retired session model; the safe re-install could not complete post-adopt. `15-DOC-SWEEP.md:77`. **Closed by Phase 16** — the destination was retired, not re-documented (plans 16-01, 16-04). |
| W-1 | warning | 11 → 13 | Phase 11 env must-keep rows never annotated when Phase 13 reversed them. `11-DISPOSITIONS.md:110` vs `13-SOT-APPLY.md:80`. **Closed by Phase 16** — annotated in place by plan 16-08. |
| W-2 | warning | 11 → 15 | Archive path drift: per-application directories under `.config/` recorded for Waybar, rofi and swaync; `stow/waybar`, `stow/rofi`, `stow/swaync` actual. `11-DISPOSITIONS.md:201`. **Closed by Phase 16** — corrected in place by plan 16-08. |
| W-3 | warning | 13 → 15 | Playbook duplicated the apply fence with no assert comparing the copies. **Closed by Phase 16** — `scripts/phase13-d19-assert.sh` now compares them (plan 16-06). |

## End-to-End Flows — 1/3 complete at audit time; 2/2 after the Phase 16 dispositions, with one flow retired

**Flow A — safe re-install: RETIRED.** At audit time it was BROKEN. Hops 1-3
were verified with real artifacts, and the final hop had no artifact on an
adopted machine: the flow depended on two ii hook lines being injected into a
live `~/.config/hypr/hyprland.conf`, and the wrapper function that located that
target found only the repo copy after upstream renamed the live file to `.old`.
Phase 16 retired the flow instead of repairing it. The residual-flag injection
hops 2 and 3 describe was deleted (plan 16-01), and the hook-injection machinery
the final hop depended on was removed in Phase 16 with it (D-08); ii owns the
session hooks now. Waybar, rofi and swaync were accept-removed at Phase 11 D-11
and their trees stay under `stow/` as archive, not as a destination. The flow
leaves the completion count rather than sitting in it as broken.

**Flow B — full adopt from scratch: COMPLETE, restated.** As recorded, the flow
ran playbook section 3 gate → `10-INVENTORY.md` / `11-DISPOSITIONS.md` →
`scripts/phase14-preflight.sh` → `install --full` → `13-SOT-APPLY.md` apply
fence → `scripts/phase14-verify.sh` (`FAIL=0`) → runbook section 14 rollback
tiers, and every hop had a real, executable or committed artifact. Three of
those hops were removed in Phase 16: `scripts/phase14-preflight.sh` was deleted
whole (plan 16-03, closing IN-11), the `--full` selector became a no-op alias
(plan 16-02), and the tier list was replaced (plan 16-05). Restated for the
wrapper as it now stands, the flow is playbook section 3 gate →
`10-INVENTORY.md` / `11-DISPOSITIONS.md` → `./arch/dots-hyprland.sh install` →
`13-SOT-APPLY.md` apply fence → `scripts/phase14-verify.sh` → the recovery
paragraph in playbook section 9. Every hop still has a real artifact, so the
flow stays COMPLETE — and the IN-11 caveat that qualified it is gone with the
script that carried it.

**Flow C — steady-state pin-bump after a full adopt: COMPLETE.** Broken at the
apply hop at audit time (B-1). The apply hop is now
`./arch/dots-hyprland.sh install-files`, which carries no profile decision and
therefore updates the ii `hypr/` tree the live session depends on; playbook
section 10.3 prescribes that one command and nothing else (plans 16-01, 16-04).
Closed by the same change that closed B-1.

## Nyquist Coverage

| Phase | VALIDATION.md | status | nyquist_compliant | Classification | Action |
|---|---|---|---|---|---|
| 10 | exists | complete (legacy) | true | COMPLIANT | none — 6/6 rows green |
| 11 | exists | validated | true | COMPLIANT | none |
| 12 | exists | validated | true | COMPLIANT | none |
| 13 | exists | validated | true | COMPLIANT | none |
| 14 | exists | validated | false | PARTIAL | `/gsd-validate-phase 14` |
| 15 | exists | draft | false | NOT-VALIDATED | `/gsd-validate-phase 15` |

Overall: partial.

Phase 14 is a genuine PARTIAL — `status: validated` with
`nyquist_compliant: false` and `wave_0_complete: false`, even though all 15 task
rows read COVERED. Phase 15 is a coverage TODO rather than a compliance failure:
its file is `status: draft` with all 20 task rows pending, meaning validate-phase
never reconciled it (#2117). Phase 10 carries the legacy pre-#2117
`status: complete` value with all rows green, so it is treated as COMPLIANT.

## Tech Debt

See the `tech_debt` block in the frontmatter for the full itemisation. The
headline items:

**Unowned, no closing phase (2):** the WR-02 three-roles question, and the D-38
`graphical-session.target` restoration. At audit time this list held three
items, all recorded in `15-DOC-SWEEP.md` with `unowned` in the last column
because Phase 15's documentation-only scope (D-19) forbade editing scripts.
Two of the three left the list in Phase 16. IN-11 is closed by deletion of the
script that carried the stale remediation text (plan `16-03`). The third item,
the missing path back to the retired session model, left the same way: Phase 16
retired the destination on the user's explicit instruction rather than writing
the path (plans `16-01`, `16-04`). WR-02 joins the list from `14-REVIEW.md`,
where it was already open; Phase 16 killed only its third role, the pre-adopt
hook-injection target, leaving two live roles and nothing saying which governs.
Phase 16 assigns an owner to neither remaining item. D-38 is additionally
tracked in `STATE.md` as open with no owning phase.

**Missing artifacts (2):** Phases 11 and 15 have neither SECURITY.md nor
UAT.md. Phases 10, 12, 13, 14 have both. Both phase verifications state no human
verification was required, so the UAT absence is defensible; the SECURITY.md
absence is a process gap.

**Review findings deliberately left open (20):** WR-02, WR-04, WR-05, WR-07,
WR-10 through WR-13, plus 12 Info findings in `14-REVIEW.md`.

**Bookkeeping:** the ROADMAP.md footer still reads "Last updated: 2026-08-07 —
Phase 10 complete" although Phase 15 completed 2026-09-06.

## Recommended Closure

The two blockers were documentation defects with a common owner, and one closure
phase carried all of them: Phase 16. The recommendations recorded on 2026-09-06
are kept below as the record of what was proposed, each followed by what actually
happened. Three of the four were obsolete by the time the phase ran, because the
phase removed the machinery they prescribed.

1. *Proposed:* add a full-profile branch to playbook section 10.3 so an adopted
   machine's pin-bump reaches the opt-in flag, and state which profile a given
   machine is on.
   *Done instead:* the flag was made unnecessary. `arch/dots-hyprland.sh` stopped
   injecting the residual flags on every subcommand (plan `16-01`), so section
   10.3 is one flat `./arch/dots-hyprland.sh install-files` with no profile to
   state (plan `16-04`). Closes B-1.
2. *Proposed:* write the path back to the retired session model — re-establishing
   a live hook target and bringing back the `stow/` trees for Waybar, rofi and
   swaync — and reference it from both the playbook and the runbook.
   *Done instead:* the destination was retired, on the user's explicit
   instruction. The session-hook machinery is deleted (plan `16-01`), no such
   path is documented anywhere, and the `stow/waybar`, `stow/rofi` and
   `stow/swaync` trees stay in the repository as archive only. Closes B-2.
3. *Proposed:* annotate `11-DISPOSITIONS.md:110` with the Phase 13 reversal (W-1)
   and correct the archive path at `11-DISPOSITIONS.md:201` to `stow/` (W-2).
   *Status:* still the right fix, and it is made in place by plan `16-08` in this
   same phase.
4. *Proposed:* optionally add an assert comparing the playbook's apply fence to
   the `13-SOT-APPLY.md` source of truth (W-3), and gate the preflight's
   `--rotate-backup` remediation on the pre-adopt condition (IN-11).
   *Done instead:* the fence assert was added to `scripts/phase13-d19-assert.sh`
   (plan `16-06`), closing W-3. IN-11 was not gated — the script carrying the
   message was deleted whole (plan `16-03`), which closes the entry by deletion.

**Still open — WR-02 and D-38.** Phase 16 assigns an owner to neither, and has
no authority to. WR-02 is the three roles of the repo `.config/hypr/hyprland.conf`
— rollback source, frozen D-36 evidence, and a pre-adopt hook-injection target
that Phase 16 killed, leaving two live roles and nothing saying which governs.
D-38 is the `graphical-session.target` / `hyprland-session.service` autostart
bootstrap lost when the conf was renamed at the Phase 14 adopt;
`scripts/phase14-verify.sh` keeps emitting it as an allowed `[FINDING]`.

Running `/gsd-validate-phase 15` and `/gsd-validate-phase 14` may retroactively
close the Nyquist coverage gaps without a new phase.
